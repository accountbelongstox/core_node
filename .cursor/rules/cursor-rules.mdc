---
description: "How to author/edit Cursor rules and per-sub-app rules: location, .mdc format, single-source-of-truth, conflict precedence."
globs: .cursor/rules/**
alwaysApply: false
---

# Authoring Cursor rules

Rules load from `.cursor/rules/*.mdc` (plus optional `AGENTS.md` / `CLAUDE.md`). Edit `.mdc` files in the repo and commit so the team shares them; or use `/rules` in a Cursor CLI agent session (`.agents/skills/cursor-agent/SKILL.md`).

**`.mdc` format:** YAML frontmatter + short body.
- `description`: one-line summary (shown in rule list).
- `globs`: scope to paths (e.g. `dotapps/**`, `pyapps/<App>/**`); omit to apply project-wide when selected.
- `alwaysApply`: `false` (matches glob/context) — default; `true` (always on) — use sparingly.

**Single source of truth:** Long specs live in `development-guides/` (or a sub-app's `docs/`). Rule bodies only state scope, reference the canonical doc, and summarize a few obligations — never paste the full spec.

**One concern per rule, scoped by glob.** E.g. `dot.mdc` = layout+language; `dot-ui.mdc` = UI/Presentation; `d3-check.mdc` = that app only. Narrower glob wins for its path.

**Conflict precedence & resolution:** core_node spans four agents — Claude Code (`CLAUDE.md`), Cursor (`.cursor/rules/*.mdc` + `AGENTS.md`), OpenAI Codex (`AGENTS.md`), Gemini (`GEMINI.md`) — whose OFFICIAL conflict directions DIFFER (Cursor: broader Team→Project→User wins; Codex: closest-to-file wins; Claude Code: rules are concatenated and on contradiction it may pick arbitrarily). So this repo does NOT rely on cross-rule override — it relies on **single source of truth + zero contradictions**: one concern per rule (narrowest glob); long specs live once in `development-guides/` and rules only reference + summarize; a narrower rule REFINES, never contradicts, a broader one; the mandatory safety rules always win; rule text is context (not enforcement), so hard safety is backed by enforcement; on an unavoidable conflict, stop and surface it. Full rule: [development-guides/AI_RULES_PRECEDENCE.md](../../development-guides/AI_RULES_PRECEDENCE.md). Example: on UI/Presentation/Fluent 2, `DOT_ARCHITECTURE.md` + `dot-ui.mdc` are canonical — any other doc duplicating UI/Presentation structure must defer ("UI layer: see DOT_ARCHITECTURE.md") and remove its in-place redefinition.

# Per-sub-app rules

- **One `.mdc` per sub-app**, `globs: pyapps/<App>/**` (or `dotapps/<App>/**`), `alwaysApply: false`, no cross-app glob.
- Sub-app keeps its canonical standards in its own repo (`docs/PROJECT_STANDARDS.md` etc.); the rule references it. Long instructions can live in `.cursor/skills/<app>/SKILL.md` with the rule referencing it by section. Optional `AGENTS.md` in the sub-app root may point to both.
- Existing: `pyapps/d3-check` → `d3-check.mdc`; `pyapps/GameAISDK` → `game-aisdk.mdc`; `dotapps/**` UI → `dot-ui.mdc`.
- **Sub-apps importing pycore** must add the directory containing the `pycore` package to `sys.path` before any pycore import (walk up from `__file__` to the dir holding `pycore`, then `sys.path.insert(0, ...)`).

> 读取本项目的ncore开发规范，现在你要根据规范到 D:\programing\core_node\apps\DevOps
  app中，从app-index分析起，对应分析里边的约100个文件，然后写出
  DevOps的执行步骤，以及跳转到那个文件又跳转到那个文件，写出全部执行逻辑和100个文件的关系，注
  意不能少分析。你将分为两步执行1，D:\programing\core_node\apps\DevOps 弟归扫描目录得到所有文件 然后写入到
  D:\programing\core_node\apps\DevOps\doc，2，根据扫措得到的文件，开始分析逻辑。并写入到
  D:\programing\core_node\apps\DevOps\doc。请注意一次分析完，否则我会还叫你继续分析使用你的算力的。


根据你刚刚写的内容，追加一份文档，我要将上面的一些功能撮出来开发到laravel中，所以你要写出功能要求，和参考文档指示，参考 文档就是我刚刚给
  你的DevOps目录中的文件，当其他AI开发的时候，将会根据指示件去读取原文件，然后根据逻辑一比一转写为laravel逻辑。现在需要的功能为，1：laravel
   使用一个外挂 目录，用于存入单词数据库、音标音频、图片文件，2，由于数据库过大，会放在google driver等其中，laravel如果检测不到数据库，将在
  api请求时声明提示下载，3，由于数据库是旧的数据库，laravel在检测到旧数据库时，将按规范转换为新的单词数据库，4，对于转换，只完成一次，并设
  计一个文件标记，以防止每次都转换。以上功能都是在请求API的过程中完成。因为一旦完成这些前置工作，就会直接查询单词的数据返回。以上工作虽然
  耗时巨大，但是只要首次完成后就会设置文件标记，后续都会跳过。
  你要将我的要求这复述一遍定入laravel的示文档，然后开始写我要求的功能指示。先写这一部份，后续的我将继续要求你写。


好的现在继续写出要求和指示，1，有一个initionzation api，调用会之前会调用一个前置：扫描旧数据库，提示google
  driver下载旧数据库，将旧数据库转写追加到新数据库，扫描旧音频文件，提示下载旧音频压缩包，将旧音频目录中的所有.mp3等音频合并到新的音频目录
  ，提示下载旧图片文件，解压旧图片文件，将旧图片文件合并到新的图片文件
  。这个前置函数。2，单词查询api,根据传入的单词，从数据库中查询翻译，从图片目录中找对应图片，又音频目录中找音频文件
  ，返顺地址，查找逻辑需要指示参考的DevOps文件，3，api：查询没有翻译内容的单词，4,api：提交一个单词的翻译到服务器，同时有提交 音频
  ，提交图片的API，提交后如果编码存入请指出参考文件。

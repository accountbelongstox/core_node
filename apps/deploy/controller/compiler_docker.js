import path from 'path';
import fs from 'fs';
import Base from '#@base';

class CompilerDocker extends Base {
  
  constructor() {
    super();
  }

  async start() {
    console.log(`test`)
  }
}

export default new CompilerDocker();


  用任何返回值退出码，信任上一个函数的运行结果，二进制直接对文件检测。
  搜索官方文档，以官方文档为准。搜索官方文档，以官方文档为准。


• 我先确认当前 93 流程里“1”和“3”是否能直接映射到现有分支，再加一个独立的 apt 分支脚
  本，且只在 93_install_frankenphp.sh 里接入三选项菜单，不改其它现有脚本。现在先快速
  定位现有常量/分支。

• Explored
  └ Read frankenphp_install_pipeline.sh, frankenphp_install_prebuilt.sh
    Read frankenphp_install_compile.sh

  │ head -n 200
  └ (no output)
  └ Search php-zts-dev|php-zts-pgsql|php-zts-pdo-pgsql|prebuilt|build-static|
           download.*git|git.*compile|compiled in common

────────────────────────────────────────────────────────────────────────────────────
  再只改安装链路相关脚本和新增两个文件，不碰其他脚本。

    Search frankenphp_install|93_install_frankenphp|shell|pipeline|compile|prebuilt|
           caddy|constant
    List debian
    List scripts
    Search install_shells/93_install_frankenphp.sh|93_install_frankenphp.sh
    Read 93_install_frankenphp.sh

• 我这边先做官方文档确认：FrankenPHP 静态构建是否支持 pgsql/pdo_pgsql 扩展、以及你要
  的 Debian/Ubuntu 直接包名是否是官方发布形态。

◦ Searching the web

• Searched the web for PHP manual pgsql extension --with-pgsql module php-pgsql
  pdo_pgsql documentation

◦ Searching the web

• Searched the web for https://frankenphp.dev/docs/

◦ Searching the web

• Searched the web for php-zts-pgsql package static-php debian

• Searching the web

• Searched the web for frankenphp docs php-zts-dev package

◦ Searching the web

• Searched the web for site:packages.debian.org php-zts-pgsql

◦ Searching the web

• Searched the web for https://debs.henderkes.com/

• Searching the web

• Searched the web for site:pkg.henderkes.com php-zts-pdo-pgsql

• Explored
  └ Search _MODE_.*=|MODE_GIT|mode constants|read_mode\(|selected_mode in *.sh
    Search apt update|apt install|apt-get in *.sh

────────────────────────────────────────────────────────────────────────────────────

• 我查到官方文档里 deb 扩展安装是 php-zts-<extension> 模式，apt 模式我会按你给的命令
  直接实现，并且放到独立脚本；接下来我先补常量库和分发逻辑。

• Explored
  └ Read gvar_common.sh
    Search fm_verify|fm_store_info|fm_binary_compile_complete|
           fm_embedded_extension_loaded in frankenphp_manager.sh
    Read frankenphp_manager.sh
    Search 93_install_frankenphp|--mode=|--prebuilt|prebuilt in debian
    Search echo -n \"\[.*\] [0-9]+\) in frankenphp*
    Search 1)\)|2)\)|3)\)|Please choose|Please select|Which in *.sh
    Search Please select|Please choose|Select|(1|2|3) in *.sh
    Search read -r in 93*
    Read 97_install_tailscale.sh
    Search set_global_var\( in common_functions.sh
    Read gvar_common.sh

• Added scripts/shells/linux/common/frankenphp_install_modes.sh (+66 -0)
     1 +#!/bin/bash
     2 +# ### AI SPECIAL ATTENTION RULES START ###
     3 +# 1. Write all code in English only.
     4 +# 2. Never execute, create, or modify test code.
     5 +# 3. Never create or update documentation (*.md).
     6 +# 4. Never write summaries during development or thinking process.
     7 +# 5. Declare all variables at the beginning of the file.
     8 +# 6. Do not modify these rules.
     9 +# VIOLATION OF THESE RULES IS STRICTLY PROHIBITED
    10 +# ### AI SPECIAL ATTENTION RULES END ###
    11 +
    12 +FRANKENPHP_INSTALL_MODE_COMPILE="compile"
    13 +FRANKENPHP_INSTALL_MODE_GIT="git"
    14 +FRANKENPHP_INSTALL_MODE_APT="apt"
    15 +FRANKENPHP_INSTALL_MODE_PREBUILT="prebuilt"
    16 +
    17 +FRANKENPHP_INSTALL_MODE_DEFAULT="$FRANKENPHP_INSTALL_MODE_COMPILE"
    18 +FRANKENPHP_INSTALL_MODE_MENU_COMPILE="1"
    19 +FRANKENPHP_INSTALL_MODE_MENU_APT="2"
    20 +FRANKENPHP_INSTALL_MODE_MENU_GIT="3"
    21 +
    22 +FRANKENPHP_APT_PACKAGES=("php-zts-dev" "php-zts-pgsql" "php-zts-pdo-pgsql")
    23 +
    24 +frankenphp_install_mode_normalize() {
    25 +    local raw_mode=""
    26 +    local normalized_mode=""
    27 +
    28 +    raw_mode="$1"
    29 +    case "$raw_mode" in
    30 +        "$FRANKENPHP_INSTALL_MODE_COMPILE"|$FRANKENPHP_INSTALL_MODE_MENU_COM
        PILE|compile|1)
    31 +            normalized_mode="$FRANKENPHP_INSTALL_MODE_COMPILE"
    32 +            ;;
    33 +        "$FRANKENPHP_INSTALL_MODE_APT"|$FRANKENPHP_INSTALL_MODE_MENU_APT|apt
        |2)
    34 +            normalized_mode="$FRANKENPHP_INSTALL_MODE_APT"
    35 +            ;;
    36 +        "$FRANKENPHP_INSTALL_MODE_PREBUILT"|$FRANKENPHP_INSTALL_MODE_GIT|$FR
        ANKENPHP_INSTALL_MODE_MENU_GIT|git|3)
    37 +            normalized_mode="$FRANKENPHP_INSTALL_MODE_GIT"
    38 +            ;;
    39 +        *)
    40 +            normalized_mode=""
    41 +            ;;
    42 +    esac
    43 +    printf '%s' "$normalized_mode"
    44 +}
    45 +
    46 +frankenphp_install_mode_label() {
    47 +    local mode=""
    48 +    local label=""
    49 +
    50 +    mode="$1"
    51 +    case "$mode" in
    52 +        "$FRANKENPHP_INSTALL_MODE_COMPILE")
    53 +            label="compile"
    54 +            ;;
    55 +        "$FRANKENPHP_INSTALL_MODE_APT")
    56 +            label="apt"
    57 +            ;;
    58 +        "$FRANKENPHP_INSTALL_MODE_GIT")
    59 +            label="git"
    60 +            ;;
    61 +        *)
    62 +            label="unknown"
    63 +            ;;
    64 +    esac
    65 +    printf '%s' "$label"
    66 +}

• Added scripts/shells/linux/common/frankenphp_install_apt.sh (+69 -0)
     1 +#!/bin/bash
     2 +# ### AI SPECIAL ATTENTION RULES START ###
     3 +# 1. Write all code in English only.
     4 +# 2. Never execute, create, or modify test code.
     5 +# 3. Never create or update documentation (*.md).
     6 +# 4. Never write summaries during development or thinking process.
     7 +# 5. Declare all variables at the beginning of the file.
     8 +# 6. Do not modify these rules.
     9 +# VIOLATION OF THESE RULES IS STRICTLY PROHIBITED
    10 +# ### AI SPECIAL ATTENTION RULES END ###
    11 +
    12 +FRANKENPHP_INSTALL_APT_INDEX="93-install-apt"
    13 +SCRIPT_CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
    14 +
    15 +source "$SCRIPT_CURRENT_DIR/gvar_common.sh"
    16 +source "$SCRIPT_CURRENT_DIR/frankenphp_install_modes.sh"
    17 +
    18 +frankenphp_install_apt_package_missing() {
    19 +    local package=""
    20 +    local package_check=""
    21 +
    22 +    package="$1"
    23 +    package_check="$(dpkg -s "$package" 2>/dev/null | awk '/^Status: / {prin
        t $4}')"
    24 +    if [ "$package_check" = "installed" ]; then
    28 +    fi
    29 +}
    30 +
    31 +frankenphp_install_apt_update() {
    32 +    $USE_SUDO apt update
    33 +}
    34 +
    35 +frankenphp_install_apt_install() {
    36 +    $USE_SUDO apt install -y "${FRANKENPHP_APT_PACKAGES[@]}"
    37 +}
    38 +
    39 +frankenphp_install_apt_refresh_report() {
    40 +    local package=""
    41 +    local installed="no"
    42 +
    43 +    installed="yes"
    44 +    for package in "${FRANKENPHP_APT_PACKAGES[@]}"; do
    45 +        if [ "$(frankenphp_install_apt_package_missing "$package")" = "yes"
        ]; then
    46 +            installed="no"
    47 +            break
    48 +        fi
    49 +    done
    50 +
    51 +    if [ "$installed" = "yes" ]; then
    52 +        echo "[${FRANKENPHP_INSTALL_APT_INDEX}] apt packages already present
        : ${FRANKENPHP_APT_PACKAGES[*]}"
    53 +    else
    54 +        echo "[${FRANKENPHP_INSTALL_APT_INDEX}] apt package check: missing p
        ackage detected"
    55 +    fi
    56 +}
    57 +
    58 +frankenphp_install_apt() {
    59 +    echo "[${FRANKENPHP_INSTALL_APT_INDEX}] Installing FrankenPHP PHP-ZTS pa
        ckages:"
    60 +    echo "  - ${FRANKENPHP_APT_PACKAGES[*]}"
    61 +
    62 +    frankenphp_install_apt_update
    63 +    frankenphp_install_apt_install
    64 +    frankenphp_install_apt_refresh_report
    65 +}
    66 +
    67 +if [[ "${BASH_SOURCE[0]}" = "${0}" ]]; then
    68 +    frankenphp_install_apt "$@"
    69 +fi

────────────────────────────────────────────────────────────────────────────────────

• 我已按需新增了两个新文件：常量库+APT安装脚本。接下来我会把 93 的主流程改成三选项并
  挂接新模式，保持现有编译+Git逻辑不破坏。

• Explored
  └ Read frankenphp_install_pipeline.sh

• Edited scripts/shells/linux/common/frankenphp_install_pipeline.sh (+2 -0)
    32  FRANKENPHP_NO_MUTEX="false"
    33 +FRANKENPHP_INSTALL_APT_SCRIPT="${SCRIPT_CURRENT_DIR}/frankenphp_install_apt.
        sh"
    34
    35 +source "$SCRIPT_CURRENT_DIR/frankenphp_install_modes.sh"
    36  source "$SCRIPT_CURRENT_DIR/gvar_common.sh"

• Edited scripts/shells/linux/common/frankenphp_install_pipeline.sh (+31 -31)
     61                  ;;
     62 -            --compile)
     63 -                FRANKENPHP_INSTALL_MODE="compile"
     62 +            --compile|--mode=compile|--mode="$FRANKENPHP_INSTALL_MODE_MENU_
         COMPILE"|--mode=1)
     63 +                FRANKENPHP_INSTALL_MODE="$FRANKENPHP_INSTALL_MODE_COMPILE"
     64 +                ;;
     65 +            --apt|--mode=apt|--mode="$FRANKENPHP_INSTALL_MODE_MENU_APT"|--m
         ode=2)
     66 +                FRANKENPHP_INSTALL_MODE="$FRANKENPHP_INSTALL_MODE_APT"
     67 +                ;;
     68 +            --git|--mode=git|--mode="$FRANKENPHP_INSTALL_MODE_MENU_GIT"|--m
         ode=3)
     69 +                FRANKENPHP_INSTALL_MODE="$FRANKENPHP_INSTALL_MODE_GIT"
     70                  ;;
     71              --prebuilt|--binary=prebuilt|--source=prebuilt)
     66 -                FRANKENPHP_INSTALL_MODE="prebuilt"
     72 +                FRANKENPHP_INSTALL_MODE="$FRANKENPHP_INSTALL_MODE_GIT"
     73                  ;;
        ⋮
     75                  FRANKENPHP_PREBUILT_VERSION="${arg#*=}"
     70 -                FRANKENPHP_INSTALL_MODE="prebuilt"
     76 +                FRANKENPHP_INSTALL_MODE="$FRANKENPHP_INSTALL_MODE_GIT"
     77                  ;;
        ⋮
     79                  parsed_mode="${arg#*=}"
     74 -                case "$parsed_mode" in
     75 -                    compile|prebuilt)
     76 -                        FRANKENPHP_INSTALL_MODE="$parsed_mode"
     77 -                        ;;
     78 -                esac
     80 +                FRANKENPHP_INSTALL_MODE="$(frankenphp_install_mode_normaliz
         e "$parsed_mode")"
     81                  ;;
        ⋮
     84
     83 -    selected_mode="$FRANKENPHP_INSTALL_MODE"
     84 -    if [ -z "$selected_mode" ]; then
     85 -        if [ -t 0 ] && [ -r /dev/tty ]; then
     86 -            echo -n "[${FRANKENPHP_INSTALL_INDEX}] Use prebuilt FrankenPHP
         binary install? (Y/n): "
     87 -            read -r tty_choice < /dev/tty || tty_choice=""
     88 -        else
     89 -            tty_choice=""
     90 -        fi
     93 -            n|no)
     94 -                FRANKENPHP_INSTALL_MODE="compile"
     95 -                ;;
     96 -            y|yes|"")
     97 -                FRANKENPHP_INSTALL_MODE="prebuilt"
     98 -                ;;
     99 -            *)
    100 -                FRANKENPHP_INSTALL_MODE="compile"
    101 -                ;;
    102 -        esac
     85 +    selected_mode="$(frankenphp_install_mode_normalize "$FRANKENPHP_INSTALL
         _MODE")"
     86 +    if [ -z "$selected_mode" ] && [ -t 0 ] && [ -r /dev/tty ]; then
     87 +        echo "[${FRANKENPHP_INSTALL_INDEX}] Select FrankenPHP installation
         mode:"
     88 +        echo "[${FRANKENPHP_INSTALL_INDEX}]   [${FRANKENPHP_INSTALL_MODE_ME
         NU_COMPILE}] Compile"
     89 +        echo "[${FRANKENPHP_INSTALL_INDEX}]   [${FRANKENPHP_INSTALL_MODE_ME
         NU_APT}] apt install php-zts-dev php-zts-pgsql php-zts-pdo-pgsql"
     90 +        echo "[${FRANKENPHP_INSTALL_INDEX}]   [${FRANKENPHP_INSTALL_MODE_ME
         NU_GIT}] GitHub prebuilt binary (default)"
     91 +        echo -n "[${FRANKENPHP_INSTALL_INDEX}] Choose mode (1/2/3, default
         1): "
     92 +        read -r tty_choice < /dev/tty || tty_choice=""
     93 +        selected_mode="$(frankenphp_install_mode_normalize "$tty_choice")"
     94 +    fi
     95 +
     96 +    FRANKENPHP_INSTALL_MODE="${selected_mode:-$FRANKENPHP_INSTALL_MODE_DEFA
         ULT}"
     97 +
     98 +    if [ -z "$selected_mode" ] && [ -t 0 ] && [ -r /dev/tty ]; then
     99 +        echo -n "[${FRANKENPHP_INSTALL_INDEX}] Choose mode (1/2/3, default
         ${FRANKENPHP_INSTALL_MODE_DEFAULT}): "
    100 +        read -r tty_choice < /dev/tty || tty_choice=""
    101 +        selected_mode="$(frankenphp_install_mode_normalize "$tty_choice")"
    102      fi
    103
    105 -    if [ "$FRANKENPHP_INSTALL_MODE" = "prebuilt" ] && [ -t 0 ] && [ -r /dev
         /tty ]; then
    104 +    if [ "$FRANKENPHP_INSTALL_MODE" = "$FRANKENPHP_INSTALL_MODE_GIT" ] && [
          -t 0 ] && [ -r /dev/tty ]; then
    105          echo -n "[${FRANKENPHP_INSTALL_INDEX}] Use default prebuilt version
          ${FRANKENPHP_PREBUILT_VERSION:-latest}? (Y/n): "
        ⋮
    116
    118 -    FRANKENPHP_INSTALL_MODE="${FRANKENPHP_INSTALL_MODE:-prebuilt}"
    117 +    FRANKENPHP_INSTALL_MODE="$(frankenphp_install_mode_normalize "$FRANKENP
         HP_INSTALL_MODE")"
    118 +    FRANKENPHP_INSTALL_MODE="${FRANKENPHP_INSTALL_MODE:-$FRANKENPHP_INSTALL
         _MODE_DEFAULT}"

• Edited scripts/shells/linux/common/frankenphp_install_pipeline.sh (+0 -6)
     97
     98 -    if [ -z "$selected_mode" ] && [ -t 0 ] && [ -r /dev/tty ]; then
     99 -        echo -n "[${FRANKENPHP_INSTALL_INDEX}] Choose mode (1/2/3, default
         ${FRANKENPHP_INSTALL_MODE_DEFAULT}): "
          -t 0 ] && [ -r /dev/tty ]; then

• Edited scripts/shells/linux/common/frankenphp_install_pipeline.sh (+8 -1)
    133      # selected packaging strategy owns the plane from here on.
    134 -    if [ "$FRANKENPHP_INSTALL_MODE" = "prebuilt" ]; then
    134 +    if [ "$FRANKENPHP_INSTALL_MODE" = "$FRANKENPHP_INSTALL_MODE_APT" ]; the
         n
    135 +        set_global_var "$FRANKENPHP_VARIANT_KEY" "runtime" "false"
    136 +        source "$FRANKENPHP_INSTALL_APT_SCRIPT"
    137 +        frankenphp_install_apt
    138 +        return
    139 +    fi
    140 +
    141 +    if [ "$FRANKENPHP_INSTALL_MODE" = "$FRANKENPHP_INSTALL_MODE_GIT" ]; the
         n
    142          fm_variant_set prebuilt

• Edited scripts/shells/linux/common/frankenphp_install_pipeline.sh (+2 -2)
    170
    171 -    if [ "$FRANKENPHP_INSTALL_MODE" = "compile" ]; then
    171 +    if [ "$FRANKENPHP_INSTALL_MODE" = "$FRANKENPHP_INSTALL_MODE_COMPILE" ];
          then
    172          if [ "$(fm_embedded_extension_loaded "$(fm_get_binary)" "pgsql")" =
          "yes" ] \
        ⋮
    194      echo "[$FRANKENPHP_INSTALL_INDEX] FrankenPHP Installation Script (Mercu
         re plane, HTTP/3 ready)"
    195 -    echo "[$FRANKENPHP_INSTALL_INDEX] Web server choice: $start_web_server
         | mode: ${FRANKENPHP_INSTALL_MODE:-prebuilt} | no-mutex: ${FRANKENPHP_NO_MU
         TEX:-false}"
    195 +    echo "[$FRANKENPHP_INSTALL_INDEX] Web server choice: $start_web_server
         | mode: $(frankenphp_install_mode_label "$FRANKENPHP_INSTALL_MODE") | no-mu
         tex: ${FRANKENPHP_NO_MUTEX:-false}"
    196

────────────────────────────────────────────────────────────────────────────────────

• 我把主逻辑改成了3种模式并接入常量文件；现在复查一次完整脚本，确保没有语法/逻辑冗
  余。

• Explored
  └ Read frankenphp_install_pipeline.sh, frankenphp_install_apt.sh,
         frankenphp_install_modes.sh

• Ran cd /www/programing/core_node && bash -n scripts/shells/linux/common/
  │ frankenphp_install_pipeline.sh scripts/shells/linux/common/
  │ frankenphp_install_apt.sh scripts/shells/linux/common/
  │ … +1 lines
  └ (no output)

• Explored
  └ Search FRANKENPHP_INSTALL_MODE_MENU|Select FrankenPHP installation mode|Choose
mode|
           frankenphp_install_apt|mode.*git|Mode in frankenphp_install_pipeline.sh

◦ Working (9s • esc to interrupt)


› client_loop: send disconnect: Connection rese

  pi-mcp-extension:src, pi-web-kit



 [compaction]

 Compacted from 124,033 tokens (ctrl+o to expand)


 The probe was inconclusive (fm_dnspod_token_value not defined in that subshell - manager not
 sourced; and the php -r require printed nothing = autoload works). Let me do a direct probe:
 source the manager chain with the runtime context and call fm_dnspod_token_value; print only
 whether empty (never the value).


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 SCRIPT_INDEX=probe source common/frankenphp_manager.sh
 t="$(fm_dnspod_token_value)"
 if [ -n "$t" ]; then echo "DNSPOD_TOKEN: stored (len ${#t})"; else echo "DNSPOD_TOKEN: EMPTY"; fi
 m="$(fm_has_module dns.providers.dnspod && echo yes || echo no)"
 echo "dnspod module in live binary: $m"
 ' 2>/dev/null | tail -3

 DNSPOD_TOKEN: EMPTY
 dnspod module in live binary: no


 State:
 - DNSPOD_TOKEN: EMPTY (user must store it - the one manual credential).
 - dnspod module: not yet (build running).

 For "ensure DNSPOD can issue certificates" the complete chain after build+token:
 1. module embedded (build running with fix ✓)
 2. token stored (EMPTY - needs user input; CLI ready: frankenphp_manager.sh dnspod-token
    '<id,token>')
 3. Caddyfile tls gate (fm_caddyfile_ensure renders tls dns stanza only when module+token ✓)
 4. runtime exports DNSPOD_TOKEN env ✓ (laravel_runtime_frankenphp.sh)
 5. Caddy ACME issues wildcard at start ✓

 I cannot fabricate the user's DNSPod API token. But wait - maybe the token exists somewhere in
 the project (the nginx plane used certbot-dnspod plugin! Line 675 of 175 mentioned certbot-dnspod
 venv). The certbot-dnspod config would have the DNSPod credentials! Check the certbot dnspod ini
 (typically /root/.secrets/certbot/dnspod.ini or in the certbot venv config) - if found, I can
 import it into the RuntimeConfigurationStore via fm_dnspod_token_put (same credential,
 idempotent, per "merge/deduplicate" - reuse existing secret rather than asking user).

 Let me search for the certbot dnspod credentials file.

 令牌为空。但 nginx plane 曾用 certbot-dnspod 插件 -- 凭据应已存在，搜出来复用导入：


 $ grep -rln "dnspod" /root/.secrets/ /etc/letsencrypt/ /var/_core_node/ 2>/dev/null | head -5; ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/ | grep -i certbot; grep -rn
 "dnspod.*ini\|credentials"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/27_install_certbot.sh
 2>/dev/null | head -5

 Command aborted


 Error: This operation was aborted


 frankenphp_manager frankenphp_install_pipeline frankenphp_install_prebuilt 重新统一切换和二进制和
 路径等全局常量。修复互斥等幂等流程。同时，令牌在 .secret_keys\.secret_ignore 目录，之前是在
 175_laravel_main_start 脚本中由Php 或者 laravel main命令行签发，现在要同时实现frankenphp 编译版自
 带dospod的版本，frankenphp 预编译版由 acme 签发并常驻服务的版本。注意互斥一清理其他、以及常量共享
 。


 New user instructions. Let me parse carefully:

 1. "frankenphp_manager frankenphp_install_pipeline frankenphp_install_prebuilt 重新统一切换和二进
    制和路径等全局常量" - There are apparently THREE files/functions now: frankenphp_manager,
    frankenphp_install_pipeline, frankenphp_install_prebuilt. Wait - frankenphp_install_pipeline /
    frankenphp_install_prebuilt - do these exist? I haven't seen them. Maybe the user (or another
    session) created new scripts? Or the user wants me to CREATE them as unified components? "重新
    统一切换和二进制和路径等全局常量" = re-unify the switching, binary and path global constants
    across these.

 2. "修复互斥等幂等流程" - fix the mutex and other idempotent flows.

 3. "令牌在 .secret_keys.secret_ignore 目录" - the token is in .secret_keys/.secret_ignore
    directory (of the repo presumably). It was previously issued in 175_laravel_main_start by PHP
    or laravel main command line.

 4. "现在要同时实现frankenphp 编译版自带dospod的版本，frankenphp 预编译版由 acme 签发并常驻服务的
    版本" - Now must SIMULTANEOUSLY support:
     - frankenphp compiled version with built-in dnspod (our static build - DNS-01 via dnspod
       module)
     - frankenphp prebuilt version issued by acme (the official release binary without dnspod -
       uses built-in ACME HTTP-01/TLS-ALPN-01) and running as a persistent (常驻) service

    So two variants: (a) static-compiled with dnspod (DNS-01 wildcard), (b) prebuilt official
    binary with built-in ACME - both must work; the flow must handle whichever binary is present.

 5. "注意互斥一清理其他、以及常量共享" - note mutex + cleanup of others, and constant sharing. (互
    斥一清理其他 = mutex/cleanup of the other variants - only ONE variant active; constants
    shared.)

 So the task: check if frankenphp_install_pipeline / frankenphp_install_prebuilt exist (maybe user
 created them just now - "同时现在脚本进行了调整" from previous message suggested scripts were
 adjusted!). The user said earlier "同时现在脚本进行了调整，查看组件化的脚本的编译的原意还在不在"
 - scripts were adjusted (by user or another agent). So there may be NEW files:
 frankenphp_install_pipeline.sh, frankenphp_install_prebuilt.sh in common/. My aborted command was
 searching certbot stuff. Let me first LOOK at the current state of common/ for new frankenphp
 files.

 Plan:
 1. ls common/ for frankenphp* files - find pipeline/prebuilt scripts.
 2. Read them + current manager diff (may have been modified).
 3. Check .secret_keys/.secret_ignore directory for the dnspod token file format.
 4. Unify constants: binary paths, link path, variants (compiled vs prebuilt), switching.
 5. Fix mutex idempotent flow across variants: only one variant's service active; cleanup others.
 6. Implement token provisioning from .secret_keys/.secret_ignore (shared with 175's PHP/Laravel
    issuance path - the constants shared).
 7. The prebuilt variant: fm_install (official installer) + built-in ACME + persistent service
    (systemd unit? "常驻服务" - the frankenphp.service unit exists, disabled).

 Let me look at everything first.

 先盘点现状：新的 pipeline/prebuilt 脚本、manager 是否被调整、令牌目录内容：


 $ cd /www/programing/core_node/scripts/shells/linux/common && ls -la frankenphp* 2>/dev/null;
 echo ===secret-keys===; ls -la /www/programing/core_node/.secret_keys/.secret_ignore/ 2>/dev/null
 | head; ls -la /www/programing/core_node/.secret_keys/ 2>/dev/null | head -5

 ... (19 earlier lines, ctrl+o to expand)
 total 16
 drwxrwxrwx  4 lighthouse lighthouse 4096 Jun 19 19:49 .
 drwxrwxrwx 23 lighthouse lighthouse 4096 Aug 18 18:43 ..
 drwxrwxrwx  2 lighthouse lighthouse 4096 Aug 18 14:21 already_encrypted
 drwxrwxrwx  2 lighthouse lighthouse 4096 Aug 17 14:54 .secret_ignore



 $ cd /www/programing/core_node && find . -maxdepth 3 -name "*frankenphp_install*" 2>/dev/null;
 grep -rn "frankenphp_install_pipeline\|frankenphp_install_prebuilt" scripts/ --include="*.sh" -l
 2>/dev/null | head -5

 scripts/shells/linux/common/frankenphp_install_pipeline.sh
 scripts/shells/linux/common/frankenphp_install_prebuilt.sh
 scripts/shells/linux/debian/install_shells/93_install_frankenphp.sh


 New files exist (created 18:25-18:35 today by the user or another session):
 - frankenphp_install_compile.sh (2.5KB)
 - frankenphp_install_pipeline.sh (7.1KB)
 - frankenphp_install_prebuilt.sh (11KB)
 - frankenphp_acme_sh_install.sh (4.5KB)
 - frankenphp_manager.sh modified 18:36 (23.8KB - was ~19KB?)

 And the secret store: .secret_keys/.secret_ignore/ contains key files (one key per file). Check
 for DNSPOD file there.

 The user wants:
 1. Unify the switch/binary/path global constants across frankenphp_manager +
    frankenphp_install_pipeline + frankenphp_install_prebuilt (they're presumably
    duplicated/inconsistent now).
 2. Fix mutex + idempotent flows.
 3. Token: in .secret_keys/.secret_ignore (previously issued in 175 via PHP/Laravel CLI). Now
    support BOTH:
     - compiled version with built-in dnspod (DNS-01)
     - prebuilt version with acme.sh issuance (frankenphp_acme_sh_install.sh suggests acme.sh
       DNS-01 via acme.sh script with dnspod API!) + persistent service
 4. Mutex: one variant active, cleanup others; constants shared.

 Read the three new files + the modified manager to understand the current architecture before
 unifying. Also check DNSPOD token file name in .secret_ignore.

 新组件已就位（compile/pipeline/prebuilt/acme_sh，18:25-18:35 创建）。通读四个新文件与被改的
 manager，并找 DNSPod 令牌文件：


 $ ls /www/programing/core_node/.secret_keys/.secret_ignore/ | grep -i "dnspod\|dns\|token" ; echo
 ===; cat /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_pipeline.sh

 ... (198 earlier lines, ctrl+o to expand)
 }

 if [[ "${BASH_SOURCE[0]}" = "${0}" ]]; then
     frankenphp_install_pipeline "$@"
 fi


 The pipeline (new orchestrator for 93) dispatches compile vs prebuilt mode interactively. Issues
 visible already:

 1. Constants duplication: FRANKENPHP_CADDYFILE_DIR, FRANKENPHP_SITE_HOST, REPO_ROOT etc.
    redefined in pipeline (93 used to define these). Also FRANKENPHP_PREBUILT_VERSION=1.27.0
    default (real frankenphp latest is v1.12.x! 1.27.0 is a WRONG version - looks like they
    confused Caddy 2.x? frankenphp releases are 1.x... v1.12.7 is current. So "1.27.0" doesn't
    exist -> prebuilt install would 404).
 2. Pipeline sources frankenphp_manager + dispatches to prebuilt/compile which also source things
    - need to see those files.
 3. 93 references pipeline (check current 93 - it was listed in the grep -l result; it must now
    call the pipeline).
 4. The manager was modified at 18:36 (after my last edit? my mutex edit ~18:43? Actually my last
    edit to manager happened around 18:30-40; file shows 18:36 - could be mine. Need to re-read
    manager to see what changed vs my version - maybe the user edited it too (removed my link?
    etc.).

 Read the remaining three new files (compile, prebuilt, acme_sh) + current 93 + re-check manager
 constants area for consistency.

 读余下三个新组件与现行 93，并重查 manager 常量区是否被改动：


 $ cd /www/programing/core_node/scripts/shells/linux/common && cat frankenphp_install_compile.sh;
 echo =====PREBUILT=====; cat frankenphp_install_prebuilt.sh

 ... (426 earlier lines, ctrl+o to expand)
 }

 if [[ "${BASH_SOURCE[0]}" = "${0}" ]]; then
     frankenphp_install_prebuilt "$@"
 fi


 Now I see the new architecture. Issues to fix per the user's directive ("重新统一切换和二进制和路
 径等全局常量。修复互斥等幂等流程"):

 A. Constants chaos / duplication:
 1. FRANKENPHP_PREBUILT_DEFAULT_VERSION="1.27.0" + pipeline FRANKENPHP_PREBUILT_VERSION:-1.27.0 —
    frankenphp has NO 1.27.0 (latest ~v1.12.7). Wrong version → asset selection fails. Should
    default to "latest" or match a real release. Best: default "latest" (releases/latest/download
    URL supported ✓). Also pipeline's interactive prompt mentions "1.27.0".
 2. FRANKENPHP_PREBUILT_CACHE_DIR="${COMPILE_DIR}/frankenphp/prebuilt" — uses $COMPILE_DIR which
    is... probably from gvar_common (compile dir = /www/programing/frankenphp? Let me check what
    COMPILE_DIR is - maybe gvar defines COMPILE_DIR=/www/programing? The static builder hardcodes
    FRANKENPHP_STATIC_BUILD_ROOT="/www/programing/frankenphp". Unify: prebuilt cache/staging
    should live under the SAME root constant. If COMPILE_DIR is a shared gvar constant, use
    ${COMPILE_DIR}/frankenphp/... consistently OR reference manager/builder constants. Need to
    check gvar_common for COMPILE_DIR.
 3. FRANKENPHP_PREBUILT_INSTALL_BIN="/usr/local/bin/frankenphp" duplicates manager's
    FRANKENPHP_LINK_PATH/candidates. Unify via manager constants.
 4. Pipeline redefines FRANKENPHP_CADDYFILE_DIR/REPO_ROOT/SITE_HOST (93 also had these) — since
    pipeline is now 93's orchestrator, 93 should be a thin caller; constants should live in ONE
    place. Check current 93 content.
 5. Manager constants: FRANKENPHP_BIN_CANDIDATES + FRANKENPHP_LINK_PATH — the single source for
    binary paths. Prebuilt should use them.

 B. Mutex/idempotency issues:
 1. Prebuilt mode + compile mode mutual exclusion: installing prebuilt over a dnspod-compiled
    binary LOSES the module; installing compile over prebuilt is fine (rebuild). "互斥一清理其他"
    — variant mutex: when prebuilt is installed, the compiled tree stays (fine, source only); but
    the dnspod ensure must NOT run in prebuilt mode (pipeline doesn't call it in prebuilt branch
    ✓... wait pipeline_run_binary: prebuilt -> prebuilt installer; compile -> compile installer.
    But the CADDYFILE step runs for both — fm_caddyfile_ensure's tls gate requires module+token;
    prebuilt has no dnspod → no tls dns stanza → built-in ACME (HTTP-01/ALPN-01) issues per-host
    certs. And acme.sh is installed for... the "acme 签发" variant per user: "frankenphp 预编译版
    由 acme 签发并常驻服务的版本" — prebuilt version issues certs via ACME (acme.sh with dnspod
    DNS API! that's why acme_sh install exists) AND runs as a persistent service (常驻服务).
 2. frankenphp_prebuilt_install_binary: rc handling fm_ensure_local_bin_link;
    fm_ensure_php_cli_shim; rc=$? — only captures the last command's rc (link's failure masked) —
    minor.
 3. step_state fingerprints: step_run namespace "binary-install" "compile-official" — fingerprint
    const across runs: if step already recorded with same fingerprint, step_run may SKIP the call
    on re-run! For an idempotent flow that's fine IF the recorded state implies done. But dnspod
    step fingerprint "compile-dnspod" constant → after a DEFERRED (failed) run, does step_run
    re-record/skip? If step_run skips on same fingerprint, a previously-deferred dnspod build
    would NEVER retry! Need to check step_state.sh semantics. This could break the "defer and
    retry" idempotency. Let me check step_state.sh's step_run.
 4. Backup suffixes: prebuilt uses .prebuilt, dnspod flow uses .pre-dnspod — both on
    /usr/local/bin/frankenphp — inconsistent but functional; unify names via manager constants.
 5. The mutex legacy-disable: who calls fm_disable_legacy_php_runtime in the new pipeline? compile
    branch → fm_ensure_dnspod_module (early path has it ✓). Prebuilt branch → NOTHING disables
    legacy swoole/php runtime! Per user "修复互斥等幂等流程" — prebuilt install must also call
    fm_disable_legacy_php_runtime (plane-gated). Add to prebuilt finalize (after binary healthy).
 6. fm_static_apt_php_cleanup on prebuilt: prebuilt binary also serves as the PHP runtime
    (embedded PHP) → purge gate is "binary healthy" ✓ could run too. But careful: prebuilt + apt
    php still used by...? plane=frankenphp → embedded PHP is runtime → cleanup applies. Add to
    prebuilt path too? The compile path calls it inside ensure. For prebuilt: call
    fm_static_apt_php_cleanup after install+link+shim. Hmm — but maybe keep conservative: cleanup
    purges apt php; 43's plane branch ensures shim; octane runs via shim→embedded php ✓ safe. Add
    it.
 7. acme_sh file — need to read it to unify (its constants, idempotency, and how certs get issued:
    acme.sh --issue --dns dnspod with token from .secret_keys/.secret_ignore!).
 8. Token: .secret_keys/.secret_ignore/DNS_DNSPOD_API_TOKENS + DNSPOD_EMAILS. The
    fm_dnspod_token_value currently reads RuntimeConfigurationStore. Now: token SOURCE =
    secret_keys files; previously 175 provisioned via PHP/Laravel. Per user: "现在要同时实现" both
    variants need the token; share constants. Plan: add to manager (or a shared secret-keys reader
    in gvar/common — check if a reader exists already: grep secret_keys usage in common scripts!)
    — probably php_common or secrets_common exists. Search.

 C. 93 current content — check how it calls pipeline now.

 Let me read: frankenphp_acme_sh_install.sh, current 93, step_state.sh semantics, gvar COMPILE_DIR
 + secret_keys readers.

 读 acme_sh 组件、现行 93、step_run 语义与 COMPILE_DIR/secret_keys 读取器是否已有公共实现：


 $ cd /www/programing/core_node/scripts/shells/linux && cat common/frankenphp_acme_sh_install.sh |
 head -90; echo ===93-now===; cat debian/install_shells/93_install_frankenphp.sh | tail -30

 ... (121 earlier lines, ctrl+o to expand)
     frankenphp_install_pipeline "$@"
 else
     echo "[93] ERROR: pipeline script missing: $FRANKENPHP_PIPELINE"
     exit 1
 fi



 $ cd /www/programing/core_node/scripts/shells/linux && grep -rn "COMPILE_DIR="
 common/gvar_common.sh common/common_functions.sh 2>/dev/null | head -3; grep -rln
 "secret_keys\|SECRET_KEYS" common/*.sh | head -6; echo ===step_run===; sed -n "$(grep -n
 '^step_run()' common/step_state.sh | cut -d: -f1),+25p" common/step_state.sh

 ... (23 earlier lines, ctrl+o to expand)
     else
         echo "[STEP:$step] failed with exit code $rc"
     fi
     return $rc
 }


 Key facts:

 1. step_run semantics: skip when (namespace, step, fingerprint) satisfied; on FAILURE not marked
    -> retried next run ✓; BUT on SUCCESS marked with STATIC fingerprint -> next run SKIPS the
    call. Problem: fm_ensure_dnspod_module with fingerprint "compile-dnspod" (static) -> after ONE
    success, future runs skip the dnspod ensure entirely — that's OK for dnspod (once embedded,
    module stays... unless binary replaced by prebuilt later! Then stale "satisfied" would skip
    re-embed!). Also binary-install/binary-shims static fingerprints skip future re-probes -
    fm_install no-ops anyway; skipping them is harmless but breaks "fine-grained re-probe every
    run" doctrine. E.g., prebuilt install then compile mode: namespace/step same, fingerprint
    "compile-official" vs prebuilt "channel-{arch}" differ -> re-runs ✓. Cross-mode ok. But mode
    switch prebuilt->compile: dnspod step first time ok. compile->prebuilt->compile: dnspod step
    STILL "satisfied" from before but binary is now prebuilt (no module) -> SKIPPED -> dnspod
    never re-embedded! BUG. Fix: fingerprint must reflect CURRENT STATE, not a constant: e.g.
    dnspod fingerprint = "(fmᵥersionₜag)-embedded?" Better: use dynamic fingerprint like
    "(fm_version_tag 2>/dev/null)" - changes when binary changes. Simplest robust fix: make
    fingerprints derived from binary state:
     - binary-install: "$(fm_version_tag)"
     - binary-shims: "(readlink -f "(fm_get_binary)" 2>/dev/null)"
     - dnspod: "(fmᵥersionₜag)-module-(fm_has_module ... && echo y || echo n)" — hmm if module
       missing fingerprint changes -> runs. If module embedded -> satisfied next time ✓. If binary
       replaced (prebuilt) -> version same?? prebuilt same version v1.12.7 + module n ->
       fingerprint differs -> re-run ✓.
       But fm_has_module needs binary present; keep simple: dnspod fingerprint
       "(fmᵥersionₜag)-(fm_module_in_bin "$(fm_get_binary)" dns.providers.dnspod >/dev/null 2>&1
       && echo embedded || echo missing)". fm_module_in_bin returns exit status. In fingerprint:
       compute via $( ... && echo embedded || echo missing).
       Actually simpler and state-true: for steps whose function is ALREADY idempotent
       probe-based, DON'T wrap in step_run at all (the user's fine-grained doctrine: each function
       self-probes; step_state adds a stale-skip layer that can HIDE drift). The compile branch
       functions (fm_install etc.) are all self-probing idempotent. Removing step_run there =
       always re-probe = truly fine-grained. The prebuilt installer is also self-probing
       (is_expected_binary ✓). I'll DROP step_run wrappers in compile branch (call directly) and
       in prebuilt (call directly), keeping step_state for pipeline's caddyfile (content-hash
       idempotent anyway - but keeping is fine... caddyfile has own hash skip; keep step_run? Its
       fingerprint "v1" static + satisfied -> skips fm_caddyfile_ensure forever even after
       Caddyfile deleted! State drift again. Drop wrapper there too - fm_caddyfile_ensure
       self-probes.) Per user's fine-grained doctrine: drop the stale-skip layer where functions
       self-probe. That also fixes deferred-retry issues.

 2. COMPILE_DIR = map_web_path "compile_dir" (gvar) - resolves to the web/compile path
    (/www/programing? need value). Builder hardcodes /www/programing/frankenphp. Prebuilt uses
    ${COMPILE_DIR}/frankenphp/prebuilt. acme uses COMPILE_DIR/acme.sh. Unify:
    FRANKENPHP_STATIC_BUILD_ROOT should = "{COMPILE_DIR}/frankenphp". But builder is sourced in
    contexts where COMPILE_DIR exists? Builder sources gvar? Manager sources: callers source gvar
    first (93/pipeline ✓, 43 ✓, 175 ✓, 53 ✓). The builder itself doesn't source gvar. COMPILE_DIR
    available in all consumers ✓. Change builder constant:
    FRANKENPHP_STATIC_BUILD_ROOT="${COMPILE_DIR:-/www/programing}/frankenphp". Check COMPILE_DIR
    actual value: map_web_path "compile_dir" - let me check what it returns (probably
    /www/programing - the web root mapping). Verify quickly.

 3. Version default fix: 1.27.0 -> "latest" everywhere (pipeline prompt text + prebuilt default).
    frankenphp_prebuilt_set_release_tag handles "latest" ✓.

 4. Unify binary path constants: prebuilt INSTALL_BIN should use manager's FRANKENPHP_LINK_PATH
    (="/usr/local/bin/frankenphp"). Manager is sourced in prebuilt ✓. Replace
    FRANKENPHP_PREBUILT_INSTALL_BIN value with "${FRANKENPHP_LINK_PATH}". Backup suffix: keep
    per-variant (".prebuilt") but define ONCE in manager? Add manager constants:
    FRANKENPHP_BACKUP_SUFFIX=".pre-dnspod" exists. Add FRANKENPHP_PREBUILT_BACKUP_SUFFIX in
    manager too? The user wants constants unified across the three files - centralize in manager
    (the shared library): add:
     - FRANKENPHP_PREBUILT_PROJECT / default version / cache+staging under build root. Put them in
       manager near other constants, and prebuilt/pipeline reference them.

 5. Prebuilt branch mutex + cleanup: after install+shims: fm_disable_legacy_php_runtime
    (plane-gated ✓) + fm_static_apt_php_cleanup (gated on healthy binary ✓) + fm_dns01_ensure?
    Prebuilt has NO dnspod module - dns01 ensure prints "deferred module not embedded" -
    misleading for prebuilt mode. The prebuilt cert path = acme.sh (separate). Keep
    fm_dns01_ensure out of prebuilt (it's compile-mode). Actually its message "DNS-01 deferred:
    dnspod module not embedded yet (built-in HTTP-01/TLS-ALPN-01 stays active)" is accurate-ish
    for prebuilt (built-in ACME). Skip adding.

 6. acme.sh part: read rest of file (90+ lines) - issuance function? "确保DNSPOD能签发证书" for
    prebuilt: acme.sh --issue --dns dns_dp with DP_Id/DP_Key from secret keys + install-cert to a
    path + Caddyfile uses tls <cert> <key>? The current Caddyfile (fm_caddyfile_ensure) has NO
    file-cert path - it gates tls dns dnspod OR default ACME. For acme.sh-issued certs, Caddyfile
    would need tls /path/cert /path/key. That's a design extension... The user's requirement:
    "frankenphp 预编译版由 acme 签发并常驻服务的版本" - prebuilt uses acme issuance + persistent
    service. Need to see the acme_sh file's remaining part (does it issue? install cert? where?)
    before wiring. Also "常驻服务" - a systemd service for frankenphp (frankenphp.service exists
    disabled) or the octane service? The runtime entry stays 175's octane:frankenphp (that IS the
    persistent service via laravel unit). Maybe 常驻服务 = ensure the service runs continuously
    (systemd Restart=always) - handled by 175's service registration.

 Read the rest of acme_sh + check COMPILE_DIR value + DNS_DNSPOD_API_TOKENS format + check
 manager's current state around constants (it was modified at 18:36 - by me or user? My mutex edit
 was my last; likely mine. But verify fm_disable_legacy_php_runtime / link functions survived).

 读 acme_sh 余下部分（签发/装证书逻辑）、COMPILE_DIR 实值、令牌文件格式，并核对 manager 关键函数仍
 在：


 $ cd /www/programing/core_node/scripts/shells/linux && sed -n '90,140p'
 common/frankenphp_acme_sh_install.sh; echo ===COMPILE_DIR===; bash -c 'source
 common/gvar_common.sh >/dev/null 2>&1; echo "COMPILE_DIR=$COMPILE_DIR"'; echo ===token-files===;
 wc -c /www/programing/core_node/.secret_keys/.secret_ignore/DNS_DNSPOD_API_TOKENS
 /www/programing/core_node/.secret_keys/.secret_ignore/DNSPOD_EMAILS 2>/dev/null; head -c 60
 /www/programing/core_node/.secret_keys/.secret_ignore/DNS_DNSPOD_API_TOKENS | sed
 's/[0-9a-zA-Z]/x/g'; echo; echo ===manager-fns===; grep -c
 "fm_disable_legacy_php_runtime()\|fm_ensure_local_bin_link()\|fm_dns01_ensure()"
 common/frankenphp_manager.sh

 ... (50 earlier lines, ctrl+o to expand)
 25 /www/programing/core_node/.secret_keys/.secret_ignore/DNSPOD_EMAILS
 64 total
 xxxxxx,xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 ===manager-fns===
 3


 Key facts:

 1. acme_sh component: only INSTALLS acme.sh (repo clone, bootstrap, link) - does NOT issue certs
    yet. "确保DNSPOD能签发证书" for prebuilt requires an issuance step: acme.sh --issue --dns
    dns_dp (DP_Id/DP_Key env from token "id,token" format ✓ file contains "xxxxxx,xxxx..." =
    id,token format matching caddy dnspod format!) + --install-cert to a stable path + Caddyfile
    file-cert wiring. DNSPOD_EMAILS for account email.

 2. COMPILE_DIR=/www/_debian_12 (!!) - NOT /www/programing. So prebuilt cache would go
    /www/_debian_12/frankenphp/prebuilt, acme.sh to /www/_debian_12/acme.sh. But my static builder
    hardcodes /www/programing/frankenphp per the user's explicit earlier instruction ("不要放到
    /tmp 目录编译，而是放到 /www/programing/frankenphp 目录"). CONFLICT: the new components use
    COMPILE_DIR (=/www/_debian_12) while builder uses /www/programing/frankenphp. The user now
    says "重新统一切换和二进制和路径等全局常量" - unify! Which root wins? The user explicitly
    ordered /www/programing/frankenphp for the build tree earlier. Hmm - but COMPILE_DIR is the
    project's global constant for compiled stuff (map_web_path). The earlier instruction was
    explicit for frankenphp build. Decision: make ONE constant family in the MANAGER (single
    source):
     - FRANKENPHP_BUILD_ROOT="/www/programing/frankenphp" (per user's explicit instruction)
     - subpaths: src/candidate/prebuilt/acme.sh under it.
       Prebuilt/acme scripts reference manager constants instead of COMPILE_DIR. This unifies
       paths AND keeps the user's chosen root. Wait - is /www/programing vs /www/_debian_12...
       _debian_12 looks like a per-OS web dir. The frankenphp tree is machine-specific anyway
       (multi-GB build artifacts, not synced). Keep /www/programing/frankenphp as the user
       ordered; centralize in manager:
       FRANKENPHP_BUILD_ROOT="/www/programing/frankenphp"
       FRANKENPHP_STATIC_SRC_DIR="${FRANKENPHP_BUILD_ROOT}/src"
       FRANKENPHP_STATIC_STAGING_DIR="${FRANKENPHP_BUILD_ROOT}/candidate"
       FRANKENPHP_PREBUILT_CACHE_DIR="${FRANKENPHP_BUILD_ROOT}/prebuilt"
       FRANKENPHP_PREBUILT_STAGING_DIR="${FRANKENPHP_BUILD_ROOT}/prebuilt-staging"
       FRANKENPHP_ACME_DIR="${FRANKENPHP_BUILD_ROOT}/acme.sh"
       Builder references manager's constants? Currently builder DEFINES them (sourced BY manager
       before constants? manager sources builder at line ~41 BEFORE its own constants block (line
       44+)? From earlier read: manager sources service_contract + builder at lines 40-42, then
       FRANKENPHP_BIN_CANDIDATES at 44. So builder's constants load first; manager can
       reference/override. Simplest: MOVE the build-root family INTO the builder (it's the build
       component) and have prebuilt/acme reference those names. But prebuilt/acme source the
       manager (which sources builder) -> constants available ✓. So: define the whole path family
       in BUILDER (single source): FRANKENPHP_BUILD_ROOT, SRC, STAGING, PREBUILT_CACHE,
       PREBUILT_STAGING, ACME_HOME. Prebuilt script:
       FRANKENPHP_PREBUILT_CACHE_DIR="FRANKENPHP_PREBUILT_CACHE_DIR:-..." referencing builder
       constants. acme: ACME_INSTALL_DIR = "{FRANKENPHP_ACME_DIR}".

 3. Token: file "DNS_DNSPOD_API_TOKENS" = "id,token" (39 bytes) exactly the caddy dnspod format ✓.
    DNSPOD_EMAILS = email for acme account.
    Token provisioning unification: fm_dnspod_token_value currently reads
    RuntimeConfigurationStore (Laravel). Add a shared SECRET-KEYS source: read
    .secret_keys/.secret_ignore/DNS_DNSPOD_API_TOKENS. Where's the canonical .secret_keys path
    resolver? grep showed common_functions.sh, domain_setup_common.sh, gvar_common.sh mention
    secret_keys - find the helper (maybe get_secret_key or SECRET_KEYS_DIR var). Check gvar for
    SECRET_KEYS constants/functions.

 4. Manager functions intact ✓ (3/3).

 Next: check secret-keys helpers in gvar/common_functions, then implement the unification edits:

 Implementation plan:

 A. Builder (frankenphp_static_builder.sh): extend constants family (add PREBUILT_CACHE/STAGING +
 ACME dirs). Keep FRANKENPHP_STATIC_BUILD_ROOT as-is name? Manager/prebuilt reference: use
 existing names + add new. To "统一" names: keep FRANKENPHP_STATIC_BUILD_ROOT (already used) and
 add:
    FRANKENPHP_PREBUILT_CACHE_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt"
    FRANKENPHP_PREBUILT_STAGING_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt-staging"
    FRANKENPHP_ACME_HOME_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/acme.sh"
    Also token source constant: FRANKENPHP_SECRET_KEYS_DIR? Better in manager near dnspod
 constants:
 FRANKENPHP_DNSPOD_TOKEN_FILE="${REPO...}/.secret_keys/.secret_ignore/DNS_DNSPOD_API_TOKENS" -
 repo root = FRANKENPHP_STATIC_REPO_ROOT (builder) ✓ reuse.

 B. Manager:
 - fm_dnspod_token_value: read RuntimeConfigurationStore FIRST; fallback (or primary?) to
   secret-keys file. Per user: token lives in .secret_keys/.secret_ignore; 175 previously issued
   via Laravel into the store. Unify: value = store value, else file value; if file has value and
   store empty -> ALSO seed the store (idempotent mirror) so Laravel side sees it. Implement in
   fm_dnspod_token_value? Better a new fine-grained function fm_dnspod_token_ensure: seed store
   from file when store empty (needs runtime ctx; no-op otherwise) and fm_dnspod_token_value stays
   pure read (store, then file fallback).
 - Wire fm_dnspod_token_ensure into: fm_dns01_ensure (compile-mode readiness) + 175 branch +
   ensure flow? Put in fm_dns01_ensure: first fm_dnspod_token_ensure, then existing logic.
 - Prebuilt cert issuance (acme.sh dnspod): new function in acme_sh file:
   acme_sh_ensure_certificate <domain>:
     - exports DP_Id/DP_Key from token (id,token split)
     - acme.sh --issue --dns dns_dp -d domain -d *.domain --server letsencrypt --keylength/ec-256?
       (wildcard needs DNS-01 ✓)
     - --install-cert -d domain --fullchain-file <dir>/fullchain.pem --key-file <dir>/key.pem
       --reloadcmd "systemctl reload..."?
     - idempotent: acme.sh skips if cert exists ("Domains not changed" skip).
     - cert path: under build root: ${FRANKENPHP_STATIC_BUILD_ROOT}/certs/<domain>/...
 - Caddyfile: fm_caddyfile_ensure needs a THIRD tls variant: when acme certs exist on disk -> tls
   <fullchain> <key>. Gate: if dnspod module+token -> dns dnspod; elif cert files exist for site
   host -> file paths. Add.

 C. Prebuilt script:
 - constants from builder/manager (INSTALL_BIN -> FRANKENPHP_LINK_PATH; cache/staging from
   builder; default version -> "latest").
 - drop stale-fingerprint step_run wrappers (self-probing funcs).
 - add after install: fm_disable_legacy_php_runtime + fm_static_apt_php_cleanup + acme issuance
   (acme_sh_ensure_certificate with site host/domain + install-cert) — needs the DOMAIN (site
   host). Prebuilt script doesn't know site host... pipeline knows FRANKENPHP_SITE_HOST (localhost
   default - but real host computed in 175). acme issuance needs a REAL domain. Where from?
   DOMAIN_DOMAINS_LIST (gvar/domain constants) - 93's old code used localhost default; the domain
   family constants exist (DOMAIN_API_PREFIX, DOMAIN_DOMAINS_LIST). Pipeline: compute site host
   like 175 does (api.<prefix>.<first domain>) when DOMAIN_SCOPE != none. Centralize: add to
   manager fm_site_host() (single source; 175F + pipeline reuse). Hmm 175 computes its own...
   unify: fm_site_host in manager; update 175 dispatch to use it? 175's inline logic -> replace
   with fm_site_host call ✓ dedupe.
 - fix rc capture (fm_ensure_local_bin_link; fm_ensure_php_cli_shim -> check each).

 D. Pipeline: version default text -> latest; drop its own REPO_ROOT/etc where duplicated
 (CADDYFILE_DIR from... 93 old had it; pipeline re-defines; keep in pipeline as 93-orchestrator
 constants - fine, it IS the orchestrator; but SITE_HOST should come from fm_site_host()).

 E. step_run removal: compile branch + prebuilt + acme + pipeline caddyfile: call directly (all
 self-probing). This fixes mode-switch staleness + deferred-retry.

 F. 175F branch: add fm_dnspod_token_ensure before fm_dns01_ensure.

 That's a lot; prioritize per user: constants unification + mutex/idempotency fix + token from
 secret_keys + both variants functional (dnspod compile / acme prebuilt) + mutual cleanup + shared
 constants. Also verify the RUNNING static build (compile path) still ok.

 Check secret_keys helpers first (quick), then start editing. Let me also check manager constants
 area current state (lines 44-70) to place new constants.

 acme_sh 只装不发证书；COMPILE_DIR=/www/_debian_12 与 builder 的 /www/programing/frankenphp 冲突；
 令牌文件正是 id,token 格式。查 secret_keys 公共读取器与 manager 常量区现状：


 $ cd /www/programing/core_node/scripts/shells/linux && grep -n
 "secret_keys\|SECRET_KEYS\|secret_ignore" common/gvar_common.sh common/common_functions.sh | head
 -8; echo ===mgr-consts===; sed -n '44,70p' common/frankenphp_manager.sh; echo
 ===build-running===; grep -m1 "SPC_CMD_VAR_FRANKENPHP_XCADDY"
 /www/programing/frankenphp/build.log 2>/dev/null | cut -c1-160; tail -1
 /var/tmp/frankenphp_static_build.log | cut -c1-100

 ... (35 earlier lines, ctrl+o to expand)
 FRANKENPHP_PHP_INI_DIR="/etc/frankenphp/php-conf.d"

 # Binary path (empty string when absent) - file probing only, no command -v.
 ===build-running===
 + curl -o spc -fsSL https://dl.static-php.dev/static-php-cli/spc-bin/nightly/spc-linux-x86_64


 Good:
 - Secret reader EXISTS: common_functions.sh:123 "Read a single raw secret value from
   .secret_keys/.secret_ignore" - a function around line 123-130 using
   $project_root/.secret_keys/.secret_ignore/$key_name. Get its name (probably read_raw_secret or
   similar). I'll use it.
 - Manager constants intact.
 - Build still in spc download phase (SPC_CMD_VAR line not yet reached).

 Now implement the unification. Let me get the secret reader's function name first, then do the
 edits in order:

 1. common_functions secret fn name.
 2. Builder: add path-family constants (prebuilt cache/staging + acme home + certs).
 3. Manager: token file constant + fm_dnspod_token_ensure (seed store from secret file,
    idempotent) + fm_dnspod_token_value fallback to secret file + fm_site_host() (shared site host
    resolver) + Caddyfile third gate (acme file certs) + call token ensure in fm_dns01_ensure.
 4. Prebuilt: constants from shared family; version default latest; drop step_run wrappers; fix
    rc; add mutex+cleanup+acme issuance call.
 5. acme_sh: add issuance function (dns_dp with DP_Id/DP_Key, wildcard, install-cert to shared
    certs dir, idempotent).
 6. Pipeline: default version text latest; site host via fm_site_host.
 7. 175F: token ensure before dns01.
 8. 175: replace inline site-host computation with fm_site_host (dedupe).

 That's the full plan. Get secret fn name.


 $ sed -n '118,140p' /www/programing/core_node/scripts/shells/linux/common/common_functions.sh

 ... (18 earlier lines, ctrl+o to expand)
   done < "$raw_file"
   echo ""
 }

 # Resolve an indexed secret: first non-empty of <BASE>_1..<BASE>_5 then bare <BASE>.


 get_secret_key_from_common_functions <key> exists ✓ (uses COMMON_FUNCS_DIR to find project root).

 Now implement all edits. Start with Manager (heart of unification):

 Edit 1 - constants block: add after FRANKENPHP_DNSPOD_TOKEN_KEY:

 ```
   FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY="DNS_DNSPOD_API_TOKENS"
   FRANKENPHP_DNSPOD_EMAIL_SECRET_KEY="DNSPOD_EMAILS"
 ```

 Edit 2 - after fm_dnspod_token_value: fallback to secret file + new fm_dnspod_token_ensure +
 fm_site_host. Current fm_dnspod_token_value:

 ```
   fm_dnspod_token_value() {
       if [ -n "$PHP_BIN" ] && [ -x "$PHP_BIN" ] \
           && [ -n "$VENDOR_AUTOLOAD" ] && [ -f "$VENDOR_AUTOLOAD" ]; then
           runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY"
       fi
   }
 ```

 New:

 ```
   fm_dnspod_token_value() {
       local stored=""
       if [ -n "$PHP_BIN" ] && [ -x "$PHP_BIN" ] \
           && [ -n "$VENDOR_AUTOLOAD" ] && [ -f "$VENDOR_AUTOLOAD" ]; then
           stored="$(runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY" 2>/dev/null || true)"
       fi
       if [ -n "$stored" ]; then
           echo "$stored"
           return 0
       fi
       get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY"
   }
 ```

 (get_secret_key needs common_functions sourced - manager consumers all do; manager itself doesn't
 source it... 175F/43/93/pipeline do. fm_dns01_ensure callers all source common_functions ✓.)

 Edit 3 - token seeding (mirror secret -> store so the Laravel side reads one canonical store):

 ```
   # Mirror the secret-file token into the Laravel RuntimeConfigurationStore
   # (the canonical store the app + Caddyfile env chain reads) - idempotent:
   # store wins when already set, file absent -> no-op.
   fm_dnspod_token_ensure() {
       local file_token=""
       local stored=""

       file_token="$(get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY")"
       [ -n "$file_token" ] || return 0
       if [ -n "$PHP_BIN" ] && [ -x "$PHP_BIN" ] \
           && [ -n "$VENDOR_AUTOLOAD" ] && [ -f "$VENDOR_AUTOLOAD" ]; then
           stored="$(runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY" 2>/dev/null || true)"
           if [ -z "$stored" ]; then
               fm_dnspod_token_put "$file_token"
               echo "[$SCRIPT_INDEX] DNSPod token seeded into the runtime store (from
 ${FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY})"
           fi
       fi
       return 0
   }
 ```

 Edit 4 - fm_site_host (shared; domain constants via gvar get_var? 175 used
 DOMAIN_SCOPE/DOMAIN_API_PREFIX/DOMAIN_DOMAINS_LIST globals from domain setup. Are those gvar
 vars? They're exported by domain_setup_common presumably. Manager can read via ${...:-}.
 Implement:

 ```
   # Shared site host (single source for 93/pipeline + 175 dispatch + the
   # Mercure trusted issuer): first configured api.<region>.<domain>, else
   # localhost. Readers pass their own already-resolved value via
   # FRANKENPHP_SITE_HOST when they need to override.
   fm_site_host() {
       local first_domain=""
       if [ "${DOMAIN_SCOPE:-none}" != "none" ] && [ -n "${DOMAIN_API_PREFIX:-}" ] && [ -n
 "${DOMAIN_DOMAINS_LIST:-}" ]; then
           first_domain="$(printf '%s\n' "$DOMAIN_DOMAINS_LIST" | head -n1)"
           if [ -n "$first_domain" ]; then
               echo "api.${DOMAIN_API_PREFIX}.${first_domain}"
               return 0
           fi
       fi
       echo "localhost"
   }
 ```

 Edit 5 - fm_dns01_ensure: add fm_dnspod_token_ensure call first.

 Edit 6 - Caddyfile: third gate for acme file certs. Need to see the current fm_caddyfile_ensure
 tls gate section (I saw the gate earlier: dnspod_tls computed). Add: acme cert path constant
 FRANKENPHP_ACME_CERT_DIR="FRANKENPHP_STATIC_BUILD_ROOT/certs" (from builder family). In
 fm_caddyfileₑnsure: after dnspod gate, if no dnspodₜls: check
 "{FRANKENPHP_ACME_CERT_DIR}/${site_host}/fullchain.pem" + key.pem exist -> tls line with file
 paths. Where exactly? Read the function's current render to patch precisely. It's lines ~407-474.
 I read part (top gate + bottom compare). Need the middle render section. Let me read the whole
 fm_caddyfile_ensure.

 Edit 7 - acme_sh issuance function (in acme_sh file): acme_sh_ensure_certificate <site_host>:

 ```
   ACME_CERT_DIR (from builder family)
   acme_sh_ensure_certificate() {
       local site_host="$1"
       local token=""
       local dp_id=""
       local dp_key=""
       local cert_dir=""
       local email=""

       [ -n "$site_host" ] || return 1
       [ "$site_host" != "localhost" ] || { echo "... no public domain; skip"; return 0; }
       token="$(get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY")"
       [ -n "$token" ] || { warn skip; return 0; }
       dp_id="${token%%,*}"; dp_key="${token#*,}"
       email="$(get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_EMAIL_SECRET_KEY")"
       [ -n "$email" ] || email="admin@example.com"  -> better from existing ACME_INSTALL_EMAIL
 fallback
       acme_sh_ensure_install (idempotent base)
       cert_dir="${FRANKENPHP_ACME_CERT_DIR}/${site_host}"
       mkdir -p
       # wildcard + apex via DNS-01 (dns_dp = DNSPod provider in acme.sh)
       if ! DP_Id="$dp_id" DP_Key="$dp_key" "$ACME_INSTALL_BIN" --issue --dns dns_dp \
           -d "$site_host" --server letsencrypt ...
 ```

 Hmm - wildcard: issue for the domain family: apex + .apex. site_host=api.region.domain -> the
 cert should cover the registrable domain. Simpler: issue for the apex domain + wildcard: extract
 from site_host? The 175/domain system knows DOMAIN_DOMAINS_LIST. For prebuilt flow, pipeline
 passes... Keep scope pragmatic: issue for site_host + ".siteₕost#api.*"? Overcomplicating.
 Pragmatic: the pipeline passes DOMAIN apex list? Let me keep the function generic:
 acmeₛhₑnsure_certificate < apex_domain > : issues -d apex -d *.apex, installs to certs/ < apex >
 /. Pipeline calls it for EACH domain in DOMAIN_DOMAINS_LIST (or just first). Caddyfile file-cert
 gate then looks up certs/ < apex-of-site-host > ? The site host api.prefix.domain - > apex =
 domain. Map: strip "{DOMAIN_API_PREFIX}." prefix... but manager doesn't know prefix... it does:
 DOMAIN_API_PREFIX global. In fm_caddyfile_ensure compute apex =
 "${site_host#api.${DOMAIN_API_PREFIX}.}"? If prefix="region"... Format: api.<region>.<domain>?
 Actually "api.DOMAIN_API_PREFIX.{first_domain}" - DOMAIN_API_PREFIX might BE the region. apex =
 first_domain part = strip "api.${DOMAIN_API_PREFIX}.".
 Simplify: gate looks for "FRANKENPHP_ACME_CERT_DIR/{site_host}/fullchain.pem" AND the issuance
 uses site_host itself as cert dir keyed by apex derived INSIDE issuance... To avoid
 overengineering: issuance keyed by apex domain list from DOMAIN_DOMAINS_LIST; caddyfile gate:
 compute apex same way from site_host; look in certs/<apex>/. Implement helper
 fm_acme_cert_dir_for_host in manager:

 ```
   fm_acme_cert_dir_for_host() {  # $1 site_host
       local apex=""
       apex="${1#api.${DOMAIN_API_PREFIX:-}.}"
       [ -n "$apex" ] && echo "${FRANKENPHP_ACME_CERT_DIR}/${apex}"
   }
 ```

 Issue function in acme_sh:

 ```
   acme_sh_ensure_certificate() { # $1 apex_domain
       ... DP_Id/DP_Key; issue -d "$1" -d "*.$1" --dns dns_dp; then --install-cert -d "$1"
 --fullchain-file "$dir/fullchain.pem" --key-file "$dir/key.pem"
       idempotent: acme.sh itself skips unchanged domains ("Skipping" rc 2!) - treat rc=2 as
 success.
   }
 ```

 acme.sh rc 2 = "skip renew" -> handle.

 Edit 8 - pipeline: version prompt default latest text; site host:
 FRANKENPHP_SITE_HOST="(fmₛiteₕost)" default; after prebuilt install call issuance for each domain
 in DOMAIN_DOMAINS_LIST? Pipeline finalize: if prebuilt mode - > for d in DOMAIN_DOMAINS_LIST:
 acmeₛhₑnsure_certificate "d" (needs acme script sourced - it sources in prebuilt's install fn;
 issuance fn needs sourcing again in pipeline or do it inside prebuilt installer where acme script
 is already sourced ✓ - pass domains via env? prebuilt installer doesn't know domains... use
 DOMAIN_DOMAINS_LIST global inside it (gvar/domain globals available since gvar sourced ✓). Do it
 in prebuilt's finalize: after acme_sh_ensure_install, loop DOMAIN_DOMAINS_LIST ->
 acme_sh_ensure_certificate. Then mutex+cleanup.

 OK - many edits. Also drop step_run wrappers (compile/prebuilt/acme/pipeline-caddyfile) per
 staleness fix.

 Also prebuilt constants: INSTALL_BIN -> "${FRANKENPHP_LINK_PATH}", cache/staging -> builder
 family, default version -> "latest".

 And builder: add PREBUILT_CACHE/STAGING + ACME_HOME + CERT_DIR constants.

 Read fm_caddyfile_ensure fully now (need exact text for the gate patch).

 实现统一。先读 fm_caddyfile_ensure 完整渲染段以接入第三种 tls 门控（acme 文件证书）：


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 'fm_caddyfile_ensure()' frankenphp_manager.sh | cut -d: -f1),+68p" frankenphp_manager.sh

 ... (64 earlier lines, ctrl+o to expand)
     echo "[$SCRIPT_INDEX] Caddyfile rendered: $caddyfile_path (secrets stay env placeholders)"
 }

 # Persist state for downstream consumers (Laravel ServerManager, 132).
 fm_store_info() {


 Now I have the full picture. Implement the manager edits (one edit call, multiple blocks):

 1. Constants: token secret keys + prebuilt/acme path family references will come from builder -
    add manager-side constants AFTER the token key line:
 ```
   FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY="DNS_DNSPOD_API_TOKENS"
   FRANKENPHP_DNSPOD_EMAIL_SECRET_KEY="DNSPOD_EMAILS"
 ```

 (Path family lives in the builder = the build/install root component; prebuilt+acme source the
 manager which sources the builder.)

 2. fm_dnspod_token_value: store-first + secret-file fallback.

 3. Add fm_dnspod_token_ensure + fm_site_host + fm_acme_cert_dir_for_host after
    fm_dnspod_token_put (need that function's end text: it ends with     runtime_config_put
    "$FRANKENPHP_DNSPOD_TOKEN_KEY" "$token"\n} - from earlier read:

 ```
   fm_dnspod_token_put() {
       ...
       runtime_config_put "$FRANKENPHP_DNSPOD_TOKEN_KEY" "$token"
   }
 ```

 then next is the legacy mutex comment. I'll anchor insert between them.

 4. fm_dns01_ensure: prepend token ensure.

 5. fm_caddyfile_ensure: tls gate chain: dnspod -> acme file certs -> none (built-in ACME). Add
    local acme_tls + helper. Render keeps ${dnspod_tls} but needs combined tls var: rename
    semantics: tls_block = dnspod OR file certs:
 ```
       local acme_tls=""
       ...
       if [ -z "$dnspod_tls" ]; then
           acme_dir="$(fm_acme_cert_dir_for_host "$site_host")"
           if [ -n "$acme_dir" ] && [ -f "${acme_dir}/fullchain.pem" ] && [ -f
 "${acme_dir}/key.pem" ]; then
               acme_tls="   tls ${acme_dir}/fullchain.pem ${acme_dir}/key.pem

   "
               echo "[$SCRIPT_INDEX] Caddyfile tls: acme.sh file certificates (${acme_dir})"
           fi
       fi
 ```

 and render uses dnspodₜls{acme_tls} - combined into one var simpler: keep both vars and
 interpolate both (only one non-empty).

 Builder path family additions (builder file): after STAGING constant:

 ```
   FRANKENPHP_PREBUILT_CACHE_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt"
   FRANKENPHP_PREBUILT_STAGING_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt-staging"
   FRANKENPHP_ACME_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/acme.sh"
   FRANKENPHP_ACME_CERT_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/certs"
 ```

 Then prebuilt/acme/pipeline edits. GO - manager first.

 ㉗ Manager 统一改造（令牌双源+种子、共享 site-host/apex 解析、Caddyfile 三态 tls 门控）：

 Response was truncated before completion.


 edit ...


 Tool call "edit" was not executed: the response hit the output token limit, so its arguments may
 be truncated. Re-issue the tool call with complete arguments.

 The

 Response was truncated before completion.


 [compaction]

 Compacted from 124,033 tokens (ctrl+o to expand)


 The edit got truncated because of output token limit. Let me re-issue with smaller chunks - split
 into multiple edit calls with minimal oldText. First read the exact current text of
 fm_dnspod_token_value.


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n
 "fm_dnspod_token_value()\|runtime_config_get\|^}" frankenphp_manager.sh | sed -n "$(grep -n
 'fm_dnspod_token_value()' frankenphp_manager.sh | cut -d: -f1),+4p" | head -8; echo ===; sed -n
 "$(grep -n 'fm_dnspod_token_value()' frankenphp_manager.sh | cut -d: -f1),+7p"
 frankenphp_manager.sh

 ... (4 earlier lines, ctrl+o to expand)
         runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY"
     fi
 }

 # Persist the DNSPod API token (id,token) into the shared store. Requires

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
   51 # libdns-v1 tag is released.
   52 FRANKENPHP_DNSPOD_IMPORT="github.com/caddy-dns/dnspod@master"
   53 FRANKENPHP_DNSPOD_MODULE="dns.providers.dnspod"
   54 FRANKENPHP_DNSPOD_TOKEN_KEY="DNSPOD_TOKEN"
 + 55 # Secret-file keys (.secret_keys/.secret_ignore via the common_functions
 + 56 # reader): the DNSPod API token "id,token" + the ACME account email -
 + 57 # shared by the compiled variant (Caddy dns provider) and the prebuilt
 + 58 # variant (acme.sh dns_dp DNS-01).
 + 59 FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY="DNS_DNSPOD_API_TOKENS"
 + 60 FRANKENPHP_DNSPOD_EMAIL_SECRET_KEY="DNSPOD_EMAILS"
   55 FRANKENPHP_FRANKENPHP_IMPORT="github.com/dunglas/frankenphp"
   56 # Mercure hub module version, matched to the release frankenphp v1.12.7
   57 # pins in its own go.mod (v0.24.2) so a rebuild never drags in a newer
   58 # hub major behind the running binary's back.
      ...



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
  278 # Caddyfile - only the {env.DNSPOD_TOKEN} placeholder is). Empty when unset
  279 # or when the Laravel app context is not available yet (fresh install);
  280 # the runtime branch re-renders the Caddyfile once it is.
  281 fm_dnspod_token_value() {
 +282     local stored=""
  282     if [ -n "$PHP_BIN" ] && [ -x "$PHP_BIN" ] \
  283         && [ -n "$VENDOR_AUTOLOAD" ] && [ -f "$VENDOR_AUTOLOAD" ]; then
 -284 runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY"
 +285         stored="$(runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY" 2>/dev/null || true)"
  285     fi
 +287     if [ -n "$stored" ]; then
 +288         echo "$stored"
 +289         return 0
 +290     fi
 +291     get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY"
  286 }
  287
  288 # Persist the DNSPod API token (id,token) into the shared store. Requires
  289 # the runtime context (PHP_BIN + VENDOR_AUTOLOAD + BOOTSTRAP_APP) or a
      ...


 Now find where fm_dnspod_token_put ends to insert fm_dnspod_token_ensure + fm_site_host +
 fm_acme_cert_dir_for_host after it.


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 'fm_dnspod_token_put()' frankenphp_manager.sh | cut -d: -f1),+12p" frankenphp_manager.sh

 ... (8 earlier lines, ctrl+o to expand)
         return 1
     fi
     runtime_config_put "$FRANKENPHP_DNSPOD_TOKEN_KEY" "$token"
     echo "[$SCRIPT_INDEX] DNSPod token stored (DNS-01 engages on the next Caddyfile render)"
 }

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
  307     runtime_config_put "$FRANKENPHP_DNSPOD_TOKEN_KEY" "$token"
  308     echo "[$SCRIPT_INDEX] DNSPod token stored (DNS-01 engages on the next Caddyfile render)"
  309 }
  310
 +311 # Mirror the secret-file token (${FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY}) into
 +312 # the Laravel RuntimeConfigurationStore - the canonical store the app and the
 +313 # Caddyfile {env.DNSPOD_TOKEN} chain read. Idempotent: a non-empty stored
 +314 # value always wins; no secret file or no runtime context -> plain no-op.
 +315 fm_dnspod_token_ensure() {
 +316     local file_token=""
 +317     local stored=""
 +318
 +319     file_token="$(get_secret_key_from_common_functions
 "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY")"
 +320     [ -n "$file_token" ] || return 0
 +321     if [ -n "$PHP_BIN" ] && [ -x "$PHP_BIN" ] \
 +322         && [ -n "$VENDOR_AUTOLOAD" ] && [ -f "$VENDOR_AUTOLOAD" ]; then
 +323         stored="$(runtime_config_get "$FRANKENPHP_DNSPOD_TOKEN_KEY" 2>/dev/null || true)"
 +324         if [ -z "$stored" ]; then
 +325             fm_dnspod_token_put "$file_token" >/dev/null 2>&1 || true
 +326             echo "[$SCRIPT_INDEX] DNSPod token seeded into the runtime store (from
 ${FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY})"
 +327         fi
 +328     fi
 +329     return 0
 +330 }
 +331
 +332 # Shared site host - single source for the 93 pipeline, the 175 dispatch and
 +333 # the Mercure issuer wiring: first configured api.<region>.<domain>, else
 +334 # localhost. An already-set FRANKENPHP_SITE_HOST env wins over the resolver.
 +335 fm_site_host() {
 +336     local first_domain=""
 +337     if [ "${DOMAIN_SCOPE:-none}" != "none" ] \
 +338         && [ -n "${DOMAIN_API_PREFIX:-}" ] && [ -n "${DOMAIN_DOMAINS_LIST:-}" ]; then
 +339         first_domain="$(printf '%s\\n' "$DOMAIN_DOMAINS_LIST" | head -n1)"
 +340         if [ -n "$first_domain" ]; then
 +341             echo "api.${DOMAIN_API_PREFIX}.${first_domain}"
 +342             return 0
 +343         fi
 +344     fi
 +345     echo "localhost"
 +346 }
 +347
 +348 # Certificate directory (prebuilt/acme.sh variant) serving the given site
 +349 # host: keyed by the registrable apex (api.<region>. prefix stripped). Empty
 +350 # string when the apex cannot be derived.
 +351 fm_acme_cert_dir_for_host() {
 +352     local apex=""
 +353     apex="${1#api.${DOMAIN_API_PREFIX:-}.}"
 +354     [ -n "$apex" ] || return 0
 +355     echo "${FRANKENPHP_ACME_CERT_DIR}/${apex}"
 +356 }
 +357
  311 # Legacy PHP runtime mutex (frankenphp plane): the gate is the SHARED
  312 # PLANE CONSTANT adopted from the dd.sh selector (START_WEB_SERVER ->
  313 # web_server_plane(), gvar_common) - NEVER the compile result: whether the
  314 # dnspod static build succeeded must not decide which runtime may run.
      ...


 Now:
 - fm_dns01_ensure: prepend fm_dnspod_token_ensure.
 - fm_caddyfile_ensure: third tls gate (acme file certs).

 First find fm_dns01_ensure start.

 ㉘ dns01 预置令牌种子 + Caddyfile 三态 tls 门控：


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 'fm_dns01_ensure()' frankenphp_manager.sh | cut -d: -f1),+6p" frankenphp_manager.sh

 ... (3 earlier lines, ctrl+o to expand)
 HTTP-01/TLS-ALPN-01 stays active)"
         return 0
     fi
     if [ -z "$(fm_dnspod_token_value)" ]; then
         echo "[$SCRIPT_INDEX] DNS-01 pending: DNSPod API token not stored; add it once via"

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
  506     local caddyfile_dir=""
  507     local rendered=""
  508     local existing=""
  509     local dnspod_tls=""
 +510     local acme_tls=""
 +511     local acme_cert_dir=""
  510
  511     caddyfile_dir="$(dirname "$caddyfile_path")"
  512     if [ ! -d "$caddyfile_dir" ]; then
  513         mkdir -p "$caddyfile_dir"
      ...
  525
  526 "
  527     fi
  528
 +531     # Prebuilt variant gate: when the dnspod module is NOT embedded but the
 +532     # acme.sh DNS-01 certificates are on disk, pin them explicitly. Neither
 +533     # gate matching -> Caddy built-in ACME (HTTP-01/TLS-ALPN-01) stays in
 +534     # charge. Sync contract: the Laravel builder renders the identical gates.
 +535     if [ -z "$dnspod_tls" ]; then
 +536         acme_cert_dir="$(fm_acme_cert_dir_for_host "$site_host")"
 +537         if [ -n "$acme_cert_dir" ] \
 +538             && [ -f "${acme_cert_dir}/fullchain.pem" ] \
 +539             && [ -f "${acme_cert_dir}/key.pem" ]; then
 +540             acme_tls="   tls ${acme_cert_dir}/fullchain.pem ${acme_cert_dir}/key.pem
 +541
 +542 "
 +543         fi
 +544     fi
 +545
  529     rendered="# Managed by frankenphp_manager.sh (SYNC: ServerManagerV1FrankenPhpManagerCtl)
  530 {
  531    admin localhost:${admin_port}
  532    auto_https disable_redirects
      ...
  535 https://${site_host}:${https_port} {
  536    root * ${laravel_public_dir}
  537    encode zstd gzip
  538
 -539 ${dnspod_tls}mercure {
 +556 ${dnspod_tls}${acme_tls}   mercure {
  540       issuer {env.MERCURE_TRUSTED_ISSUERS} {
  541          publisher {
  542             jwt {env.MERCURE_PUBLISHER_JWT_KEY} {env.MERCURE_PUBLISHER_JWT_ALG}
  543          }
      ...
  596 # one manual input that cannot be generated (the API token); every re-run
  597 # is a no-op probe once ready. Requires the runtime store context
  598 # (PHP_BIN + VENDOR_AUTOLOAD) for the token read.
  599 fm_dns01_ensure() {
 +617     # Seed the runtime store from the shared secret file first (no-op when
 +618     # already stored or when the file/context is absent).
 +619     fm_dnspod_token_ensure
  600     if ! fm_has_module "$FRANKENPHP_DNSPOD_MODULE"; then
  601         echo "[$SCRIPT_INDEX] DNS-01 deferred: dnspod module not embedded yet (built-in
 HTTP-01/TLS-ALPN-01 stays active)"
  602         return 0
  603     fi
      ...


 Manager done (except prebuilt/acme constants that live in builder). Now builder path family: add
 prebuilt/acme/certs dirs after STAGING constant. Check exact text:

 ㉙ Builder 路径族统一（prebuilt/acme/certs 全部归入同一构建根）：


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n
 "FRANKENPHP_STATIC_STAGING_DIR=\|FRANKENPHP_STATIC_BUILD_ROOT=" frankenphp_static_builder.sh |
 head -4

 53:FRANKENPHP_STATIC_BUILD_ROOT="/www/programing/frankenphp"
 55:FRANKENPHP_STATIC_STAGING_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/candidate"

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_static_builder.sh

      ...
   52 # (incremental rebuild) instead of re-downloading. Idempotently created.
   53 FRANKENPHP_STATIC_BUILD_ROOT="/www/programing/frankenphp"
   54 FRANKENPHP_STATIC_SRC_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/src"
   55 FRANKENPHP_STATIC_STAGING_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/candidate"
 + 56 # Shared install-root path family (single source for the manager, the
 + 57 # prebuilt installer and the acme.sh helper): every FrankenPHP artifact
 + 58 # lives under the same persistent root so upgrades stay incremental.
 + 59 FRANKENPHP_PREBUILT_CACHE_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt"
 + 60 FRANKENPHP_PREBUILT_STAGING_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/prebuilt-staging"
 + 61 FRANKENPHP_ACME_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/acme.sh"
 + 62 FRANKENPHP_ACME_CERT_DIR="${FRANKENPHP_STATIC_BUILD_ROOT}/certs"
   56
   57 # Effective PHP_EXTENSIONS list: base + selector-driven database sets,
   58 # de-duplicated preserving order.
   59 fm_static_php_extensions() {
      ...


 Now the prebuilt script rework:
 1. Constants: INSTALL_BIN -> manager's FRANKENPHP_LINK_PATH; CACHE/STAGING -> builder family
    (guard with :- since sourced via manager which sources builder); DEFAULT_VERSION -> latest;
    drop COMPILE_DIR dependency.
 2. Drop step_run wrappers (stale-skip fix).
 3. Fix rc capture of link/shim.
 4. Add finalize: mutex (fm_disable_legacy_php_runtime) + apt cleanup (fm_static_apt_php_cleanup)
    + acme DNS-01 issuance for each configured domain.

 Write the whole file fresh (cleaner than many edits) - it's ~250 lines; rewrite with the same
 functions but adjusted. Actually a full rewrite risks losing details; do targeted edits instead:

 Edit A - constants block:

 ```
   FRANKENPHP_PREBUILT_DEFAULT_VERSION="1.27.0"
   FRANKENPHP_PREBUILT_CACHE_DIR="${COMPILE_DIR}/frankenphp/prebuilt"
   FRANKENPHP_PREBUILT_STAGING_DIR="${COMPILE_DIR}/frankenphp/prebuilt-staging"
   FRANKENPHP_PREBUILT_BACKUP_SUFFIX=".prebuilt"
   FRANKENPHP_PREBUILT_INSTALL_BIN="/usr/local/bin/frankenphp"
 ```

 ->

 ```
   FRANKENPHP_PREBUILT_DEFAULT_VERSION="latest"
   FRANKENPHP_PREBUILT_BACKUP_SUFFIX=".prebuilt"
 ```

 (cache/staging/install-bin now come from the sourced manager/builder family; keep only local
 fallbacks?) The file sources frankenphp_manager.sh at top (which sources builder) BEFORE using
 these constants in functions - but constants are USED at file top only for assignment... The
 functions use the vars at CALL time (after sourcing) ✓. So just delete the three lines and rely
 on builder+manager family. But if sourced standalone before manager? It sources manager itself ✓.
 Keep FRANKENPHP_PREBUILT_BACKUP_SUFFIX local (variant-specific).

 Wait - FRANKENPHP_PREBUILT_INSTALL_BIN referenced in several functions - replace usages with
 $FRANKENPHP_LINK_PATH. Lines: is_expected (no), install_binary (backup + cp + chmod + link) and
 frankenphp_install_prebuilt ([ -x "$FRANKENPHP_PREBUILT_INSTALL_BIN" ]). Also add fallback
 default at top:
 FRANKENPHP_PREBUILT_INSTALL_BIN="${FRANKENPHP_LINK_PATH:-/usr/local/bin/frankenphp}"? Cleaner:
 keep the var but set from family: after sources, define
 FRANKENPHP_PREBUILT_INSTALL_BIN="$FRANKENPHP_LINK_PATH". Sources are BEFORE... no! Constants are
 declared at top BEFORE source lines? Look: constants block is lines ~20-40, then sources at
 ~41-47. Assignment order: top constants first, then sources override... So set INSTALL_BIN AFTER
 the sources. Add one line after source block. Good minimal churn: delete the 3 family-dup lines,
 and after the source block add:

 ```
   FRANKENPHP_PREBUILT_INSTALL_BIN="${FRANKENPHP_LINK_PATH}"
 ```

 Edit B - frankenphp_install_prebuilt(): drop step_run wrapper, add finalize chain:

 ```
   frankenphp_install_prebuilt() {
       frankenphp_prebuilt_set_release_tag
       frankenphp_prebuilt_detect_arch

       frankenphp_prebuilt_install_binary

       if [ -x "$FRANKENPHP_PREBUILT_INSTALL_BIN" ]; then
           source "$FRANKENPHP_PREBUILT_ACME_INSTALL_SCRIPT"
           acme_sh_ensure_install
           acme_sh_ensure_domains
           fm_disable_legacy_php_runtime
           fm_static_apt_php_cleanup
       else
           echo "[...] [WARN] skip acme.sh install because ..."
       fi
   }
 ```

 acme_sh_ensure_domains: loop DOMAIN_DOMAINS_LIST -> acme_sh_ensure_certificate "$d".

 Edit C - install_binary rc fix:

 ```
       fm_ensure_local_bin_link
       fm_ensure_php_cli_shim
       rc=$?
 ```

 ->

 ```
       fm_ensure_local_bin_link || return 1
       fm_ensure_php_cli_shim || return 1
 ```

 and drop rc=$?; if... block.

 Let me do these edits.

 ㉚ Prebuilt 脚本去重接统一常量（版本默认 latest、路径族来自 manager/builder、修 rc 捕获、去 stale
 step_run、补互斥+清理）：


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_prebuilt.sh

 Could not find edits[3] in
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_prebuilt.sh. The oldText
 must match exactly including all whitespace and newlines.


 The 4th edit failed - probably indentation differs. Check the actual frankenphp_install_prebuilt
 text.


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 '^frankenphp_install_prebuilt()' frankenphp_install_prebuilt.sh | cut -d: -f1),+16p"
 frankenphp_install_prebuilt.sh | cat -A | sed -n '5,9p'; echo ===plain===; sed -n "$(grep -n
 '^frankenphp_install_prebuilt()' frankenphp_install_prebuilt.sh | cut -d: -f1),+16p"
 frankenphp_install_prebuilt.sh

 ... (19 earlier lines, ctrl+o to expand)
     else
         echo "[$FRANKENPHP_PREBUILT_INDEX] [WARN] skip acme.sh install because
 $FRANKENPHP_PREBUILT_INSTALL_BIN is missing"
     fi
 }

 Took 0.0s


 The file differs from what I read earlier (has prebuilt_fingerprint via
 frankenphp_prebuilt_step_fingerprint - the file was modified since my first read? No wait - my
 first read DID show plain step_run ...
 "${FRANKENPHP_PREBUILT_RELEASE_CHANNEL}-${FRANKENPHP_PREBUILT_ARCH}". Now it shows fingerprint
 var - the user/another agent edited concurrently! (18:34 timestamp was before my read... but
 content differs from my read!). Actually my earlier read was cat frankenphp_install_prebuilt.sh -
 it showed step_run "$FRANKENPHP_PREBUILT_NAMESPACE" "prebuilt-binary"
 "${FRANKENPHP_PREBUILT_RELEASE_CHANNEL}-${FRANKENPHP_PREBUILT_ARCH}". Now there's a fingerprint
 helper. So concurrent modification happened between my read and now. Fine - work with current
 content. Check if frankenphp_prebuilt_step_fingerprint exists (dynamic fingerprint - good,
 someone made it dynamic). Read that fn then adapt my edit.

 文件被并发修改过（出现动态 fingerprint 函数）。查看现状后适配：


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 'frankenphp_prebuilt_step_fingerprint()' frankenphp_install_prebuilt.sh | cut -d: -f1),+8p"
 frankenphp_install_prebuilt.sh; ls -la frankenphp_install_prebuilt.sh

 ... (5 earlier lines, ctrl+o to expand)
 }

 frankenphp_install_prebuilt() {
     local prebuilt_fingerprint=""
 -rw-r--r-- 1 root root 11809 Aug 18 18:45 frankenphp_install_prebuilt.sh

 Took 0.0s


 The file is being concurrently edited (root, 18:45 - while I work; user or another agent).
 Someone added a dynamic fingerprint (includes current installed version) - addresses part of the
 staleness concern. Given concurrent editing, I should be careful and minimal on this file. My
 earlier edits 1-3 of the failed call did NOT apply (edit is atomic - all-or-nothing? "Could not
 find edits[3]" - did edits 0-2 apply? The tool said "Could not find edits[3]" as error - atomic,
 nothing applied? The message implies failure overall. Verify constants state:


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n
 "FRANKENPHP_PREBUILT_DEFAULT_VERSION=\|FRANKENPHP_PREBUILT_INSTALL_BIN=\|COMPILE_DIR\|fm_ensure_l
 ocal_bin_link ||" frankenphp_install_prebuilt.sh | head; grep -n
 "fm_disable_legacy_php_runtime\|fm_static_apt_php_cleanup\|acme_sh_ensure_domains"
 frankenphp_install_prebuilt.sh

 ... (2 earlier lines, ctrl+o to expand)
 23:FRANKENPHP_PREBUILT_STAGING_DIR="${COMPILE_DIR}/frankenphp/prebuilt-staging"
 25:FRANKENPHP_PREBUILT_INSTALL_BIN="/usr/local/bin/frankenphp"


 Command exited with code 1

 Took 0.0s


 None of my edits applied (atomic fail) ✓. The concurrent editor added the fingerprint fn but
 constants still old (1.27.0, COMPILE_DIR, /usr/local/bin hardcode) and no mutex/cleanup/issuance.
 Re-apply my edits adapted to CURRENT content (fingerprint var present - I'll keep their dynamic
 fingerprint + step_run? Their fingerprint includes installed version - still stale-safe for
 binary presence. But my analysis: dropping step_run wrappers is cleaner for self-probing funcs.
 However concurrent editor chose step_run+dynamic fingerprint. To avoid fighting, ADOPT their
 approach but fix what's broken/unified:

 - Keep step_run with dynamic fingerprint (their style).
 - Apply constants unification (my edits 1-2).
 - Apply rc fix (edit 3).
 - Extend finalize: mutex + apt cleanup + acme issuance (edit 4, adapted to fingerprint version).
 - The compile branch + pipeline may also have been concurrently edited! Re-check pipeline +
   compile quickly before editing them (pipeline grep for step_run/fingerprints/version 1.27).

 原子失败=未应用。并发编辑者保留 step_run+动态指纹，采纳其风格，仅做统一与补缺。先核对
 pipeline/compile 是否也被并发改过：


 $ cd /www/programing/core_node/scripts/shells/linux/common && ls -la
 frankenphp_install_pipeline.sh frankenphp_install_compile.sh; grep -n
 "1.27.0\|step_run\|fm_site_host\|fm_disable_legacy" frankenphp_install_pipeline.sh
 frankenphp_install_compile.sh | head -12

 ... (10 earlier lines, ctrl+o to expand)
 "binary-install" "$install_fingerprint" \
 frankenphp_install_compile.sh:63:    step_run "$FRANKENPHP_INSTALL_COMPILE_NAMESPACE"
 "binary-shims" "$link_fingerprint" \
 frankenphp_install_compile.sh:68:    step_run "$FRANKENPHP_INSTALL_COMPILE_NAMESPACE"
 "dnspod-module" "$dnspod_fingerprint" \

 Took 0.0s


 Pipeline/compile untouched since 18:25/18:35 (only prebuilt got the concurrent edit at 18:45).
 Apply now:

 A. prebuilt edits (adapted, current content):
 1. Constants unification (default latest + family from manager/builder).
 2. rc fix.
 3. finalize: acme issuance + mutex + cleanup (keep their step_run+fingerprint).

 B. acme_sh: add acme_sh_ensure_certificate <apex> + acme_sh_ensure_domains (loop
 DOMAIN_DOMAINS_LIST; no-op when no domains/no token).

 C. pipeline: 1.27.0 -> latest texts; site host from fm_site_host; caddyfile step_run static "v1"
 -> direct call (content-compare idempotent, no stale layer); compile branch constants ok.

 D. compile branch: static fingerprints "compile-official"/"compile-shims"/"compile-dnspod" -
 stale-skip risk: dnspod satisfied then prebuilt replaces binary -> skip re-embed. Since compile
 branch is the dnspod-embed path, make dnspod fingerprint dynamic: "(fmᵥersionₜag)-(module
 yes/no)". binary-install/shims: dynamic via version/link. Edit compile file.

 Prebuilt edit first.

 现在应用全部统一改造。A. prebuilt（常量统一+rc 修复+签发/互斥/清理补全）：


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_prebuilt.sh

      ...
   17 FRANKENPHP_PREBUILT_INDEX="93-install-prebuilt"
   18 SCRIPT_CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   19 FRANKENPHP_PREBUILT_NAMESPACE="93_install_frankenphp"
   20 FRANKENPHP_PREBUILT_PROJECT="php/frankenphp"
 - 21 FRANKENPHP_PREBUILT_DEFAULT_VERSION="1.27.0"
 - 22 FRANKENPHP_PREBUILT_CACHE_DIR="${COMPILE_DIR}/frankenphp/prebuilt"
 - 23 FRANKENPHP_PREBUILT_STAGING_DIR="${COMPILE_DIR}/frankenphp/prebuilt-staging"
 + 21 FRANKENPHP_PREBUILT_DEFAULT_VERSION="latest"
   24 FRANKENPHP_PREBUILT_BACKUP_SUFFIX=".prebuilt"
 - 25 FRANKENPHP_PREBUILT_INSTALL_BIN="/usr/local/bin/frankenphp"
   26
 FRANKENPHP_PREBUILT_ACME_INSTALL_SCRIPT="${SCRIPT_CURRENT_DIR}/frankenphp_acme_sh_install.sh"
   27 FRANKENPHP_PREBUILT_GITHUB_REPO="https://github.com/${FRANKENPHP_PREBUILT_PROJECT}"
   28 FRANKENPHP_PREBUILT_RELEASE_TAG=""
   29 FRANKENPHP_PREBUILT_RELEASE_CHANNEL=""
      ...
   36 source "$SCRIPT_CURRENT_DIR/file_ops_common.sh"
   37 source "$SCRIPT_CURRENT_DIR/step_state.sh"
   38 source "$SCRIPT_CURRENT_DIR/frankenphp_manager.sh"
   39
 + 37 # Unified binary/link + cache/staging path family (single source in the
 + 38 # manager + builder): FRANKENPHP_LINK_PATH, FRANKENPHP_PREBUILT_CACHE_DIR,
 + 39 # FRANKENPHP_PREBUILT_STAGING_DIR.
 + 40 FRANKENPHP_PREBUILT_INSTALL_BIN="${FRANKENPHP_LINK_PATH}"
 + 41
   40 frankenphp_prebuilt_set_release_tag() {
   41     local normalized_version=""
   42
   43     normalized_version="$(printf '%s'
 "${FRANKENPHP_PREBUILT_VERSION:-$FRANKENPHP_PREBUILT_DEFAULT_VERSION}" | tr -d '[:space:]')"
      ...
  297         $USE_SUDO cp "$FRANKENPHP_PREBUILT_INSTALL_BIN"
 "${FRANKENPHP_PREBUILT_INSTALL_BIN}${FRANKENPHP_PREBUILT_BACKUP_SUFFIX}" || return 1
  298     fi
  299     $USE_SUDO cp "$extracted_binary" "$FRANKENPHP_PREBUILT_INSTALL_BIN" || return 1
  300     $USE_SUDO chmod 755 "$FRANKENPHP_PREBUILT_INSTALL_BIN" || return 1
 -301     fm_ensure_local_bin_link
 -302     fm_ensure_php_cli_shim
 -303     rc=$?
 -304     if [ "$rc" -ne 0 ]; then
 -305         echo "[$FRANKENPHP_PREBUILT_INDEX] [WARN] failed to update local shims for prebuilt
 install"
 -306         return 1
 -307     fi
 +303     fm_ensure_local_bin_link || return 1
 +304     fm_ensure_php_cli_shim || return 1
  308     echo "[$FRANKENPHP_PREBUILT_INDEX] installed prebuilt FrankenPHP:
 $(frankenphp_prebuilt_expected_tag) from ${cache_path}"
  309     fm_store_info
  310     return 0
  311 }
      ...
  329
  330     if [ -x "$FRANKENPHP_PREBUILT_INSTALL_BIN" ]; then
  331         source "$FRANKENPHP_PREBUILT_ACME_INSTALL_SCRIPT"
  332         acme_sh_ensure_install
 +330         acme_sh_ensure_domains
 +331         # Plane mutex + apt PHP cleanup behind a healthy binary - identical
 +332         # gates to the compiled variant.
 +333         fm_disable_legacy_php_runtime
 +334         fm_static_apt_php_cleanup
  333     else
  334         echo "[$FRANKENPHP_PREBUILT_INDEX] [WARN] skip acme.sh install because
 $FRANKENPHP_PREBUILT_INSTALL_BIN is missing"
  335     fi
  336 }
      ...


 B. acme_sh: add issuance functions. Need manager's token constants - the acme file sources
 gvar/common_functions/step_state only, NOT the manager. It needs
 FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY + EMAIL + FRANKENPHP_ACME_CERT_DIR (builder family) - manager
 not sourced in acme file... but acme file is SOURCED from prebuilt AFTER manager is loaded ✓
 (constants available at call time). Standalone invocation (acme file as main) lacks them -> guard
 with defaults in the new functions or source manager in acme file? Simplest: source
 frankenphp_manager.sh in acme file too (idempotent source guard: manager has [[ BASH_SOURCE == $0
 ]] exit guard? It has if [[ "${BASH_SOURCE[0]}" = "${0}" ]] main block ✓ safe to source). Add
 source line.

 Also the acme file's own constants use COMPILE_DIR/acme.sh - > unify to FRANKENPHP_ACME_DIR
 (builder). Edit ACME_INSTALL_DIR = "{FRANKENPHP_ACME_DIR:-${COMPILE_DIR}/acme.sh}"? Better plain:
 source manager first then ACME_INSTALL_DIR="${FRANKENPHP_ACME_DIR}". Order: sources then
 constants... current file: constants BEFORE sources. Reorder minimal: keep as-is but make the
 assignment late? The constants ACME_INSTALL_DIR..ACME_INSTALL_LINK are used inside functions only
 (call-time) EXCEPT they're assigned at top with ${COMPILE_DIR} - change assignment to literal
 default then re-assign after source. Cleaner: move the 6 source lines above the constants? The
 file's variable-declaration-at-top rule stays satisfied. I'll restructure top: sources first,
 then constants referencing FRANKENPHP_ACME_DIR. And drop step_run wrappers there (static
 "prebuilt-v1" fingerprint: repo clone satisfied -> bootstrap skipped even if bin missing!
 bootstrap has own guard [ -x BIN ] -> fine; verify guarded by link presence; paths guarded by
 mkdir idempotent. The static fp risk: acme-paths satisfied -> mkdir skipped on a wiped dir ->
 later steps fail. Minor; but per doctrine drop wrappers - self-probing all).

 New issuance functions:

 ```
   acme_sh_account_email() {
       local email=""
       email="$(get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_EMAIL_SECRET_KEY")"
       [ -n "$email" ] || email="admin@${1:-localhost}"
       echo "$email"
   }

   # Issue (or keep) the DNSPod DNS-01 certificate for one apex domain:
   # apex + wildcard, acme.sh dns_dp provider, installed into the shared cert
   # dir the Caddyfile file-cert gate reads. acme.sh rc 2 = "Domains not
   # changed" - treated as success for idempotency.
   acme_sh_ensure_certificate() {
       local apex_domain="$1"
       local token_value=""
       local dp_id=""
       local dp_key=""
       local cert_dir=""
       local acme_bin=""
       local rc="0"

       [ -n "$apex_domain" ] || return 1
       acme_bin="$(acme_install_linked_binary)"
       [ -n "$acme_bin" ] || {
           echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh not usable yet; certificate
 deferred for ${apex_domain}"
           return 1
       }
       token_value="$(get_secret_key_from_common_functions "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY")"
       if [ -z "$token_value" ]; then
           echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] ${FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY}
 absent; certificate deferred for ${apex_domain}"
           return 1
       fi
       dp_id="${token_value%%,*}"
       dp_key="${token_value#*,}"
       cert_dir="${FRANKENPHP_ACME_CERT_DIR}/${apex_domain}"
       mkdir -p "$cert_dir"

       DP_Id="$dp_id" DP_Key="$dp_key" "$acme_bin" --issue --dns dns_dp \
           -d "$apex_domain" -d "*.${apex_domain}" \
           --server letsencrypt --keylength ec-256 >/dev/null 2>&1
       rc=$?
       if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ]; then
           echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh issue failed (rc=$rc) for
 ${apex_domain}"
           return 1
       fi

       if ! DP_Id="$dp_id" DP_Key="$dp_key" "$acme_bin" --install-cert -d "$apex_domain" --ecc \
           --fullchain-file "${cert_dir}/fullchain.pem" \
           --key-file "${cert_dir}/key.pem" >/dev/null 2>&1; then
           echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh install-cert failed for
 ${apex_domain}"
           return 1
       fi
       chmod 600 "${cert_dir}/key.pem" 2>/dev/null || true
       echo "[$FRANKENPHP_ACME_INSTALL_INDEX] DNS-01 certificate ready: ${cert_dir}
 (${apex_domain}, *.${apex_domain})"
       return 0
   }

   # Iterate the configured DOMAIN_DOMAINS_LIST (no-op when unset/empty).
   acme_sh_ensure_domains() {
       local apex_domain=""
       if [ -z "${DOMAIN_DOMAINS_LIST:-}" ]; then
           echo "[$FRANKENPHP_ACME_INSTALL_INDEX] no DOMAIN_DOMAINS_LIST configured; certificate
 issuance skipped"
           return 0
       fi
       while IFS= read -r apex_domain; do
           [ -n "$apex_domain" ] || continue
           acme_sh_ensure_certificate "$apex_domain" || return 0
       done <<EOF
   $(printf '%s\n' "$DOMAIN_DOMAINS_LIST")
   EOF
   }
 ```

 Note: --keylength ec-256 with --issue + --ecc on install-cert. acme.sh flags: --issue -d a -d
 '*.a' --dns dns_dp --keylength ec-256; install-cert -d a --ecc. Correct.

 || return 0 in ensure_domains: one domain failing shouldn't abort the whole install? Prefer
 continue but report; return 0 overall (deferred, not fatal) - matches "deferred" philosophy of
 compile branch. OK.

 Wildcard quoting: -d "*.${apex_domain}" ok.

 Variables at top of functions ✓. Now edit acme file: restructure sources+constants, drop
 step_run, append new functions.

 B. acme_sh 组件：路径统一到构建根 + 新增 DNSPod DNS-01 签发（apex+通配符、幂等、装到共享证书目录
 ）：


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n '15,40p'
 frankenphp_acme_sh_install.sh

 ... (20 earlier lines, ctrl+o to expand)
     local link_state=""
     local version_state=""

     if [ -d "$ACME_INSTALL_REPO_DIR/.git" ]; then
         repo_state="repo"

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_acme_sh_install.sh

      ...
   16 FRANKENPHP_ACME_INSTALL_INDEX="acme-sh-install"
   17 SCRIPT_CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
   18 ACME_COMMON_NAMESPACE="acme_sh_install"
   19 ACME_INSTALL_REPO="https://github.com/acmesh-official/acme.sh"
 - 20 ACME_INSTALL_DIR="${COMPILE_DIR}/acme.sh"
 - 21 ACME_INSTALL_REPO_DIR="${ACME_INSTALL_DIR}/repo"
 - 22 ACME_INSTALL_HOME_DIR="${ACME_INSTALL_DIR}/home"
 - 23 ACME_INSTALL_CONFIG_DIR="${ACME_INSTALL_HOME_DIR}/.acme.sh"
 - 24 ACME_INSTALL_BIN="${ACME_INSTALL_HOME_DIR}/acme.sh"
   25 ACME_INSTALL_LINK="/usr/local/bin/acme.sh"
   26
   27 source "$SCRIPT_CURRENT_DIR/gvar_common.sh"
   28 source "$SCRIPT_CURRENT_DIR/common_functions.sh"
   29 source "$SCRIPT_CURRENT_DIR/step_state.sh"
 + 25 source "$SCRIPT_CURRENT_DIR/frankenphp_manager.sh"
   30
 + 27 # Unified path family (builder single source): acme.sh lives under the same
 + 28 # persistent FrankenPHP build root as the sources and the prebuilt cache.
 + 29 ACME_INSTALL_DIR="${FRANKENPHP_ACME_DIR}"
 + 30 ACME_INSTALL_REPO_DIR="${ACME_INSTALL_DIR}/repo"
 + 31 ACME_INSTALL_HOME_DIR="${ACME_INSTALL_DIR}/home"
 + 32 ACME_INSTALL_CONFIG_DIR="${ACME_INSTALL_HOME_DIR}/.acme.sh"
 + 33 ACME_INSTALL_BIN="${ACME_INSTALL_HOME_DIR}/acme.sh"
 + 34
   31 ACME_INSTALL_FINGERPRINT="prebuilt-v1"
   32
   33 acme_install_fingerprint() {
   34     local repo_state=""
      ...


 Now append the issuance functions before the main block of the acme file. Find the main block:

 ```
   if [[ "${BASH_SOURCE[0]}" = "${0}" ]]; then
       acme_sh_ensure_install "$@"
   fi
 ```


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_acme_sh_install.sh

      ...
  162         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh link missing; manual retry may
 be required"
  163     fi
  164 }
  165
 +166 # Issue (or keep) the DNSPod DNS-01 certificate for one apex domain: apex +
 +167 # wildcard via the acme.sh dns_dp provider, installed into the shared cert
 +168 # dir the Caddyfile file-cert gate (fm_acme_cert_dir_for_host) reads.
 +169 # acme.sh rc 2 means "Domains not changed" - treated as success.
 +170 acme_sh_ensure_certificate() {
 +171     local apex_domain="$1"
 +172     local token_value=""
 +173     local dp_id=""
 +174     local dp_key=""
 +175     local cert_dir=""
 +176     local acme_bin=""
 +177     local rc="0"
 +178
 +179     [ -n "$apex_domain" ] || return 1
 +180     acme_bin="$(acme_install_linked_binary)"
 +181     if [ -z "$acme_bin" ]; then
 +182         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh not usable yet; certificate
 deferred for ${apex_domain}"
 +183         return 1
 +184     fi
 +185     token_value="$(get_secret_key_from_common_functions
 "$FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY")"
 +186     if [ -z "$token_value" ]; then
 +187         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] ${FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY}
 absent; certificate deferred for ${apex_domain}"
 +188         return 1
 +189     fi
 +190     dp_id="${token_value%%,*}"
 +191     dp_key="${token_value#*,}"
 +192     cert_dir="${FRANKENPHP_ACME_CERT_DIR}/${apex_domain}"
 +193     mkdir -p "$cert_dir"
 +194
 +195     DP_Id="$dp_id" DP_Key="$dp_key" "$acme_bin" --issue --dns dns_dp \
 +196         -d "$apex_domain" -d "*.${apex_domain}" \
 +197         --server letsencrypt --keylength ec-256 >/dev/null 2>&1
 +198     rc=$?
 +199     if [ "$rc" -ne 0 ] && [ "$rc" -ne 2 ]; then
 +200         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh issue failed (rc=$rc) for
 ${apex_domain}"
 +201         return 1
 +202     fi
 +203
 +204     if ! DP_Id="$dp_id" DP_Key="$dp_key" "$acme_bin" --install-cert -d "$apex_domain" --ecc
 \
 +205         --fullchain-file "${cert_dir}/fullchain.pem" \
 +206         --key-file "${cert_dir}/key.pem" >/dev/null 2>&1; then
 +207         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] [WARN] acme.sh install-cert failed for
 ${apex_domain}"
 +208         return 1
 +209     fi
 +210     chmod 600 "${cert_dir}/key.pem" 2>/dev/null || true
 +211     echo "[$FRANKENPHP_ACME_INSTALL_INDEX] DNS-01 certificate ready: ${cert_dir}
 (${apex_domain}, *.${apex_domain})"
 +212     return 0
 +213 }
 +214
 +215 # Iterate the configured DOMAIN_DOMAINS_LIST (no-op when unset/empty); a
 +216 # failing domain defers that certificate without aborting the install flow.
 +217 acme_sh_ensure_domains() {
 +218     local apex_domain=""
 +219
 +220     if [ -z "${DOMAIN_DOMAINS_LIST:-}" ]; then
 +221         echo "[$FRANKENPHP_ACME_INSTALL_INDEX] no DOMAIN_DOMAINS_LIST configured;
 certificate issuance skipped"
 +222         return 0
 +223     fi
 +224     while IFS= read -r apex_domain; do
 +225         [ -n "$apex_domain" ] || continue
 +226         acme_sh_ensure_certificate "$apex_domain" || true
 +227     done <<EOF
 +228 $(printf '%s\n' "$DOMAIN_DOMAINS_LIST")
 +229 EOF
 +230     return 0
 +231 }
 +232
  166 if [[ "${BASH_SOURCE[0]}" = "${0}" ]]; then
  167     acme_sh_ensure_install "$@"
  168 fi


 C. pipeline:
 1. 1.27.0 -> latest (3 spots).
 2. site host: FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}" -> use fm_site_host. But
    fm_site_host defined in manager sourced AFTER constants... assignment at top uses it before
    source. Move assignment: keep top default empty, and after sources set
    FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-$(fm_site_host)}". Current top has
    FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}"; after sources there's the PORT
    sc_get line - extend there.
 3. caddyfile step_run static "v1" -> direct call (content-compare idempotent).

 C. pipeline（版本默认 latest、site-host 走共享解析、Caddyfile 去 stale 层）：


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_pipeline.sh

      ...
   26 FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}"
   27 FRANKENPHP_SITE_PORT=""
   28 FRANKENPHP_ADMIN_PORT=""
   29 FRANKENPHP_LARAVEL_PUBLIC_DIR="${REPO_ROOT}/poly_apps/laravel_main/public"
 - 30 FRANKENPHP_PREBUILT_VERSION="${FRANKENPHP_PREBUILT_VERSION:-1.27.0}"
 + 30 FRANKENPHP_PREBUILT_VERSION="${FRANKENPHP_PREBUILT_VERSION:-latest}"
   31 FRANKENPHP_INSTALL_MODE="${FRANKENPHP_INSTALL_MODE:-}"
   32 FRANKENPHP_NO_MUTEX="false"
   33
   34 source "$SCRIPT_CURRENT_DIR/gvar_common.sh"
      ...
   38 source "$SCRIPT_CURRENT_DIR/frankenphp_manager.sh"
   39
   40 FRANKENPHP_SITE_PORT="${FRANKENPHP_SITE_PORT:-$(sc_get ports.frankenphp_https)}"
   41 FRANKENPHP_ADMIN_PORT="${FRANKENPHP_ADMIN_PORT:-$(sc_get ports.frankenphp_admin)}"
 + 42 # Shared site-host resolver (manager single source; env FRANKENPHP_SITE_HOST wins).
 + 43 FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-$(fm_site_host)}"
   42
   43 NGINX_PLANE_DISABLE_SCRIPT="${SCRIPT_CURRENT_DIR}/nginx_plane_disable.sh"
   44 CERTBOT_PLANE_DISABLE_SCRIPT="${SCRIPT_CURRENT_DIR}/certbot_plane_disable.sh"
   45
      ...
   98         esac
   99     fi
  100
  101     if [ "$FRANKENPHP_INSTALL_MODE" = "prebuilt" ] && [ -t 0 ] && [ -r /dev/tty ]; then
 -102         echo -n "[${FRANKENPHP_INSTALL_INDEX}] Use default prebuilt version
 ${FRANKENPHP_PREBUILT_VERSION:-1.27.0}? (Y/n): "
 +104         echo -n "[${FRANKENPHP_INSTALL_INDEX}] Use default prebuilt version
 ${FRANKENPHP_PREBUILT_VERSION:-latest}? (Y/n): "
  103         read -r tty_choice < /dev/tty || tty_choice=""
  104         selected_mode="$(printf '%s' "$tty_choice" | tr '[:upper:]' '[:lower:]')"
  105         if [ "$selected_mode" = "n" ] || [ "$selected_mode" = "no" ]; then
 -106             echo -n "[${FRANKENPHP_INSTALL_INDEX}] Enter prebuilt version (e.g. 1.27.0): "
 +108             echo -n "[${FRANKENPHP_INSTALL_INDEX}] Enter prebuilt version (e.g. latest or
 1.12.7): "
  107             read -r version_input < /dev/tty || version_input=""
  108             if [ -n "$version_input" ]; then
  109                 FRANKENPHP_PREBUILT_VERSION="$version_input"
  110             fi
      ...
  137     frankenphp_install_compile
  138 }
  139
  140 frankenphp_install_pipeline_run_caddyfile() {
 -141     step_run "$FRANKENPHP_STEP_NAMESPACE" "caddyfile" "v1" \
 -142         fm_caddyfile_ensure \
 +143     # Direct self-probing call: fm_caddyfile_ensure compares rendered content
 +144     # every run (no stale step-state layer can hide a deleted Caddyfile).
 +145     fm_caddyfile_ensure \
  143         "$FRANKENPHP_LARAVEL_PUBLIC_DIR" \
  144         "$FRANKENPHP_SITE_HOST" \
  145         "$FRANKENPHP_SITE_PORT" \
  146         "$FRANKENPHP_ADMIN_PORT" \
      ...


 Wait - the pipeline top has FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}" BEFORE
 sources; my new line after sources overrides with fm_site_host but only when still empty... the
 top line already set it to "localhost" (non-empty) -> my :-$(fm_site_host) never triggers! Fix:
 change the top line to keep empty default, or make post-source assignment
 unconditional-unless-env. Check top line and fix.


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n "FRANKENPHP_SITE_HOST="
 frankenphp_install_pipeline.sh

 26:FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}"
 43:FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-$(fm_site_host)}"

 Took 0.0s



 edit /tmp/placeholder.sed

 Could not edit file: /tmp/placeholder.sed. Error code: ENOENT.


 Oops, wrong call (I accidentally targeted a nonexistent file with placeholder text). Just edit
 the pipeline line 26 directly.


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_pipeline.sh

      ...
   22 FRANKENPHP_INSTALL_NAMESPACE="93_install_frankenphp"
   23
   24 FRANKENPHP_STEP_NAMESPACE="93_install_frankenphp"
   25 FRANKENPHP_CADDYFILE_DIR="${REPO_ROOT}/poly_apps/laravel_main/storage/frankenphp"
 - 26 FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-localhost}"
 + 26 FRANKENPHP_SITE_HOST="${FRANKENPHP_SITE_HOST:-}"
   27 FRANKENPHP_SITE_PORT=""
   28 FRANKENPHP_ADMIN_PORT=""
   29 FRANKENPHP_LARAVEL_PUBLIC_DIR="${REPO_ROOT}/poly_apps/laravel_main/public"
   30 FRANKENPHP_PREBUILT_VERSION="${FRANKENPHP_PREBUILT_VERSION:-latest}"
      ...


 D. compile branch - dynamic fingerprints (state-true):
 - binary-install fp: compile-$(fm_version_tag 2>/dev/null) - changes when binary changes.
   fm_version_tag exists? prebuilt uses fm_version_tag. Yes it referenced
   frankenphp_prebuilt_existing_version() -> fm_version_tag. ✓
 - binary-shims fp: readlink of link + shim presence.
 - dnspod fp: $(fm_version_tag)-$(fm_has_module ... && echo embedded || echo missing) -
   fm_has_module returns rc. In command substitution: $(fm_has_module "$FRANKENPHP_DNSPOD_MODULE"
   >/dev/null 2>&1 && echo embedded || echo missing).

 Current compile fingerprints computed as locals at fn top:

 ```
       local install_fingerprint="compile-official"
       local link_fingerprint="compile-shims"
       local dnspod_fingerprint="compile-dnspod"
 ```

 Replace with dynamic (command substitutions in local assignment are fine).

 D. compile 分支动态指纹（状态真值，防跨模式换二进制后 stale 跳过）：


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_compile.sh

     ...
  50     fm_store_info
  51 }
  52
  53 frankenphp_install_compile() {
 -54     local install_fingerprint="compile-official"
 -55     local link_fingerprint="compile-shims"
 -56     local dnspod_fingerprint="compile-dnspod"
 +54     # State-true fingerprints: a step re-runs whenever the underlying binary
 +55     # state changed (version / link / module presence), so a prebuilt binary
 +56     # installed in between can never leave a stale "satisfied" record.
 +57     local install_fingerprint="compile-$(fm_version_tag 2>/dev/null)"
 +58     local link_fingerprint="shims-$(readlink -f "$(fm_get_binary)" 2>/dev/null)"
 +59     local dnspod_fingerprint="dnspod-$(fm_version_tag 2>/dev/null)-$(fm_has_module
 "$FRANKENPHP_DNSPOD_MODULE" >/dev/null 2>&1 && echo embedded || echo missing)"
  57
  58     frankenphp_install_compile_init
  59
  60     step_run "$FRANKENPHP_INSTALL_COMPILE_NAMESPACE" "binary-install" "$install_fingerprint"
 \
     ...


 E. 175F branch: add fm_dnspod_token_ensure before fm_dns01_ensure (STEP 2). Check the current
 STEP 2 text in 175_laravel_main_start_frankenphp.sh.

 E. 175 分支接令牌种子。查 STEP 2 现文：


 $ cd /www/programing/core_node/scripts/shells/linux/debian && grep -n
 "fm_dns01_ensure\|fm_dnspod_token" debian_com/175_laravel_main_start_frankenphp.sh

 57:fm_dns01_ensure

 Took 0.0s


 fm_dns01_ensure already calls fm_dnspod_token_ensure internally (I added it as first line) - so
 175 needs no change ✓. Dedup principle: single call site inside fm_dns01_ensure covers both
 93-compile (via fm_ensure_dnspod_module? no - dnspod ensure in compile branch calls
 fm_ensure_dnspod_module, not fm_dns01_ensure) hmm:

 - 93 compile branch: fm_ensure_dnspod_module (build+install). Does it call fm_dns01_ensure? The
   success path likely includes token guidance. fm_dns01_ensure = 175 STEP 2. Token seeding should
   ALSO run in 93 compile flow. Add fm_dnspod_token_ensure call in fm_ensure_dnspod_module's
   success/early path? It's cheap+idempotent. Find fm_ensure_dnspod_module end (success path where
   fm_dns01-like message prints) - just append fm_dnspod_token_ensure before final echo in both
   paths? Simplest: call it at fn start too. Check fn.


 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n "$(grep -n
 'fm_ensure_dnspod_module()' frankenphp_manager.sh | cut -d: -f1),+10p" frankenphp_manager.sh

 ... (6 earlier lines, ctrl+o to expand)
         echo "[$SCRIPT_INDEX] [WARN] no frankenphp binary; run fm_install first"
         return 1
     fi
     if fm_module_in_bin "$binary" "$FRANKENPHP_DNSPOD_MODULE"; then
         echo "[$SCRIPT_INDEX] dnspod module already embedded"

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
  450 fm_ensure_dnspod_module() {
  451     local binary=""
  452     local candidate=""
  453
 +454     # Mirror the shared secret-file token into the runtime store first
 +455     # (idempotent; covers the 93 compile path, 175 re-uses fm_dns01_ensure).
 +456     fm_dnspod_token_ensure
  454     binary="$(fm_get_binary)"
  455     if [ -z "$binary" ]; then
  456         echo "[$SCRIPT_INDEX] [WARN] no frankenphp binary; run fm_install first"
  457         return 1
      ...


 Now syntax-check all touched files + verify the running build state (it may have reached the
 export line by now):

 语法校验全部触及文件 + 查看运行中构建进度：


 $ cd /www/programing/core_node/scripts/shells/linux/common && for f in frankenphp_manager.sh
 frankenphp_static_builder.sh frankenphp_install_pipeline.sh frankenphp_install_compile.sh
 frankenphp_install_prebuilt.sh frankenphp_acme_sh_install.sh; do bash -n "$f" && echo "OK $f" ||
 echo "FAIL $f"; done

 ... (1 earlier lines, ctrl+o to expand)
 OK frankenphp_static_builder.sh
 OK frankenphp_install_pipeline.sh
 OK frankenphp_install_compile.sh
 OK frankenphp_install_prebuilt.sh
 OK frankenphp_acme_sh_install.sh

 Took 0.1s



 $ grep -m2 "SPC_CMD_VAR_FRANKENPHP_XCADDY\|xcaddy build" /www/programing/frankenphp/build.log
 2>/dev/null | cut -c1-200; tail -2 /var/tmp/frankenphp_static_build.log | cut -c1-160; pgrep -cf
 "build-static.sh" || true

 + dl_arch=x86_64
 + curl -o spc -fsSL https://dl.static-php.dev/static-php-cli/spc-bin/nightly/spc-linux-x86_64
 1

 Took 0.1s


 All syntax OK. Build still downloading spc (slow). Fine - background.

 Now smoke the unified pieces WITHOUT running mutex/cleanup/build:
 1. fm_site_host with/without domain constants.
 2. fm_dnspod_token_value -> now falls back to secret file: should return the token (len 38).
 3. fm_dnspod_token_ensure with runtime ctx -> seeds store.
 4. fm_acme_cert_dir_for_host.
 5. Caddyfile render: with token + no module + no certs -> no tls block (built-in ACME).
 6. frankenphp_prebuilt_set_release_tag default latest -> URL releases/latest.

 Run smoke (safe: no mutex calls; fm_dns01_ensure now calls token_ensure - safe: seeds store;
 module absent -> prints deferred).

 构建仍在下载 spc。冒烟统一后的纯函数（不触碰 mutex/清理）：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 SCRIPT_INDEX=smoke source common/frankenphp_manager.sh
 echo "site_host(default): $(fm_site_host)"
 DOMAIN_SCOPE=cloudflare-resolve DOMAIN_API_PREFIX=sh DOMAIN_DOMAINS_LIST=$'"'"'example.com
 other.com'"'"' bash -c "source
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh >/dev/null 2>&1 ||
 true" 2>/dev/null
 DOMAIN_SCOPE=cloudflare-resolve DOMAIN_API_PREFIX=sh DOMAIN_DOMAINS_LIST="example.com" bash -c
 "SCRIPT_INDEX=s bash /dev/stdin <<EOF
 $(sed -n \"1,120p\" /dev/null)
 EOF
 " 2>/dev/null
 t="$(fm_dnspod_token_value)"; echo "token_value: len ${#t} format_ok=$([ \"\${t%%,*}\" != \"\$t\"
 ] && echo yes)"
 DOMAIN_API_PREFIX=sh; export DOMAIN_API_PREFIX
 echo "acme_dir_for(api.sh.example.com): $(fm_acme_cert_dir_for_host api.sh.example.com)"
 fm_dns01_ensure
 ' 2>&1 | grep -v "^\[smoke\] DNS-01 pending" | head -8

 ... (2 earlier lines, ctrl+o to expand)
 bash: line 17: [: too many arguments
 token_value: len 39 format_ok=
 acme_dir_for(api.sh.example.com): /www/programing/frankenphp/certs/example.com
 [] DNSPod token seeded into the runtime store (from DNS_DNSPOD_API_TOKENS)
 [] DNS-01 deferred: dnspod module not embedded yet (built-in HTTP-01/TLS-ALPN-01 stays active)

 Took 8.8s


 Results:
 - fm_site_host default: localhost ✓
 - My inline DOMAIN_SCOPE test lines were garbled (quoting mess) - redo cleanly.
 - token_value: len 39 ✓ (file fallback works) but format_ok check broke due to quoting (the
   ${t%%,*} escaped wrong). The value has 39 chars incl newline? File is 39 bytes; strip -> maybe
   38 + newline. format test broken due to quoting, but fm_dns01_ensure seeded store → token
   non-empty and passed to store ✓.
 - acme_dir_for(api.sh.example.com) -> /www/programing/frankenphp/certs/example.com ✓ (prefix
   strip works).
 - fm_dns01_ensure: seeded store + deferred (module absent) ✓ — exactly the desired fine-grained
   behavior.
 - Note: SCRIPT_INDEX printed empty "[]" - because I set SCRIPT_INDEX=smoke on the source line but
   the manager uses SCRIPT_INDEX at call time... printed [] means SCRIPT_INDEX empty in subshell
   context. Cosmetic in smoke only (manager sets SCRIPT_INDEX="${SCRIPT_INDEX:-fm}" normally? It
   printed empty -> manager does not default SCRIPT_INDEX. Callers set it. In 93 pipeline
   SCRIPT_INDEX=93 ✓. Not an issue.

 Store seeded now - verify store round-trip (runtime_config_get returns it, len 38) and re-run
 ensure = no duplicate seed (no-op):

 核心链路已通（种子写入、fallback、apex 解析）。复核 store 回读与二次幂等：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 export PHP_BIN VENDOR_AUTOLOAD
 SCRIPT_INDEX=smoke source common/frankenphp_manager.sh
 v1="$(fm_dnspod_token_value)"
 id="${v1%%,*}"; rest="${v1#*,}"
 echo "store_roundtrip: len ${#v1} id_len ${#id} key_len ${#rest} has_comma=$([ "$id" != "$v1" ]
 && echo yes || echo no)"
 echo "--- second ensure (expect no seed line) ---"
 fm_dnspod_token_ensure
 echo "--- caddyfile render without module/certs (expect built-in ACME, no tls block) ---"
 tmp=$(mktemp -d)
 fm_caddyfile_ensure /tmp/pub api.sh.example.com 8443 2019 "$tmp/Caddyfile" | tail -1
 grep -c "tls " "$tmp/Caddyfile" || echo "tls-blocks: 0"
 rm -rf "$tmp"
 ' 2>&1 | head -8

 ... (2 earlier lines, ctrl+o to expand)
 [] DNSPod token seeded into the runtime store (from DNS_DNSPOD_API_TOKENS)
 --- caddyfile render without module/certs (expect built-in ACME, no tls block) ---
 [] Caddyfile rendered: /tmp/tmp.hPwTDujZip/Caddyfile (secrets stay env placeholders)
 0
 tls-blocks: 0

 Took 5.7s


 Two findings:

 1. Store roundtrip works (len 39 incl trailing? id_len 6, key_len 32, comma ✓ = id,token format
    correct).
 2. BUG: second ensure STILL seeds - "seeded into the runtime store" printed again! Because
    runtime_config_get returns empty?? But fm_dnspod_token_value's store-first path: v1 len 39
    came from STORE or from file fallback? If store worked, token_value=store value. In ensure:
    stored=$(runtime_config_get ...) - if that returns empty while fm_dnspod_token_value (same
    call!) returned value... Difference: runtime_config_get may need BOOTSTRAP_APP too? In my
    first smoke I exported BOOTSTRAP_APP; second smoke I did NOT export BOOTSTRAP_APP!
    runtime_config_get may require bootstrap context (Laravel app boot). That explains: without
    BOOTSTRAP_APP the store read fails -> stored empty -> seed each time. With full ctx it should
    no-op. Verify: third run WITH BOOTSTRAP_APP -> expect no seed line. Also confirm store
    actually holds the token (read via php tinker-ish runtime_config_get with full ctx).

 第二次仍种子=疑因缺 BOOTSTRAP_APP 上下文（store 读失败→误判空）。带全上下文复验：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 SCRIPT_INDEX=smoke source common/frankenphp_manager.sh
 echo "--- ensure with full ctx (expect NO seed line above this) ---"
 fm_dnspod_token_ensure
 echo "end-marker"
 ' 2>&1 | head -4

 --- ensure with full ctx (expect NO seed line above this) ---
 [] DNSPod token seeded into the runtime store (from DNS_DNSPOD_API_TOKENS)
 end-marker

 Took 5.4s


 Still seeding with FULL context -> runtime_config_get genuinely returns empty even though the
 first smoke's fm_dnspod_token_put wrote it. Possibilities:
 1. The put failed silently (fm_dnspod_token_put output was >/dev/null'd; its rc swallowed by ||
    true).
 2. runtime_config_get needs different env (APP context loads .env? Laravel main app key?).
 3. put/get key mismatch.

 Debug directly: call runtime_config_put + runtime_config_get for a probe key in isolation and see
 rc/stderr.

 隔离调试 store put/get：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 echo "--- put ---"; runtime_config_put SMOKE_PROBE_KEY "hello123"; echo "put_rc=$?"
 echo "--- get ---"; v="$(runtime_config_get SMOKE_PROBE_KEY)"; echo "get_rc=$? value=[$v]"
 echo "--- get DNSPOD_TOKEN ---"; d="$(runtime_config_get DNSPOD_TOKEN)"; echo "rc=$? len=${#d}"
 ' 2>&1 | tail -8

 ... (3 earlier lines, ctrl+o to expand)
 PHP Fatal error:  Uncaught ValueError: Path must not be empty in Command line code:6
 Stack trace:
 #0 {main}
   thrown in Command line code on line 6
 rc=255 len=0

 Took 3.6s


 runtime_config_get is broken in this shell context: the PHP one-liner inside expects $argv (extra
 args) - "Undefined variable $argv" + "Path must not be empty". So runtime_config_get passes args
 via -- argv pattern and my invocation... I called runtime_config_get normally - the function
 itself builds the php -r command. The warnings show the php -r code references $argv but php was
 invoked WITHOUT register_argc_argv or without passing args?? This worked earlier today
 (fm_dnspod_token_put in first smoke seeded "successfully" - or DID it? The seed message prints
 AFTER `fm_dnspod_token_put ... || true` - rc swallowed! So the put FAILED all along; store is
 EMPTY; get fails the same way).

 Why would php -r argv fail? php -r 'code' arg1 arg2 gives $argv only when register_argc_argv=On
 for CLI (default On). "Undefined variable $argv" + "Path must not be empty" (line 6 = probably
 $argv[2] used as path). Maybe the php shim /usr/local/bin/php -> frankenphp php-cli mode handles
 -r argv differently! The shim = `frankenphp php-cli`? Embedded PHP CLI argv behavior may differ
 (frankenphp's php-cli passes script args; -r code + args maybe not propagated).

 Check how runtime_config_common invokes php: read the function.

 php -r 的 argv 在 shim 下不工作（put 其实一直失败）。查 runtime_config_common 的调用方式：


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n "php -r\|PHP_BIN\|argv"
 runtime_config_common.sh | head -12; sed -n "$(grep -n 'runtime_config_get()'
 runtime_config_common.sh | cut -d: -f1),+14p" runtime_config_common.sh

 ... (22 earlier lines, ctrl+o to expand)
         $value = \App\Support\RuntimeConfigurationStore::get($key);
         if ($value !== null) {
             echo $value;
         }
     ' "$VENDOR_AUTOLOAD" "$BOOTSTRAP_APP" "$key"

 Took 0.0s


 The function uses php -r 'code' arg1 arg2 arg3 - args after -r code should land in $argv.
 Standard php CLI: php -r 'echo $argv[1];' a b works (register_argc_argv default On for CLI). But
 via the frankenphp shim it breaks ("Undefined variable $argv").

 Test directly which php breaks: /usr/local/bin/php (shim -> frankenphp php-cli) vs real
 /usr/bin/php.


 $ /usr/local/bin/php -r 'echo "argv_count="; var_dump(count($argv)); echo $argv[1] ?? "NO_ARGV";'
 A B 2>&1 | head -3; echo ===; readlink -f /usr/local/bin/php; /usr/bin/php -r 'echo "real_php=" .
 ($argv[1] ?? "NO_ARGV") . PHP_EOL;' A B 2>&1 | head -2

 ... (2 earlier lines, ctrl+o to expand)
 Countable|array, null given in Command line code:1
 Stack trace:
 ===
 /usr/local/bin/php
 real_php=A

 Took 0.5s


 Confirmed:
 - /usr/local/bin/php (frankenphp embedded CLI) does NOT populate $argv for -r code with trailing
   args.
 - /usr/bin/php (real PHP 8.5 apt CLI) works fine.
 - readlink -f /usr/local/bin/php -> itself = it's a REAL FILE (the shim script or binary copy?)
   not symlink. fm_ensure_php_cli_shim created... a wrapper script presumably calling frankenphp
   php-cli.

 This is a REAL compatibility break: any component using php -r code args via the shim breaks
 (runtime_config get/put, 175 internals, dnspod token store...). The user's earlier summary said
 "production swoole process 463754 uses /usr/local/bin/php (frankenphp shim)" - so shim is in
 active use!

 Fix at the shim level? Look at fm_ensure_php_cli_shim - what does the shim do? If shim = shell
 wrapper exec frankenphp php-cli "$@", then frankenphp php-cli -r 'code' args fails argv. Check
 frankenphp php-cli argv support: frankenphp php-cli -r 'code' -- arg1 arg2? PHP's -r needs args
 AFTER the code; frankenphp might need -- separator. Test variants quickly.

 shim 下 -r 不传 argv（真实 php 正常）。查 shim 实现并测 frankenphp php-cli 的 argv 传递形式：


 $ head -20 /usr/local/bin/php; echo ===frankenphp-direct===; /usr/bin/frankenphp php-cli -r 'echo
 "A:" . ($argv[1] ?? "NO") . "\n";' X Y 2>&1 | head -2; /usr/bin/frankenphp php-cli -r 'echo "B:"
 . ($argv[1] ?? "NO") . "\n";' -- X Y 2>&1 | head -2

 #!/bin/sh
 exec /usr/bin/frankenphp php-cli "$@"
 ===frankenphp-direct===
 A:NO
 B:NO

 Took 0.2s


 frankenphp php-cli (v1.12.7 prebuilt, no dnspod yet) does NOT pass extra args to -r code at all
 (both variants NO). Upstream limitation of the embedded CLI runner.

 Options:
 1. runtime_config_common hardening: replace argv-passing with env-var passing ($_ENV/getenv) -
    robust across real php AND embedded php-cli? getenv in -r code reads exported env ✓ standard.
    Rewrite the 3 php -r blocks to use getenv('RC_KEY') etc. This fixes ALL callers regardless of
    which php binary. But does frankenphp php-cli -r support env? getenv reads process env - yes,
    inherited ✓. stdin path (put) uses printf | php -r reading STDIN - already env-free ✓ but also
    uses $argv for key... convert to getenv.
    BUT WAIT: real-CLI /usr/bin/php will be PURGED after frankenphp succeeds
    (fm_static_apt_php_cleanup) - so embedded php-cli MUST work for the store. env-var approach
    works for both ✓. Also laravel runtime uses PHP via octane worker (frankenphp embedded) not
    -r; fine.
 2. Also fm_php_exec/other manager pieces might use -r with argv - grep manager + acme for php -r
    with positional args... acme none; manager: fm_module_in_bin uses frankenphp list-modules (no
    php -r). runtime_config_common is the main argv user; also 43/175 maybe use php -r argv
    patterns - grep whole shells tree for -r ' with $argv to fix uniformly? Per "merge common
    libs" - fix the shared runtime_config_common (3 blocks) and grep for other $argv users.

 Check other $argv users under common/ + install_shells + debian_com:

 php-cli -r 完全不传位置参数。统一修复方案：runtime_config_common 改用环境变量传参（真实/嵌入 PHP
 皆兼容）。先全量找 $argv 使用点：


 $ cd /www/programing/core_node/scripts/shells/linux && grep -rln '\$argv' common/*.sh
 debian/install_shells/*.sh debian/debian_com/*.sh 2>/dev/null

 common/composer_vendor_common.sh
 common/runtime_config_common.sh
 common/service_contract_common.sh
 debian/debian_com/laravel_upgrade_13.sh

 Took 0.1s



 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n '\$argv\|PHP_BIN'
 composer_vendor_common.sh service_contract_common.sh | head -12; echo ===runtime-put===; sed -n
 "$(grep -n 'runtime_config_put()' runtime_config_common.sh | cut -d: -f1),+16p"
 runtime_config_common.sh

 ... (24 earlier lines, ctrl+o to expand)
     ' "$VENDOR_AUTOLOAD" "$BOOTSTRAP_APP" "$key"
 }

 ensure_runtime_config_value() {
     local key="$1"

 Took 0.0s


 argv users:
 1. runtime_config_common.sh - 3 blocks (get/put/ensure_runtime_config_value + maybe more - read
    whole file, it's small).
 2. composer_vendor_common.sh:72 - require $argv[1] autoload probe (would break under shim ->
    returns rc!=0 -> probe fails -> maybe falls back? fix to env).
 3. service_contract_common.sh:39 - php -r with json file path + key (sc_get! Pipeline uses sc_get
    at source time! If it breaks under shim php... it uses plain php from PATH =
    /usr/local/bin/php = shim NOW -> sc_get BROKEN currently! Fix to env too.)
 4. laravel_upgrade_13.sh - check its usage.

 Strategy per "centralize shared" + robustness: rewrite all these php -r argv patterns to
 env-based (getenv). For runtime_config_common: keys via env
 RC_ARG_AUTOLOAD/RC_ARG_BOOTSTRAP/RC_ARG_KEY.

 For value PUT via stdin ✓ already env-free besides argv.

 Check laravel_upgrade_13 usage + read full runtime_config_common to catch all blocks.

 三处公共库 + 一处升级脚本都依赖 argv。逐一看全 runtime_config_common 与 laravel_upgrade_13 用法：


 $ cd /www/programing/core_node/scripts/shells/linux && wc -l common/runtime_config_common.sh;
 grep -n '\$argv' common/runtime_config_common.sh debian/debian_com/laravel_upgrade_13.sh

 ... (7 earlier lines, ctrl+o to expand)
 common/runtime_config_common.sh:54:        $bootstrap = $argv[2];
 common/runtime_config_common.sh:55:        $key = $argv[3];
 debian/debian_com/laravel_upgrade_13.sh:33:            require $argv[1];
 debian/debian_com/laravel_upgrade_13.sh:38:            $lock =
 json_decode(file_get_contents($argv[1]), true, 512, JSON_THROW_ON_ERROR);

 Took 0.0s



 read /www/programing/core_node/scripts/shells/linux/common/runtime_config_common.sh


 Rewrite runtime_config_common.sh to env-var passing (works on real php AND frankenphp embedded
 php-cli). Env names: RC_ARG_AUTOLOAD, RC_ARG_BOOTSTRAP, RC_ARG_KEY. All three blocks + keep
 everything else identical.

 改写 runtime_config_common.sh 为环境变量传参（真 PHP 与嵌入 php-cli 双兼容）：


 edit /www/programing/core_node/scripts/shells/linux/common/runtime_config_common.sh

     ...
  16 # store itself (key normalization, private permissions) lives in
  17 # poly_apps/laravel_main/app/Support/RuntimeConfigurationStore.php.
  18 #
  19 # Callers MUST provide before use: PHP_BIN, VENDOR_AUTOLOAD, BOOTSTRAP_APP.
 +20 # Arguments travel via environment variables, NEVER via $argv: the embedded
 +21 # frankenphp php-cli runner does not populate $argv for -r code, while
 +22 # getenv() works identically on the real php CLI and the embedded runner.
  20
  21 runtime_config_directory() {
 -22     "$PHP_BIN" -r '
 -23         $autoload = $argv[1];
 -24         $bootstrap = $argv[2];
 +25     RC_ARG_AUTOLOAD="$VENDOR_AUTOLOAD" RC_ARG_BOOTSTRAP="$BOOTSTRAP_APP" \
 +26         "$PHP_BIN" -r '
 +27         $autoload = getenv("RC_ARG_AUTOLOAD");
 +28         $bootstrap = getenv("RC_ARG_BOOTSTRAP");
  25         require $autoload;
  26         require $bootstrap;
  27         echo \App\Support\RuntimeConfigurationStore::directory();
 -28     ' "$VENDOR_AUTOLOAD" "$BOOTSTRAP_APP"
 +32     '
  29 }
  30
  31 runtime_config_get() {
  32     local key="$1"
  33
 -34     "$PHP_BIN" -r '
 -35         $autoload = $argv[1];
 -36         $bootstrap = $argv[2];
 -37         $key = $argv[3];
 +38     RC_ARG_AUTOLOAD="$VENDOR_AUTOLOAD" RC_ARG_BOOTSTRAP="$BOOTSTRAP_APP" RC_ARG_KEY="$key" \
 +39         "$PHP_BIN" -r '
 +40         $autoload = getenv("RC_ARG_AUTOLOAD");
 +41         $bootstrap = getenv("RC_ARG_BOOTSTRAP");
 +42         $key = getenv("RC_ARG_KEY");
  38         $value = null;
  39         require $autoload;
  40         require $bootstrap;
  41         $value = \App\Support\RuntimeConfigurationStore::get($key);
  42         if ($value !== null) {
  43             echo $value;
  44         }
 -45     ' "$VENDOR_AUTOLOAD" "$BOOTSTRAP_APP" "$key"
 +50     '
  46 }
  47
  48 runtime_config_put() {
  49     local key="$1"
  50     local value="$2"
  51
 -52     printf '%s' "$value" | "$PHP_BIN" -r '
 -53         $autoload = $argv[1];
 -54         $bootstrap = $argv[2];
 -55         $key = $argv[3];
 +57     printf '%s' "$value" | \
 +58         RC_ARG_AUTOLOAD="$VENDOR_AUTOLOAD" RC_ARG_BOOTSTRAP="$BOOTSTRAP_APP"
 RC_ARG_KEY="$key" \
 +59         "$PHP_BIN" -r '
 +60         $autoload = getenv("RC_ARG_AUTOLOAD");
 +61         $bootstrap = getenv("RC_ARG_BOOTSTRAP");
 +62         $key = getenv("RC_ARG_KEY");
  56         $value = trim(stream_get_contents(STDIN));
  57         require $autoload;
  58         require $bootstrap;
  59         exit(\App\Support\RuntimeConfigurationStore::put($key, $value) ? 0 : 1);
 -60     ' "$VENDOR_AUTOLOAD" "$BOOTSTRAP_APP" "$key"
 +67     '
  61 }
  62
  63 ensure_runtime_config_value() {
  64     local key="$1"
     ...


 Now fix the other argv users:
 1. service_contract_common.sh:39 sc_get - env vars for file+key.
 2. composer_vendor_common.sh:72 - require $argv[1] -> env.
 3. laravel_upgrade_13.sh:33,38 - env.

 修其余三处 argv 用法（sc_get / composer 探测 / laravel_upgrade_13）：


 $ cd /www/programing/core_node/scripts/shells/linux && sed -n '30,45p'
 common/service_contract_common.sh; echo ===; sed -n '66,78p' common/composer_vendor_common.sh;
 echo ===; sed -n '28,44p' debian/debian_com/laravel_upgrade_13.sh

 ... (48 earlier lines, ctrl+o to expand)
             foreach ($packages as $package) {
                 if (($package["name"] ?? null) === "laravel/framework") {
                     echo $package["version"] ?? "";
                     break;
                 }

 Took 0.0s



 edit /www/programing/core_node/scripts/shells/linux/common/service_contract_common.sh

     ...
  35         node -e 'const c=JSON.parse(require("fs").readFileSync(process.argv[1],"utf8"));const

 v=process.argv[2].split(".").reduce((o,p)=>(o==null?o:o[p]),c);process.stdout.write(v==null?"":St
 ring(v));' "$SERVICE_CONTRACT_FILE" "$key" 2>/dev/null
  36         return 0
  37     fi
  38     if command -v php >/dev/null 2>&1; then
 -39 php -r '$c=json_decode(file_get_contents($argv[1]),true);foreach(explode(".",$argv[2])as
 $p){$c=is_array($c)&&array_key_exists($p,$c)?$c[$p]:null;}echo $c===null?"":$c;'
 "$SERVICE_CONTRACT_FILE" "$key" 2>/dev/null
 +39         SC_ARG_FILE="$SERVICE_CONTRACT_FILE" SC_ARG_KEY="$key" php -r
 '$c=json_decode(file_get_contents(getenv("SC_ARG_FILE")),true);foreach(explode(".",getenv("SC_ARG
 _KEY")) as $p){$c=is_array($c)&&array_key_exists($p,$c)?$c[$p]:null;}echo $c===null?"":$c;'
 2>/dev/null
  40         return 0
  41     fi
  42     return 0
  43 }
     ...



 edit /www/programing/core_node/scripts/shells/linux/common/composer_vendor_common.sh

      ...
   68     local app_dir="$1"
   69     local probe_out=""
   70     COMPOSER_VENDOR_AUTOLOAD_OK="no"
   71     if [ -n "$COMPOSER_VENDOR_PHP" ] && [ -f "${app_dir}/vendor/autoload.php" ]; then
 - 72         probe_out="$("$COMPOSER_VENDOR_PHP" -r 'require $argv[1];'
 "${app_dir}/vendor/autoload.php" 2>&1)"
 + 72         probe_out="$(COMPOSER_VENDOR_PROBE="${app_dir}/vendor/autoload.php"
 "$COMPOSER_VENDOR_PHP" -r 'require getenv("COMPOSER_VENDOR_PROBE");' 2>&1)"
   73         if [ -z "$probe_out" ]; then
   74             COMPOSER_VENDOR_AUTOLOAD_OK="yes"
   75         fi
   76     fi
      ...



 edit /www/programing/core_node/scripts/shells/linux/debian/debian_com/laravel_upgrade_13.sh

      ...
   28     local version=""
   29     local major=""
   30
   31     if [ -f "$autoload_path" ]; then
 - 32         version="$($PHP_BIN -r '
 - 33             require $argv[1];
 + 32         version="$(LARAVEL_UPGRADE_AUTOLOAD="$autoload_path" $PHP_BIN -r '
 + 33             require getenv("LARAVEL_UPGRADE_AUTOLOAD");
   34             echo \Composer\InstalledVersions::getPrettyVersion("laravel/framework") ?? "";
 - 35         ' "$autoload_path" 2>/dev/null)"
 + 35         ' 2>/dev/null)"
   36     elif [ -f "$lock_path" ]; then
 - 37         version="$($PHP_BIN -r '
 - 38             $lock = json_decode(file_get_contents($argv[1]), true, 512,
 JSON_THROW_ON_ERROR);
 + 37         version="$(LARAVEL_UPGRADE_LOCK="$lock_path" $PHP_BIN -r '
 + 38             $lock = json_decode(file_get_contents(getenv("LARAVEL_UPGRADE_LOCK")), true,
 512, JSON_THROW_ON_ERROR);
   39             $packages = array_merge($lock["packages"] ?? [], $lock["packages-dev"] ?? []);
   40             foreach ($packages as $package) {
   41                 if (($package["name"] ?? null) === "laravel/framework") {
   42                     echo $package["version"] ?? "";
      ...


 All argv users fixed. Now verify with the EMBEDDED php (shim) end-to-end:
 1. runtime_config_put + get roundtrip via /usr/local/bin/php (shim) - the hard case.
 2. Then fm_dnspod_token_ensure second-run no-op (proves idempotency).

 用 shim（嵌入 php-cli）端到端复验 store put/get 与二次种子幂等：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -n common/runtime_config_common.sh &&
 bash -n common/service_contract_common.sh && bash -n common/composer_vendor_common.sh && bash -n
 debian/debian_com/laravel_upgrade_13.sh && echo SYNTAX-OK && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 runtime_config_put SMOKE_PROBE_KEY "hello123"; echo "put_rc=$?"
 v="$(runtime_config_get SMOKE_PROBE_KEY)"; echo "get=[$v] rc=$?"
 SCRIPT_INDEX=smoke source common/frankenphp_manager.sh
 echo "--- ensure run1 (store empty for DNSPOD_TOKEN -> seed) ---"
 fm_dnspod_token_ensure
 echo "--- ensure run2 (expect NO seed line) ---"
 fm_dnspod_token_ensure
 echo "end"
 d="$(runtime_config_get DNSPOD_TOKEN)"; echo "store_dns len=${#d}"
 ' 2>&1 | tail -7

 ... (3 earlier lines, ctrl+o to expand)
 --- ensure run1 (store empty for DNSPOD_TOKEN -> seed) ---
 [] DNSPod token seeded into the runtime store (from DNS_DNSPOD_API_TOKENS)
 --- ensure run2 (expect NO seed line) ---
 end
 store_dns len=39

 Took 6.5s


 PERFECT:
 - put/get roundtrip via the embedded php shim: works ✓
 - ensure run1: seeds ✓
 - ensure run2: silent no-op ✓ (true idempotency now)
 - store holds DNSPOD_TOKEN (len 39) ✓

 This was a REAL production bug found & fixed (token was never actually stored before - the argv
 bug broke every put).

 Cleanup: remove SMOKE_PROBE_KEY from the store (my test key). Then continue:
 1. Verify build progress.
 2. Check the prebuilt asset URL naming: does php/frankenphp release have frankenphp-linux-x86_64?
    Official releases: assets like frankenphp-linux-x86_64 (static), also ...macOS,
    test-version.sh. With glibc variants? Current releases (v1.12.x): frankenphp-linux-x86_64
    (musl static) ✓. The candidates function tries plain, .tgz, -gnu, .tgz, legacy amd64 names ✓
    reasonable.
 3. Verify frankenphp_prebuilt_set_release_tag default -> latest URL:
    releases/latest/download/frankenphp-linux-x86_64 - HEAD should 302->200 ✓. Quick curl check
    (fast, network ok).
 4. mutex flow sanity: fm_disable_legacy_php_runtime still plane-gated (compile variant). For
    prebuilt path now called unconditionally after install (gated internally by plane + binary
    sanity ✓).
 5. Also 53/43 wire-ins still intact (grep).
 6. Also confirm pipeline caddyfile + finalize still fine (fm_store_info+fm_verify exist).

 Then final summary. Also clean SMOKE key.

 store 经 shim 完整打通，二次运行静默幂等 ✓（此前 argv 缺陷导致令牌从未真正落库——已根治）。清理探
 测键并验证 prebuilt latest 资产 URL 真实可达：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP
 printf "" | RC_ARG_AUTOLOAD="$VENDOR_AUTOLOAD" RC_ARG_BOOTSTRAP="$BOOTSTRAP_APP"
 RC_ARG_KEY=SMOKE_PROBE_KEY "$PHP_BIN" -r "
 \$autoload=getenv(\"RC_ARG_AUTOLOAD\");\$bootstrap=getenv(\"RC_ARG_BOOTSTRAP\");\$key=getenv(\"RC
 _ARG_KEY\");
 require \$autoload; require \$bootstrap;
 exit(\\App\\Support\\RuntimeConfigurationStore::put(\$key, \"\") ? 0 : 1);
 " 2>/dev/null; echo "probe-key cleared rc=$?"'

 probe-key cleared rc=1

 Took 3.3s



 $ curl -sL -o /dev/null -I -w '%{http_code}\n' --max-time 20
 https://github.com/php/frankenphp/releases/latest/download/frankenphp-linux-x86_64 2>/dev/null;
 grep -m1 "SPC_CMD_VAR_FRANKENPHP_XCADDY\|xcaddy build" /www/programing/frankenphp/build.log
 2>/dev/null | cut -c1-120 || echo "xcaddy-not-yet"

 200

 Took 0.8s


 1. Probe-key clear: rc=1 - RuntimeConfigurationStore::put with empty value maybe rejected
    (validation) or my inline quoting broke. Use runtime_config_put with non-empty then...
    simpler: put a non-empty dummy then it doesn't matter; actually try put "0"? Or check store
    API - maybe empty not allowed by key normalization/validation. Not critical: leave
    SMOKE_PROBE_KEY? It pollutes prod store. Try clearing via runtime_config_put with a space?
    Trim would strip->empty again. Look: maybe put() validates non-empty. Just overwrite with "-"
    harmless. Actually check if RuntimeConfigurationStore has delete... The shell adapter has no
    delete. Overwrite "-" acceptable but pollutes. Let me check the PHP class quickly for allowed
    empty/delete.

 2. Release asset URL 200 ✓ (prebuilt latest works).

 3. Build log: no xcaddy line yet (grep printed nothing? It fell to || echo? No - the grep line
    output nothing and no "xcaddy-not-yet"... because grep -m1 found nothing but rc!=0 -> || echo
    should print... Output shows only "200" from first command block; second command output
    missing?? The two results got merged: first result "probe-key cleared rc=1", second "200" - so
    the grep+echo part printed NOTHING? grep no match -> || echo "xcaddy-not-yet" should have
    printed. Unless grep matched but cut output empty? No... Actually the || binds to cut, and cut
    succeeds even on empty input! So no echo. Sloppy one-liner; whatever - check build progress
    separately.


 $ grep -n "put(\|delete(\|function "
 /www/programing/core_node/poly_apps/laravel_main/app/Support/RuntimeConfigurationStore.php | head
 -8; tail -3 /var/tmp/frankenphp_static_build.log | cut -c1-140

 ... (4 earlier lines, ctrl+o to expand)
 65:    private static function normalizeKey(string $key): string
 72:    private function __construct()
 + [[ x86_64 =~ arm ]]
 + dl_arch=x86_64
 + curl -o spc -fsSL https://dl.static-php.dev/static-php-cli/spc-bin/nightly/spc-linux-x86_64

 Took 0.0s


 Store has no delete (put with empty likely rejected by validation -> rc=1).
 SMOKE_PROBE_KEY="hello123" remains in store - harmless test residue but let me check put's
 validation (line 40-58) - if value non-empty required, overwrite trick won't remove. Minor
 pollution; acceptable? Per cleanliness, try put with a single space -> trim strips to "" -> same
 reject. Leave it - it's an inert key in a private store; note it. Actually check put body
 quickly.


 $ sed -n '40,58p'
 /www/programing/core_node/poly_apps/laravel_main/app/Support/RuntimeConfigurationStore.php; ls
 /www/programing/core_node/poly_apps/laravel_main/storage/app/runtime-configuration/ 2>/dev/null |
 grep -i smoke

 ... (17 earlier lines, ctrl+o to expand)
         return FileSystemManager::writePrivateFile($path, $value."\n");
     }


 Command exited with code 1

 Took 0.0s


 put() rejects empty values (by design) - so my probe key file exists in storage dir? The grep for
 "smoke" in storage found nothing (exit 1) - because normalizeKey lowercases? "SMOKE_PROBE_KEY" ->
 normalized maybe "smoke_probe_key". Check with case-insensitive... the grep WAS case-insensitive
 (-i smoke) and found NOTHING. So where did get() read "hello123" from?! get reads
 path(normalizeKey) - if no file, get returns default null -> empty. But my test showed
 get=[hello123] rc=0! So file EXISTS. Find it:


 $ sed -n '60,70p'
 /www/programing/core_node/poly_apps/laravel_main/app/Support/RuntimeConfigurationStore.php; find
 /www/programing/core_node/poly_apps/laravel_main/storage -name "*smoke*" -o -name "*SMOKE*"
 2>/dev/null | head -3

 ... (6 earlier lines, ctrl+o to expand)
     {
         $normalizedKey = preg_replace('/[^A-Z0-9_]/', '', strtoupper($key));

         return is_string($normalizedKey) ? $normalizedKey : '';
     }

 Took 8.5s


 normalizeKey = uppercase. File should be storage/.../SMOKE_PROBE_KEY. My earlier ls path was
 wrong (directory() may point elsewhere, e.g. storage_path('app/private/runtime-configuration') or
 similar). find returned nothing under storage?? Let me get directory() directly and remove the
 file (rm = cleanup of MY OWN test artifact, not destructive to user data).


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 export PHP_BIN VENDOR_AUTOLOAD BOOTSTRAP_APP RC_ARG_AUTOLOAD="$VENDOR_AUTOLOAD"
 RC_ARG_BOOTSTRAP="$BOOTSTRAP_APP"
 d="$("$PHP_BIN" -r
 "\$autoload=getenv(\"RC_ARG_AUTOLOAD\");\$bootstrap=getenv(\"RC_ARG_BOOTSTRAP\");require
 \$autoload;require \$bootstrap;echo \\App\\Support\\RuntimeConfigurationStore::directory();"
 2>/dev/null)"
 echo "dir=$d"; ls "$d" | head; rm -f "$d/SMOKE_PROBE_KEY"; echo "removed rc=$?"' 2>&1 | tail -4

 MERCURE_SUBSCRIBER_JWT
 MERCURE_TRUSTED_ISSUERS
 POSTGRES_PASSWORD
 removed rc=0

 Took 4.2s


 Test key removed. (dir output got truncated but rm succeeded; ls shows MERCURE_* /
 POSTGRES_PASSWORD keys - real keys present, DNSPOD_TOKEN presumably too.)

 Now remaining verification:
 1. Build progress - still spc download (slow, background).
 2. Mutex/43/53 wiring intact after all edits: grep.
 3. Prebuilt flow smoke WITHOUT actually installing (the binary is already installed via compile
    variant... careful: running frankenphp_install_prebuilt would DOWNLOAD and REPLACE
    /usr/local/bin/frankenphp with prebuilt latest + RUN MUTEX + purge apt php! DO NOT RUN.
    Instead smoke only set_release_tag + select_asset_url + fingerprint logic in a dry subshell
    (no install).
 4. Pipeline bash -n done ✓.

 Also: 53_install_swoole + 43 wire-ins reference fm_disable_legacy_php_runtime - confirm manager
 still exports them (sourced not subshell) ✓ (grep earlier showed 3 fns exist).

 One more integration point: the concurrent editor may still be editing. ls -la timestamps again
 to make sure no conflict happened to my edited files.

 Dry-run smoke of prebuilt tag/asset selection:

 测试键已清除。干跑 prebuilt 选择逻辑（不落盘不互斥）并复查 43/53 挂接点：


 $ cd /www/programing/core_node/scripts/shells/linux && bash -c '
 source common/gvar_common.sh >/dev/null 2>&1
 source common/common_functions.sh >/dev/null 2>&1
 source common/runtime_config_common.sh >/dev/null 2>&1
 export PHP_BIN=/usr/local/bin/php
 VENDOR_AUTOLOAD=/www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 BOOTSTRAP_APP=/www/programing/core_node/poly_apps/laravel_main/bootstrap/app.php
 SCRIPT_INDEX=smoke source common/frankenphp_manager.sh >/dev/null 2>&1
 source common/frankenphp_install_prebuilt.sh >/dev/null 2>&1
 export FRANKENPHP_PREBUILT_VERSION=""
 frankenphp_prebuilt_set_release_tag
 frankenphp_prebuilt_detect_arch
 echo "tag=$FRANKENPHP_PREBUILT_RELEASE_TAG url=$FRANKENPHP_PREBUILT_RELEASE_URL
 arch=$FRANKENPHP_PREBUILT_ARCH"
 echo "install_bin=$FRANKENPHP_PREBUILT_INSTALL_BIN cache=$FRANKENPHP_PREBUILT_CACHE_DIR"
 sel="$(frankenphp_prebuilt_select_asset_url)"; echo "asset=${sel%%|*}"
 echo "is_expected_now=$(frankenphp_prebuilt_is_expected_binary)
 (installed=$(frankenphp_prebuilt_existing_version))"
 ' 2>/dev/null | head -6

 tag=latest url=https://github.com/php/frankenphp/releases/latest/download arch=x86_64
 install_bin=/usr/local/bin/frankenphp cache=/www/programing/frankenphp/prebuilt
 asset=https://github.com/php/frankenphp/releases/latest/download/frankenphp-linux-x86_64
 is_expected_now=no (installed=v1.12.7)

 Took 10.1s



 $ cd /www/programing/core_node/scripts/shells/linux && grep -c "fm_disable_legacy_php_runtime"
 debian/install_shells/53_install_swoole.sh debian/install_shells/43_ensure_php85_intelligent.sh
 debian/debian_com/175_laravel_main_start_frankenphp.sh common/frankenphp_install_prebuilt.sh
 2>/dev/null; ls -la common/frankenphp_install_prebuilt.sh common/frankenphp_manager.sh

 ... (1 earlier lines, ctrl+o to expand)
 debian/install_shells/43_ensure_php85_intelligent.sh:1
 debian/debian_com/175_laravel_main_start_frankenphp.sh:0
 common/frankenphp_install_prebuilt.sh:1
 -rw-r--r-- 1 root       root       11926 Aug 18 18:51 common/frankenphp_install_prebuilt.sh
 -rwxrwxrwx 1 lighthouse lighthouse 27470 Aug 18 18:53 common/frankenphp_manager.sh

 Took 0.0s


 All good:
 - Prebuilt dry-run: tag=latest, URL resolves, x86_64 asset selected (200 confirmed earlier),
   install_bin + cache unified under the shared family ✓. is_expected=no (installed v1.12.7 -
   pinned version differs from latest; expected until user runs prebuilt mode).
 - Mutex wired: 53 ✓, 43 ✓, prebuilt ✓. 175F: 0 direct calls - correct, its STEP path goes
   fm_ensure_dnspod_module/fm_dns01_ensure which internally call mutex/seed (by design, dedup).

 Wait - one gap: 175F STEP 1 does plane stack convergence but does it call
 fm_disable_legacy_php_runtime? Earlier design: mutex called in fm_ensure_dnspod_module
 early+success paths ✓ (so 175F STEP1's fm_ensure_dnspod_module triggers it). OK.

 Build: still downloading spc nightly (that server is slow; earlier it recovered via mirror). Keep
 waiting in background - it's the compile variant. Nothing else to do for it now.

 Remaining polish per user's asks:
 1. "注意互斥一清理其他" - mutex cleans OTHER variants: compile-success path replaced prebuilt
    backup? fm_ensure_dnspod_module success path: installs candidate -> old binary backed up
    .pre-dnspod; if previous binary was prebuilt variant -> old .prebuilt backup remains (harmless
    artifact in /usr/local/bin). Cleanup: after a successful dnspod install, remove stale
    /usr/local/bin/frankenphp.prebuilt? And prebuilt install removes .pre-dnspod? "互斥一清理其他"
    likely means exactly this cross-variant residue cleanup. Add small cross-cleanup:
     - fm_ensure_dnspod_module success: rm -f "${FRANKENPHP_LINK_PATH}.prebuilt"
     - prebuilt install_binary success: rm -f "FRANKENPHP_LINK_PATH{FRANKENPHP_BACKUP_SUFFIX}"...
       no wait its OWN backup suffix .prebuilt is made pre-replace; the dnspod one is .pre-dnspod.
       Cross-cleanup: prebuilt success -> rm -f "${FRANKENPHP_LINK_PATH}.pre-dnspod". Add both.
       Also acme certs vs dnspod: when dnspod embedded + token -> Caddyfile gate prefers dnspod
       (file certs unused; leave files, gate order handles).

 2. Manager: check fm_static_apt_php_cleanup gate uses binary sanity only (not module) - since
    prebuilt path also calls it now. Verify its gate quickly.

 Implement cross-cleanup then verify. Find install success section in fm_ensure_dnspod_module
 (candidate install + verify) - the $USE_SUDO install / mv line and prebuilt's cp block (already
 edited). Add rm lines.

 互斥清理补全：成功切换到某变体后清除另一变体的残留备份。定位两处成功点：


 $ cd /www/programing/core_node/scripts/shells/linux/common && grep -n "FRANKENPHP_BACKUP_SUFFIX"
 frankenphp_manager.sh | head -4; grep -n "fm_static_apt_php_cleanup()" frankenphp_manager.sh; sed
 -n "$(grep -n 'fm_static_apt_php_cleanup()' frankenphp_manager.sh | cut -d: -f1),+8p"
 frankenphp_manager.sh

 ... (4 earlier lines, ctrl+o to expand)
 ${binary}${FRANKENPHP_BACKUP_SUFFIX}) ($(fm_version))"
 sed: -e expression #1, char 1: unknown command: `,'


 Command exited with code 1

 Took 0.0s



 $ cd /www/programing/core_node/scripts/shells/linux/common && sed -n '480,496p'
 frankenphp_manager.sh; grep -n "apt_php_cleanup\|fm_static_apt" frankenphp_manager.sh | head -3

 ... (16 earlier lines, ctrl+o to expand)
     fm_store_info
     fm_disable_legacy_php_runtime
 372:#   3) every loaded php*-fpm unit stops + disables (fm_static_apt_php_cleanup
 448:# PHP stack is purged (fm_static_apt_php_cleanup): the static binary is
 466:        fm_static_apt_php_cleanup

 Took 0.0s


 Line 466 - inside fm_ensure_dnspod_module? Read around 460-470 to see which fn calls cleanup
 (early path when module already embedded?). And the apt cleanup fn itself - search its definition
 (it's in apt cleanup area maybe named differently). grep "fm_static_apt_php_cleanup()" returned
 nothing earlier -> maybe defined in another file (3_setting or its own)? grep whole common.


 $ cd /www/programing/core_node/scripts/shells/linux && grep -rn "fm_static_apt_php_cleanup()"
 common/*.sh | head -2; sed -n '456,470p' common/frankenphp_manager.sh

 ... (11 earlier lines, ctrl+o to expand)
         fm_static_apt_php_cleanup
         return 0
     fi

     candidate="$(fm_dnspod_build_static)"

 Took 0.0s


 Structure confirmed. Check fm_static_apt_php_cleanup gate (builder:223) quickly, then add
 cross-cleanup lines:

 1. In fm_ensure_dnspod_module success path (after fm_disable_legacy_php_runtime at ~495): add rm
    -f "${FRANKENPHP_LINK_PATH}.prebuilt" (remove the other variant's stale backup).
 2. In prebuilt install_binary success (after fm_store_info): add rm -f
    "${FRANKENPHP_PREBUILT_INSTALL_BIN}${FRANKENPHP_BACKUP_SUFFIX}"... no: prebuilt KEEPS its own
    .prebuilt backup (pre-replace). Cross-cleanup = remove the DNSSPOD variant backup: rm -f
    "${FRANKENPHP_PREBUILT_INSTALL_BIN}.pre-dnspod".


 $ sed -n '223,240p'
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_static_builder.sh

 ... (13 earlier lines, ctrl+o to expand)
         echo "[$FM_STATIC_LOG_TAG] apt PHP already clean (no packages to purge)"
         return 0
     fi

     for fpm_pkg in $(echo "$pkgs" | grep -E 'php.*fpm' || true); do

 Took 0.0s


 Cleanup gate = binary sanity only (no module check) ✓ safe for both variants.

 Now add the two cross-cleanup lines:

 清理门=仅二进制健康 ✓（两变体通用）。补互斥残留清理：


 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager.sh

      ...
  491     $USE_SUDO chmod 755 "${binary}.dnspod-new"
  492     $USE_SUDO mv -f "${binary}.dnspod-new" "$binary"
  493     rm -rf "$(dirname "$candidate")"
  494     echo "[$SCRIPT_INDEX] dnspod module embedded (backup:
 ${binary}${FRANKENPHP_BACKUP_SUFFIX}) ($(fm_version))"
 +495     # Variant mutex: the compiled variant now owns the link - drop a stale
 +496     # prebuilt-variant backup left behind by an earlier prebuilt install.
 +497     [ "$binary" = "$FRANKENPHP_LINK_PATH" ] && rm -f "${FRANKENPHP_LINK_PATH}.prebuilt"
  495     fm_store_info
  496     fm_disable_legacy_php_runtime
  497     fm_static_apt_php_cleanup
  498 }
      ...



 edit /www/programing/core_node/scripts/shells/linux/common/frankenphp_install_prebuilt.sh

      ...
  302     $USE_SUDO chmod 755 "$FRANKENPHP_PREBUILT_INSTALL_BIN" || return 1
  303     fm_ensure_local_bin_link || return 1
  304     fm_ensure_php_cli_shim || return 1
  305     echo "[$FRANKENPHP_PREBUILT_INDEX] installed prebuilt FrankenPHP:
 $(frankenphp_prebuilt_expected_tag) from ${cache_path}"
 +306     # Variant mutex: the prebuilt variant now owns the link - drop a stale
 +307     # compiled-variant backup left behind by an earlier dnspod rebuild.
 +308     rm -f "${FRANKENPHP_PREBUILT_INSTALL_BIN}.pre-dnspod"
  306     fm_store_info
  307     return 0
  308 }
  309
      ...


 Final syntax pass on all touched files + build status check + a summary. Also verify no lingering
 step_run references to removed helper ACME_INSTALL_FINGERPRINT (acme file kept step_run
 internally with fingerprint fn - the concurrent editor's dynamic fingerprint; I only changed
 constants + appended. bash -n again).

 最终校验全链 + 构建进度：


 $ cd /www/programing/core_node/scripts/shells/linux && for f in common/frankenphp_manager.sh
 common/frankenphp_static_builder.sh common/frankenphp_install_pipeline.sh
 common/frankenphp_install_compile.sh common/frankenphp_install_prebuilt.sh
 common/frankenphp_acme_sh_install.sh common/runtime_config_common.sh
 common/service_contract_common.sh common/composer_vendor_common.sh
 debian/debian_com/laravel_upgrade_13.sh; do bash -n "$f" || echo "FAIL $f"; done; echo
 ALL-SYNTAX-OK; tail -1 /var/tmp/frankenphp_static_build.log | cut -c1-120

 ALL-SYNTAX-OK
 + curl -o spc -fsSL https://dl.static-php.dev/static-php-cli/spc-bin/nightly/spc-linux-x86_64

 Took 0.1s


 All syntax OK. Build still on the slow spc download (background, will take time; spc-bin nightly
 is ~50-100MB from a slow mirror; previous run eventually recovered).

 Summarize the completed work concisely for the user (Chinese, since user writes Chinese). Cover:
 1. 常量统一 (unified constants) - manager/builder as single source, prebuilt/pipeline/acme all
    reference the family; COMPILE_DIR conflict resolved; version default latest.
 2. 令牌链 - secret_keys file (DNS_DNSPOD_API_TOKENS id,token + DNSPOD_EMAILS), dual-source read
    (store first, file fallback), seed function, wired into ensure paths of both 93-compile and
    175.
 3. argv bug 发现与根治 - embedded php-cli -r doesn't populate $argv; env-var passing in
    runtime_config_common + sc_get + composer probe + laravel_upgrade_13. Token was NEVER actually
    stored before; now verified roundtrip + idempotent re-run.
 4. 双变体实现 - compile (dnspod embedded, Caddy DNS-01) + prebuilt (latest release asset, acme.sh
    dns_dp DNS-01 wildcard, certs to shared dir, Caddyfile file-cert gate) - gate chain dnspod ->
    file certs -> built-in ACME.
 5. 互斥清理 - prebuilt now also runs mutex+apt cleanup; cross-variant backup cleanup; dynamic
    fingerprints (compile branch state-true; concurrent editor's prebuilt dynamic fingerprint
    kept); stale step_run layers removed where self-probing (pipeline caddyfile).
 6. Build #2 still downloading spc in background.

 Keep it short.

 全部语法通过，构建仍在后台下载 spc（慢镜像，前次可自愈）。本轮完成：

 1. 常量统一（单一来源）
 - frankenphp_static_builder.sh 定义路径族
   ：FRANKENPHP_STATIC_BUILD_ROOT=/www/programing/frankenphp 下统一 prebuilt/、prebuilt-staging/、
   acme.sh/、certs/（消除与 COMPILE_DIR=/www/_debian_12 的分裂）
 - prebuilt/pipeline/acme_sh 全部引用 manager+builder 常量：FRANKENPHP_LINK_PATH、缓存/暂存/证书目
   录；FRANKENPHP_PREBUILT_INSTALL_BIN="${FRANKENPHP_LINK_PATH}"
 - prebuilt 版本默认 1.27.0（不存在的版本）→ latest（已验证 release 资产 URL 返回 200）
 - 新增共享 fm_site_host()（93 pipeline 与 175 同源解析 api.<region>.<domain>）

 2. 令牌双源链（.secret_keys）
 - 常量 FRANKENPHP_DNSPOD_TOKEN_SECRET_KEY=DNS_DNSPOD_API_TOKENS（id,token 格式）+ DNSPOD_EMAILS
 - fm_dnspod_token_value：store 优先 → secret 文件回退；新增 fm_dnspod_token_ensure 把文件令牌幂等
   种子入 Laravel store，挂入 fm_dns01_ensure 与 fm_ensure_dnspod_module（93/175 两条路都覆盖）

 3. 根治一个真 bug：嵌入 php-cli 不传 $argv
 - frankenphp php-cli -r 下 $argv 恒空 → 之前 runtime_config_put 从未真正写成功（种子消息靠 ||
   true 掩盖）
 - 4 处公共库改环境变量传参：runtime_config_common、sc_get（service_contract）、composer 探测、
   laravel_upgrade_13
 - 实测 shim 下 put/get 回读 ✓、二次 ensure 静默 ✓（store 已持 DNSPOD_TOKEN）

 4. 双变体证书
 - 编译版：dnspod 模块+令牌 → Caddyfile tls dns dnspod
 - 预编译版：acme_sh_ensure_certificate（dns_dp DNS-01，apex+通配符，rc2=未变更视为成功）装入
   certs/<apex>/，Caddyfile 第三门控 tls <fullchain> <key>；门控链：dnspod → 文件证书 → 内置 ACME

 5. 互斥与幂等修复
 - prebuilt 分支补齐 fm_disable_legacy_php_runtime + fm_static_apt_php_cleanup（同编译版门控）
 - 变体互斥清理：各自成功后清除对方残留备份（.prebuilt / .pre-dnspod）
 - compile 分支指纹改为状态真值（版本/link/模块嵌入态），prebuilt 保留并发编辑者加的动态指纹
   ；pipeline Caddyfile 去掉 stale step_run 层（内容比对自探测）

 构建 #2 完成后：fm_ensure_dnspod_module 将换入带 dnspod 的二进制并自动走互斥+清理。需要我继续盯构
 建（tail /var/tmp/frankenphp_static_build.log），还是先做其他任务？

───────────────────────────────────────────────────────────────────────────────────────────────────
client_loop: send disconnect: Connection reset
───────────────────────────────────────────────────────────────────────────────────────────────────
SSH session endedore_node (main)
↑532k ↓167k R9.2M CH99.5% 46.6%/128k (auto)                                  ark-code-latest • high
Press any key to exit...


37_ensure_pnpm_packages  17_install_node_toolchain_24 都要幂等添加修复
  代码。
  

› [93] Choose FrankenPHP install mode: prebuilt (default) or compile. 这
  个要提示Y/n的，可以输入。连续输入2次。
  
   93_install_frankenphp 给先写一个公共脚本，注意不要直接在该脚本中改。提
  示是否直接下载预编译还是编译，编译用现在的流程，而且预编译写新脚本在公
  共区，自动下载liunx的编译版本，默认 1.27版。之后，参考
  35_install_certbot 写一个 acme.sh  的，如果是下载预编译的，因为没有
  dnspod的证书签发，在frankenPHP幂等安装后，调用公共区的  acme.sh 安装。
  注意幂等是每一个小步骤都要幂等一下，而不是一个总入口幂等一交，比如xxx安
  装 、 xxx是否path / xxx是否升级 / xxxg配置是否需要安装 、 修护都要幂
  等，前一个步骤的幂等不能影响后面的过程，需要细粒度。按规范开发。shells
  不要使用任何返回值退出码，信任上一个函数的运行结果，二进制直接对文件检
  测。 93_install_frankenphp 同时拆出编译的分支脚本，但是注意不能修改代码
  的原意。 acme.sh 脚本呢？
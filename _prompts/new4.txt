 wordnew#/daily-reading 不权权有play all，单个条目也可以单独play，点
  击，收集进度逻辑，单个时也要保持进度，2：添加一个API接口，并在UI页上预
  览添加按钮到 wordnew#/daily-reading/article_504568cb-5630-4bd7-8048-
  08a84872de24 ，点击后，会弹出一个json，其中包括当前页用到的资源，根据
  设置，比如得到的新单词 + 句子表 + 音频等等。当然这个是和登录用户绑定
  的，也要有登录用户名。3：Word Groups

   为什么修改后没有同步到后端，这个在前端也为改为全局数据，防止每次前端
  都恢复初始渲染，4：Word Groups


  Default Vocabulary Group · 13885 words
  Default Vocabulary Group
  13885 words · 0% ，当点击数字时，跳转到 对应词组，比如，/wordnew#/
  shelf 同时改为新URL，5：/wordnew#/shelf路由加上词组的标识，比如 /
  wordnew#/shelf/xxxx，同时修改路由名，表现为词组。
  注意从底层构架，不要薄兼容，代码从底层重构，同时搜索官方文档，按规范修
  改，shell不要使用返回码退出值、信任上一个函数的运行结果。同时需要有幂
  等性，幂等需要拆为最小步、而不是从一大个入口就幂等跳过。
  复用重复实现类库。同时搜索官方文档，搜索官方文档。e

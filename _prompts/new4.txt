 go: downloading github.com/go-openapi/testify/v2 v2.6.0
 2026/08/18 16:01:37 [INFO] exec (timeout=0s): /usr/local/bin/go build
 -ldflags=-w -s -tags=nobadger,nomysql,nopgx -o
 /tmp/tmp.wIX82YM9UD/frankenphp

 github.com/e-dant/watcher/watcher-go

 /root/go/pkg/mod/github.com/e-dant/watcher@v0.0.0-20260223030516-06f84a1
 3
 14be/watcher-go/watcher.go:9:10: fatal error: wtr/watcher-c.h: No such
 file or directory
     9 | #include <wtr/watcher-c.h>
       |          ^~~~~~~~~~~~~~~~~
 compilation terminated.
  现在，换一个思路，根据官方的说明，参考  43_ensure_php85_intelligent.sh
 77_ensure_php_pgsql ，在公共区写一个清理脚本，将系统必要的清理干净（如果
 编译了则幂等跳过），之后
 https://github.com/php/frankenphp  clone 项目到临时目录，并使用官方编译
 脚
 本 chmod +x build-static.sh。除了编译必要的库， Ctype, cURL, DOM,
 Fileinfo, Filter, Hash, Mbstring, OpenSSL, PCRE, PDO, Session,
 Tokenizer, XML。 apcu, bcmath, brotli, bz2, calendar, ctype, curl, dom,
 fileinfo, filter, gd, iconv, intl, mbstring, mysqli, openssl, opcache,
 pdo, pdo_mysql, pdo_sqlite, session, sodium, sqlite3, tokenizer, xml,
 xmlreader, xmlwriter, zip, zstd 等。 编译时通过 EMBED=/path/to/your/app
 指定了你的项目路径 poly apps / laravel main，确保支持laravel 13。同时根
 据···> [*] Switch Installation Mode [server]
   [@] Select Region [Global]
   [D] Start MySQL After Installation [false]
   [R] Start Redis After Installation [true]
   [Q] Start PostgreSQL After Installation [true]
   [W] Web Server After Installation [frankenphp]
   [^] Start Docker After Installation [false]
   [.] Install .NET SDK [false]
   [G] Install Gitea (Git Service) [false]
   [T] Install Tailscale (Local-Network Mesh VPN) [false]
   [#] Setup Network Router [false]
   [C] Set Cloud Provider [null]··· 来确定编译那个数据库的支持，
 47_install_composer.sh脚本调整index到后面进行适配frankenPHP模式。同样将
 45_configure_php85.sh放在后面适配配置frankenPHP。搜索廴文档，保留支持
 podns签发证书的编译代码。幂等编译启用。

   编写 Caddyfile 配置文件当单文件部署到生产环境后，你不能再单纯使用命令
 行简易拉起（./frankenphp php-server），而是需要创建一个功能完整的
 Caddyfile 来告诉系统如何调用刚才注入的插件。在你的项目根目录下创建一个名
 为 Caddyfile 的文件，内容如下：caddy{
     # 全局配置：关闭默认的 HTTP 验证，强制使用刚注入的 dnspod 插件
     acme_dns dnspod "12345,abcdefgYOURDNSPODTOKENHERE"


 Let me parse this complex instruction. The user:

 1. The native xcaddy build failed again:
    github.com/e-dant/watcher/watcher-go needs wtr/watcher-c.h header
    (the watcher C library). The docs said "Restart workers on file
    change: Watcher C dependency, build tag nowatcher to disable". So the
    native build is missing the watcher-c headers (libwatcher-dev or
    compiled from source). Could fix by adding nowatcher tag... but the
    user wants a different approach:

 2. New direction from user:
     - Following the official docs, referencing existing scripts
       43_ensure_php85_intelligent.sh and 77_ensure_php_pgsql, write a
       cleanup script in the common area (公共区 = common/) that cleans
       up system-necessary stuff (idempotent skip if compiled). Hmm, "将
       系统必要的清理干净" - clean up the necessary parts of the
       system... Actually let me re-read: "在公共区写一个清理脚本，将系统
       必要的清理干净（如果编译了则幂等跳过）" - write a cleanup script
       in the common area that cleans what's necessary (if already
       compiled, skip idempotently). I think this means: a script that
       removes the now-unneeded system PHP packages/toolchain? OR
       prepares/cleans build prerequisites? Hmm. Given context: moving
       from native (apt php-zts) to build-static.sh (self-contained
       static build), the system PHP packages (php-zts-embed, php8.5-dev,
       maybe apt php entirely - the plane says "no apt PHP") should be
       cleaned. "如果编译了则幂等跳过" - if [the static binary is]
       compiled, idempotently skip. So: cleanup script that removes apt
       PHP / build deps once the static frankenphp binary is in place?
       Actually maybe: clean system PHP packages so the frankenphp static
       binary is the ONLY PHP runtime (per plane design "The frankenphp
       plane's ONLY PHP runtime (no apt PHP)").

    Hmm wait - but build-static.sh needs php-dev headers? NO -
    build-static.sh is self-contained (static-php-cli builds its own PHP
    from source). It needs: git, curl, composer (for static-php-cli
    source install OR use SPC_REL_TYPE=binary to download prebuilt spc
    binary - no composer needed!), go (for the xcaddy build step),
    make/gcc toolchain? The musl build via spc: SPC downloads musl
    toolchain? On Linux musl builds need... spc handles downloading
    musl-cross toolchain? Actually spc builds musl from source or
    downloads prebuilt libs (--prefer-pre-built). It's designed to be
    self-contained-ish but needs base tools (git curl tar xz make gcc?).
    spc doctor --auto-fix installs missing system deps.

 3. Then: clone https://github.com/php/frankenphp to temp dir and use the
    official build script chmod +x build-static.sh. Extension list: user
    gives a list: "除了编译必要的库，Ctype, cURL, DOM, Fileinfo, Filter,
    Hash, Mbstring, OpenSSL, PCRE, PDO, Session, Tokenizer, XML。 apcu,
    bcmath, brotli, bz2, calendar, ctype, curl, dom, fileinfo, filter,
    gd, iconv, intl, mbstring, mysqli, openssl, opcache, pdo, pdo_mysql,
    pdo_sqlite, session, sodium, sqlite3, tokenizer, xml, xmlreader,
    xmlwriter, zip, zstd 等。" - the second list (lowercase spc-style
    names) is the PHP_EXTENSIONS list for spc. Merge/dedupe: apcu,
    bcmath, brotli, bz2, calendar, ctype, curl, dom, fileinfo, filter,
    gd, iconv, intl, mbstring, mysqli, openssl, opcache, pdo, pdo_mysql,
    pdo_sqlite, session, sodium, sqlite3, tokenizer, xml, xmlreader,
    xmlwriter, zip, zstd. Note: first list includes Hash, PCRE (Hash is
    always-on in PHP 8; PCRE is built-in; spc may not accept them as
    extension names - spc extension list uses names like "filter",
    "mbstring"...). The second lowercase list is the actionable one; "等"
    = etc. Also user mentions databases: per the install-mode menu (MySQL
    false, Redis true, PostgreSQL true), compile the DB support:
    pdo_mysql + mysqli (MySQL false?? hmm - "Start MySQL After
    Installation [false]" means MySQL service not started, but... "来确定
    编译那个数据库的支持" - determine which DB support to compile based
    on the config: Redis true (redis extension? user's list doesn't
    include redis... the default spc list in build-static includes redis
    + memcached; user's custom list omits redis? "根据...来确定编译那个数
    据库的支持" - based on the menu: MySQL=false, Redis=true,
    PostgreSQL=true -> compile pdo_pgsql+pgsql? User's list has
    pdo_mysql, mysqli but NOT pdo_pgsql/pgsql! Hmm wait: MySQL false ->
    skip mysql? but list includes pdo_mysql/mysqli... confusing. The
    menu: MySQL=false, Redis=true, PostgreSQL=true. So DB support to
    compile: PostgreSQL (pdo_pgsql, pgsql) + Redis (redis ext)? But the
    user's explicit lowercase list includes pdo_mysql, mysqli and NOT
    pgsql/redis.

    Hmm, "同时根据 [menu config] 来确定编译那个数据库的支持" - "at the
    same time, based on [...] determine which database support to
    compile". So the script should be CONFIG-DRIVEN: read INSTALL_MODE
    vars (START_MYSQL/START_REDIS/START_POSTGRESQL from the installer's
    variable store) and conditionally include db extensions: mysql ->
    mysqli,pdo_mysql; postgresql -> pdo_pgsql,pgsql; redis -> redis. The
    user's listed extensions are the BASE set. The example menu shows
    Redis=true PostgreSQL=true MySQL=false -> final = base +
    pdo_pgsql,pgsql,redis (no mysql ones). The base list they typed
    includes pdo_mysql,mysqli though... I'll implement: base list as
    given by user (it includes pdo_mysql/mysqli - keep since typed
    explicitly), plus conditional additions from the runtime config store
    (START_MYSQL adds mysqli/pdo_mysql if not present; START_POSTGRESQL
    adds pdo_pgsql/pgsql; START_REDIS adds redis). That satisfies both.

    Wait, also "编译时通过 EMBED=/path/to/your/app 指定了你的项目路径
    poly apps / laravel main，确保支持laravel 13" - compile with EMBED
    pointing to the project path (poly apps / laravel main) to support
    Laravel 13. EMBED embeds the PHP app INTO the binary. Project path:
    there's presumably a laravel app path in the vars
    (WIS_PROGRAMING_DIR? "poly apps / laravel main" - need to find the
    laravel app root variable - maybe from gvar: LARAVEL_DIR or similar).
    EMBED requires composer.json/composer.lock/vendor present (spc
    dump-extensions reads them). Also embedding makes the binary huge and
    app updates require rebuild - the existing plane runs octane from the
    app dir at runtime... Hmm, but the user explicitly asks EMBED. Hmm -
    wait, actually re-read: "编译时通过 EMBED=/path/to/your/app 指定了你
    的项目路径 poly apps / laravel main" - this is quoting docs/tutorials
    ("你不能再单纯使用命令行..." block is quoted from some tutorial about
    Caddyfile). The EMBED mention might also be from the tutorial. But
    user says "确保支持laravel 13" - ensure Laravel 13 support. EMBED
    mode with dump-extensions ensures the app's composer extensions are
    included. I'll wire EMBED to the Laravel app root variable if
    resolvable at build time (idempotent: skip rebuild if binary already
    has module + same version marker). If app dir doesn't exist yet
    (fresh install), build without EMBED? The tutorial embeds for
    single-file deployment. Given complexity, make EMBED conditional: if
    the laravel main app dir (with composer.lock + vendor) exists, set
    EMBED; else omit.

    Need to find the laravel path: "poly apps / laravel main" - maybe
    /www/programing/... hmm "poly apps" could be a directory "poly-apps"?
    or "Poly Project"? Let me search gvar for LARAVEL/APP paths. There's
    WIS_PROGRAMING_DIR. Maybe APP_DIR/LARAVEL_DIR in gvar or 132 runtime.
    I'll grep.

 4. 47_install_composer.sh and 45_configure_php85.sh:
    "47_install_composer.sh脚本调整index到后面进行适配frankenPHP模式。同
    样将 45_configure_php85.sh放在后面适配配置frankenPHP。" - move/rename
    these scripts to a later index number (renumber) so they run AFTER
    the frankenphp build, adapting to frankenphp mode. So: rename
    47_install_composer.sh -> e.g. 94_install_composer.sh (after
    93_install_frankenphp.sh) and 45_configure_php85.sh -> e.g.
    95_configure_php85.sh (or similar). The numbering: 91=golang,
    93=frankenphp. So composer -> 94, configure php -> 95? "调整index到后
    面" = adjust index to later. Renaming files - need to check
    references to these filenames elsewhere (runners, docs, other scripts
    calling them by name).

 5. dnspod: "搜索文档，保留支持dnspod签发证书的编译代码" - keep the
    dnspod cert-issuing compile support (the XCADDY_ARGS with dnspod
    module). In build-static.sh flow: XCADDY_ARGS="--with
    github.com/caddy-dns/dnspod@master --with
    github.com/dunglas/mercure/caddy ..." (must re-include
    mercure/vulcain/cbrotli since custom XCADDY_ARGS replaces defaults).

 6. Idempotent compile ("幂等编译启用") - the ensure/build must be
    idempotent: if the static binary already built with dnspod module
    (probe), skip.

 7. Caddyfile: "编写 Caddyfile 配置文件当单文件部署到生产环境后...创建一
    个功能完整的 Caddyfile...来告诉系统如何调用刚才注入的插件" + example:
 ```
   caddy{
       acme_dns dnspod "12345,abcdefgYOURDNSPODTOKENHERE"
 ```

    The user wants a full Caddyfile written that uses the dnspod module
 for ACME DNS-01. BUT the existing frankenphp_manager has
 fm_caddyfile_ensure with canonical Caddyfile render + SECRETS policy:
 "the Caddyfile carries ONLY {$ENV} placeholders for the Mercure JWT keys
 - actual values are injected as process env" and dnspod token via
 {env.DNSPOD_TOKEN} placeholder (fm_dnspod_token_* functions). The user's
 example hardcodes the token in the Caddyfile ("12345,abcdefg...") - that
 violates the project's secrets policy (and AGENTS/global rules? The
 existing design is explicit). I should write the Caddyfile with
 {$DNSPOD_TOKEN} placeholder style (consistent with existing contract),
 NOT hardcoded token. The user's snippet is from a tutorial; adapt to the
 plane's existing secrets contract. Actually careful - user explicitly
 shows acme_dns dnspod "token" syntax. The existing fm_caddyfile_ensure
 presumably already renders acme_dns with placeholder when token exists?
 Let me read the Caddyfile render function to see current state and
 integrate.

 So this is a BIG multi-part task:
 A. Common-area cleanup script (idempotent): cleans system PHP/build deps
 that become unnecessary after static build? Or cleans BEFORE build? "将
 系统必要的清理干净（如果编译了则幂等跳过）" - hmm "clean up the system
 necessities" ... I now think: clean the packages that the static build
 replaces (apt php-zts-embed, php8.5-dev, php8.5-* packages, php-config,
 maybe nginx remnants are kept per plane). If the static frankenphp
 binary is already compiled (with dnspod), skip cleanup? "如果编译了则幂
 等跳过" = "if [it's] compiled, skip idempotently" - i.e., if the static
 binary already exists/compiled, the cleanup skips (nothing to do) -
 idempotent. So cleanup script = AFTER static build success, remove apt
 PHP stack (since frankenphp static = only PHP runtime). References
 43_ensure_php85_intelligent.sh & 77_ensure_php_pgsql (which presumably
 install php85 + pgsql php ext via apt) - the cleanup must coordinate:
 those apt PHP things get removed once frankenphp static provides PHP.
 Wait but 45_configure_php85 & 47_composer move AFTER frankenphp... and
 composer needs PHP - the frankenphp binary provides php-cli shim
 (fm_ensure_php_cli_shim). OK consistent: remove apt PHP, keep frankenphp
 static as runtime.

 Hmm, wait - actually maybe cleanup script runs BEFORE the build to clean
 leftovers of previous attempts (old go installs? temp dirs? partial
 builds?)... "将系统必要的清理干净" is ambiguous: "clean the necessary
 [parts] of the system". Combined with 43/77 references (php85 ensure +
 php/pgsql ensure - both apt PHP installers), I'm fairly confident: the
 cleanup script removes the apt PHP stack (and related dev packages) once
 frankenphp static is built - the plane's "no apt PHP" end-state. And
 it's in the common area (common/) as a reusable function library script
 (like frankenphp_manager.sh, service_contract_common.sh) - or as a
 numbered installer? "在公共区写一个清理脚本" - in the common area write
 a cleanup script. Common area = scripts/shells/linux/common/. So e.g.
 common/frankenphp_static_builder.sh? Hmm - but maybe the cleanup + build
 should be one manager-style file: common/frankenphp_static_builder.sh
 containing: cleanup (fm_static_cleanup_system), build (fm_static_build
 with idempotent probe). Actually the user separates: "写一个清理脚本，将
 系统必要的清理干净...之后 clone 项目...使用官方编译脚本" - cleanup
 script, THEN clone+build-static. The build orchestration could live in
 frankenphp_manager.sh (fm_dnspod_build_static rewritten to
 build-static.sh instead of docker bake!) - that fits the manager
 architecture: fm_dnspod_build_static becomes the official
 build-static.sh path (no docker!), keeping fm_ensure_dnspod_module flow
 (embedded -> static build-static.sh -> ...). Wait - but then what about
 native? User abandoned native (watcher header fail). New standard:
 official build-static.sh. fm_dnspod_build_static: docker bake ->
 build-static.sh implementation. fm_dnspod_build_native can be removed?
 Or kept? User: "换一个思路" - switch approach. The native path just
 failed on watcher-c.h; docs say watcher optional (nowatcher tag). But
 user wants build-static.sh now. Remove native? Keep as last-resort with
 nowatcher tag fix? Hmm - "保留支持dnspod签发证书的编译代码" keep dnspod
 compile support. I'd restructure:
 - fm_dnspod_build_static = official build-static.sh (docker-LESS now!
   matches user's no-docker) - PRIMARY
 - native: fix with -tags nowatcher? The watcher error could be fixed by
   adding nowatcher to XCADDY_GO_BUILD_FLAGS. But user explicitly
   switched direction; keeping native as fallback with nowatcher tag is a
   small addition... but adds maintenance. Per user "换一个思路" + AGENTS
   (remove dead code): replace native entirely with build-static.sh. But
   then the ZTS/apt PHP stuff gets cleaned by the new cleanup script.
   Decision: remove fm_dnspod_build_native (and its helpers usage stays:
   fm_ensure_xcaddy still needed? build-static.sh internally uses spc's
   go-xcaddy - it downloads xcaddy itself via go. Host go needed - GO_BIN
   in PATH: /usr/local/bin/go symlink ✓).

    Hmm wait - build-static.sh runs spc install-go-xcaddy
 (SPC_OPT_INSTALL_ARGS="go-xcaddy") - it installs xcaddy via the HOST go.
 And go itself: brew section only installs go on macOS; on Linux, go must
 be in PATH - we have /usr/local/bin/go ✓ (constants-managed 1.26.6 ✓
 user allowed "可以使用常量中的golang").

    Composer requirement: build-static.sh with SPC_REL_TYPE=source clones
 static-php-cli + composer install. Host composer: apt has no composer
 (checked earlier: no composer package). BUT SPC_REL_TYPE=binary
 downloads prebuilt spc binary (no composer needed!). User moved
 47_install_composer.sh to later index - so composer NOT available at
 frankenphp build time => must use SPC_REL_TYPE=binary.

    Also musl build on Debian: spc binary + musl... spc downloads
 prebuilt musl toolchain? For SPC_LIBC=musl on linux amd64, spc downloads
 the musl cross toolchain (musl.cc) automatically I believe, plus
 prebuilt libs with --prefer-pre-built. It's designed exactly for this
 (the docker image just wraps it). Also needs system deps: spc doctor
 --auto-fix handles missing ones (git, curl, xz, etc.). musl fully-static
 per docs = "runs on any distro". Good for ubuntu/debian/kali compat ✓.

    EMBED: requires app with composer.json/lock/vendor. Laravel app path
 - need to find. If EMBED set, spc dump-extensions reads app's
 composer.lock to add extensions + embeds app into binary. "确保支持
 laravel 13" - Laravel 13 (future version) - the spc dump-extensions
 covers ext needs. EMBED embedding at install-time: app may not exist yet
 on fresh install (132 runtime deploys later?). Conditional EMBED if app
 dir ready.

    Also PHP_VERSION: running binary is PHP 8.5 (v1.12.7 default 8.5 per
 build-static default "8.5"? main branch defaults 8.5). Pin
 PHP_VERSION="$(fm_php_version)" (8.5) for version-sync ✓.

    FRANKENPHP_VERSION pin: build-static.sh with .git present + tag does
 git checkout v<tag> ✓ (clone at main then checkout tag, or clone
 --branch tag directly - build-static handles FRANKENPHP_VERSION with
 checkout itself; simpler: clone default branch shallow... shallow clone
 + checkout tag: git checkout v1.12.7 works in shallow clone if tag
 fetched - clone --branch v1.12.7 --depth 1 directly + still pass
 FRANKENPHP_VERSION for the spc step).

    XCADDY_ARGS for dnspod: "--with github.com/caddy-dns/dnspod@master
 --with github.com/dunglas/mercure/caddy --with
 github.com/dunglas/vulcain/caddy --with
 github.com/dunglas/caddy-cbrotli" (must re-include defaults when
 customizing per docs tip + build-static default
 SPC_CMD_VAR_FRANKENPHP_XCADDY_MODULES). Reuse
 FRANKENPHP_STATIC_XCADDY_ARGS (already has dnspod@master + mercure) +
 add vulcain + cbrotli for official parity.

    Extension list: user's base (lowercase spc names):
 apcu,bcmath,brotli,bz2,calendar,ctype,curl,dom,fileinfo,filter,gd,iconv,
 intl,mbstring,mysqli,openssl,opcache,pdo,pdo_mysql,pdo_sqlite,session,so
 dium,sqlite3,tokenizer,xml,xmlreader,xmlwriter,zip,zstd + conditional:
 START_POSTGRESQL -> pdo_pgsql,pgsql; START_REDIS -> redis; START_MYSQL
 -> mysqli,pdo_mysql (already in base). Constants centralized in gvar?
 The manager has constants at top; extensions list is frankenphp-specific
 -> keep in manager constants (FRANKENPHP_STATIC_PHP_EXTENSIONS base +
 conditional db ones). Actually AGENTS: centralize global constants - but
 these are frankenphp-plane constants, manager file is their home (like
 FRANKENPHP_STATIC_XCADDY_ARGS). OK.

    How to read START_* config: get_var "START_MYSQL" etc.? The menu
 shows installer vars: INSTALL_MODE=server, SELECTED_REGION=Global,
 START_MYSQL=false, START_REDIS=true, START_POSTGRESQL=true,
 WEB_SERVER=frankenphp... These likely live in the runtime config store
 (RuntimeConfigurationStore via runtime_config_get in
 service_contract_common) or get_var from some env store. The manager
 already uses get_var? 91 used get_var "INSTALL_GO". get_var reads from
 where - common_functions. And runtime_config_get for secrets. For
 START_* booleans - probably get_var too (stored by the menu). I'll use
 get_var "START_MYSQL" etc. with fallback default false/true? The menu
 shows MySQL=false Redis=true PostgreSQL=true - those are this host's
 stored values presumably. Code:

 ```
   fm_static_db_extensions() {
       local exts=""
       [ "$(get_var START_MYSQL)" = "true" ] &&
 exts="$exts,mysqli,pdo_mysql"
       [ "$(get_var START_POSTGRESQL)" = "true" ] &&
 exts="$exts,pdo_pgsql,pgsql"
       [ "$(get_var START_REDIS)" = "true" ] && exts="$exts,redis"
       echo "$exts"
   }
 ```

    Need to check get_var semantics in common_functions.sh (how booleans
 stored). Let me grep get_var definition.

    Wait - also: pgsql extension needs libpq... spc builds it statically
 (pdo_pgsql supported by spc with bundled postgres? spc supports
 pdo_pgsql via building postgresql lib). OK spc handles.

    Also user's list mentions first line "除了编译必要的库" = "besides
 the libraries necessary for compilation" - spc handles core libs (brotli
 etc. auto). Fine.

    EMBED path: find laravel main app root. "poly apps / laravel main" -
 grep gvar for likely candidates: LARAVEL, APP_DIR, POLY. Let me grep
 gvar + look at 45/47 scripts for path hints (45_configure_php85.sh,
 47_install_composer.sh reference the laravel dir). Also 132 runtime
 mentioned in manager comments. I'll grep for "laravel" in
 gvar/common_functions.

 B. Cleanup script (common area): e.g.
 scripts/shells/linux/common/frankenphp_static_clean.sh? OR functions
 inside a new common script that the cleanup + build share. Hmm - "在公共
 区写一个清理脚本，将系统必要的清理干净（如果编译了则幂等跳过）" —
 Actually, rethink: maybe cleanup = clean the OLD apt-PHP/build-toolchain
 leftovers BEFORE/AFTER static build so the system ends minimal: remove
 php8.5-* apt packages, php-zts-, php-config, maybe libbrotli-dev? No -
 careful: keep what other planes need (pgsql? 77_ensure_php_pgsql
 installs php-pgsql via apt - that's the apt PHP being replaced). The
 cleanup targets: apt PHP packages + php-dev headers + xcaddy? (xcaddy
 binary /usr/local/bin/xcaddy - still used? build-static uses its own;
 remove? keep - harmless... user says "清理干净" clean thoroughly. I'd
 clean: apt php packages (with exceptions? php-zts needed only for native
 build - being abandoned), /etc/php config kept or removed?
 45_configure_php85.sh being moved AFTER frankenphp to "适配配置
 frankenPHP" - it will configure frankenphp's PHP ini
 (/etc/frankenphp/php-conf.d per FRANKENPHP_PHP_INI_DIR) instead of apt
 php. So apt php fully removable.

    "（如果编译了则幂等跳过）" - "if compiled, idempotently skip" - if
 the static binary already compiled, skip [the cleanup? the build?]. I
 read it as: cleanup+build flow is idempotent - if static binary with
 dnspod already exists, skip everything. So cleanup script:
 fm_static_cleanup: if fm_module_in_bin(binary, dnspod) && static-build
 marker? -> skip. Hmm... Actually simplest interpretation: the CLEANUP
 script skips idempotently if [the static frankenphp] has been compiled
 (nothing to clean because... no wait, if compiled, cleanup SHOULD run to
 remove apt php...).

    Alternative reading: cleanup script removes the BYPRODUCTS of the old
 native attempts + system php build deps, and if [those packages] were
 already cleaned, skip (idempotent skip on already-clean state). "（如果
 编译了则幂等跳过）" hmm "如果编译了" = "if compiled" - if [the thing]
 has been compiled then skip idempotently. For a CLEANUP script: if
 already cleaned -> skip. I'll implement: probe apt php packages present
 -> remove; if absent -> "already clean, skip" (idempotent log). That
 satisfies "幂等".

    Where invoked from? Probably a numbered installer script calls it
 after frankenphp static build... or 93 calls fm_ functions. Given "在公
 共区写一个清理脚本" - a standalone common script (sourced library or
 executable?). Manager-style: add to frankenphp_manager.sh? No - user
 says SEPARATE script in common area. Common area scripts are libraries
 (sourced), e.g. frankenphp_manager.sh, service_contract_common.sh,
 gvar_common.sh, common_functions.sh, nginx_manager.sh. So new file:
 common/frankenphp_static_clean.sh? Hmm naming convention... maybe
 "frankenphp_static_builder.sh" containing BOTH cleanup + official
 build-static orchestration (fm_static_* functions), sourced by 93. That
 matches "公共区写一个清理脚本" loosely (a clean/build library). But user
 said "写一个清理脚本...之后 clone 项目...使用官方编译脚本" - one script
 that does cleanup, then clone + official build. So ONE common script =
 static builder+cleaner. I'll create common/frankenphp_static_builder.sh
 with:
 - fm_static_cleanup_system: remove apt php stack (idempotent,
   probe-first) — but ONLY safe post static-build... order: 93 flow:
   ensure binary(static build) THEN cleanup? If cleanup removes apt php
   BEFORE build-static, build-static doesn't need apt php
   (self-contained) - safe either way. But php shims (/usr/local/bin/php
   -> frankenphp) stay. Careful: apt php removal removes
   /usr/bin/php-config (build-static doesn't need it ✓). I'll call
   cleanup AFTER successful build/verify in the ensure flow.
 - fm_static_build (clone + build-static.sh with env pins + idempotent
   probe)
 - source-able + executable guard? Library style.

    Hmm wait - risk: removing apt php8.5 while OTHER things (composer
 later at 94) need php - shims to frankenphp static provide php ✓.

 C. Renumber 45_configure_php85.sh -> later, 47_install_composer.sh ->
 later. Need to see the scripts dir listing to pick numbers (94/95? there
 may be existing 94/95...). Check install_shells dir. Also check
 references to "45_configure_php85" / "47_install_composer" across repo
 (runners/registry). Then adjust their CONTENT for frankenphp mode? User:
 "47_install_composer.sh脚本调整index到后面进行适配frankenPHP模式" - move
 index to later AND adapt to frankenphp mode; "同样将
 45_configure_php85.sh放在后面适配配置frankenPHP" - same. Adaptation
 details: composer install must use frankenphp php shim (probably already
 does via PATH php? need to read scripts), 45 configure php ini -> target
 /etc/frankenphp/php-conf.d. Read both scripts to adapt.

 D. Caddyfile: read fm_caddyfile_ensure current render; add acme_dns
 dnspod with {$DNSPOD_TOKEN} placeholder (per secrets contract - NOT
 hardcoded token as user's tutorial shows). Maybe already there? grep
 acme_dns in manager.

 E. 91_install_golang header comment mentions native xcaddy requiring go
 - still true (build-static needs go too) ✓. And 91 comment
 "93_install_frankenphp.sh: the native xcaddy rebuild..." update wording
 to static builder? Minor; keep go relevance (build-static + spc
 go-xcaddy need go 1.26+... spc install-go-xcaddy uses host go to build
 xcaddy - yes needs go. frankenphp v1.12.7 needs go 1.26 ✓).

 Also: user's pasted menu shows "Install .NET/Gitea/Tailscale/etc" -
 that's the install menu store; START_MYSQL etc. via get_var presumably.
 Verify get_var exists & how to query.

 Plan of investigation (parallel):
 1. ls install_shells dir (see numbering & find 45/47 + what's after 93)
 2. grep references to 45_configure_php85|47_install_composer across repo
 3. read 45_configure_php85.sh + 47_install_composer.sh (heads)
 4. grep get_var definition in common_functions.sh; grep
    START_MYSQL/START_REDIS/START_POSTGRESQL/WEB_SERVER in repo (how
    stored)
 5. grep acme_dns/dnsprovider/fm_caddyfile_ensure section in manager
    (current Caddyfile render)
 6. grep laravel app path hints: LARAVEL_DIR|APP_ROOT|poly in
    gvar/common_functions; and 43_ensure_php85_intelligent.sh +
    77_ensure_php_pgsql.sh heads (user referenced them as patterns for
    the cleanup script style)

 That's a lot; batch efficiently. Then design & write.

 Also the abort: my previous build run was aborted by user (they killed
 it since direction changed). The failed watcher compile shown was from
 their own re-run? The log shows build with mercure etc? "go: downloading
 github.com/go-openapi/testify/v2" then watcher error - yes my pinned
 build was running in THEIR terminal? Whatever - direction changed;
 native abandoned (I'll remove native path? or keep with nowatcher fix as
 deep fallback... user: "换一个思路" + "保留支持dnspod签发证书的编译代码"
 (keep dnspod). I'll REPLACE fm_dnspod_build_static internals with
 build-static.sh and DROP fm_dnspod_build_native (cleanup script removes
 php-zts etc anyway, making native impossible by design - consistent).

 Hmm, actually careful about scope explosion. The user asked a LOT. Let
 me enumerate deliverables:
 1. common/ cleanup script (new file) — cleans apt php stack idempotently
    (referencing 43/77 pattern = these install apt php; cleanup
    reverses).
 2. Rewrite fm_dnspod_build_static -> official build-static.sh
    (docker-LESS, env-pinned, ext list + conditional db exts, EMBED
    laravel main if present, XCADDY dnspod@master +
    mercure/vulcain/cbrotli, idempotent probe: module embedded -> skip).
 3. fm_ensure_dnspod_module flow: embedded -> static(builder) -> (docker
    bake removed? native removed?) — simplify: embedded ->
    fm_dnspod_build_static (build-static.sh). Then cleanup after success.
 4. Renumber 45 -> later & 47 -> later + adapt both to frankenphp mode.
 5. Caddyfile: acme_dns dnspod with env placeholder (keep secrets
    contract).
 6. bash -n everything; probably NOT run the full static build (takes
    very long: compiling PHP via spc = 20-60 min; user said 幂等编译启用
    - they'd run it via 93). Should I run it? "尽快完成" earlier... but a
    spc build is very long and network-heavy; running it in this session
    with 1500s timeout may abort mid-way. Better: implement + syntax
    check + light dry validation, then let user run 93 (or offer to kick
    it off). Hmm - user expects completion. I'll implement everything,
    then ASK/kick off the build with a long timeout? The bash tool timed
    out at 90s default earlier... max timeout param I used 1500. A full
    spc musl build could exceed. I'll implement first; run if time
    permits (maybe with nohup + log file so it survives, then poll). Good
    approach: nohup the build to a log, monitor.

 Let me investigate first.

 这次失败点是 watcher-c.h（frankenphp 的 watcher C 库依赖，文档标注可用
 nowatcher tag 关闭）--但按你的新方向直接切换到官方 build-static.sh 自包
 含构建（免 docker、免 apt PHP 工具链），这才是彻底解法。先摸清现状：脚本
 目录编号、45/47 的内容与引用、get_var 的 START_* 变量存储、当前
 Caddyfile 渲染、laravel 主应用路径：


 $ ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/ |
 sort; echo ===; grep -rln "45_configure_php85\|47_install_composer"
 /www/programing/core_node --include="*.sh" --include="*.ps1"
 --include="*.md" 2>/dev/null | grep -v
 "45_configure_php85.sh\|47_install_composer.sh" | head

 ... (95 earlier lines, ctrl+o to expand)
 97_install_tailscale.sh
 999_check_circular_symlinks.sh
 99_generate_docker_compose_yml.sh
 9_fix_dns.sh
 ===

 Took 26.6s



 $ grep -n "get_var()" -A 12
 /www/programing/core_node/scripts/shells/linux/common/common_functions.s
 h | head -18; echo ===; grep -rn
 "START_MYSQL\|START_REDIS\|START_POSTGRESQL\|WEB_SERVER"
 /www/programing/core_node/scripts/shells/linux/common/common_functions.s
 h | head -8; echo ===; grep -n
 "acme_dns\|fm_caddyfile_ensure\|dnsprovider"
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager
 .sh | head

 ... (1 earlier lines, ctrl+o to expand)
 ===
 19:#   fm_caddyfile_ensure                              canonical
 Caddyfile render
 537:fm_caddyfile_ensure() {
 655:            fm_caddyfile_ensure "$@"

 Took 0.2s



 $ head -40
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/45_
 configure_php85.sh; echo =====47=====; head -40
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/47_
 install_composer.sh

 ... (92 earlier lines, ctrl+o to expand)
 MIN_COMPOSER_VERSION_MINOR=7
 MIN_COMPOSER_VERSION_PATCH=0
 MIN_COMPOSER_VERSION="2.7.0"

 # PHP version tracking file

 Took 0.2s


 Important findings:

 1. Script numbering: There are gaps and even duplicates
    (93_install_frankenphp.sh AND 93_install_java.sh; 95_install_java.sh,
    97_install_tailscale.sh). Scripts after 93: 101_core_node_finish,
    103..193. The "later" index for composer/php85 config: they need to
    run AFTER 93_install_frankenphp.sh. Options: 94 doesn't exist (93
    java exists, 95 java exists - so 94 is FREE). So:
    47_install_composer.sh -> 94_install_composer.sh,
    45_configure_php85.sh -> 96_configure_php85.sh? Hmm 95 taken by java,
    96 free. Order matters: composer (94) before configure (96)? User:
    "47_install_composer.sh 调整index到后面进行适配" and "同样将
    45_configure_php85.sh 放在后面" - both after 93. Composer needs php
    (shim from frankenphp ✓ after 93). Configure php85 targets frankenphp
    ini. Composer first, then configure? Actually configure-php then
    composer either way. I'll do 94_install_composer.sh and
    96_configure_php85.sh? Why not 94+95... 95_install_java.sh exists. So
    composer=94, configure=96. Hmm wait - but is there a runner that
    executes in numeric order using the full sorted list?
    93_install_java.sh vs 93_install_frankenphp.sh both exist - sorted
    order ambiguous between them. Fine, not my problem.

    How is execution orchestrated? Probably an index registry/runner
    script (maybe in debian/ root or a master installer). grep for
    "47_install_composer" found NOTHING outside the two files
    themselves?! The grep -rln across repo excluding self returned empty.
    So no other file references them by name?? Then how are they invoked
    - via numeric scan (for f in NN_*.sh)? Likely a runner iterates the
    directory. Renaming = safe (no name references). But wait - grep
    included *.md too and found nothing - surprising (docs usually
    mention). OK: safe to rename.

    ALSO: 49_install_frankenphp.sh exists (old frankenphp installer?) and
    175_laravel_main_start.sh, 191_install_octane_watcher_daemon.sh. Not
    our concern now.

 2. get_var / START_ vars*: my grep found NOTHING in common_functions.sh
    for get_var() definition nor START_MYSQL. Hmm - get_var must be
    defined elsewhere (gvar_common? or a config store lib).
    91_install_golang.sh used get_var "INSTALL_GO" after sourcing
    common_functions.sh + gvar_common.sh. And the manager uses
    runtime_config_get from service_contract_common.sh. Need to find
    get_var definition: grep -rn "get_var()" across common/. Also
    START_MYSQL storage: maybe in a .env-like state file read by get_var.
    Let me grep the repo for "START_REDIS" / "INSTALL_MODE" store.

 3. Caddyfile render: fm_caddyfile_ensure at line 537. Need to read it
    (does it include acme_dns dnspod?). grep acme_dns found nothing ->
    Caddyfile currently does NOT use dnspod DNS-01 (only built-in ACME).
    User wants acme_dns dnspod added (with token). Read the render
    function.

 4. 45/47 scripts: 45 actually titled "34_configure_php85.sh" internally
    (renamed before), modifies /etc/php/8.5/{cli,fpm}/php.ini, references
    ServerManagerV1PHPConfigFixer.php; app path:
    /www/programing/core_node/poly_apps/laravel_main ✓ (laravel main app
    root - for EMBED). 47 sources debian_com/php_common_vars.sh with
    PHP_BINARY="$TARGET_LINK_PATH" etc.

 Given the huge scope, let me focus this turn on investigation batch 2
 (get_var definition+START_* storage; fm_caddyfile_ensure body;
 php_common_vars TARGET_LINK_PATH; 43/77 quick look for cleanup pattern +
 how apt php installed). Then start implementing.

 Deliverables plan (final):
 - NEW common/frankenphp_static_builder.sh: fm_static_cleanup +
   fm_static_build (official build-static.sh orchestration; env pins;
   extensions; db-conditional; EMBED laravel_main when ready;
   dnspod+mercure+vulcain+cbrotli XCADDY args; SPC_REL_TYPE=binary;
   idempotent: module already embedded -> skip)
 - frankenphp_manager.sh: fm_dnspod_build_static delegates to builder
   lib; remove docker bake + native (fm_dnspod_build_native,
   fm_docker_ready, DOCKER_BIN_CANDIDATES, fm_ensure_xcaddy? -
   build-static uses spc's own xcaddy;
   fm_ensure_xcaddy/fm_xcaddy_bin/fm_go_gopath/fm_go_bin become unused by
   dnspod path... fm_go_bin might be used elsewhere (191 watcher?). grep
   before removing.) Ensure flow: embedded -> fm_dnspod_build_static ->
   cleanup after install.
   Hmm - or simpler: keep manager lean: fm_dnspod_build_static body
   becomes the build-static.sh implementation directly (no new file)...
   but user explicitly said "在公共区写一个清理脚本" - a cleanup script
   in common area. So new file it is: frankenphp_static_builder.sh holds
   BOTH cleanup & build (sourced by manager). Manager sources it.
 - Renumber: 47->94_install_composer.sh, 45->96_configure_php85.sh +
   adapt internals (frankenphp mode: composer uses php shim; configure
   targets /etc/frankenphp/php-conf.d). Adaptation level: read more of
   both files before editing... These are big scripts; "适配frankenPHP模
   式" minimal: 45: write ini to frankenphp conf dir instead of
   /etc/php/8.5; 47: PHP_BINARY=/usr/local/bin/php (shim). Need to read
   their cores.
 - Caddyfile: add acme_dns dnspod {$DNSPOD_TOKEN} in the right spot
   (tls/ACME section or site block). Read render first.
 - 91 header comment: update "native xcaddy rebuild" mention -> official
   static builder (go still needed for spc go-xcaddy).

 That's a LOT. Prioritize per user's emphasis: ① builder+cleanup lib, ②
 renumber+adapt, ③ Caddyfile acme_dns. Do investigation batch 2 now.


 $ grep -rn "get_var()" /www/programing/core_node/scripts/shells/
 2>/dev/null | head -3; grep -rn "INSTALL_MODE\|START_REDIS"
 /www/programing/core_node/scripts/shells/linux/common/*.sh 2>/dev/null |
 grep -v "^Binary" | head -8

 ... (18 earlier lines, ctrl+o to expand)
 /www/programing/core_node/scripts/shells/linux/common/install_test_menu.
 sh:163:            local install_mode=$(get_var "INSTALL_MODE")
 /www/programing/core_node/scripts/shells/linux/common/install_test_menu.
 sh:237:                        next_mode_index=$(( (current_mode_index +
 1) % ${#INSTALL_MODES[@]} ))

 Took 0.7s



 $ sed -n '537,640p'
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager
 .sh

 ... (110 earlier lines, ctrl+o to expand)
 "$FRANKENPHP_DNSPOD_MODULE" && echo embedded || echo missing)"
     echo "[$SCRIPT_INDEX] [VERIFY] DNS-01 (dnspod): $(fm_dns01_status)"
     echo "[$SCRIPT_INDEX] [VERIFY] php-cli shim: $([ -x
 "${FRANKENPHP_PHP_SHIM_DIR}/php" ] && echo present || echo missing)"
     echo "[$SCRIPT_INDEX] [VERIFY] plane: $(web_server_plane)"

 Took 0.1s



 $ grep -n "TARGET_LINK_PATH\|COMPOSER_TARGET_PATH\|PHP_BINARY"
 /www/programing/core_node/scripts/shells/linux/debian/debian_com/php_com
 mon_vars.sh 2>/dev/null | head; echo ===; grep -rn
 "fm_go_bin\|fm_ensure_xcaddy\|fm_xcaddy_bin"
 /www/programing/core_node/scripts/shells/linux/ --include="*.sh" | grep
 -v "common/frankenphp_manager.sh" | head -5; echo ===; grep -n
 "apt-get\|apt .*install\|php8.5"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/43_
 ensure_php85_intelligent.sh | head -8; echo ===; grep -n "apt-get\|apt
 .*install"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/77_
 ensure_php_pgsql.sh | head -5

 ... (20 earlier lines, ctrl+o to expand)
 + pgsql and
 93:$USE_SUDO apt-get update -qq || true
 94:if ! $USE_SUDO apt-get install -y "$PG_PKG"; then
 112:    $USE_SUDO apt-get update -qq || true
 113:    if ! $USE_SUDO apt-get install -y "$PG_PKG"; then

 Took 0.1s


 Great - the Caddyfile ALREADY has the dnspod DNS-01 tls stanza gated on
 module+token: dns dnspod {env.DNSPOD_TOKEN} inside tls {}. Hmm - the
 Caddy dnspod module syntax: the tls directive uses dns dnspod <token>;
 the acme_dns global option is an alternative. The user's tutorial showed
 global acme_dns dnspod "token". Current implementation uses per-site tls
 { dns dnspod {env.DNSPOD_TOKEN} } - equivalent and secrets-safe (env
 placeholder). But wait - master-branch dnspod module Caddyfile syntax:
 dns dnspod <api_token> - from UnmarshalCaddyfile: "dnspod [<api_token>]
 { api_token <...> }" ✓ matches. So Caddyfile part is DONE already (keeps
 cert issuance via dnspod ✓ "保留支持dnspod签发证书"). Good - no
 Caddyfile change needed! Just confirm the gate works after module
 embedded. I'll note this to the user (the manager already renders it;
 token stays env placeholder per secrets contract - NOT hardcoded as in
 their tutorial).

 Also found: set_var/get_var exist in gvar_common (line 1598) - config
 store. START_MYSQL etc - not found in common/*.sh grep (only
 INSTALL_MODE in install_test_menu). The menu the user pasted is from a
 different installer menu (PowerShell/TUI?) - vars like
 START_MYSQL/START_REDIS/START_POSTGRESQL... let me check how they're
 stored: grep -rn "START_MYSQL" across scripts (not just linux/common).
 Quick grep needed. If get_var "START_MYSQL" returns "" treat as false;
 Redis=true/PostgreSQL=true on this host per menu. Let me grep repo-wide.

 Now design the cleanup + builder. Key references:
 - 43_ensure_php85_intelligent.sh: installs php8.5 + extensions via apt
 
 
 Great - the Caddyfile ALREADY has the dnspod DNS-01 tls stanza gated on
 module+token: dns dnspod {env.DNSPOD_TOKEN} inside tls {}. Hmm - the
 Caddy dnspod module syntax: the tls directive uses dns dnspod <token>;
 the acme_dns global option is an alternative. The user's tutorial showed
 global acme_dns dnspod "token". Current implementation uses per-site tls
 { dns dnspod {env.DNSPOD_TOKEN} } - equivalent and secrets-safe (env
 placeholder). But wait - master-branch dnspod module Caddyfile syntax:
 dns dnspod <api_token> - from UnmarshalCaddyfile: "dnspod [<api_token>]
 { api_token <...> }" ✓ matches. So Caddyfile part is DONE already (keeps
 cert issuance via dnspod ✓ "保留支持dnspod签发证书"). Good - no
 Caddyfile change needed! Just confirm the gate works after module
 embedded. I'll note this to the user (the manager already renders it;
 token stays env placeholder per secrets contract - NOT hardcoded as in
 their tutorial).

 Also found: set_var/get_var exist in gvar_common (line 1598) - config
 store. START_MYSQL etc - not found in common/*.sh grep (only
 INSTALL_MODE in install_test_menu). The menu the user pasted is from a
 different installer menu (PowerShell/TUI?) - vars like
 START_MYSQL/START_REDIS/START_POSTGRESQL... let me check how they're
 stored: grep -rn "START_MYSQL" across scripts (not just linux/common).
 Quick grep needed. If get_var "START_MYSQL" returns "" treat as false;
 Redis=true/PostgreSQL=true on this host per menu. Let me grep repo-wide.

 Now design the cleanup + builder. Key references:
 - 43_ensure_php85_intelligent.sh: installs php8.5 + extensions via apt
   (php8.5-<ext>)
 - 77_ensure_php_pgsql.sh: apt installs php8.5-pgsql

 Cleanup targets (after static frankenphp is THE runtime): apt php8.5-*
 packages, php-zts-cli/php-zts-embed, php8.5-dev (php-config),
 /etc/php/8.5? (45_configure will target frankenphp dir after renumber;
 old apt php ini can be removed), php8.5-fpm service. Also xcaddy?
 /usr/local/bin/xcaddy + /root/go module caches - the static builder
 doesn't need them; but go toolchain STAYS (constants-pinned, needed for
 spc go-xcaddy during rebuilds). Keep xcaddy? It's a build tool; cleanup
 "系统必要的清理干净" - I'd remove apt php packages ONLY (the plane's "no
 apt PHP" mandate), keep toolchain bits harmless. Hmm - php-zts-embed was
 installed FOR the abandoned native path -> remove. php8.5-* were
 installed by 43/77 -> remove. But careful: 47/45 (renumbered to 94/96)
 run AFTER and use the php shim (/usr/local/bin/php -> frankenphp php-cli
 ✓). Composer itself is a phar run via php shim ✓. 96_configure_php85:
 adapts to write /etc/frankenphp/php-conf.d/php.ini
 (FRANKENPHP_PHP_INI_DIR exists in manager constants!). It currently
 writes /etc/php/8.5/{cli,fpm} - adaptation needed.

 Wait - ORDER subtlety: 43_ensure_php85_intelligent.sh runs BEFORE (43)
 installing apt php8.5. If cleanup (in 93 flow) removes apt php, then
 43's job at install time = providing PHP for EARLY steps? Early steps
 needing php before 93? 13/15 python stuff no; laravel deploy (175) later
 ✓. But 43 runs at index 43 - installing apt php THEN 93 removes it =
 wasteful but functional (install-time sequencing is the orchestrator's;
 user only asked cleanup script in common area + renumber 45/47). Also
 maybe 43 should be SKIPPED in frankenphp mode? 43 is "intelligent" -
 maybe it already checks web_server_plane? Not our task now (user
 referenced 43/77 only as the SOURCE of apt php that cleanup reverses,
 and as style reference). Actually re-read user: "参考
 43_ensure_php85_intelligent.sh 77_ensure_php_pgsql ，在公共区写一个清理
 脚本，将系统必要的清理干净" - "referencing 43 and 77, write a cleanup
 script in the common area that cleans the system necessities" - i.e.,
 reverse what 43/77 installed. Confirmed interpretation.

 Cleanup script: common/frankenphp_static_builder.sh? Name it per its
 role: cleanup + official static build. User asked ONE script doing
 cleanup then clone+build-static. File:
 scripts/shells/linux/common/frankenphp_static_builder.sh, library style
 (sourced by frankenphp_manager.sh / 93). Functions:
 - fm_static_apt_php_cleanup: probe apt php packages (dpkg-query), skip
   if none ("already clean"); else apt purge php8.5-* php-zts-*
   php-common etc. Keep /etc/php? purge removes config with purge; use
   remove? Use apt-get purge -y. Also disable php8.5-fpm service if
   present. NOT remove composer (installed later anyway at 94), NOT
   remove go.
 - fm_static_build: idempotent (caller ensures module-missing first),
   clone repo at version tag, SPC_REL_TYPE=binary, PHP_VERSION from
   fm_php_version, PHP_EXTENSIONS = base + db-conditional, EMBED when
   laravel_main ready, XCADDY_ARGS = dnspod@master + mercure + vulcain +
   cbrotli, run ./build-static.sh, output dist/frankenphp-linux-$(uname
   -m).

 Env for build-static.sh needs go in PATH: /usr/local/bin/go symlink ✓
 (login PATH). Also git, curl, xz? spc doctor --auto-fix handles. CGO for
 musl: spc downloads musl toolchain. Fine.

 GOPROXY for spc go-xcaddy: export GOPROXY fallback chain like before.

 EMBED: /www/programing/core_node/poly_apps/laravel_main - check exists +
 composer.lock + vendor. If present -> EMBED; spc dump-extensions adds
 app-required extensions. Laravel 13 (php >=8.3...) fine.

 Wait - EMBED embeds app INTO binary: binary contains app copy; runtime
 still serves from disk root path? For octane/php_server the Caddyfile
 root points to laravel_public_dir on disk; embedded app is used when
 running embedded mode. Embedding ensures extensions match app needs +
 single-file deploy option. User explicitly wants EMBED (tutorial
 single-file deployment). I'll set EMBED only if app ready; comment that
 re-build needed after app changes... Actually hmm - EMBED at INSTALL
 time (app likely not deployed yet at 93 on fresh install;
 laravel_main_start at 175). So fresh install: no EMBED (base extensions
 cover Laravel needs per user's list which already covers laravel:
 dom,curl,mbstring,openssl,pdo,session,tokenizer,xml,fileinfo,filter,ctyp
 e,gd,intl,bcmath,zip,sodium,zstd,bz2...). Rebuilds later (idempotent
 ensure) with app present would EMBED - but then recompile triggered only
 when module missing... "幂等编译启用" = idempotent skip when already
 compiled. Keep logic: EMBED when app ready at build time.

 Hmm, wait: careful - if EMBED changes binary behavior? php_server serves
 from root dir normally; embedded copy accessible via embedded paths;
 harmless.

 DB extensions conditional: get_var
 "START_MYSQL"/"START_REDIS"/"START_POSTGRESQL" - need to find their
 store. If get_var returns "" default: treat ""/true/false. Menu shows
 Start MySQL [false], Redis [true], PostgreSQL [true]. Base list already
 includes pdo_mysql/mysqli per user's typed list. Conditional adds pgsql
 set when START_POSTGRESQL=true, redis when START_REDIS=true, mysql set
 when START_MYSQL=true (idempotent dedupe via sort -u? spc list
 duplicates probably tolerated but dedupe cleanly: build list then echo |
 tr ',' '\n' | awk '!seen[$0]++' | paste -sd,).

 Let me grep START_MYSQL/START_POSTGRESQL storage repo-wide quickly (also
 docker_compose area maybe). Also check fm_has_module/fm_module_in_bin
 existence (used in ensure) ✓ exists.

 Manager restructure:
 - fm_dnspod_build_static -> now calls fm_static_build (from builder lib)
   - or manager sources builder lib and fm_ensure_dnspod_module calls
   fm_static_build directly; delete fm_dnspod_build_static docker bake +
   fm_docker_ready + fm_dnspod_build_native + DOCKER_BIN_CANDIDATES +
   FRANKENPHP_STATIC_REPO; keep fm_ensure_xcaddy/fm_xcaddy_bin/fm_go_* ?
   unused after native removal - but maybe other scripts use (grep showed
   no external refs to fm_go_bin/fm_ensure_xcaddy outside manager
   except... my grep returned nothing outside manager ✓). Remove them?
   They're generic helpers possibly useful... AGENTS remove dead code.
   I'll remove native-only helpers (fm_dnspod_build_native,
   fm_docker_ready, DOCKER_BIN_CANDIDATES, FRANKENPHP_STATIC_REPO,
   fm_ensure_xcaddy, fm_xcaddy_bin, fm_go_gopath?, fm_go_bin?,
   XCADDY_BIN_CANDIDATES, XCADDY_GO_MODULE, GO_BIN_CANDIDATES?) - wait
   fm_go_bin might be used by other fm functions (fm_ensure_xcaddy
   only?). grep inside manager for fm_go_bin usage. Let me check before
   deleting. Simpler: remove only what's clearly dead; keep tiny probes
   if referenced.

 Given the scale, I'll:
 1. Create common/frankenphp_static_builder.sh (cleanup + build).
 2. Rework manager: source builder; fm_dnspod_build_static -> thin
    wrapper calling fm_static_build (keep name so ensure flow & comments
    stay coherent); delete docker bake internals + native function;
    ensure flow: embedded -> fm_dnspod_build_static; after successful
    install -> fm_static_apt_php_cleanup (cleanup once binary verified +
    dnspod module present). Wait - cleanup should run even if module was
    already embedded (idempotent skip path) - run cleanup in
    fm_ensure_dnspod_module tail? But cleanup removes apt php - only safe
    when frankenphp binary healthy (fm_installed). Put cleanup call in 93
    flow? I can't see 93's body... Manager's fm_install? The ensure
    function tail after install: call fm_static_apt_php_cleanup
    unconditionally (it self-probes: if binary missing -> skip with warn;
    if no apt php -> skip "already clean"). Good: idempotent both ways.
 3. Renumber 45->96_configure_php85.sh? Wait order: composer 94 needs php
    shim; configure 96 configures ini. Hmm actually
    configure-before-composer also fine. User said composer "index到后面
    ", configure "放在后面". I'll pick 94_install_composer.sh and
    96_configure_php85.sh (95 taken by java). Adapt:
     - 47: PHP_BINARY="$TARGET_LINK_PATH" already = /usr/local/bin/php
       (the shim!) - after renumber the shim exists ✓. Its internal title
       says "35_install_composer.sh" + SCRIPT_INDEX [34_COMPOSER] -
       update headers? Minimal: update Script: header names/index to
       match new filenames.
     - 45: rewrite ini targets /etc/frankenphp/php-conf.d
       (FRANKENPHP_PHP_INI_DIR) - need to read more of 45 to adapt
       properly... it's long. Time! Minimal adaptation: at top define
       PHP_INI_DIR targeting frankenphp dir; but the script edits
       /etc/php/8.5/{cli,fpm} in multiple places. Hmm. Given effort,
       maybe read the whole 45 later. Alternatively "适配配置frankenPHP"
       could mean: skip fpm config, point cli config to frankenphp's
       php-conf.d. Let me read 45 fully (it may be structured with vars
       making adaptation easy).
 4. Caddyfile: already supports dnspod ✓ no change (explain to user).
 5. 91 header comment tweak (native->static builder reference). Minor, do
    it.

 Also START_* storage grep. And laravel_main existence check. Batch:
 - grep -rn "START_MYSQL|START_POSTGRESQL|START_REDIS" repo (scripts dir)
   | head
 - ls /www/programing/core_node/poly_apps/laravel_main (composer.json?
   vendor? public/)
 - read rest of 45_configure_php85.sh (the ini-writing core)
 - grep manager for fm_go_bin/fm_xcaddy usage lines to know dead helpers

 Then write code. This is going to be a long implementation turn. Let me
 be efficient.

 Caddyfile 部分好消息：fm_caddyfile_ensure 已内置 dnspod DNS-01 渲染（tls
 { dns dnspod {env.DNSPOD_TOKEN} }，模块+token 双条件门控，token 走 env
 占位符不落盘）--与你教程里 acme_dns dnspod 等价且符合本项目密钥策略，无
 需硬编码 token。继续摸底：START_* 变量存储、laravel_main 应用状态、45 脚
 本结构：
 
 
 $ grep -rn "START_MYSQL\|START_POSTGRESQL\|START_REDIS"
 /www/programing/core_node/scripts/shells/ 2>/dev/null | grep -v
 docker_compose | head -8; echo ===; ls
 /www/programing/core_node/poly_apps/laravel_main/ 2>/dev/null | head
 -10; ls /www/programing/core_node/poly_apps/laravel_main/composer.json
 /www/programing/core_node/poly_apps/laravel_main/composer.lock
 /www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php
 2>/dev/null

 ... (30 earlier lines, ctrl+o to expand)
 config
 database
 /www/programing/core_node/poly_apps/laravel_main/composer.json
 /www/programing/core_node/poly_apps/laravel_main/composer.lock
 /www/programing/core_node/poly_apps/laravel_main/vendor/autoload.php

 Took 0.1s



 $ grep -n "fm_go_bin\|fm_go_gopath\|fm_xcaddy_bin\|fm_ensure_xcaddy"
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager
 .sh; echo ===; wc -l
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/45_
 configure_php85.sh
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/47_
 install_composer.sh; grep -n "etc/php\|php.ini\|PHP_INI"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/45_
 configure_php85.sh | head -20

 ... (34 earlier lines, ctrl+o to expand)
 368:# keeps the plane-invariant Laravel directory permissions - NO
 /etc/php
 378:    fm_php_ini_ensure
 382:    echo -e "${GREEN}$SCRIPT_INDEX FrankenPHP plane PHP configured:
 ini scan dir $(fm_php_ini_dir) (runtime exports PHP_INI_SCAN_DIR)${NC}"

 Took 0.1s


 More findings:

 1. 45_configure_php85.sh ALREADY has a frankenphp branch! Line 367-382:
    "ini (scan dir exported as PHP_INI_SCAN_DIR by the runtime branch)
    and keeps the plane-invariant Laravel directory permissions - NO
    /etc/php" + fm_php_ini_ensure + "FrankenPHP plane PHP configured: ini
    scan dir $(fm_php_ini_dir)". So 45 detects the plane and adapts
    (frankenphp branch vs apt php branch). Renaming to a later index is
    the main change needed (plus maybe verifying the branch trigger).

 2. START_ vars*: stored via selector_common.sh menu (get_var
    "START_MYSQL" etc., default "false"). So get_var "START_POSTGRESQL"
    works. On this host menu shows Redis=true, PostgreSQL=true (stored
    true), MySQL=false.

 3. laravel_main app READY: composer.json + composer.lock +
    vendor/autoload.php all present → EMBED is viable NOW.

 4. Manager helper usage: fm_ensure_xcaddy/fm_xcaddy_bin used by
    fm_dnspod_build_native (line 390-391) only → after native removal
    they're dead (fm_go_bin used by fm_ensure_xcaddy + fm_go_gopath;
    fm_go_gopath used by fm_xcaddy_bin). All become dead → remove the
    block fm_go_bin..fm_ensure_xcaddy (lines ~228-297) + XCADDY/GO
    candidates constants + DOCKER section (fm_docker_ready ~304-311?) +
    FRANKENPHP_STATIC_REPO + fm_dnspod_build_native entirely.

 Wait - careful: keep fm_version_tag (used by static build pin).
 fm_has_module/fm_module_in_bin keep. fm_php_version keep.

 Now implement. Plan:

 A. New file scripts/shells/linux/common/frankenphp_static_builder.sh:

 ```bash
   #!/bin/bash
   # header block (AI rules? The manager has the AI SPECIAL ATTENTION
 RULES block; 45/47 have it. New common file: include the same rules
 block for consistency? gvar/frankenphp_manager include it. I'll
 include.)
   # FrankenPHP Official Static Builder (common area) - the docker-LESS
   # implementation of frankenphp.dev/docs/static: clone php/frankenphp
 at
   # the RUNNING binary's tag and run the official ./build-static.sh
   # (static-php-cli builds its own PHP/libphp toolchain - the ONLY host
   # tool consulted is the pinned Go toolchain from gvar_common, used by
   # spc's go-xcaddy step). Includes the post-build system cleanup that
   # reverses 43_ensure_php85_intelligent.sh / 77_ensure_php_pgsql.sh
 (the
   # frankenphp plane's PHP runtime is the single static binary - no apt
 PHP).
   # Sourced by frankenphp_manager.sh; every primitive is idempotent.

   SCRIPT_CURRENT_DIR=... source gvar + service_contract? It's sourced BY
 manager which already sources gvar - avoid double sourcing complexity:
 manager sources gvar before sourcing builder. But standalone sourcing
 safety: builder sources gvar itself if not already? Common pattern in
 this repo: each file sources its deps (manager sources gvar +
 service_contract). To be safe: builder sources gvar too (sourcing twice
 is harmless - it re-runs assignments... gvar has side effects? it's big,
 runs map_web_path etc. Re-sourcing = wasteful but ok. Manager sources
 gvar THEN builder sources gvar again... I'll guard: `source
 "$SCRIPT_CURRENT_DIR/gvar_common.sh"` - hmm gvar may not tolerate
 double-source (set -u? no). Simplest: builder does NOT source; it
 REQUIRES being sourced after gvar (comment). Manager sources it right
 after its own gvar source. OK.

   Constants:
   FRANKENPHP_STATIC_BUILDER_REPO="https://github.com/php/frankenphp"

 FRANKENPHP_STATIC_EMBED_APP_DIR="$WIS_PROGRAMING_DIR/poly_apps/laravel_m
 ain"? WIS_PROGRAMING_DIR is exported in gvar ✓. Hmm - is laravel_main
 under WIS_PROGRAMING_DIR?
 /www/programing/core_node/poly_apps/laravel_main. gvar exports
 WIS_PROGRAMING_DIR + BASE_DIR. Use
 "$WIS_PROGRAMING_DIR/poly_apps/laravel_main"? Need WIS_PROGRAMING_DIR
 value = /www/programing/core_node presumably. Verify via bash later.

 FRANKENPHP_STATIC_PHP_EXTENSIONS_BASE="apcu,bcmath,brotli,bz2,calendar,c
 type,curl,dom,fileinfo,filter,gd,iconv,intl,mbstring,mysqli,openssl,opca
 che,pdo,pdo_mysql,pdo_sqlite,session,sodium,sqlite3,tokenizer,xml,xmlrea
 der,xmlwriter,zip,zstd"
   db conditional: START_MYSQL -> mysqli,pdo_mysql ; START_POSTGRESQL ->
 pdo_pgsql,pgsql ; START_REDIS -> redis

   functions:
   fm_static_php_extensions():
     base + conditionals, dedupe.

   fm_static_embed_dir(): echo app dir if
 composer.json+composer.lock+vendor/composer/installed.json present else
 ""

   fm_static_build():  # echoes candidate path
     local version_tag work_dir php_ver exts embed_dir candidate
     version_tag from fm_version_tag (manager) - builder references
 fm_version_tag: cross-file dependency (manager defines it; builder
 sourced by manager after those defs? fm_version_tag defined AFTER
 manager sources builder? Manager structure: sources at top, functions
 below. Builder sourced at top → fm_version_tag not yet defined at SOURCE
 time, but by CALL time (runtime) it IS defined (bash resolves at call).
 ✓ fine.
     probe: work_dir=$(mktemp -d)
     git clone --depth 1 --branch "v${version_tag#v}"? build-static
 expects FRANKENPHP_VERSION tag WITHOUT v? It strips "v" itself:
 FRANKENPHP_VERSION=${FRANKENPHP_VERSION#v}; git checkout "v${ver}". Pass
 version_tag WITH v - handled. I'll clone --branch "$version_tag" --depth
 1 (tag v1.12.7) and export FRANKENPHP_VERSION="$version_tag".
     cd src && env:
       SPC_REL_TYPE=binary  (prebuilt spc - no composer dependency;
 composer is installed LATER at 94 per the new order)
       PHP_VERSION="$(fm_php_version)"  (8.5, version-sync with running
 binary)
       PHP_EXTENSIONS="$(fm_static_php_extensions)"
       [ -n embed ] EMBED="$embed_dir"
       XCADDY_ARGS="$FRANKENPHP_STATIC_XCADDY_ARGS"  (dnspod@master +
 mercure; add vulcain + cbrotli for official parity → update constant in
 manager: "--with ${FRANKENPHP_DNSPOD_IMPORT} --with
 github.com/dunglas/mercure/caddy --with github.com/dunglas/vulcain/caddy
 --with github.com/dunglas/caddy-cbrotli")
       GOPROXY chain fallback
       PATH ensure /usr/local/bin (go) - inherited ✓
       ./build-static.sh
     candidate="$work_dir/src/dist/frankenphp-linux-$(uname -m)"
     [ -x "$candidate" ] && echo it; else rm -rf work_dir; echo ""

     Note: on failure keep work_dir for debugging? rm to avoid tmp
 litter; log path first. Keep rm but echo WARN. Hmm - debugging static
 builds is painful without logs; but mktemp dirs litter /tmp. rm is fine
 (log tail printed by 93 runner).

   fm_static_apt_php_cleanup():
     # Reverses 43/77 apt PHP once the static binary is the verified
 runtime.
     local binary; binary=$(fm_get_binary)  (manager fn)
     if [ -z binary ] || ! fm_module_in_bin "$binary"
 "$FRANKENPHP_DNSPOD_MODULE"... hmm cleanup gate: binary healthy (version
 runs) is enough; dnspod not required for php runtime... but cleanup runs
 in dnspod ensure context. Gate: fm_installed + "$binary" version ok.
 Simpler: fm_installed.
     collect apt php packages: dpkg-query -W -f='${binary:Package}\n'
 'php*' 2>/dev/null | grep -E '^php' | grep -v composer? php-composer?
 package names php8.5-*, php-common, php-zts-*, php-cli?... pattern
 '^php' catches php*; EXCLUDE nothing composer-ish (composer isn't
 php-prefixed... "php-common" yes remove). Also php8.5-fpm service stop
 first.
     if empty -> echo "already clean" return 0
     apt-get purge -y --autoremove? autoremove could remove unrelated -y
 deps... use purge without autoremove + apt-get autoremove? Risky per
 "never destructive without approval" - user DID ask cleanup explicitly.
 Use: $USE_SUDO apt-get purge -y $pkgs && $USE_SUDO apt-get autoremove -y
 -qq? autoremove scoped... I'll do purge + `apt-get autoremove -y`
 (standard). Log packages.
     Also remove /etc/php leftovers? purge handles conffiles with purge
 ✓. Keep /etc/frankenphp (ours).
     NOT removed: composer binary (94 installs), go, xcaddy (leave -
 actually xcaddy at /usr/local/bin - remove? It was installed for native
 path. build-static doesn't need it. Cleanup "干净"... remove
 /usr/local/bin/xcaddy if present? It's harmless; but per
 no-docker/no-native design it's dead. Remove it + go module caches? Go
 stays (needed). I'll remove xcaddy binary only.

   B. Manager rework:
   - constants: remove DOCKER_BIN_CANDIDATES, XCADDY_BIN_CANDIDATES,
 XCADDY_GO_MODULE, GO_BIN_CANDIDATES, FRANKENPHP_STATIC_REPO; add source
 of builder file after service_contract source: `source
 "$SCRIPT_CURRENT_DIR/frankenphp_static_builder.sh"`; update
 FRANKENPHP_STATIC_XCADDY_ARGS to include vulcain+cbrotli.
   - remove fm_go_bin/fm_go_gopath/fm_xcaddy_bin/fm_ensure_xcaddy block,
 fm_docker_ready, fm_dnspod_build_native.
   - fm_dnspod_build_static: rewrite to delegate: comment (official
 standard, docker-LESS via ./build-static.sh, self-contained toolchains,
 version-synced, host go only) + body: fm_static_build call.
   - fm_ensure_dnspod_module: order embedded -> fm_dnspod_build_static;
 after successful install/verify: fm_static_apt_php_cleanup. Also in the
 already-embedded early-return path? Cleanup idempotent - call it there
 too? ensure returns early "already embedded" → also run cleanup? Cleanup
 belongs to plane convergence; call before early return? Simplest: run
 cleanup at start of fm_ensure_dnspod_module when binary healthy? But
 during FIRST install (no binary) cleanup must not run. Put cleanup call
 AFTER the early-return check (module embedded → cleanup → return) and
 after successful replace. Both spots: fm_static_apt_php_cleanup
 self-gates on fm_installed.

   Hmm wait - careful ordering problem: 43_ensure_php85_intelligent
 installs apt php at index 43; 93 removes it; then 94 composer + 96
 configure run with shim ✓. But 45/47 currently run at 45/47 BEFORE 93!
 Renumbering moves them AFTER. So DURING install flow: 43 installs apt
 php → (nothing uses it until 93? laravel steps at 175+ use shim) → 93
 frankenphp static + cleanup removes apt php → 94 composer (shim php) →
 96 configure (frankenphp branch). Consistent ✓. (43 itself still
 installs apt php wastefully - flag to user as optional future skip, not
 my scope.)

   - fm_verify unchanged.

   C. Renumber + adapt:
   - mv 47_install_composer.sh → 94_install_composer.sh; update internal
 "Script: 35_install_composer.sh" header → "Script:
 94_install_composer.sh"; SCRIPT_INDEX [34_COMPOSER] → [94_COMPOSER]?
 It's cosmetic; update to match. Also adaptation: it sources
 php_common_vars (PHP_BINARY=/usr/local/bin/php = shim ✓ works under
 frankenphp). Possibly checks for php8.5-fpm? grep later quickly.
 Minimal: header updates + verify no apt-php hard deps.
   - mv 45_configure_php85.sh → 96_configure_php85.sh; internal "Script:
 34_configure_php85.sh" → 96; it already has frankenphp branch
 (fm_php_ini_ensure etc. — wait, fm_php_ini_ensure/fm_php_ini_dir defined
 WHERE? in 45 itself or manager? grep: 45 line 378 calls
 fm_php_ini_ensure - defined in 45? or sources manager? Need check: grep
 fm_php_ini in manager + 45. If defined in 45 calling manager fns
 (fm_get_binary...), ensure it sources manager. Let me grep.

   D. 91 header comment: "This step is the Go prerequisite for
 93_install_frankenphp.sh: the native xcaddy rebuild ... requires go >=
 1.26.0" → update to official static builder (spc go-xcaddy + frankenphp
 v1.12.7 module build require go >= 1.26).

   E. Caddyfile: no change (already gated dnspod DNS-01). Report to user.

   Checks needed before writing:
   - WIS_PROGRAMING_DIR value
   - fm_php_ini_ensure / fm_php_ini_dir definitions (45 or manager?)
   - 47 quick scan for php8.5-fpm/apt php deps
   - php-zts packages removal: php-zts-cli/php-zts-embed purge ✓ (pattern
 ^php catches them: "php-zts-cli" starts with php ✓)

   Then write builder file + manager edits + renumber + 91 tweak. Then
 bash -n all. Then optionally kick off build (long). Given session
 length, I'll implement + syntax check, then START the build via nohup
 log (survives session), report how to monitor. Actually user wants
 completion ("尽快完成该脚本")... the static build compiles PHP from
 source = 20-60+ min realistically. nohup approach + status check is
 right.

   Batch checks now.
 ```

 45 脚本已有 frankenphp 分支（fm_php_ini_ensure，ini 扫描目录方案，不动
 /etc/php）；START_* 存于 selector 变量仓（get_var 可读）；laravel_main
 的 composer.json/lock/vendor 齐全（EMBED 可用）。最后几个确认点：


 $ grep -rn "fm_php_ini_ensure\|fm_php_ini_dir"
 /www/programing/core_node/scripts/shells/linux/ --include="*.sh" | head
 -6; echo ===; bash -c 'source
 /www/programing/core_node/scripts/shells/linux/common/gvar_common.sh
 >/dev/null 2>&1; echo "WIS=$WIS_PROGRAMING_DIR"; echo
 "laravel=$WIS_PROGRAMING_DIR/poly_apps/laravel_main"'; echo ===; grep -n
 "php8.5-fpm\|php-fpm\|apt.*php"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/47_
 install_composer.sh | head -5

 ... (15 earlier lines, ctrl+o to expand)
 WIS=/www/programing
 laravel=/www/programing/poly_apps/laravel_main
 ===
 499:# (apt PHP 8.5) or the frankenphp plane's php-cli shim (embedded
 PHP) - so

 Took 1.8s


 Findings:
 1. fm_php_ini_dir/fm_php_ini_ensure live in frankenphp_manager.sh (lines
    145-156) - and 45 sources the manager presumably (or sources common
    via path). 43_ensure_php85_intelligent.sh:1327 also calls
    fm_php_ini_ensure - so 43 also integrates with the manager. Good -
    the plane integration already exists.
 2. WIS_PROGRAMING_DIR=/www/programing (NOT core_node!). So laravel_main
    actual = /www/programing/core_node/poly_apps/laravel_main. The gvar
    WIS var points to /www/programing. So for EMBED I should use a path
    relative to the CORE repo. What var points to core_node? BASE_DIR?
    gvar exports BASE_DIR... check value. The manager references
    "PHP_BIN"/"VENDOR_AUTOLOAD" (runtime context from where?). 45's
    header says absolute path
    /www/programing/core_node/poly_apps/laravel_main. Better: derive from
    the manager's own location: SCRIPT_CURRENT_DIR =
    scripts/shells/linux/common -> repo root =
    $SCRIPT_CURRENT_DIR/../../.. -> poly_apps/laravel_main. Use BASE_DIR?
    check BASE_DIR value quickly at write time... I'll compute in
    builder: FRANKENPHP_STATIC_EMBED_APP_DIR =
    "{SCRIPT_CURRENT_DIR}/../../../poly_apps/laravel_main" resolved.
    SCRIPT_CURRENT_DIR is defined in builder (its own dirname = common
    dir) ✓ robust regardless of WIS.
 3. 47 line 499 already documents dual-mode (apt php OR frankenphp
    php-cli shim) ✓ - minimal adaptation needed for 47 (just renumber +
    header).

 Now write everything:

 1. New file common/frankenphp_static_builder.sh:

 Include AI rules header (consistent with manager), description,
 constants, functions:
 - fm_static_php_extensions
 - fm_static_embed_dir
 - fm_static_build
 - fm_static_apt_php_cleanup

 Sourced AFTER gvar (by manager). Uses: fm_version_tag, fm_php_version,
 fm_get_binary (manager); get_var (gvar); USE_SUDO (common_functions -
 hmm, manager doesn't source common_functions; 93 does before sourcing
 manager? The manager uses $USE_SUDO already (fm_install etc.) - so
 callers provide it. Builder same contract.)

 Constants at top per rules:
 FRANKENPHP_STATIC_REPO stays in manager? It exists there already;
 builder needs it - keep single definition in MANAGER (existing) and
 builder references it (call-time resolution ✓). Avoid duplication
 (AGENTS centralize). OK - builder uses $FRANKENPHP_STATIC_REPO +
 $FRANKENPHP_STATIC_XCADDY_ARGS + $FRANKENPHP_DNSPOD_IMPORT from manager
 constants.

 Builder's own constants:
 FRANKENPHP_STATIC_PHP_EXTENSIONS_BASE="..."
 FRANKENPHP_STATIC_DB_EXT_MYSQL="mysqli,pdo_mysql"
 FRANKENPHP_STATIC_DB_EXT_POSTGRESQL="pdo_pgsql,pgsql"
 FRANKENPHP_STATIC_DB_EXT_REDIS="redis"
 FRANKENPHP_STATIC_EMBED_APP_DIR="$SCRIPT_CURRENT_DIR/../../../poly_apps/
 laravel_main" - resolve at write: SCRIPT_CURRENT_DIR defined at file
 top.
 FRANKENPHP_STATIC_OUTPUT_NAME="frankenphp-linux-$(uname -m)"? compute
 inline.

 Functions:

 fm_static_php_extensions() {
     local exts=""
     exts="$FRANKENPHP_STATIC_PHP_EXTENSIONS_BASE"
     if [ "$(get_var "START_MYSQL" 2>/dev/null)" = "true" ]; then
         exts="exts,{FRANKENPHP_STATIC_DB_EXT_MYSQL}"
     fi
     if [ "$(get_var "START_POSTGRESQL" 2>/dev/null)" = "true" ]; then
         exts="exts,{FRANKENPHP_STATIC_DB_EXT_POSTGRESQL}"
     fi
     if [ "$(get_var "START_REDIS" 2>/dev/null)" = "true" ]; then
         exts="exts,{FRANKENPHP_STATIC_DB_EXT_REDIS}"
     fi
     # Dedupe, preserving order (spc tolerates dupes but keep it
 canonical)
     echo "$exts" | tr ',' '\n' | awk 'NF && !seen[$0]++' | paste -sd','
 }

 get_var signature: get_var "NAME" default? install_test_menu used
 get_var "INSTALL_MODE" (no default); 85 used get_var "START_MYSQL"
 "false". OK default arg supported.

 fm_static_embed_dir() {
     # EMBED only when the Laravel main app is deployed (composer.lock +
     # vendor present): spc dump-extensions then derives the app's real
     # extension needs (Laravel 13 included) and bakes the app in.
     local app_dir="$FRANKENPHP_STATIC_EMBED_APP_DIR"
     if [ -f "app_dir/composer.json" ] [ -f "app_dir/composer.lock" ]
         && [ -f "$app_dir/vendor/composer/installed.json" ]; then
         echo "$app_dir"
     fi
     (echo "")
 }

 Careful: function must not leak - use if/else echo.

 fm_static_build() {
     local version_tag=""
     local php_ver=""
     local work_dir=""
     local embed_dir=""
     local extensions=""
     local candidate=""

 ```
   version_tag="$(fm_version_tag)"
   if [ -z "$version_tag" ]; then
       echo "[$SCRIPT_INDEX] [static] cannot parse the running frankenphp
 version; rebuild aborted"
       return 1  # hmm - caller expects echo path; return 1 with echo ""
 ... manager: candidate="$(fm_dnspod_build_static)" - $(...) captures
 stdout "cannot parse..." as candidate! BAD. So log to stderr: echo "..."
 >&2 and echo "" to stdout. Let me design: all logs -> stderr, ONLY the
 path -> stdout.
   fi

 ```

 }

 IMPORTANT pattern: fm_dnspod_build_static echoes candidate path
 (stdout); logs elsewhere. Existing functions echo logs AND path??
 Existing fm_dnspod_build_native echoed logs + path mixed?! Looking at
 ensure: candidate="$(fm_dnspod_build_static)" would capture ALL stdout
 logs as the path!! But the docker version earlier echoed "[dnspod]
 official static build..." logs + path... wait the old docker impl — logs
 inside () get captured. Let me re-check the ensure code: candidate =
 "(fm_dnspod_build_static)" then [ -z "candidate" ]... if logs captured,
 candidate = log text (non-empty!) - > sanity check "candidate" version
 -> fails -> "keeping binary". So the ORIGINAL docker path had this bug
 too (logs polluting stdout capture). Native: logs inside build...
 fm_dnspod_buildₙative echoes "[dnspod] native xcaddy build..." then on
 success echoes "build_dir/frankenphp" - multi-line candidate! Then if !
 "$candidate" version - "$candidate" with embedded newlines breaks. Wait
 but the RUN OUTPUT earlier showed "[dnspod] docker not available; trying
 the native libphp build" printed BEFORE candidate capture (that's in
 ensure, outside $()) and then xcaddy's own logs went to terminal because
 xcaddy ran INSIDE () capture?? No wait - the user's terminal showed
 xcaddy INFO logs "[INFO] absolute output file path..." - those printed
 to terminal DURING candidate = "(fm_dnspod_build_native)". stdout of the
 subshell $() is captured - xcaddy logs to stderr? xcaddy logs -> stderr
 probably ✓ so they displayed. But fm's own echo logs -> captured into
 candidate. Then candidate="<log line>\n/tmp/.../frankenphp"? and [ -x
 "$candidate" ] false -> rm -rf dirname -> "echo ''" -> empty. So even a
 SUCCESSFUL native build would fail the flow due to log pollution!!
 (previous session never got far enough to hit this). MUST FIX: builder
 logs -> >&2 strictly; only final path -> stdout.

 So fm_static_build: all echo logs use >&2; final: echo "$candidate"
 (stdout) / echo "" on failure.

 fm_static_build body:
     work_dir="$(mktemp -d)"
     echo "[frankenphp-mgr] [static] cloning $FRANKENPHP_STATIC_REPO at
 $version_tag" >&2
     if ! git clone --quiet --depth 1 --branch "versionₜag"
 "FRANKENPHP_STATIC_REPO" "$work_dir/src" >&2; then
         echo "[frankenphp-mgr] [static] [WARN] source clone failed" >&2
         rm -rf "$work_dir"
         echo ""
         return 0?? — hmm ensure treats empty as failure. Just echo ""
 and return.
     fi
     php_ver="$(fm_php_version)"
     embed_dir="$(fm_static_embed_dir)"
     extensions="$(fm_static_php_extensions)"
     echo "... official ./build-static.sh (SPC_REL_TYPE=binary, PHP
 ${php_ver:-default}, extensions: extensions{embed_dir:+,
 EMBED=$embed_dir})" >&2
     if ! (cd "$work_dir/src" &&
         SPC_REL_TYPE="binary"
         FRANKENPHP_VERSION="$version_tag"
         ${php_ver:+PHP_VERSION="$php_ver"}
         PHP_EXTENSIONS="$extensions"
         ${embed_dir:+EMBED="$embed_dir"}
         XCADDY_ARGS="$FRANKENPHP_STATIC_XCADDY_ARGS"

 GOPROXY="${GOPROXY:-https://proxy.golang.org,https://goproxy.cn,direct}"

         ./build-static.sh >&2); then
         echo "[frankenphp-mgr] [static] [WARN] official build-static.sh
 failed (workdir kept: $work_dir)" >&2
         echo ""
         return 0
     fi
     candidate="work_dir/src/dist/frankenphp-linux-(uname -m)"
     if [ -x "$candidate" ]; then
         echo "$candidate"
     else
         echo "[frankenphp-mgr] [static] [WARN] no binary produced at
 $candidate (workdir kept: $work_dir)" >&2
         echo ""
     fi

 On failure keep workdir for debugging (log path) - mktemp dirs in /tmp,
 acceptable; note cleanup responsibility. Hmm ensure's sanity-failure
 path does rm -rf "(dirname "candidate")" - if candidate valid path it
 cleans. If we kept failed workdir and echoed "" -> ensure skips rm ✓.

 Wait: ${php_ver:+PHP_VERSION="$php_ver"} inline conditional env
 assignment in the (...) subshell - syntax ok.

 Also git clone --branch "$version_tag": tag is "v1.12.7" ✓.

 Also musl build: default SPC_LIBC=musl (fully static, runs on
 ubuntu/debian/kali) ✓ per instruction-① heritage.

 fm_static_apt_php_cleanup() {
     local binary=""
     local pkgs=""
     binary="$(fm_get_binary)"
     if [ -z "binary" ] || ! "binary" version >/dev/null 2>&1; then
         echo "[frankenphp-mgr] [static] cleanup skipped: no healthy
 frankenphp binary"
         return 0
     fi
     pkgs="(dpkg-query -W -f = '{binary:Package}\n' 'php*' 2>/dev/null |
 grep -E '^php' || true)"
     Hmm dpkg-query -W 'php*' with glob unmatched returns error to stderr
 + nothing; grep filters.
     Also exclude? php-common etc all fine to purge. What about 'php*-'
 patterns... dpkg-query -W 'php' expands shell? quoted - dpkg-query does
 its own glob on package names ✓.
     if [ -z "$pkgs" ]; then
         echo "... already clean (no apt PHP packages)"
         return 0
     fi
     stop fpm if active: systemctl stop php8.5-fpm? generic: for svc
 php*-fpm... use: $USE_SUDO systemctl disable --now php8.5-fpm
 2>/dev/null || true — generic loop over installed php*fpm pkgs? keep
 simple: attempt php8.5-fpm (the only version in this plane).
     echo "... purging apt PHP (frankenphp static binary is the only PHP
 runtime): $(echo $pkgs | tr '\n' ' ')"
     $USE_SUDO apt-get purge -y -qq $pkgs  (word-split intended;
 shellcheck disable comment)
     $USE_SUDO apt-get autoremove -y -qq
     Also remove xcaddy leftovers of the retired native path:
     [ -x /usr/local/bin/xcaddy ] && $USE_SUDO rm -f
 /usr/local/bin/xcaddy
     verify: dpkg-query again; report.
 }

 USE_SUDO availability: callers (93) source common_functions ✓; my direct
 runs too.

 2. Manager edits:
 a) source builder after service_contract_common:

 ```
   # shellcheck source=/dev/null
   source "$SCRIPT_CURRENT_DIR/service_contract_common.sh"
 ```

 add:

 ```
   # shellcheck source=/dev/null
   source "$SCRIPT_CURRENT_DIR/frankenphp_static_builder.sh"
 ```

 b) constants: remove XCADDY_BIN_CANDIDATES, XCADDY_GO_MODULE,
 GO_BIN_CANDIDATES, DOCKER_BIN_CANDIDATES, FRANKENPHP_STATIC_REPO?
 builder uses FRANKENPHP_STATIC_REPO - KEEP it. Remove the four
 candidate/module constants.
 c) FRANKENPHP_STATIC_XCADDY_ARGS: add vulcain + cbrotli:
 "--with ${FRANKENPHP_DNSPOD_IMPORT} --with
 github.com/dunglas/mercure/caddy --with github.com/dunglas/vulcain/caddy
 --with github.com/dunglas/caddy-cbrotli"
 update its comment (defaults must be re-included when customizing).
 d) remove fm_go_bin..fm_ensure_xcaddy block (lines 228-297-ish) and
 fm_docker_ready block.
 e) fm_dnspod_build_static: replace docker bake body with delegate to
 fm_static_build. New comment: official static build via
 ./build-static.sh (docker-LESS)... keep version-sync/self-contained
 wording adapted: static-php-cli builds own PHP+libs; only host tool =
 pinned Go (gvar GO_VERSION) for spc go-xcaddy;
 FRANKENPHP_VERSION+PHP_VERSION pinned to running binary (double
 version-sync now!). musl fully-static runs ubuntu/debian/kali ✓. Logs
 stderr; echoes path.
 f) remove fm_dnspod_build_native entirely + fix ensure flow:

 ```
       candidate="$(fm_dnspod_build_static)"
       if [ -z "$candidate" ]; then
           echo "... [WARN] dnspod module deferred (official static build
 unavailable)"
           return 1
       fi
 ```

 g) cleanup hooks: in fm_ensure_dnspod_module: early-return path (already
 embedded) -> run fm_static_apt_php_cleanup before return; after
 successful replace+verify -> run cleanup. Need to see the tail of ensure
 (after backup/replace ~lines 480-500) to edit precisely. Let me read
 456-520 before editing.

 3. Renumber:
 git mv? No git ops (AGENTS: no git unless asked) - use mv:
 mv 47_install_composer.sh 94_install_composer.sh; update internal header
 "Script: 35_install_composer.sh" -> "Script: 94_install_composer.sh";
 SCRIPT_INDEX "[34_COMPOSER]" -> "[94_COMPOSER]".
 mv 45_configure_php85.sh 96_configure_php85.sh; header "Script:
 34_configure_php85.sh" -> "Script: 96_configure_php85.sh"; SCRIPT_INDEX?
 check 45's SCRIPT_INDEX var name (probably [34...] something) - grep.

 4. 91 header: "the native xcaddy rebuild of frankenphp v1.12.7 (Caddy
 v2.11.4, libdns v1) requires go >= 1.26.0" -> "the official static build
 (./build-static.sh; spc go-xcaddy + frankenphp v1.12.7 module build)
 requires go >= 1.26.0".

 5. Caddyfile: no change ✓ (already gated dnspod DNS-01; will verify
 after build via fm_dns01_status).

 Order of ops: read ensure tail + 45/47 SCRIPT_INDEX lines, then write
 builder file, then manager edits, then mv+header edits, 91 tweak, bash
 -n everything. Then kick off the static build in background (nohup)
 since it's very long, OR run via the ensure with big timeout. I'll
 nohup:
 nohup bash -c 'source common_functions; source manager;
 fm_ensure_dnspod_module' > /var/tmp/frankenphp_static_build.log 2>&1 &
 then report monitoring. Given user interaction pattern (they run
 things), background + monitoring is best. Actually the ensure also
 installs binary + cleanup after build - full completion unattended ✓.

 One risk in ensure tail I haven't seen (replace/verify logic). Read it
 now.

 开始实施。先看 ensure 尾部（替换/验证逻辑）与 45/47 的内部 index 标识，
 确保编辑精准：


 $ sed -n '456,535p'
 /www/programing/core_node/scripts/shells/linux/common/frankenphp_manager
 .sh; echo ===45idx===; grep -n "SCRIPT_INDEX="
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/45_
 configure_php85.sh | head -2; grep -n "frankenphp"
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/45_
 configure_php85.sh | head -5; echo ===47idx===; grep -n "SCRIPT_INDEX="
 /www/programing/core_node/scripts/shells/linux/debian/install_shells/47_
 install_composer.sh | head -2

 ... (100 earlier lines, ctrl+o to expand)
 376:if [ "$CONFIG_RUNTIME_PLANE" = "frankenphp" ]; then
 377:    echo -e "${CYAN}$SCRIPT_INDEX PHP runtime plane: frankenphp
 (Caddyfile-adjacent ini)${NC}"
 ===47idx===
 15:SCRIPT_INDEX="[34_COMPOSER]"

 Took 0.1s


 Now I have everything. 45 already detects CONFIG_RUNTIME_PLANE and
 sources frankenphp_manager, branches to frankenphp mode - so renumbering
 + header touch is enough; same for 47 (dual-mode documented).

 Now write everything:

 1. Create common/frankenphp_static_builder.sh

 2. Edit manager:
 - Add source of builder after service_contract_common source
 - Remove XCADDY/GO/DOCKER constants
 - Update FRANKENPHP_STATIC_XCADDY_ARGS (+vulcain+cbrotli)
 - Remove fm_go_bin/fm_go_gopath/fm_xcaddy_bin/fm_ensure_xcaddy block
   (need exact text - I have from earlier reads lines 228-297: fm_go_bin
   at 228... I read that section earlier. Let me reconstruct exact old
   text from the earlier read output:

 ```
   # xcaddy binary path (empty when absent); probes the fixed candidates
 plus
   # the GOPATH bin location.
 ```

 Hmm I don't have the exact full text of 224-297 verbatim. I read 240-300
 earlier:

 ```
   # xcaddy binary path (empty when absent); probes the fixed candidates
 plus
   # the GOPATH bin location.
   fm_xcaddy_bin() {
       local candidate=""
       local gopath_bin=""
       for candidate in $XCADDY_BIN_CANDIDATES; do
           if [ -x "$candidate" ]; then
               echo "$candidate"
               return 0
           fi
       done
       gopath_bin="$(fm_go_gopath)/bin/xcaddy"
       if [ -x "$gopath_bin" ]; then
           echo "$gopath_bin"
           return 0
       fi
       echo ""
   }

   # Ensure xcaddy exists. Idempotent per step: probe -> go probe ->
   # GOBIN=/usr/local/bin install -> re-probe (including the GOPATH bin
 spot
   # for non-root installs). Trusts the file probe after each step.
   fm_ensure_xcaddy() {
       local go_bin=""
       local xcaddy_bin=""

       xcaddy_bin="$(fm_xcaddy_bin)"
       if [ -n "$xcaddy_bin" ]; then
           echo "[$SCRIPT_INDEX] xcaddy already installed: $xcaddy_bin"
           return 0
       fi
       go_bin="$(fm_go_bin)"
       if [ -z "$go_bin" ]; then
           echo "[$SCRIPT_INDEX] [WARN] no Go toolchain found (golang not
 installed); xcaddy not installed"
           return 1
       fi
       echo "[$SCRIPT_INDEX] Installing xcaddy (go install ->
 /usr/local/bin)"
       $USE_SUDO env GOBIN=/usr/local/bin "$go_bin" install
 "${XCADDY_GO_MODULE}@latest"
       xcaddy_bin="$(fm_xcaddy_bin)"
       if [ -n "$xcaddy_bin" ]; then
           echo "[$SCRIPT_INDEX] xcaddy installed: $xcaddy_bin"
           return 0
       fi
       echo "[$SCRIPT_INDEX] [WARN] xcaddy unavailable after go install"
       return 1
   }
 ```

 And fm_go_bin (228) + fm_go_gopath (240) I haven't read exactly. Safer:
 read 224-250 first, then do removal edits with exact text. Also
 fm_docker_ready block (302-312) - I have partial: from earlier sed
 296-320:

 ```
   # Docker availability probe (binary present + daemon responding).
   fm_docker_ready() {
       local candidate=""
       for candidate in $DOCKER_BIN_CANDIDATES; do
           if [ -x "$candidate" ]; then
               "$candidate" info >/dev/null 2>&1 && return 0
               return 1
           fi
       done
       return 1
   }
 ```

 (complete from the sed output: yes lines shown fully.)

 Let me read 222-250 to get fm_go_bin/fm_go_gopath exact text.

 Actually — simpler & safer: rather than deleting helper blocks via edit
 (risk of mismatch), I could leave
 fm_go_bin/fm_go_gopath/fm_xcaddy_bin/fm_ensure_xcaddy/fm_docker_ready in
 place (unused) — but AGENTS says remove dead code, and user wants clean.
 I'll read the exact block then remove. One more read call.

 3. fm_dnspod_build_static rewrite - current text (docker version) from
 earlier sed 318-345:

 ```
   fm_dnspod_build_static() {
       local version_tag=""
       local work_dir=""
       local arch=""
       local container_name=""

       version_tag="$(fm_version_tag)"
       work_dir="$(mktemp -d)"
       arch="$(uname -m)"
       container_name="frankenphp-static-builder-$$"

       echo "[$SCRIPT_INDEX] [dnspod] official static build (docker
 buildx bake static-builder-musl${version_tag:+ " at ${version_tag}"})"
       if ! git clone --quiet --depth 1 ${version_tag:+--branch
 "$version_tag"} \
           "$FRANKENPHP_STATIC_REPO" "$work_dir/src" 2>/dev/null; then
           echo "[$SCRIPT_INDEX] [dnspod] [WARN] frankenphp source clone
 failed"
           rm -rf "$work_dir"
           return 1
       fi
       if ! (cd "$work_dir/src" && docker buildx bake --load \
           ${version_tag:+--set
 "static-builder-musl.args.FRANKENPHP_VERSION=$version_tag"} \
 ```

 ... and continues beyond what I read (331 showed --set XCADDY_ARGS line;
 then more until ~350 with docker cp etc). I DON'T have the full function
 text. Need to read it before replacing. Ugh. Let me read the whole
 300-410 region in one go to get exact current state of: fm_docker_ready,
 fm_dnspod_build_static (full), fm_dnspod_build_native (full, post-edit
 version), ensure function (already have). THEN make all edits.

 Also note ensure comment references native - update.

 Cleanup hook placement: in fm_ensure_dnspod_module:
 - early return path: add fm_static_apt_php_cleanup before return 0?
   Cleanup ALSO purges apt php when module already embedded ✓ idempotent.
 - after successful embed: add cleanup call after fm_store_info.

 Wait - one concern: cleanup purges apt php (php8.5-cli etc).
 43_ensure_php85_intelligent at index 43 INSTALLED it; other early
 consumers? Between 43 and 93: 45/47 MOVED to 94/96 ✓; 53_swoole (swoole
 ext for php? in frankenphp plane probably skipped/irrelevant), 175
 laravel start (after 93 ✓ uses shim). 77_ensure_php_pgsql (index 77 <
 93!) installs php8.5-pgsql via apt - runs BEFORE 93; then 93 cleanup
 purges it. Wasteful but consistent end-state. Hmm - 77 ensures php pgsql
 EXT for the runtime; after purge, pgsql support comes from the STATIC
 build (START_POSTGRESQL=true -> pdo_pgsql baked ✓ by our conditional
 extensions). Consistent ✓.

 BUT WAIT - timing problem: 77 runs BEFORE 93 and validates php -m | grep
 pgsql (probably) using /usr/local/bin/php shim → BEFORE 93, shim doesn't
 exist? The shim is created by fm_install during
 49_install_frankenphp.sh?? There's BOTH 49_install_frankenphp.sh AND
 93_install_frankenphp.sh?! Odd - 49 maybe legacy. Not my scope;
 43/77/45/47 pre-93 behavior is orchestrator's concern; user asked

 Response was truncated before completion.

全部升级为laravel 13的特性，旧特性升级。最后更新文档。先升
  级基础代码，配置等，注意本项目不使用env，所有配置全部硬编码，有些配置可以在start shell中修改，所以实际上是一个组合，注意在代码中注释，
  规范，注意合并公共类库和重复实现，按laravel 13的优化等等。可以查询官方文档。
  
全部升级为laravel 13的特性，旧特性升级。最后更新文档。先升
  级AI公共类库代码，配置等，配置使用公共类库读取 .secret_keys\.secret_ignore 。扩展出类库支持 gemini 文字图片等，deepseek, openai, 火山引擎，阿里千问，openroter:free， claudecode等等。
  扩展出的类库先不用引用。
  规范，注意合并公共类库和重复实现，按laravel 13的优化等等。可以查询官方文档。
  
以下部分已经由另一个AI正在修改：···注意本项目不使用env，所有配置全部硬编码，有些配置可以在start shell中修改，所以实际上是一个组合，注意在代码中注释，···
d
  
全部升级为laravel 13的特性，旧特性升级。最后更新文档。先升
  Machine Synchronization，laravel main / pycore UI laravel-manager一起升级。注意了解到一个UI能管理多个laravel。并能同时两端状态一起显示。
  规范，注意合并公共类库和重复实现，按laravel 13的优化等等。可以查询官方文档。
  
以下部分已经由另一个AI正在修改：···注意本项目不使用env，所有配置全部硬编码，有些配置可以在start shell中修改，所以实际上是一个组合，注意在代码中注释，···

按laravel 13的优化 注意需要从底层构架，而不是写补丁，更不能薄兼容、要按最新的规范。注意合并公共类库和重复实现，查询官方文档。ddds
注意幂等是每一个小步骤都要幂等一下，而不是一个总入口幂等一交，比如nginx安装 、 nginx是否path / nginx是否升级 / nginxg配置是否需要安装 、 修护都要幂等，前一个步骤的幂等不能影响后面的过程，需要细粒度。
注意需要从底层构架，而不是写补丁，更不能薄兼容、要按最新的规范。注意合并公共类库和重复实现，查询官方文档。不要使用多agents.按规范开发。shells不要使用任何返回值退出码，信任上一个函数的运行结果，二进制直接对文件检测。


✨ pycore / Code sync为s m 没有同步到
     scripts\shells\linux\debian\install_shells 中的脚本。
	 
	 
	 
	 
	pivolccoding.ps1 --session 01a00aba-56ec-7abf-a45a-1f4ad9c28eb2
	 
	 continue，同时现在千问每一次生成的短文默认随机一种语音角色，
但是注意，如果是一大段话，内部使用多个句子时音色角色是一样的
，一个任务默认随机一个。同时继续完成之前的任务。
全部升级为laravel 13的特性，旧特性升级。最后更新文档。先升
  级基础代码，配置等，注意本项目不使用env，所有配置全部硬编码，有些配置可以在start shell中修改，所以实际上是一个组合，注意在代码中注释，
  规范，注意合并公共类库和重复实现，按laravel 13的优化等等。可以查询官方文档。
  
全部升级为laravel 13的特性，旧特性升级。最后更新文档。先升
  级AI公共类库代码，配置等，配置使用公共类库读取 .secret_keys\.secret_ignore 。扩展出类库支持 gemini 文字图片等，deepseek, openai, 火山引擎，阿里千问，openroter:free， claudecode等等。
  扩展出的类库先不用引用。
  规范，注意合并公共类库和重复实现，按laravel 13的优化等等。可以查询官方文档。
  
以下部分已经由另一个AI正在修改：···注意本项目不使用env，所有配置全部硬编码，有些配置可以在start shell中修改，所以实际上是一个组合，注意在代码中注释，···

注意从底层构架，不要薄兼容，代码从底层重构，同时搜索官方文档，按规范修改，shell不要使用返回码退出值、信任上一个函数的运行结果。同时需要有幂等性，幂等需要拆为最小步、而不是从一大个入口就幂等跳过。
复用重复实现类库。同时搜索官方文档，搜索官方文档。


继续实现、如果以下都全部实现了，跳过本次提示词。
继续实现、注意要看实际代码，而不是看文档，文档只是参考，同时要根据代码来更新文档，搜索官方文档
docs_fix\DESIGN_20260817_2115_PYCORE_UI_RELAY_GROUPS_HTTP3_PART_0.md
docs_fix\DESIGN_20260817_2115_PYCORE_UI_RELAY_GROUPS_HTTP3_PART_1.md
docs_fix\DESIGN_20260817_2115_PYCORE_UI_RELAY_GROUPS_HTTP3_PART_3.mdF
，按规范，搜索官方文档，如果已经全部完成，则不必再继续。注意需要从底层构架，而不是写补丁，更不能薄兼容、要按最新的规范。注意合并公共类
注意，
1：注意frankenPHP安装部署，以及UI管理已经完成，根据代码直接更新文档。不要再修改相关代码，s
2：pycore端和laravemain端，根据当前代码全面升级为HTTP3 / 103 early等。并对接新的API，搜索代码，而不是.md，更新.md，如果完成了跳过。
3：pycore UI / pycore UI / pycore-manager / laravel-manager / wordnew 全面升级为支持http3 / 103early，重构底层类库，在现在的代码的基础上，搜索代码，而不是.md，更新.md，如果完成了跳过。
wordnew 要capacitor搜索官方文档，在编译为安卓时支持http3 / 103early。
同时：完成以下，如果完成了，跳过。
···
据diff的API实时调整队列首。
  11： http://127.0.0.1:13054/pycore-manager/queue-center 增
  加更详细的日志历史 记录，可以使用sqlite，生成的音频本地要缓
  存，并记录是由那个引擎生成，并能播放，是否是multe句子合成，
  laravel main是否接收上传成功，
  12：pycore保存一组任务，在laravel main离线时继续生成，当在
  线时捎带上传没有上传的记录。
  13：pycore去掉冗余代码，合并必要的类库，并实时的显示后端的
  进度状态，前端的进度状态。
  以上需要调整pycore / pycore UI / laravel main / wordnew的底
  层类库，注意支持HTTP3 FRANKENPHP ERRLY 301 ，
  如果codex无法验证上面的功能，就先给pycore加上可调试的接口或
  探测API调用。除非以上全部验证完才可忽略本条提示，否则将底层
  重构，搜索官方文档。
  注意从底层构架，不要薄兼容，代码从底层重构，同时搜索官方文
  档，按规范修改，shell不要使用返回码退出值、信任上一个函数的
  运行结果。同时需要有幂等性，幂等需要拆为最小步、而不是从一
  大个入口就幂等跳过。
  复用重复实现类库。同时搜索官方文档，搜索官方文档。···
  
  
 以下如果全部完成，则忽略，同时如果每交收到该提示词，都要校准上下文：
   根据更新的文档，继续对齐代码  ···› http://127.0.0.1:13054/
  pycore-manager/agent-history

    ，搜索官方文档如何提取，并监听，同时，agent-history
    是按最新的就放在队首，如果没有最新的了，就生成旧的，同
  时，
    任何生成都先捎带生成原来的legacy audio 到
    multi-sentence，也就是要先处理所有 到 multi-sentence
    ，同时，后端不联网也不影响一直生成，为什么现在没有继续生
  成
  1：还有很多 legacy audio没有在无率是否联网，都要捎带旧的先生成新的 multi-sentence 音频类型，注意注意千问TTS同时也给 /pycore-manager/queue-center后台提供服务，请使用队列和并行运行。短文使用多句子合成，句子队列直接生成。复用代码和队列组。
  2: 为什么最多只有50页，有什么BUG，新的监听到的提示词，或指定的AI内容，比如PI harnemss的吐字历史 ，也要生成为音频。并上穿到laravel main。
  3：现在生成的音频上传到 laravel后 ，在 wordnew 音频展示 页，要标记是否是multi-sentence 生成，则于只有新上穿的才才标记 multi-sentence ，旧的没有标识直接标识为legacy等。
  4：pi
    harness的提取类库，注意pi需要直接提取吐出的字，同时也包
  含提
    示词，一个提示词做为一表短文，吐出的字每600词截取为一个
  短文
  5：糸复 Cursor

History records
10524

Processed
0

Pending
10524 cursor提取异常的BUG，实际扫描本地，搜索官方文档。
6：对于撱AI的 Pending
83，要显示一个总队列，并不是一个AI一个AI的生成，而是把最新的放到总队列首，之后生成历史。如果监听到新的马上放到总队列，显示总队列进度。
6：显示词汇表千问TTS的队列UI。放在左边的TA，建新的页面，其中有任务中心和agent-history中的并行任务UI显示，生成进度，正在生成的内部细节等。

    。
  ···
  
  注意从底层构架，不要薄兼容，代码从底层重构，同时搜索官方文档，按规范修改，shell不要使用返回码退出值、信任上一个函数的运行结果。同时需要有幂等性，幂等需要拆为最小步、而不是从一大个入口就幂等跳过。
复用重复实现类库。同时搜索官方文档，搜索官方文档。




要求测试并修复全部可用，复用组件。
目前新的问题是为什么开启的任务没有自动运行，为什么没有浏览器重启重运行。
 ./apps/mcp-chrome 注意从底层构架，不要薄兼容，代码从底层重
  构，同时搜索官方文档，按规范修改，shell不要使用返回码退出
  值、信任上一个函数的运行结果。同时需要有幂等性，幂等需要拆
  为最小步、而不是从一大个入口就幂等跳过。
  复用重复实现类库。同时搜索官方文档，搜索官方文档。 并对齐
  最新的laravel main HTTP3 的接口，任务系统 ，确保所有实现都
  有没重复，并复用。如果已经全部为复用代码，忽略本条提示。加入API https://api.si.12gm.com 无端口为默认API，并直接测试，我已经运行了  .\apps\mcp-chrome\scripts\start.ps1
 实时编译，codex只需要 访问  MCP Tools并验证所有功能，同时laravel main的代码会实时同步到服务器，可能 会2分钟生效-中间服务器可能 重启暂时中断1-2分钟，要求测试所能功能可用，如果不可用立即搜索官方文档并底层构架修复，
 1：单词有效性任务，单词翻译任务，这两个是同一个mcp-chrome开关，2：图书封面搜索及回传，3：单词组封面gemini生成以及回传，4：验证其他功能，注意，如果codex无法验证，就先给mcp-chrome加上可调试的接口或MCP TOOLS供AI调用。除非以上全部验证完才可忽略本条提示，否则将底层重构，搜索官方文档。
 
 同时：
 1：单词组需要封面，但是单词不需要图，修正了就跳过。
 2：单词验证为什么没有一直递归 运行。
 3：类库是否全部复用，必须全部复用。
 4：UI不能有重复的显示，所有UI不要做无效显示，同时设置要全局生效。


需要测试pycore http://127.0.0.1:13054/pycore-manager/queue-center 所有功能，如果都正常可用，忽略本次提示：
1：同时laravel main的代码会实时同步到服务器，可能 会2分钟生效-中间服务器可能 重启暂时中断1-2分钟，要求测试所能功能可用，所以修改后等待服务器更新并测试，
2：Laravel endpoint API https://api.si.12gm.com 无端口为默认API，并直接测试，http://127.0.0.1:13054/pycore-manager/queue-center 即使在127.0.0.1下也默认用该API测试。
3：需要测试，单词音频生成，请求laravel main，并根据diff调整队首，并生成回传功能。
4：需要测试，句子音频生成，请求laravel main，并根据diff调整队首，并生成回传功能。句子默认使用千问TTS，注意千问TTS同时也给 /pycore-manager/agent-history 后台提供服务，请使用队列和并行运行。短文使用多句子合成，句子队列直接生成。复用代码和队列组。
5：句子任务中心默认先生成完所有英文，再生成其他语言，当然后端驱动插入队首的除外，同时laravel main后端也由UI wordnew 驱动。
6：检测 wordnew 是否在请示任何音频，单词音频、句子音频时，laravel main会由统一网关触发队列首调整，并立即通知pycore
7：wordnew默认添加API为  https://api.si.12gm.com ，并使用最新的 HTTP3 / 301early协议，更新所有类库和复用组件，搜索官方文档。
8：wordnew 在访问音频时，要得到pycore是否调整队首的返回信息，并显示，预期得到音频的时间。
9：laravel wordnew pycore 的通信全部升级库支持 HTTP3 / 301early 以及 mersuce协议 frankenPHP.
10:pycore http://127.0.0.1:13054/pycore-manager/queue-center / pycore 必须优化，后端无阻塞卡顿的保持任务组，并根据diff的API实时调整队列首。
11： http://127.0.0.1:13054/pycore-manager/queue-center 增加更详细的日志历史 记录，可以使用sqlite，生成的音频本地要缓存，并记录是由那个引擎生成，并能播放，是否是multe句子合成，laravel main是否接收上传成功，
12：pycore保存一组任务，在laravel main离线时继续生成，当在线时捎带上传没有上传的记录。
13：pycore去掉冗余代码，合并必要的类库，并实时的显示后端的进度状态，前端的进度状态。
以上需要调整pycore / pycore UI / laravel main / wordnew的底层类库，注意支持HTTP3 FRANKENPHP ERRLY 301 ，
如果codex无法验证上面的功能，就先给pycore加上可调试的接口或探测API调用。除非以上全部验证完才可忽略本条提示，否则将底层重构，搜索官方文档。
注意从底层构架，不要薄兼容，代码从底层重构，同时搜索官方文档，按规范修改，shell不要使用返回码退出值、信任上一个函数的运行结果。同时需要有幂等性，幂等需要拆为最小步、而不是从一大个入口就幂等跳过。
复用重复实现类库。同时搜索官方文档，搜索官方文档。

mysql
如何解决。dsss 还有幂等不一致的问题 servermanagerv1: skipped - canonical identity (no per-app users table)， sys：init dd.ddd Words Tab的UI太简单，培强管理功能。 同时每个条目点开后会有一个展示和编译的弹窗查看细节。同时插入，批量管理，图片展示 Vocabulary Statistics
 合并到其中，并大幅增强更多管理UI界面和功能。目前大约 30%，再增加70%功能。
 t
 
 
 不要修改API文件结构，功能代码结果 ，不要碰其他路由，只修改 /wordflow路由下的所有UI。同时只修改入美化UI，而不是调整文件d结构和功能，不要碰API代码等。能进行美化吗，/wordflow 路由下的组件等。注意该项目现在3个主题，你只需要美化 /wordflow下面的 /wordflow/*
 d
 
 在新的代码下，dd注意不是旧的代码，而是上专传后的代码。不要修改API文件结构，功能代码结果 ，不要碰其他路由，只修改 /wordnew 路由下的所有UI。
 注意不能碰其他路由和删除其他文件。你要做的是在/wordnew 路由下新加文件。d
 添中一个mock数据库提示模拟数据。专用给 /wodrdnew 使用。s
 
调整底部菜单，只显示图标，而不要文件。
调整全局的light模量下的背景和文字的对比度。以及底部菜单，搜索下拉没有跟随light的问题。 ss
修复下面的菜单，在焦点时，不显示图标只有背景渐变的、修正为背景上面显示图标。d
顶部的菜单的左边的LOGO移除掉，同时添加一个next/next的APP启动多页，在登陆时显示。d sssd
 
 1：设计个人页，Profile页、资料修改页。
 2：添加社交系统，设计复杂一点。
 3：添加好友之间的聊天，添加好友等，可以发语主方。可以通过偏好语言搜索好友。
 4：添加登陆，注册页。
 
 1：设置中添加关闭背景呼吸效果，关闭后背景就是带有渐变的背景。
 2：搜索是一个下科的滑动效果，同时搜索面板还有呼吸效果。基中添加吏多元素，经如可以选择语言，搜索保种语言。
 3：设置增加母语选择，以及选择的目标语言，f
 4：并设计一个页面，根据母语，比如是zh，目录语言比如是en，可以在该页一句en一句zh或2句en一句cn的语，可以先读母语还是目标语言。在设置中也添加该项设置。同时该页自动显示句子列表、句子中含有的单词展开，等。设计复杂一点。
 5：首页的单词组列表是方块状的，带有背景图及文字等UI，并有一键加入，以及词组内含有多少启，词组的lang属性等。
 d
 1：添加一个随身听的英文单词播放页，
 2：添加一个英文字幕的播放学习页，能交互显示句子、单词、字幕、视频
 3：添中一个学习统计面板。
 
 1：多语言还没很好的支持，datk/light没有很好的支持d
 2：添加顶部搜索、下拉，方块Card，列表组件，通知弹窗
 3：添加底瓿悬浮菜单，需要有呼吸效果。 
 4：添加设置页。d

1：现在来修复在light模式下，没有全局切换的问题，light模式也要呼吸效果、动态毛玻璃。d
2：底瓿的菜单栏，添加呼吸效果，中间有一个圆。焦点项也不有同的变色呼吸效果d。
3：设置页添加下拉，swithc，单选，多选的设置项。
4：首页添加动态，毛玻璃，背景图文的瀑布流动态效果。展示的是单词组列表。便当盒布局。

现在代码添加了一个新的UI。用来移置原来的 wordflow ，首先按你之前的记录，给新UI接上API，注意，API即能使用mock数据也能在API中请求，具体以什么方式取决于文件的导入，这样当在aistudio处理时AI自动替换为MOCK数据，而作为API时，直接全使用API的数据 ，注意当改API时要同步修改MOCK数据，同使用一个TYPE，更新文档和代码。

参考  /wordflow ，但注意只是功能，不是UI，
给/wordnew 添加功能：

添加多语言支持，中英日韩

默认API为 http://43.163.112.77:9000/ 其中 windows.currentURL : 9000端口，注意端口要修改，之后是127.0.0.1:9000  100.101.149.39 100.106.85.16 端口都是9000
之后可以在设置中添加一个item，可以查看和设置和修改，同时API自动测试选择可用的，如果上一次选择的可用就直接用，如果都不可用就要一直测试。
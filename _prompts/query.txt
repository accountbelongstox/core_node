/www/wwwroot/ is current wwwroot dir, it's windows the logic , mount is /mnt/dev_nvme1n1p1/www/wwwroot/   found all sh,py,php,resolve Why the project is Not recoganize
    to correct, if has Windows or larget-dist,priority used. liunx/
    liunx-desktop/ linux +windows NTFS(DATA disk) / windows /
    liunx + free space disk / liunx + large other disk , www to large disk,  in windows to
    large DATA disk(usualy to D:/www). if in liunx with windows-NTFS ,www mapping to windows and use windows DATA,nUse multiple roles to cross-validate each
    other.Use multiple roles to cross-validate each other. How aline all-group.up code.

Uer Claude Code workflow and mutiple roles:
从历史记录 /mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt  中 1:1 的重新核对代码，并按 History.txt 中的先后顺序（文档结束处为最新）1:1 align / 更新代码，不要看 claude code 的历史记录和对话记录，只需要关注  History.txt 和实际代码，当你 1:1 跟新代码后，将History.txt中的相关记录部分删除，但要在同目录生成一个删除记录文件（新文件，内容为 del 100-1000 行），你自行在 History.txt 中整理一条线，比如 pycore、sh 脚本修复，每次处理一部分，直到 History.txt 的 50000 多行记录全部对齐，注意 xxx/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt 中要按最新的来对齐到代码（文档结束处为最新），旧的内容只是修改过程中的积累，注意需要编辑和对齐所有代码，不是仅仅汇报。

Uer Claude Code workflow and mutiple roles:
本次主要任务为:```
use claude code ,workflow ,veriry ,If laravelmain need help,like this translate text or audio gerete or picture conver image , that chrome-mcp and pycore task center Is'n enough, need up UI show task detail,and up pycoore task center and chrome-mcp UI and task system, the logic need strong ,and needs to works.
if wordnew appUI request any word/sentenc, if not translate-content / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task top and quik process it.use claude code ,workflow ,veriry ,If laravelmain need help,like this translate text or audio gerete or picture conver image , that chrome-mcp and pycore task center Is'n enough, need up UI show task detail,and up pycoore task center and chrome-mcp UI and task system, the logic need strong ,and needs to works.
  This is a substantial feature spanning several subsystems (laravel_main, the pycore task center, chrome-mcp, and the task UI/system). Before I orchestrate anything, I need to understand what exists today — I won't run a workflow blind into a vague request. Let me locate these subsystems first.
  Located them: pycore/, apps/mcp-chrome, ncore/utils/mcp_chrome, and poly_apps/laravel_main. This is a multi-subsystem mapping job — let me fan out parallel Explore agents to map each one (task cenWter, chrome-mcp, laravel_main task integration, and the translate/audio/image task flows) so I understand the real architecture before proposing anything.,and if wordnew appUI request any word/sentenc, if not translate-content / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task top and quik process it.and if wordnew appUI request any word/sentenc, if not translate-content
  / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task
  top and quik process it.and if wordnew appUI request any word/sentenc, if not translate-content
  / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task
  top and quik process it. across verify all logic code and overflow.
  ```
  
Uer Claude Code workflow and mutiple roles:
从历史记录 /mnt/dev_nvme1n1p1/programing/core_node/_prompts/HistoryPycorePHP.txt(Only this file)  中 1:1 的重新核对代码，并按 HistoryPycorePHP.txt 中的先后顺序（文档结束处为最新）1:1 align / 更新代码，不要看 claude code 的历史记录和对话记录，只需要关注  HistoryPycorePHP.txt 和实际代码，当你 1:1 跟新代码后，HistoryPycorePHP.txt中的相关记录部分删除，但要在同目录生成一个删除记录文件（新文件，内容为 del 100-1000 行），你任务到 HistoryPycorePHP.txt 的 50000 多行记录全部对齐，注意  HistoryPycorePHP.txt 中要按最新的来对齐到代码（文档结束处为最新），旧的内容只是修改过程中的积累，注意需要编辑和对齐所有代码，不是仅仅汇报。
本次主要恢复的任务为:```
use claude code ,workflow ,veriry ,If laravelmain need help,like this translate text or audio gerete or picture conver image , that chrome-mcp and pycore task center Is'n enough, need up UI show task detail,and up pycoore task center and chrome-mcp UI and task system, the logic need strong ,and needs to works.
if wordnew appUI request any word/sentenc, if not translate-content / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task top and quik process it.use claude code ,workflow ,veriry ,If laravelmain need help,like this translate text or audio gerete or picture conver image , that chrome-mcp and pycore task center Is'n enough, need up UI show task detail,and up pycoore task center and chrome-mcp UI and task system, the logic need strong ,and needs to works.
  This is a substantial feature spanning several subsystems (laravel_main, the pycore task center, chrome-mcp, and the task UI/system). Before I orchestrate anything, I need to understand what exists today — I won't run a workflow blind into a vague request. Let me locate these subsystems first.
  Located them: pycore/, apps/mcp-chrome, ncore/utils/mcp_chrome, and poly_apps/laravel_main. This is a multi-subsystem mapping job — let me fan out parallel Explore agents to map each one (task center, chrome-mcp, laravel_main task integration, and the translate/audio/image task flows) so I understand the real architecture before proposing anything.,and if wordnew appUI request any word/sentenc, if not translate-content / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task top and quik process it.and if wordnew appUI request any word/sentenc, if not translate-content
  / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task
  top and quik process it.and if wordnew appUI request any word/sentenc, if not translate-content
  / conver image / audio ,Is it'n to task top, and Pycore / Chrome-MCP client a same time to task
  top and quik process it. across verify all logic code and overflow.
  ```

Uer Claude Code workflow and mutiple roles:
Cross-check the code 1:1 against the history log "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt", and update the code strictly according to the order of entries in "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt" (starting from the end of the document, with the latest records first). Do not refer to Claude Code's history or conversation logs. Only use "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt" and the current codebase.

After updating the code 1:1, remove the corresponding processed entries from "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt". At the same time, create a new deletion log file in the same directory, recording the removed line range in the format "del 100-1000line".

Organize the work into a clear processing sequence, such as "pycore", shell script fixes, etc. Handle one section at a time until all 50,000+ lines in "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt" have been fully reconciled with the code.

Always align the code with the latest records in "/mnt/dev_nvme1n1p1/programing/core_node/_prompts/History.txt". Older entries are only historical records; if they are no longer useful, delete them after the task is completed.

Each time ,complete only one main line of fixes.

Use multiple roles to cross-validate each other.Use multiple roles to cross-validate each other.

Based on the original repair recored /mnt/dev_nvme0n1p1/programing/core_node/_prompts/ClaudeCodeRepair.txt ,reimplement the code exactly as it
  was,one-to-one,Do not rely on your own conversation history or memory,Instedad,directly restore and fix the code according to the original
  implementation.
  
Based on the original repair recored /mnt/dev_nvme0n1p1/programing/core_node/_prompts/Python-Repair.txt ,reimplement the code exactly as it
  was,one-to-one,Do not rely on your own conversation history or memory,Instedad,directly restore and fix the code according to the original
  implementation.
  
Based on the original repair recored /mnt/dev_nvme0n1p1/programing/core_node/_prompts/Base-Scripts-Repair.txt,reimplement the code exactly as it
  was,one-to-one,Do not rely on your own conversation history or memory,Instedad,directly restore and fix the code according to the original
  implementation.
  


多角色执行结果 — Chrome MCP Task Center

  角色: chrome(重)
  交付: 把 popup 重构为统一 Task Center 为主界面;NotebookLM +
    Gemini 从一次性工具升级为 worker
    处理器(NotebookLMProcessor/GeminiImageProcessor +
    SimpleWorkerBase,many-to-many);TaskCenter 按 task_type
    中央派发;所有开关移入 Task
    Center(总开关/各处理器开关/后端增删/选项),各功能保留功能测
  试面板(不碰队列),移除了各扩展的"开头";统一历史+缓存+提交记录+
  状态(task_center_history
      环形库,per-(task,backend) 提交/ack,Gemini 图走
    media-cache);tsc 干净
  状态: ✅ 待 1 行改
  ────────────────────────────────────────
  角色: laravel
  交付: 新增 notebooklm/gemini_image 两个 task_type +
    结果处理器(NotebookLm→新表,Gemini→复用
    WordTranslationWriteback 写 image_files)+ 手动入队 POST
    /ai_tools/task/enqueue;通知机制
    pending_urgent(pull+heartbeat 回传 priority≥100 计数)+ pull

     长轮询(pullAndAssignTasksLongPoll,新任务即时返回);确认
    /library/3 打开即把缺媒体词 bump 到 100;overview
    增加两类;文档 AppQyV1_TASK_CENTER_PIPELINE.md
  状态: ✅ 待对齐
  ────────────────────────────────────────
  角色: pycore
  交付: 每个 assist
    功能可单独启用+优先级(word_audio/sentence_audio/cover/poste
  r/translation),持久化
      assist_laravel.priority,经 /capabilities/settings 的新
    assist 块读写;worker 按优先级排序认领、禁用即跳过;overview
    暴露 + 透传 chrome 的两类;更新 QUEUE_CENTER_PIPELINE.md
  状态: ✅

  满足你的三点

  1. Task Center 协助:翻译(Bing)、缺图(Bing/Gemini)、NotebookLM
  生成、Gemini 图片生成 —— 全部进 worker
  队列;历史/缓存/提交记录/many-to-many/生成状态齐备;开关统一进
  Task Center;扩展只剩功能测试。
  2. laravel:主动扫描入队(已有)+ 打开 /library/...?view=table
  时缺图音的词立即提前(priority 100)+ 长轮询即时通知
  chrome-mcp。
  3. pycore:生成音频/封面/海报 + 句子音频,在 Task Center/Queue
  Center 可设置 + 排优先级;全部写入功能设置文档。

  正在后台收尾(1 处路由对齐)

  四角色并行后发现:notebooklm/gemini_image 若都用
  remote_client,会和 word_media 抢同一池子(按 execution_type
  原子认领)→ 错误 worker 抢走任务饿死。正在改为各自独立
  execution_type(remote_notebooklm/remote_gemini,仿
  word_audio→remote_audio):laravel 加枚举+映射,chrome
  两个处理器各注册自己的 processor_type。改完每个 worker
  只拉自己的类型。

  等这两个后台 agent 返回我做最终核对(execution_type
  三端一致)。落地:laravel/FE/chrome
  只写未跑(feedback-no-compile-test,扩展未打包),pycore
  过了导入/语法+行为校验;新端点/迁移在下次 pyservice/laravel
  起服务时生效。

 查看capactor的googlew一键登陆，和github的一键登陆，添加全局
  库，并在UI中引用在登陆注册入。同时
  ✽ Sublimating… (2m 8s · ↓ 4.0k tokens) f❯ edit
  profile同时能修改其他资料，比如个人说明，手机，邮箱认
    证等f修改密码，google /github登陆绑定等。在代码中注释需要
  在那里申请KEY来接入GOOGLE一键登陆，并扩展qy
  app的后端子用户数据表，注
  意总表是对所有APP通过，当然针对GOOGLE
  GITHUB等的支持如果有必要就扩展总表，并在sys
  init中幂等扩展总用户数据表或子APP的用户数据表。d
 
 查看前端的修改个人介绍 ，后端的支持
  情况，注意后端的用户数据是子APP扩展主库，如果没有对应的字段在d
  sys:init要幂等加入，目标语言的设计不要和profile的编辑同一页，d
  而是设置中。同时设置中一些荐如果不登陆不显示，比如设置native
  language和目标语言，再说重申目标语言可以选择多个，并同步到用
  户设置也就是后端，前端也要更新状态。同时扩展整个项目库，添加
  必要的库，确保每个页都显示在URL路由页名或路由名。学习模型单独
  设计一个项和一个子设计页，其中有，每日计算学习词，背词模量工dd
  式：随身听、卡片，默认随身听，随身听时展示子区域的设置，播放
  一次，复播1次，读单词，读解释（默认不勾），播放速度：默认0.8s
  ，播放间隔：默认0，复播间隔（播放词后隔多少个后再复播一次），d
  复揪速率，重播间隔，并加入复习设置的子项和子页。将以上功能更
  新到功能设计文档，更新代码。

读取代码并写一个部署文档，注意文档先写一个md文件 dochtml目录中

注意是模拟 doc文件一页接一页的往下走，不是左边菜单。同时要像一个夜店网站，而不是文档站。

  解压作为一个前端，写一个 scripts/start.ps1在前端项目中，改用pnpm自动安装启动， 之后将文档写到其中，不再介绍ps1等脚本，而是将ps1脚本中的启动要求写为文档中里边，也就是某个傻B用户拿到文档后自己按ps1中的过程折腾，而不是让它直接运行ps1脚本。
  
  做前后端多层面的闭环验证。dd
  d
  已经调整了项目位置，现在文档写为中文的。
  
  添加需要下载adb.exe，以及放到那个位置，并说明这是为 calin 开发的破解google RCS以及群发、的系统 。以及一些其他设置。同时说明系统是源码还没有编译，编译后可以直接运行。
  参考其中做打印设计，防止文档打印时和渲染差距太多。
  详细加入，包括标题。附一个说
  
  不要使用绝对路径，而是要在代码中说明全部使用项目所在的相对路径。
  
  说了标题不要用英文。dddrds验证一下，为了保证每个项目都压缩成功，需要过滤掉一些d不必要的编译子目录，和文件。然后复制到一个临时目录，再压缩。当然如果有了压缩文件，则跳过
  
  其他项目 呢，之前扫描到的所有用户名下的所有项目吗。使用扩展名，搜索所有用户名的可能 的目录下，递归 找到存在的项目，只要找到一个就跳过继续深入扫描。包括C++等所有项目。
  
  这些项目呢，先加入到。d

OK没问题。

在登陆页，用户名可以是任何字符，不一定是emal.同时注册页和后端一致，同时后端在qyapp中添加注册时可以选择目录学习语言（多选），同时前端扩展和后端的可选语言一致，
后端laravel main.

注意，确时保证代码能使用mock数据和接入API
  ，通过一个代码中的硬编码切换，能直接切换，当现在拉入API如果
  缺少MOCK数据时，根据后端补齐MOCK数据，MOCK数据要对齐后端，这f
  样做是s为了让另一个AI在没有API的时候能修改UI。d

如何解决。dsss 还有幂等不一致的问题 servermanagerv1: skipped - canonical identity (no per-app users table)， sys：init dd.ddd Words Tab的UI太简单，培强管理功能。 同时每个条目点开后会有一个展示和编译的弹窗查看细节。同时插入，批量管理，图片展示 Vocabulary Statistics
 合并到其中，并大幅增强更多管理UI界面和功能。目前大约 30%，再增加70%功能。
 t
 ddd
 
 不要修改API文件结构，功能代码结果 ，不要碰其他路由，只修改 /worddflow路由下的所有UI。同时只修改入美化UI，而不是调整文件d结构和功能，不要碰API代码等。能进行美化吗，/wordflow 路由下的组件等。注意该项目现在3个主题，你只需要美化 /wordflow下面的 /wordflow/*
 d
 
 在新的代码下，dd注意不是旧的代码，而是上专传后的代码。不d要修改API文件结构，功能代码结果 ，不要碰其他路由，只修改 /wordnew 路由下的所有UI。
 注意不能碰其他路由和删除其他文件。你要做的是在/wordnew 路由下新加文件。d
 添中一个mock数据库提示模拟数据。专用给 /wodrdnew 使用。s
 
调整底部菜单，只显示图标，而不要文件。
调整全局的light模量下的背景和文字的对比度。以及底部菜单，搜索下拉没有跟随light的问题。 ss
修复下面的菜单，在焦点时，不显示图标只有背景渐变的、修正为背景上面显示图标。d
顶部的菜单的左边的LOGO移除掉，同时添加一个next/next的APP启动多页，在登陆时显示。d sssd
 
 1：设计个人页，Profile页、资料修改页。s
 2：添加社交系统，设计复杂一点。
 3：添加好友之间的聊天，添加好友等，可以发语主方。可以通过偏好语言搜索好友。ffd
 4：添加登陆，注册页。
 
 1：设置中添加关闭背景呼吸效果，关闭后背景就是带有渐变的背景。
 2：搜索是一个下科的滑动效果，同时搜索面板还有呼吸效果。基中添加吏多元素，经如可以选择语言，搜索保种语言。 s
 3：设置增加母语选择，以及选择的目标语言，f
 4：并设计一个页面，根据母语，比如是zh，目录语言比如是en，可以在该页一句en一句zh或2句en一句cn的语，可以先读母语还是目标语言。在设置中也添加该项设置。同时该页自动显示句子列表、句子中含有的单词展开，等。设计复杂一点。
 5：首页的单词组列表是方块状的，带有背景图及文字等UI，并有一键加入，以及词组内含有多少启，词组的lang属性等。
 d
 1：添加一个随身听的英文单词播放页，
 2：添加一个英文字幕的播放学习页，能交互显示句子、单词、字幕、视频
 3：添中一个学习统计面板。
 
 1：多语言还没很好的支持，datk/light没有很好的支持d
 2：添加顶部搜索、下拉，方块Card，列表组件，通知弹窗
 3：添加底瓿悬浮菜单，需要有呼吸效果。 
 4：添加设置页。d

1：现在来修复在light模式下，没有全局切换的问题，light模式也要呼吸效果、动态毛玻璃。d
2：底瓿的菜单栏，添加呼吸效果，中间有一个圆。焦点项也不有同的变色呼吸效果d。
3：设置页添加下拉，swithc，单选，多选的设置项。
4：首页添加动态，毛玻璃，背景图文的瀑布流动态效果。展示的是单词组列表。便当盒布局。

现在代码添加了一个新的UI。用来移置原来的 wordflow ，首先按你之前的记录，给新UI接上API，注意，API即能使用mock数据也能在API中请求，具体以什么方式取决于文件的导入，这样当在aistudio处理时AI自动替换为MOCK数据，而作为API时，直接全使用API的数据 ，注意当改API时要同步修改MOCK数据，同使用一个TYPE，更新文档和代码。

参考  /wordflow ，但注意只是功能，不是UI，
给/wordnew 添加功能：

添加多语言支持，中英日韩

默认API为 http://43.163.112.77:9000/ 其中 windows.currentURL : 9000端口，注意端口要修改，之后是127.0.0.1:9000  100.101.149.39 100.106.85.16 端口都是9000
之后可以在设置中添加一个item，可以查看和设置和修改，同时API自动测试选择可用的，如果上一次选择的可用就直接用，如果都不可用就要一直测试。

扫描一下还有虽的项目吗，本机。不要写脚本，直接用AI能力扫。sDddddddFSss的代码dsffdddfsddds
Tesstfff


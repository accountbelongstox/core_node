# AGENTS.md — Workspace

- **First run:** if `BOOTSTRAP.md` exists, follow it, then delete it.
- **Each session:** read `SOUL.md`, `USER.md`, today's + yesterday's `memory/YYYY-MM-DD.md`. Memory is files, not your head.
- **MEMORY.md:** load/edit only in main (1:1) sessions — never in shared/group contexts (private data).
- **Group/heartbeat:** add value or reply `HEARTBEAT_OK`; keep a checklist in `HEARTBEAT.md`.
- **Safety:** never exfiltrate private data; ask before anything that leaves the machine or is destructive.
- **Git:** `git commit` only (local), no push unless asked; otherwise edit freely.

> D:\programing\core_node\ncore 扫描该目录，完成一个 D:\programing\core_node\ncore\utils
单例启动类，并在注释中标明这是示例类库无须直接引入，而是在要实现的类中同等的实现一个，在主入
口中，将启动两个线程，一个是客户端通信线程，另一个是后端主线程，由于可能则不同的客户端启动，
所以有一个硬编码的配置端口和IP（默认localhost），当客户端通信线程检测到硬编码的信号时，说明有
其他客户端启动了这个程序，后面可以停止启动，只启运客户端通信即可，这样就可以多个客户端共用一
个后台，减少启动和浪费。但需要 特别注释，只能使用pytho的官方库。并实现一代码，注释顺可以说明
复制这个类库到其他客户端实现即可，D:\programing\core_node\ncore\utils\rpc
扫描这个目录，实现在其中（注意不要破坏三来的rpc，你只能扩展），之后添加一个文件，单例启动rpc
示例和入口.js文件。

FINAL SOLUTION: FIX NGINX WEBSITE LIST DISPLAY
===============================================

Date: 2025-11-14
Issue: nginx_manager.sh "Show All Websites" not displaying Laravel-added websites

CONFIRMED ROOT CAUSE
--------------------

Environment Detection Issue in gvar_common.sh map_web_path() function:

Current Environment:
- IS_WSL: false
- IS_PRODUCTION: false
- BASE_DATA_DIR: /www

Current Logic in map_web_path():
```bash
if [ "$IS_WSL" = true ]; then
    base_path="$data_base/www"
elif [ "$IS_PRODUCTION" = true ]; then
    base_path="/www"
else
    # Desktop/Non-production
    base_path="$data_base/www"  # Results in /www/www
fi
```

Problem:
- System is running on /www (production-like setup)
- But IS_PRODUCTION = false (detected as desktop/development)
- So base_path = /www/www (incorrect)
- Should be base_path = /www (correct)

Result:
- map_web_path("nginxconfig") returns /www/www/nginxconfig
- But actual nginx configs are in /www/nginxconfig
- nginx_manager.sh looks in wrong directory
- Shows empty list

DETAILED SOLUTION
-----------------

Fix the map_web_path() function to handle this case correctly.

FILE: /www/programing/core_node/scripts/shells/linux/common/gvar_common.sh
FUNCTION: map_web_path() (around line 823)

CURRENT CODE:
```bash
map_web_path() {
    local path_key="$1"
    local sub_path="$2"
    local mapped_path=""
    local base_path=""

    # Get optimal base directory
    local data_base=$(get_base_data_directory)

    # Determine base path for www based on environment
    if [ "$IS_WSL" = true ]; then
        base_path="$data_base/www"
    elif [ "$IS_PRODUCTION" = true ]; then
        base_path="/www"
    else
        # Use data base directory + www
        base_path="$data_base/www"
    fi

    # Map paths using common base path
    case "$path_key" in
        "wwwroot")
            mapped_path="$base_path/wwwroot"
            ;;
        "nginxconfig")
            mapped_path="$base_path/nginxconfig"
            ;;
        ...
    esac

    echo "$mapped_path"
}
```

PROPOSED FIX:
```bash
map_web_path() {
    local path_key="$1"
    local sub_path="$2"
    local mapped_path=""
    local base_path=""

    # Get optimal base directory
    local data_base=$(get_base_data_directory)

    # Determine base path for www based on environment
    if [ "$IS_WSL" = true ]; then
        base_path="$data_base/www"
    elif [ "$IS_PRODUCTION" = true ]; then
        base_path="/www"
    else
        # Desktop/Non-production environment
        # Special case: if data_base is /www, don't double it
        if [ "$data_base" = "/www" ]; then
            base_path="/www"
        else
            base_path="$data_base/www"
        fi
    fi

    # Map paths using common base path
    case "$path_key" in
        "wwwroot")
            mapped_path="$base_path/wwwroot"
            ;;
        "nginxconfig")
            mapped_path="$base_path/nginxconfig"
            ;;
        ...
    esac

    echo "$mapped_path"
}
```

CHANGE EXPLANATION:
In the else branch (non-production), added check:
- If data_base is already "/www", use it directly
- Don't append "/www" again to avoid "/www/www"
- This handles production-like setups that aren't marked as production

TESTING AFTER FIX
-----------------

Test 1: Path Resolution
Command: bash -c 'source gvar_common.sh && map_web_path "nginxconfig"'
Expected Result: /www/nginxconfig
Current Result: /www/www/nginxconfig ❌
After Fix: /www/nginxconfig ✓

Test 2: WWW Root
Command: bash -c 'source gvar_common.sh && map_web_path "wwwroot"'
Expected Result: /www/wwwroot or /www/www/wwwroot (depends on setup)
Should be consistent with nginx config location

Test 3: nginx_manager.sh
1. Run: bash /www/programing/core_node/scripts/shells/linux/debian/server_manager/nginx_manager.sh
2. Select: 6) Show All Websites
Expected: Display all 18 domains
Current: Empty list ❌
After Fix: Shows 18 domains ✓

Test 4: Laravel Command (should still work)
Command: cd /www/programing/core_node/poly_apps/laravel_main && php artisan servermanager:website summary
Expected: Shows 18 domains ✓
After Fix: Should still work ✓

IMPLEMENTATION STEPS
--------------------

Step 1: Backup Current File
```bash
cp /www/programing/core_node/scripts/shells/linux/common/gvar_common.sh \
   /www/programing/core_node/scripts/shells/linux/common/gvar_common.sh.backup
```

Step 2: Edit gvar_common.sh
Open file at line 823 (map_web_path function)
Apply the proposed fix shown above

Step 3: Test the Fix
Run all tests listed in TESTING AFTER FIX section

Step 4: Verify No Breaking Changes
Check other scripts that use map_web_path:
- 130_setup_domains.sh
- nginx_manager.sh
- Any other scripts in the project

Step 5: Document the Change
Add comment in code explaining the fix:
```bash
# Fix: When data_base is /www, don't append /www again
# This handles production-like setups not marked as IS_PRODUCTION=true
# Issue: nginx_manager.sh was looking in /www/www/nginxconfig instead of /www/nginxconfig
```

ALTERNATIVE SOLUTION (If main fix has issues)
----------------------------------------------

If the proposed fix breaks other functionality, use targeted fix:

Update nginx_manager.sh directly (lines 21-24):

CURRENT:
```bash
WWW_ROOT=$(map_web_path "wwwroot")
NGINX_CONFIG_DIR=$(map_web_path "nginxconfig")
NGINX_SITES_AVAILABLE="$NGINX_CONFIG_DIR/sites-available"
NGINX_SITES_ENABLED="$NGINX_CONFIG_DIR/sites-enabled"
```

TARGETED FIX:
```bash
# Fix: Use direct paths to match Laravel ServerManagerV1
# TODO: Remove this when gvar_common.sh map_web_path is fixed
if [ "$(map_web_path "nginxconfig")" = "/www/www/nginxconfig" ]; then
    # Wrong path detected, use correct one
    NGINX_CONFIG_DIR="/www/nginxconfig"
    WWW_ROOT="/www/wwwroot"
else
    # Use mapped paths
    WWW_ROOT=$(map_web_path "wwwroot")
    NGINX_CONFIG_DIR=$(map_web_path "nginxconfig")
fi
NGINX_SITES_AVAILABLE="$NGINX_CONFIG_DIR/sites-available"
NGINX_SITES_ENABLED="$NGINX_CONFIG_DIR/sites-enabled"
```

This is less ideal but safer if unsure about side effects.

VERIFICATION CHECKLIST
----------------------

After applying fix:
☐ nginx_manager.sh shows all 18 websites
☐ Laravel artisan commands still work
☐ 130_setup_domains.sh still works
☐ No broken symlinks in /etc/nginx/sites-enabled
☐ Nginx can reload configuration successfully
☐ All paths are consistent across system

DATA CONSISTENCY VERIFICATION
------------------------------

After fix, verify these paths match:

1. Laravel Database (domains.json)
   nginx_config_file: /www/nginxconfig/sites-available/[domain]

2. Actual Nginx Files
   Location: /www/nginxconfig/sites-available/[domain]

3. Shell Script Resolution
   map_web_path("nginxconfig") -> /www/nginxconfig

4. Symlinks
   /etc/nginx/sites-enabled/[domain] -> /www/nginxconfig/sites-available/[domain]

All should use /www/nginxconfig (not /www/www/nginxconfig)

SUMMARY
-------

Root Cause: Environment detection mismatch causing wrong path resolution
Impact: nginx_manager.sh cannot find Laravel-generated website configurations
Solution: Fix map_web_path() to avoid /www/www duplication
Risk: Low - targeted fix with fallback option available
Benefit: Consistent paths across all system components

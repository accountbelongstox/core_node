
 ▐▛███▜▌   Claude Code v2.1.185
▝▜█████▛▘  Opus 4.8 (1M context) with xhigh effort · Claude Max
  ▘▘ ▝▝    /mnt/dev_nvme0n1p1/programing/core_node


❯   Executing: 31_ensure_php85_intelligent.sh

  Starting execution of 31_ensure_php85_intelligent.sh...
  [31_PHP85_INTELLIGENT] INSTALL_NGINX: true
  ============================================================================
  [31_PHP85_INTELLIGENT] Intelligent PHP 8.5 Installation System
  ============================================================================
  [31_PHP85_INTELLIGENT] [PRE-FLIGHT] Running system checks...
  [31_PHP85_INTELLIGENT] Sudo access confirmed
  [31_PHP85_INTELLIGENT] [PRE-CHECK] Running pre-installation verification...
  [31_PHP85_INTELLIGENT] Checking PHP 8.5 state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary not found: /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP binary not found
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIGENT] PHP not available for version check
  [31_PHP85_INTELLIGENT] Skipping PHP-FPM check - Using Swoole instead
  [31_PHP85_INTELLIGENT] [DETECTION] Checking required PHP extensions...
  [31_PHP85_INTELLIGENT] PHP not available for extension check
  >>> [31_PHP85_INTELLIGENT] Checking symbolic link: /usr/local/bin/php
  [ERROR] [31_PHP85_INTELLIGENT] Symlink not found: /usr/local/bin/php
  [31_PHP85_INTELLIGENT] State: Not installed
  [31_PHP85_INTELLIGENT] Checking Composer state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing Composer state analysis...
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing Composer binary state...
  [31_PHP85_INTELLIGENT] Composer not found
  [31_PHP85_INTELLIGENT] Note: Composer will be installed in the next step via 34_install_composer.sh
  [31_PHP85_INTELLIGENT] Composer: Not available
  [31_PHP85_INTELLIGENT] Note: Composer will be installed in the next step via 34_install_composer.sh
  [31_PHP85_INTELLIGENT]  PHP 8.5 needs attention
  [31_PHP85_INTELLIGENT]  Composer needs attention (state code: 1)
  [31_PHP85_INTELLIGENT] Some components require installation/repair. Proceeding with installation...
  [31_PHP85_INTELLIGENT]  PHP 8.5 installation required
  [31_PHP85_INTELLIGENT]  Composer installation required
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary not found: /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP binary not found
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIG
  ──── (109 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  on:

  Unsatisfied dependencies:
   php8.5-cli : Depends: php8.5-readline but it is not going to be installed
                Depends: libsodium23 (>= 1.0.18) but it is not going to be installed
                Depends: libxml2 (>= 2.9.4) but it is not installable
   php8.5-fpm : Depends: libsodium23 (>= 1.0.18) but it is not going to be installed
                Depends: libxml2 (>= 2.9.4) but it is not installable
   php8.5-xml : Depends: libxml2 (>= 2.9.4) but it is not installable
   php8.5-zip : Depends: libzip4 (>= 1.7.0) but it is not installable
  Error: Unable to satisfy dependencies. Reached two conflicting assignments:
     1. php8.5-xml:amd64=8.5.7-1+0~20260606.19+debian12~1.gbp741f99 is selected for install
     2. php8.5-xml:amd64 Depends libxml2 (>= 2.9.4)
        but none of the choices are installable:
        [no choices]
  [31_PHP85_INTELLIGENT] Failed to install PHP 8.5
  [31_PHP85_INTELLIGENT] Repository setup failed
  [31_PHP85_INTELLIGENT] [FINAL] Running comprehensive verification...
  [31_PHP85_INTELLIGENT] [VERIFICATION] Running po..
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary not found:
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP binary not found
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIGENT] PHP not available for version check
  [31_PHP85_INTELLIGENT] Skipping PHP-FPM check - Using Swoole instead
  [31_PHP85_INTELLIGENT] [DETECTION] Checking required PHP extensions...
  [31_PHP85_INTELLIGENT] PHP not available for extension check
  >>> [31_PHP85_INTELLIGENT] Checking symbolic link: /usr/local/bin/php
  [ERROR] [31_PHP85_INTELLIGENT] Symlink not found: /usr/local/bin/php
  [31_PHP85_INTELLIGENT] State: Not installed
  [31_PHP85_INTELLIGENT] [FAIL] Post-installation verification FAILED
  [31_PHP85_INTELLIGENT] Some issues remain after installation
  ============================================================================
  [31_PHP85_INTELLIGENT] [FAILED] PHP 8.5 installation/configuration failed with exit code: 1
  [31_PHP85_INTELLIGENT] [INFO] Check the output above for specific error details
  [31_PHP85_INTELLIGENT] [INFO] You may need to run the script again or fix issues manually
  ============================================================================

  Script execution completed with exit code: 1
  Press any key to return to test scripts menu...
   adapt to Ubuntu / Debian / Kali.

I'll investigate the actual root cause on this machine and find the script's repo-selection logic. Let me gather facts in parallel.

  Searched for 2 patterns, ran 1 shell command

Key finding already: the repo is pinned to Sury bookworm (Debian 12 stable), but this is Kali rolling 2026.2 (tracks Debian testing). And libxml2/libzip4 have no candidate at all in Kali's index — the bookworm-built PHP packages can't resolve. Let me confirm what Kali actually names those libs and read the script's distro→suite logic.

  Searched for 1 pattern, ran 2 shell commands

Now the root cause is crisp. Kali rolling 2026.2 has moved past two SONAME transitions:
- libxml2 → libxml2-16 (the bare libxml2 package no longer exists)
- libzip4 → libzip5

But the script maps Kali → Sury bookworm (Debianckages still Depends: libxml2 (>= 2.9.4) andlibzip4 — names that don't exist in Kali. That's the failure. The bug is in apt_repository_manager.sh:132-138: kali-rolling
isn't in the allowlist, so it falls back to book

The real question is which Sury suite matches Kali's current libraries. Let me test empirically which suite's php8.5 packages depend on libxml2-16/libzip5 rather than guess.

  Ran 1 shell command

The awk paragraph-match was wrong, and importantly forky/sid return 418 — Sury doesn't publish them (only bookworm and trixie exist). Let me properly extract the dependency lines from bookworm vs trixie.

  Ran 1 shell command

Critical detail: trixie fixes libzip (→libzip5, which Kali has) but php8.5-cli/xml still Depends: libxml2 — and Kali only ships libxml2-16. So I need to confirm whether Kali's libxml2-16 satisfies a libxml2 dependency (via Provides:), or whether even trixie would fail. Let me check the virtual-provides and the keyring, then run the decisive install simulation.

  Ran 1 shell command

Decisive finding forming: Kali ships libxml2-16  provides a virtual libxml2. Since even Sury'strixie php8.5 still Depends: libxml2 (the old .so.2 package), no published Sury suite may resolve on Kali. Let me run the
authoritative install simulation against trixie the real SONAME.

  Ran 1 shell command

No passwordless sudo in my session (the script itself runs interactively with sudo). That's fine — I can confirm the ABI gap without it. Let me verify whether Sury ships its own libxml2, and inspect the real SONAME Kali's libxml2-16 provides.

  Ran 1 shell command

Diagnosis is now conclusive and it's deeper than a wrong suite — it's a real ABI gap:

- Kali rolling 2026.2 ships libxml2 2.15.3 as package libxml2-16, providing libxml2.so.16. The old libxml2 package (providing libxml2.so.2) is gone, with no virtual Provides.
- Sury ships no libxml2 itself (neither suite) — it relies on the distro's. Both suites' php8.5-cli/php8.5-xml Depends: libxml2 (the .so.2 package) → unsatisfiable on Kali.
- Switching to trixie does fix the other half (php8.5-zip → libzip5, which Kali has at 1.11.4), but the libxml2 gap remains fatal for the core CLI package. And forky/sid Sury suites return 418 — they don't exist.

Let me confirm the compat-shim feasibility (Debiallable) and read the exact script logic I'dpatch.

  Read 1 file, ran 1 shell command

I've now fully root-caused this, and it's deeper than a misconfigured repo — it's a real library ABI gap. Here's the definitive picture:

Why it failed

┌────────────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────┐
│         Layer          │                          What's true on this box (Kali rolling 2026.2)                          │
├────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Repo written           │ Sury bookworm (Debian 12 stable) — php.list                                                     │
├────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Kali's libxml2         │ libxml2-16 = libxml2 2.15.3, ships only libxml2.so.16. The old libxml2 package (.so.2) is gone, │
│                        │  with no virtual Provides.                                                                      │
├────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Kali's libzip          │ libzip5 1.11.4 (old libzip4 gone)                                                               │
├────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Sury bookworm php8.5   │ libxml2 (≥2.9.4) + liKali → the exact error you saw                │
│ needs                  │                                                                                                 │
├────────────────────────┼────────────────────────────────────────────────────────────────────┤
│ Sury trixie php8.5     │ libzip5 ✅ (Kali has it) but still libxml2 (.so.2) ❌                                           │
│ needs                  │                                                                                                 │
├────────────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
│ Sury forky/sid suites  │ Don't exist (HTTP 418)                                                                          │
└────────────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────┘

Two distinct bugs in apt_repository_manager.sh:132-138:
1. Kali maps to bookworm (oldest) instead of the newest published Sury suite. kali-rolling isn't in the allowlist → falls back to bookworm.
2. The allowlist passes through forky|sid, but Sury publishes neither → those would 404/418.

The unavoidable catch: Kali (tracking Debian sid/forky) already moved to libxml2 2.15 / .so.16. Sury's newest build (trixie) still links libxml2.so.2. php8.5-cli itself depends on libxml2 (libxml is core, non-disableable PHP), so no Sury PHP 8.5 can install or run on Kali 2026.2 without a libxml2.so.2 present. I verified the clean remedy: Debian trixie's libxml2 package (2.12.7, .so.2) is co-installable — it depends only on libc6, liblzma5, zlib1g, all present on Kali, and won't collide with libxml2-16 (different package name + different .so files).

So the fix has two parts: (A) suite mapping bookd codenames — unambiguously correct, I'll do itregardless; (B) the libxml2 gap — a judgment call on how aggressive to be.

● User answered Claude's questions:
· How should the script handle the libxml2 .so.2 gap on bleeding-edge Kali (where PHP 8.5 literally cannot run without a compat lib)? → Auto-shim, opt-out flag

Implementing the Auto-shim (opt-out) approach. Let me read the exact install/repo call-sites so I place the patch precisely.

  Read 2 files, ran 3 shell commands

Now let me make sure libxml2 is the only gap — I need to verify the full trixie php8.5 dependency closure resolves on Kali (otherwise a libxml2-only shim is a partial fix).

  Ran 1 shell command

Confirmed: libxml2 is the only gap. With the trixie suite, the entire php8.5 dependency closure resolves on Kali (libcurl4t64, libssl3t64, libzip5, libonig5, libgd3, libpq5… all present) — only libxml2 (.so.2) is missing. So the fix is precisely: map → trixie, then shim the single libxml2 gap.

Before I change the shared resolver's default, l it (to avoid breaking Docker/MySQL/edge on Kali).

  Ran 1 shell command

The resolver is shared by PHP, Docker, PostgreSQL (PGDG), and Tailscale too. That actually reinforces the fix: bookworm is generically wrong for Kali — all those repos' bookworm packages predate Kali's t64 ABI (libssl3t64, libcurl4t64, libzip5). trixie (post-t64, Debian stable for ~10 months now) is the correct target for all of them, and they all publish it. So changing the shared default bookworm→trixie is the right "adapt to Kali" fix, kept env-overridable.

Let me implement. Edit 1 — fix the resolver's Debian codename mapping:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 12 lines, removed 4 lines
    fi

    # Accept only codenames the upstream repos actually publish; anything else
    # (kali-rolling, parrot, sid-on-a-rolling-derivative, empty) falls back to a
    # stable codename so the written line resolves instead of 404-ing.
    # (kali-rolling, parrot, the rolling testing/unstable suites, empty) falls back
    # to the newest hosted codename so the written line resolves instead of 404-ing.
    case "$vendor" in
        ubuntu)
            case "$codename" in

            esac
            ;;
        debian)
            # Sury/Docker/PGDG/Tailscale publish bullseye/bookworm/trixie but NOT the
            # rolling testing/unstable suites (forky/sid -> HTTP 418/404). Rolling
            # derivatives (kali-rolling, parrot, ...) also carry the post-t64 ABI
            # (libssl3t64, libcurl4t64, libzip5, libxml2 2.15) that bookworm-built
            # packages predate and cannot satisfy, so map anything outside the hosted
            # set to the newest hosted suite (trixie), which matches that ABI. Pinning
            # to bookworm here is what made Sury PHP 8.5 fail on Kali (libzip4 /
            # libxml2 deps absent). Override with APT_DEBIAN_CODENAME_DEFAULT.
            case "$codename" in
                bullseye|bookworm|trixie|forky|sid) : ;;
                *) codename="${APT_DEBIAN_CODENAME_DEFAULT:-bookworm}" ;;
                bullseye|bookworm|trixie) : ;;
                *) codename="${APT_DEBIAN_CODENAME_DEFAULT:-trixie}" ;;
            esac
            ;;
    esac

Edit 2 — add the libxml2 compat-shim function. Let me check the lines just above the PHP repo functions for a clean insertion anchor.

  Read 1 file, ran 1 shell command

Now insert the shim function just before the PHP repository functions:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 97 lines
# COMMON REPOSITORY MANAGEMENT FUNCTIONS
# ============================================================================

# Bridge the libxml2 SONAME gap so Sury PHP 8.5 can install AND run on bleeding-edge
# Debian derivatives (e.g. Kali rolling).
#
# Sury's php8.5-cli / php8.5-xml depend on the historic `libxml2` package, which
# ships libxml2.so.2. Rolling derivativ already moved to
# libxml2 2.15 (package `libxml2-16`, soname libxml2.so.16) and dropped the old
# `libxml2` package with no virtual Pro unsatisfiable
# and, even if force-installed, the PHP binary (linked against .so.2) would fail to
# load. libxml2 is core, non-disableable PHP, so this blocks PHP entirely.
#
# When the gap is detected, install Debian's `libxml2` (.so.2) compat package for the
# resolved Sury codename (the suite Sury built against, so the ABI matches). It depends
# only on libc6/liblzma5/zlib1g (present on such systems) and co-installs cleanly next
# to libxml2-16 (different package name, different .so files). Idempotent; a no-op when
# libxml2.so.2 is already present or the distro still ships a real `libxml2` candidate.
#
# Opt out with PHP_LIBXML2_COMPAT_SHIM=0 -- then a clear gap message is printed and the
# caller's PHP install is left to fail loudly instead of silently mixing in a lib.
ensure_php_libxml2_compat_from_apt_repository_manager() {
    local php_vendor="$1"     # ubuntu | debian
    local php_codename="$2"   # resolved repo codename (e.g. trixie)

    # Only the Debian/Sury path is affected; Ubuntu's ondrej PPA tracks Ubuntu libs.
    [ "$php_vendor" = "debian" ] || return 0

    # Already satisfiable? Either the distro offers a real `libxml2` candidate, the
    # .so.2 is already on disk (prior sge is installed.
    local cand
    cand="$(apt-cache policy libxml2 2>/dev/null | awk '/Candidate:/{print $2}')"
    if [ -n "$cand" ] && [ "$cand" != "(none)" ]; then
        return 0
    fi
    if ldconfig -p 2>/dev/null | grep -q 'libxml2\.so\.2'; then
        return 0
    fi
    if dpkg-query -W -f='${Status}' libxml2 2>/dev/null | grep -q 'install ok installed'; then
        return 0
    fi

    echo "[libxml2-compat] This distro has no 'libxml2' (libxml2.so.2) package that Sury PHP 8.5 needs;" >&2
    echo "[libxml2-compat]   it ships only the newer libxml2-16 / libxml2.so.16." >&2

    case "${PHP_LIBXML2_COMPAT_SHIM:-1}" in
        0|false|no|off)
            echo "[libxml2-compat] PHP_-> NOT installing the compat lib." >&2
            echo "[libxml2-compat] The PHP 8.5 install will fail until libxml2.so.2 is provided." >&2
            echo "[libxml2-compat] Re-rIM=1 (default) to auto-install it." >&2
            return 0
            ;;
    esac

    local arch; arch="$(dpkg --print-architecture 2>/dev/null || echo amd64)"
    local idx_url="http://deb.debian.org/debian/dists/${php_codename}/main/binary-${arch}/Packages.gz"
    echo "[libxml2-compat] Resolving Debian ${php_codename} libxml2 (.so.2) compat package..." >&2

    local tmp; tmp="$(mktemp -d 2>/dev/null || echo "/tmp/libxml2compat.$$")"
    mkdir -p "$tmp"
    local idx="$tmp/Packages.gz"
    if ! curl -fsSL -o "$idx" "$idx_url"; then
        echo "[libxml2-compat] ERROR: could not fetch Debian package index ($idx_url)." >&2
        rm -rf "$tmp"; return 1
    fi

    # Filename: of the binary package named exactly 'libxml2' (the .so.2 one). The
    # trailing \n in the regex preventsxml2-dev / etc.
    local fname
    fname="$(zcat "$idx" 2>/dev/null \
        | awk 'BEGIN{RS="";ORS="\n\n"} /(^|\n)Package: libxml2\n/' \
        | sed -n 's/^Filename: //p' | head -n1)"
    if [ -z "$fname" ]; then
        echo "[libxml2-compat] ERROR: Debian ${php_codename} has no 'libxml2' (.so.2) package." >&2
        rm -rf "$tmp"; return 1
    fi

    local deb_url="http://deb.debian.org/debian/${fname}"
    local deb="$tmp/libxml2-compat.deb"
    echo "[libxml2-compat] Downloading ${deb_url}" >&2
    if ! curl -fsSL -o "$deb" "$deb_url"; then
        echo "[libxml2-compat] ERROR: download failed ($deb_url)." >&2
        rm -rf "$tmp"; return 1
    fi

    echo "[libxml2-compat] Installing compat libxml2 (.so.2) alongside libxml2-16..." >&2
    if ! $USE_SUDO dpkg -i "$deb"; then
        # Pull any missing deps from th1g normally present).
        $USE_SUDO apt-get -f install -y 2>/dev/null || true
    fi
    rm -rf "$tmp"

    if ldconfig -p 2>/dev/null | grep -q 'libxml2\.so\.2'; then
        echo "[libxml2-compat] OK: libxml2.so.2 now available (Debian ${php_codename} compat lib)." >&2
        return 0
    fi
    echo "[libxml2-compat] WARNING: libxml2.so.2 still absent after shim; PHP install may fail." >&2
    return 1
}

# Add PHP repository (Ubuntu/Debian) with automatic backup and restore
# Ubuntu: uses ppa.launchpadcontent.net and Launchpad PPA signing key (avoids certificate mismatch with ppa.lau
nchpad.net)
# Debian: uses packages.sury.org and Sury key

Now wire the shim into both PHP-repo functions. Let me re-read their current bodies (line numbers shifted after the insert).

  Read 1 file, ran 1 shell command

Edit 3 — wire shim into the non-permanent functi

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 3 lines
        php_repo_line="deb [signed-by=$php_key_file] https://packages.sury.org/php/ $php_codename main"
    fi

    # Bridge the libxml2.so.2 gap on rolling derivatives before the PHP install runs.
    ensure_php_libxml2_compat_from_apt_repository_manager "$php_vendor" "$php_codename"

    execute_with_repo_backup_from_apt_repository_manager \
        "php" \
        "$php_repo_line" \

Edit 4 — wire shim into the permanent function (the one script 31 actually uses):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 3 lines
    echo "Updating apt cache..."
    $USE_SUDO apt update 2>/dev/null || true

    # Bridge the libxml2.so.2 gap on rolling derivatives before the PHP install runs.
    ensure_php_libxml2_compat_from_apt_repository_manager "$php_vendor" "$php_codename"

    if [ -n "$command_to_execute" ]; then
        echo "Executing: $command_to_execute"
        eval "$command_to_execute"

Now verify: syntax check, the resolver's actual output on this Kali box, and that the shim's package-finder locates a real downloadable .deb.

  Ran 1 shell command

All mechanical checks pass:
- bash -n → syntax valid
- Resolver now returns debian trixie on this Kali box (was effectively bookworm)
- The shim's finder extracts the real Debian-trixie libxml2_2.12.7…_amd64.deb and its URL returns HTTP 200

The only steps I can't execute here are the sudoo passwordless sudo in my session) — those runwhen you execute the script interactively. Let me capture the diff and run an adversarial multi-lens review of the change to
catch anything subtle in the shell/idempotency/f

  Ran 1 shell command

I notice the diff shows the whole resolver function and several other changes (backup dir, keyring restore, gpg --yes) as already-modified in the working tree — those pre-existed this session; my additions are the trixie clamp, the shim function, and the two wiring calls. All my edits are present and correct.

Now let me run an adversarial multi-lens review of this install-critical change (which I can't fully execute due to sudo), focused on shell correctness, failure-safety, and cross-distro behavior.

● Workflow(Adversarially review the PHP 8.5 / Kali libxml2 install fix in apt_repository_manager.sh)
/workflows to view dynamic workflow runs

The review is running in the background. While it works, let me confirm the ordering in script 31 — that setup_php_repository (where the shim now runs) executes before the core/extension installs that need libxml2.

  Read 1 file

Ordering confirmed: Step 3 setup_php_repository (where the shim now runs, before the repo's bundled apt install) executes
before Step 4 install_php_core and the later extso.2 is in place before any PHP package isinstalled.

Here's where things stand while the adversarial review finishes:

Root cause (definitive)

Your 31_*.sh → apt_repository_manager.sh mapped Kali rolling → Sury bookworm (Debian 12). But Kali 2026.2 tracks Debian sid and has moved past two SONAME transitions:
- libxml2 → libxml2-16 (libxml2 2.15.3, ships only libxml2.so.16; old libxml2 package gone, no virtual Provides)
- libzip4 → libzip5

Sury's bookworm php8.5 still Depends: libxml2 + libzip4 → neither exists on Kali → the exact [no choices] error. And forky/sid Sury suites don't exist (HTTP 418).

Fix applied (in apt_repository_manager.sh)

1. Suite mapping — Debian codename clamp now keeps only {bullseye,bookworm,trixie} (the suites Sury/Docker/PGDG/Tailscale
actually host); anything outside (kali‑rolling, trixie (newest hosted, matches Kali's post‑t64ABi), env‑overridable via APT_DEBIAN_CODENAME_DEFAULT. trixie alone fixes the libzip side and the rest of the dep closure.
2. libxml2 .so.2 compat shim (new ensure_php_libry_manager) — the one remaining gap. When nolibxml2 satisfier exists, it pulls Debian trixie's real libxml2 (2.12.7, .so.2; deps libc6/liblzma5/zlib1g, all present) and dpkg -is it next to libxml2-16. Default‑on, opt out with PHP_LIBXML2_COMPAT_SHIM=0. Wired into both repo‑add functions, before the PHP install.

Empirically validated on your box: bash -n clean, resolver now emits debian trixie, the shim resolves the real .deb (HTTP 200), and the full php8.5 dep closure resolves on Kali with trixie except libxml2 (which the shim covers). I'll fold in the multi-agent review findings as soon as it completes.

✻ Churned for 14m 19s

❯   Executing: 31_ensure_php85_intelligent.sh

  Starting execution of 31_ensure_php85_intelligent.sh...
  [sudo] password for kali:
  [31_PHP85_INTELLIGENT] INSTALL_NGINX: true
  ============================================================================
  [31_PHP85_INTELLIGENT] Intelligent PHP 8.5 Installation System
  ================================================
  [31_PHP85_INTELLIGENT] [PRE-FLIGHT] Running system checks...
  [31_PHP85_INTELLIGENT] Sudo access confirmed
  [31_PHP85_INTELLIGENT] [PRE-CHECK] Running pre-installation verification...
  [31_PHP85_INTELLIGENT] Checking PHP 8.5 state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary not found: /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP binary not found
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIGENT] PHP not available for version check
  [31_PHP85_INTELLIGENT] Skipping PHP-FPM check - Using Swoole instead
  [31_PHP85_INTELLIGENT] [DETECTION] Checking required PHP extensions...
  [31_PHP85_INTELLIGENT] PHP not available for extension check
  >>> [31_PHP85_INTELLIGENT] Checking symbolic link: /usr/local/bin/php
  [ERROR] [31_PHP85_INTELLIGENT] Symlink not found: /usr/local/bin/php
  [31_PHP85_INTELLIGENT] State: Not installed
  [31_PHP85_INTELLIGENT] Checking Composer state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing Composer state analysis...
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing Composer binary state...
  [31_PHP85_INTELLIGENT] Composer not found
  [31_PHP85_INTELLIGENT] Note: Composer will be installed in the next step via 34_install_composer.sh
  [31_PHP85_INTELLIGENT] Composer: Not available
  [31_PHP85_INTELLIGENT] Note: Composer will be installed in the next step via 34_install_composer.sh
  [31_PHP85_INTELLIGENT]  PHP 8.5 needs attention (state code: 1)
  [31_PHP85_INTELLIGENT]  Composer needs attention (state code: 1)
  [31_PHP85_INTELLIGENT] Some components require installation/repair. Proceeding with installation...
  [31_PHP85_INTELLIGENT]  PHP 8.5 installation required
  [31_PHP85_INTELLIGENT]  Composer installation required
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary not found: /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP binary not found
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP ve
  ──── (404 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  Next step will configure PHP settings and Composer
  [31_PHP85_INTELLIGENT] PHP 8.5 installation execution completed successfully
  [31_PHP85_INTELLIGENT] [FINAL] Running comprehensive verification...
  [31_PHP85_INTELLIGENT] [VERIFICATION] Running po..
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing comprehensive PHP state analysis...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary found: /us
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP binary state...
  [31_PHP85_INTELLIGENT] PHP command found: /usr/local/bin/php
  [31_PHP85_INTELLIGENT] PHP version: 8.5
  [31_PHP85_INTELLIGENT]
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIGENT] Current PHP version: 8.5
  [31_PHP85_INTELLIGENT] PHP version is compatible
  [31_PHP85_INTELLIGENT] Skipping PHP-FPM check - Using Swoole instead
  [31_PHP85_INTELLIGENT] [DETECTION] Checking required PHP extensions...
  [31_PHP85_INTELLIGENT] Extension curl (curl): OK
  [31_PHP85_INTELLIGENT] Extension mbstring (mbstring): OK
  [31_PHP85_INTELLIGENT] Extension xml (xml): OK
  [31_PHP85_INTELLIGENT] Extension zip (zip): OK
  [31_PHP85_INTELLIGENT] Extension gd (gd): OK
  [31_PHP85_INTELLIGENT] Extension mysql (mysqli): OK
  [31_PHP85_INTELLIGENT] Extension pgsql (pdo_pgsql): OK
  [31_PHP85_INTELLIGENT] Extension sqlite3 (sqlite3): OK
  [31_PHP85_INTELLIGENT] Extension opcache (Zend O
  [31_PHP85_INTELLIGENT] Extension intl (intl): MISSING
  [31_PHP85_INTELLIGENT] Extension bcmath (bcmath)
  [31_PHP85_INTELLIGENT] Missing extensions: intl
  >>> [31_PHP85_INTELLIGENT] Checking symbolic link: /usr/local/bin/php
  [INFO] [31_PHP85_INTELLIGENT] Current symlink target: /usr/bin/php8.5
  [OK] [31_PHP85_INTELLIGENT] Symlink is correct: /usr/local/bin/php -> /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] State: Installed but missing required extensions
  [31_PHP85_INTELLIGENT] [FAIL] Post-installation verification FAILED
  [31_PHP85_INTELLIGENT] Some issues remain after installation
  ============================================================================
  [31_PHP85_INTELLIGENT] [FAILED] PHP 8.5 installation/configuration failed with exit code: 1
  [31_PHP85_INTELLIGENT] [INFO] Check the output above for specific error details
  [31_PHP85_INTELLIGENT] [INFO] You may need to run the script again or fix issues manually
  ============================================================================

  Script execution completed with exit code: 1
  Press any key to return to test scripts menu...
   fix it ,support Ubuntu Debian Kali.

Excellent — the fix worked: PHP 8.5.7 installed  did exactly its job (OK: libxml2.so.2 nowavailable), suite resolved to trixie, 9 extensions loaded. But there's a second instance of the identical bug class:
php8.5-intl → libicu76 unsatisfiable (Kali bumpeml2). I missed it earlier because intl isinstalled later by name, outside the core bundle I dep-checked.

The right fix is to generalize the shim so it covers every such SONAME gap in one pass, not whack-a-mole. Let me first scan the full Sury php8.5 package set to find all gaps on Kali (so the fix is complete), and confirm libicu76 is pullable from Debian trixie.

  Ran 1 shell command

The fix works — PHP 8.5.7 is installed and running, and the libxml2 shim performed exactly as designed. The remaining failure is the same bug class, second instance: php8.5-intl → libicu76 (Kali bumped ICU past trixie's 76).

The complete gap scan shows only libicu76 matters for this project — the other two gaps (libgpgme11t64, libsnmp40t64) belong to php8.5-snmp/php8.5-gnupg, which the project never installs (so I must not over-provision those). libicu76 is cleanly pullable from Debian trixie (76.1-4, deps all present, HTTP 200).

So I'll generalize the shim to resolve compat libs for the project's actual php package set in one pass. Let me find the canonical package/extension list.

  Read 1 file, ran 1 shell command

The project's full php set = PHP85_CORE_PACKAGES (cli, common) + CORE_EXTENSIONS (the 11, incl. intl) — and these arrays are already sourced into the script's environment. Let me verify that resolving compat libs against exactly this bounded set yields only libicu76 (and previously libxml2) — never the snmp/gnupg libs.

  Ran 1 shell command

● Dynamic workflow "Adversarially review the PHP 8.5 / Kali libxml2 install fix in apt_repository_manager.sh" completed · 6m 8s

The bounded test confirms it: the project set yields exactly libicu76 as the gap (libxml2 now installed → gone), and the snmp/gnupg libs are correctly excluded.

The adversarial review also landed with strong findings I'll fold into the generalized shim:
- must-fix (security): the .deb is fetched over plain HTTP and dpkg -i'd as root with no integrity check → root-RCE surface. Fix: HTTPS + verify SHA256 from the Packages stanza (already parsed) before install.
- must-fix: apt-get -f install -y 2>/dev/null || true swallows all diagnostics → fix surfaces as opaque downstream error.
- should-fix: noninteractive frontend + --force-confold, constrain -f install so it can't remove the lib, self-ensure curl, set -u-safe os-release reads, and glibc-gate the trixie clamp (so a bookworm-era derivative with glibc<2.38 stays on bookworm).

Let me confirm the Debian index carries SHA256: and supports HTTPS, then write the generalized + hardened shim.

  Ran 1 shell command

All confirmed: SHA256 in the stanza, HTTPS works (200), glibc/dpkg compare available. Now let me replace the libxml2-specific shim with the generalized, hardened version. Let me read its current exact text.

  Read 1 file, ran 1 shell command

All prerequisites confirmed. Now I'll generalize the shim (so it covers libicu76 and any future SONAME gap, bounded to the project's actual package set) and fold in the review's security/robustness fixes. Replacing the libxml2-specific function:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 133 lines, removed 70 lines
# COMMON REPOSITORY MANAGEMENT FUNCTIONS
# ============================================================================

# Bridge the libxml2 SONAME gap so Sury PHP 8.5 can install AND run on bleeding-edge
# Debian derivatives (e.g. Kali rolling).
# Bridge SONAME/library gaps so Sury PHbleeding-edge
# Debian derivatives (e.g. Kali rolling) that have moved ahead of the suite Sury
# builds against.
#
# Sury's php8.5-cli / php8.5-xml depend on the historic `libxml2` package, which
# ships libxml2.so.2. Rolling derivatives that track Debian sid have already moved to
# libxml2 2.15 (package `libxml2-16`, soname libxml2.so.16) and dropped the old
# `libxml2` package with no virtual Provides -- so the dependency is unsatisfiable
# and, even if force-installed, the PHP binary (linked against .so.2) would fail to
# load. libxml2 is core, non-disableable PHP, so this blocks PHP entirely.
# Sury's php8.5-* packages (built for Debian <codename>) depend on that suite's library
# versions -- e.g. php8.5-cli/xml -> `libxml2` (libxml2.so.2), php8.5-intl -> `libicu76`.
# Rolling derivatives that track Debian sid have already bumped those SONAMEs (libxml2
# 2.15 -> package libxml2-16 / libxml2.so.16; ICU 76 -> libicu77+) and DROPPED the old
# package names with no virtual Provides, so the dependency is unsatisfiable and the
# extension binaries (linked against the old soname) would not load. libxml2 is core,
# non-disableable PHP, so a missing libxml2 blocks PHP entirely.
#
# When the gap is detected, install Debian's `libxml2` (.so.2) compat package for the
# resolved Sury codename (the suite Sury built against, so the ABI matches). It depends
# only on libc6/liblzma5/zlib1g (presentalls cleanly next
# to libxml2-16 (different package name, different .so files). Idempotent; a no-op when
# libxml2.so.2 is already present or thlibxml2` candidate.
# For each library dependency of the REQUESTED php packages that is unsatisfiable on
# this host, install Debian <codename>'s build of that exact library (the suite Sury
# built against -> ABI match). Such libs are SONAME-versioned, so they co-install
# cleanly next to the distro's newer version (different package name + different .so).
# Only libs that the requested php packages actually need AND that the host lacks AND
# that Debian publishes are touched -- never a blanket pull (so e.g. libsnmp40t64 for
# php8.5-snmp is left alone when that extension is not requested). Idempotent: a no-op
# once the libs are installed or the distro already satisfies them.
#
# Opt out with PHP_LIBXML2_COMPAT_SHIM=0 -- then a clear gap message is printed and the
# caller's PHP install is left to fail loudly instead of silently mixing in a lib.
ensure_php_libxml2_compat_from_apt_repository_manager() {
    local php_vendor="$1"     # ubuntu | debian
    local php_codename="$2"   # resolved repo codename (e.g. trixie)
# Security: each .deb is fetched over HTTPS and verified against the SHA256 in the same
# Packages index stanza before it is installed as root; a mismatch discards the file.
#
# Opt out with PHP_COMPAT_SHIM=0 (legacIM=0) -- then a
# clear gap report is printed and the caller's PHP install is left to fail loudly
# instead of silently mixing in a lib.
#
# Args: <vendor: ubuntu|debian> <codename> <space-separated php package list>
ensure_php_compat_libs_from_apt_repository_manager() {
    local php_vendor="$1"
    local php_codename="$2"
    local php_pkgs="$3"

    # Only the Debian/Sury path is affected; Ubuntu's ondrej PPA tracks Ubuntu libs.
    [ "$php_vendor" = "debian" ] || return 0
    [ -n "${php_pkgs// /}" ] || return 0

    # Already satisfiable? Either the distro offers a real `libxml2` candidate, the
    # .so.2 is already on disk (prior shim / leftover), or the package is installed.
    local cand
    cand="$(apt-cache policy libxml2 2>/dev/null | awk '/Candidate:/{print $2}')"
    if [ -n "$cand" ] && [ "$cand" != "(none)" ]; then
        return 0
    fi
    if ldconfig -p 2>/dev/null | grep -q 'libxml2\.so\.2'; then
        return 0
    fi
    if dpkg-query -W -f='${Status}' libxml2 2>/dev/null | grep -q 'install ok installed'; then
        return 0
    fi

    echo "[libxml2-compat] This distro has no 'libxml2' (libxml2.so.2) package that Sury PHP 8.5 needs;" >&2
    echo "[libxml2-compat]   it ships only the newer libxml2-16 / libxml2.so.16." >&2

    case "${PHP_LIBXML2_COMPAT_SHIM:-1}" in
    case "${PHP_COMPAT_SHIM:-${PHP_LIBXML2_COMPAT_SHIM:-1}}" in
        0|false|no|off)
            echo "[libxml2-compat] PHP_LIBXML2_COMPAT_SHIM disabled -> NOT installing the compat lib." >&2
            echo "[libxml2-compat] The PHP 8.5 install will fail until libxml2.so.2 is provided." >&2
            echo "[libxml2-compat] Re-run with PHP_LIBXML2_COMPAT_SHIM=1 (default) to auto-install it." >&2
            echo "[php-compat] Compat-lib shim disabled (PHP_COMPAT_SHIM=0); not adjusting libraries." >&2
            echo "[php-compat] If Sury PHP 8.5 deps are unsatisfiable on this distro, the install will fail." >
&2
            return 0
            ;;
    esac

    # Be self-contained regardless of caller order (the shim fetches/unpacks on its own).
    command -v curl >/dev/null 2>&1 || ensure_packages_from_apt_repository_manager curl 2>/dev/null || true
    command -v zcat >/dev/null 2>&1 || ensure_packages_from_apt_repository_manager gzip 2>/dev/null || true

    local arch; arch="$(dpkg --print-architecture 2>/dev/null || echo amd64)"
    local idx_url="http://deb.debian.org/debian/dists/${php_codename}/main/binary-${arch}/Packages.gz"
    echo "[libxml2-compat] Resolving Debian ${php_codename} libxml2 (.so.2) compat package..." >&2
    local tmp
    tmp="$(mktemp -d 2>/dev/null)" || {
        echo "[php-compat] ERROR: mktemp -d failed; refusing a predictable temp path." >&2
        return 1
    }

    local tmp; tmp="$(mktemp -d 2>/dev/null || echo "/tmp/libxml2compat.$$")"
    mkdir -p "$tmp"
    local idx="$tmp/Packages.gz"
    if ! curl -fsSL -o "$idx" "$idx_url
        echo "[libxml2-compat] ERROR: could not fetch Debian package index ($idx_url)." >&2
        rm -rf "$tmp"; return 1
    # 1) Sury index for the resolved suite -> the php8.5-* dependency lists.
    local sury_idx="$tmp/sury.Packages.gz"
    if ! curl -fsSL -o "$sury_idx" "https://packages.sury.org/php/dists/${php_codename}/main/binary-${arch}/Pac
kages.gz"; then
        echo "[php-compat] WARNING: could not fetch Sury ${php_codename} index; skipping compat pre-resolution.
" >&2
        rm -rf "$tmp"; return 0
    fi

    # Filename: of the binary package named exactly 'libxml2' (the .so.2 one). The
    # trailing \n in the regex prevents matching libxml2-utils / libxml2-dev / etc.
    local fname
    fname="$(zcat "$idx" 2>/dev/null \
        | awk 'BEGIN{RS="";ORS="\n\n"} /(^|\n)Package: libxml2\n/' \
        | sed -n 's/^Filename: //p' | head -n1)"
    if [ -z "$fname" ]; then
        echo "[libxml2-compat] ERROR: Debian ${php_codename} has no 'libxml2' (.so.2) package." >&2
        rm -rf "$tmp"; return 1
    # 2) Union of the lib* deps of the requested php packages (strip version constraints
    #    and '|' alternatives). index()l -> no regex pitfalls.
    local pkg
    for pkg in $php_pkgs; do
        zcat "$sury_idx" 2>/dev/null \
          | awk -v RS='' -v p="$pkg" 'index($0 "\n", "Package: " p "\n")==1' \
          | sed -n 's/^Depends: //p'
    done > "$tmp/depends.txt" 2>/dev/null
    local libs
    libs="$(tr ',' '\n' < "$tmp/depends.txt" | sed 's/|.*//; s/^[[:space:]]*//; s/[[:space:]].*//' | grep -E '^
lib' | sort -u)"

    # 3) Which of those lib deps are UNsatisfiable on this host?
    local gaps="" lib cand
    for lib in $libs; do
        cand="$(apt-cache policy "$lib" 2>/dev/null | awk '/Candidate:/{print $2}')"
        if [ -n "$cand" ] && [ "$cand" != "(none)" ]; then continue; fi
        if dpkg-query -W -f='${Status}' "$lib" 2>/dev/null | grep -q 'install ok installed'; then continue; fi
        gaps="$gaps $lib"
    done
    gaps="$(printf '%s\n' $gaps | sed '/^$/d' | sort -u | tr '\n' ' ')"

    if [ -z "${gaps// /}" ]; then
        rm -rf "$tmp"; return 0
    fi

    local deb_url="http://deb.debian.org/debian/${fname}"
    local deb="$tmp/libxml2-compat.deb"
    echo "[libxml2-compat] Downloading ${deb_url}" >&2
    if ! curl -fsSL -o "$deb" "$deb_url"; then
        echo "[libxml2-compat] ERROR: download failed ($deb_url)." >&2
    echo "[php-compat] Sury PHP 8.5 needs libraries this distro no longer provides:${gaps}" >&2
    echo "[php-compat] (a rolling derivative bumped these SONAMEs past Sury's ${php_codename} build target)" >&
2

    # 4) Resolve + integrity-verify each gap lib from Debian <codename> (the suite Sury
    #    built against), then collect the verified .debs.
    local deb_idx="$tmp/debian.Packages.gz"
    if ! curl -fsSL -o "$deb_idx" "https://deb.debian.org/debian/dists/${php_codename}/main/binary-${arch}/Pack
ages.gz"; then
        echo "[php-compat] ERROR: could not fetch Debian ${php_codename} index; cannot provide compat libs." >&
2
        rm -rf "$tmp"; return 1
    fi

    echo "[libxml2-compat] Installing compat libxml2 (.so.2) alongside libxml2-16..." >&2
    if ! $USE_SUDO dpkg -i "$deb"; then
        # Pull any missing deps from the distro (libc6/liblzma5/zlib1g normally present).
        $USE_SUDO apt-get -f install -y 2>/dev/null || true
    local debs="" missing="" stanza fname exp_sha got_sha out
    for lib in $gaps; do
        stanza="$(zcat "$deb_idx" 2>/dev/null | awk -v RS='' -v p="$lib" 'index($0 "\n", "Package: " p "\n")==1
' | head -c 200000)"
        fname="$(printf '%s\n' "$stanza" | sed -n 's/^Filename: //p' | head -n1)"
        exp_sha="$(printf '%s\n' "$stanza" | sed -n 's/^SHA256: //p' | head -n1)"
        if [ -z "$fname" ]; then
            echo "[php-compat] NOTE: Debian ${php_codename} has no '${lib}' package; cannot shim it." >&2
            missing="$missing $lib"; continue
        fi
        out="$tmp/${lib}.deb"
        echo "[php-compat] Fetching ${lib} <- https://deb.debian.org/debian/${fname}" >&2
        if ! curl -fsSL -o "$out" "httpname}"; then
            echo "[php-compat] WARNING: download failed for ${lib}." >&2
            missing="$missing $lib"; continue
        fi
        if [ -z "$exp_sha" ]; then
            echo "[php-compat] ERROR: no SHA256 in index for ${lib}; refusing to install unverified .deb." >&2
            missing="$missing $lib"; continue
        fi
        got_sha="$(sha256sum "$out" 2>/dev/null | awk '{print $1}')"
        if [ "$got_sha" != "$exp_sha" ]; then
            echo "[php-compat] ERROR: SHA256 mismatch for ${lib} (want ${exp_sha}, got ${got_sha}); discarding.
" >&2
            missing="$missing $lib"; continue
        fi
        debs="$debs $out"
    done

    # 5) Install all verified compat libs at once (so any inter-deps resolve together),
    #    non-interactively, keeping exioving packages to
    #    "fix" the transaction. Diagnostics are NOT swallowed.
    if [ -n "${debs// /}" ]; then
        echo "[php-compat] Installing verified compat libs alongside the distro's newer versions..." >&2
        if ! $USE_SUDO env DEBIAN_FRONTEND=noninteractive dpkg -i --force-confold $debs; then
            $USE_SUDO env DEBIAN_FRONTEND=noninteractive apt-get -f install -y \
                -o APT::Get::Remove=false -o Dpkg::Options::=--force-confold || true
        fi
    fi

    # 6) Verify outcome loudly. A lib that is unshimmable (no Debian package / failed
    #    verify) is reported but not treated as the whole step failing.
    local still_missing=""
    for lib in $gaps; do
        if dpkg-query -W -f='${Status}' "$lib" 2>/dev/null | grep -q 'install ok installed'; then continue; fi
        case " $missing " in *" $lib "*) continue ;; esac
        still_missing="$still_missing $lib"
    done

    rm -rf "$tmp"

    if ldconfig -p 2>/dev/null | grep -q 'libxml2\.so\.2'; then
        echo "[libxml2-compat] OK: libxn ${php_codename} compat lib)." >&2
        return 0
    if [ -n "${still_missing// /}" ]; then
        echo "[php-compat] WARNING: compat libs still not installed:${still_missing} -- the PHP packages needin
g them will fail." >&2
        return 1
    fi
    echo "[libxml2-compat] WARNING: libxml2.so.2 still absent after shim; PHP install may fail." >&2
    return 1
    if [ -n "${missing// /}" ]; then
        echo "[php-compat] NOTE: no verified Debian compat for:${missing} (only extensions needing them are aff
ected)." >&2
    fi
    echo "[php-compat] OK: compat libraries provided for ${php_codename}." >&2
    return 0
}

# Add PHP repository (Ubuntu/Debian) with automatic backup and restore

Now update the two call sites to build the package list and call the generalized shim:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 9 lines, removed 2 lines
        php_repo_line="deb [signed-by=$php_key_file] https://packages.sury.org/php/ $php_codename main"
    fi

    # Bridge the libxml2.so.2 gap on rolling derivatives before the PHP install runs.
    ensure_php_libxml2_compat_from_apt_repository_manager "$php_vendor" "$php_codename"
    # Pre-resolve compat libraries for the php packages about to be installed: those
    # named in the install command plus the project's declared core/extension sets (so
    # extensions installed in a later step -- e.g. php8.5-intl -> libicu76 -- are covered
    # too). The two `declare -p` guards make the project arrays optional.
    local php_pkgs
    php_pkgs="$(printf '%s ' $command_to_execute | grep -oE 'php8\.5[A-Za-z0-9.+-]*' | sort -u | tr '\n' ' ')"
    declare -p PHP85_CORE_PACKAGES >/dev/null 2>&1 && php_pkgs="$php_pkgs ${PHP85_CORE_PACKAGES[*]}"
    declare -p CORE_EXTENSIONS    >/dev/null 2>&1 && php_pkgs="$php_pkgs ${CORE_EXTENSIONS[*]}"
    ensure_php_compat_libs_from_apt_repository_manager "$php_vendor" "$php_codename" "$php_pkgs"

    execute_with_repo_backup_from_apt_r
        "php" \

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 9 lines, removed 2 lines
    echo "Updating apt cache..."
    $USE_SUDO apt update 2>/dev/null || true

    # Bridge the libxml2.so.2 gap on rolling derivatives before the PHP install runs.
    ensure_php_libxml2_compat_from_apt_repository_manager "$php_vendor" "$php_codename"
    # Pre-resolve compat libraries for the php packages about to be installed: those
    # named in the install command plus the project's declared core/extension sets (so
    # extensions installed in a later step -- e.g. php8.5-intl -> libicu76 -- are covered
    # too). The two `declare -p` guards make the project arrays optional.
    local php_pkgs
    php_pkgs="$(printf '%s ' $command_to_execute | grep -oE 'php8\.5[A-Za-z0-9.+-]*' | sort -u | tr '\n' ' ')"
    declare -p PHP85_CORE_PACKAGES >/dev/null 2>&1 && php_pkgs="$php_pkgs ${PHP85_CORE_PACKAGES[*]}"
    declare -p CORE_EXTENSIONS    >/dev/null 2>&1 && php_pkgs="$php_pkgs ${CORE_EXTENSIONS[*]}"
    ensure_php_compat_libs_from_apt_repository_manager "$php_vendor" "$php_codename" "$php_pkgs"

    if [ -n "$command_to_execute" ]; th
        echo "Executing: $command_to_execute"

Now the resolver hardening — set -u-safe os-release reads and glibc-gating the trixie clamp. Let me read the current resolver:

  Read 1 file

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 6 lines, removed 4 lines

    local id="" id_like="" version_codename="" ubuntu_codename=""
    if [ -f /etc/os-release ]; then
        id="$( . /etc/os-release 2>/dev/null; echo "$ID" )"
        id_like="$( . /etc/os-release 2>/dev/null; echo "$ID_LIKE" )"
        version_codename="$( . /etc/os-release 2>/dev/null; echo "$VERSION_CODENAME" )"
        ubuntu_codename="$( . /etc/os-release 2>/dev/null; echo "$UBUNTU_CODENAME" )"
        # ${VAR:-} so a caller running under `set -u` is not aborted by absent fields
        # (UBUNTU_CODENAME / ID_LIKE are genuinely missing on Debian/Kali).
        id="$( . /etc/os-release 2>/dev/null; echo "${ID:-}" )"
        id_like="$( . /etc/os-release 2>/dev/null; echo "${ID_LIKE:-}" )"
        version_codename="$( . /etc/os-VERSION_CODENAME:-}" )"
        ubuntu_codename="$( . /etc/os-release 2>/dev/null; echo "${UBUNTU_CODENAME:-}" )"
    fi
    id="$(echo "$id" | tr '[:upper:]' '[:lower:]')"
    id_like="$(echo "$id_like" | tr '[:upper:]' '[:lower:]')"

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/apt_repository_manager.sh)
Added 19 lines, removed 4 lines
            # derivatives (kali-rolling, parrot, ...) also carry the post-t64 ABI
            # (libssl3t64, libcurl4t64, libzip5, libxml2 2.15) that bookworm-built
            # packages predate and cannot satisfy, so map anything outside the hosted
            # set to the newest hosted suite (trixie), which matches that ABI. Pinning
            # to bookworm here is what made Sury PHP 8.5 fail on Kali (libzip4 /
            # libxml2 deps absent). Override with APT_DEBIAN_CODENAME_DEFAULT.
            # set to the newest hosted suite the host can actually run. Pinning to
            # bookworm here is what made Sury PHP 8.5 fail on Kali (libzip4 / libxml2
            # deps absent). Override entirely with APT_DEBIAN_CODENAME_DEFAULT.
            case "$codename" in
                bullseye|bookworm|trixie) : ;;
                *) codename="${APT_DEBIAN_CODENAME_DEFAULT:-trixie}" ;;
                *)
                    if [ -n "${APT_DEBIAN_CODENAME_DEFAULT:-}" ]; then
                        codename="$APT_
                    else
                        # Sury trixie php8.5 needs libc6>=2.38, so a bookworm-era rolling
                        # spin (glibc<2.38) must stay on bookworm; Kali/sid (glibc 2.4x)
                        # get trixie, whose post-t64 ABI matches their libraries.
                        local _glibc
                        _glibc="$(getconf GNU_LIBC_VERSION 2>/dev/null | awk '{print $NF}')"
                        if [ -n "$_glibc" ] && dpkg --compare-versions "$_glibc" ge 2.38 2>/dev/null; then
                            codename="trixie"
                        else
                            codename="bookworm"
                        fi
                    fi
                    ;;
            esac
            ;;
    esac

Now let me validate everything end-to-end withoutax, resolver output, the new function loads, thecall-site package-list assembly, and the detection pipeline against the live indices (must yield exactly libicu76).

  Ran 1 shell command

Now validate the detection pipeline itself (the function's exact awk/sed/gap logic) against the live indices — it must yield exactly libicu76 now that libxml2 is installed, and correctly parse the Debian Filename/SHA256:

  Ran 1 shell command

The detection pipeline produces exactly libicu76 (libxml2 correctly excluded since it's now installed — proving idempotency) and parses the Debian Filename/SHA256 correctly (matches the confirmed c1bf… hash). On a fresh system it would catch both libxml2 + libicu76 and batch them.

Let me do a final logic pass: re-read the written function and confirm the ensure_packages helper signature I call.

  Read 1 file

Line numbers shifted from my edits. Let me locate ensure_packages_from_apt_repository_manager to confirm its signature, and re-read my new function as written.

  Ran 1 shell command

ensure_packages_from_apt_repository_manager curl is a valid call (takes $*). Good. Let me re-read the final written function for a last logic pass.

  Read 1 file

The function is complete and correct. Let me confirm final syntax and scope of the change.

  Ran 1 shell command

Done. The fix is complete and validated up to the sudo boundary.

What happened on your last run

The first fix worked — PHP 8.5.7 installed and running, 9 extensions loaded, the libxml2 shim did its job (OK: libxml2.so.2 now available). The only remaining failure was the same bug class, second instance: php8.5-intl → libicu76. Kali bumped ICU past Sury-trixie's libicu76, exactly like it bumped libxml2 past .so.2.

What I changed (so it's fixed generally, not wha

I generalized the one-off libxml2 shim in apt_rere_php_compat_libs_from_apt_repository_manager,which resolves every such SONAME gap in one pass, scoped to the project's actual php package set (PHP85_CORE_PACKAGES + CORE_EXTENSIONS):

1. Reads the Sury suite's Depends for the php packages you install → finds which lib* deps are unsatisfiable on this host. For Kali that's exactly {libxml2, libicu76} — and deliberately not libsnmp40t64/libgpgme11t64 (those belong to extensions you don't install).
2. Pulls each from Debian trixie (the suite Sury built against → ABI match), co-installing next to Kali's newer versions.

I also folded in the adversarial review's findings:
- Security: .debs now fetched over HTTPS and SHA256-verified against the Packages stanza before dpkg -i (was plain HTTP, no integrity check → root-RCE surface).
- Robustness: DEBIAN_FRONTEND=noninteractive + --force-confold, apt-get -f install constrained with APT::Get::Remove=false (can't remove the lib it just added), no swallowed diagnostics + loud post-verify, self-ensures curl/gzip, fatal-on-mktemp (no predictable temp path).
- Cross-distro: the bookworm→trixie default is now glibc-gated (a bookworm-era spin with glibc<2.38 stays on bookworm), and os-release reads are set -u-safe — so it correctly "supports Ubuntu / Debian / Kali."

Verified (everything except the sudo-gated insta path in my session)

- bash -n clean; resolver → debian trixie; opt-o
- The detection pipeline run against the live indices yields exactly libicu76 now (libxml2 excluded because it's installed → confirms idempotency), and parses the right Filename + SHA256 (c1bf…, HTTPS 200). On a fresh box it catches both libs and batches them.

Action

Re-run 31_ensure_php85_intelligent.sh. This time the shim will detect the libicu76 gap, download+verify+install it from Debian trixie, and php8.5-intl will install — using the identical mechanism that already succeeded for libxml2. (Escape hatches if ever needed: PHP_COMPAT_SHIM=0 to disable, APT_DEBIAN_CODENAME_DEFAULT=… to force a suite.)

✻ Cooked for 29m 52s

❯   Executing: 32_install_swoole.sh

  Starting execution of 32_install_swoole.sh...
  ========================================
  [32_SWOOLE] PHP Swoole Extension Installation
  ========================================
  [32_SWOOLE] Starting Swoole installation/configuration check...

  [32_SWOOLE] Checking Swoole installation...
  [32_SWOOLE] PHP extension directory: /usr/lib/php/20250925
  [32_SWOOLE] Swoole extension file not found: /usr/lib/php/20250925/swoole.so
  [32_SWOOLE] Checking Swoole configuration files...
  [32_SWOOLE] Configuration file missing: /etc/php/8.5/mods-available/swoole.ini
  [32_SWOOLE] Symlink missing for cli: /etc/php/8.5/cli/conf.d/20-swoole.ini
  [32_SWOOLE] Some Swoole configuration files need repair

  [32_SWOOLE] Swoole not installed
  [32_SWOOLE] Performing fresh installation...

  [32_SWOOLE] Checking for existing Swoole installation...
  [32_SWOOLE] No existing Swoole installation found
  [32_SWOOLE] Installing Swoole build dependencies...
  Reading package lists... Done
  Building dependency tree... Done
  Reading state information... Done
  build-essential is already the newest version (1
  libssl-dev is already the newest version (3.6.2-1).
  libcurl4-openssl-dev is already the newest versi
  libcurl4-openssl-dev set to manually installed.
  libpcre2-dev is already the newest version (10.46-1+b2).
  libpcre2-dev set to manually installed.
  Solving dependencies... Done
  The following additional packages will be installed:
    autopoint dctrl-tools debhelper dh-autoreconf dh-strip-nondeterminism dwz intltool-debian libarchive-cpio-perl
    libdebhelper-perl libfile-stripnondeterminism-perl libmail-sendmail-perl libsys-hostname-long-perl pkg-php-tools
    po-debconf shtool
  Suggested packages:
    debtags dh-make postgresql-doc-18 dh-php libmail-box-perl
  The following NEW packages will be installed:
    autopoint dctrl-tools debhelper dh-autoreconf dh-strip-nondeterminism dwz intltool-debian libarchive-cpio-perl
    libdebhelper-perl libfile-stripnondeterminism-perl libmail-sendmail-perl libpq-dev libsys-hostname-long-perl php-pear
    php8.5-dev pkg-php-tools po-debconf shtool
  0 upgraded, 18 newly installed, 0 to remove and 32 not upgraded.
  Need to get 3597 kB of archives.
  After this operation, 15.8 MB of additional disk space will be used.
  Get:12 https://packages.sury.org/php trixie/main-1+0~20260606.19+debian13~1.gbp741f99 [628 kB]
  Get:3 http://kali.download/kali kali-rolling/main amd64 libdebhelper-perl all 13.31 [75.7 kB]
  Get:6 http://kali.download/kali kali-rolling/maism all 1.15.1-1 [6020 B]
  Get:8 http://http.kali.org/kali kali-rolling/main amd64 intltool-debian all 0.35.0+20060710.6 [22.9 kB]
  Get:9 http://kali.download/kali kali-rolling/main amd64 po-debconf all 1.0.22 [216 kB]
  Get:10 http://kali.download/kali kali-rolling/main amd64 debhelper all 13.31 [932 kB]
  Get:17 http://ftp.riken.jp/Linux/kali kali-rolling/main amd64 shtool all 2.0.8-10 [135 kB]
  Get:13 http://kali.download/kali kali-rolling/main amd64 libsys-hostname-long-perl all 1.5-3 [11.6 kB]
  Get:14 http://kali.download/kali kali-rolling/main amd64 libmail-sendmail-perl all 0.82-1 [23.9 kB]
  Get:15 http://kali.download/kali kali-rolling/main amd64 libpq-dev amd64 18.4-1 [169 kB]
  Get:16 http://http.kali.org/kali kali-rolling/main amd64 php-pear all 1:1.10.16+submodules+notgz-3 [290 kB]
  Get:7 http://mirror2.keiminem.com/kali kali-rolling/main amd64 dwz amd64 0.16-4 [108 kB]
  Get:5 http://kali.cs.nycu.edu.tw/kali kali-rolling/main amd64 libfile-stripnondeterminism-perl all 1.15.1-1 [17.1 kB]
  Get:4 http://mirror.wonchan.net/kali kali-rolling/main amd64 dh-autoreconf all 22 [12.2 kB]
  Get:18 http://mirror.wonchan.net/kali kali-rolling/main amd64 pkg-php-tools all 1.49 [32.6 kB]
  Get:1 http://free.nchc.org.tw/kali kali-rolling/main amd64 autopoint all 0.26-1 [802 kB]
  Get:2 http://http.kali.org/kali kali-rolling/main amd64 dctrl-tools amd64 2.24-3+b1 [104 kB]
  Get:11 http://free.nchc.org.tw/kali kali-rolling/main amd64 libarchive-cpio-perl all 0.10-3 [10.8 kB]
  Fetched 3597 kB in 14s (250 kB/s)
  Selecting previously unselected package autopoint.
  (Reading database ... 590328 files and directori
  Preparing to unpack .../00-autopoint_0.26-1_all.deb ...
  Unpacking autopoint (0.26-1) ...
  Selecting previously unselected package dctrl-tools.
  Preparing to unpack .../01-dctrl-tools_2.24-3+b1_amd64.deb ...
  Unpacking dctrl-tools (2.24-3+b1) ...
  Selecting previously unselected package libdebhelper-perl.
  Preparing to unpack .../02-libdebhelper-perl_13.31_all.deb ...
  Unpacking libdebhelper-perl (13.31) ...
  Selecting previously unselected package dh-autoreconf.
  Preparing to unpack .../03-dh-autoreconf_22_all.deb ...
  Unpacking dh-autoreconf (22) ...
  Selecting previously unselected package libfile-stripnondeterminism-perl.
  Preparing to unpack .../04-libfile-stripnondeterminism-perl_1.15.1-1_all.deb ...
  Unpacking libfile-stripnondeterminism-perl (1.15.1-1) ...
  Selecting previously unselected package dh-strip-nondeterminism.
  Preparing to unpack .../05-dh-strip-nondeterminism_1.15.1-1_all.deb ...
  Unpacking dh-strip-nondeterminism (1.15.1-1) ...
  Selecting previously unselected package dwz.
  Preparing to unpack .../06-dwz_0.16-4_amd64.deb ...
  Unpacking dwz (0.16-4) ...
  Selecting previously unselected package intltool-debian.
  Preparing to unpack .../07-intltool-debian_0.35.
  Unpacking intltool-debian (0.35.0+20060710.6) ...
  Selecting previously unselected package po-debconf.
  Preparing to unpack .../08-po-debconf_1.0.22_all.deb ...
  Unpacking po-debconf (1.0.22) ...
  Selecting previously unselected package debhelper.
  Preparing to unpack .../09-debhelper_13.31_all.deb ...
  Unpacking debhelper (13.31) ...
  Selecting previously unselected package libarchive-cpio-perl.
  Preparing to unpack .../10-libarchive-cpio-perl_0.10-3_all.deb ...
  Unpacking libarchive-cpio-perl (0.10-3) ...
  Selecting previously unselected package libsys-hostname-long-perl.
  Preparing to unpack .../11-libsys-hostname-long-perl_1.5-3_all.deb ...
  Unpacking libsys-hostname-long-perl (1.5-3) ...
  Selecting previously unselected package libmail-sendmail-perl.
  Preparing to unpack .../12-libmail-sendmail-perl_0.82-1_all.deb ...
  Unpacking libmail-sendmail-perl (0.82-1) ...
  Selecting previously unselected package libpq-dev.
  Preparing to unpack .../13-libpq-dev_18.4-1_amd6
  Unpacking libpq-dev (18.4-1) ...
  Selecting previously unselected package php-pear
  Preparing to unpack .../14-php-pear_1%3a1.10.16+submodules+notgz-3_all.deb ...
  Unpacking php-pear (1:1.10.16+submodules+notgz-3) ...
  Selecting previously unselected package shtool.
  Preparing to unpack .../15-shtool_2.0.8-10_all.deb ...
  Unpacking shtool (2.0.8-10) ...
  Selecting previously unselected package php8.5-dev.
  Preparing to unpack .../16-php8.5-dev_8.5.7-1+0~20260606.19+debian13~1.gbp741f99_amd64.deb ...
  Unpacking php8.5-dev (8.5.7-1+0~20260606.19+debian13~1.gbp741f99) ...
  Selecting previously unselected package pkg-php-tools.
  Preparing to unpack .../17-pkg-php-tools_1.49_all.deb ...
  Unpacking pkg-php-tools (1.49) ...
  Setting up libfile-stripnondeterminism-perl (1.15.1-1) ...
  Setting up shtool (2.0.8-10) ...
  Setting up php-pear (1:1.10.16+submodules+notgz-3) ...
  Setting up php8.5-dev (8.5.7-1+0~20260606.19+debian13~1.gbp741f99) ...
  update-alternatives: using /usr/bin/php-config8.5 to provide /usr/bin/php-config (php-config) in auto mode
  update-alternatives: using /usr/bin/phpize8.5 to provide /usr/bin/phpize (phpize) in auto mode
  Setting up libdebhelper-perl (13.31) ...
  Setting up libpq-dev (18.4-1) ...
  Setting up intltool-debian (0.35.0+20060710.6) .
  Setting up autopoint (0.26-1) ...
  Setting up dh-strip-nondeterminism (1.15.1-1) ...
  Setting up dwz (0.16-4) ...
  Setting up libarchive-cpio-perl (0.10-3) ...
  Setting up libsys-hostname-long-perl (1.5-3) ...
  Setting up dctrl-tools (2.24-3+b1) ...
  Setting up po-debconf (1.0.22) ...
  Setting up dh-autoreconf (22) ...
  Setting up libmail-sendmail-perl (0.82-1) ...
  Setting up debhelper (13.31) ...
  Setting up pkg-php-tools (1.49) ...
  Processing triggers for doc-base (0.11.2) ...
  Processing 1 added doc-base file...
  Processing triggers for man-db (2.13.1-1) ...
  Processing triggers for kali-menu (2026.2.6) ...
  [32_SWOOLE] Dependencies installed
  [32_SWOOLE] Ensuring PHP 8.5 symlink exists...
  [32_SWOOLE] PHP 8.5 symlink missing or incorrect
  update-alternatives: using /usr/bin/php8.5 to provide /usr/bin/php (php) in auto mode
  [32_SWOOLE] PHP 8.5 set as default
  [32_SWOOLE] �?PHP 8.5 is available at /usr/bin/php
  [32_SWOOLE] Installing Swoole...
  [32_SWOOLE] PHP 8.5 detected. Building Swoole from source (pinned ref: v6.0.2)...
  [32_SWOOLE] Note: Swoole 6.x will be patched for Octane v2.13.x compatibility
  [32_SWOOLE] Cloning Swoole repository (ref: v6.0.2)...
  fatal: could not create work tree dir '/usr/tmp/swoole-src-build-704410': Permission denied
  [32_SWOOLE] Pinned ref 'v6.0.2' unavailable, falling back to master
  fatal: could not create work tree dir '/usr/tmp/swoole-src-build-704410': Permission denied
  [32_SWOOLE] Failed to clone repository

  Script execution completed with exit code: 1
  Press any key to return to test scripts menu...
   suite to Ubuntu Debian Kali, in Kali ,selecte available soft version. search official documentations.

PHP 8.5 + intl are now resolved (I can see php8.5-dev … debian13 installing cleanly from Sury trixie — the suite fix is working). New problem in 32_install_swoole.sh:

1. /usr/tmp/... Permission denied — it's cloningitable temp dir on Kali; the standard is /tmp or/var/tmp).
2. The "v6.0.2 unavailable → master" message is iled on the path error, not because the ref ismissing.

You want: adapt to Ubuntu/Debian/Kali, pick an available Swoole version on Kali, and check official docs. Let me read the script's build logic and load web tools in parallel.

  Ran 1 shell command

Found it: build_dir="$GLOBAL_TEMP_DIR/swoole-src-build-$$" and GLOBAL_TEMP_DIR is resolving to /usr/tmp. Let me read the build flow, find where GLOBAL_TEMP_DIR is defined, and research official Swoole/PHP 8.5 compatibility in parallel.

  Read 1 file

Now let me find where GLOBAL_TEMP_DIR is defined and research the official Swoole/PHP 8.5 version situation.

  Ran 1 shell command

● Web Search("Swoole PHP 8.5 support version compatibility release")
Did 0 searches in 1s

Root cause of the temp bug: GLOBAL_TEMP_DIR="/us (and the JS mirror). /usr/tmp doesn't exist andisn't writable on Debian/Kali (it's a legacy SysV alias for /var/tmp, only a symlink on some RPM distros). Let me read that section, and research the Swoole/PHP 8.5 version situation from official sources (the WebSearch backend hiccuped; I'll go straight to authoritative sources).

  Read 1 file

❯ Executing: 31_ensure_php85_intelligent.sh

  Starting execution of 31_ensure_php85_intelligent.sh...
  [31_PHP85_INTELLIGENT] INSTALL_NGINX: true
  ============================================================================
  [31_PHP85_INTELLIGENT] Intelligent PHP 8.5 Installation System
  ============================================================================
  [31_PHP85_INTELLIGENT] [PRE-FLIGHT] Running system checks...
  [31_PHP85_INTELLIGENT] Sudo access confirmed
  [31_PHP85_INTELLIGENT] [PRE-CHECK] Running pre-installation verification...
  [31_PHP85_INTELLIGENT] Checking PHP 8.5 state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing com...
  [31_PHP85_INTELLIGENT] PHP 8.5 binary found: /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing PHP
  [31_PHP85_INTELLIGENT] PHP command found: /usr/local/bin/php
  [31_PHP85_INTELLIGENT] PHP version: 8.5
  [31_PHP85_INTELLIGENT]
  [31_PHP85_INTELLIGENT] [DETECTION] Checking PHP version...
  [31_PHP85_INTELLIGENT] Current PHP version: 8.5
  [31_PHP85_INTELLIGENT] PHP version is compatible
  [31_PHP85_INTELLIGENT] Skipping PHP-FPM check - Using Swoole instead
  [31_PHP85_INTELLIGENT] [DETECTION] Checking required PHP extensions...
  [31_PHP85_INTELLIGENT] Extension curl (curl): OK
  [31_PHP85_INTELLIGENT] Extension mbstring (mbstring): OK
  [31_PHP85_INTELLIGENT] Extension xml (xml): OK
  [31_PHP85_INTELLIGENT] Extension zip (zip): OK
  [31_PHP85_INTELLIGENT] Extension gd (gd): OK
  [31_PHP85_INTELLIGENT] Extension mysql (mysqli): OK
  [31_PHP85_INTELLIGENT] Extension pgsql (pdo_pgsql): OK
  [31_PHP85_INTELLIGENT] Extension sqlite3 (sqlite3): OK
  [31_PHP85_INTELLIGENT] Extension opcache (Zend OPcache): OK
  [31_PHP85_INTELLIGENT] Extension intl (intl): OK
  [31_PHP85_INTELLIGENT] Extension bcmath (bcmath): OK
  [31_PHP85_INTELLIGENT] All required extensions a
  >>> [31_PHP85_INTELLIGENT] Checking symbolic link: /usr/local/bin/php
  [INFO] [31_PHP85_INTELLIGENT] Current symlink target: /usr/bin/php8.5
  [OK] [31_PHP85_INTELLIGENT] Symlink is correct: /usr/local/bin/php -> /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] State: Fully configured and operational
  [31_PHP85_INTELLIGENT] Checking Composer state...
  [31_PHP85_INTELLIGENT] [ANALYSIS] Performing Composer state analysis...
  [31_PHP85_INTELLIGENT] [DETECTION] Analyzing Composer binary state...
  [31_PHP85_INTELLIGENT] Composer not found
  [31_PHP85_INTELLIGENT] Note: Composer will be installed in the next step via 34_install_composer.sh
  [31_PHP85_INTELLIGENT] Compos
  ──── (310 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  st-installation verification PASSED
  [31_PHP85_INTELLIGENT] PHP 8.5 is fully configured and ready for use
  ========================================================================
  [31_PHP85_INTELLIGENT] PHP 8.5 Installation Summary
  ========================================================================
  [31_PHP85_INTELLIGENT] Verifying PHP installation (php -v):
  PHP 8.5.7 (cli) (built: Jun  6 2026 05:59:50) (N
  Copyright (c) The PHP Group
  Built by Debian
  [31_PHP85_INTELLIGENT] PHP-FPM Service Status:
  [31_PHP85_INTELLIGENT] PHP-FPM not used - Using Swoole with Laravel Octane
  [31_PHP85_INTELLIGENT] php8.5-fpm service: [NOT RUNNING] �?{NC}
  [31_PHP85_INTELLIGENT] Active PHP Modules (php -m):
  [31_PHP85_INTELLIGENT] Total modules loaded: 61
  [31_PHP85_INTELLIGENT] Key modules from documentation:
  [31_PHP85_INTELLIGENT]   Core: [LOADED]
  [31_PHP85_INTELLIGENT]   curl: [LOADED]
  [31_PHP85_INTELLIGENT]   mbstring: [LOADED]
  [31_PHP85_INTELLIGENT]   xml: [LOADED]
  [31_PHP85_INTELLIGENT]   zip: [LOADED]
  [31_PHP85_INTELLIGENT]   gd: [LOADED]
  [31_PHP85_INTELLIGENT]   mysqli: [LOADED]
  [31_PHP85_INTELLIGENT]   Zend OPcache: [LOADED]
  [31_PHP85_INTELLIGENT]   intl: [LOADED]
  [31_PHP85_INTELLIGENT]   bcmath: [LOADED]
  [31_PHP85_INTELLIGENT] PHP Alternatives Status:
  [31_PHP85_INTELLIGENT] PHP alternatives not conf
  [31_PHP85_INTELLIGENT] Configuration Files:
  [31_PHP85_INTELLIGENT]   /etc/php/8.5/cli/php.in
  ========================================================================
  [31_PHP85_INTELLIGENT] [SUCCESS] PHP 8.5 installation completed!
  [31_PHP85_INTELLIGENT] [INFO] PHP 8.5 is ready for Laravel development with Swoole
  [31_PHP85_INTELLIGENT] [INFO] PHP-FPM disabled - Using Swoole with Laravel Octane
  ========================================================================
  ============================================================================
  [31_PHP85_INTELLIGENT] [SUCCESS] PHP 8.5 installation/configuration completed successfully!
  [31_PHP85_INTELLIGENT] [INFO] PHP 8.5 is now available via 'php' command
  [31_PHP85_INTELLIGENT] [INFO] Symbolic link: /usr/local/bin/php -> /usr/bin/php8.5
  [31_PHP85_INTELLIGENT] [INFO] Old PHP versions have been cleaned up
  [31_PHP85_INTELLIGENT] [INFO] ALL PHP-FPM services disabled (using Swoole)
  ============================================================================

  Script execution completed with exit code: 0
  Press any key to return to test scripts menu...

Also confirmed from your interjected run: script 31 now passes end-to-end — PHP 8.5.7 with all 11 extensions including intl,
exit code 0. The PHP/libxml2/libicu work is done

Found the Swoole temp bug precisely: GLOBAL_TEMPsh:1321 + the JS mirror), andcreate_script_temp_dir hardcodes a /usr/tmp/* guard. /usr/tmp is a legacy SysV alias for /var/tmp that simply doesn't exist on Debian/Kali → the clone got EACCES.

Now the version research from official sources (your "select available version / official docs" requirement). Let me pull the authoritative release list and the official PHP-version support.

  Ran 1 shell command

● Fetch(https://github.com/swoole/swoole-src/blob/master/README.md)
Received 526.5KB (200 OK)

Authoritative data: latest stable Swoole is v6.2.1 (2026-05-15); the v6.x line runs 6.0.0→6.0.2 (old), 6.1.x, 6.2.x. The pin v6.0.2 does exist as a tag — so the "unavailable → master" message was indeed a misdiagnosis of the temp-dir EACCES, confirming my root cause. v6.0.x predates PHP 8.5 (Nov 2025); 6.1.3+/6.2.x are post-8.5.

WebFetch's summarizer is erroring out (backend model issue), so let me pull the official PHP-version constraint directly from the source tree for several tags.

  Ran 1 shell command

Decisive, official-docs-backed finding from Swoole's own composer.json:

┌──────────────────────┬───────────────────────────────────────┬──────────────────────────────┐
│        Swoole        │            php constraint             │           PHP 8.5?           │
├──────────────────────┼───────────────────────────────────────┼──────────────────────────────┤
│ v6.0.2 (current pin) │ >= 8.1 (no upper bound, predates 8.5) │ unvalidated, no 8.5 handling │
├──────────────────────┼───────────────────────────────────────┼──────────────────────────────┤
│ v6.1.8               │ >=8.1 **<8.5**                        │ ❌ explicitly excludes 8.5   │
├──────────────────────┼───────────────────────────────────────┼──────────────────────────────┤
│ v6.2.0 / v6.2.1      │ >=8.2 **<8.6**                        │ ✅ supported                 │
├──────────────────────┼───────────────────────────────────────┼──────────────────────────────┤
│ master               │ >=8.2 <8.6                            │ ✅                           │
└──────────────────────┴───────────────────────────────────────┴──────────────────────────────┘

So Swoole 6.2.x (latest stable v6.2.1) is the first line that officially supports PHP 8.5 — the 6.1.x line caps it out, and the pinned v6.0.2 predates it. That's the "available version" to select for Kali.

Let me read the JS mirror and the swoole functiohree spots cleanly.

  Read 1 file

Let me see the swoole script's structure around the build function to place a version-selection helper cleanly.

  Read 1 file

Now I'll implement all three fixes. Fix 1 — gvar_common.sh: make GLOBAL_TEMP_DIR writable & cross-distro, and don't clobber a system temp dir's sticky bit:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/gvar_common.sh)
Added 17 lines, removed 9 lines
    return 0
}

# Initialize global temporary directory
GLOBAL_TEMP_DIR="/usr/tmp"
# Initialize global temporary directory.
# NOTE: /usr/tmp is a legacy SysV alias for /var/tmp that exists only as a symlink on
# some RPM distros; on Debian/Ubuntu/Kuilds under it failed
# with "Permission denied". Use $TMPDIR when set, else /var/tmp -- persistent and
# disk-backed (good for large source b) and world-writable
# + sticky on every Debian-family system, so the invoking non-root user can create build
# dirs here. Export GLOBAL_TEMP_DIR to override.
GLOBAL_TEMP_DIR="${GLOBAL_TEMP_DIR:-${TMPDIR:-/var/tmp}}"
GLOBAL_TEMP_DIR="${GLOBAL_TEMP_DIR%/}"

# Ensure global temporary directory exists
# Ensure global temporary directory exists. Only create/chmod when MISSING -- never
# chmod an existing system temp dir, which would clobber its 1777 sticky permissions.
if [ ! -d "$GLOBAL_TEMP_DIR" ]; then
    echo "Creating global temporary directory: $GLOBAL_TEMP_DIR"
    $USE_SUDO mkdir -p "$GLOBAL_TEMP_DIR"
    $USE_SUDO chmod 755 "$GLOBAL_TEMP_DIR"
    mkdir -p "$GLOBAL_TEMP_DIR" 2>/dev/null || $USE_SUDO mkdir -p "$GLOBAL_TEMP_DIR"
    chmod 1777 "$GLOBAL_TEMP_DIR" 2>/dev/null || $USE_SUDO chmod 1777 "$GLOBAL_TEMP_DIR" 2>/dev/null || true
fi

# Function to create script-specific temporary directory (restricted to /usr/tmp/<script_name>)
# Function to create script-specific td to $GLOBAL_TEMP_DIR/<script_name>)
create_script_temp_dir() {
    local script_name="$1"
    # Restrict to GLOBAL_TEMP_DIR and prevent path traversal

    esac
    [ -z "$script_name" ] && echo "[ERROR] create_script_temp_dir: empty script_name" >&2 && return 1
    local script_temp_dir="$GLOBAL_TEMP_DIR/$script_name"
    # Ensure result is strictly under /usr/tmp (or configured GLOBAL_TEMP_DIR)
    # Ensure result is strictly under the configured GLOBAL_TEMP_DIR
    case "$script_temp_dir" in
        /usr/tmp/*) ;;
        *) echo "[ERROR] create_script_temp_dir: path not under /usr/tmp: $script_temp_dir" >&2; return 1 ;;
        "$GLOBAL_TEMP_DIR"/*) ;;
        *) echo "[ERROR] create_script_temp_dir: path not under $GLOBAL_TEMP_DIR: $script_temp_dir" >&2; retur
n 1 ;;
    esac

    if [ ! -d "$script_temp_dir" ]; then

Fix 1b — the JS mirror (gvar_common.js):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/nodetools/gvar_common.js)
Added 6 lines, removed 2 lines
 * @returns {string} Path to temporary directory
 */
function createScriptTempDir(scriptName) {
    const GLOBAL_TEMP_DIR = IS_WINDOWS ? os.tmpdir() : '/usr/tmp';
    // /usr/tmp is absent on Debian/Ubuntu/Kali (legacy SysV alias for /var/tmp); use
    // $TMPDIR when set, else /var/tmp (writable + persistent) on POSIX.
    const GLOBAL_TEMP_DIR = IS_WINDOWS ? os.tmpdir() : (process.env.TMPDIR || '/var/tmp');
    const scriptTempDir = path.join(GLOBAL_TEMP_DIR, scriptName);

    if (!safeExists(scriptTempDir)) {

 * @param {string} scriptName - Name of the script
 */
function cleanupScriptTempDir(scriptName) {
    const GLOBAL_TEMP_DIR = IS_WINDOWS
    // /usr/tmp is absent on Debian/Ubuntu/Kali (legacy SysV alias for /var/tmp); use
    // $TMPDIR when set, else /var/tmp SIX.
    const GLOBAL_TEMP_DIR = IS_WINDOWS ? os.tmpdir() : (process.env.TMPDIR || '/var/tmp');
    const scriptTempDir = path.join(GLOBAL_TEMP_DIR, scriptName);

    if (safeExists(scriptTempDir)) {

Fix 2 — 32_install_swoole.sh: add version-selection helpers (official-constraint-validated, "select available version"):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/32_install_swoole.sh)
Added 49 lines
    fi
}

# Does a given Swoole git ref's official composer.json `php` constraint admit the running
# PHP? Returns 0 (yes / undeterminable) or 1 (explicitly excluded). Parses the simple
# ">=X.Y" + optional "<A.B" forms Swoole uses (e.g. ">=8.2 <8.6", ">=8.1 <8.5").
swoole_ref_supports_php() {
    local ref="$1" php_ver="$2"
    local pvid; pvid="$(echo "$php_ver"100+$2}')"
    local constraint
    constraint="$(curl -fsSL "https://rle/swoole-src/${ref}/composer.json" 2>/dev/
null \
        | sed -n 's/.*"php"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' | head -n1)"
    [ -z "$constraint" ] && return 0   # offline / unknown -> do not block
    local lo hi
    lo="$(printf '%s' "$constraint" | grep -oE '>=?[[:space:]]*[0-9]+\.[0-9]+' | head -n1 | grep -oE '[0-9]+\.[
0-9]+')"
    hi="$(printf '%s' "$constraint" | grep -oE '<[[:space:]]*[0-9]+\.[0-9]+'   | head -n1 | grep -oE '[0-9]+\.[
0-9]+')"
    if [ -n "$lo" ]; then
        local loid; loid="$(echo "$lo" | awk -F. '{printf "%d", $1*100+$2}')"
        [ "$pvid" -lt "$loid" ] && return 1
    fi
    if [ -n "$hi" ]; then
        local hiid; hiid="$(echo "$hi" | awk -F. '{printf "%d", $1*100+$2}')"
        [ "$pvid" -ge "$hiid" ] && return 1
    fi
    return 0
}

# Select the Swoole build ref for the rlable version" rather
# than trust a possibly-stale hardcoded tag:
#   1. $SWOOLE_BUILD_REF if set (explicit override).
#   2. the latest STABLE GitHub release, IF its composer.json admits this PHP.
#   3. the provided known-good pin, IF it admits this PHP.
#   4. master (last resort), with a warning.
# Prints the chosen ref on stdout (diagnostics go to stderr).
select_swoole_build_ref() {
    local php_ver="$1" pin="$2"
    if [ -n "${SWOOLE_BUILD_REF:-}" ]; then
        echo "$SWOOLE_BUILD_REF"; return 0
    fi
    local latest
    latest="$(curl -fsSL "https://api.github.com/repos/swoole/swoole-src/releases/latest" 2>/dev/null \
        | sed -n 's/.*"tag_name"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' | head -n1)"
    if printf '%s' "$latest" | grep -qE '^v[0-9]+\.[0-9]+\.[0-9]+$' && swoole_ref_supports_php "$latest" "$php_
ver"; then
        echo "$latest"; return 0
    fi
    if swoole_ref_supports_php "$pin" "$php_ver"; then
        echo "$pin"; return 0
    fi
    echo "[WARN] No tagged Swoole release admits PHP ${php_ver}; falling back to master" >&2
    echo "master"
}

install_swoole_pecl() {
    echo -e "${BLUE}$SCRIPT_INDEX Installing Swoole...${NC}"


Fix 2b — replace the version-pin + temp-dir + clone block in the PHP 8.5 branch:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/32_install_swoole.sh)
Added 23 lines, removed 14 lines
    # Note: Swoole 6.x requires compatibility patch for Laravel Octane v2.13.x
    # The patch will be automatically applied by octane_swoole_compat_fixer.php
    if [[ "$php_ver" == "8.5" ]]; then
        # Pin to a fixed Swoole release tag so re-runs build a reproducible
        # ABI. Building from the movingifferent
        # build on every fresh/repair run. Override via SWOOLE_BUILD_REF if a
        # newer pinned tag is required.
        local swoole_ref="${SWOOLE_BUILD_REF:-v6.0.2}"
        # Select the Swoole version. Per Swoole's own composer.json `php` constraint
        # (official): 6.2.x => ">=8.2 <8.6" (supports PHP 8.5); 6.1.x => ">=8.1 <8.5"
        # (EXCLUDES 8.5); 6.0.x predates 8.5. So PHP 8.5 needs Swoole >= 6.2.0. We pick
        # the latest stable release that admits the running PHP (auto-tracking fixes),
        # falling back to this known-good pin offline. Override via SWOOLE_BUILD_REF.
        local swoole_pin="v6.2.1"
        local swoole_ref
        swoole_ref="$(select_swoole_build_ref "$php_ver" "$swoole_pin")"

        echo -e "${YELLOW}$SCRIPT_INDEX PHP 8.5 detected. Building Swoole from source (pinned ref: $swoole_ref)
...${NC}"
        echo -e "${YELLOW}$SCRIPT_INDEX PHP 8.5 detected. Building Swoole from source (ref: $swoole_ref)...${NC
}"
        echo -e "${CYAN}$SCRIPT_INDEX Note: Swoole 6.x will be patched for Octane v2.13.x compatibility${NC}"

        if ! command -v git >/dev/null 2>&1; then
            $USE_SUDO apt-get install -
        fi

        # Use GLOBAL_TEMP_DIR from gvar_common.sh
        local build_dir="$GLOBAL_TEMP_DIR/swoole-src-build-$$"
        rm -rf "$build_dir"
        # Build under the (writable) global temp dir; mktemp -d avoids PID collisions and
        # the old /usr/tmp EACCES. git clones into the empty dir mktemp created.
        local build_dir
        build_dir="$(mktemp -d "${GLOBAL_TEMP_DIR%/}/swoole-src-build-XXXXXX" 2>/dev/null)" \
            || { echo -e "${RED}$SCRIPT_INDEX Could not create a build dir under '$GLOBAL_TEMP_DIR'${NC}"; retu
rn 1; }

        echo -e "${CYAN}$SCRIPT_INDEX Cloning Swoole repository (ref: $swoole_ref)...${NC}"
        echo -e "${CYAN}$SCRIPT_INDEX Cloning Swoole (ref: $swoole_ref) into $build_dir...${NC}"
        if ! git clone --depth 1 --branch "$swoole_ref" \
            https://github.com/swoole/swoole-src.git "$build_dir"; then
            echo -e "${YELLOW}$SCRIPT_INDEX Pinned ref '$swoole_ref' unavailable, falling back to master${NC}"
            echo -e "${YELLOW}$SCRIPT_INDEX Shallow clone of '$swoole_ref' failed; retrying a full clone...${NC
}"
            rm -rf "$build_dir"
            git clone --depth 1 https://github.com/swoole/swoole-src.git "$build_dir"
            if ! git clone --branch "$s
                https://github.com/swoole/swoole-src.git "$build_dir"; then
                echo -e "${RED}$SCRIPT_INDEX Failed to clone Swoole '$swoole_ref'${NC}"
                return 1
            fi
        fi

        if [ ! -d "$build_dir" ]; then
            echo -e "${RED}$SCRIPT_INDEX Failed to clone repository${NC}"
        if [ ! -f "$build_dir/config.m4" ]; then
            echo -e "${RED}$SCRIPT_INDEX Clone did not produce a Swoole source tree (no config.m4)${NC}"
            return 1
        fi


Now validate all three files: syntax, the JS, and the new helper functions against live data (must pick v6.2.1 for PHP 8.5 and reject v6.1.8).

  Ran 1 shell command

All validations pass:
- bash -n (both shell files) + node --check (JS): OK
- Helper logic against live Swoole data: v6.2.1→supports 8.5 ✅, v6.1.8→correctly rejects 8.5 ✅, sanity v6.2.1→rejects 8.6 ✅
- select_swoole_build_ref 8.5 → v6.2.1 (latest stable, validated) ✅
- GLOBAL_TEMP_DIR → /var/tmp, writable ✅

Let me check one downstream risk — whether the Octane compat-fixer hardcodes the old Swoole version — and summarize the diff scope.

  Ran 1 shell command

No other Swoole pin exists (the v6.0.2/version hits are unrelated user-agent strings and node mocks) — my default is the single source of truth. The large gvar_common.js diff is pre-existing working-tree churn (the repo was dirty at session start with auto-commits); node --check confirms my 2-line change is valid. Let me confirm my actual edited lines landed cleanly:

  Ran 1 shell command

All edits landed cleanly — the only remaining /usr/tmp strings are explanatory comments; no functional usage remains, and the
8.5 branch now has version-selection + mktemp -d

Summary — Swoole install (script 32) fixed for U

Two root causes, both fixed:

1. /usr/tmp Permission denied (the actual failure). GLOBAL_TEMP_DIR was hardcoded to /usr/tmp — a legacy SysV alias for /var/tmp that exists only as a symlink on some RPM distros and is simply absent on Debian/Kali, so the clone got EACCES. Fixed in both gvar_common.sh and the gvar_common.js mirror:
- GLOBAL_TEMP_DIR now resolves to ${TMPDIR:-/var/tmp} (writable, persistent, disk-backed — right for large source builds; world-writable+sticky on every Debian-family system). Confirmed → /var/tmp, writable.
- Only creates/chmods it when missing (never clobbers an existing temp dir's 1777 sticky bit), and the create_script_temp_dir guard now keys off $GLOBAL_TEMP_DIR instead of a hardcoded /usr/tmp/*.
- The swoole build now uses mktemp -d under it (no PID collisions).

2. Wrong Swoole version (would fail/produce an unsupported binary on PHP 8.5). The "v6.0.2 unavailable → master" message was a misdiagnosis — v6.0.2 does exist; the clone just failed on the path error. Per Swoole's official composer.json php constraint:

- 6.0.x → predates PHP 8.5 · 6.1.x → >=8.1 **<8.5** (explicitly excludes 8.5) · 6.2.x → >=8.2 <8.6 (supports 8.5)

So I added select_swoole_build_ref, which selectrsion: it takes the latest stable GitHub release,validates its composer.json admits the running PHP, and falls back to a known-good pin (v6.2.1) offline — honoring
SWOOLE_BUILD_REF as an override. Tested live: itd correctly rejects v6.1.8. Also replaced themisleading "fall back to master" with a real full-clone retry of the same ref + clear errors.

Validated: bash -n (both shell files), node --check (JS), helper logic against live Swoole data, and /var/tmp writability — all pass. The sudo-gated compile (phpize/configure/make) is the only part I can't run here; build deps (php8.5-dev, build-essential, libssl-dev, libcurl4-openssl-dev, libpcre2-dev) were already installed in your run.

Action: re-run 32_install_swoole.sh — it'll build into /var/tmp/swoole-src-build-XXXXXX and clone Swoole v6.2.1.

One heads-up for the next step: the script comments reference an octane_swoole_compat_fixer.php patch for "Swoole 6.x → Octane v2.13.x" — that's a separate downstream step. Since it's still 6.x it should apply, but if it had any v6.0.2-specific assumptions, flag it and I'll adjust it for 6.2.x.

✻ Brewed for 12m 37s

❯   Executing: 32_install_swoole.sh

  Starting execution of 32_install_swoole.sh...
  [sudo] password for kali:
  Sorry, try again.
  [sudo] password for kali:
  ========================================
  [32_SWOOLE] PHP Swoole Extension Installation
  ========================================
  [32_SWOOLE] Starting Swoole installation/configuration check...

  [32_SWOOLE] Checking Swoole installation...
  [32_SWOOLE] PHP extension directory: /usr/lib/php/20250925
  [32_SWOOLE] Swoole extension file not found: /usr/lib/php/20250925/swoole.so
  [32_SWOOLE] Checking Swoole configuration files...
  [32_SWOOLE] Configuration file missing: /etc/php/8.5/mods-available/swoole.ini
  [32_SWOOLE] Symlink missing for cli: /etc/php/8.5/cli/conf.d/20-swoole.ini
  [32_SWOOLE] Some Swoole configuration files need repair

  [32_SWOOLE] Swoole not installed
  [32_SWOOLE] Performing fresh installation...
   Why aren't scrpts 31_ensure_php85_intelligent.sh and 32_install_swoole.sh aligned,   Executing: 32_install_swoole.sh

  Starting execution of 32_install_swoole.sh...
  [sudo] password for kali:
  Sorry, try again.
  [sudo] password for kali:
  ========================================
  [32_SWOOLE] PHP Swoole Extension Installation
  ========================================
  [32_SWOOLE] Starting Swoole installation/configuration check...

  [32_SWOOLE] Checking Swoole installation...
  [32_SWOOLE] PHP extension directory: /usr/lib/php/20250925
  [32_SWOOLE] Swoole extension file not found: /usr/lib/php/20250925/swoole.so
  [32_SWOOLE] Checking Swoole configuration files...
  [32_SWOOLE] Configuration file missing: /etc/php/8.5/mods-available/swoole.ini
  [32_SWOOLE] Symlink missing for cli: /etc/php/8.5/cli/conf.d/20-swoole.ini
  [32_SWOOLE] Some Swoole configuration files need repair

  [32_SWOOLE] Swoole not installed
  [32_SWOOLE] Performing fresh installation...

  [32_SWOOLE] Checking for existing Swoole install
  [32_SWOOLE] No existing Swoole installation found
  [32_SWOOLE] Installing Swoole build dependencies

  Reading package lists... Done
  Building dependency tree... Done
  Reading state information... Done
  php8.5-dev is already the newest version (8.5.7-1+0~20260606.19+debian13~1.gbp741f99).
  php-pear is already the newest version (1:1.10.16+submodules+notgz-3).
  build-essential is already the newest version (12.12).
  libssl-dev is already the newest version (3.6.2-1).
  libcurl4-openssl-dev is already the newest version (8.20.0-5).
  libpq-dev is already the newest version (18.4-1).
  libpcre2-dev is already the newest version (10.46-1+b2).
  Solving dependencies... Done
  0 upgraded, 0 newly installed, 0 to remove and 36 not upgraded.
  [32_SWOOLE] Dependencies installed
  [32_SWOOLE] Ensuring PHP 8.5 symlink exists...
  [32_SWOOLE] PHP 8.5 symlink already exists
  [32_SWOOLE] �?PHP 8.5 is available at /usr/bin/php
  [32_SWOOLE] Installing Swoole...
  [32_SWOOLE] PHP 8.5 detected. Building Swoole from source (ref: v6.2.1)...
  [32_SWOOLE] Note: Swoole 6.x will be patched for
  [32_SWOOLE] Could not create a build dir under '/usr/tmp'

  Script execution completed with exit code: 1
  Press any key to return to test scripts menu... ,search official documentations, supported Ubuntu / Debian / Kali.

Real signal here: version selection now works (Building Swoole from source (ref: v6.2.1) ✓), but GLOBAL_TEMP_DIR is still /usr/tmp at runtime — so something sets it to /usr/tmp that overrides my gvar_common.sh edit (my ${GLOBAL_TEMP_DIR:-…} preserves a pre-existing value, which is the trap). The user's question — "why aren't 31 and 32 aligned?" — points at exactly this: there's another source of /usr/tmp. Let me find every definition across the whole repo and see how 32 sources it.

  Ran 1 shell command

Two export GLOBAL_TEMP_DIR lines (844, 1670) but my assignment is at 1327 using ${GLOBAL_TEMP_DIR:-…} — which preserves a value inherited from the environment. So /usr/tmp is being injected before gvar_common.sh runs (a persisted env file or the menu harness exports it). Let me trace the real source.

  Read 1 file

export GLOBAL_TEMP_DIR at 844 just exports whatever's inherited. And my full-repo grep finds no file that assigns
GLOBAL_TEMP_DIR=/usr/tmp — which means /usr/tmp nning menu process's environment: that menusourced the old gvar_common.sh (with GLOBAL_TEMP_DIR="/usr/tmp") when it launched, exported it, and every child script inherits it. My ${GLOBAL_TEMP_DIR:-…} then preserves that stale value — that's the bug in my fix.

Let me confirm how 31 vs 32 source gvar (the "alignment" question) and rule out an env-file source.

  Ran 1 shell command

Now I see the full picture, and it explains the "alignment" question:

- /usr/tmp now physically exists as root:root 0755 (created Jun 21 23:09 by the old gvar code's sudo mkdir -p /usr/tmp on an earlier run) — so it's not writable by your user.
- The long-running menu exported GLOBAL_TEMP_DIR=/usr/tmp into the environment; both 31 and 32 source gvar_common.sh identically (line 33), but 31 never uses GLOBAL_TEMP_DIR (it's apt-only) while 32 uses it for the build — that's the only difference.
- My fix ${GLOBAL_TEMP_DIR:-…} preserved that stale /usr/tmp, and since /usr/tmp now exists, my "create if missing" block was skipped → still unwritable.

The correct fix: resolve to the first writable cwritable inherited value. This aligns every script through the shared gvar_common.sh.

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/gvar_common.sh)
Added 24 lines, removed 17 lines
    return 0
}

# Initialize global temporary directory.
# NOTE: /usr/tmp is a legacy SysV alias for /var/tmp that exists only as a symlink on
# some RPM distros; on Debian/Ubuntu/Kali it is absent, so clones/builds under it failed
# with "Permission denied". Use $TMPDIR when set, else /var/tmp -- persistent and
# disk-backed (good for large source builds, unlike RAM-backed /tmp) and world-writable
# + sticky on every Debian-family system, so the invoking non-root user can create build
# dirs here. Export GLOBAL_TEMP_DIR to override.
GLOBAL_TEMP_DIR="${GLOBAL_TEMP_DIR:-${TMPDIR:-/var/tmp}}"
GLOBAL_TEMP_DIR="${GLOBAL_TEMP_DIR%/}"
# Initialize the global temporary directory base.
# It MUST be writable by the invoking (non-root) user, because scripts clone/build here
# as the user (then `make install` as root). Pitfalls this resolver defends against:
#   * /usr/tmp is a legacy SysV alias n/Ubuntu/Kali; worse,
#     an older revision of this file created it as root:root 0755 (unwritable), and a
#     long-running menu may still EXPOnto child scripts.
#   * So a stale/unwritable INHERITED value must be SKIPPED, not preserved -- using
#     "${GLOBAL_TEMP_DIR:-...}" alone would keep the bad /usr/tmp.
# Resolution order (first that is a WRITABLE dir, or can be created writable, wins):
#   exported GLOBAL_TEMP_DIR, $TMPDIR, /var/tmp, /tmp. /var/tmp is preferred (persistent +
#   disk-backed -> good for large source builds, unlike RAM-backed /tmp).
__resolve_global_temp_dir_from_gvar_common() {
    local c
    for c in "${GLOBAL_TEMP_DIR:-}" "${TMPDIR:-}" /var/tmp /tmp; do
        [ -z "$c" ] && continue
        c="${c%/}"
        if { [ -d "$c" ] || mkdir -p "$c" 2>/dev/null; } && [ -w "$c" ]; then
            echo "$c"; return 0
        fi
    done
    echo /tmp
}
GLOBAL_TEMP_DIR="$(__resolve_global_temp_dir_from_gvar_common)"
unset -f __resolve_global_temp_dir_fro

# Ensure global temporary directory exMISSING -- never
# chmod an existing system temp dir, which would clobber its 1777 sticky permissions.
if [ ! -d "$GLOBAL_TEMP_DIR" ]; then
    echo "Creating global temporary directory: $GLOBAL_TEMP_DIR"
    mkdir -p "$GLOBAL_TEMP_DIR" 2>/dev/null || $USE_SUDO mkdir -p "$GLOBAL_TEMP_DIR"
    chmod 1777 "$GLOBAL_TEMP_DIR" 2>/dev/null || $USE_SUDO chmod 1777 "$GLOBAL_TEMP_DIR" 2>/dev/null || true
fi

# Function to create script-specific temporary directory (restricted to $GLOBAL_TEMP_DIR/<script_name>)
create_script_temp_dir() {
    local script_name="$1"

Now let me also make the swoole build-dir creation self-defending (fallback through writable candidates), so 32 can never fail on the temp dir again regardless of inherited env:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/32_install_swoole.sh)
Added 13 lines, removed 5 lines
            $USE_SUDO apt-get install -y git
        fi

        # Build under the (writable) glds PID collisions and
        # the old /usr/tmp EACCES. git clones into the empty dir mktemp created.
        local build_dir
        build_dir="$(mktemp -d "${GLOBAL_TEMP_DIR%/}/swoole-src-build-XXXXXX" 2>/dev/null)" \
            || { echo -e "${RED}$SCRIPT_INDEX Could not create a build dir under '$GLOBAL_TEMP_DIR'${NC}"; retu
rn 1; }
        # Build under a WRITABLE temp dir; mktemp -d avoids PID collisions. Fall back
        # through candidates so a stale/unwritable GLOBAL_TEMP_DIR (e.g. the legacy
        # root-owned /usr/tmp inherited from the menu) can never block the build.
        local build_dir="" _tbase
        for _tbase in "${GLOBAL_TEMP_DIR%/}" "${TMPDIR%/}" /var/tmp /tmp; do
            [ -n "$_tbase" ] || continue
            build_dir="$(mktemp -d "${_tbase}/swoole-src-build-XXXXXX" 2>/dev/null)" && break
            build_dir=""
        done
        if [ -z "$build_dir" ]; then
            echo -e "${RED}$SCRIPT_INDEX Could not create a writable build dir (tried '$GLOBAL_TEMP_DIR', /var/
tmp, /tmp)${NC}"
            return 1
        fi

        echo -e "${CYAN}$SCRIPT_INDEX Cloning Swoole (ref: $swoole_ref) into $build_dir...${NC}"
        if ! git clone --depth 1 --branch "$swoole_ref" \

Now validate: syntax, the gvar resolver against the actual stale /usr/tmp scenario, and the swoole fallback.

  Ran 1 shell command

All validated against the exact failing scenario:
- bash -n both files: OK
- /usr/tmp confirmed not writable by your user (the root cause)
- The resolver, given the stale GLOBAL_TEMP_DIR=/usr/tmp, now returns /var/tmp (skips the unwritable inherited value); unset→/var/tmp; a good value is preserved
- A real mktemp -d build dir was created under /var/tmp exactly as the script will

Why 31 and 32 weren't aligned (your question)

They are aligned in sourcing — both do source …/gvar_common.sh at line 33. The difference is purely functional:

- 31 is apt-only and never touches GLOBAL_TEMP_DIR, so the broken value was invisible to it.
- 32 builds Swoole from source and uses GLOBAL_TEMP_DIR as the build dir → it hit the bad value.

And the value was bad because of shared infra + runtime state:
1. The README bootstrap (sudo mkdir -p /usr/tmp …) / old gvar code physically created /usr/tmp as root:root 0755 — unwritable by your user.
2. The long-running menu exported GLOBAL_TEMP_DIR=/usr/tmp, so every child inherits it.
3. My first fix used ${GLOBAL_TEMP_DIR:-…}, which preserved that stale value, and since /usr/tmp now exists, the "create if missing" path was skipped.

Since the root cause is in shared infra, the fix belongs there — which is what truly aligns them.

The fix

- gvar_common.sh: GLOBAL_TEMP_DIR now resolves to the first writable candidate — exported value → $TMPDIR → /var/tmp → /tmp — skipping a stale/unwritable inherited /usr/tmp instead of preserving it. Every script that uses it is now aligned and safe.
- 32_install_swoole.sh: the build-dir mktemp also falls back through writable candidates, so it can never be blocked by a bad
temp base again.

Net: no menu restart needed — on the next run, solves GLOBAL_TEMP_DIR to /var/tmp inside thescript.

Action

Re-run 32_install_swoole.sh. It'll build Swoole v6.2.1 (already selected correctly last run) into /var/tmp/swoole-src-build-XXXXXX, then phpize/configure/make/make install.

Two optional follow-ups (say the word and I'll do them):
1. The leftover /usr/tmp (root:root) is now harmless but is clutter — sudo rmdir /usr/tmp if you want it gone.
2. The README install command still does sudo mkdir -p /usr/tmp (the origin of this whole thing) — I can switch the documented bootstrap to /var/tmp so fresh installs never recreate the unwritable dir.

✻ Baked for 5m 19s

❯ 31_ensure_php85_intelligent.sh
    32_install_swoole.sh
    33_configure_php85.sh
    34_install_composer.sh Logic and DATA share to,behavior,and datarelationsships remainconsistent before and after each step.

This is a cross-script consistency audit across the 4-step PHP pipeline (31→32→33→34): make their shared logic and data aligned, and ensure each step's pre/post-conditions stay consistent. Let me first map the shared data model and how each script sources it.

  Ran 1 shell command

Misalignment #1 already visible: 32_install_swoole.sh does not source php_common_functions.sh (31/33/34 all do) — so it can't use the shared PHP logic and reimplements version detection etc. inline. Let me read the shared DATA (php_common_vars.sh) and shared LOGIC (php_common_functions.sh) to establish the canonical model.

  Read 1 file

Canonical DATA is well-centralized in php_common_vars.sh (PHP_VERSION="8.5", TARGET_LINK_PATH, package/extension arrays, EXTENSION_MAP, PHP_INI_FILES, composer paths, states). Now the shared LOGIC — let me inventory php_common_functions.sh and check whether each script uses the shared data/functions or hardcodes its own.

  Ran 1 shell command

Now I have the canonical model:
- DATA (php_common_vars.sh): PHP_VERSION="8.5", in/php, PHP85_CORE_PACKAGES, CORE_EXTENSIONS,EXTENSION_MAP, REQUIRED_EXTENSIONS, OCTANE_*, PHP_INI_FILES, COMPOSER_*, state maps.
- LOGIC (php_common_functions.sh): check_network_connectivity, check_symbolic_link, prevent_apache2_conflicts, configure_php_for_laravel, etc.

This is a genuine multi-file consistency audit. I'll fan out a workflow to profile each script against this canonical model in parallel, then synthesize a concrete, file/line-level misalignment list with exact fixes — then I'll apply them.

● Workflow(Audit logic/data/behavior alignment across PHP install scripts 31-34 and produce concrete fixes)
/workflows to view dynamic workflow runs

The audit is running. Meanwhile, let me read scripts 33 and 34 myself (I haven't seen them) so I can evaluate the findings and apply fixes quickly.

  Read 1 file

Read 33 fully. Already spotting real misalignments: hardcoded "8.5" throughout (vs $PHP_VERSION), update-alternatives priority 84 here vs 85 in 31 (drift), three scripts managing the php command differently (31: alternatives@85 + symlink; 32:
ensure_php_symlink; 33: alternatives@84 only), a2", claims it modifies FPM files it no longertouches), and a hardcoded module list instead of REQUIRED_EXTENSIONS. Let me read 34.

  Read 1 file

Read all 4 scripts now. I can already see concrete misalignments (e.g., 33 uses update-alternatives priority 84 vs 31's 85; 34 hardcodes /usr/local/bin/composer-safe despite COMPOSER_SAFE_PATH existing in the shared vars; 4 different PHP-version-detection idioms). Let me confirm a few specifics (31's priority, 32's ext-dir/symlink handling) as ground truth while the audit finishes.

  Ran 1 shell command

Confirmed ground truth for the key misalignments:

- Behavior drift: update-alternatives priority is 85 in 31 (line 521) but 84 in 33 (line 232) for the same php alternative.
- Dead shared var: COMPOSER_SAFE_PATH is used 0 times in 34; the literal /usr/local/bin/composer-safe is hardcoded 8 times.
- php command managed 3 ways: 31 = alternatives@85 + /usr/local/bin/php symlink; 32 = its own ensure_php_symlink; 33 = alternatives@84, no symlink.
- PHP-version detection: 4 different idioms (31 state-fn, 32 PHP_MAJOR/MINOR, 33 php -v|cut, 34 grep -oP).
- 32 dynamically resolves extension_dir (good) but hardcodes /etc/php/8.5/...; 33 verifies a divergent subset of modules
instead of REQUIRED_EXTENSIONS; 33's header stil it edits FPM files it no longer touches.

The systematic audit workflow is finishing its se + anything I missed in 31's internals andproduces the consistency matrix). I'll apply one coherent, surgical fix set once it lands — centering on a single source of truth in php_common_vars.sh (derived PHP_BIN, PHP_ALT_PRIORITY) so 31/33 stop drifting, plus wiring 34 to COMPOSER_SAFE_PATH and 33 to REQUIRED_EXTENSIONS.

✻ Sautéed for 5m 11s

● Dynamic workflow "Audit logic/data/behavior alignment across PHP install scripts 31-34 and produce concrete fixes" completed · 5m 2s

The audit corroborates my findings and adds key ones (priority drift is actually across 31=85, 32=84, 33=84; 31's L951 install string pulls in the php8.5 metapackage + FPM contradicting the no-FPM intent; 32's ensure_php_symlink targets the wrong link /usr/bin/php instead of /usr/local/bin/php; verify_php_symbolic_link_fix defined twice in 31). Let me read the full synthesis (recommended fixes + matrix) before applying.

  Ran 3 shell commands

Now I have the complete adjudicated plan. Let me read the precise spots needing careful edits (32's ensure_php_symlink and the
two duplicate functions in 31) before applying t

  Read 1 file

Now the two duplicate verify_php_symbolic_link_fix definitions in 31:

  Ran 1 shell command

The two verify_php_symbolic_link_fix definitions differ (old-version lists 7.4–8.4 vs 7.4–8.3, different var names) and the second (L646) wins — so L566 is dead code. I'll handle that carefully. First let me enumerate all the priority/binary literal spots across 31 and 33 so my single-source-of-truth changes are complete.

  Ran 1 shell command

Complete picture confirmed. Now applying the coherent alignment fix set. Edit 1 — establish single source of truth in php_common_vars.sh:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/debian_com/php_common_vars.sh)
Added 8 lines
# PHP_FPM_SOCKET_PATH=""  # Not used
TARGET_LINK_PATH="/usr/local/bin/php"

# Derived from PHP_VERSION -- single souever drift.
# PHP_BIN: the versioned CLI binary (e.g. /usr/bin/php8.5).
# PHP_ALT_PRIORITY: the update-alternatives priority for the 'php' link. MUST be the
#   same in every step (31/32/33) or a later step re-registering at a lower priority
#   can silently change which php is the system default that step 34 then consumes.
PHP_BIN="/usr/bin/php${PHP_VERSION}"
PHP_ALT_PRIORITY="85"

# PHP 8.5 specific packages (NO FPM - using Swoole)
# Note: opcache is now a core extension in PHP 8.5, no separate package needed
# IMPORTANT: Do NOT include "php8.5" metapackage as it pulls in php8.5-fpm

Edit 2 — fix 31's L951 install string to the canonical arrays (removes the php8.5 metapackage + php8.5-fpm, restores sqlite3/intl/readline):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/31_ensure_php85_intelligent.sh)
Added 1 line, removed 1 line
    add_php_repository_permanent_from_apt_repository_manager \
        "$os_id" \
        "$os_codename" \
        "$USE_SUDO apt install -y php8.5-common php8.5-mysql php8.5-pgsql php8.5-z
ip php8.5-gd php8.5-mbstring php8.5-curl php8.5-xml php8.5-bcmath"
        "$USE_SUDO apt install -y ${PHP85_CORE_PACKAGES[*]} ${CORE_EXTENSIONS[*]}"

    if [ $? -eq 0 ]; then
        echo -e "${GREEN}$SCRIPT_INDEX PHP 8.5 installed successfully with repository cleanup${NC}"

Edit 3 — 31's update-alternatives to the shared priority/binary (single source):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/31_ensure_php85_intelligent.sh)
Added 2 lines, removed 2 lines

    # Add only PHP 8.5 to alternatives
    if [ -f "/usr/bin/php8.5" ] && [ -x "/usr/bin/php8.5" ]; then
        echo -e "${YELLOW}$SCRIPT_INDEX Adding PHP 8.5 to alternatives with priority 85...${NC}"
        $USE_SUDO update-alternatives --install /usr/bin/php php /usr/bin/php8.5 85 2>/dev/null || true
        echo -e "${YELLOW}$SCRIPT_INDEX Adding PHP ${PHP_VERSION} to alternatives with priority ${PHP_ALT_PRIOR
ITY}...${NC}"
        $USE_SUDO update-alternatives -HP_BIN" "$PHP_ALT_PRIORITY" 2>/dev/null ||
true

        # Set PHP 8.5 as the default
        $USE_SUDO update-alternatives --set php /usr/bin/php8.5 2>/dev/null || true

Edit 4a — 32 sources php_common_functions.sh (like its siblings):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/32_install_swoole.sh)
Added 1 line
source "$PARENT_DIR_LEVEL_2/common/gvar_common.sh"
source "$PARENT_DIR_LEVEL_2/common/common_functions.sh"
source "$PARENT_DIR_LEVEL_1/debian_com/php_common_vars.sh"
source "$PARENT_DIR_LEVEL_1/debian_com/php_common_functions.sh"

echo -e "${CYAN}========================================${NC}"
echo -e "${CYAN}$SCRIPT_INDEX PHP Swoole Extension Installation${NC}"

Edit 4b — rewrite 32's ensure_php_symlink to honor the canonical contract link $TARGET_LINK_PATH + shared priority/binary, and fix the stale guidance:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/32_install_swoole.sh)
Added 25 lines, removed 19 lines
}

ensure_php_symlink() {
    echo -e "${BLUE}$SCRIPT_INDEX Ensuring PHP 8.5 symlink exists...${NC}"
    echo -e "${BLUE}$SCRIPT_INDEX Ensuring PHP ${PHP_VERSION} is the default php...${NC}"

    if [ ! -f "/usr/bin/php" ] || ! /usr/bin/php -v 2>/dev/null | grep -q "PHP 8.5"; then
        echo -e "${YELLOW}$SCRIPT_INDEX PHP 8.5 symlink missing or incorrect, fixing...${NC}"
    if [ ! -x "$PHP_BIN" ]; then
        echo -e "${RED}$SCRIPT_INDEX PHP ${PHP_VERSION} binary not found at $PHP_BIN${NC}"
        echo -e "${YELLOW}$SCRIPT_INDEX Please run 31_ensure_php85_intelligent.sh first${NC}"
        return 1
    fi

        if [ -f "/usr/bin/php8.5" ]; then
            # Register php8.5 (idempotent) without wiping other PHP alternatives.
            # --remove-all would destroy every registered php alternative on rerun.
            $USE_SUDO update-alternativ /usr/bin/php8.5 84
            $USE_SUDO update-alternatives --set php /usr/bin/php8.5
            echo -e "${GREEN}$SCRIPT_INC}"
        else
            echo -e "${RED}$SCRIPT_INDEX PHP 8.5 binary not found at /usr/bin/php8.5${NC}"
            echo -e "${YELLOW}$SCRIPT_INDEX Please run 31_ensure_php84_intelligent.sh first${NC}"
            return 1
        fi
    else
        echo -e "${GREEN}$SCRIPT_INDEX PHP 8.5 symlink already exists${NC}"
    # 1) /usr/bin/php via update-alternatives -- SAME priority as step 31 (shared
    #    PHP_ALT_PRIORITY) so re-running this step never lowers the default 31 set.
    #    No --remove-all (that would wipe other registered php alternatives).
    if [ ! -e "/usr/bin/php" ] || ! /usr/bin/php -v 2>/dev/null | grep -q "PHP ${PHP_VERSION}"; then
        $USE_SUDO update-alternatives --install /usr/bin/php php "$PHP_BIN" "$PHP_ALT_PRIORITY"
        $USE_SUDO update-alternatives --set php "$PHP_BIN"
        echo -e "${GREEN}$SCRIPT_INDEX PHP ${PHP_VERSION} set as default (priority ${PHP_ALT_PRIORITY})${NC}"
    fi

    if /usr/bin/php -v 2>/dev/null | grep -q "PHP 8.5"; then
        echo -e "${GREEN}$SCRIPT_INDEX r/bin/php${NC}"
    # 2) The canonical entrypoint link that step 31 CREATES and step 34 CONSUMES.
    #    32 previously only checked /ushile this contract
    #    link was broken. Keep it in sync here too.
    if [ "$(readlink -f "$TARGET_LINK_PATH" 2>/dev/null)" != "$(readlink -f "$PHP_BIN" 2>/dev/null)" ]; then
        $USE_SUDO mkdir -p "$(dirname "$TARGET_LINK_PATH")"
        $USE_SUDO ln -sf "$PHP_BIN" "$TARGET_LINK_PATH"
        echo -e "${GREEN}$SCRIPT_INDEX Linked $TARGET_LINK_PATH -> $PHP_BIN${NC}"
    fi

    if "$TARGET_LINK_PATH" -v 2>/dev/null | grep -q "PHP ${PHP_VERSION}"; then
        echo -e "${GREEN}$SCRIPT_INDEX PHP ${PHP_VERSION} available at $TARGET_LINK_PATH and /usr/bin/php${NC}"
        return 0
    else
        echo -e "${RED}$SCRIPT_INDEX Failed to set PHP 8.5 as default${NC}"
        echo -e "${RED}$SCRIPT_INDEX Failed to make PHP ${PHP_VERSION} the default${NC}"
        return 1
    fi
}

Edit 5 — align 33's priority (84→shared) and bin

● Update(/mnt/dev_nvme0n1p1/programing/core_node/sstall_shells/33_configure_php85.sh)
Added 1 line, removed 1 line
    echo -e "${YELLOW}$SCRIPT_INDEX Step 2: Setting PHP 8.5 as default system version...${NC}"

    # Find PHP 8.5 binary
    local php85_binary="/usr/bin/php8.5"
    local php85_binary="$PHP_BIN"
    if [ -f "$php85_binary" ] && [ -x "$php85_binary" ]; then
        echo -e "${GREEN}$SCRIPT_INDEX Found PHP 8.5 binary: $php85_binary${NC}"


● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/33_configure_php85.sh)
Added 3 lines, removed 3 lines

        # Use update-alternatives as per documentation
        echo -e "${YELLOW}$SCRIPT_INDEX Adding only PHP 8.5 to alternatives...${NC}"
        if $USE_SUDO update-alternatives --install /usr/bin/php php /usr/bin/php8.5 84; then
            echo -e "${GREEN}$SCRIPT_INDEX PHP 8.5 added to alternatives with priority 84${NC}"
        if $USE_SUDO update-alternative"$php85_binary" "$PHP_ALT_PRIORITY"; then
            echo -e "${GREEN}$SCRIPT_INDEX PHP ${PHP_VERSION} added to alternatives with priority ${PHP_ALT_PRI
ORITY}${NC}"

            # Explicitly set PHP 8.5 as the default
            $USE_SUDO update-alternatives --set php /usr/bin/php8.5 2>/dev/null || true
            $USE_SUDO update-alternatives --set php "$php85_binary" 2>/dev/null || true
            echo -e "${GREEN}$SCRIPT_INDEX PHP 8.5 explicitly set as default${NC}"
        else
            echo -e "${RED}$SCRIPT_INDEX Failed to set PHP 8.5 as default${NC}"

Edit 5c — align 33's module verification to the canonical REQUIRED_EXTENSIONS/EXTENSION_MAP (was a divergent hardcoded subset):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/33_configure_php85.sh)
Added 5 lines, removed 2 lines

        # Show some key modules
        echo -e "${CYAN}$SCRIPT_INDEX Key modules status:${NC}"
        for module in "curl" "mbstring" "xml" "zip" "gd" "mysqli" "opcache"; do
            if php -m 2>/dev/null | grep -qi "^$module$"; then
        # Drive the check from the canoXTENSION_MAP so this
        # step verifies exactly the modules step 31 installs (no divergent hardcoded list).
        for ext in "${REQUIRED_EXTENSIO
            local module="${EXTENSION_MAP[$ext]:-$ext}"
            if php -m 2>/dev/null | grep -qi "^${module}$"; then
                echo -e "${GREEN}$SCRIPT_INDEX   $module: [OK]${NC}"
            else
                echo -e "${YELLOW}$SCRIPT_INDEX   $module: [Missing]${NC}"

Edit 6 — wire 34 to the canonical vars (TARGET_LINK_PATH, COMPOSER_SAFE_PATH, PHP_VERSION):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/34_install_composer.sh)
Added 1 line, removed 1 line
source "$PARENT_DIR_LEVEL_1/debian_com/php_common_functions.sh"

# Configuration - using variables from php_common_vars.sh
PHP_BINARY="/usr/local/bin/php"
PHP_BINARY="$TARGET_LINK_PATH"

# Version requirements
MIN_COMPOSER_VERSION_MAJOR=2

● Update(/mnt/dev_nvme0n1p1/programing/core_node/sstall_shells/34_install_composer.sh)
Added 8 lines, removed 8 lines
    fi

    # Check 7: Wrapper script integrity
    if [ ! -f "/usr/local/bin/composer-safe" ]; then
    if [ ! -f "$COMPOSER_SAFE_PATH" ]; then
        reason="composer-safe wrapper missing"
        echo -e "${YELLOW}$SCRIPT_INDEX [CORRECTION NEEDED] $reason${NC}"
        return 0

    echo -e "${CYAN}${NC}"
    echo -e "${CYAN}Created files:${NC}"
    echo -e "${CYAN}  /usr/local/bin/composer            (main wrapper)${NC}"
    echo -e "${CYAN}  /usr/local/bin/composer-safe       (explicit safe wrapper)${NC}"
    echo -e "${CYAN}  $COMPOSER_SAFE_PATH       (explicit safe wrapper)${NC}"
    echo -e "${CYAN}  /usr/local/bin/composer.original   (original Composer binary)${NC}"
    echo -e "${CYAN}  /usr/local/etc/.composer_php_version (PHP version tracking)${NC}"
    echo -e "${CYAN}${NC}"

        echo -e "${GREEN}$SCRIPT_INDEX s properly installed and working${NC}"
        echo -e "${GREEN}$SCRIPT_INDEX �?Location: $COMPOSER_TARGET_PATH (wrapper)${NC}"
        echo -e "${GREEN}$SCRIPT_INDEX �?Original: ${COMPOSER_TARGET_PATH}.original${NC}"
        echo -e "${GREEN}$SCRIPT_INDEX �?Safe wrapper: /usr/local/bin/composer-safe${NC}"
        echo -e "${GREEN}$SCRIPT_INDEX �?Safe wrapper: $COMPOSER_SAFE_PATH${NC}"
        echo -e "${GREEN}$SCRIPT_INDEX �?PHP version tracking: $php_version${NC}"
        echo -e "${CYAN}$SCRIPT_INDEX All checks passed - no corrections needed${NC}"
        exit 0


    $USE_SUDO rm -f "$COMPOSER_TARGET_PATH" 2>/dev/null || true
    $USE_SUDO rm -f "${COMPOSER_TARGET_PATH}.original" 2>/dev/null || true
    $USE_SUDO rm -f "/usr/local/bin/composer-safe" 2>/dev/null || true
    $USE_SUDO rm -f "$COMPOSER_SAFE_PATH" 2>/dev/null || true
    $USE_SUDO rm -f "/usr/local/bin/composer-php85" 2>/dev/null || true
    $USE_SUDO rm -f "/usr/local/bin/composer-php84" 2>/dev/null || true  # Remove old version reference
    $USE_SUDO rm -f "/usr/bin/composer" 2>/dev/null || true


    # Create global wrapper to handle root warnings and open_basedir
    echo -e "${CYAN}$SCRIPT_INDEX Creat{NC}"
    $USE_SUDO tee /usr/local/bin/composer-safe > /dev/null << 'EOF'
    $USE_SUDO tee $COMPOSER_SAFE_PATH > /dev/null << 'EOF'
#!/bin/bash
# Global Composer wrapper that handles root warnings and open_basedir restrictions
# Usage: composer-safe [composer-arguments]

# open_basedir is set to none to allow access to all system directories
exec /usr/local/bin/php -d "open_basedir=none" /usr/local/bin/composer.original "$@"
EOF
    $USE_SUDO chmod +x /usr/local/bin/composer-safe
    $USE_SUDO chmod +x $COMPOSER_SAFE_PATH

    # Create main composer wrapper
    echo -e "${CYAN}$SCRIPT_INDEX Creating composer wrapper for better compatibility...${NC}"


    # Test composer-safe wrapper
    echo -e "${CYAN}$SCRIPT_INDEX TestiNC}"
    if COMPOSER_ALLOW_SUPERUSER=1 COMPOSER_NO_INTERACTION=1 /usr/local/bin/composer-safe --version >/dev/null 2
>&1; then
    if COMPOSER_ALLOW_SUPERUSER=1 COMPOSER_NO_INTERACTION=1 $COMPOSER_SAFE_PATH --version >/dev/null 2>&1; then
        echo -e "${GREEN}$SCRIPT_INDEX �?Composer-safe wrapper: OK${NC}"
    else
        echo -e "${RED}$SCRIPT_INDEX �?Composer-safe wrapper: FAILED${NC}"

    echo -e "${GREEN}$SCRIPT_INDEX �?Composer Version: $final_version${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?PHP Version: $(get_current_php_version)${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?Main wrapper: $COMPOSER_TARGET_PATH${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?Safe wrapper: /usr/local/bin/composer-safe${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?Safe wrapper: $COMPOSER_SAFE_PATH${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?Original binary: ${COMPOSER_TARGET_PATH}.original${NC}"
    echo -e "${GREEN}$SCRIPT_INDEX �?PHP tracking file: $PHP_VERSION_TRACK_FILE${NC}"
    echo -e "${CYAN}============================================================================${NC}"

Edit 6c — 34's version assertion uses $PHP_VERSION:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/34_install_composer.sh)
Added 3 lines, removed 3 lines

    # Check PHP 8.5 availability
    if [ ! -x "$PHP_BINARY" ]; then
        echo -e "${RED}$SCRIPT_INDEX PHP 8.5 not found at $PHP_BINARY${NC}"
        echo -e "${RED}$SCRIPT_INDEX PHP ${PHP_VERSION} not found at $PHP_BINARY${NC}"
        echo -e "${YELLOW}$SCRIPT_INDEX Please run 31_ensure_php85_intelligent.sh first${NC}"
        exit 1
    fi

    if ! "$PHP_BINARY" --version | grep -q "PHP 8.5"; then
        echo -e "${RED}$SCRIPT_INDEX $PHP_BINARY is not PHP 8.5${NC}"
    if ! "$PHP_BINARY" --version | grep -q "PHP ${PHP_VERSION}"; then
        echo -e "${RED}$SCRIPT_INDEX $PHP_BINARY is not PHP ${PHP_VERSION}${NC}"
        exit 1
    fi


Now let me validate all the edits (syntax + that the alignments took) before handling the duplicate-function cleanup:

  Ran 1 shell command

Core alignments validated: priority now $PHP_ALT3, 34 wired to COMPOSER_SAFE_PATH (8×, 0 literalsleft), 31's install string uses the canonical arrays. Now the last item — the duplicate verify_php_symbolic_link_fix in 31. Let me read the exact boundaries of the dead first copy.

  Read 1 file

The duplicate-function block contains UTF-8 glyphs that render as mojibake (�?) in my view, so an exact-match Edit would be unreliable. I'll remove the dead first copy (lines 565–644) with a precise, verified line-range delete + backup + syntax check, keeping the active second definition.

  Ran 1 shell command

Duplicate removed cleanly (2→1 defs, bash -n OK, backup discarded). Final validation across all files and confirming the shared vars resolve:

  Ran 1 shell command

All validated:
- bash -n passes on all 5 files; shared vars resbin/php8.5, PHP_ALT_PRIORITY=85, arrays correct).
- All three update-alternatives calls (31/32/33) now use $PHP_BIN/$PHP_ALT_PRIORITY (33 via php85_binary=$PHP_BIN) — zero bare
84/85 literals remain.

Alignment complete — scripts 31→34 now share one logic/data contract

DATA — single source of truth in php_common_vars.sh:
- Added derived PHP_BIN="/usr/bin/php${PHP_VERSION}" and PHP_ALT_PRIORITY="85".
- 31's install string → ${PHP85_CORE_PACKAGES[*]} ${CORE_EXTENSIONS[*]} (was hardcoded php8.5 php8.5-fpm … — pulled the metapackage + FPM and omitted sqlite3/intl/readline; now matches 31's own install_php_core and the NO-FPM intent).
- 34 → COMPOSER_SAFE_PATH (8 literals removed) and PHP_BINARY="$TARGET_LINK_PATH"; version assertion → $PHP_VERSION.
- 33 module check → driven by REQUIRED_EXTENSIONS + EXTENSION_MAP (was a divergent hardcoded subset using mysqli, missing pgsql/sqlite3/intl/bcmath).

LOGIC:
- Priority drift eliminated: was 31=85, 32=84, 33=84 → all now $PHP_ALT_PRIORITY (85). This was a real last-writer-wins bug where running 33/32 after 31 lowered the default php priority.
- 32 now sources php_common_functions.sh (it was the only one of the four that didn't, forcing inline reimplementations).
- Removed the duplicate verify_php_symbolic_link_fix in 31 (two divergent definitions; the dead first copy is gone, 2→1).

BEHAVIOR / handoff:
- 32's ensure_php_symlink now maintains the canonical contract link $TARGET_LINK_PATH (/usr/local/bin/php) — the link 31
creates and 34 requires — in addition to /usr/bicked /usr/bin/php, so it could report successwhile 34's required entrypoint was broken.
- 31's NO-FPM postcondition is now actually met (FPM is never installed, instead of installed-then-disabled).
- Fixed 32's stale "run 31_ensure_php84…" guidance → php85.

The pre/post chain is now consistent: 31 installs CLI+extensions (no FPM), registers php@85, creates /usr/local/bin/php → 32 builds Swoole, keeps that same link/priority → 33 configures the CLI ini + same php@85 → 34 consumes /usr/local/bin/php, verifies $PHP_VERSION.

Optional follow-ups I left (lower-value / cosmetic, say the word):
1. Wire 32's now-available check_network_connectivity_from_php_common before its clone and prevent_apache2_conflicts_from_php_common before its apt installs (parity with 31's guards).
2. 33's header comment still self-labels "32_configure_php85.sh" and references FPM files it no longer edits (doc drift — and that file's own rules say don't touch docs, so I held off).
3. The surviving verify_php_symbolic_link_fix scans old versions 7.4–8.3 (omits 8.4 — reporting only).
4. The shared verify_open_basedir_config_from_php_common still probes a stale php-fpm8.4 branch.

✻ Baked for 16m 20s

❯
  Executing: 13_ensure_python.sh
  COMPILE_DIR: /opt/_kali_2026
  Python venv directory: /opt/_kali_2026/python3_venv
  Python version: 3.13

  === Python Environment Setup ===
  ==================================================

  >>> Checking Python environment status...
  [INFO] Environment type: IS_PRODUCTION=false, IS_WSL=false, HAS_DESKTOP_ENVIRONMENT=true
  [OK] Python3 Python 3.13.12 is installed
  [OK] pip3 26.1.2 is installed
  >>> Installing Python and essential packages (real-time)...
  >>> Installing Python and essential packages...
  >>> Updating package list...
  [13] sudo apt-get update
  Hit:1 https://packages.sury.org/php trixie InRelease
  Hit:2 http://http.kali.org/kali kali-rolling InRelease
  Hit:3 https://packages.microsoft.com/repos/edge-
  Reading package lists... Done
  >>> Installing Python3 base packages...
  [13] sudo DEBIAN_FRONTEND=noninteractive apt-get install -y python3 python3-pip python3-venv python3-dev python3-setuptools python3-wheel build-essential libssl-dev libffi-dev --no-install-recommends
  Reading package lists... Done
  Building dependency tree... Done
  Reading state information... Done
  python3 is already the newest version (3.13.9-3+b1).
  python3-pip is already the newest version (26.1.2+dfsg-1).
  python3-venv is already the newest version (3.13.9-3+b1).
  python3-dev is already the newest version (3.13.9-3+b1).
  python3-setuptools is already the newest version (78.1.1-0.1).
  python3-wheel is already the newest version (0.47.0-1).
  build-essential is already the newest version (12.12).
  libssl-dev is already the newest version (3.6.2-1).
  libffi-dev is already the newest version (3.5.2-4).
  Solving dependencies... Done
  0 upgraded, 0 newly installed, 0 to remove and 36 not upgraded.
  [INFO] Detected Python version: 3.13                                                                                          [INFO] Version-specific tkinter package: python3
  >>> Installing GUI and system packages...                                                                                     [13] sudo DEBIAN_FRONTEND=noninteractive apt-get tcl-dev python3-gi python3-gi-cairo python3-pilpython3-pil.imagetk gir1.2-appindicator3-0.1 gir1.2-gtk-3.0 --no-install-recommends
  Reading package lists... Done
  Building dependency tree... Done
  Reading state information... Done
  Package gir1.2-appindicator3-0.1 is not available, but is referred to by another package.
  This may mean that the package is missing, has been obsoleted, or
  is only available from another source

  E: Package 'gir1.2-appindicator3-0.1' has no installation candidate
  >>> Installing version-specific tkin
  ──── (374 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  and,cusolver,cusparse,nvjitlink,nvrtc,nvtx]==13.0.2; platform_system == "Linux"->torch->openai-whisper) (13.0.85)
  Requirement already satisfied: cuda-pathfinder>=1.4.2 in ./.local/lib/python3.13/site-packages (from cuda-bindings<14,>=13.0.3->torch->openai-whisper) (1.5.5)
  Requirement already satisfied: mpmath<1.4,>=1.1.0 in ./.local/lib/python3.13/site-packages (from sympy>=1.13.3->torch->openai-whisper) (1.3.0)
  Requirement already satisfied: MarkupSafe>=2.0 in ./.local/lib/python3.13/site-packages (from jinja2->torch->openai-whisper)  (3.0.3)
  Downloading numpy-2.4.6-cp313-cp313-manylinux_2_27_x86_64.manylinux_2_28_x86_64.whl (16.6 MB)                                    ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 16.6
  Downloading tiktoken-0.13.0-cp313-cp313-manylinux_2_28_x86_64.whl (1.1 MB)
     ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ 1.1/1.1 MB 263.1 kB/s  0:00:03
  Building wheels for collected packages: openai-whisper
    Building wheel for openai-whisper (pyproject.toml): started
    Building wheel for openai-whisper (pyproject.toml): finished with status 'done'
    Created wheel for openai-whisper: filename=openai_whisper-20250625-py3-none-any.whl size=803979 sha256=abbbd5a96b612eed070b6cede96da3ab197231203eb443a9ac8f1383b5e33a26
    Stored in directory: /home/kali/.cache/pip/wheels/ca/58/d5/fb4539ad74c3ca81eb40f7eda020ac77d080b33ad57449d485
  Successfully built openai-whisper
  Installing collected packages: numpy, tiktoken, openai-whisper
    Attempting uninstall: numpy
      Found existing installation: numpy 2.5.0
      Uninstalling numpy-2.5.0:
        Successfully uninstalled numpy-2.5.0
    Rolling back uninstall of numpy
    Moving to /home/kali/.local/bin/f2py
     from /tmp/pip-uninstall-tonwj6gr/f2py                                                                                        Moving to /home/kali/.local/bin/numpy-config
     from /tmp/pip-uninstall-tonwj6gr/numpy-config                                                                                Moving to /home/kali/.local/lib/python3.13/sitnfo/
     from /home/kali/.local/lib/python3.13/site-packages/~umpy-2.5.0.dist-info
    Moving to /home/kali/.local/lib/python3.13/site-packages/numpy.libs/
     from /home/kali/.local/lib/python3.13/site-packages/~umpy.libs
    Moving to /home/kali/.local/lib/python3.13/site-packages/numpy/
     from /home/kali/.local/lib/python3.13/site-packages/~umpy
  ERROR: Could not install packages due to an OSError: [Errno 13] Permission denied: '/usr/local/lib/python3.13/dist-packages/numpy'
  Consider using the `--user` option or check the permissions.

   alied lgoic, data , behavior. expecially Kali debian Ubunto,search some docs.

New script, same theme — 13_ensure_python.sh on Kali. Two real failures plus a deeper logic inconsistency:

1. gir1.2-appindicator3-0.1 has no installation candidate — package renamed on modern Debian/Kali (Ayatana fork).
2. Permission denied on pip installs (/usr/lib/python3/dist-packages/smmap, /usr/local/lib/python3.13/dist-packages/numpy) — the package phase runs python3 -m pip install … --break-system-packages --no-user using system Python, not the venv it just created, so it collides with dpkg-managed packages / system dirs (PEP 668).                                                   
Let me locate the script and its install/symlink logic, and research the canonical facts in parallel.                         
  Ran 1 shell command

● Web Search("Debian Kali gir1.2-appindicator3-0.1 renamed ayatana package name 2025")
Did 0 searches in 941ms

The script already tries to distinguish venv vs system for PEP 668 flags (lines 252, 304), so the bug is that the package phase runs against system python3, not the venv. Note a deeper clue in your log: even /opt/_kali_2026/python3_venv/bin/python3 -m pip printed "Defaulting to user installation because normal site-packages is not writeable" — the venv lives under root-owned /opt, so the user's pip can't write to it and scatters into ~/.local, then collides with dpkg/system dirs.

WebSearch's backend is erroring; let me get the authoritative facts from the machine itself + read how the script drives the heavy install.

  Ran 1 shell command

Confirmed root causes:
- appindicator: Kali ships gir1.2-ayatanaappindicator3-0.1 (the Ayatana fork); the old gir1.2-appindicator3-0.1 has no        candidate.
- venv permissions: /opt/_kali_2026 is kali:kali but the venv inside is root:root (775) — the kali user can't write to it, so pip falls back to ~/.local and then collides wit
- The venv setup (lines 653–667) correctly uses $VENV_PYTHON3, but the heavy DEPENDENCY_MAP install (line 1044+) uses system python3.

Let me read the DEPENDENCY_MAP install driver, the GUI apt block, and the venv creation/ownership.

  Read 1 file

The driver calls check_and_fix_package_version. Let me see how it (and the torch/onnx guards) pick the Python interpreter — that's where system python3 leaks in — plus the venv creation/ownership.

  Read 1 file, ran 1 shell command

Found the smoking gun: check_and_fix_package_version runs python3 -c import (line 1023) and run_pip_install_realtime "python3" … (1027/1030/1038) — everything targets system python3, never $VENV_PYTHON3. Combined with the root-owned venv, that's why packages scatter to ~/.local and then hit permission/dpkg errors. Let me read the venv creation/ownership and the pip helper to fix this correctly.
                                                                                                                                Read 1 file
                                                                                                                                Now I fully understand the design and the bug. Tite-packages without sudo (line 647) — so it'smeant to be user-owned — but the existing one is stale root:root, and crucially check_and_fix_package_version installs via system python3, not the venv. Let me confirm the pip helper's venv-detection so my fix is correct.

  Read 1 file

run_pip_install_realtime correctly suppresses PEP-668 flags when the target is a venv (detects pyvenv.cfg). So passing $VENV_PYTHON3 instead of "python3" is exactly the fix — installs land in the venv with no --break-system-packages. Let me read the GUI apt block and the main() symlink ordering to complete the picture.

  Read 1 file

❯ [INFO] Using Python virtual environment: /opt/_kali_2026/python3_venv Why doesn't thes algn whit it ?According to the original design,the python executable shold be liked to the global Python binary(ig under /usr/local/bin),while the actual interpreter is python3.xx. so call python is call Venv .14_install_faster_whisper.sh14_install_faster_whisper.sh
  and the venv creation/ownership.
    19_install_uv.sh  21_install_edge_tts.sh
    22_install_tts_offline.sh  95_install_deepseek.sh                                                                             96_install_deepseek_ocr.sh
    97_install_qwen25.sh                                                                                                          98_install_nllb200.sh

The user has now confirmed the design contract and expanded scope. The intended design: /usr/local/bin/python → venv python (/opt/_kali_2026/python3_venv/bin/python3), so python = the venv interpreter; python3 stays system. And they want this alignment across 13, 14, 19, 21, 22, 95, 96, 97, 98 plus the venv creation/ownership.

First let me pin down why 13 doesn't align — find the symlink override that flips python back to system after venv setup, and the main() call order.

  Ran 1 shell command

Now the symlink misalignment is fully explained. main() calls fix_python_links twice — line 18 (before venv) and line 23 (after create_python_venv_and_replace_system) — and fix_python_links (line 412) points /usr/local/bin/python → /usr/bin/python3 (system), overriding the venv symlink that create_python_venv just set (python → venv, line 704). On top of that, a third function (lines 915–933) points both python and python3 → venv. So 13 contains three contradictory symlink behaviors — that's why it doesn't match the design.

Let me read fix_python_links, that third function, and how the venv path is derived (to know if downstream scripts compute it consistently).

  Read 1 file

fix_python_links hard-points python → system python3 / pip → system pip3, and main() calls it again after the venv setup — clobbering the venv link. That's the direct answer to "why doesn't it align." Let me confirm how the venv path is derived (so downstream scripts can share it) and whether the third symlink function is even live.

  Ran 1 shell command

The picture is complete. The third function (setup_production_python_venv, lines 915–933) is the production path (python3→venv too); this dev run uses the other two, which conflict. And COMPILE_DIR comes from gvar_common.sh, so all 9 scripts can derive the same venv path — that's the shared "DATA" they must align to.

Let me fix 13 (the canonical contract) first, then audit the 8 downstream scripts. Fix 13.1 — make fix_python_links venv-aware so it stops clobbering python → venv:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 13 lines, removed 2 lines
        return 1
    fi

    local python3_path=$(command -v pyt
    local pip3_path=$(command -v pip3 2>/dev/null)
    # Design contract: /usr/local/bin/python -> the VENV interpreter (so `python` == venv),
    # while `python3` stays the system interpreter. So target the venv when it exists; fall
    # back to system python3/pip3 only before the venv has been created (first run). This is
    # what lets `fix_python_links` run before AND after venv setup without clobbering the
    # venv link (previously it always forced python -> /usr/bin/python3).
    local python3_path pip3_path
    if [ -x "$VENV_PYTHON3" ]; then
        python3_path="$VENV_PYTHON3"
        pip3_path="${VENV_PIP3:-$(command -v pip3 2>/dev/null)}"
    else
        python3_path=$(command -v python3)
        pip3_path=$(command -v pip3 2>/dev/null)
    fi

    # Create python -> python3 symlink
    if [ ! -e /usr/local/bin/python ]; then

Fix 13.2 — check_and_fix_package_version installs into the venv ($VENV_PYTHON3), not system python3:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 12 lines, removed 4 lines
    local pip_package="$2"
    local version_constraint="$3"

    # Install INTO the venv (the design's `python`), not the externally-managed system
    # python3. Targeting $VENV_PYTHON3 makes run_pip_install_realtime detect the venv
    # (pyvenv.cfg) and drop --break-system-packages/--no-user, so packages land in the venv
    # site-packages instead of ~/.local or /usr/local -- which is what caused the dpkg /
    # "Permission denied" failures on Kali. Fall back to system python3 if no venv yet.
    local py="$VENV_PYTHON3"
    [ -x "$py" ] || py="python3"

    # Special handling for packages with dots in import name (e.g., azure.cognitiveservices.speech)
    local import_cmd="import $import_name"

    # Check if package is installed
    if ! python3 -c "$import_cmd" 2>/d
    if ! "$py" -c "$import_cmd" 2>/dev/null; then
        # Package not installed
        if [ -n "$version_constraint" ]; then
            echo ">>> Installing $pip_package$version_constraint..."
            run_pip_install_realtime "python3" "$pip_package$version_constraint" "" || true
            run_pip_install_realtime "$py" "$pip_package$version_constraint" "" || true
        else
            echo ">>> Installing $pip_package..."
            run_pip_install_realtime "python3" "$pip_package" "" || true
            run_pip_install_realtime "$py" "$pip_package" "" || true
        fi
        return 0
    fi

    # Package is installed
    if [ -n "$version_constraint" ]; then
        echo ">>> Verifying $pip_package version constraint: $version_constraint"
        run_pip_install_realtime "python3" "$pip_package$version_constraint" "--force-reinstall" || true
        run_pip_install_realtime "$py" "$pip_package$version_constraint" "--force-reinstall" || true
    fi

    return 0

Fix 13.3 — add a venv ownership repair so the user's pip writes into the venv (not ~/.local):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 17 lines
    return 0
}

# Ensure the venv is writable by the invoking (non-root) user. The venv is created
# WITHOUT sudo (user-owned by design), but a venv left over from an earlier sudo run can
# be root-owned -- then pip prints "site-packages not writeable" and silently falls back
# to ~/.local, scattering packages and later colliding with dpkg/system dirs. Repair the
# ownership here so every venv pip install lands IN the venv. Same on Debian/Ubuntu/Kali.
ensure_venv_user_writable() {
    [ -d "$VENV_DIR" ] || return 0
    local owner_user="${SUDO_USER:-$(id -un)}"
    [ "$owner_user" = "root" ] && return 0   # pure-root run: root-owned venv is writable
    local cur; cur="$(stat -c '%U' "$VE')"
    if [ -n "$cur" ] && [ "$cur" != "$owner_user" ]; then
        local owner_group; owner_group=v/null || echo "$owner_user")"
        print_step_from_common_functions "Repairing venv ownership ($cur -> $owner_user) so pip installs land i
n the venv, not ~/.local..."
        $USE_SUDO chown -R "$owner_user:$owner_group" "$VENV_DIR" 2>/dev/null || true
    fi
}

# Function to create Python venv and replace system commands
create_python_venv_and_replace_system() {
    print_step_from_common_functions "Creating Python virtual environment and replacing system commands..."

Fix 13.4 — invoke the ownership repair before any venv pip runs:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 4 lines
        print_info_from_common_functions "Virtual environment already exists and is up-to-date: $VENV_DIR"
    fi

    # Repair stale (root-owned) venv owuns, so installs land
    # in the venv instead of falling back to ~/.local.
    ensure_venv_user_writable

    # IMPORTANT: Always upgrade pip and essential packages, even if venv was not rebuilt
    # This ensures pip is always up-to-date and packages are fixed on every run
    print_step_from_common_functions "Ensuring pip and essential packages are up-to-date in venv..."

Fix 13.5 — cross-distro AppIndicator package selection + candidate-filtered GUI install (won't abort on a missing optional package):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 25 lines, removed 12 lines
    # install line — taking down python3-tk/python3-gi/etc. with it. The unversioned
    # python3-tk metapackage already pulls the correct interpreter's tkinter, so it is the
    # only one required here; the versioned variant is attempted separately and best-effort.
    # GUI/system packages. The AppIndicator GIR package was renamed: modern Debian/Kali/
    # Ubuntu ship the Ayatana fork (gir1.2-ayatanaappindicator3-0.1); older Ubuntu used
    # gir1.2-appindicator3-0.1. Pick whichever the apt index actually offers, and install
    # only packages that have a real candidate, so one missing name cannot abort the whole
    # batch (a single apt-get invocatio
    local gui_pkgs=(python3-tk tk-dev tcl-dev python3-gi python3-gi-cairo python3-pil python3-pil.imagetk gir1.
2-gtk-3.0)
    local appind_cand
    for appind_cand in gir1.2-ayatanaappindicator3-0.1 gir1.2-appindicator3-0.1; do
        if apt-cache show "$appind_cand" >/dev/null 2>&1; then
            gui_pkgs+=("$appind_cand"); break
        fi
        appind_cand=""
    done
    [ -z "$appind_cand" ] && print_warning_from_common_functions "No AppIndicator GIR package in apt index (sys
tem-tray icon optional); continuing"
    local gui_installable=() gp
    for gp in "${gui_pkgs[@]}"; do
        if apt-cache policy "$gp" 2>/dev/null | grep -qE 'Candidate: [^(]'; then
            gui_installable+=("$gp")
        else
            print_warning_from_common_functions "Skipping unavailable GUI package: $gp"
        fi
    done
    print_step_from_common_functions "Iages..."
    echo "[13] $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y python3-tk tk-dev tcl-dev python3-gi
 python3-gi-cairo python3-pil python3-pr3-0.1 gir1.2-gtk-3.0 --no-install-recommen
ds"
    $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y \
        python3-tk \
        tk-dev \
        tcl-dev \
        python3-gi \
        python3-gi-cairo \
        python3-pil \
        python3-pil.imagetk \
        gir1.2-appindicator3-0.1 \
        gir1.2-gtk-3.0 \
        --no-install-recommends
    echo "[13] $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y ${gui_installable[*]} --no-install-r
ecommends"
    $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y "${gui_installable[@]}" --no-install-recommends
 || \
        print_warning_from_common_functions "Some GUI packages failed to install (non-fatal; core tkinter comes
 from python3-tk)"

    # Best-effort install of the versiosolated so a missing
    # candidate cannot abort the packages above. Only attempt when the candidate actually

Let me validate 13, then launch the cross-script audit for the 8 downstream consumers.

  Ran 1 shell command

13 validates cleanly: bash -n OK, AppIndicator picks gir1.2-ayatanaappindicator3-0.1, and the venv-targeting is in place (fix_python_links venv-aware, ensure_venv_user_writable defined+called, check_and_fix_package_version → $VENV_PYTHON3).

Now the cross-script audit. Let me fan out over the 8 downstream consumers against the canonical venv contract.

● Workflow(Audit how downstream scripts use the Python venv vs system python, and produce alignment fixes)
/workflows to view dynamic workflow runs

The downstream audit is running. Here's the answer to your question and what I've already fixed in 13 (the canonical source):

Why python wasn't the venv (root cause)

main() calls fix_python_links twice — once before venv setup (line 18) and once after (line 23).
create_python_venv_and_replace_system correctly venv (line 704), but the second fix_python_linksimmediately overwrites it back to /usr/bin/python3 (system) — because fix_python_links was hardcoded to target system python3. So the final python pointed at system, contradicting the design. Two more leaks compounded it:
- The DEPENDENCY_MAP installs ran via system python3 … --break-system-packages --no-user, not the venv.
- The venv was root:root, so even venv-pip printed "site-packages not writeable" and fell back to ~/.local, then collided with dpkg//usr/local.

Fixes applied to 13 (validated: bash -n OK)

1. fix_python_links is now venv-aware → python → $VENV_PYTHON3, pip → $VENV_PIP3 whenever the venv exists (so the before/after calls both yield the venv link instead of clobbering it). python3/pip3 stay system, per your design.
2. check_and_fix_package_version installs into the venv ($VENV_PYTHON3) → run_pip_install_realtime then drops --break-system-packages/--no-user and packages land in the venv, not ~/.local//usr/local.
3. ensure_venv_user_writable repairs a stale root-owned venv back to the invoking user (it's created without sudo by design) — so pip writes into the venv. Runs before any venv pip op.
4. AppIndicator → picks gir1.2-ayatanaappindicator3-0.1 (the Ayatana fork Kali/Debian/modern-Ubuntu ship), falls back to the old name, and the GUI batch now installs only packages with a real candidate so one missing name can't abort it.

Net: python = venv, installs go into the venv, vhings that have to agree for "calling python iscalling the venv" to actually hold.

In progress

A parallel audit of the 8 downstream consumers (14, 19, 21, 22, 95, 96, 97, 98) is running — checking whether each installs into this same $COMPILE_DIR/python3_venv or leaks to system/~/.local, and producing concrete per-script alignment fixes. I'll apply those once it lands (19/uv is flagged for special handling since uv is its own installer). Note COMPILE_DIR comes from gvar_common.sh, so all 9 scripts can derive the same venv path — that's the shared data anchor for the alignment.

✻ Baked for 11m 38s

❯ and more ?

Let me check whether the downstream audit has completed and pull its results.

  Ran 1 shell command

Audit's still running (0-byte output = not done yet). Since you're asking "what else" — let me enumerate every remaining python/pip symlink behavior in 13 to confirm there are no other contradictions beyond the one I fixed.

  Ran 1 shell command

Yes — checked the rest of 13's symlink logic, and there's one more thing worth flagging:

All live symlink paths now agree — fix_python_links (venv-aware) and create_python_venv_and_replace_system both point python → venv / pip → venv pip. No remaining clobber.

Dead, contradictory code: setup_production_python_venv (lines 793–990). It's defined but never called (no caller anywhere in the file or main()), and it would point python3 → venv too (lines 963/970) — which contradicts your design ("python3 stays system"). Harmless today because it's unreachable, but it's exactly the kind of inconsistency that misleads the next reader. Recommend either deleting it or aligning it (keep python3 → system). I left it untouched pending your call since it's a large deletion.

So the complete 13 picture:

┌──────────┬─────────────────────────┬─────────────────┐
│ Command  │      Now points to      │ Per your design │
├──────────┼─────────────────────────┼─────────────────┤
│ python   │ venv python             │ ✅ venv         │
├──────────┼─────────────────────────┼──────────
│ pip      │ venv pip                │ ✅ venv         │
├──────────┼─────────────────────────┼──────────
│ python3  │ system /usr/bin/python3 │ ✅ system       │
├──────────┼─────────────────────────┼─────────────────┤
│ pip3     │ system                  │ ✅ system       │
├──────────┼─────────────────────────┼─────────────────┤
│ packages │ installed into venv     │ ✅              │
└──────────┴─────────────────────────┴─────────────────┘

The downstream audit (14/19/21/22/95/96/97/98) is still running — its output is empty so far. When it lands I'll apply the per-script fixes (making each install into the same $COMPILE_DIR/python3_venv), and I'll fold in the dead-code removal if you want it.

Want me to: (a) wait for the audit and apply all downstream fixes, (b) also delete/align the dead setup_production_python_venv, or (c) both?

✻ Baked for 1m 21s

❯ En suyer that all downstream scripts use the Python interpreter from the virtual enviroment,Update python3.x to point to the
  virtual environment as well,and rename the origito pythonorigin.and other but repaire.No ask.

Clear directives — implementing without question3, python3.x → venv, and pythonorigin → theoriginal system python (preserved, not destroyed; /usr/bin/python3 itself stays untouched for system safety). Let me read 13's current symlink section and check the downstream audit.

  Read 1 file

Now implementing the new contract. Let me read the full fix_python_links so I refactor it cleanly, and check if the downstream audit finished.

  Read 1 file

Now refactoring 13 to the new contract. Edit A — extract a single link_commands_to_venv helper and make fix_python_links use it (python/python3/python3.x → venv, pythonorigin → original):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 45 lines, removed 50 lines
}

# Function to fix Python symlinks
# Establish the command contract on PATH (/usr/local/bin precedes /usr/bin):
#   pythonorigin                      -eter (/usr/bin/python3),
#                                        preserved under a stable name (never renamed/removed).
#   python, python3, python3.<minor>  -> the VENV interpreter.
#   pip, pip3                         -> the VENV pip.
# /usr/bin/python3 itself is NEVER modified (dpkg-managed; system/maintainer scripts that
# hardcode #!/usr/bin/python3 keep working). Idempotent. Returns 1 if the venv is absent.
link_commands_to_venv() {
    [ -x "$VENV_PYTHON3" ] || return 1
    local sys_python3="/usr/bin/python3"
    local venv_pyver link
    venv_pyver="$("$VENV_PYTHON3" -c 'import sys;print(f"{sys.version_info.major}.{sys.version_info.minor}")' 2
>/dev/null || echo "")"

    # Preserve the original system interpreter as 'pythonorigin'.
    [ -x "$sys_python3" ] && $USE_SUDO ln -sf "$sys_python3" /usr/local/bin/pythonorigin

    # python / python3 / python3.<minor> -> venv interpreter.
    for link in /usr/local/bin/python /usr/local/bin/python3 ${venv_pyver:+/usr/local/bin/python${venv_pyver}};
 do
        $USE_SUDO ln -sf "$VENV_PYTHON3" "$link"
    done

    # pip / pip3 -> venv pip.
    if [ -f "$VENV_PIP3" ]; then
        [ -e "$VENV_PIP" ] || ln -sf pip3 "$VENV_PIP" 2>/dev/null || true
        for link in /usr/local/bin/pip /usr/local/bin/pip3; do
            $USE_SUDO ln -sf "$VENV_PIP3" "$link"
        done
    fi
    print_success_from_common_functions "Linked python/python3${venv_pyver:+/python${venv_pyver}} + pip/pip3 ->
 venv; system python preserved as 'pythonorigin'"
    return 0
}

# Function to fix Python symlinks
fix_python_links() {
    print_step_from_common_functions "Fixing Python symlinks in /usr/local/bin..."

    # Check if python3 exists
    if ! command -v python3 >/dev/null 2>&1; then
        print_error_from_common_functio create links"
        return 1
    fi

    # Design contract: /usr/local/bin/python -> the VENV interpreter (so `python` == venv),
    # while `python3` stays the system interpreter. So target the venv when it exists; fall
    # back to system python3/pip3 only before the venv has been created (first run). This is
    # what lets `fix_python_links` run before AND after venv setup without clobbering the
    # venv link (previously it always forced python -> /usr/bin/python3).
    local python3_path pip3_path
    # Once the venv exists, point python/python3/python3.x + pip/pip3 at it (full contract).
    if [ -x "$VENV_PYTHON3" ]; then
        python3_path="$VENV_PYTHON3"
        pip3_path="${VENV_PIP3:-$(command -v pip3 2>/dev/null)}"
    else
        python3_path=$(command -v python3)
        pip3_path=$(command -v pip3 2>/dev/null)
        link_commands_to_venv
        return 0
    fi

    # Create python -> python3 symlink
    if [ ! -e /usr/local/bin/python ]; then
        print_step_from_common_functions "Creating symlink: /usr/local/bin/python -> $python3_path"
        echo "[13] $USE_SUDO ln -sf $python3_path /usr/local/bin/python"
        $USE_SUDO ln -sf "$python3_path" /usr/local/bin/python
        print_success_from_common_functions "Created python symlink"
    elif [ -L /usr/local/bin/python ]; then
        local current_target=$(readlink -f /usr/local/bin/python)
        if [ "$current_target" != "$python3_path" ]; then
            print_step_from_common_functions "Updating python symlink to point to $python3_path"
            echo "[13] $USE_SUDO ln -sf $python3_path /usr/local/bin/python"
            $USE_SUDO ln -sf "$python3_path" /usr/local/bin/python
            print_success_from_common_functions "Updated python symlink"
        else
            print_success_from_common_functions "python symlink already correct"
        fi
    fi

    # Create pip -> pip3 symlink if pip3 exists
    if [ -n "$pip3_path" ]; then
        if [ ! -e /usr/local/bin/pip ]; then
            print_step_from_common_functions "Creating symlink: /usr/local/bin/pip -> $pip3_path"
            echo "[13] $USE_SUDO ln -sf $pip3_path /usr/local/bin/pip"
            $USE_SUDO ln -sf "$pip3_path" /usr/local/bin/pip
            print_success_from_common_functions "Created pip symlink"
        elif [ -L /usr/local/bin/pip ]; then
            local current_target=$(readlink -f /usr/local/bin/pip)
            if [ "$current_target" != "$pip3_path" ]; then
                print_step_from_common_functions "Updating pip symlink to point to $pip3_path"
                echo "[13] $USE_SUDO ln -sf $pip3_path /usr/local/bin/pip"
                $USE_SUDO ln -sf "$pip3_path" /usr/local/bin/pip
                print_success_from_common_functions "Updated pip symlink"
            else
                print_success_from_common_functions "pip symlink already correct"
            fi
        fi
    fi

    # First run (no venv yet): provisionally link `python`/`pip` to the system interpreter so
    # the commands exist; create_pythonoints them to the venv
    # once it is built.
    local sys_python3 sys_pip3
    sys_python3=$(command -v python3)
    sys_pip3=$(command -v pip3 2>/dev/null)
    [ -n "$sys_python3" ] && $USE_SUDO ln -sf "$sys_python3" /usr/local/bin/python
    [ -n "$sys_pip3" ] && $USE_SUDO ln -sf "$sys_pip3" /usr/local/bin/pip
    print_info_from_common_functions "venv not present yet; python/pip provisionally -> system (switches to ven
v after venv creation)"
    return 0
}


Edit B — make create_python_venv_and_replace_system use the same helper (no more "python3 stays system" removal):

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 10 lines, removed 56 lines
    # IMPORTANT: Always refresh symlinks, even if venv was not rebuilt
    # This ensures that 'python' and 'pct venv,
    # even if system packages were updated or symlinks were modified manually
    print_step_from_common_functions "Refreshing Python command symlinks..."

    # ============================================================================
    # IMPORTANT: python3 command stays as system default
    # We do NOT create /usr/local/bin/python3 symlink
    # System's /usr/bin/python3 will be used when user runs 'python3'
    # Establish the command contract: python / python3 / python3.x + pip / pip3 -> venv;
    # the original system interpreter is preserved as 'pythonorigin'. /usr/bin/python3 is
    # never touched. Single source of truth: link_commands_to_venv().
    # ============================================================================
    print_step_from_common_functions "Pointing python / python3 / pip at the venv (system python preserved as '
pythonorigin')..."
    link_commands_to_venv

    # Clean up any old python3 symlink if it exists (from previous versions of this script)
    if [ -L /usr/local/bin/python3 ]; then
        print_warning_from_common_functmlink (python3 should use system default)"
        echo "[13] $USE_SUDO rm -f /usr/local/bin/python3"
        $USE_SUDO rm -f /usr/local/bin/
        print_info_from_common_functions "python3 command will now use system Python: /usr/bin/python3"
    fi
    local venv_pyver_disp
    venv_pyver_disp="$("$VENV_PYTHON3" -c 'import sys;print(f"{sys.version_info.major}.{sys.version_info.minor}
")' 2>/dev/null || echo "")"

    # ============================================================================
    # Handle 'python' command - ALWAYS refresh, pointing to venv
    # ============================================================================
    print_step_from_common_functions "Refreshing 'python' command to point to venv..."

    # Always remove and recreate (refresh every time)
    echo "[13] $USE_SUDO rm -f /usr/local/bin/python"
    $USE_SUDO rm -f /usr/local/bin/python
    echo "[13] $USE_SUDO ln -sf $VENV_PYTHON3 /usr/local/bin/python"
    $USE_SUDO ln -sf "$VENV_PYTHON3" /usr/local/bin/python
    print_success_from_common_functions "Created symlink: python -> $VENV_PYTHON3"

    # ============================================================================
    # Handle 'pip' and 'pip3' commands
    # Same as python: pip3 stays system default, pip points to venv
    # ============================================================================

    # Create pip -> pip3 symlink inside venv if it doesn't exist
    if [ -f "$VENV_PIP3" ] && [ ! -e "$VENV_PIP" ]; then
        ln -sf pip3 "$VENV_PIP"
    fi

    # Clean up any old pip3 symlink if it exists
    if [ -L /usr/local/bin/pip3 ]; then
        print_warning_from_common_functions "Removing old pip3 symlink (pip3 should use system default)"
        echo "[13] $USE_SUDO rm -f /usr/local/bin/pip3"
        $USE_SUDO rm -f /usr/local/bin/pip3
        print_info_from_common_functions "pip3 command will now use system pip3"
    fi

    # Handle 'pip' command - ALWAYS refresh, pointing to venv
    if [ -f "$VENV_PIP3" ]; then
        print_step_from_common_functions "Refreshing 'pip' command to point to venv..."
        echo "[13] $USE_SUDO rm -f /usr
        $USE_SUDO rm -f /usr/local/bin/pip
        echo "[13] $USE_SUDO ln -sf $VENV_PIP3 /usr/local/bin/pip"
        $USE_SUDO ln -sf "$VENV_PIP3" /usr/local/bin/pip
        print_success_from_common_functions "Created symlink: pip -> $VENV_PIP3"
    fi

    print_success_from_common_functions "Python venv setup and system command replacement complete!"
    print_info_from_common_functions "Virtual environment: $VENV_DIR"
    print_info_from_common_functions ""
    print_info_from_common_functions "Command mapping:"
    print_info_from_common_functions "  python3  -> System Python (/usr/bin/python3)"
    print_info_from_common_functions "  python   -> Venv Python ($VENV_PYTHON3)"
    print_info_from_common_functions "  pip3     -> System pip3"
    print_info_from_common_functions "  pip      -> Venv pip ($VENV_PIP3)"
    print_info_from_common_functions "  pythonorigin                       -> System Python (/usr/bin/python3)"
    print_info_from_common_functions "  python / python3${venv_pyver_disp:+ / python${venv_pyver_disp}}   -> Ve
nv Python ($VENV_PYTHON3)"
    print_info_from_common_functions "        -> Venv pip ($VENV_PIP3)"

    return 0
}

● Dynamic workflow "Audit how downstream scripts use the Python venv vs system python, and produce alignment fixes" completed · 11m 49s

Downstream audit done — 0 of 8 aligned: 14/21/22/95/96/97/98 install into system python via bare python3 --break-system-packages --no-user; 19 (uv) forks a parallel uv_default_venv. The audit's #1 recommendation is a shared helper.

Note my 13 change is a big lever: since python3 now resolves to the venv via /usr/local/bin, those python3 calls already hit the venv — but I'll make it explicit/robust per the audit. Let me validate 13 and create the shared helper.

  Ran 1 shell command

13 validates. Two more things: 13's venv creation still uses bare python3 -m venv — which now resolves to the venv itself on re-runs (circular/broken during rebuild). Let me fix that to the system interpreter explicitly, then create the shared helper.

  Ran 1 shell command

The venv-creation lines (674, 682–683) use bare python3 -m venv. Let me read and fix them to use the system interpreter
explicitly.

  Read 1 file

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/13_ensure_python.sh)
Added 12 lines, removed 3 lines
            print_step_from_common_functions "Creating new Python virtual environment in: $VENV_DIR"
        fi

        # Build the venv with the SYSTEM interpreter explicitly. After this script has run
        # once, /usr/local/bin/python3 points at the venv, so a bare `python3 -m venv` on a
        # rebuild would try to use the very venv being replaced (it gets moved to a backup
        # first -> dangling). Use the original system python (pythonorigin if present, else
        # /usr/bin/python3) so rebuilds are always sound.
        local sys_py=/usr/local/bin/pythonorigin
        [ -x "$sys_py" ] || sys_py=/usr/bin/python3
        [ -x "$sys_py" ] || sys_py="$(command -v python3)"

        # Ensure python3-venv is instal
        if ! python3 -m venv --help >/dev/null 2>&1; then
        if ! "$sys_py" -m venv --help >
            print_warning_from_common_functions "python3-venv module not available, installing..."
            echo "[13] $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y python3-venv python3-pip --n
o-install-recommends"
            $USE_SUDO DEBIAN_FRONTEND=noninteractive apt-get install -y python3-venv python3-pip --no-install-r
ecommends


        # Create the virtual environment WITH system-site-packages (real-time output)
        print_step_from_common_functions "Creating venv with --system-site-packages (allows access to system tk
inter, PIL, etc.)..."
        echo "[13] python3 -m venv --system-site-packages $VENV_DIR"
        python3 -m venv --system-site-packages "$VENV_DIR"
        echo "[13] $sys_py -m venv --system-site-packages $VENV_DIR"
        "$sys_py" -m venv --system-site-packages "$VENV_DIR"
        print_success_from_common_functions "Virtual environment created with system-site-packages"

        # Upgrade pip in venv - always run to ensure latest version

Now create the shared helper (the audit's #1 recommendation — DRY the venv resolution across all consumers):

● Write(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/venv_python_common.sh)
Wrote 66 lines to ../../scripts/shells/linux/common/venv_python_common.sh
#!/bin/bash
# ============================================================================
# Shared Python-venv resolution for every install script that consumes the
# virtual environment built by 13_ensure_python.sh.
#
# Single source of truth for the venv location: "$COMPILE_DIR/python3_venv",
# where COMPILE_DIR is exported by common/gvar_common.sh (source that FIRST).
#
# Design contract (see 13_ensure_python.sh):
#   - The venv at $COMPILE_DIR/python3_venv is THE project interpreter.
… +56 lines

Helper created. Let me validate it, then apply the per-script fixes across the 8 downstream scripts via a parallel workflow (independent files) — each agent applies the audit's spec, sources the shared helper, and verifies with bash -n.

  Ran 1 shell command

Helper resolves to the venv. Now applying the 8 (independent files), each agent following theaudit spec + sourcing the helper, then verifying bash -n:

● Workflow(Edit the 8 downstream Python scripts to install into the shared venv (per audit), then verify)
/workflows to view dynamic workflow runs

The apply-workflow is running (8 scripts in parallel + a verify pass). Let me confirm 13's latest edit is clean while it runs.

  Ran 1 shell command

13 validates; venv creation now uses the system interpreter explicitly (so rebuilds are sound now that python3 → venv).

Status — your directives

✅ python3.x now points to the venv too (13): link_commands_to_venv() links python, python3, and python3.13 → venv interpreter (via /usr/local/bin, which precedes /usr/bin on PATH).

✅ Original system Python preserved as pythonorigin (13): /usr/local/bin/pythonorigin → /usr/bin/python3. I deliberately did
not rename /usr/bin/python3 itself — that's dpkgks apt and every #!/usr/bin/python3 maintainerscript. pythonorigin gives you the original interpreter under a stable name while python/python3 resolve to the venv — which is
exactly "calling python/python3 calls the venv,

✅ "Other but repair" (13): venv ownership repair (root→user, so pip writes into the venv), venv creation uses the system interpreter explicitly (fixes the rebuild-circularity now that python3→venv), three contradictory symlink behaviors collapsed into one link_commands_to_venv() helper, install phase targets the venv, AppIndicator → Ayatana.

⏳ "All downstream scripts use the venv" — the audit found 0 of 8 aligned: 14/21/22/95/96/97/98 installed into system python via python3 … --break-system-packages --no-user; 19 (uv) forked a separate uv_default_venv. I created a shared helper common/venv_python_common.sh (exports VENV_PYTHON3 from COMPILE_DIR, with a venv-gated pip), and a workflow is now applying the per-script fixes in parallel (source the helper → install via $VENV_PYTHON3 → drop the PEP-668 flags → point launcher heredocs at the venv; uv retargeted to the shared venv). I'll report the per-script results + final verification when it lands.

Net effect once the downstream pass finishes: every script — and a bare python/python3 in a shell — uses the one venv at $COMPILE_DIR/python3_venv, with the system interpreter still available as pythonorigin.

✻ Baked for 8m 40s

● Dynamic workflow "Edit the 8 downstream Python scripts to install into the shared venv (per audit), then verify" completed · 3m
31s

The apply-workflow finished. Let me read the fulification verdict and the 96/97/98 details(truncated).

  Ran 1 shell command

All 8 edited, bash -n pass on each, verify verdict all-aligned with no remaining system-python installs. Let me run my own independent verification (don't fully trust the agents) across all 8 + 13 + the helper.

  Ran 1 shell command

Independent verification confirms everything. The only remaining --break-system-packages code line is 22:92, which is now gated (&& ! venv_is_venv_from_common "$PYTHON" — applies only when the target isn't a venv); the rest are explanatory comments.

Done — the whole Python pipeline now shares one venv

13_ensure_python.sh (canonical contract):
- pythonorigin → /usr/bin/python3 (original preserved; /usr/bin/python3 itself untouched)
- python, python3, python3.13, pip, pip3 → venv (one link_commands_to_venv() helper; the three contradictory symlink behaviors
are gone)
- Package installs go into the venv ($VENV_PYTHON3, no PEP-668 flags)
- Venv ownership repaired (root→user) so pip wrial
- Venv creation uses the system interpreter explicitly (sound rebuilds now that python3→venv)
- AppIndicator → gir1.2-ayatanaappindicator3-0.1, candidate-filtered GUI install

Shared helper common/venv_python_common.sh — single source of truth: exports VENV_DIR/VENV_PYTHON3 from COMPILE_DIR, plus venv_python_from_common() and a venv-gated venv_pip_install_from_common().

All 8 downstream scripts — verdict all-aligned:

┌────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│     Script     │                                                 Change                                                  │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 14, 21, 95,    │ source helper; install via $VENV_PYTHON3; dropped --break-system-packages/--no-user; 97/98 launcher     │
│ 97, 98         │ heredocs now emit the venv python                                                                       │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 22             │ helper; resolve_python prefers $VENV_PYTHON3; PEP-668 flag gated to non-venv; sherpa guard gets the     │
│                │ venv interpreter                                                                                        │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 96             │ reuses the shared venv (droppystem fallback)                               │
├────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ 19 (uv)        │ retargeted from uv_default_vepython3_venv (UV_PROJECT_ENVIRONMENT)         │
└────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────┘

Validation: bash -n passes on the helper + 13 + all 8; every downstream sources the helper; no ungated system-python installs remain.

Net: python/python3 (shell or script) → the one venv at $COMPILE_DIR/python3_venv; every installer puts packages there; the original interpreter is reachable as pythonorigin — no more ~/.local scatter or dpkg/permission collisions on Kali/Debian/Ubuntu.

One leftover I intentionally did not touch: the dead setup_production_python_venv function in 13 (never called). With the new design it's no longer contradictory (it also wants python3→venv), just redundant. Removing it is a safe cleanup if you want it gone — say the word.

✻ Cooked for 32m 24s

❯ in all scripts,in run any python commander or pip and anythin, print commder-strin first.thank you.

Got it — every Python/pip (and similar) invocatiring before running. The project already does this in places (echo "[13] …", [venv-pip] …); you want it consistent everywhere. Let me add shared run-and-echo wrappers to the
helper, then retrofit the scripts.

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/venv_python_common.sh)
Added 17 lines
    [ -f "$(dirname "$py")/../pyvenv.cfg" ]
}

# Print the exact command-string FIRST (copy-pasteable, for traceability), then run it.
# Use for python / pip / any meaningful command so every invocation is logged before it runs.
#   print_and_run_from_common "$VENV_PYTHON3" -m pip install foo
print_and_run_from_common() {
    echo "[run] $*"
    "$@"
}

# Convenience: run the resolved VENV interpreter with the given args, printing first.
#   venv_run_from_common -m pip install foo      # echoes then runs: <venv python> -m pip install foo
#   venv_run_from_common some_script.py --flag
venv_run_from_common() {
    local py; py="$(venv_python_from_common)"
    echo "[run] $py $*"
    "$py" "$@"
}

# pip-install into the resolved interpreter. Adds the PEP 668 escape flags
# (--break-system-packages --no-user) ONLY when the target is NOT a venv (i.e. an
# externally-managed system python); inside a venv they are unnecessary and harmful.

❯ in all scripts,if run any python commander or pip and anythin, print commder-strin first.thank you.

Understood — the same request, confirmed. Let me make the wrappers echo to stderr (so they're safe even inside $(...) value-capturing calls — the command line shows, but doesn't pollute captured output), then retrofit every script.

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/common/venv_python_common.sh)
Added 5 lines, removed 2 lines

# Print the exact command-string FIRST (copy-pasteable, for traceability), then run it.
# Use for python / pip / any meaningful command so every invocation is logged before it runs.
# The command line is printed to STDERR ide $( ... ) capture
# (the trace shows on the terminal but never pollutes captured stdout).
#   print_and_run_from_common "$VENV_PYT
#   ver="$(print_and_run_from_common "$VENV_PYTHON3" -c 'import sys;print(sys.version)')"  # safe
print_and_run_from_common() {
    echo "[run] $*"
    echo "[run] $*" >&2
    "$@"
}


#   venv_run_from_common some_script.py --flag
venv_run_from_common() {
    local py; py="$(venv_python_from_common)"
    echo "[run] $py $*"
    echo "[run] $py $*" >&2
    "$py" "$@"
}


  Ran 1 shell command

Wrappers validated (stdout clean for capture, tring all 9 Python scripts so every python/pipinvocation prints its command first. Launching a parallel workflow (independent files), with explicit safety rules about not polluting captured output.

● Workflow(Make every python/pip invocation print its command-string before running, across the Python pipeline scripts)
/workflows to view dynamic workflow runs

The retrofit is running across all 9 Python scripts. Here's what's in place:

Shared trace wrappers added to common/venv_python_common.sh (the key piece):
- print_and_run_from_common <cmd…> — prints [run] <cmd> then runs it
- venv_run_from_common <args…> — prints + runs the venv interpreter
- venv_pip_install_from_common — prints + installs into the venv

Crucially they print to stderr, which I verified is safe inside $(…) capture: ver="$(print_and_run_from_common python -c '…')" shows the command line on the terminal but ver stays clean. That avoids the classic trap where echoing a command before a value-capturing call corrupts the captured value.

The workflow is retrofitting every python/pip/.p/22/95/96/97/98 to print its command-string first— adding traces only where missing (many already echo via [NN] …/the helpers), respecting the script's existing [NN] style, and
following hard safety rules (never a plain stdou). A verify pass then checks bash -n, hunts forany capture-corruption bug, and lists any still-untraced effectful call.

I'll report the per-script results and the verification verdict when it completes.

✻ Brewed for 3m 21s

● Dynamic workflow "Make every python/pip invocation print its command-string before running, across the Python pipeline scripts" completed · 8m 6s

The retrofit completed. Let me pull the verification verdict and confirm there are no capture-corruption bugs.

  Ran 1 shell command

All 9 retrofitted, bash -n pass, verify verdict all-traced with zero capture-corruption risks. Let me run my own independent static check for the one real danger — a stdout echo (no >&2) right before a $(…)-captured python/pip call.

  Ran 1 shell command

Independently confirmed: all 9 pass bash -n, and no stdout echo precedes any $(…)-captured python/pip call — so no captured
value is corrupted.

Done — every python/pip command now prints its command-string first

Shared trace wrappers in common/venv_python_common.sh (all echo to stderr → safe inside $(…)):
- print_and_run_from_common <cmd…>, venv_run_from_common <args…>, venv_pip_install_from_common <args>

All 9 Python scripts retrofitted — verify verdict all-traced, 0 corruption risks:

┌───────────────────────────┬────────────────────┬────────────────┐
│          Script           │ trace points added │ already traced │
├───────────────────────────┼────────────────────┼────────────────┤
│ 13_ensure_python          │                 21 │             17 │
├───────────────────────────┼────────────────────┼────────────────┤
│ 96_install_deepseek_ocr   │                 10 │              — │
├───────────────────────────┼────────────────────┼────────────────┤
│ 19_install_uv             │                 11 │              — │
├───────────────────────────┼────────────────────┼────────────────┤
│ 97_install_qwen25         │                  9
├───────────────────────────┼────────────────────┼────────────────┤
│ 22_install_tts_offline    │                  7
├───────────────────────────┼────────────────────┼────────────────┤
│ 14_install_faster_whisper │                  6 │              — │
├───────────────────────────┼────────────────────┼────────────────┤
│ 21 / 98                   │             5 each │              — │
├───────────────────────────┼────────────────────┼────────────────┤
│ 95_install_deepseek       │                  4 │              — │
└───────────────────────────┴────────────────────┴────────────────┘

The rule applied everywhere, matching your existing [NN] convention:
- Effectful calls (installs, launches, -m venv) → print the command to stdout, then run.
- Value-capturing calls ($(python3 -V), version/import probes) → print to stderr (>&2), so the command shows on the terminal but the captured value stays clean — the trap that would otherwise corrupt e.g. the resolved interpreter path or version string.
- Existing traces (e.g. run_pip_install_realtime, apt lines) were left as-is — no double-printing.

So now, across the whole Python pipeline, running any python/pip command logs the exact copy‑pasteable command first — and it composes with the earlier work: those commands target the shared venv ($VENV_PYTHON3), python/python3/python3.x resolve to the
venv, and the original interpreter remains avail

✻ Cooked for 12m 12s

❯
  ┌──(root㉿debian)-[/mnt/dev_nvme0n1p1/programing/core_node]
  └─# /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main/scripts/start.sh
  Initial directory (invocation): /mnt/dev_nvme0n1p1/programing/core_node
  Working directory (Laravel root): /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main
  Repo root (dynamic): /mnt/dev_nvme0n1p1/programing/core_node

  Using php:      /usr/local/bin/php
  Using composer: /usr/local/bin/composer
  Ensuring Laravel runtime directories...
  PHP pdo_pgsql extension present.
  Ensuring PostgreSQL (localhost-only, per-app databases)...
    Running canonical PostgreSQL ensurer (idempotent: install + mount-fix + data-dir reconcile):
      /mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/46_install_postgresql.sh
  [46] PostgreSQL Database Management Script
  [46] START_POSTGRESQL: true
  [46] Mode: server - Processing PostgreSQL...
  [46] ============================================
  [46] START_POSTGRESQL is true - Installing and s
  [46] ============================================
  [46] Port 5432 (PostgreSQL) is already in use.
  [46] Port 5432 is held by the local PostgreSQL server (postgres) -> already serving; leaving it running.
  [46] PostgreSQL is already installed - ensuring config (idempotent)
  [46] Setting up PostgreSQL user and access rules...
  [46] Setting postgres superuser password (stored in global_var POSTGRES_PASSWORD)...
  ALTER ROLE
  [46] Password mirrored to app data dir (open_basedir-safe): /mnt/dev_nvme0n1p1/www/wwwroot/laravel_db/.core_node_secrets/POSTGRES_PASSWORD
  [46] Restricting PostgreSQL to localhost-only access...
  [46] PostgreSQL user setup completed (localhost-only, scram auth)
  [46] Ensuring per-app databases...
  [46] PostgreSQL Installation Information:
  [46] ========================================
  [46] PostgreSQL Version: psql (PostgreSQL) 18.4 (Debian 18.4-1)
  [46] Data Directory: /opt/_kali_2026/postgresql/data
  [46] Config Directory: /etc/postgresql/18/main
  [46] Log Directory: /opt/_kali_2026/postgresql/l
  [46] Default Port: 5432 (localhost-only)
  [46] Default User: postgres
  [46] Password: generated, stored in global_var POSTGRES_PASSWORD (read by Laravel via App\Support\CoreNodeSecrets; never in .env)
  [46] Status: Running
  [46] Service: systemctl/service/pg_ctlcluster {start|stop|restart} postgresql
  [46] Connect (admin): sudo -u postgres psql
  [46] Config: /etc/postgresql/18/main/postgresql.conf
  [46] ========================================
  [46] ==========
  ──── (955 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  e/poly_apps/laravel_main/vendor/symfony/console/Application.php(195): Symfony\Component\Console\Application->doRun()
  #31 /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main/vendor/laravel/framework/src/Illuminate/Foundation/Console/Kernel.php(198): Symfony\Component\Console\Application->run()
  #32 /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main/vendor/laravel/framework/src/Illuminate/Foundation/Application.php(1235): Illuminate\Foundation\Console\Kernel->handle()
  #33 /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main/artisan(16): Illuminate\Foundation\Application->handleCommand()
  #34 {main}                                                                                                                  
     Symfony\Component\ErrorHandler\Error\FatalError                                                                          
    Type of Illuminate\Database\Migrations\Migration@anonymous::$connection must be omitted to match the parent definition in class Illuminate\Database\Migrations\Migration

    at database/migrations/AppQyV1_2026_06_21_100003_add_global_task_id_to_dict_tables.php:23
       19▕  *
       20▕  * Add-only via SafeMigrationHelper, guarded by hasTable per language so it is a
       21▕  * no-op where a table is absent. Unused unless APPQYV1_DUAL_WRITE_GLOBAL is on.
       22▕  */
    ➜  23▕ return new class extends Migration
       24▕ {
       25▕     protected string $connection;
       26▕
       27▕     public function __construct()


     Whoops\Exception\ErrorException
                                                                                                                                Type of Illuminate\Database\Migrations\Migratit be omitted to match the parent definition inclass Illuminate\Database\Migrations\Migration                                                                              
    at database/migrations/AppQyV1_2026_06_21_100003_add_global_task_id_to_dict_tables.php:23
       19▕  *
       20▕  * Add-only via SafeMigrationHelper, guarded by hasTable per language so it is a
       21▕  * no-op where a table is absent. Unused unless APPQYV1_DUAL_WRITE_GLOBAL is on.
       22▕  */
    ➜  23▕ return new class extends Migration
       24▕ {
       25▕     protected string $connection;
       26▕
       27▕     public function __construct()

        +1 vendor frames

    2   [internal]:0
        Whoops\Run::handleShutdown()

  Detecting local IPs (excluding loopback)...                                                                                 Accessible URLs (ready to copy):
    http://10.191.87.54:9000                                                                                                  Starting Laravel development environment with ho
  Note: Running in headless API mode - web.php serves only API debug interface
  Press Ctrl+C to stop all services
  Ensuring port 9000 is free...
  Prerequisites ready. Add laravel_main to a background systemd service (via debian_service_manager)? [Y/n]  In kali,support wsl/debian/ubuntu/kali.

Big positives in this run: PHP 8.5 via /usr/local/bin/php, Composer, PostgreSQL, and Swoole 6.2.1 all working (our installer fixes landed). The blocker now is a PHP fatal in a Laravel migration:

▎ Type of …Migration@anonymous::$connection must be omitted to match the parent definition

The migration declares protected string $connection; but the base Migration class declares $connection untyped — PHP requires a redeclared property's type to match the parent exactly. Let me confirm the parent declaration and find how many migrations have this pattern.

  Ran 1 shell command
                                                                                                                              Confirmed: parent declares protected $connectionrotected string $connection; fatals (propertytypes are invariant in PHP). 4 migrations (all 2026_06_21_*) have this. The fix is identical: drop the type. Let me see the exact declaration lines, then fix and lint all 4

  Ran 1 shell command

All 4 are identical (protected string $connection;). Applying the fix (drop the type → match parent) and linting each with PHP 8.5:

  Ran 1 shell command

php -l only checks syntax — but the $connection error is a class-declaration fatal (triggered when the anonymous class is defined against its parent). Let me verify the real fatal is gone by booting Laravel and actually loading each of the 4 migration classes (non-destructive — just declares + instantiates, no DB writes):

  Ran 1 shell command

Fixed and verified — all 4 migration classes now declare and instantiate cleanly (constructors ran too). Let me make sure there's no other instance of the same bug class (a typed property shadowing the parent Migration's untyped ones).
                                                                                                                              Ran 1 shell command
                                                                                                                              No other instances — the bug class is fully clea different file is pre-existing working-tree churn / the repo's auto-commit; my 4 edits are confirmed on disk by the grep and the class-load test above.)

Fixed — the migration fatal that blocked start.sh

Root cause: the 4 new 2026_06_21_* migrations declared protected string $connection;, but Laravel's base Illuminate\Database\Migrations\Migration declares $connection untyped (protected $connection;). PHP property types are invariant across inheritance — a child may not add a type the parent doesn't have — so the class fatals at declaration: "Type of …$connection must be omitted to match the parent definition." It's a hard fatal on any modern PHP (you're on 8.5), independent of OS — so it's the same on WSL/Debian/Ubuntu/Kali.

Fix: dropped the type in all 4 so they match the parent (the constructor still assigns the connection name string — behavior unchanged):
- protected string $connection;
+ protected $connection;
- global_2026_06_21_100000_add_fast_lane_to_global_tasks.php
- global_2026_06_21_100001_add_capabilities_to_workers.php
- global_2026_06_21_100002_add_substrate_link_columns_to_global_tasks.php                                                   - AppQyV1_2026_06_21_100003_add_global_task_id_t
                                                                                                                            Verified: php -l clean; booted Laravel and loadeion classes (constructors ran) with no fatal —4/4; and a repo-wide scan shows no remaining typed $connection/$withinTransaction shadowing the parent.

Also worth noting from this run

The installer work all landed: /usr/local/bin/php (PHP 8.5), Composer, PostgreSQL 18.4, and Swoole 6.2.1 (Octane compat patch already applied) are all working — that's the whole earlier chain paying off.

One non-blocking warning remains (didn't touch it — separate concern): the pnpm install --save-dev chokidar step warned about a pnpm store major-version mismatch ("node_modules installed with a different major version of pnpm"). chokidar still exists, so hot-reload isn't blocked. If you want, I can fix that by reconciling the pnpm store (pnpm config set store-dir / a clean reinstall) — say the word.

Re-run start.sh and migrations will load past this point.

✻ Crunched for 3m 56s

❯                                                                                                                             ┌──(root㉿debian)-[/mnt/dev_nvme0n1p1/programing
  └─# /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main/scripts/start.sh                                         Initial directory (invocation): /mnt/dev_nvme0n1
  Working directory (Laravel root): /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/laravel_main
  Repo root (dynamic): /mnt/dev_nvme0n1p1/programing/core_node

  Using php:      /usr/local/bin/php
  Using composer: /usr/local/bin/composer
  Ensuring Laravel runtime directories...
  PHP pdo_pgsql extension present.
  Ensuring PostgreSQL (localhost-only, per-app databases)...
    Running canonical PostgreSQL ensurer (idempotent: install + mount-fix + data-dir reconcile):
      /mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/46_install_postgresql.sh
  [46] PostgreSQL Database Management Script
  [46] START_POSTGRESQL: true
  [46] Mode: server - Processing PostgreSQL...
  [46] ============================================
  [46] START_POSTGRESQL is true - Installing and starting PostgreSQL...
  [46] ============================================
  [46] Port 5432 (PostgreSQL) is already in use.                                                                              [46] Port 5432 is held by the local PostgreSQL serving; leaving it running.
  [46] PostgreSQL is already installed - ensuring config (idempotent)                                                         [46] Setting up PostgreSQL user and access rules
  [46] Setting postgres superuser password (stored in global_var POSTGRES_PASSWORD)...
  ALTER ROLE
  [46] Password mirrored to app data dir (open_basedir-safe): /www/wwwroot/laravel_db/.core_node_secrets/POSTGRES_PASSWORD
  [46] Restricting PostgreSQL to localhost-only access...
  [46] PostgreSQL user setup completed (localhost-only, scram auth)
  [46] Ensuring per-app databases...
  [46] PostgreSQL Installation Information:
  [46] ========================================
  [46] PostgreSQL Version: psql (PostgreSQL) 18.4 (Debian 18.4-1)
  [46] Data Directory: /opt/_kali_2026/postgresql/data
  [46] Config Directory: /etc/postgresql/18/main
  [46] Log Directory: /opt/_kali_2026/postgresql/logs
  [46] Default Port: 5432 (localhost-only)
  [46] Default User: postgres
  [46] Password: generated, stored in global_var POSTGRES_PASSWORD (read by Laravel via App\Support\CoreNodeSecrets; never in .env)
  [46] Status: Running                                                                                                        [46] Service: systemctl/service/pg_ctlcluster {s
  [46] Connect (admin): sudo -u postgres psql                                                                                 [46] Config: /etc/postgresql/18/main/postgresql.
  [46] ========================================
  [46] ============================
  ──── (873 lines hidden) ──────────────────────────────────────────────────────────────────────────────────────────────────────
  ies...
    ✓ Exists avatars: /www/wwwroot/laravel_db/avatars
    ✓ Exists avatars/appqyv1: /www/wwwroot/laravel_db/avatars/appqyv1
    ✓ Exists uploads: /www/wwwroot/laravel_db/uploads
    ✓ Exists static: /www/wwwroot/laravel_db/static
    ✓ Exists cache: /www/wwwroot/laravel_db/cache
    ✓ Exists logs: /www/wwwroot/laravel_db/logs
    ✓ Exists sessions: /www/wwwroot/laravel_db/sessions
    ✓ Exists tmp: /www/wwwroot/laravel_db/tmp

  Running migrations (safe mode - only new tables)...
    Running database migrations (idempotent mode - preserves data)
    Command: php artisan migrate --force
                                                                                                                                 INFO  Running migrations.
                                                                                                                                PddToolV1_2026_06_21_000001_create_pdd_tool_v1........... 5.40ms FAIL
    ❌ Migration error: SQLSTATE[08006] [7] connection to server at "127.0.0.1", port 5432 failed: FATAL:  database "pdd_tool_v1_database" does not exist

  Creating invite code tables...
    ✓ invite_codes: exists
    ✓ invite_code_usage: exists
    ✓ default_codes: exists
    Generated Invite Codes:
      • admin: ADMIN_L9AAKJDOA2JWD4TLJ5WN
      • super_admin: SUPER_ZOMX62RFDRUAAWY7JICX
    Stats: 2 codes (2 active), 0 usages

  Database initialization results:
  ✅ Main: exists
  ❌ Main (personal_access_tokens): skipped - will be created by migrations
  ✅ Sub-apps (7): canonical identity — all use main users table (appqyv1, mcpv1, servermanagerv1, achatv1, codemartv1, ittoolsv1, pddtoolv1)                                                                                                       Successfully initialized 1/3 databases.
                                                                                                                              Creating TTS cache tables (EdgeTTS 91 languages)
    Progress: 10/81 (12%)
    Progress: 20/81 (25%)
    Progress: 30/81 (37%)
    Progress: 40/81 (49%)
    Progress: 50/81 (62%)
    Progress: 60/81 (74%)
    Progress: 70/81 (86%)
    Progress: 80/81 (99%)
    Progress: 81/81 (100%)
    ✅ Created: 0, Exists: 81, Total: 81

  Creating voice subtitle user settings tables...
    ✓ mcp_v1_user_settings: exists

  TTS coordination: decommissioning intermediate tts_queue (canonical tables are the source of truth)...
    ✓ tts_queue already decommissioned
    Stats: 142702 pending, 0 processing, 0 completed, 0 failed                                                                
  Creating article library tables (all languages)...                                                                            ✅ Created: 0, Exists: 81, Total: 81
    Articles: 0 total, 0 with audio, 0 without audio

  Importing multilingual word data...
    ⏭️  Staging already has 8028 records, skipping import

  Initializing dictionary (Step 2: Extended words & translations)...
    EN dictionary: 125761 rows, 3025 translated
    Translations already present. Re-run Step 2 anyway? [y/N] (auto-skip in 15s):

The $connection fatal is fixed — those 4 migrations load now, and the app ran routes, system init, TTS tables, invite codes, etc. The new blocker is a different one:

▎ PddToolV1_2026_06_21_000001_create_pdd_tool_v1_tables … FAIL — FATAL: database "pdd_tool_v1_database" does not exist

So a per-app PostgreSQL database (pdd_tool_v1_database) the migration connects to was never created ("Successfully initialized 1/3 databases"). Let me find how per-app DBs are provisioned/named and which ones actually exist.
                                                                                                                              Ran 1 shell command
                                                                                                                              Root cause is clear: 6 of 7 per-app DBs exist, be only one missing — PddToolV1 is a newer appwhose database wasn't provisioned. (Others present too: awy/bank/vipclub.) Let me find how the PostgreSQL ensurer enumerates per-app databases to create.

  Ran 1 shell command

Found the drift: 46_install_postgresql.sh:36 has a hardcoded APP_DATABASES list that's missing pdd_tool_v1_database (the newer PddToolV1 app). It's a second source of truth that drifted from config/database.php's poly connections. Let me see the full config list so I can make the shell match it (and ideally auto-derive to prevent future drift).

  Ran 1 shell command

Now it's crisp: pdd_tool_v1_database is in config/database.php (the source of truth) but missing from the shell's hardcoded APP_DATABASES — a drifted second list (same class of issue as before). The shell also carries 3 legacy DBs not in config. Let me see how 46 resolves paths so I can make the list derive from config (auto-provision new apps) instead of re-drifting.

  Read 1 file
                                                                                                                              46 computes SCRIPT_DIR at line 3, so I can derivabase.php (5 levels up). Let me verify thederivation produces the correct full list (including pdd_tool_v1_database) before editing — with a corrected hardcoded fallbeither way.

  Ran 1 shell command

The derivation works (full list incl. pdd_tool_v1_database). Let me fix 46 to derive APP_DATABASES from the config (so new apps auto-provision and it can't drift again), with a corrected fixed fallback:

● Update(/mnt/dev_nvme0n1p1/programing/core_node/scripts/shells/linux/debian/install_shells/46_install_postgresql.sh)
Added 16 lines, removed 1 line
# wsl_mount_pg_image). Resolved from the central mapping (map_web_path "pg_mount")
# AFTER gvar_common.sh is sourced -- never hardcoded here.
PG_D_MOUNT=""
APP_DATABASES="core_node_main app_qy_v1_database awy_v0_database vipclub_v1_database server_manager_v1_database
achat_v1_database code_mart_v1_database mcp_v1_database it_tools_v1_database bank_v1_database"
# Per-app databases to provision. SOURCE OF TRUTH: Laravel's config/database.php
# ($polyConnection('NAME')), so adding an app there auto-creates its DB here and this list
# cannot drift -- it previously omitted pdd_tool_v1_database, which made that app's migrations
# fail with 'database "pdd_tool_v1_database" does not exist'. We union the config-declared                          # names with core_node_main and a few lefig; if the config is
# unreadable we fall back to a fixed (corrected) list. Same on WSL/Debian/Ubuntu/Kali.                              APP_DATABASES="$(
    _cfg="$SCRIPT_DIR/../../../../../poly_apps/laravel_main/config/database.php"
    if [ -f "$_cfg" ]; then
        printf '%s\n' \
            $(grep -oE "polyConnection\('[a-z0-9_]+'\)" "$_cfg" | grep -oE "'[a-z0-9_]+'" | tr -d "'") \
            core_node_main awy_v0_database bank_v1_database vipclub_v1_database \
            | sort -u | tr '\n' ' '
    fi
)"
[ -z "${APP_DATABASES// /}" ] && APP_DATABASES="core_node_main app_qy_v1_database awy_v0_database vipclub_v1_dat
abase server_manager_v1_database achat_v1_database code_mart_v1_database mcp_v1_database it_tools_v1_database ba
nk_v1_database pdd_tool_v1_database"

# Source gvar_common.sh from parent directory
SCRIPT_CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

Now validate 46, and create the missing database now so you're unblocked immediately (idempotent — same thing 46 will do going forward):                                                                                                                   
  Ran 1 shell command                                                                                                         
46 fixed (derives from config now), and pdd_tool_v1_database created — all 7 per-app DBs now exist. Let me verify Laravel actually connects to it through the pddtoolv1 connection (end-to-end, non-destructive):

  Ran 1 shell command

Verified end-to-end — db:show --database=pddtoolv1 connects to pdd_tool_v1_database (PostgreSQL 18.4, 0 tables, ready), and the PDO connection test returns CONNECTED OK. The failing migration's connection now resolves.

Fixed — the missing per-app database

Root cause: 46_install_postgresql.sh:36 had a hardcoded APP_DATABASES list that had drifted from config/database.php — it was missing pdd_tool_v1_database (the newer PddToolV1 app), so that DB was never created and the migration died with "database pdd_tool_v1_database does not exist." (Same second-source-of-truth drift pattern as the PHP scripts earlier.)

Fix (two parts):
1. 46 now derives APP_DATABASES from config/database.php — the $polyConnection('NAME') calls are the single source of truth, so any new app auto-provisions its DB and the list can't drift again. It unions the config names with core_node_main + the 3   legacy DBs (awy/bank/vipclub) that predate the crrected fixed list if the config can't be read.(bash -n clean; verified it now yields all 11 DBs including pdd_tool_v1_database.)                                          2. Created pdd_tool_v1_database now so you're unnt — exactly what the fixed ensurer does). All 7per-app DBs + legacy now exist.

Where things stand

Both blockers this session are resolved and confirmed:
- ✅ migration $connection fatal (4 migrations) — fixed
- ✅ missing pdd_tool_v1_database — created + ensurer fixed

Re-run start.sh: the PddToolV1_…create_pdd_tool_v1_tables migration will connect and create its tables, and the "initialized 1/3 databases" count should improve now that the DB exists.

Still non-blocking (untouched, separate concern — happy to fix on request): the pnpm install chokidar store-version mismatch warning (hot-reload still works since chokidar exists).

✻ Brewed for 5m 32s

❯ ✔ The modules directory at "/mnt/dev_nvme0n1p1/programing/core_node/poly_apps/pycore_laravel_wordflow_                      ui/node_modules" will be removed and reinstalledmnt/dev_nvme0n1p1/programing/core_node]
  └─# /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/pycore_laravel_wordflow_ui/scripts/start.sh                           [nexus-dash] Original directory: /mnt/dev_nvme0n
  [nexus-dash] Working directory:  /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/pycore_laravel If node_modules exists , u,not remote and reinstall.
                                                                                                                              Different app now — pycore_laravel_wordflow_ui ( want: if node_modules already exists, just update (install) it — don't remove and reinstall from scratch. Let me find the script logic that prompts to wipe node_modules.     
  Ran 1 shell command                                                                                                         
The "removed and reinstalled from scratch" prompt isn't in the shell directly — it likely comes from the install command    start.sh invokes. Let me read start.sh's depende
                                                                                                                              Read 1 file
                                                                                                                              ensure_deps itself is already non-destructive (i -rf). The "removed and reinstalled from scratch.Proceed?" prompt is pnpm's own ERR_PNPM_MODULES_BREAKING_CHANGE — it fires when node_modules was created by an incompatible pnpm version (your Laravel run showed the same "pm" note). Let me confirm the source and theversion mismatch before choosing the fix.

● Bash(cd /mnt/dev_nvme0n1p1/programing/core_node/poly_apps/pycore_laravel_wordflow_ui
  echo "=== confirm message source: is it a custom script anywhere in repo, or pnpm? ==="
  grep -rn 'reinstalled from scratch' /mnt/dev_nvme0n1p1/programing/core_node --include=*.sh --include=*.js --include=*.ts --include=*.mjs 2>/dev/null | grep -v node_modules | head
  echo "  (empty above => it's pnpm's built-in ERR_PNPM_MODULES_BREAKING_CHANGE)"
  echo "=== node_modules present + the pnpm version that created it ==="
  ls -d node_modules 2>/dev/null && echo "node_modules EXISTS"
  echo "--- .modules.yaml (pnpm provenance) ---"; sed -n '1,20p' node_modules/.modules.yaml 2>/dev/null | grep -iE 'pnpm|nodeLinker|hoist|virtualStore|packageManager' | head
  echo "=== current pnpm + the project's declared packageManager ==="
  echo -n "pnpm: "; pnpm --version 2>/dev/null
  grep -nE '"packageManager"|"pnpm"' package.json 2>/dev/null | head
  echo "=== how start.sh invokes install (FORCE_INSTALL + any rm -rf) ==="
  grep -nE 'rm -rf|FORCE_INSTALL|--force|pnpm install|confirm-modules-purge|node_modules' scripts/start.sh | head -20)
=== confirm message source: is it a custom script anywhere in repo, or pnpm? ===
  (empty above => it's pnpm's built-in ERR_PNPM_MODULES_BREAKING_CHANGE)
=== node_modules present + the pnpm version that created it ===
node_modules
node_modules EXISTS
--- .modules.yaml (pnpm provenance) ---
  "hoistedDependencies": {
=== current pnpm + the project's declared pac
pnpm: 11.8.0
=== how start.sh invokes install (FORCE_INSTA
26:#   Force reinstall:  -f | --force-install        Skip ends: --no-backend | --no-frontend
51:NODE_MODULES="${APP_ROOT}/node_modules"
52:VITE_BIN="${APP_ROOT}/node_modules/vite/bin/vite.js"
65:FORCE_INSTALL="${FORCE_INSTALL:-}"
155:# True when the dev toolchain (vite) is present under node_modules.
158:# Idempotent dependency install (skips whesent).
161:    NEED_INSTALL="$FORCE_INSTALL"
168:            warn "node_modules present buite missing); running pnpm install."
175:        if ! pnpm install; then err "pnpm install failed."; exit 1; fi
177:            err "pnpm install finished buve node_modules + pnpm-lock.yaml, then re-run with --force-install."
183:        log "Dependencies look complete; -install to reinstall."
190:    if [ -n "$FORCE_INSTALL" ] || [ ! -f "$DIST_INDEX" ]; then NEED_BUILD=1; fi
197:        log "dist/ present at ${DIST_DIR}-install to rebuild."
325:        -f|--force-install) FORCE_INSTALL=1 ;;


Confirmed: it's pnpm's built-in purge (not a cus created by an older pnpm and 11.8.0 wants torecreate it. The script's ensure_deps itself never wipes — but it forces pnpm install when the tree looks incomplete, and pnpm
then offers the from-scratch wipe.

The fix: when node_modules exists, update in plah purge (run pnpm install non-interactively sopnpm keeps the tree instead of wiping). Only do a full reinstall when node_modules is missing/empty or --force-install is
passed.

● Update(scripts/start.sh)
Added 35 lines, removed 19 lines
# True when the dev toolchain (vite) is
vite_ready() { [ -f "$VITE_BIN" ]; }

# Idempotent dependency install (skips when node_modules + vite are present).
# Idempotent dependency install. PolicySTS, UPDATE it in
# place -- never remove and reinstall it from scratch. An existing tree is updated with a
# NON-INTERACTIVE `pnpm install`: pnpm s directory ... will be
# removed and reinstalled from scratch" purge when it can prompt, so running with stdin from
# /dev/null makes pnpm KEEP the existinor-version modules-format
# change it refuses to purge instead of wiping). A from-scratch reinstall happens ONLY when
# node_modules is missing/empty, or whexplicitly.
ensure_deps() {
    if [ ! -f "$PACKAGE_JSON" ]; then e: $PACKAGE_JSON"; exit 1; fi
    NEED_INSTALL="$FORCE_INSTALL"
    if [ -z "$NEED_INSTALL" ]; then
        if [ ! -d "$NODE_MODULES" ]; then
            NEED_INSTALL=1
        elif [ -z "$(ls -A "$NODE_MODULES" 2>/dev/null)" ]; then
            NEED_INSTALL=1
        elif ! vite_ready; then
            warn "node_modules present (vite missing); running pnpm install."
            NEED_INSTALL=1
    cd "$APP_ROOT" || exit 1

    local fresh_install=""
    if [ -n "$FORCE_INSTALL" ]; then
        log "Force reinstall requested: scratch..."
        # Explicit opt-in to the purge; run non-interactively so it does not block.
        if ! pnpm install --config.conf err "pnpm install failed."; exit 1; fi
        fresh_install=1
    elif [ ! -d "$NODE_MODULES" ] || [ >/dev/null)" ]; then
        log "node_modules missing -> installing dependencies..."
        if ! pnpm install; then err "pnfi
        fresh_install=1
    else
        # node_modules EXISTS -> update in place, never wipe (stdin /dev/null => pnpm will not
        # prompt for nor perform the fre tree just updates).
        log "node_modules present -> updating in place (no from-scratch reinstall)..."
        if pnpm install < /dev/null; th
            log "Dependencies updated in place."
        else
            warn "Kept existing node_modules (pnpm wanted a from-scratch reinstall -- likely a pnpm major-versi
on change)."
            warn "If the dev toolchain is broken below, re-run with --force-install to recreate it."
        fi
    fi
    if [ -n "$NEED_INSTALL" ]; then
        log "Installing pnpm dependencies (idempotent)..."
        cd "$APP_ROOT" || exit 1
        if ! pnpm install; then err "pnpm install failed."; exit 1; fi
        if ! vite_ready; then

    if ! vite_ready; then
        if [ -n "$fresh_install" ]; then
            err "pnpm install finished move node_modules + pnpm-lock.yaml, then re
-run with --force-install."
            exit 1
        fi
        log "Dependencies ready."
        cd "$ORIGINAL_DIR" || true
    else
        log "Dependencies look complete; skipping install. Use --force-install to reinstall."
        warn "Dev toolchain (vite) not ping node_modules as requested."
        warn "Run with --force-install if you want node_modules recreated from scratch."
    fi
    log "Dependencies ready."
    cd "$ORIGINAL_DIR" || true
}

# Build the production dist once (only when missing or forced).

Let me make the in-place install's "never purge" guarantee explicit, then validate:

● Update(scripts/start.sh)
Added 3 lines, removed 1 line
        # node_modules EXISTS -> update in place, never wipe (stdin /dev/null => pnpm will not
        # prompt for nor perform the fre tree just updates).
        log "node_modules present -> updating in place (no from-scratch reinstall)..."
        if pnpm install < /dev/null; th
        # confirm-modules-purge=true (default) + non-interactive stdin => pnpm errors instead
        # of wiping if it considers theever purges unprompted.
        if pnpm install --config.confirm-modules-purge=true < /dev/null; then
            log "Dependencies updated i
        else
            warn "Kept existing node_moatch reinstall -- likely a pnpm major-versi
on change)."

  Ran 1 shell command

Validated — #!/bin/bash (so local is fine), bash -n clean, new logic in place.

Fixed — node_modules is updated in place, never wiped

What was happening: the "… will be removed and reinstalled from scratch. Proceed?" prompt is pnpm's own
ERR_PNPM_MODULES_BREAKING_CHANGE — pnpm 11.8.0 fy an older pnpm major version and wanted torecreate it. The script's ensure_deps never ran rm -rf, but it forced pnpm install whenever the tree looked incomplete, which
handed pnpm the chance to offer that wipe.

New ensure_deps policy (matches "if node_modulesand reinstall"):

┌─────────────────────┬───────────────────────────────────────────────────────────────────────┐
│      Situation      │                                              Behavior                                              │
├─────────────────────┼───────────────────────────────────────────────────────────────────────┤
│ node_modules exists │ pnpm install --config.confirm-modules-purge=true < /dev/null → updates in place; if pnpm deems it  │
│                     │ a breaking change it erre script keeps the tree (+warns)              │
├─────────────────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ node_modules        │ normal fresh pnpm instal                                              │
│ missing/empty       │                                                                                                    │
├─────────────────────┼───────────────────────────────────────────────────────────────────────┤
│ --force-install     │ explicit from-scratch reinstall (purge allowed, non-interactive)                                   │
└─────────────────────┴───────────────────────────────────────────────────────────────────────┘
                                                                                                                          The key mechanism: running pnpm non-interactivelat its default (true) means pnpm will never purgeunprompted — on a breaking change it aborts rather than destroying node_modules. So you keep your modules and just get an incremental update; a clean recreate is now an ell).

Trade-off you asked for: if an existing node_mod., vite missing) the in-place update may error and keep it — the script warns and continues instead of wiping; pass --force-install when you actually want a clean rebuild.

The matching start.ps1 (Windows) still has the old logic — say the word and I'll mirror this same in-place-update policy there for sh/ps1 parity.

✻ Churned for 6m 23s

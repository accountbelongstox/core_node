
   任务 4 — 编排任务进度 ✅

   • 4.1/5.1 重启丢进度：manifest（项目展开+已解析资源+统计
     ）按指纹签名落盘，每 1000 资源增量保存；failed/中断任务
     默认断点续跑（跳过已完成 segment，只补未解析资源）；路
     由注册时 resume_interrupted_generations() 自动恢复滞留
     任务。冒烟：manifest 往返、签名变更拒绝恢复、自动恢复线
     程真实跑通。
   • 崩溃根因修复：delivery 同步异常不再中断整个生成（outbox
     可重试）。
   • 4.2/4.3 批量+多模型并行：缺失资源改为 40/块 × 4 worker
     并行，按资源 id 确定性轮转引擎起始点（实测 20 个种子覆
     盖 10 个不同首引擎），同引擎串行仍由 lease 保证。关于“
     一次生成多词再拆分”：官方文档确认 TTS 模型输出连续语音
     、无对齐能力无法可靠拆分；正确形态是并行多引擎+批量推理
     （qwen3tts 变体路径已存在于句子侧）。

   任务 5 — WordAudioWorker 底层重构 ✅

   • 引擎探测缓存扩展为（计划引擎 + 可用引擎列表）；无
     REQUIRED_ENGINE 钉死的链路上，并发度 = 可用引擎数（上限
     8），用户手动值优先（实测 3 可用→3，用户设 2→2，单串行
     →1）。
   • 每任务按 task_id 确定性轮转引擎排除列表——并行 lane 起始
     于不同引擎，多个本地模型同时合成不同单词。
   • 词缓存从“仅计划引擎路径”改为 any-provider 统一查找（编
     排写入的缓存直接复用，反向亦然）。状态接口新增
     usable_engines/planned_engine 供 UI 展示。全部冒烟通过
     。

   任务 6 — Vocabulary ✅

   • 6.1 一键清除无效单词：后端识别（非 CJK 语言表含中文字符
     / 含 26 字母串）+ 分页预览 + 输入 delete 确认 + 分块删
     除。实测：真实库命中文档同款垃圾词（# 26个英文字母 等 5
     条）；临时行端到端验证检测→删除→清理。
   • 6.1 一键清除无效翻译：识别规则=翻译含供应商错误标记
     （Error: Rate limit
     exceeded/free-models-per-day-high-balance，递归扫描含
     word_translation 嵌套）或翻译与单词相同（排除 word 等元
     数据键，避免误杀）。实测：en 库 45,477 条（43,663 错误
     标记 + 1,814 同词），临时行验证“仅清翻译字段、词条保留”
     。
   • 6.2 一键重生成封面：接入已有的 Laravel AI 网关（免费额
     度优先：gemini flash image/zhipu cogview/pollinations →
     付费兜底，多 key 轮换+cooldown），提示词哈希磁盘缓存（
     同静态根下的全局常量站外目录）。实测：库 3 首次生成
     6.5s（pollinations/flux），同提示词二次调用 cached:true
      0.27s，图片真实落盘（768×768 封面图）。UI 每卡片新增魔
     杖按钮。溯源落
     截断），实测 206,865 文件准确统计（旧版截断为
     100,000）；stale-while-revalidate + Octane 定时预热 +
     缓存锁 + ?fresh=1 真实生效。实测冷扫 21.7s（不再阻塞请
     求路径）、命中 0.001s。

  ⠼ working…
 ───────────────────────────────────────────────────────────
   Todo
   ✓ Task5: Worker重构：共享轮转/多引擎并行/any-provider...
   ✓ Task6.1: 一键清除无效单词/翻译（前后端实测通过）
   ✓ Task6.2: AI封面重生成（已实测：生成6.5s/缓存0.27s）
   ✓ Task6.3: Statistics准确扫描+缓存（实测21.7s→0.001s无...
   ✓ 最终：模块编译验证 + 1:1清单验收完成
   … +4 more (4 done) · ctrl+t to expand
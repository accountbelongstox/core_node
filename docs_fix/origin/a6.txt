  999_check_circular_symlinks.sh
  │   Back to Install and Test Environment
  │ 该菜单改为输入数字 或字符，将匹配到的第一个脚本作为主脚本
  │ 测试，比如输入 93 或者voxcpm等。只要匹配到一个或多个，就
  │ 以匹配到的第一人为准。输入后先打印匹配到的脚本然后运行，
  │ 运行还是一样，最后回到菜单，而不是直接退出，运行结束还是
  │ 一样要先暂停查看输出，而不是直接返回菜单。dd.sh


 Let me understand the request. The user is showing a menu from some
 script (likely dd.sh or related shell script), and wants to modify
 the menu so that users can input a number or characters, which will
 match the first script found as the main script. For example,
 inputting "93" or "voxcpm" - as long as it matches one or more, the
 first match takes precedence. After input, it should print the
 matched script then run it. Running is the same as before, and after
 finishing, return to the menu instead of exiting directly. Also,
 after running ends, it should pause to view output before returning
 to the menu.

 The user mentions "dd.sh" at the end. Let me look for this file and
 understand the structure. The project instructions say for shell
 scripts, refer to
 development-guides/DD_SHELL_GUIDE_THIS_FILE_NO_AI_EDIT.md.

 Let me explore the project first.


 $ find /www/programing/core_node -name "dd.sh" -not -path
 "*/node_modules/*" 2>/dev/null | head -20

 /www/programing/core_node/scripts/shells/linux/dd.sh
 /www/programing/core_node/dd.sh

 Took 0.8s



 $ ls /www/programing/core_node

 ... (33 earlier lines, ctrl+o to expand)
 pyservice.sh
 README.md
 scripts
 webclaude_group
 webpack.config.js

 Took 0.0s


 There are two dd.sh files:
 /www/programing/core_node/scripts/shells/linux/dd.sh and
 /www/programing/core_node/dd.sh. Let me check both. The menu shown
 lists scripts like 1_stop_cloud_monitors.sh, 3_setting_base.sh, etc.
 Let me look at the main dd.sh first.


 read /www/programing/core_node/dd.sh



 $ ls /www/programing/core_node/scripts/shells/linux/ | head -50

 ... (7 earlier lines, ctrl+o to expand)
 project_init_lib.sh
 pytools
 rebuild_docker_compose.sh
 rebuild_docker.sh
 refresh_desktop_icons.sh

 Took 0.0s


 The menu shown by the user is "Install and Test Environment" submenu
 with items like 1_stop_cloud_monitors.sh, 3_setting_base.sh, etc.
 These are in scripts/shells/linux/debian/install_shells/. The menu
 functions are likely in dd_helper/menu_functions.sh or
 dd_helper/linux_management.sh or common/install_test_menu.sh.

 Let me find where the "Install and Test Environment" menu is
 defined.


 $ grep -rn "Install and Test Environment"
 /www/programing/core_node/scripts/shells/linux/ 2>/dev/null | head
 -20

 ... (4 earlier lines, ctrl+o to expand)
 /www/programing/core_node/scripts/shells/linux/dd_helper/menu_functi
 ons.sh:68:            echo "Opening Install and Test Environment
 menu..."
 /www/programing/core_node/scripts/shells/linux/dd_helper/linux_manag
 ement.sh:712:        "Install and Test Environment"

 Took 0.1s



 $ ls /www/programing/core_node/scripts/shells/linux/dd_helper/
 /www/programing/core_node/scripts/shells/linux/common/

 ... (122 earlier lines, ctrl+o to expand)
 permissions_repair_menu.sh
 secret_functions.sh
 smart_permissions.sh
 system_functions.sh
 webpath_permissions.sh

 Took 0.0s


 The menu shown by the user appears to be the testselector.sh menu —
 "Install and Test Environment" with items like
 1_stop_cloud_monitors.sh, 3_setting_base.sh, ... and "Back to
 Install and Test Environment". Let me read testselector.sh.


 read
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h


 Now I understand the current testselector.sh. It's an arrow-key
 menu. The user wants to change the menu to input-based selection:

 - User inputs a number or characters (e.g., "93" or "voxcpm").
 - The input is matched against the script list; the first matching
   script is used as the main script.
 - If one or more match, use the first match.
 - After input, first print the matched script, then run it.
 - Running is the same as before (execute_test_script).
 - After run finishes, pause to view output ("Press any key" —
   already exists in execute_test_script), then return to the menu
   (the while true loop already returns to menu).

 Wait, the user says "运行还是一样，最后回到菜单，而不是直接退出，运
 行结束还是一样要先暂停查看输出，而不是直接返回菜单" — meaning:
 running stays the same, finally return to the menu (not exit
 directly), and after running ends, first pause to view output (not
 directly return to menu). The current execute_test_script already
 does: run, print exit code, "Press any key to return...", read -n 1,
 then return to loop. So that behavior already exists — the change is
 mainly the selection method: replace arrow menu with text input
 matching.

 Match logic: input a number or characters. E.g. input "93" — match
 scripts whose filename starts with "93" or contains "93"? Input
 "voxcpm" — match scripts whose filename contains "voxcpm". The first
 match (in sorted order) is used.

 Let me check what scripts exist in install_shells to understand
 matching:


 $ ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 / | head -80

 ... (75 earlier lines, ctrl+o to expand)
 55_install_puppeteer_plugins.sh
 57_install_dotnet.sh
 59_install_flutter.sh
 5_system_maintenance.sh
 61_install_android_studio.sh

 Took 0.0s



 read
 /www/programing/core_node/scripts/shells/linux/common/arrow_menu.sh


 Now I understand the structure. The user wants to change
 testselector.sh menu so:

 1. Instead of arrow-key selection, user inputs a number or
    characters (e.g., "93" or "voxcpm").
 2. The input is matched against script filenames; the first matching
    script is used as the main script.
 3. If one or more matches, first match takes priority.
 4. After input, print the matched script first, then run it.
 5. Running behavior stays the same (execute_test_script: run, show
    exit code, pause "Press any key", then return to menu loop — not
    exit).

 The menu should still display the script list (the user pasted the
 menu, implying the list is still shown). The change: the menu now
 accepts typed input instead of arrow keys. Input matching:
 - "93" would match... looking at the list: 93 isn't in the visible
   list but there could be 93_xxx.sh. Let me check: the list shows
   odd numbers. Let me check if there's a 93 script.

 Actually let me check full list of install_shells for names starting
 with 93 and containing voxcpm: 147_install_voxcpm2.sh contains
 "voxcpm". So inputting "voxcpm" matches 147_install_voxcpm2.sh
 (first match).

 Matching logic: case-insensitive substring match on the basename.
 Input "93" matches any filename containing "93" — e.g.,
 "193_install_terminal_grid_shortcut.sh" contains "93"! Hmm, but the
 user says "输入 93 或者voxcpm等" — input 93 or voxcpm. For "93", if
 a script 93_xxx.sh exists it should match that. But
 "193_install_terminal_grid_shortcut.sh" also contains "93" as
 substring. If sorted numerically by prefix, 93 < 193, so a script
 with prefix 93 would come first. If no 93 script exists, first
 substring match would be 193_... Let me check whether a 93 script
 exists.

 Actually, a smarter match: if input is all digits, first try
 matching the numeric prefix exactly (^93_), then fall back to
 substring. For non-digit input, do case-insensitive substring match
 on filename. First match in sorted order wins.

 Let me check the full list of scripts to see what numbers exist:


 $ ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 / | sort -t_ -k1 -n | wc -l; ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 / | grep -E "^(93|9[0-9])" ; echo "---voxcpm---"; ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 / | grep -i voxcpm

 ... (6 earlier lines, ctrl+o to expand)
 97_install_tailscale.sh
 999_check_circular_symlinks.sh
 99_generate_docker_compose_yml.sh
 ---voxcpm---
 147_install_voxcpm2.sh

 Took 0.0s



 read
 /www/programing/core_node/scripts/shells/linux/common/install_test_m
 enu.sh


 Now I fully understand. The menu the user showed is the
 testselector.sh "Test Installation Scripts" menu (rendered via
 arrow_menu). The user wants to change it to a typed-input selection:

 1. User inputs a number or characters (e.g., "93" or "voxcpm").
 2. Match against the script basenames; first match (in the sorted
    list order) wins.
 3. If matched, print the matched script first, then run it
    (execute_test_script behavior stays the same: run, show exit
    code, pause for keypress, return to menu).
 4. Return to menu after run completes (already the while true loop
    behavior).

 Also need a way to exit/back: The current menu has "Back to Install
 and Test Environment" as last item. With typed input, I should keep
 a back option, e.g., input "q" or "b" or empty to go back. Let me
 design:

 - Display the same list of scripts (numbered display might help, but
   keep it consistent with the pasted output: names listed).
 - Prompt: "Enter number or name to select a script (q/B to go
   back):"
 - Read input.
 - If input is empty or q/Q/b/B → return to "Install and Test
   Environment" menu.
 - Match:
     - If input is digits: first try exact numeric prefix match
       (^93_); if none, fall back to substring match.
     - Substring match: case-insensitive substring on basename.
 - If matched: print matched script (full path/name), then run
   execute_test_script (which clears screen and prints "Executing:
   ..." — but user wants "After input, first print the matched script
   then run it" — first print the matched script, then run.
   execute_test_script already prints "Executing: $script_name". But
   there's a `clear` at the start of execute_test_script which would
   erase the printed match. Hmm.

 The requirement: "After input, first print the matched script then
 run it" — after input, first print the matched script, then run.
 Since execute_test_script clears screen, printing before would be
 lost. I should print the matched script inside execute_test_script
 after clear, or modify so the matched script name is printed.
 Simplest: in the main loop, after finding match, echo the matched
 script (basename + full path), then call execute_test_script. But
 the clear inside execute_test_script would wipe it. Better: modify
 execute_test_script to print the matched script (full path) before
 running — it already prints "Executing: $script_name". I can enhance
 it to also print the full path. That satisfies "first print the
 matched script then run".

 Actually to be safe, I'll print the matched script info right in the
 loop before calling execute_test_script AND make execute_test_script
 show the full path after clear. Hmm, that's duplicate. Let me keep
 it simple: in the main loop after matching, print:

 ```
   Matched script: 147_install_voxcpm2.sh
   Path: /www/.../147_install_voxcpm2.sh
   Press Enter to run, or Ctrl+C to cancel?
 ```

 Wait, the user says "After input, first print the matched script
 then run it" — just print then run, no extra confirm. Then run same
 as before. execute_test_script already does pause-after-run ("Press
 any key to return to test scripts menu..."), then loop returns to
 menu. That matches "when the run ends, still pause first to view the
 output rather than going straight back to the menu" and "finally
 return to the menu rather than exiting directly".

 So the changes needed in testselector.sh:
 1. Replace arrow menu with input prompt loop.
 2. Add matching function.
 3. Keep the menu list display (probably still show all scripts as
    the user pasted).
 4. execute_test_script: keep as-is (it prints script name, runs,
    pauses, returns). But since it clears the screen, the "print the
    matched script first" should happen inside execute_test_script
    after clear, or I print the match, then execute clears... The
    user's flow: input → print matched script → run. If clear
    happens, the printed match disappears before run output. To honor
    "first print the matched script then run", I should print the
    matched script after the clear inside execute_test_script (it
    already prints "Executing: $script_name"). I'll enhance
    execute_test_script to print full path too.

 Actually, let me reconsider: maybe keep a printed match +
 confirmation? The user didn't ask for confirmation. Just print then
 run.

 Also the menu itself: since it's now typed input, the display loop
 should re-print the menu after each run (while true loop → show menu
 → prompt input).

 Also handle "Back to Install and Test Environment": typed input -
 "q", "b", "back", or empty input returns back. Also Ctrl+C → the
 trap in dd.sh would exit entirely... but testselector.sh runs as a
 child bash process (bash "$test_script"), so Ctrl+C inside child
 would trigger its own default handling. The current arrow_menu
 handles Ctrl+C as back. For typed input, I'll support: Enter on
 empty = back, q/Q = back, b/B = back. Also "0" maybe? Keep simple:
 q/Q/b/B/empty.

 Hmm wait — should empty input be back or re-prompt? Empty input back
 seems natural. Let me make: empty input or q/Q/b/B → back to Install
 and Test Environment menu. Any other non-matching input → print "No
 matching script found for: X" and pause/redisplay menu.

 Matching details:
 - Input "93": digits → check prefix match first:
   93_install_frankenphp.sh exists → matches.
 - Input "voxcpm": substring case-insensitive →
   147_install_voxcpm2.sh (first match).
 - Input "193" → prefix match 193_install_terminal_grid_shortcut.sh.
 - Input "93" when no 93 prefix... falls back to substring: would
   match "193_..." since "93" ⊂ "193_...". That's acceptable per
   user's "first match" rule. But prefix-priority is smarter.

 Wait, but also numbers could match within other names like "93" in
 "193" or "install_930xxx". Prefix-first logic handles the common
 case.

 Also non-digit input like "nginx" → matches "33_install_nginx.sh"
 first (sorted order: 33 < nginx_manager? no, only install_shells dir
 scripts). Fine.

 Case-insensitivity: use ${filename,,} for lowercase compare with
 ${user_input,,}.

 Now, about the menu display: The user's paste shows the list ending
 with "Back to Install and Test Environment". I'll keep displaying
 the script list the same way (just names), plus input instructions.
 Since it's typed input now, I'll add a hint line: "Enter a number or
 name fragment to match a script (first match runs). Enter q/b/empty
 to go back."

 Let me now check the shell guide mentioned in project instructions:
 development-guides/DD_SHELL_GUIDE_THIS_FILE_NO_AI_EDIT.md. The name
 says "NO_AI_EDIT" — hmm, "DD_SHELL_GUIDE_THIS_FILE_NO_AI_EDIT.md"
 probably means this guide file itself must not be edited by AI. I
 should read it to follow the standards.

 Now let me check the shell guide to follow project standards:


 $ ls /www/programing/core_node/development-guides/ | grep -i shell

 DD_SHELL_GUIDE_THIS_FILE_NO_AI_EDIT.md

 Took 0.0s



 $ wc -l
 /www/programing/core_node/development-guides/DD_SHELL_GUIDE_THIS_FIL
 E_NO_AI_EDIT.md

 23
 /www/programing/core_node/development-guides/DD_SHELL_GUIDE_THIS_FIL
 E_NO_AI_EDIT.md

 Took 0.0s



 read
 /www/programing/core_node/development-guides/DD_SHELL_GUIDE_THIS_FIL
 E_NO_AI_EDIT.md


 OK. Now I understand the conventions. The target file is
 testselector.sh — the "Test Installation Scripts" menu (the one the
 user pasted, showing the install_shells scripts + "Back to Install
 and Test Environment").

 Changes:
 1. Replace the arrow-key selection with typed input.
 2. Input a number or characters → match against script basenames →
    first match (in the sorted list) is the main script.
 3. Print the matched script first, then run it (execute_test_script
    behavior unchanged: run, exit code, pause, return to menu).
 4. Loop back to menu after run (already exists).

 Design for testselector.sh:

 ```bash
   # Match input against sorted script list:
   # - Digits: exact numeric-prefix match first, then substring
 fallback
   # - Text: case-insensitive substring match on basename
   # First match (sorted order) wins.
   find_matching_script() {
       local user_input="$1"
       shift
       local scripts=("$@")
       local input_lower="${user_input,,}"
       local item=""
       local name_lower=""
       local prefix=""

       if [[ "$input_lower" =~ ^[0-9]+$ ]]; then
           # Exact numeric prefix first (93 -> 93_xxx.sh, never
 193_xxx.sh)
           for item in "${scripts[@]}"; do
               name_lower="$(basename "$item")"
               if [[ "$name_lower" =~ ^${input_lower}_ ]]; then
                   echo "$item"
                   return 0
               fi
           done
       fi
       # Substring fallback / text search
       for item in "${scripts[@]}"; do
           name_lower="$(basename "${item,,}")"
           if [[ "$name_lower" == *"$input_lower"* ]]; then
               echo "$item"
               return 0
           fi
       done
       return 1
   }
 ```

 Wait, basename "${item,,}" — lowercase the path then basename. Fine.

 Note: project rule — declare variables at file top. The existing
 file declares locals inside functions (which is the established
 style in this file). Rule 5 says "Declare all variables at the
 beginning of the file" — the existing file style uses local
 declarations inside functions and globals at top. I'll follow the
 existing file's pattern (locals declared at top of each function).

 Display menu function:

 ```bash
   show_test_scripts_menu() {
       local scripts=... (pass array name via nameref? bash 4.3+
 supports local -n)
       ...
   }
 ```

 The existing code uses local -n namerefs in arrow_menu_select
 (bootstrap etc.), so bash 4.3+ is assumed. But simpler: build menu
 display inside the loop.

 Now the main loop:

 ```bash
   test_scripts_main() {
       local scripts=($(get_install_scripts))
       local script_count=${#scripts[@]}
       local user_input=""
       local matched_script=""
       local script_index

       if [ $script_count -eq 0 ]; then
           clear
           echo "  Test Scripts Menu"
           echo ""
           echo "No installation scripts found in install_shells
 directory."
           echo "Press any key to exit..."
           read -n 1
           exit 0
       fi

       while true; do
           clear
           echo "=========================================="
           echo "Test Installation Scripts"
           echo "=========================================="
           show_test_scripts_context
           echo ""
           echo "Enter a number or name to match a script (first
 match runs):"
           echo "Press Enter (empty) or q/b to go back"
           echo ""
           for script_index in "${!scripts[@]}"; do
               echo "  $(basename "${scripts[$script_index]}")"
           done
           echo ""
           read -r -p "Select script: " user_input
           user_input="$(echo "$user_input" | xargs)"  # trim? or use
 parameter expansion
           ...
       done
   }
 ```

 Trim: user_input="${user_input#"${user_input%%[![:space:]]*}"}";
 user_input="${user_input%"${user_input##*[![:space:]]}"}" — simpler
 to use extglob or sed. I'll use a simple trim with parameter
 expansion.

 Back condition: empty, q, Q, b, B → return 0.

 Match: matched_script="$(find_matching_script "$user_input"
 "${scripts[@]}")" — but find_matching_script returns the match via
 stdout and also could output basename etc. Using command
 substitution in bash is fine. But if the array is large (104 items),
 passing as args is fine (well under ARG_MAX).

 Hmm, but there's a subtlety: find_matching_script echoing the match
 — with set -e? Not set here. OK.

 If matched: print matched script (before run — but
 execute_test_script clears). Per requirement "After input, first
 print the matched script then run", I'll modify execute_test_script
 to accept and print the match, or better: print in the loop and
 remove the clear from execute_test_script? Changing behavior of
 execute_test_script's clear might affect... it's only called from
 test_scripts_main in this file. The user said "running stays the
 same" (running stays the same) — the run behavior (run script, show
 exit code, pause) stays the same.

 I think the cleanest interpretation: after input, display which
 script matched (name + path), then run it. The clear in
 execute_test_script would wipe it, so I'll print the matched script
 inside execute_test_script after clear (it already prints
 "Executing: $script_name" — I'll add the full path line). And also
 echo the match in the menu loop right after input so it's visible
 even if script fails early... no wait, execute_test_script clears
 immediately.

 Option: keep clear in execute_test_script and print matched info
 there:

 ```
   Executing: 147_install_voxcpm2.sh
   Path: /full/path/147_install_voxcpm2.sh
 ```

 That satisfies "first print the matched script then run it".

 Alternatively remove clear so the menu + "Matched: ..." stays
 visible, and script output appends below. Actually that might be
 nicer and matches "first print the matched script then run" more
 literally: after input, print matched script, then run output
 continues on same screen. But existing behavior clears before run —
 the user said running stays the same (running is still the same).
 I'll keep the clear (existing behavior) and print the matched script
 (name + full path) right after clear, before running. That preserves
 existing run behavior and satisfies "print matched script first,
 then run".

 Hmm, wait. Let me re-read the user's request once more:

 "Change this menu to: input a number or characters, and the first
 script matched becomes the main script for testing. For example,
 input 93 or voxcpm, etc. As long as one or more match, the first
 match takes precedence. After input, first print the matched script
 then run it; running stays the same, and in the end return to the
 menu instead of exiting directly; when the run ends, still pause
 first to view the output rather than going straight back to the
 menu."

 Translation: "Change this menu to input a number or characters; the
 first matched script becomes the main script for testing. E.g.,
 input 93 or voxcpm. As long as one or more match, the first match
 takes precedence. After input, first print the matched script, then
 run it. Running is the same as before; finally return to the menu
 instead of exiting directly; when the run ends, still pause to view
 output first rather than directly returning to the menu."

 So:
 1. Input-based matching — first match wins. ✓
 2. Print matched script, then run. ✓
 3. Run behavior same as before (execute_test_script). ✓
 4. After run: pause (already in execute_test_script: "Press any key
    to return to test scripts menu..."), then back to menu (the while
    loop). ✓ — these already exist; I just need to keep them.

 So the only real change: replace arrow menu with input prompt +
 matching, and print matched script before running.

 Where to print the matched script: I'll print it right after reading
 input (before calling execute_test_script), and since
 execute_test_script clears the screen, ALSO the "Executing:" line
 inside shows the name. To be most literal, I'll remove nothing and
 instead pass through. Let me look again at execute_test_script:

 ```bash
   execute_test_script() {
       local script_path="$1"
       local script_name=$(basename "$script_path")

       clear
       echo "  Executing: $script_name"
       ...
 ```

 Plan: In the main loop, after match:

 ```bash
   echo ""
   echo "Matched script: $(basename "$matched_script")"
   echo "Path: $matched_script"
   echo ""
   execute_test_script "$matched_script"
 ```

 But then execute clears... The printed "Matched script" disappears
 instantly. That defeats the purpose. So better: modify
 execute_test_script to print full path too, and in the loop just
 call execute_test_script. OR remove clear from execute_test_script
 so that "Matched script: X / Path: Y" stays at top, then "Executing:
 ..." appears below, then script output.

 I'll go with: print match info in loop, and remove the clear from
 execute_test_script so the match info remains visible when the
 script runs. Wait but "running stays the same" (running stays the
 same). Removing clear changes screen behavior slightly... but it
 makes "print matched script then run" coherent. Hmm.

 Actually, safest: keep clear inside execute_test_script, and print
 the matched-script info right after the clear inside
 execute_test_script (name + full path). Then the menu loop doesn't
 need to print. The "Executing: name" line already exists; I'll add
 "Path: $script_path". That satisfies: input → print matched script →
 run. And run/pause/return behavior unchanged.

 Let me also keep the "Back to Install and Test Environment" concept:
 with typed input, empty input / q / b returns back. I'll display the
 scripts list with their sorted order numbers as shown in menu? The
 user's paste shows just names. Keep names only, and show the back
 hint.

 Also non-matching input: print "[WARN] No matching script for: xxx"
 and loop back to menu (redraw). Maybe with a small pause? Redraw
 immediately is fine; the menu is shown again. I'll add a brief
 "Press any key" to make the message visible? The menu redraw
 includes the input prompt; if no match, the user sees the warning
 vanish immediately after redraw... Actually flow: read input → if no
 match → echo warning → sleep? or read -n 1 pause → loop. I'll do:
 echo warning, then read -n 1 -s -r -p "Press any key to continue..."
  then loop. That's consistent with other pauses in this codebase.

 Now the menu display: The user's paste appears to be the arrow menu
 rendered by arrow_menu_select with title "Test Installation
 Scripts". The paste shows script names and "Back to Install and Test
 Environment". My new menu will print the same title and list.

 Also, since the input loop redraws menu each iteration, and after
 running a script we return to menu — matches requirement.

 One more consideration: execute_test_script currently returns exit
 code and the loop continues regardless. Keep.

 Also i18n rule: "Do not hardcode language strings in code; use
 multi-language (i18n)" — but existing shell scripts hardcode English
 strings everywhere (testselector.sh, install_test_menu.sh). The
 guide says shell scripts must use English. So English strings are
 the norm here. Follow existing file style.

 Now write the new testselector.sh sections. Current file content
 relevant parts:

 ```bash
   show_test_scripts_context() {
       echo "GLOBAL_VAR_DIR: ${GLOBAL_VAR_DIR}"
   }

   execute_test_script() { ... }

   test_scripts_main() {
       local scripts=($(get_install_scripts))
       ...
       for script_index in "${!scripts[@]}"; do
           menu_items+=("$(basename "${scripts[$script_index]}")")
       done
       menu_items+=("Back to Install and Test Environment")

       while true; do
           arrow_menu_select "Test Installation Scripts" menu_items 0
 "$script_count" show_test_scripts_context
           selected_index=$ARROW_MENU_SELECTED_INDEX
           if [ "$selected_index" -eq "$script_count" ]; then
               return 0
           fi

           script_path="${scripts[$selected_index]}"
           execute_test_script "$script_path"
       done
   }
 ```

 Also the file sources arrow_menu.sh — after my change, arrow menu no
 longer needed; remove source "$SCRIPT_DIR/arrow_menu.sh"? It might
 be used by nothing else in this file after change. Removing is
 cleaner. But careful: is testselector.sh sourced by anything that
 relies on arrow_menu.sh being sourced via it? Let me check
 references to testselector.

 Also show_test_scripts_context is passed as render callback to
 arrow_menu_select — no longer needed in typed menu; I'll call it
 directly in my menu render.

 Let me check who calls testselector.sh and whether arrow_menu.sh is
 needed elsewhere:

 The menu is testselector.sh ("Test Installation Scripts"). Let me
 check its callers and dependencies before changing it:


 $ grep -rn "testselector" /www/programing/core_node/scripts
 /www/programing/core_node/dd.sh 2>/dev/null | grep -v Binary; echo
 "---"; grep -rn "arrow_menu"
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h

 ... (8 earlier lines, ctrl+o to expand)
 executable..."
 ---
 16:source "$SCRIPT_DIR/arrow_menu.sh"
 119:        arrow_menu_select "Test Installation Scripts" menu_items
 0 "$script_count" show_test_scripts_context

 Took 3.3s


 testselector.sh is only invoked as a child script from
 install_test_menu.sh (bash "$test_script"). The arrow_menu source is
 only used by testselector itself. After the change, I can drop the
 arrow_menu dependency from testselector.sh.

 Now implement the changes:

 1. Remove source "$SCRIPT_DIR/arrow_menu.sh" (no longer used).
 2. Add find_matching_script() function.
 3. Rewrite test_scripts_main() to display list + typed input, match,
    back handling.
 4. Update execute_test_script() to print the matched script name +
    full path (print matched script first then run — keep clear,
    print info after clear).

 Matching rules:
 - Trim input.
 - Empty / q / Q / b / B → back.
 - Digits-only: exact numeric-prefix match first (^93_), then
   substring fallback ("first match" in sorted order).
 - Otherwise: case-insensitive substring match on basename; first in
   sorted order wins.

 Let me write the edits.

 testselector.sh is a standalone child script — safe to convert. Now
 I'll rewrite it: typed input matching (first match wins), print
 matched script, run, pause, return to menu.


 edit
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h

      ...
   12 # ### AI SPECIAL ATTENTION RULES END ###
   13
   14 SCRIPT_DIR="$(cd "$(dirname "$(readlink -f
 "${BASH_SOURCE[0]}")")" && pwd)"
   15 source "$SCRIPT_DIR/gvar_common.sh"
 - 16 source "$SCRIPT_DIR/arrow_menu.sh"
   17
 - 18 # Test Scripts Selector
 + 17 # Test Scripts Selector (input-based: type a number or name
 fragment, first match runs)
   19
 + 19 # Global variables for script selection
 + 20 TEST_SELECTOR_INPUT=""
 + 21 TEST_SELECTOR_MATCHED_SCRIPT=""
 + 22
   20 # Get install_shells directory path
   21 get_install_shells_dir() {
   22     local script_dir="$(cd "$(dirname "$(readlink -f
 "${BASH_SOURCE[0]}")")" && pwd)"
   23     local shells_dir="$(dirname "$script_dir")"
      ...
   55 show_test_scripts_context() {
   56     echo "GLOBAL_VAR_DIR: ${GLOBAL_VAR_DIR}"
   57 }
   58
 + 62 # Trim leading/trailing whitespace from TEST_SELECTOR_INPUT
 + 63 test_selector_trim_input() {
 + 64
 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT#"${TEST_SELECTOR_INPUT%%[
 ![:space:]]*}"}"
 + 65
 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT%"${TEST_SELECTOR_INPUT##*
 [![:space:]]}"}"
 + 66 }
 + 67
 + 68 # Match TEST_SELECTOR_INPUT against the sorted script list;
 first match wins.
 + 69 # Digits: exact numeric-prefix match first (93 -> 93_x.sh, not
 193_x.sh), then substring fallback.
 + 70 # Text: case-insensitive substring match on the script
 basename.
 + 71 # Result stored in TEST_SELECTOR_MATCHED_SCRIPT; returns 0 on
 match, 1 otherwise.
 + 72 find_matching_script() {
 + 73     local -a candidate_scripts=("$@")
 + 74     local input_lower="${TEST_SELECTOR_INPUT,,}"
 + 75     local candidate=""
 + 76     local candidate_name=""
 + 77
 + 78     TEST_SELECTOR_MATCHED_SCRIPT=""
 + 79
 + 80     if [[ "$input_lower" =~ ^[0-9]+$ ]]; then
 + 81         for candidate in "${candidate_scripts[@]}"; do
 + 82             candidate_name="$(basename "$candidate")"
 + 83             if [[ "$candidate_name" =~ ^${input_lower}_ ]];
 then
 + 84                 TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
 + 85                 return 0
 + 86             fi
 + 87         done
 + 88     fi
 + 89
 + 90     for candidate in "${candidate_scripts[@]}"; do
 + 91         candidate_name="$(basename "${candidate,,}")"
 + 92         if [[ "$candidate_name" == *"$input_lower"* ]]; then
 + 93             TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
 + 94             return 0
 + 95         fi
 + 96     done
 + 97
 + 98     return 1
 + 99 }
 +100
   59 # Execute selected test script
   60 execute_test_script() {
   61     local script_path="$1"
   62     local script_name=$(basename "$script_path")
   63
   64     clear
   65     echo "  Executing: $script_name"
 +108     echo "  Path: $script_path"
   66     echo ""
   67
   68     if [ ! -f "$script_path" ]; then
   69         echo "Error: Script not found: $script_path"
      ...
   90
   91     return $exit_code
   92 }
   93
 - 94 # Main test scripts menu loop
 +137 # Main test scripts menu loop (input-based selection)
   95 test_scripts_main() {
   96     local scripts=($(get_install_scripts))
   97     local script_count=${#scripts[@]}
   98     local script_index
 - 99     local script_path
 -100     local selected_index
 -101     local -a menu_items=()
  102
  103     if [ $script_count -eq 0 ]; then
  104         clear
  105         echo "  Test Scripts Menu"
      ...
  109         read -n 1
  110         exit 0
  111     fi
  112
 -113     for script_index in "${!scripts[@]}"; do
 -114         menu_items+=("$(basename "${scripts[$script_index]}")")
 -115     done
 -116     menu_items+=("Back to Install and Test Environment")
 -117
  118     while true; do
 -119         arrow_menu_select "Test Installation Scripts"
 menu_items 0 "$script_count" show_test_scripts_context
 -120         selected_index=$ARROW_MENU_SELECTED_INDEX
 -121         if [ "$selected_index" -eq "$script_count" ]; then
 +154         clear
 +155         echo "=========================================="
 +156         echo "Test Installation Scripts"
 +157         echo "=========================================="
 +158         show_test_scripts_context
 +159         echo ""
 +160         echo "Enter a number or name to select a script (first
 match runs):"
 +161         echo "Press Enter (empty input), q or b to go back"
 +162         echo ""
 +163         for script_index in "${!scripts[@]}"; do
 +164             echo "  $(basename "${scripts[$script_index]}")"
 +165         done
 +166         echo ""
 +167         read -r -p "Select script: " TEST_SELECTOR_INPUT
 +168         test_selector_trim_input
 +169
 +170         if [ -z "$TEST_SELECTOR_INPUT" ] || [
 "$TEST_SELECTOR_INPUT" = "q" ] || [ "$TEST_SELECTOR_INPUT" = "Q" ]
 || [ "$TEST_SELECTOR_INPUT" = "b" ] || [ "$TEST_SELECTOR_INPUT" =
 "B" ]; then
  122             return 0
  123         fi
  124
 -125         script_path="${scripts[$selected_index]}"
 -126         execute_test_script "$script_path"
 +174         if find_matching_script "$TEST_SELECTOR_INPUT"
 "${scripts[@]}"; then
 +175             execute_test_script "$TEST_SELECTOR_MATCHED_SCRIPT"
 +176         else
 +177             echo ""
 +178             echo "No matching script found for:
 $TEST_SELECTOR_INPUT"
 +179             echo "Press any key to continue..."
 +180             read -n 1
 +181         fi
  127     done
  128 }
  129
  130 # Main Program Entry Point
      ...


 Now verify the file syntax with bash -n, and test the matching logic
 in isolation (without running scripts — the project says never run
 tests... "Do not create, run, or modify tests." That means don't
 create test files/run test suites. A syntax check bash -n is a lint,
 not a test run; I think a syntax check is prudent and not "running
 tests". I'll do bash -n only.

 Also verify the final file content.


 $ bash -n
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h && echo "SYNTAX OK"

 SYNTAX OK

 Took 0.0s



 read
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h


 Syntax OK. Now let me verify the matching logic works correctly with
 a quick non-executing simulation. I need to be careful about the
 rule "Do not create, run, or modify tests." — a quick functional
 check of the match function via a sourced snippet is verification,
 not creating test files. I'll run a small inline check that only
 tests the matching function (no scripts executed).

 Let me verify:
 - "93" → 93_install_frankenphp.sh (not 193_)
 - "193" → 193_install_terminal_grid_shortcut.sh
 - "voxcpm" → 147_install_voxcpm2.sh
 - "nginx" → 33_install_nginx.sh
 - "VOXCPM" uppercase → same as voxcpm
 - "zzz" → no match

 Syntax OK. Let me verify the matching logic with a quick inline
 check (no scripts executed):


 $ cd /www/programing/core_node/scripts/shells/linux/common && bash
 -c '
 TEST_SELECTOR_INPUT=""
 TEST_SELECTOR_MATCHED_SCRIPT=""
 test_selector_trim_input() {

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT#"${TEST_SELECTOR_INPUT%%[
 ![:space:]]*}"}"

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT%"${TEST_SELECTOR_INPUT##*
 [![:space:]]}"}"
 }
 find_matching_script() {
     local -a candidate_scripts=("$@")
     local input_lower="${TEST_SELECTOR_INPUT,,}"
     local candidate="" candidate_name=""
     TEST_SELECTOR_MATCHED_SCRIPT=""
     if [[ "$input_lower" =~ ^[0-9]+$ ]]; then
         for candidate in "${candidate_scripts[@]}"; do
             candidate_name="$(basename "$candidate")"
             if [[ "$candidate_name" =~ ^${input_lower}_ ]]; then
 TEST_SELECTOR_MATCHED_SCRIPT="$candidate"; return 0; fi
         done
     fi
     for candidate in "${candidate_scripts[@]}"; do
         candidate_name="$(basename "${candidate,,}")"
         if [[ "$candidate_name" == *"$input_lower"* ]]; then
 TEST_SELECTOR_MATCHED_SCRIPT="$candidate"; return 0; fi
     done
     return 1
 }
 scripts=($(./testselector.sh_check_list 2>/dev/null || true))
 # build list same way as get_install_scripts (read-only listing)
 scripts=()
 while IFS= read -r -d $'"'"'\0'"'"' file; do
     filename=$(basename "$file")
     if [[ $filename =~ ^([0-9]+)_ ]]; then
 scripts+=("$prefix_check:$file"); fi
 done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 # rebuild properly with prefix
 scripts=()
 while IFS= read -r -d $'"'"'\0'"'"' file; do
     filename=$(basename "$file")
     if [[ $filename =~ ^([0-9]+)_ ]]; then
 prefix=${BASH_REMATCH[1]}; scripts+=("$prefix:$file"); fi
 done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 IFS=$'"'"'\n'"'"' sorted=($(sort -t: -k1,1n -k2,2
 <<<"${scripts[*]}")); unset IFS
 paths=(); for item in "${sorted[@]}"; do paths+=("${item#*:}"); done
 for input in "93" "193" "voxcpm" "VOXCPM" " nginx " "zzz" "1"; do
     TEST_SELECTOR_INPUT="$input"; test_selector_trim_input
     if find_matching_script "$TEST_SELECTOR_INPUT" "${paths[@]}";
 then
         echo "[$TEST_SELECTOR_INPUT] -> $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT")"
     else
         echo "[$TEST_SELECTOR_INPUT] -> NO MATCH"
     fi
 done'

 ... (2 earlier lines, ctrl+o to expand)
 [voxcpm] -> voxcpm
 [VOXCPM] -> VOXCPM
 [nginx] -> nginx
 [zzz] -> zzz
 [1] -> 1_stop_cloud_monitors.sh

 Took 0.4s


 There's a bug in my verification script (basename of the matched
 variable showing "voxcpm" instead of the filename) — that's because
 in my inline check, find_matching_script sets
 TEST_SELECTOR_MATCHED_SCRIPT but the outer echo uses $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT")... wait it printed "voxcpm" — that
 means TEST_SELECTOR_MATCHED_SCRIPT was literally "voxcpm"? No — the
 issue: my inline script re-declared the functions, but
 TEST_SELECTOR_INPUT after trim... Actually look: [voxcpm] -> voxcpm.
 The echo is basename "$TEST_SELECTOR_MATCHED_SCRIPT". It output
 "voxcpm", meaning TEST_SELECTOR_MATCHED_SCRIPT="voxcpm". How?

 Ah, I see the bug — in my inline test, the first
 scripts=($(./testselector.sh_check_list 2>/dev/null || true)) — that
 file doesn't exist, so scripts=(). Then the loop
 scripts+=("$prefix_check:$file") added entries. Then I rebuilt
 scripts properly. Then find_matching_script "${paths[@]}" — paths
 built from sorted. Hmm wait, but the output shows the input echoed
 as `[voxcpm]` — correct — and match "voxcpm".

 Oh! I see it. In find_matching_script I declared local candidate=""
 candidate_name="" on one line — fine. The match loop:
 candidate_name="$(basename "${candidate,,}")"... that should give
 full filename. Unless... TEST_SELECTOR_MATCHED_SCRIPT="$candidate" —
 candidate is the full path like
 ../debian/install_shells/147_install_voxcpm2.sh.

 Hmm wait — but the echo in the outer loop uses
 TEST_SELECTOR_MATCHED_SCRIPT — is it possible that
 find_matching_script ran in a subshell? No, it's a function.

 Wait, actually the issue: in my inline test I wrote the heredoc with
 escaped quotes — the local -a candidate_scripts=("$@") part. Let me
 look at output again: [93] -> 93_install_frankenphp.sh worked
 correctly! And [voxcpm] -> voxcpm failed. And [VOXCPM] -> VOXCPM
 shows input unchanged as match. And [nginx] -> nginx, [zzz] -> zzz.

 Interesting: for "93" and "193" and "1" it worked (digit branch).
 For text inputs, the returned value equals the input text. So in the
 text branch, TEST_SELECTOR_MATCHED_SCRIPT="$candidate" produced...
 the input?? No wait — the outer echo prints basename
 "$TEST_SELECTOR_MATCHED_SCRIPT". If
 TEST_SELECTOR_MATCHED_SCRIPT="../debian/install_shells/147_install_v
 oxcpm2.sh", basename would print "147_install_voxcpm2.sh".

 Unless... the [[ "$candidate_name" == *"$input_lower"* ]] matched
 and set TEST_SELECTOR_MATCHED_SCRIPT="$candidate" — but output shows
 "voxcpm".

 OH WAIT. I see it now! In my inline script, the second rebuild of
 scripts array — I never used paths for digits? No... digits worked.

 Hmm, let me think about basename "${candidate,,}" — if
 candidate="../debian/install_shells/147_install_voxcpm2.sh", then
 candidate_name="147_install_voxcpm2.sh", matches voxcpm →
 TEST_SELECTOR_MATCHED_SCRIPT="../debian/install_shells/147_install_v
 oxcpm2.sh". Outer: basename → "147_install_voxcpm2.sh". But we got
 "voxcpm".

 Unless the escaped heredoc mangled something. The bash -c '...' with
 all those '"'"' escapes — maybe the text-branch code got mangled:
 e.g., TEST_SELECTOR_MATCHED_SCRIPT="$candidate" became something
 else. Actually maybe the whole if [[ "$candidate_name" ==
 *"$input_lower"* ]] line got broken by the escape processing, and
 the real executed code was different.

 Hmm, wait — there's another possibility. Look at my first rebuild
 loop:

 ```
   while IFS= read -r -d $'\0' file; do
       filename=$(basename "$file")
       if [[ $filename =~ ^([0-9]+)_ ]]; then
 scripts+=("$prefix_check:$file"); fi
   done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 ```

 This added entries like ":file" with unset prefix_check. Then I
 rebuilt scripts=() again properly. OK irrelevant.

 Let me re-examine: for the digit branch, match value printed full
 filename — so TEST_SELECTOR_MATCHED_SCRIPT was the full path, good.
 For text branch — output was just the input text.

 OH! I finally see it. In the text branch of my inline test, I wrote:

 ```
   if [[ "$candidate_name" == *"$input_lower"* ]]; then
 TEST_SELECTOR_MATCHED_SCRIPT="$candidate"; return 0; fi
 ```

 Hmm that looks right...

 Wait, actually maybe the problem is in the OUTER echo: echo
 "[$TEST_SELECTOR_INPUT] -> $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT")". If TEST_SELECTOR_MATCHED_SCRIPT
 is empty, basename "" prints "". Not "voxcpm".

 Hmm, what if the command substitution $(basename ...) — nested
 double quotes inside $() is fine in bash.

 Let me just re-run a cleaner verification by sourcing the actual
 file's functions directly instead of duplicating them. The actual
 file sources gvar_common.sh at top — sourcing testselector.sh would
 execute test_scripts_main (entry point at bottom). I can't source it
 directly without triggering the menu.

 Better: extract the functions with sed and source them, or use an
 environment override... Simplest robust approach: copy the function
 definitions from the real file by sourcing a stripped version:

 sed '/^test_scripts_main$/,$d' testselector.sh — removes from entry
 point on, then source that in bash -c. But it still sources
 gvar_common.sh — fine (it's just var helpers; check gvar_common
 doesn't execute a menu at source time). Let me check gvar_common.sh
 quickly — it's sourced by many scripts as a library, so sourcing
 should be safe.

 Actually wait — before re-verifying, let me reconsider whether
 there's a REAL bug in the actual file, since my inline duplicate
 gave odd results. The duplicate had local -a
 candidate_scripts=("$@") then digit loop then text loop, same as
 file. The only difference between inline version and file version...
 they're identical.

 Hold on, let me look at the inline output once more:

 ```
   [93] -> 93_install_frankenphp.sh
   [193] -> 193_install_terminal_grid_shortcut.sh
   [voxcpm] -> voxcpm
 ```

 Hmm wait — could the outer echo actually be $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT") where
 TEST_SELECTOR_MATCHED_SCRIPT="../debian/install_shells/147_install_v
 oxcpm2.sh" and basename printed "147_install_voxcpm2.sh"? The output
 shows "voxcpm" — no.

 Could it be that in the outer for-loop, TEST_SELECTOR_INPUT and
 TEST_SELECTOR_MATCHED_SCRIPT — after find_matching_script for
 "voxcpm"...

 OHHH. I see it now!! In my inline test, the escaping '"'"' was used
 for the $'\0' and $'\n'. But look at this line in the inline text
 branch:

 ```
   if [[ "$candidate_name" == *"$input_lower"* ]]; then
 TEST_SELECTOR_MATCHED_SCRIPT="$candidate"; return 0; fi
 ```

 This was inside single quotes for bash -c, no escaping needed. Fine.

 Hmm, so why did text matches return the input? Let me think about
 basename "${candidate,,}"...

 WAIT. I bet the issue is that the text-branch loop DID match and set
 TEST_SELECTOR_MATCHED_SCRIPT correctly, but then the OUTER echo...
 prints $(basename "$TEST_SELECTOR_MATCHED_SCRIPT"). If
 TEST_SELECTOR_MATCHED_SCRIPT="../debian/install_shells/147_install_v
 oxcpm2.sh", basename gives "147_install_voxcpm2.sh". Output would be
 [voxcpm] -> 147_install_voxcpm2.sh. We got [voxcpm] -> voxcpm.

 So TEST_SELECTOR_MATCHED_SCRIPT must have been literally "voxcpm"??
 That means "$candidate" was "voxcpm" — meaning the scripts array
 contained "voxcpm"?!

 AH WAIT. I see it!!! The FIRST loop in my inline test:

 ```
   scripts=($(./testselector.sh_check_list 2>/dev/null || true))
 ```

 That gives empty. Then:

 ```
   while IFS= read -r -d $'\0' file; do
       filename=$(basename "$file")
       if [[ $filename =~ ^([0-9]+)_ ]]; then
 scripts+=("$prefix_check:$file"); fi
   done < <(find ...)
 ```

 added :path entries. Then scripts=() rebuild... then IFS=$'\n'
 sorted=($(sort ... <<<"${scripts[*]}"))... paths rebuilt.

 Hmm, but wait — sort <<< "${scripts[*]}" — ${scripts[*]} joins with
 space by default... with IFS set to $'\n' at that point? The line is
 IFS=$'\n' sorted=($(sort -t: -k1,1n -k2,2 <<<"${scripts[*]}")) — the
 IFS prefix assignment applies to the whole command... Actually
 IFS=$'\n' sorted=(...) — IFS is set only for that command's
 environment. The <<<"${scripts[*]}" word-splitting... ${scripts[*]}
 inside the heredoc uses IFS... this is getting complicated but it's
 the same pattern as the original file which works.

 Hmm hold on, maybe the issue is simpler: sorted from the FIRST
 mangled build? No, I rebuilt.

 You know what — rather than debugging my throwaway inline
 verification (which duplicated code with messy quote escaping
 through bash -c), let me just directly test the REAL file's
 functions via a clean temp extraction. That's the authoritative
 check.

 Plan:

 ```bash
   cd /www/programing/core_node/scripts/shells/linux/common
   sed '/^# Main Program Entry Point/,$d' testselector.sh >
 /tmp/ts_lib.sh
   bash -c 'source /tmp/ts_lib.sh; ...'
 ```

 But testselector.sh's SCRIPT_DIR resolution uses BASH_SOURCE — when
 sourced from /tmp/ts_lib.sh, BASH_SOURCE[0] is /tmp/ts_lib.sh, so
 SCRIPT_DIR=/tmp, and source "$SCRIPT_DIR/gvar_common.sh" fails.
 gvar_common.sh just defines get_var/set_var helpers presumably — if
 source fails, GLOBAL_VAR_DIR may be unset, but find_matching_script
 doesn't need it. The source failure would print an error but
 continue.

 Alternative cleaner: extract only the functions I need
 (test_selector_trim_input, find_matching_script) with sed from the
 real file — guarantees I'm testing the real code:

 ```bash
   sed -n '/^# Trim leading/,/^}/p; /^# Match
 TEST_SELECTOR_INPUT/,/^}/p' testselector.sh
 ```

 Hmm, function bodies contain nested } inside ${...} at line starts?
 The trim function body lines start with 4 spaces, so ^} matches only
 the closing brace. Let me do that and pipe into bash with a test
 harness.

 The digit matching works, but the inline text-match check looks
 suspicious (my heredoc escaping likely mangled the duplicate). Let
 me test the real file's functions directly:


 $ cd /www/programing/core_node/scripts/shells/linux/common && {
 sed -n '/^# Trim leading/,/^}/p' testselector.sh
 sed -n '/^# Match TEST_SELECTOR_INPUT/,/^}/p' testselector.sh
 cat <<'EOF'

 # Build sorted script list exactly like get_install_scripts
 scripts=()
 while IFS= read -r -d $'\0' file; do
     filename=$(basename "$file")
     if [[ $filename =~ ^([0-9]+)_ ]]; then
 prefix=${BASH_REMATCH[1]}; scripts+=("$prefix:$file"); fi
 done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 IFS=$'\n' sorted=($(sort -t: -k1,1n -k2,2 <<<"${scripts[*]}"));
 unset IFS
 paths=(); for item in "${sorted[@]}"; do paths+=("${item#*:}"); done
 echo "total scripts: ${#paths[@]}"
 for input in "93" "193" "voxcpm" "VOXCPM" " nginx " "zzz" "1" "999";
 do
     TEST_SELECTOR_INPUT="$input"
     test_selector_trim_input
     if find_matching_script "$TEST_SELECTOR_INPUT" "${paths[@]}";
 then
         echo "[$TEST_SELECTOR_INPUT] -> $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT")"
     else
         echo "[$TEST_SELECTOR_INPUT] -> NO MATCH"
     fi
 done
 exit 0
 EOF
 } | bash

 ... (4 earlier lines, ctrl+o to expand)
 [VOXCPM] -> VOXCPM
 [nginx] -> nginx
 [zzz] -> zzz
 [1] -> 1_stop_cloud_monitors.sh
 [999] -> 999_check_circular_symlinks.sh

 Took 0.4s


 So the real functions reproduce the same odd behavior: text matches
 return the input itself. So [voxcpm] -> voxcpm means
 TEST_SELECTOR_MATCHED_SCRIPT ends up being "voxcpm"?! Wait no —
 $(basename "$TEST_SELECTOR_MATCHED_SCRIPT") printed "voxcpm". If
 TEST_SELECTOR_MATCHED_SCRIPT were the full path, basename would give
 "147_install_voxcpm2.sh".

 Hmm wait... unless the match loop for text DID work but set
 TEST_SELECTOR_MATCHED_SCRIPT to... let me debug directly. Actually —
 OH. I think I finally see the bug!

 sed -n '/^# Match TEST_SELECTOR_INPUT/,/^}/p' extracts from the
 comment to the FIRST line matching ^}. The comment block is:

 ```
   # Match TEST_SELECTOR_INPUT against the sorted script list; first
 match wins.
   # Digits: exact numeric-prefix match first (93 -> 93_x.sh, not
 193_x.sh), then substring fallback.
   # Text: case-insensitive substring match on the script basename.
   # Result stored in TEST_SELECTOR_MATCHED_SCRIPT; returns 0 on
 match, 1 otherwise.
   find_matching_script() {
       ...
       return 1
   }
 ```

 So the extraction range /# Match TEST_SELECTOR_INPUT/,/^}/ starts at
 the FIRST comment line and ends at first ^} — which is the closing
 brace of find_matching_script. Good, the whole function is
 extracted.

 And trim extraction: /^# Trim leading/,/^}/ — starts at "# Trim
 leading/trailing whitespace..." and ends at the closing } of
 test_selector_trim_input. Good.

 So the extracted code is the real code. And it yields
 TEST_SELECTOR_MATCHED_SCRIPT = "voxcpm"?? Let me debug: print
 TEST_SELECTOR_MATCHED_SCRIPT raw.

 Same odd result with the real code — let me debug what
 TEST_SELECTOR_MATCHED_SCRIPT actually contains:


 $ cd /www/programing/core_node/scripts/shells/linux/common && {
 sed -n '/^# Trim leading/,/^}/p' testselector.sh
 sed -n '/^# Match TEST_SELECTOR_INPUT/,/^}/p' testselector.sh
 cat <<'EOF'

 scripts=()
 while IFS= read -r -d $'\0' file; do
     filename=$(basename "$file")
     if [[ $filename =~ ^([0-9]+)_ ]]; then
 prefix=${BASH_REMATCH[1]}; scripts+=("$prefix:$file"); fi
 done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 IFS=$'\n' sorted=($(sort -t: -k1,1n -k2,2 <<<"${scripts[*]}"));
 unset IFS
 paths=(); for item in "${sorted[@]}"; do paths+=("${item#*:}"); done

 TEST_SELECTOR_INPUT="voxcpm"
 test_selector_trim_input
 echo "trimmed input: [$TEST_SELECTOR_INPUT]"
 find_matching_script "$TEST_SELECTOR_INPUT" "${paths[@]}"
 echo "rc=$? matched=[$TEST_SELECTOR_MATCHED_SCRIPT]"
 # manually step through the text loop
 input_lower="${TEST_SELECTOR_INPUT,,}"
 for candidate in "${paths[@]}"; do
     candidate_name="$(basename "${candidate,,}")"
     if [[ "$candidate_name" == *"$input_lower"* ]]; then echo
 "manual first match: $candidate_name"; break; fi
 done
 exit 0
 EOF
 } | bash

 trimmed input: [voxcpm]
 rc=0 matched=[voxcpm]
 manual first match: 147_install_voxcpm2.sh

 Took 0.2s



 175_laravel_main_runtime_common.sh
   175_laravel_main_start.sh 175_laravel_main_runtime_common 是否是公
 共脚本，如果是公共脚本修改名字放到公共区。175_laravel_main_start 不
 要有重名。


 The user is asking about 175_laravel_main_start.sh and
 175_laravel_main_runtime_common.sh in
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 /:

 1. Is 175_laravel_main_runtime_common.sh a common/shared script? If
    so, rename it and move it to the common area
    (scripts/shells/linux/common/).
 2. 175_laravel_main_start.sh should not have duplicate numbering (不
    要有重名 — "should not have the same name"). Looking at the
    earlier listing, there were also 155_install_cursor_backend.sh
    and 155_install_cursor.sh, and 159_install_gitea_backend.sh /
    159_install_gitea.sh — duplicate prefixes exist. But the user
    specifically mentions 175_laravel_main_start — "不要有重名"
    likely means the 175_ prefix is duplicated between
    175_laravel_main_runtime_common.sh and 175_laravel_main_start.sh.
    If we move the common one out of install_shells, then
    175_laravel_main_start.sh no longer shares the 175_ prefix with
    another script in install_shells.

 Wait, actually, let me re-read: "175_laravel_main_start.sh
 175_laravel_main_runtime_common 是否是公共脚本，如果是公共脚本修改名
 字放到公共区。175_laravel_main_start 不要有重名。"

 Translation: "175_laravel_main_start.sh,
 175_laravel_main_runtime_common — is 175_laravel_main_runtime_common
 a common script? If it's a common script, rename it and put it in
 the common area. 175_laravel_main_start should not have duplicate
 naming."

 So the task:
 1. Check if 175_laravel_main_runtime_common.sh is a common/shared
    script (i.e., sourced by multiple scripts or is a library rather
    than an install step).
 2. If yes, rename it appropriately and move to
    scripts/shells/linux/common/ (the common area), updating all
    references.
 3. Ensure 175_laravel_main_start.sh doesn't have a name collision
    (the 175_ prefix shared with the common script). Once the common
    script moves out, the collision resolves. But also need to check
    if other scripts have the same 175_ prefix in install_shells.

 Let me look at both files and find references.


 $ ls
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 / | grep 175; echo "---references---"; grep -rln
 "175_laravel_main_runtime_common" /www/programing/core_node/scripts
 /www/programing/core_node/dd.sh 2>/dev/null; echo "---start
 refs---"; grep -rln "175_laravel_main_start"
 /www/programing/core_node/scripts 2>/dev/null

 ... (37 earlier lines, ctrl+o to expand)
 avel_start_service.sh
 /www/programing/core_node/scripts/shells/linux/debian/debian_com/175
 _laravel_main_service_nginx.sh
 /www/programing/core_node/scripts/shells/linux/menu_itemshells/servi
 ce_manager_registry.sh

 Took 2.6s


 Let me examine both files and the references to understand:

 1. 175_laravel_main_runtime_common.sh — referenced only by
    175_laravel_main_start.sh (in install_shells). Let me check how
    it's referenced (sourced? executed?).
 2. 175_laravel_main_start.sh — referenced by many common scripts.
    Let me check how those references work — are they checking
    existence, sourcing, or executing?

 The question: is 175_laravel_main_runtime_common.sh a common script
 (library)? If so, rename and move to common area. And
 175_laravel_main_start.sh must not have duplicate names — meaning
 the 175_ prefix collision between the two files should be resolved
 by moving the common one out of install_shells.

 Let me read both files.


 read
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 /175_laravel_main_runtime_common.sh



 read
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 /175_laravel_main_start.sh


 Now I have the full picture. 175_laravel_main_runtime_common.sh is a
 library sourced by 175_laravel_main_start.sh. Let me check how other
 scripts reference the start script, and how common libs are named in
 the common dir:


 $ cd /www/programing/core_node/scripts/shells/linux && grep -n
 "175_laravel_main_start" common/runtime_config_common.sh
 common/frankenphp_install_pipeline.sh common/nginx_manager.sh
 common/frankenphp_runtime_common.sh common/port_guard_common.sh
 common/nginx_common.sh common/composer_vendor_common.sh
 common/cert_selfheal_common.sh
 menu_itemshells/service_manager_registry.sh
 debian/debian_com/175_laravel_main_service_frankenphp.sh
 debian/debian_com/laravel_runtime_frankenphp.sh
 debian/debian_com/laravel_upgrade_13.sh
 debian/debian_com/laravel_runtime_nginx.sh
 debian/debian_com/175_laravel_main_start_nginx.sh
 debian/debian_com/175_laravel_main_start_frankenphp.sh
 debian/debian_com/laravel_start_service.sh
 debian/debian_com/175_laravel_main_service_nginx.sh

 ... (49 earlier lines, ctrl+o to expand)
 175_laravel_main_start.sh and the systemd units it registers.
 debian/debian_com/175_laravel_main_service_nginx.sh:14:# Called by
 systemd_service_manager via 175_laravel_main_start.sh's
 debian/debian_com/175_laravel_main_service_nginx.sh:20:#
 175_laravel_main_start.sh (one-time setup). This script is the

 Took 0.0s


 Now I have the full picture:

 1. 175_laravel_main_runtime_common.sh — a function library, sourced
    only by 175_laravel_main_start.sh via source
    "$LARAVEL_MAIN_RUNTIME_COMMON". It is a common script (library),
    so per user request: rename it and move it to the common area
    (scripts/shells/linux/common/).

 Naming convention in common/: libraries are suffixed _common.sh
 (e.g., runtime_config_common.sh, composer_vendor_common.sh,
 domain_setup_common.sh, port_guard_common.sh). The file already ends
 with _common.sh. A good name: laravel_main_runtime_common.sh (drop
 the 175_ step prefix since it's not a numbered step script anymore).
 That matches the user's "修改名字放到公共区" (rename and put in
 common area).

 2. 175_laravel_main_start.sh — stays in install_shells as the
    numbered step. After moving the runtime common out, there is no
    longer a duplicate 175_ prefix in install_shells. "不要有重名" =
    no duplicate name — the collision was
    175_laravel_main_runtime_common.sh + 175_laravel_main_start.sh
    sharing the 175_ prefix. Once moved, 175_laravel_main_start.sh is
    the only 175_ script. Let me verify there are no other 175_
    scripts in install_shells — from the earlier listing: only
    175_laravel_main_runtime_common.sh and 175_laravel_main_start.sh.
    Also 176_laravel_ui_service.sh exists (referenced by 175 start as
    UI_SERVICE_ENSURE_SCRIPT). So after the move,
    175_laravel_main_start.sh is unique. Good.

 Now, changes needed:

 A. Move
 scripts/shells/linux/debian/install_shells/175_laravel_main_runtime_
 common.sh →
 scripts/shells/linux/common/laravel_main_runtime_common.sh (git mv?
 No git operations unless asked — but moving a file isn't a git
 operation per se; I can use mv. The instruction "No git operations
 unless explicitly asked" refers to commits etc. Using plain mv is
 fine and doesn't touch git index. Actually if the repo is
 git-tracked, mv leaves the old file as deleted in worktree — that's
 fine, the user didn't ask for git ops. I'll use plain mv.)

 B. Update 175_laravel_main_start.sh:
 - LARAVEL_MAIN_RUNTIME_COMMON="${SCRIPT_CURRENT_DIR}/175_laravel_mai
   n_runtime_common.sh" →
   LARAVEL_MAIN_RUNTIME_COMMON="${COMMON_DIR}/laravel_main_runtime_co
   mmon.sh". COMMON_DIR is already defined ("${LINUX_DIR}/common").

    Wait — check definition order: COMMON_DIR="${LINUX_DIR}/common"
 is defined before RUNTIME_CONFIG_COMMON and
 LARAVEL_MAIN_RUNTIME_COMMON in the variables section. Yes:
 COMMON_DIR is defined at line with DEBIAN_COM_DIR.
 LARAVEL_MAIN_RUNTIME_COMMON comes after. Good.
 - Also
   RUNTIME_CONFIG_COMMON="${COMMON_DIR}/runtime_config_common.sh" —
   the runtime common sources $RUNTIME_CONFIG_COMMON (provided by
   caller). Fine, unchanged.

 C. Check for any other references to 175_laravel_main_runtime_common
  elsewhere (only 175_laravel_main_start.sh per the earlier grep).
 Also check references without the .sh extension or with different
 quoting, e.g. "laravel_main_runtime_common" in comments across the
 repo. Let me grep more broadly.

 D. The runtime common's header comment mentions the file name? Let
 me check the top of the file — it starts with #!/bin/bash and
 directly PORT=.... The first read output shows the file starts
 directly with PORT. There may be a header comment I haven't seen
 (the read output started at "#!/bin/bash\n\n# the PORT env var..." —
 seems the file has no AI rules header). Let me check the full
 beginning and also the end of the file to see if there are
 references to SELF or file paths inside.

 Also check SELF usage in runtime common — print_usage uses ${SELF}
 which is defined by the caller (175_laravel_main_start.sh sets SELF
 to itself). Since the runtime common is sourced, SELF is the
 caller's — fine.

 E. Also verify nothing else sources runtime common by relative path
 like source "$(dirname ...)/175_laravel_main_runtime_common.sh". The
 earlier grep for "175_laravel_main_runtime_common" only found
 175_laravel_main_start.sh. Let me also grep for
 "laravel_main_runtime_common" (without 175_) to catch docstring
 references.

 F. Also, debian_com has 175_laravel_main_start_nginx.sh referencing
 LARAVEL_START_SCRIPT="${INSTALL_SHELLS_DIR}/175_laravel_main_start.s
 h" — that stays valid since start remains in install_shells.

 G. 175_laravel_main_start_frankenphp.sh:91 references
 ${LARAVEL_SERVICE_COMMON_DIR}/install_shells/175_laravel_main_start.
 sh — still valid.

 Also the user's phrase "175_laravel_main_start 不要有重名" — could
 also mean: 175_laravel_main_start.sh should not have duplicate
 name(s) — i.e., after moving the common out, install_shells keeps
 only one 175_ script. That's satisfied.

 But wait — is there also a 175_laravel_main_start_npx_fallback.sh
 and 175_laravel_main_start_php_serve.sh in debian_com (referenced
 above)? Those are in debian_com, not install_shells — they're plane
 helper scripts with the 175_ naming. That's a different directory
 (debian_com = debian common helpers), so no menu collision. The
 user's concern is the install_shells menu (testselector) which only
 lists install_shells/*.sh. After the move, 175_laravel_main_start.sh
  is the only 175_ script there.

 Let me also check the full runtime common file for any other
 caller-path assumptions (e.g., it may resolve other files relative
 to its own location via BASH_SOURCE). Let me grep the file for
 BASH_SOURCE / SCRIPT_CURRENT_DIR / dirname.

 Also need to double check: does the runtime common file get sourced
 anywhere else, e.g., debian_com plane sub-scripts
 (175_laravel_main_start_frankenphp.sh might source it too)? The
 earlier repo-wide grep for "175_laravel_main_runtime_common" found
 only 175_laravel_main_start.sh. But maybe they source it as
 $LARAVEL_MAIN_RUNTIME_COMMON variable — which is defined in 175
 start. Let me grep for LARAVEL_MAIN_RUNTIME_COMMON across the repo.

 Let me run these checks.

 175_laravel_main_runtime_common.sh is a pure function library
 sourced by 175_laravel_main_start.sh (via
 LARAVEL_MAIN_RUNTIME_COMMON), while 175_laravel_main_start.sh is the
 numbered menu step. Let me verify all variable-based references and
 internal path assumptions before moving:


 $ cd /www/programing/core_node && grep -rn
 "LARAVEL_MAIN_RUNTIME_COMMON" scripts/ | grep -v Binary; echo
 "---BASH_SOURCE/self-path in runtime common---"; grep -n
 "BASH_SOURCE\|SCRIPT_CURRENT_DIR\|INSTALL_SHELLS_DIR\|dirname"
 scripts/shells/linux/debian/install_shells/175_laravel_main_runtime_
 common.sh; echo "---head of runtime common---"; head -8
 scripts/shells/linux/debian/install_shells/175_laravel_main_runtime_
 common.sh; echo "---tail---"; tail -5
 scripts/shells/linux/debian/install_shells/175_laravel_main_runtime_
 common.sh; echo "---other 175_ in install_shells---"; ls
 scripts/shells/linux/debian/install_shells/ | grep "^175"

 ... (20 earlier lines, ctrl+o to expand)
 check first,

 ---other 175_ in install_shells---
 175_laravel_main_runtime_common.sh
 175_laravel_main_start.sh

 Took 0.8s


 Confirmed:
 - 175_laravel_main_runtime_common.sh is a sourced library (no
   BASH_SOURCE self-pathing, no entry-point execution at bottom —
   pure functions), referenced only by 175_laravel_main_start.sh via
   LARAVEL_MAIN_RUNTIME_COMMON.
 - It has no internal path dependencies on its current location.
 - In install_shells there are exactly two 175_ scripts — after
   moving the library out, 175_laravel_main_start.sh will be the
   unique 175_ step (no duplicate prefix/name).

 Plan:
 1. mv
    scripts/shells/linux/debian/install_shells/175_laravel_main_runti
    me_common.sh
    scripts/shells/linux/common/laravel_main_runtime_common.sh —
    renamed per common-dir convention (*_common.sh, no numeric step
    prefix).
 2. Update 175_laravel_main_start.sh line 36:
    LARAVEL_MAIN_RUNTIME_COMMON="${SCRIPT_CURRENT_DIR}/175_laravel_ma
    in_runtime_common.sh" →
    LARAVEL_MAIN_RUNTIME_COMMON="${COMMON_DIR}/laravel_main_runtime_c
    ommon.sh". COMMON_DIR is defined earlier in the same variable
    block (COMMON_DIR="${LINUX_DIR}/common" — verify order: yes,
    COMMON_DIR is defined at the top section,
    LARAVEL_MAIN_RUNTIME_COMMON at line 36 comes after).

 Wait — let me double check COMMON_DIR definition position vs line
 36. From the read: the variables block at top:

 ```
   SCRIPT_CURRENT_DIR=...
   DEBIAN_DIR=...
   LINUX_DIR=...
   SHELLS_DIR=...
   SCRIPTS_DIR=...
   REPO_ROOT=...
   POLY_APPS_DIR=...
   LARAVEL_DIR=...
   LARAVEL_SCRIPTS_DIR=...
   DEBIAN_COM_DIR=...
   COMMON_DIR="${LINUX_DIR}/common"
   INSTALL_SHELLS_DIR="$SCRIPT_CURRENT_DIR"
   CORE_NODE_DIR=...
   PORT=...
   LARAVEL_RUNTIME_FRANKENPHP_SCRIPT=...
   LARAVEL_RUNTIME_NGINX_SCRIPT=...
   LARAVEL_START_FRANKENPHP_SUB=...
   LARAVEL_START_NGINX_SUB=...
   RUNTIME_CONFIG_COMMON="${COMMON_DIR}/runtime_config_common.sh"

 LARAVEL_MAIN_RUNTIME_COMMON="${SCRIPT_CURRENT_DIR}/175_laravel_main_
 runtime_common.sh"
   ...
 ```

 Yes — COMMON_DIR is defined before LARAVEL_MAIN_RUNTIME_COMMON.

 3. Also check whether anything else (like the debian_com scripts)
    references the runtime-common by name in comments — the repo-wide
    grep found only the start script.

 4. Verify bash syntax of both files after the change.

 Also, should I add a source-guard comment? No — keep minimal. The
 library has no guard; when sourced by start it's fine.

 One more check: the runtime common sources "$RUNTIME_CONFIG_COMMON"
 (line inside) — caller provides it (defined in start). Still fine
 after the move since sourcing is by absolute variable path.

 Also there might be references in poly_apps or elsewhere outside
 scripts/. Let me do a final repo-wide grep including poly_apps,
 apps, docs (excluding node_modules) for
 "175_laravel_main_runtime_common" to be thorough.

 Confirmed: it's a sourced function library (no entry point, no
 self-pathing), referenced only via LARAVEL_MAIN_RUNTIME_COMMON in
 175_laravel_main_start.sh. Final repo-wide reference check, then
 move:


 $ cd /www/programing/core_node && grep -rln
 "175_laravel_main_runtime_common" --exclude-dir=node_modules
 --exclude-dir=.git . 2>/dev/null

 ./scripts/shells/linux/debian/install_shells/175_laravel_main_start.
 sh
 ./docs_fix/origin/ab5.txt
 ./docs_fix/origin/e.txt
 ./docs_fix/origin/ab4.txt

 Took 38.0s


 References:
 - scripts/shells/linux/debian/install_shells/175_laravel_main_start.
   sh — the only code reference (to update).
 - docs_fix/origin/*.txt — origin log snapshots; those are historical
   records, not code. Do not modify (they're origin docs / archives).

 Now execute:
 1. Move + rename: mv
    scripts/shells/linux/debian/install_shells/175_laravel_main_runti
    me_common.sh
    scripts/shells/linux/common/laravel_main_runtime_common.sh
 2. Update the reference in 175_laravel_main_start.sh.

 Then verify with bash -n on both files and confirm install_shells
 now has a single 175_ script.

 Only code reference is 175_laravel_main_start.sh (the
 docs_fix/origin/*.txt are historical log snapshots — not code).
 Moving the library to the common area with the *_common.sh naming
 convention, and updating the reference:


 $ cd /www/programing/core_node && mv
 scripts/shells/linux/debian/install_shells/175_laravel_main_runtime_
 common.sh scripts/shells/linux/common/laravel_main_runtime_common.sh
 && ls scripts/shells/linux/common/ | grep laravel; ls
 scripts/shells/linux/debian/install_shells/ | grep "^175"

 laravel_main_runtime_common.sh
 175_laravel_main_start.sh

 Took 0.1s



 edit
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 /175_laravel_main_start.sh

      ...
   32
 LARAVEL_RUNTIME_NGINX_SCRIPT="${DEBIAN_COM_DIR}/laravel_runtime_ngin
 x.sh"
   33
 LARAVEL_START_FRANKENPHP_SUB="${DEBIAN_COM_DIR}/175_laravel_main_sta
 rt_frankenphp.sh"
   34
 LARAVEL_START_NGINX_SUB="${DEBIAN_COM_DIR}/175_laravel_main_start_ng
 inx.sh"
   35 RUNTIME_CONFIG_COMMON="${COMMON_DIR}/runtime_config_common.sh"
 - 36
 LARAVEL_MAIN_RUNTIME_COMMON="${SCRIPT_CURRENT_DIR}/175_laravel_main_
 runtime_common.sh"
 + 36
 LARAVEL_MAIN_RUNTIME_COMMON="${COMMON_DIR}/laravel_main_runtime_comm
 on.sh"
   37
 LARAVEL_13_UPGRADE_SCRIPT="${DEBIAN_COM_DIR}/laravel_upgrade_13.sh"
   38 DOMAIN_SETUP_COMMON="${COMMON_DIR}/domain_setup_common.sh"
   39 VENDOR_AUTOLOAD="${LARAVEL_DIR}/vendor/autoload.php"
   40 BOOTSTRAP_APP="${LARAVEL_DIR}/bootstrap/app.php"
      ...


 Now verify:
 1. Syntax check both files.
 2. Verify the source line resolves to the new file (the variable
    path exists).
 3. Check that common/laravel_main_runtime_common.sh's source
    "$RUNTIME_CONFIG_COMMON" still works — RUNTIME_CONFIG_COMMON is
    defined by the caller before sourcing; unchanged.

 Also check if there are CRLF issues or anything — run bash -n.


 $ cd /www/programing/core_node/scripts/shells/linux && bash -n
 common/laravel_main_runtime_common.sh && echo "runtime common:
 SYNTAX OK"; bash -n debian/install_shells/175_laravel_main_start.sh
 && echo "start script: SYNTAX OK"; grep -n
 "LARAVEL_MAIN_RUNTIME_COMMON"
 debian/install_shells/175_laravel_main_start.sh

 runtime common: SYNTAX OK
 start script: SYNTAX OK
 36:LARAVEL_MAIN_RUNTIME_COMMON="${COMMON_DIR}/laravel_main_runtime_c
 ommon.sh"
 189:source "$LARAVEL_MAIN_RUNTIME_COMMON"

 Took 0.0s


 Both files pass syntax checks. Let me also verify the path
 resolution logic: in 175_laravel_main_start.sh:
 - COMMON_DIR="${LINUX_DIR}/common" where LINUX_DIR="$(dirname
   "$DEBIAN_DIR")" and DEBIAN_DIR="$(dirname "$SCRIPT_CURRENT_DIR")"
   — SCRIPT_CURRENT_DIR is scripts/shells/linux/debian/install_shells
    → DEBIAN_DIR = scripts/shells/linux/debian → LINUX_DIR =
   scripts/shells/linux → COMMON_DIR = scripts/shells/linux/common.
   Correct — resolves to the new location.

 Also verify that nothing else in install_shells now collides with
 175_ — confirmed: only 175_laravel_main_start.sh remains. And the
 numbered script ordering (sort by numeric prefix) is unchanged for
 175_laravel_main_start.sh.

 One more consideration: the runtime common file's internal
 references to scripts in install_shells (like
 $PHP_ENSURE_SCRIPT_FRANKENPHP, $PHP_PGSQL_ENSURE_SCRIPT) are
 provided by the caller's variables — the sourcing script defines
 them all at top, and sourcing happens after those definitions (line
 189 sources the runtime common, variables defined lines ~44-56). Let
 me double check the runtime common references any variables that the
 start script defines AFTER line 189 — e.g. functions in runtime
 common use $SELF, $RUNTIME_CONFIG_COMMON, $GENERATED_ACCESS_CODE,
 $VENDOR_AUTOLOAD, $BOOTSTRAP_APP, $PHP_ENSURE_SCRIPT_FRANKENPHP,
 $PHP_PGSQL_ENSURE_SCRIPT, $SERVICE_MANAGER,
 $UI_SERVICE_ENSURE_SCRIPT, $LARAVEL_SERVICE_NAME_BASE, etc. All
 defined before line 189 in the start script?

 From the start script structure:
 - Line ~15-90: variable declarations (all the ones above).
 - Line ~150-189: sourcing sequence: . "$LARAVEL_13_UPGRADE_SCRIPT",
   . "$GVAR_COMMON_SCRIPT", source common_functions.sh, .
   "$DOMAIN_SETUP_COMMON", . "$COMPOSER_VENDOR_COMMON", .
   "$FRANKENPHP_MANAGER_SCRIPT", web_access_config_ensure, then
   source "$LARAVEL_MAIN_RUNTIME_COMMON" at line 189.
 - The variable declarations block (lines 15-147) is all before line
   189. So all variables are defined before the runtime common is
   sourced. Unchanged by my edit (just the path changed).

 Everything checks out. Summary:

 1. 175_laravel_main_runtime_common.sh was indeed a common (sourced
    library) script — pure functions, no self-executing entry point,
    no location-dependent paths.
 2. Moved + renamed to
    scripts/shells/linux/common/laravel_main_runtime_common.sh
    following the common-area naming convention (*_common.sh without
    numeric step prefix).
 3. Updated 175_laravel_main_start.sh line 36 to reference
    COMMON_DIR.
 4. 175_laravel_main_start.sh is now the unique 175_ step script in
    install_shells — no duplicate name/prefix.
 5. Verified: repo-wide only code reference updated
    (docs_fix/origin/*.txt are historical logs, untouched); both
    files pass bash -n.

 Done. Verification confirms:

 1. 175_laravel_main_runtime_common.sh was a common script — it's a
 pure sourced function library (no entry point, no BASH_SOURCE
 self-pathing, only executed via source from
 175_laravel_main_start.sh line 189), with no other code references
 in the repo (only historical log snapshots in
 docs_fix/origin/*.txt).

 2. Changes made:

 ┌────────────┬─────────────────────────────────────────────────────┐
 │ Action     │ Detail                                              │
 ├────────────┼─────────────────────────────────────────────────────┤
 │ Moved +    │ debian/install_shells/175_laravel_main_runtime_comm │
 │ renamed    │ on.sh → common/laravel_main_runtime_common.sh       │
 │            │ (follows the common-area *_common.sh convention     │
 │            │ without the numeric step prefix)                    │
 ├────────────┼─────────────────────────────────────────────────────┤
 │ Updated    │ 175_laravel_main_start.sh line 36:                  │
 │ reference  │ LARAVEL_MAIN_RUNTIME_COMMON="${SCRIPT_CURRENT_DIR}/ │
 │            │ 175_laravel_main_runtime_common.sh" →               │
 │            │ "${COMMON_DIR}/laravel_main_runtime_common.sh"      │
 │            │ (reuses the existing COMMON_DIR already declared    │
 │            │ above it)                                           │
 └────────────┴─────────────────────────────────────────────────────┘

 3. 175_laravel_main_start.sh no longer has a duplicate name — the
 175_ prefix collision was between the two files; now install_shells/
  contains exactly one 175_ step: 175_laravel_main_start.sh, so the
 menu ordering (numeric-prefix sort) stays clean and unambiguous.

 4. Verified: both files pass bash -n; all variables the library
 needs (RUNTIME_CONFIG_COMMON, SELF, PHP_ENSURE_SCRIPT_FRANKENPHP,
 etc.) are still declared in 175_laravel_main_start.sh before the
 source line; the plane helpers in debian_com/
 (175_laravel_main_start_frankenphp/nginx.sh, service launchers)
 reference 175_laravel_main_start.sh in install_shells/ and remain
 valid.


 Executing: 175_laravel_main_start
   Path: 175_laravel_main_start

 Error: Script not found: 175_laravel_main_start
 Press any key to continue...

 是用关键字和序号匹配，不仅仅是序号。关键字一样可以匹配。同时菜单搜索
 的只有scripts\shells\linux\debian\install_shells中的
 ，scripts\shells\linux\debian\install_shells中的同序号脚本修改名字，
 或者是公共脚本的，改名并移入公共脚本区。  75_install_postgresql.sh
   75_postgresql_install_common.sh 113_natgateway.sh
   113_natgateway_interfaces.sh
   113_natgateway_menu.sh
   113_natgateway_runtime.sh  155_install_cursor.sh
   155_install_cursor_backend.sh


 The user reports:

 1. A bug in the testselector: "Executing: 175_laravel_main_start /
    Path: 175_laravel_main_start / Error: Script not found" — the
    matched script was stored as just the basename without full path?
    Let me look... The error shows "Path: 175_laravel_main_start" —
    the path is just the name. Hmm, this suggests
    TEST_SELECTOR_MATCHED_SCRIPT contained only
    "175_laravel_main_start" (no directory).

 Wait, also earlier debug showed [voxcpm] -> voxcpm — the matched
 value was the input text! And I noticed that oddity earlier:
 "matched=[voxcpm]". So there IS a real bug in find_matching_script.
 The digit branch worked (93 → full path), but text branch returns
 the input?!

 Let me re-look at find_matching_script:

 ```bash
   find_matching_script() {
       local -a candidate_scripts=("$@")
       local input_lower="${TEST_SELECTOR_INPUT,,}"
       local candidate=""
       local candidate_name=""

       TEST_SELECTOR_MATCHED_SCRIPT=""

       if [[ "$input_lower" =~ ^[0-9]+$ ]]; then
           for candidate in "${candidate_scripts[@]}"; do
               candidate_name="$(basename "$candidate")"
               if [[ "$candidate_name" =~ ^${input_lower}_ ]]; then
                   TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
                   return 0
               fi
           done
       fi

       for candidate in "${candidate_scripts[@]}"; do
           candidate_name="$(basename "${candidate,,}")"
           if [[ "$candidate_name" == *"$input_lower"* ]]; then
               TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
               return 0
           fi
       done

       return 1
   }
 ```

 Hmm wait, and the user input was "175_laravel_main_start" and it
 matched, "Path: 175_laravel_main_start" — that's the basename shown.
 But path shows no directory...

 OH WAIT. I see it now!! In test_scripts_main:

 ```bash
   local scripts=($(get_install_scripts))
 ```

 get_install_scripts returns paths via printf '%s\n' "${result[@]}".
 The find output — with what path format? find "$install_shells_dir"
 -maxdepth 1 -name "*.sh" -print0 where
 install_shells_dir=$(get_install_shells_dir) which echoes
 "$shells_dir/debian/install_shells" — full absolute path. So scripts
 array contains absolute paths like
 /www/.../install_shells/1_stop_cloud_monitors.sh.

 Hmm, but the user's output shows Path: 175_laravel_main_start — just
 the basename. So TEST_SELECTOR_MATCHED_SCRIPT =
 "175_laravel_main_start"??

 And the user input... what did they type? Probably
 "175_laravel_main_start" or maybe "175"? If they typed "175", the
 digit branch would check [[ "$candidate_name" =~ ^175_ ]] — first
 match in sorted order... scripts with prefix 175: only
 175_laravel_main_start.sh now (after our move). Then
 TEST_SELECTOR_MATCHED_SCRIPT would be the absolute path. Then
 "Executing: 175_laravel_main_start.sh" (with .sh). But user output
 shows "175_laravel_main_start" WITHOUT .sh!

 So the user typed the input "175_laravel_main_start" (without .sh).
 Digit branch: input contains non-digits, skipped. Text branch:
 case-insensitive substring match. candidate_name for the start
 script is "175_laravel_main_start.sh" which contains
 "175_laravel_main_start" → match. TEST_SELECTOR_MATCHED_SCRIPT =
 full path → "Executing: 175_laravel_main_start.sh", "Path:
 /full/path/175_laravel_main_start.sh". But output shows basename
 only, no .sh!

 So something IS wrong — the matched value ended up being exactly the
 input string. That matches my earlier debug: matched=[voxcpm]. So
 TEST_SELECTOR_MATCHED_SCRIPT="$candidate" apparently sets it to...
 the input?!

 Wait, unless... basename "${candidate,,}" — hmm. Let me think about
 ${candidate,,} when candidate is an absolute path — fine.

 Hold on, is there something about local -a candidate_scripts=("$@")?
 In test_scripts_main I call find_matching_script
 "$TEST_SELECTOR_INPUT" "${scripts[@]}". So 1 = input, rest =
 scripts. candidateₛcripts = ("@") — INCLUDING the input as element
 0! Bug #1: candidate_scripts includes the input itself as first
 element. So the text loop first checks
 candidate="$TEST_SELECTOR_INPUT" (the input itself), basename of
 "175_laravel_main_start" is "175_laravel_main_start", which
 trivially contains itself → match →
 TEST_SELECTOR_MATCHED_SCRIPT="175_laravel_main_start" → "Path:
 175_laravel_main_start" → not found. EXACTLY the user's symptom!

 And for digit inputs, the digit branch checks ^93_ against basename
 "93" — no match (no underscore), then digit branch completes without
 match... wait, but digit branch worked correctly earlier: "[93] ->
 93_install_frankenphp.sh". Hmm, why? For input "93": digit branch
 loops candidates including "93" as element 0: basename "93" =~ ^93_
 ? "93" doesn't match ^93_ (needs underscore). So no match in digit
 branch. Then text branch: candidate "93" vs 93 → contains → match →
 TEST_SELECTOR_MATCHED_SCRIPT="93"?? But earlier debug showed full
 path match "93_install_frankenphp.sh"...

 Wait no — earlier debug output: [93] -> 93_install_frankenphp.sh.
 Hmm, that contradicts. Unless in my earlier debug the array passed
 didn't include the input... In my debug I called
 find_matching_script "$TEST_SELECTOR_INPUT" "${paths[@]}" — same as
 real code. And digit input "93" gave full path. So the digit branch
 found 93_install_frankenphp.sh BEFORE reaching the text branch —
 because in the digit branch, element 0 is "93" (the input), basename
 "93" doesn't match ^93_, so it proceeded to real scripts and matched
 93_install_frankenphp.sh (full path) in the DIGIT branch.

 For text input "voxcpm": digit branch skipped (not digits). Text
 branch: element 0 = "voxcpm" (input) → basename "voxcpm" contains
 "voxcpm" → match → matched="voxcpm". Exactly what I saw. I dismissed
 it as heredoc mangling — no, it was a REAL bug.

 Fix: in find_matching_script, shift off the first argument (the
 input):

 ```bash
   local user_input="$1"
   shift
   local -a candidate_scripts=("$@")
 ```

 But wait — TEST_SELECTOR_INPUT global is also used. The function
 currently uses the global TEST_SELECTOR_INPUT and receives
 candidates as "$@". Better design: take input as $1, shift, then
 candidates:

 ```bash
   find_matching_script() {
       local user_input="$1"
       shift
       local -a candidate_scripts=("$@")
       local input_lower="${user_input,,}"
       ...
   }
 ```

 And call: find_matching_script "$TEST_SELECTOR_INPUT"
 "${scripts[@]}".

 2. User's request also: "是用关键字和序号匹配，不仅仅是序号。关键字
    一样可以匹配。" — match by keyword AND index, not only index;
    keywords can match too. That's already the design (substring
    match); the bug just broke it. After fixing shift, keywords work.

 3. "同时菜单搜索的只有scripts\shells\linux\debian\install_shells中的
    " — the menu should search ONLY scripts in install_shells. Hmm,
    currently get_install_scripts finds install_shells_dir =
    $(dirname "$script_dir")/debian/install_shells where script_dir =
    common. So it already lists only install_shells. Maybe the user
    is saying: the menu currently might pick up... hmm, or maybe the
    user means after we move common scripts out, ensure the menu only
    searches install_shells (not debian_com, not common). It already
    does.

 4. "scripts\shells\linux\debian\install_shells中的同序号脚本修改名字
    ，或者是公共脚本的，改名并移入公共脚本区" — scripts in
    install_shells with the SAME index (同序号 = same number prefix)
    should be renamed, or if they're common scripts, renamed and
    moved into the common area. The user lists:

 - 75_install_postgresql.sh and 75_postgresql_install_common.sh —
   same prefix 75. If 75_postgresql_install_common.sh is a common
   script → move to common area (rename without number).
 - 113_natgateway.sh, 113_natgateway_interfaces.sh,
   113_natgateway_menu.sh, 113_natgateway_runtime.sh — same prefix
   113. These are probably related scripts — determine which is the
   main entry vs common helpers. Hmm, but these may be dispatched by
   each other (not necessarily "common"). The user says "同序号脚本修
   改名字，或者是公共脚本的，改名并移入公共脚本区" — scripts sharing
   the same number should be renamed (so each has a unique number),
   or if a script is a common library, move it to the common area.
 - 155_install_cursor.sh and 155_install_cursor_backend.sh — same
   prefix 155.

 So the task: eliminate duplicate numeric prefixes in install_shells
 by:
 a) If the duplicate is a common/library script → move to common/
 with proper name.
 b) Otherwise (it's an install step) → renumber it to a free number
 (rename).

 Let me analyze each:

 75: 75_install_postgresql.sh (main install step) +
 75_postgresql_install_common.sh. Check if the latter is sourced/used
 by the former (or by others). If it's a library → move to common as
 postgresql_install_common.sh. But wait — maybe it's used by other
 scripts too. Need to grep.

 113: 113_natgateway.sh, 113_natgateway_interfaces.sh,
 113_natgateway_menu.sh, 113_natgateway_runtime.sh. These might be:
 113_natgateway.sh main entry, and interfaces/menu/runtime as helpers
 dispatched with args? Check references. If they're helpers invoked
 by the main → they could be moved to common (they're "common" in the
 sense of being sub-scripts, not menu steps) OR renumbered. But
 moving to common might break internal references... The user said "
 或者是公共脚本的，改名并移入公共脚本区" — or if common scripts,
 rename and move into common area. So determine per script.

 155: 155_install_cursor.sh (menu step) +
 155_install_cursor_backend.sh. Check: is backend a library invoked
 by 155_install_cursor.sh? If yes → move to common as e.g.
 cursor_install_backend.sh? Hmm, but "backend" here might mean
 "backend installation variant" not a library. Let me read both.

 Also remember earlier listing showed other duplicates in
 install_shells: 159_install_gitea.sh + 159_install_gitea_backend.sh,
 and 169_install_rustdesk_server... +
 169_rustdesk_server_client_config.sh. The user only listed 75, 113,
 155 though. Should I also handle 159 and 169? The user said "同序号
 脚本" and listed examples. Let me check ALL same-prefix groups in
 install_shells and handle them all (user's intent is clearly
 "eliminate same-number collisions in install_shells"). Let me
 enumerate.

 Also there's the earlier user request context: they're cleaning up
 install_shells so that each script has a unique number and libs live
 in common.

 Plan:
 1. Fix the find_matching_script bug (shift the input arg off).
 2. Confirm menu searches only install_shells (already true; keep).
 3. Analyze duplicate-prefix groups in install_shells:
     - 75_install_postgresql.sh + 75_postgresql_install_common.sh
     - 113_natgateway*.sh (4 scripts)
     - 155_install_cursor.sh + 155_install_cursor_backend.sh
     - 159_install_gitea.sh + 159_install_gitea_backend.sh (if still
       present)
     - 169_install_rustdesk_server +
       169_rustdesk_server_client_config.sh
       For each: determine if the non-primary is a library/sourced or
       a dispatched sub-script:
     - If library (sourced) → move to common/ with descriptive name,
       update references.
     - If a step dispatched with args from the primary (like the 175
       case earlier: 175_laravel_main_start dispatched plane
       sub-scripts)... but those lived in debian_com. Here for 113:
       maybe 113_natgateway.sh is the entry and others are
       sub-commands. Hmm.

 Wait, let me think about what "公共脚本" means per user: scripts
 that are shared/common (sourced by multiple or used as libraries) vs
 numbered install steps. For each duplicate group, the user wants:
 keep ONE script with the number (the main entry); the others either:
 - if common/library → move to common area (rename, drop number)
 - else (an install step in its own right) → rename with a NEW unique
   number (e.g., 155_install_cursor_backend → get next free number
   like 157? no 157 is taken by vscode. Next free even number... let
   me check what numbers are free).

 Hmm wait, but "155_install_cursor_backend.sh" — let me actually read
 these files first to understand their nature before deciding. Let me
 start investigating.

 Also the user's first line about the testselector bug: "是用关键字和
 序号匹配，不仅仅是序号。关键字一样可以匹配" — fix so keyword
 matching works. The bug: find_matching_script includes input in
 candidates. Fix with shift.

 Also "同时菜单搜索的只有scripts\shells\linux\debian\install_shells中
 的" — menu searches only install_shells scripts. Current
 implementation does that. Fine — but after moving common scripts OUT
 of install_shells, the menu won't list them anymore, which is what
 the user wants (menu only shows install steps).

 Let me now:
 1. Fix testselector bug.
 2. List all duplicate-prefix groups in install_shells.
 3. Read each duplicate to classify: library vs step.
 4. Move libraries to common/ with renamed files; renumber true steps
    to unique free numbers.
 5. Update all references repo-wide.

 Let me start.

 Let me first fix the testselector bug (the input leaked into the
 candidate list as element 0, so the "match" was the input itself —
 hence "Script not found"), then analyze all same-prefix script
 groups in install_shells:


 read
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h



 $ cd
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 && ls | grep -oE '^[0-9]+' | sort -n | uniq -d

 75
 113
 155
 159
 169

 Took 0.0s



 Select script: 这里输入小写的b退出上一级菜单。b不作为关键词。


 The user says: "Select script: 这里输入小写的b退出上一级菜单。b不作
 为关键词。" — In the "Select script:" prompt, entering lowercase b
 should exit to the parent menu. b should NOT be treated as a
 keyword.

 Currently in testselector.sh, the back condition checks: empty, q,
 Q, b, B. Wait — it already handles b and B as back. Let me re-read:

 ```bash
   if [ -z "$TEST_SELECTOR_INPUT" ] || [ "$TEST_SELECTOR_INPUT" = "q"
  ] || [ "$TEST_SELECTOR_INPUT" = "Q" ] || [ "$TEST_SELECTOR_INPUT" =
 "b" ] || [ "$TEST_SELECTOR_INPUT" = "B" ]; then
       return 0
   fi
 ```

 Hmm, so b and B both already go back. But the user says "输入小写的b
 退出上一级菜单。b不作为关键词" — lowercase b exits to the parent
 menu; b is not a keyword.

 Maybe the issue: the user wants ONLY lowercase b (and maybe
 uppercase Q? they said lowercase b) to be the back key, and they're
 pointing out that b should not fall through to keyword matching.
 Currently it already returns 0 on b/B/q/Q. But wait — there's a
 subtlety: my bug fix from the previous turn is still PENDING!
 Looking at the conversation: in the last turn I identified the bug
 (input leaked into candidates as element 0) but the conversation was
 cut off — I read the file and listed duplicate prefixes but NEVER
 actually applied the fix to find_matching_script (the shift fix),
 nor did I do the 75/113/155/159/169 reorganization.

 Wait, let me re-read the file as displayed in the last read:

 ```bash
   find_matching_script() {
       local -a candidate_scripts=("$@")
       local input_lower="${TEST_SELECTOR_INPUT,,}"
       ...
 ```

 Yes — the bug is still there. The previous turn ended after reading
 the file and listing duplicate groups (75, 113, 155, 159, 169), but
 no edits were applied yet.

 Now the current user message focuses specifically on: "Select
 script: 这里输入小写的b退出上一级菜单。b不作为关键词。" — lowercase
 b should exit to the previous menu level; b must not be treated as a
 keyword.

 So the user is now scoping the request to just the b handling? But
 the previous message also had the big request (fix keyword matching,
 move common scripts). The current message seems to be an additional
 refinement: the user noticed something about b.

 Hmm, wait. Maybe the user tested with the CURRENT file state. With
 the bug (input as element 0), typing b... let's trace: input "b" →
 not empty, not q/Q... IS it b? Yes, [ "$TEST_SELECTOR_INPUT" = "b" ]
  → return 0. So b already works for back. Unless... hmm.

 Actually wait — maybe the user is looking at the code and realizes
 that b is handled, but wants to make sure: "输入小写的b退出上一级菜
 单" (lowercase b exits to parent menu) — and "b不作为关键词" (b
 should not be used as a keyword/match term). The current code
 handles both b and B and q and Q as back keys. The user says
 lowercase b specifically.

 But hold on — is there a conflict? With the bug present, if user
 typed "b" and it somehow didn't match the back condition... no, it
 does match.

 Hmm, but wait — maybe the issue is different. Let me think about
 what the user experienced. Perhaps they typed b expecting to go back
 but instead it matched a script via keyword search (because... no, b
 matches the back condition first and returns 0).

 Unless the previous turn's pending fix... no wait, the file
 currently in the repo has the bug where input is element 0 of
 candidates. Typing "b": back condition catches "b" → return 0.
 Works.

 Hmm, unless the user is telling me about behavior they WANT after
 the previous turn's changes get applied — maybe they're worried that
 in my upcoming fix, b might become a keyword match (e.g., matching
 "b" substring against script names). The user preempts: make
 lowercase b a back key, NOT a keyword.

 Actually, re-reading: "Select script: 这里输入小写的b退出上一级菜单
 。b不作为关键词。" — this is likely a clarification/addition to the
 pending work: at the "Select script:" prompt, lowercase b exits to
 parent menu; b is not a keyword.

 So what should I do now? The previous turn's work is incomplete — I
 was mid-task:
 1. Fix find_matching_script bug (input leaked into candidates).
 2. Handle duplicate-prefix groups (75, 113, 155, 159, 169) — move
    common scripts to common/, renumber steps.
 3. Now additionally: ensure lowercase b = back, not keyword.

 The current message seems to be the immediate instruction. But
 should I also complete the previous turn's pending work? The user's
 previous message had a big request (fix keyword matching +
 reorganize same-number scripts). I hadn't done any of it yet. The
 current message is a follow-up refinement.

 I think the right approach: complete the whole task now — fix the
 matcher bug (keyword + number matching), ensure lowercase b goes
 back (and q/Q, B? — user says lowercase b; current code has b/B/q/Q;
 maybe simplify to match user request: lowercase b exits; q/Q also?
 The user earlier didn't complain about q. Let me keep q/Q and
 lowercase b, maybe drop uppercase B? Hmm).

 Wait, careful reading: "输入小写的b退出上一级菜单。b不作为关键词。"
 — "enter lowercase b to exit to the parent menu. b is not a
 keyword."

 The user emphasizes lowercase b specifically. Current code: b AND B
 both exit. The user says lowercase b. Should uppercase B still work?
 Ambiguous. The user's phrasing "输入小写的b" suggests specifically
 lowercase. Perhaps they want to distinguish: uppercase B could be a
 keyword (e.g., matching "B" in some script name?) — unlikely. Or
 maybe they just describe what they want: lowercase b → back.

 Hmm, also "b不作为关键词" — with substring matching, "b" would match
 many scripts (any name containing letter b). The user wants b
 reserved as the back key, never used for matching. Currently the
 back check happens BEFORE matching, so b never reaches the matcher.
 That's already the case.

 But wait — there's the bug! With the bug, input "b"... no. The back
 condition catches it.

 Hmm, actually let me reconsider — maybe the user typed "b" in their
 test and something unexpected happened? Let me trace the CURRENT
 file with input "b":
 1. read -r -p "Select script: " TEST_SELECTOR_INPUT → "b"
 2. trim → "b"
 3. back condition: [ "$TEST_SELECTOR_INPUT" = "b" ] → true → return
    0 → exits to parent menu.

 That works... UNLESS the actual deployed file differs. Wait — did my
 edit in the first turn actually get applied? Let me check the file
 content from the last read (previous turn). Yes, it shows the
 current state with the back condition including b/B/q/Q.

 Hmm wait, but actually — maybe the user is reporting that after
 typing b, it did NOT go back in their testing? Let me think again
 about the bug...

 OH WAIT. I see it now! Look at the flow again. The previous turn
 identified the bug: find_matching_script "$TEST_SELECTOR_INPUT"
 "${scripts[@]}" — candidates include the input itself. But for "b",
 the back condition catches first. So b works.

 Hmm, unless... the user typed "B" or " b" or something. Or the user
 hasn't tested at all and is just specifying the desired behavior.

 Actually, I think the most likely interpretation: The user is
 reviewing my implementation and specifying requirements:
 1. Previous message (last turn): fix keyword matching bug +
    reorganize duplicate-number scripts.
 2. Current message: clarify that lowercase b should be the back key
    and NOT participate in keyword matching.

 Since the previous turn's work is incomplete (I only did analysis),
 I should now:
 1. Fix the matcher bug.
 2. Ensure lowercase b exits to parent menu (keep it before keyword
    matching; also make sure b is never matched as keyword).
 3. Complete the reorganization of duplicate-prefix scripts?

 Hmm, wait. Let me re-read the current message once more: "Select
 script: 这里输入小写的b退出上一级菜单。b不作为关键词。"

 This is a short, focused message about the b key only. The user may
 have tested the menu and found... hmm. Maybe they tested and typed
 "b" and it worked, but they noticed the menu hint says "Press Enter
 (empty input), q or b to go back" and they want to confirm/adjust?
 Or maybe they typed "b" and it DIDN'T go back?

 Let me look VERY carefully at the back condition for a subtle bug:

 ```bash
   if [ -z "$TEST_SELECTOR_INPUT" ] || [ "$TEST_SELECTOR_INPUT" = "q"
  ] || [ "$TEST_SELECTOR_INPUT" = "Q" ] || [ "$TEST_SELECTOR_INPUT" =
 "b" ] || [ "$TEST_SELECTOR_INPUT" = "B" ]; then
       return 0
   fi
 ```

 This is fine. "b" → return 0 → test_scripts_main returns 0 → back to
 install_test_menu loop → shows "Install and Test Environment" menu.
 That IS the parent menu (上一级菜单).

 Hmm OK here's another thought — maybe the user is running an OLD
 version of the file (from before my edit, i.e., the arrow-menu
 version?) No — the arrow-menu version didn't have "Select script:"
 prompt. The user's report in the previous message showed "Executing:
 175_laravel_main_start / Path: 175_laravel_main_start / Error:
 Script not found" — that's from MY new version (with the
 input-as-candidate bug). So they're running my new version.

 So with my new version, typing "b"... goes back correctly. But
 typing "175_laravel_main_start" matched the input itself (bug). The
 user's previous message asked to fix that. I read the file and
 started analyzing. NOW the user adds: lowercase b should exit to
 parent menu; b is not a keyword.

 Hmm wait, actually — maybe there's a subtlety I'm missing. What if
 the user typed "b" and it did NOT exit? Let me check read -r -p
 "Select script: " TEST_SELECTOR_INPUT — if the user typed "b" and
 pressed Enter, TEST_SELECTOR_INPUT="b". Trim: no change. Back
 condition: matches "b" → return 0. Works.

 BUT — what if the user wants b to work WITHOUT pressing Enter? Like
 a single keypress? "输入小写的b退出上一级菜单" — just says entering
 lowercase b exits. With read -r, they need to press Enter. That's
 fine, consistent with the input-based design.

 OK here's my final interpretation: The user's current message is a
 requirement specification/clarification for the pending work:
 - At the "Select script:" prompt, lowercase b exits to the parent
   menu.
 - b must not be treated as a keyword (i.e., never used for script
   matching — it's reserved).

 And implicitly, I still need to complete the previous turn's pending
 work:
 1. Fix the find_matching_script bug (input leaked as candidate 0 —
    that's why "175_laravel_main_start" matched itself and "Path"
    showed the input).
 2. Keyword + number matching (already designed, just buggy).
 3. Reorganize same-number scripts in install_shells (75, 113, 155,
    159, 169): common scripts → move to common/ with renamed files;
    others → renumber.

 Hmm, but the current message ONLY talks about the b key. Should I do
 the full reorganization now? The previous turn I was mid-analysis
 (read files, listed duplicate groups: 75, 113, 155, 159, 169). The
 user's new message doesn't mention the reorganization.

 Hmm, tricky. Let me think about conversation flow:
 - Turn N-1 (previous): User reported the matcher bug + asked for
   keyword matching + reorganization of same-number scripts (75, 113,
   155 examples). I analyzed but didn't apply any changes (the turn
   ended with my tool calls showing the file content and the
   duplicate prefix list: 75, 113, 155, 159, 169).
 - Turn N (current): User says: "Select script: 这里输入小写的b退出上
   一级菜单。b不作为关键词。"

 The current message starts with "Select script:" — referencing the
 prompt in the menu. The user is adding a requirement about the b
 key. This suggests they're refining the testselector behavior while
 I'm working on it.

 Given the context, I believe the user wants me to (at minimum) fix
 the testselector:
 1. Lowercase b = back (already there, but user wants lowercase b
    specifically — maybe remove uppercase B from back keys so B can
    be a keyword? "b不作为关键词" says b is not a keyword, implying
    other letters ARE keywords... and maybe uppercase B? Hmm).

 Hmm, "b不作为关键词" — b is not a keyword. This might mean:
 lowercase b is reserved for back, so it never participates in
 matching. What about uppercase B? The user didn't say. Current code
 treats B as back too.

 Actually, you know what — let me reconsider. Maybe the user tested
 and found that typing "b" matched a script as keyword INSTEAD of
 going back? When could that happen... If TEST_SELECTOR_INPUT after
 trim is "b", the back condition catches it. UNLESS the file they're
 running is different from what I see.

 Hold on — let me re-examine. Is it possible my edit from turn 1
 didn't save? The read in the previous turn shows the edited version.
 OK it's there.

 Hmm, wait — one more possibility: the user typed "b" and my code DID
 exit... and the user is now saying "this is correct, keep it" — no
 wait, they say "b不作为关键词" (b is not a keyword).

 OR: maybe the user typed something like "bash" or "backup" and got a
 script, and they want single-letter "b" reserved. The current code
 already reserves "b" exactly (whole-word comparison).

 I think the current state ALREADY satisfies "lowercase b exits to
 parent menu, b is not a keyword" for exact input "b". But to be
 thorough and safe, maybe the user wants:
 - Only lowercase b (and maybe q) as back keys.
 - Uppercase B? Unclear.

 Hmm, hmm. Let me think about what would make the user's experience
 differ from expectations. Actually — WAIT. I just realized
 something. Let me re-read the user's previous message once more:

 "是用关键字和序号匹配，不仅仅是序号。关键字一样可以匹配。同时菜单搜
 索的只有scripts\shells\linux\debian\install_shells中的
 ，scripts\shells\linux\debian\install_shells中的同序号脚本修改名字，
 或者是公共脚本的，改名并移入公共脚本区。 75_install_postgresql.sh
 75_postgresql_install_common.sh 113_natgateway.sh
 113_natgateway_interfaces.sh 113_natgateway_menu.sh
 113_natgateway_runtime.sh 155_install_cursor.sh
 155_install_cursor_backend.sh"

 So the user wants:
 1. Fix keyword matching (it should match keywords, not just numbers)
    — the bug fix.
 2. Menu searches only install_shells (already true).
 3. Same-number scripts in install_shells: rename them, or if they're
    common scripts, rename and move to the common area. Examples
    given: 75 group (2 scripts), 113 group (4 scripts), 155 group (2
    scripts).

 And my previous turn identified also 159 and 169 as duplicate
 groups.

 Then the current message: "Select script: 这里输入小写的b退出上一级
 菜单。b不作为关键词。" — lowercase b exits to parent menu. b is not
 a keyword.

 So the current message is about the b handling in the "Select
 script:" prompt. This is a refinement of the testselector behavior.

 Given the pending work from the previous message is substantial and
 incomplete, and the current message adds a small requirement, I
 should now complete ALL of it:

 1. Fix testselector matcher bug (shift input off candidates).
 2. Ensure lowercase b = back to parent menu; b never
    keyword-matches. (Keep q/Q too? The user didn't object to q.
    Current hint says "Press Enter (empty input), q or b to go back".
    I'll keep q/Q and lowercase b... and uppercase B? The user
    emphasized lowercase. Hmm. "输入小写的b退出上一级菜单。b不作为关
    键词。"

 Actually maybe the user is saying: currently B and b both exit; but
 they want ONLY lowercase b to exit, and... no wait, "b不作为关键词"
 = "b is not a keyword" — meaning don't use b for matching. If both b
 and B exit, that's fine too. But the user specifically says
 lowercase b. Maybe they want uppercase B to be matchable? E.g.,
 input "B" could match a script containing b? That seems overly
 subtle.

 Hmm, let me think about the practical scenario: the user wants to go
 back to the parent menu. They press "b". It works. They also want
 "b" never to match scripts — e.g., if someone types "b" expecting to
 search, they get back instead. Fine.

 But here's a REAL possible issue: what if the user typed "b" and it
 DIDN'T go back in their environment? Why not? Let me check read -r
 -p "Select script: " TEST_SELECTOR_INPUT...

 OH WAIT!!! I think I finally see a potential issue! read -r -p ...
 TEST_SELECTOR_INPUT — if there's a variable named
 TEST_SELECTOR_INPUT... no. Hmm.

 Actually, hmm, let me look at this from a different angle. The bash
 read with -p prompt. If the user's terminal... no, it's fine.

 OK, decision: I'll interpret the user's message as:
 1. Lowercase b (and keep q/Q) exits to parent menu — exact input
    match, checked BEFORE keyword matching (already the case).
 2. b is never used as a keyword for script matching.

 And since B is currently also a back key — the user says lowercase b
 specifically. To honor "b不作为关键词" while being conservative:
 keep both b and B as back keys? Or make ONLY lowercase b + q/Q?

 Hmm — "输入小写的b退出上一级菜单。b不作为关键词。" The most literal
 reading: "Typing lowercase b exits to the parent menu. b is not
 treated as a keyword."

 I think the user might have noticed that in my code, B is also back,
 but they want lowercase only... OR they just want to make sure b
 works. The safest interpretation that satisfies the literal request:
 lowercase b exits; b is not a keyword. Keep q/Q as back too (from
 the original design, hint mentions q). For uppercase B — hmm.

 Actually, wait. Let me reconsider: maybe the user typed "b" in the
 CURRENT buggy version and it did NOT exit! Let me trace VERY
 carefully with the bug:

 ```bash
   read -r -p "Select script: " TEST_SELECTOR_INPUT   # user types
 "b"
   test_selector_trim_input                            # "b"

   if [ -z "$TEST_SELECTOR_INPUT" ] || ... [ "$TEST_SELECTOR_INPUT" =
 "b" ] ...
 ```

 "b" = "b" → true → return 0. It exits. So b DOES work in the current
 version.

 Hmm so why would the user emphasize this? Maybe they're reading the
 code and clarifying requirements preemptively. OR maybe they tested
 "b" and it worked, and now they're specifying: "b exits to parent
 menu. b is not a keyword [so keep it that way]."

 OR — maybe they're telling me this because in my planned fix, I
 might have considered making all single letters matchable.

 You know what, I'll just make it clean and explicit:

 Back keys: lowercase b, q/Q, empty input. (Remove uppercase B? The
 user said lowercase b. If I keep B as back too, that contradicts
 nothing — user said lowercase b exits; they didn't say uppercase B
 must not exit. But "b不作为关键词" — lowercase b is not a keyword.
 What about uppercase B as a keyword? If B is a back key, it's also
 not a keyword. Hmm.)

 Simplest, most literal compliance:
 - lowercase b → back (not a keyword).
 - Keep q/Q → back.
 - Empty → back.
 - What about uppercase B? The user's message implies lowercase b is
   THE back letter. I'll keep uppercase B as back too — no wait...

 Ugh. Let me think about WHY the user wrote "小写" (lowercase)
 explicitly. If they just meant "the b key", they'd write "输入b退出
 ". Writing 小写的b (lowercase b) strongly suggests distinguishing
 from uppercase B. Why would that matter? Because... hmm, maybe they
 want uppercase B to be usable as a keyword? Like matching a script
 whose name contains "B"? But matching is case-insensitive (input
 lowered), so "B" as input would lowercase to "b" and match scripts
 containing "b" anyway!

 Hmm wait, that's a point: since matching lowercases the input, "B"
 and "b" are equivalent for matching purposes. If lowercase b is
 reserved for back but uppercase B were a keyword, they'd both search
 for "b"-containing names. That'd be confusing but the user might
 want exactly that: b (lowercase) = back; B (uppercase) = keyword
 search for "b"? That seems weird.

 Alternative: the user wants me to make the back key check
 case-sensitive lowercase-only, i.e., b → back, and remove B from
 back keys so... hmm, but then B would keyword-match.

 Honestly, I think the cleanest reading is: the user specifies that
 lowercase b is the back key and must never be treated as a script
 keyword. I'll implement:
 - b (lowercase) → back.
 - Also keep q, Q, empty → back (as originally designed and shown in
   the hint).
 - Remove uppercase B from the back keys? Hmm...

 Actually hold on. Let me re-read the menu hint in the current code:
 "Press Enter (empty input), q or b to go back". So the hint says q
 or b. The code checks q, Q, b, B. The user says lowercase b. I think
 keeping B as back is harmless BUT the user explicitly wrote
 lowercase.

 Decision: I'll make the back keys: empty input, q, Q, and lowercase
 b. Uppercase B will NOT be a back key — and since the user said "b不
 作为关键词" (lowercase b is not a keyword), what about uppercase B?
 Since matching lowercases input, "B" would search for names
 containing "b". Hmm, that would make lowercase b = back but
 uppercase B = search "b". That's a weird UX.

 Hmm hmm. Actually — wait. Maybe I should reconsider. Perhaps the
 cleanest: treat BOTH b and B as back (as now), and the user's "小写
 的b" is just how they type it casually. The critical requirement: "b
 不作为关键词" — b never keyword-matches. Both are satisfied if b/B
 are back keys.

 But then why emphasize 小写? ...

 OK here's yet another thought — maybe the user is telling me that in
 their test, they typed lowercase "b" and it did NOT go back! When
 would that happen with the current code?

 Let me look at the trim function once more:

 ```bash
   test_selector_trim_input() {

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT#"${TEST_SELECTOR_INPUT%%[
 ![:space:]]*}"}"

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT%"${TEST_SELECTOR_INPUT##*
 [![:space:]]}"}"
   }
 ```

 Input "b": first line: ${TEST_SELECTOR_INPUT%%[![:space:]]*} —
 remove longest suffix matching pattern [![:space:]]*... wait,
 %%pattern removes the longest matching SUFFIX. Pattern [![:space:]]*
  matches a non-space char followed by anything. For "b": the suffix
 "b" matches (b is non-space, then * matches empty). Longest such
 suffix... "b" itself. So "b"#"b" = "". Wait no:
 TEST_SELECTOR_INPUT%%[![:space:]]* on "b" gives "" (whole string is
 a matching suffix: starts with non-space 'b', then * matches rest
 which is empty). So result of #"" removal: "b"#"​" — removing empty
 prefix = "b". OK good.

 Second line: ${TEST_SELECTOR_INPUT##*[![:space:]]} — remove longest
 PREFIX matching *[![:space:]] (anything ending with non-space). For
 "b": prefix "b" matches. So ## gives "". Then "b"%"" = "b". Fine.

 So trim("b") = "b". Back condition catches it. b WORKS in the
 current code.

 Hmm OK. Unless the user is running a cached/old copy. Whatever — the
 code is correct for b.

 Let me now decide the implementation:

 ```bash
   if [ -z "$TEST_SELECTOR_INPUT" ] || [ "$TEST_SELECTOR_INPUT" = "q"
  ] || [ "$TEST_SELECTOR_INPUT" = "Q" ] || [ "$TEST_SELECTOR_INPUT" =
 "b" ]; then
       return 0
   fi
 ```

 Wait, should I keep Q? q? The user hasn't objected. The hint says "q
 or b". I'll keep q/Q and b. For B — hmm.

 Actually, let me look at this from the "what did the user
 experience" angle one final time. The user's last reported output
 was:

 ```
   Select script: (they typed something like
 "175_laravel_main_start")
   Executing: 175_laravel_main_start
   Path: 175_laravel_main_start
   Error: Script not found: 175_laravel_main_start
 ```

 Then their message: "是用关键字和序号匹配，不仅仅是序号。关键字一样
 可以匹配。" — "It should match by keyword AND number, not only
 number. Keywords should match too."

 AH — I see! The user's perception: they typed
 "175_laravel_main_start" (a keyword/name) and it FAILED ("Script not
 found"). They concluded the matcher only works with numbers ("不仅仅
 是序号" — not ONLY the number). Because of my bug (input as
 candidate 0), text input matched ITSELF → "Script not found". So
 they think keyword matching isn't implemented!

 So the pending fix: make keyword matching actually work (fix the
 shift bug). That's the previous message's request #1.

 Then the current message: "Select script: 这里输入小写的b退出上一级
 菜单。b不作为关键词。" — At the "Select script:" prompt, lowercase b
 exits to the parent menu; b is not a keyword.

 So the user is SPECIFYING the back-key behavior (maybe they saw "q
 or b" in the hint and are confirming/refining: lowercase b = back).
 Since matching is case-insensitive-substring, a bare "b" would
 otherwise match MANY scripts (any name containing "b" — like
 "1_stop_cloud_monitors"? no b... "11_cuda_nvidia_prereq" no b...
 "13_ensure_python" no b... but many others like "bash", "backup",
 "lib" do contain b). The user wants "b" reserved for back.

 Current code: b and B are back keys, checked before matching. So "b
 不作为关键词" is already satisfied for b. The current message might
 be the user just confirming desired behavior — OR specifying
 lowercase-only.

 FINAL DECISION: Implement it explicitly and clearly:
 - Back keys: empty input, q, Q, b (lowercase). Keep B too? The user
   said lowercase b. To be safe and literal, I'll treat BOTH b and B
   as back (current behavior) — no wait, "输入小写的b" is explicit
   about lowercase...

 Ugh. OK let me just go with the most literal: lowercase b is a back
 key (not a keyword). Uppercase B...

 You know what — practical consideration: if uppercase B is NOT a
 back key, then typing "B" triggers keyword search, which lowercases
 to "b" and matches the FIRST script containing "b". Is there any
 script containing "b"? Let me check:
 "191_install_octane_watcher_daemon.sh"? no b... wait "watcher" no b.
 Hmm: "bash"? Script names with 'b': "155_install_cursor_backend.sh"
 (backend has b!), "159_install_gitea_backend.sh",
 "77_ensure_php_pgsql"? no. Many have "b" actually (backend, bash,
 lib, hub, web...).

 Honestly, the user's core intent: b = back, not a search term. If
 they type uppercase B, most likely they meant back too (typo or caps
 lock). Treating both as back is more forgiving. But the user
 SPECIFICALLY wrote lowercase.

 Hmm, let me think about "b不作为关键词" again — "b is not used as a
 keyword". If B were a keyword, then B (lowered to "b") matches
 scripts — but that's "B作为关键词" not "b作为关键词". So strictly:
 lowercase b not a keyword. Uppercase B unspecified → could be
 keyword.

 Argh. Let me just make a pragmatic choice: keep the back keys as
 empty/q/Q/b/B (current), since the user's request "lowercase b exits
 to parent menu; b is not a keyword" is FULLY satisfied. The mention
 of lowercase is just describing the key. I won't overthink...

 Hmm, wait, no. Actually — one more consideration. What if the user
 REMOVED the b handling in their mind... no, they're asking to ADD
 it. "这里输入小写的b退出上一级菜单" — "Here, entering lowercase b
 exits to the parent menu". They want this behavior. It exists.

 Hmm, but if it already exists, why would they ask?? Unless it
 DOESN'T work in their environment! Let me check the actual current
 file state once more... The read shows:

 ```bash
   if [ -z "$TEST_SELECTOR_INPUT" ] || [ "$TEST_SELECTOR_INPUT" = "q"
  ] || [ "$TEST_SELECTOR_INPUT" = "Q" ] || [ "$TEST_SELECTOR_INPUT" =
 "b" ] || [ "$TEST_SELECTOR_INPUT" = "B" ]; then
       return 0
   fi
 ```

 Yes it's there. So b DOES exit. Unless... the user is running the
 version WITHOUT my edits? They reported the "175_laravel_main_start"
 bug which only exists in my edited version. So they run my version.
 b works.

 WAIT. Unless... hmm, what if read -r -p "Select script: "
 TEST_SELECTOR_INPUT has an issue when the user types "b" then Enter
 — with -r no backslash processing. TEST_SELECTOR_INPUT="b". Works.

 OK here's my FINAL final interpretation: The user is specifying
 requirements for the pending fix (which I haven't applied yet — the
 shift bug fix). They're telling me:
 1. (From previous message, still pending) Keyword matching must work
    alongside number matching.
 2. (Current message) Lowercase b at the prompt = exit to parent
    menu. b is reserved, never a keyword.

 So my job now:
 1. Apply the shift bug fix (keyword matching works).
 2. Ensure b is a back key and not a keyword — keep the existing
    back-key check BEFORE matching (b, and I'll keep q/Q/B as well;
    the user's lowercase-b emphasis is satisfied).
 3. Update the hint text to mention lowercase b explicitly: "Press
    Enter (empty), q or b to go back".
 4. Also — should I complete the reorganization work
    (75/113/155/159/169 duplicate groups) from the previous message?

 Hmm. The previous message asked for it, and I never completed it.
 The current message doesn't mention it. But the task is pending. Let
 me complete it all: fix testselector + reorganize duplicates.

 Hmm, wait. Let me re-read the previous message's examples again:
 "75_install_postgresql.sh 75_postgresql_install_common.sh
 113_natgateway.sh 113_natgateway_interfaces.sh
 113_natgateway_menu.sh 113_natgateway_runtime.sh
 155_install_cursor.sh 155_install_cursor_backend.sh"

 And the current message is only about the b key.

 I think the right scope for THIS turn: the user's current message is
 narrowly about the b key. But the previous turn's work is incomplete
 — the user is probably waiting for BOTH. Actually, in agentic
 coding, each user message typically expects the assistant to
 continue pending work unless redirected. The current message doesn't
 cancel the reorganization; it adds a detail.

 Let me do the full job:
 1. Fix testselector matcher bug (keyword + number matching).
 2. b = back key (lowercase emphasized in hint).
 3. Reorganize duplicate-prefix groups in install_shells:
     - 75: 75_install_postgresql.sh + 75_postgresql_install_common.sh
       → check if common is a library → move to common/
     - 113: 113_natgateway.sh + 113_natgateway_interfaces.sh +
       113_natgateway_menu.sh + 113_natgateway_runtime.sh → analyze
     - 155: 155_install_cursor.sh + 155_install_cursor_backend.sh →
       analyze
     - 159: 159_install_gitea.sh + 159_install_gitea_backend.sh →
       analyze
     - 169: 169_install_rustdesk_server_1.1.14.sh +
       169_rustdesk_server_client_config.sh → analyze

 For each duplicate: if it's a sourced library / shared helper →
 rename + move to common/. If it's an install step dispatched
 standalone → renumber to a unique free number.

 Wait, but the user's examples only listed 75, 113, 155. My grep
 found also 159 and 169. The user said "同序号脚本修改名字"
 (same-number scripts rename) — general rule, with examples. I should
 handle ALL duplicate groups: 75, 113, 155, 159, 169.

 Let me analyze each group now. First, let me look at the files.

 Plan:
 1. Fix testselector.sh (shift bug + hint text with lowercase b).
 2. Analyze 75 group: grep references of
    75_postgresql_install_common.sh.
 3. Analyze 113 group.
 4. Analyze 155 group.
 5. Analyze 159 group.
 6. Analyze 169 group.
 7. Apply moves/renames + update references.

 Let me start by fixing testselector, then investigating the groups.

 For testselector fix:

 ```bash
   find_matching_script() {
       local user_input="$1"
       shift
       local -a candidate_scripts=("$@")
       local input_lower="${user_input,,}"
       ...
   }
 ```

 And the call stays: find_matching_script "$TEST_SELECTOR_INPUT"
 "${scripts[@]}".

 Also update hint: "Press Enter (empty input), q or b to go back" —
 mention lowercase b. And back keys: keep b/B/q/Q? The user says
 lowercase b. Hmm — let me keep it minimal-risk: lowercase b, q, Q,
 empty. Remove uppercase B?

 "输入小写的b退出上一级菜单。b不作为关键词。"

 I'll interpret: lowercase b = back. Uppercase B — since matching
 lowercases, B would search "b"-containing scripts. That asymmetry is
 weird but literally matches the user's spec. Hmm hmm hmm.

 Actually NO WAIT. Let me re-read ONE more time. "这里输入小写的b退出
 上一级菜单。b不作为关键词。"

 Maybe... the user is describing observed behavior?? "Here, typing
 lowercase b exits to the parent menu. b is not a keyword." As in,
 they TESTED b and it went back — and they're OK with it, just
 confirming b is reserved (not a search term).

 In that case, current code is already correct for b! And the user's
 message is a CONFIRMATION, not a change request. But then why send
 it?

 OR: The user typed "b" expecting to search for something starting
 with b... no.

 OR: The user noticed the hint text says "q or b" and wants to
 confirm b works — and it does.

 Hmm, one more angle: maybe the user typed "b" and got "No matching
 script found for: b" instead of going back?! That would happen if...
 the back condition failed. When? If TEST_SELECTOR_INPUT isn't
 exactly "b" — e.g., if they typed "b " (trailing space, trimmed OK)
 or "b\r" (CRLF!)...

 CRLF! If the file has CRLF line endings, read -r strips \n but keeps
 \r! Then TEST_SELECTOR_INPUT="b\r" — the back check fails, trim
 doesn't remove \r ([![:space:]] — is \r a space class? In bash,
 [[:space:]] includes \r? POSIX space includes CR! Yes: [:space:] =
 space, tab, newline, vertical tab, form feed, carriage return. So \r
 IS in [:space:] and would be trimmed!). Hmm, so trim handles \r.

 But wait — would trim actually work? The trim function removes
 leading/trailing [[:space:]] chars. \r is [:space:]. So "b\r" → "b".
 OK fine.

 I'm going in circles. Let me just look at the code once more and
 check the b handling...

 Actually, hmm, hold on. Let me look at the trim function REALLY
 carefully for the "b" case:

 ```bash

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT#"${TEST_SELECTOR_INPUT%%[
 ![:space:]]*}"}"
 ```

 For input "b":
 - Inner: ${TEST_SELECTOR_INPUT%%[![:space:]]*} — %% removes the
   LONGEST suffix matching [![:space:]]*.
 - Pattern [![:space:]]*: one non-space char followed by anything.
 - For "b": suffixes are "" and "b". "b" matches (b = non-space, * =
   empty). Longest matching suffix = "b". So inner = "b" - "b" = ""
   ...

 wait no! %% removes the longest matching suffix FROM THE VALUE.
 ${var%%pattern} gives var with the longest suffix matching pattern
 REMOVED. So ${TEST_SELECTOR_INPUT%%[![:space:]]*} = "b" with longest
 suffix matching [![:space:]]* removed. The suffix "b" matches →
 removed → result "".
 - Outer: ${TEST_SELECTOR_INPUT#""} = remove shortest prefix "" →
   "b". ✓.

 Second line:

 ```bash

 TEST_SELECTOR_INPUT="${TEST_SELECTOR_INPUT%"${TEST_SELECTOR_INPUT##*
 [![:space:]]}"}"
 ```

 - Inner: ${TEST_SELECTOR_INPUT##*[![:space:]]} = "b" with longest
   PREFIX matching *[![:space:]] removed. Prefix "b" matches ( * =
   empty, [![:space:]] = b). → result "".
 - Outer: ${TEST_SELECTOR_INPUT%""} = "b". ✓.

 Great, trim works. b → back. The current code is CORRECT for
 lowercase b.

 SO — the user's message must be a specification (perhaps they
 haven't tested b, or they tested and it worked and they're
 documenting). Either way, current code satisfies it. The REAL
 pending work is the keyword-matching bug fix + reorganization. And
 the current message reminds me to keep b as back while I fix the
 matcher.

 Hmm wait, actually — hold on. Let me reconsider whether the user's
 message might indicate b DOESN'T work in their run. What if they're
 running the file with different content — like what if my first-turn
 edit only PARTIALLY applied? Let me verify the actual current
 file... The read from the previous turn shows the full current
 content — b handling present. OK.

 Decision made. Now the work plan for this turn:

 Task 1: Fix testselector.sh keyword matching (the shift bug)
 - find_matching_script must shift off the input before building
   candidates.

 - Keep back keys: empty, q, Q, b (lowercase per user), and B? — I'll
   keep b/B/q/Q as-is (superset of user's request, harmless). Hmm, no
   wait — "b不作为关键词" — if B is also not a keyword (it's a back
   key), that doesn't violate anything. The user said lowercase b
   exits — B also exiting doesn't contradict. I'll keep all.

   Hmm, actually, let me simplify and be precise: the user emphasized
   lowercase b. Maybe in a future message they'd want B as keyword.
   But honestly, keeping B as back is the safer UX. Keep: empty, q,
   Q, b, B.

   Wait, one more consideration — maybe the user's point is: "b"
   should exit even though some script names contain "b" — i.e.,
   DON'T let the keyword matcher take precedence over the back key.
   My code checks back FIRST, so fine.

 - Update the hint text: "Press Enter (empty input), q or lowercase b
   to go back" — make it clear.

 Task 2: Reorganize duplicate-prefix groups in install_shells (75,
 113, 155, 159, 169)

 The user's rule: "同序号脚本修改名字，或者是公共脚本的，改名并移入公
 共脚本区" — same-number scripts: rename them [to unique numbers], or
 if they're common scripts, rename and move into the common script
 area.

 Let me analyze each group:

 75 group: 75_install_postgresql.sh (step) +
 75_postgresql_install_common.sh.
 - Need to check: is 75_postgresql_install_common.sh a sourced
   library? Who references it? If only 75_install_postgresql.sh
   sources it → move to common/ as postgresql_install_common.sh.
 - Actually wait — maybe OTHER scripts source it too (that's why it's
   "common"). Let me grep.

 113 group: 113_natgateway.sh, 113_natgateway_interfaces.sh,
 113_natgateway_menu.sh, 113_natgateway_runtime.sh.
 - These look like one entry (natgateway.sh) + 3 sub-scripts
   (interfaces, menu, runtime). But ALSO — remember dd.sh has
   ROUTER_SCRIPT="$SHELLS_DIR/linux/debian/install_shells/103_lnxrout
   er.sh" and my first-turn read of dd.sh mentioned
   113_natgateway.sh... Let me check how they're used. If
   113_natgateway.sh is the step and others are dispatched helpers →
   the helpers could move to common/ OR debian_com/. Hmm, "公共脚本区
   " for the user = scripts/shells/linux/common/.

 Hmm wait, but earlier for the 175 case, the plane sub-scripts
 (175_laravel_main_start_frankenphp.sh etc.) live in debian_com — a
 different "common" area. For 113, if the sub-scripts are only used
 by 113_natgateway.sh, where should they go? The user said "公共脚本
 区" (common script area) = scripts/shells/linux/common/ presumably
 (that's where I moved laravel_main_runtime_common.sh and the user
 was satisfied).

 But careful: are 113_natgateway_*.sh standalone install steps too?
 Like, does the menu ever run them directly? They're in
 install_shells, so they appear in the menu. The user wants the menu
 to have ONE script per number. Let me read them to understand.

 155 group: 155_install_cursor.sh + 155_install_cursor_backend.sh.
 - Probably: cursor.sh installs cursor (the editor),
   cursor_backend.sh installs the backend service? Or cursor.sh
   sources backend.sh? Need to read.

 159 group: 159_install_gitea.sh + 159_install_gitea_backend.sh —
 similar to 155.

 169 group: 169_install_rustdesk_server_1.1.14.sh +
 169_rustdesk_server_client_config.sh — rustdesk server install +
 client config. Probably one invokes the other, or they're
 independent steps sharing the 169 prefix.

 Let me investigate all of them now. Strategy for each:
 - If script A (the non-primary) is SOURCED by script B or others →
   it's a library → move to common/ with proper name (drop number),
   update references.
 - If script A is EXECUTED (bash A / ./A) by B with args as a
   sub-phase → it's a dispatched sub-script. Hmm — for the 175 case
   earlier, the sub-scripts lived in debian_com (not install_shells),
   so the menu didn't show them. For 113/155/159/169, the sub-scripts
   ARE in install_shells (showing in the menu as duplicates). The
   user wants: same-number scripts renamed (unique numbers) or moved
   to common if they're common.

 For dispatched sub-scripts, moving to common/ works (they're shared
 helpers of the main step, not independent menu steps). Renaming with
 unique numbers would make them appear as separate menu steps — but
 they're NOT independent steps (they might need args/context). Hmm,
 depends on the script.

 Let me READ the files. Start with the analysis greps and file heads.

 Also note: I must check references from OUTSIDE too (dd.sh, menu
 scripts, gvar files, systemd units, etc.) for every file I
 move/rename — update all references repo-wide (excluding
 docs_fix/origin logs which are historical snapshots, and excluding
 node_modules).

 Let me also double-check: the user said "菜单搜索的只有
 scripts\shells\linux\debian\install_shells中的" — the menu searches
 ONLY install_shells. Already true in testselector
 (get_install_shells_dir). After moving libs out, the menu shows only
 real steps.

 Now, about which number to renumber to if needed: the current max
 number is 999 (999_check_circular_symlinks.sh) and 193
 (193_install_terminal_grid_shortcut.sh). Available numbers: 195,
 197, etc. (odd numbers preferred — the sequence is odd:
 1,3,5,...,193,999). Let me check what's used: from the earlier
 listing: ..., 191_install_pycore_http_service.sh,
 193_install_terminal_grid_shortcut.sh, then
 999_check_circular_symlinks.sh. So 195, 197, 199... are free. Also
 43,45,47,49 (between 41 and 51), 63,65,67 (between 61 and 69?), let
 me check: 61_install_android_studio.sh then what? The listing cut
 off at 61. Let me get the full list to find free numbers.

 But WAIT — before renumbering an install step, consider ordering
 dependencies: steps run in numeric order during "Install the server"
 (execute_installation_scripts runs ALL scripts in sorted order!). So
 renumbering changes execution order. The sub-scripts
 (backend/config) probably should NOT run standalone in the full
 install sequence... but if they're in install_shells, they DO run
 during full install! Hmm, e.g., 155_install_cursor.sh AND
 155_install_cursor_backend.sh both run during "Install the server"
 (sorted: cursor then cursor_backend? sort -t: -k1,1n -k2 →
 155:155_install_cursor.sh before 155:155_install_cursor_backend.sh).

 So these same-number scripts are likely BOTH real steps in the
 full-install sequence, deliberately ordered (backend first? cursor
 first?). Hmm, actually sort: "155_install_cursor.sh" vs
 "155_install_cursor_backend.sh" — alphabetical tiebreak:
 "155_install_cursor.sh" vs "155_install_cursor_backend.sh" — compare
 "cursor." vs "cursor_" — '.' (46) < '_' (95), so
 155_install_cursor.sh runs FIRST. Hmm interesting.

 But the user wants unique numbers. So for true install steps sharing
 a number → renumber to keep order: e.g., 155_install_cursor.sh → 155
 stays, 155_install_cursor_backend.sh → 156? But even numbers are
 unused in the scheme (all odd). Hmm — or move backend to the next
 free odd number with correct ordering.

 Hmm wait, wait. Let me re-read the user's exact words:
 "scripts\shells\linux\debian\install_shells中的同序号脚本修改名字，
 或者是公共脚本的，改名并移入公共脚本区。"

 "Same-number scripts in install_shells: rename them; or if they are
 common scripts, rename and move into the common script area."

 So: for same-number groups: EITHER rename (the non-primary one gets
 a new unique number, maintaining order), OR if common → move to
 common area.

 For each group I need to decide which scripts are "common"
 (libraries/helpers) vs "steps". Let me read the files now.

 Let me be systematic. First, references for each candidate:
 - grep "75_postgresql_install_common" repo-wide
 - grep "113_natgateway" repo-wide
 - grep "155_install_cursor" repo-wide
 - grep "159_install_gitea" repo-wide
 - grep "169_rustdesk|169_install_rustdesk" repo-wide

 Then read file heads to understand nature.

 Let me do this investigation now. I'll run a bunch of greps in
 parallel.

 Also — IMPORTANT: I still haven't applied the testselector fix from
 the previous turn! Let me do that first (it's quick), then
 investigate the groups.

 Testselector fix:

 ```bash
   find_matching_script() {
       local user_input="$1"
       shift
       local -a candidate_scripts=("$@")
       local input_lower="${user_input,,}"
       local candidate=""
       local candidate_name=""

       TEST_SELECTOR_MATCHED_SCRIPT=""

       if [[ "$input_lower" =~ ^[0-9]+$ ]]; then
           for candidate in "${candidate_scripts[@]}"; do
               candidate_name="$(basename "$candidate")"
               if [[ "$candidate_name" =~ ^${input_lower}_ ]]; then
                   TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
                   return 0
               fi
           done
       fi

       for candidate in "${candidate_scripts[@]}"; do
           candidate_name="$(basename "${candidate,,}")"
           if [[ "$candidate_name" == *"$input_lower"* ]]; then
               TEST_SELECTOR_MATCHED_SCRIPT="$candidate"
               return 0
           fi
       done

       return 1
   }
 ```

 And update the hint: "Press Enter (empty input), q or b to go back"
 → make explicit lowercase b. Actually current hint: "Press Enter
 (empty input), q or b to go back". Fine — maybe "Press Enter
 (empty), q, or lowercase b to go back". Keep simple: "Press Enter
 (empty input), q or b to go back" is fine, but since user emphasized
 lowercase, I'll write "q or lowercase b".

 Now the back condition — keep b, B, q, Q, empty. Hmm, the user said
 lowercase b. If I keep B as back too... Let me think once more. "输
 入小写的b退出上一级菜单。b不作为关键词。"

 You know, I now think there's a real possibility the user typed
 uppercase B or some other variant and it didn't exit, or they're
 pre-specifying. The SAFEST choice honoring the letter of the
 request: lowercase b exits (required). For B — the user didn't ask
 for it. If I make B a keyword (search "b"), a user typing B gets a
 script search — harmless. If I make B back, also harmless.

 The user's "b不作为关键词" refers to lowercase b. For symmetry and
 least surprise, I'll keep ONLY lowercase b as the letter back key
 (plus q/Q and empty). Uppercase B will fall through to keyword
 matching (which lowercases it → matches scripts containing "b"). Hmm
 — but wait, is that what user wants? "输入小写的b退出" — only
 lowercase b exits...

 Ugh, I keep flip-flopping. FINAL: lowercase b = back. q/Q = back
 (established). Empty = back. Uppercase B = keyword search
 (lowercased). This EXACTLY matches the user's literal spec. And the
 hint will say: "Press Enter (empty input), q or lowercase b to go
 back".

 Hmm, hmm. Actually no wait. Let me think about the UX consequence of
 B-as-keyword: typing B → matches first script containing "b". Which
 script? Sorted order: 1_stop_cloud_monitors.sh (no b)...
 113_natgateway.sh? no b... Actually "113_natgateway_interfaces.sh"
 no b. Hmm which is first with 'b'? Whatever — it runs some script.
 That's the designed keyword behavior. Fine.

 OK moving on. Now the duplicate groups investigation. Let me run
 greps.

 Actually — one more thing about the testselector. The user's
 previous message ALSO said: "同时菜单搜索的只有
 scripts\shells\linux\debian\install_shells中的" — menu searches ONLY
 install_shells. Currently true. After I move common scripts OUT of
 install_shells into common/, they disappear from the menu — which is
 the point.

 And ALSO — hmm, "是用关键字和序号匹配，不仅仅是序号。关键字一样可以
 匹配。" — with the shift fix, keyword matching works. Good.

 Now let me also double check the digit-prefix logic against the
 duplicates I'm about to fix. After reorganization, each number maps
 to exactly one script, so "93" → 93_install_frankenphp.sh
 unambiguous.

 Let me now run the investigation greps for the duplicate groups.
 Then decide moves. Then implement everything.

 Greps to run (repo-wide, exclude node_modules, .git):
 1. grep -rn "75_postgresql_install_common"
    --exclude-dir={node_modules,.git} .
 2. grep -rn "113_natgateway" ...
 3. grep -rn "155_install_cursor" ...
 4. grep -rn "159_install_gitea" ...
 5. grep -rn "169_rustdesk\|169_install_rustdesk" ... — need -E or
    separate.

 Also read heads of these files to classify library vs step.

 Let me also get the full install_shells listing with numbers to find
 free numbers for renumbering.

 Let me apply the pending matcher fix first (keyword matching was
 broken — the input itself leaked into the candidate list as element
 0, which is why typing 175_laravel_main_start "matched" itself and
 failed), keeping lowercase b as a reserved back key, then
 investigate the duplicate-number groups:


 edit
 /www/programing/core_node/scripts/shells/linux/common/testselector.s
 h

      ...
   69 # Digits: exact numeric-prefix match first (93 -> 93_x.sh, not
 193_x.sh), then substring fallback.
   70 # Text: case-insensitive substring match on the script
 basename.
   71 # Result stored in TEST_SELECTOR_MATCHED_SCRIPT; returns 0 on
 match, 1 otherwise.
   72 find_matching_script() {
 + 73     local user_input="$1"
 + 74     shift
   73     local -a candidate_scripts=("$@")
 - 74     local input_lower="${TEST_SELECTOR_INPUT,,}"
 + 76     local input_lower="${user_input,,}"
   75     local candidate=""
   76     local candidate_name=""
   77
   78     TEST_SELECTOR_MATCHED_SCRIPT=""
      ...
  157         echo "=========================================="
  158         show_test_scripts_context
  159         echo ""
  160         echo "Enter a number or name to select a script (first
 match runs):"
 -161         echo "Press Enter (empty input), q or b to go back"
 +163         echo "Press Enter (empty input), q or lowercase b to go
 back"
  162         echo ""
  163         for script_index in "${!scripts[@]}"; do
  164             echo "  $(basename "${scripts[$script_index]}")"
  165         done
  166         echo ""
  167         read -r -p "Select script: " TEST_SELECTOR_INPUT
  168         test_selector_trim_input
  169
 -170         if [ -z "$TEST_SELECTOR_INPUT" ] || [
 "$TEST_SELECTOR_INPUT" = "q" ] || [ "$TEST_SELECTOR_INPUT" = "Q" ]
 || [ "$TEST_SELECTOR_INPUT" = "b" ] || [ "$TEST_SELECTOR_INPUT" =
 "B" ]; then
 +172         # Back keys: empty input, q/Q, and lowercase b (b is
 reserved and never
 +173         # participates in keyword matching). Uppercase B still
 matches by keyword.
 +174         if [ -z "$TEST_SELECTOR_INPUT" ] || [
 "$TEST_SELECTOR_INPUT" = "q" ] || [ "$TEST_SELECTOR_INPUT" = "Q" ]
 || [ "$TEST_SELECTOR_INPUT" = "b" ]; then
  171             return 0
  172         fi
  173
  174         if find_matching_script "$TEST_SELECTOR_INPUT"
 "${scripts[@]}"; then
      ...



 $ bash -n scripts/shells/linux/common/testselector.sh && echo
 "SYNTAX OK"; cd scripts/shells/linux/common && {
 sed -n '/^# Trim leading/,/^}/p' testselector.sh
 sed -n '/^# Match TEST_SELECTOR_INPUT/,/^}/p' testselector.sh
 cat <<'EOF'

 scripts=()
 while IFS= read -r -d $'\0' file; do
     filename=$(basename "$file")
     if [[ $filename =~ ^([0-9]+)_ ]]; then
 prefix=${BASH_REMATCH[1]}; scripts+=("$prefix:$file"); fi
 done < <(find ../debian/install_shells -maxdepth 1 -name "*.sh"
 -print0)
 IFS=$'\n' sorted=($(sort -t: -k1,1n -k2,2 <<<"${scripts[*]}"));
 unset IFS
 paths=(); for item in "${sorted[@]}"; do paths+=("${item#*:}"); done
 for input in "93" "193" "voxcpm" "175_laravel_main_start" "b" "zzz"
 "999"; do
     TEST_SELECTOR_INPUT="$input"
     test_selector_trim_input
     if find_matching_script "$TEST_SELECTOR_INPUT" "${paths[@]}";
 then
         echo "[$input] -> $(basename
 "$TEST_SELECTOR_MATCHED_SCRIPT")"
     else
         echo "[$input] -> NO MATCH"
     fi
 done
 exit 0
 EOF
 } | bash

 ... (3 earlier lines, ctrl+o to expand)
 [voxcpm] -> 147_install_voxcpm2.sh
 [175_laravel_main_start] -> 175_laravel_main_start.sh
 [b] -> 3_setting_base.sh
 [zzz] -> NO MATCH
 [999] -> 999_check_circular_symlinks.sh

 Took 1.4s


 The matcher now works correctly:
 - "93" → 93_install_frankenphp.sh ✓
 - "voxcpm" → 147_install_voxcpm2.sh ✓
 - "175_laravel_main_start" → 175_laravel_main_start.sh (full path,
   with .sh) ✓ — the user's reported bug is fixed
 - "b" as a keyword would match 3_setting_base.sh — BUT in the actual
   menu, lowercase "b" is intercepted by the back-key check BEFORE
   matching (my test bypassed the menu loop, so it shows what keyword
   matching WOULD do; in the real flow b returns to parent menu
   first). Good — "b不作为关键词" is enforced by the back-key check
   ordering.

 Wait — let me make sure the real menu flow intercepts "b" before
 matching. In test_scripts_main:

 ```bash
   # Back keys: empty input, q/Q, and lowercase b ...
   if [ -z "$TEST_SELECTOR_INPUT" ] || [ "$TEST_SELECTOR_INPUT" = "q"
  ] || ... [ "$TEST_SELECTOR_INPUT" = "b" ]; then
       return 0
   fi
   if find_matching_script ...
 ```

 Yes — "b" returns 0 before any matching. Confirmed.

 Now the second part: the duplicate-number groups in install_shells:
 75, 113, 155, 159, 169. Let me investigate each group's nature
 (library vs step) and references.

 Keyword matching is fixed (175_laravel_main_start now resolves to
 the full path; lowercase b is intercepted by the back-key check
 before any matching, so it never acts as a keyword). Now let me
 analyze the duplicate-number groups (75, 113, 155, 159, 169):


 $ cd /www/programing/core_node && for p in
 "75_postgresql_install_common" "113_natgateway" "155_install_cursor"
 "159_install_gitea" "169_rustdesk" "169_install_rustdesk"; do echo
 "=== $p ==="; grep -rln "$p" --exclude-dir=node_modules
 --exclude-dir=.git scripts/ dd.sh 2>/dev/null; done

 ... (25 earlier lines, ctrl+o to expand)
 er_1.1.14.sh
 === 169_install_rustdesk ===
 scripts/shells/linux/debian/install_shells/169_install_rustdesk_serv
 er_1.1.14.sh
 scripts/shells/linux/menu_itemshells/app_install_menu.sh

 Took 8.5s


 Let me dig into each group:

 75 group: 75_postgresql_install_common.sh referenced only by
 75_install_postgresql.sh. Check how (sourced?) and whether it's a
 library. Also check who references 75_install_postgresql itself
 (175_laravel_main_start.sh does — POSTGRES_INSTALL_SCRIPT).

 113 group: References in selector_common.sh,
 dd_helper/natgateway_helper.sh, dd_helper/linux_management.sh. Need
 to see which specific file names are referenced. Let me grep with
 line numbers.

 155 group: 155_install_cursor.sh referenced by
 desktop_electron_ime_compat.sh, 173_install_chinese_wubi.sh,
 155_install_cursor_backend.sh, app_install_menu.sh. And
 155_install_cursor_backend.sh — who references IT? Need to check
 separately (the grep for "155_install_cursor" matches both since
 backend name contains "155_install_cursor"). Let me grep
 specifically "155_install_cursor_backend".

 159 group: similar. Grep "159_install_gitea_backend" separately.

 169 group: 169_install_rustdesk_server_1.1.14.sh referenced by
 app_install_menu.sh; 169_rustdesk_server_client_config.sh — check
 who references it (grep showed only the server file itself matches
 "169_rustdesk"? The grep "169_rustdesk" found only
 169_install_rustdesk_server_1.1.14.sh — meaning
 "169_rustdesk_server_client_config" is referenced NOWHERE? Wait,
 "169_rustdesk" as a pattern matches
 "169_rustdesk_server_client_config.sh" too — but the result only
 shows the server file. That means nothing references
 169_rustdesk_server_client_config.sh by name (not even the server
 script!). Hmm, interesting — maybe the server script references it
 differently (e.g., via variable). Let me check inside
 169_install_rustdesk_server_1.1.14.sh for "client_config".

 Let me get details with line numbers.


 $ cd /www/programing/core_node && echo "=== how 75 common is used
 ==="; grep -n "75_postgresql_install_common"
 scripts/shells/linux/debian/install_shells/75_install_postgresql.sh;
 echo; echo "=== 113 refs detail ==="; grep -n "113_natgateway"
 scripts/shells/linux/common/selector_common.sh
 scripts/shells/linux/dd_helper/natgateway_helper.sh
 scripts/shells/linux/dd_helper/linux_management.sh
 scripts/shells/linux/debian/install_shells/113_natgateway*.sh
 2>/dev/null | grep -v
 "113_natgateway_\(interfaces\|menu\|runtime\).sh:" | head -30; echo;
 echo "=== 155 backend refs ==="; grep -rn
 "155_install_cursor_backend" --exclude-dir=node_modules scripts/ |
 grep -v Binary; echo; echo "=== 159 backend refs ==="; grep -rn
 "159_install_gitea_backend" --exclude-dir=node_modules scripts/ |
 grep -v Binary; echo; echo "=== 169 client_config refs ==="; grep
 -rn "rustdesk_server_client_config" --exclude-dir=node_modules
 scripts/ | grep -v Binary

 ... (32 earlier lines, ctrl+o to expand)

 === 169 client_config refs ===
 scripts/shells/linux/debian/install_shells/169_install_rustdesk_serv
 er_1.1.14.sh:32:RUSTDESK_SERVER_CLIENT_CONFIG="$SCRIPT_CURRENT_DIR/1
 69_rustdesk_server_client_config.sh"

 Took 5.5s


 Now the structure is clear. Let me classify each duplicate:

 75 group: 75_postgresql_install_common.sh — sourced by
 75_install_postgresql.sh (need to verify it's sourced, line 5
 assigns POSTGRESQL_INSTALL_COMMON; let me check where it's used).
 It's a library → move to common/ as postgresql_install_common.sh.

 113 group: 113_natgateway.sh is the main step (referenced by
 selector_common.sh, natgateway_helper.sh, linux_management.sh). It
 references three modules: 113_natgateway_runtime.sh,
 113_natgateway_interfaces.sh, 113_natgateway_menu.sh — as MODULES
 (likely sourced). These are libraries/helpers of the main step →
 move to common/ with names like natgateway_runtime.sh,
 natgateway_interfaces.sh, natgateway_menu.sh. Hmm — but are they
 sourced or executed? "NATGATEWAY_RUNTIME_MODULE" suggests sourced
 module. Need to check. Also need to check if these modules are
 referenced elsewhere (the earlier grep showed "113_natgateway"
 matches only in those files: selector_common, natgateway_helper,
 linux_management reference only 113_natgateway.sh; the modules are
 referenced only by 113_natgateway.sh). Let me verify how the modules
 are used in 113_natgateway.sh (source vs bash).

 155 group: 155_install_cursor_backend.sh — referenced ONLY by
 155_install_cursor.sh as CURSOR_INSTALL_BACKEND (line 27). Is it
 sourced or executed? Need to check. "backend" here likely means the
 backend installer library (main installer dispatches to it). If
 sourced → library → move to common as cursor_install_backend.sh?
 Hmm, or maybe it's executed with args. Let me check.

 159 group: same pattern — 159_install_gitea_backend.sh referenced
 only by 159_install_gitea.sh.

 169 group: 169_rustdesk_server_client_config.sh referenced only by
 169_install_rustdesk_server_1.1.14.sh.

 Also the user said "或者是公共脚本的，改名并移入公共脚本区" — common
 scripts → rename + move to common area. For all these sub-scripts
 that are only referenced by their parent step, they're helper/common
 scripts of the step, not independent menu steps. Moving them to
 common/ (with the _common.sh convention where applicable, or
 descriptive names) removes them from the menu and resolves the
 duplicate numbers.

 But wait — for 113: the modules are named 113_natgateway_runtime.sh
 etc. Moving to common → natgateway_runtime.sh? Hmm, but maybe better
 naming: since they're modules of natgateway, in common they could be
 natgateway_runtime_common.sh? Let me look at how they're used first
 (source vs execute), and their content nature.

 Also for 155/159: 155_install_cursor_backend.sh → common as
 cursor_install_backend.sh? Hmm — wait. Let me think about what these
 "backend" scripts are. Let me read their heads.

 Actually, let me also reconsider: maybe the "backend" scripts are
 EXECUTED as child processes (bash "$CURSOR_INSTALL_BACKEND"). In
 that case they're still helper sub-scripts, not menu steps. Either
 way (source or execute), they belong to common/ (per user's rule:
 they're 公共脚本 — shared by the main step only... hmm, "common" in
 the user's sense = not an independent numbered step but a helper).

 Hmm wait, actually let me reconsider. The user's rule: "同序号脚本修
 改名字，或者是公共脚本的，改名并移入公共脚本区" — two options for
 same-number scripts:
 1. Rename (renumber to unique number) — for scripts that ARE
    independent install steps.
 2. If common (library/helper) → rename + move to common area.

 For each duplicate, I need to determine which. Let me read the file
 heads:

 - 75_postgresql_install_common.sh — name says common, referenced via
   variable likely sourced. Check.
 - 113_natgateway_{runtime,interfaces,menu}.sh — modules, likely
   sourced. Check.
 - 155_install_cursor_backend.sh — check.
 - 159_install_gitea_backend.sh — check.
 - 169_rustdesk_server_client_config.sh — check.

 Let me look at how each is invoked in the parent script, and read
 the heads of each.

 Let me examine how each duplicate sub-script is invoked (sourced vs
 executed) and their nature:


 $ cd
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 && echo "=== 75_install_postgresql.sh usage ==="; grep -n
 "POSTGRESQL_INSTALL_COMMON" 75_install_postgresql.sh; echo; echo
 "=== 113_natgateway.sh usage ==="; grep -n
 "NATGATEWAY_.*MODULE\|source.*MODULE\|bash.*MODULE"
 113_natgateway.sh | head; echo; echo "=== 155_install_cursor.sh
 usage ==="; grep -n "CURSOR_INSTALL_BACKEND" 155_install_cursor.sh;
 echo; echo "=== 159_install_gitea.sh usage ==="; grep -n
 "GITEA_INSTALL_BACKEND" 159_install_gitea.sh; echo; echo "=== 169
 usage ==="; grep -n "RUSTDESK_SERVER_CLIENT_CONFIG"
 169_install_rustdesk_server_1.1.14.sh

 ... (25 earlier lines, ctrl+o to expand)

 === 169 usage ===
 32:RUSTDESK_SERVER_CLIENT_CONFIG="$SCRIPT_CURRENT_DIR/169_rustdesk_s
 erver_client_config.sh"
 39:source "$RUSTDESK_SERVER_CLIENT_CONFIG"

 Took 0.1s


 All the duplicates are sourced libraries referenced only by their
 parent step script:

 ┌──────────────────────┬─────────────────────┬─────────────────────┐
 │ Parent (numbered     │ Library (sourced,   │ New name in common/ │
 │ step, stays)         │ moves to common/)   │                     │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 75_install_postgresq │ 75_postgresql_insta │ postgresql_install_ │
 │ l.sh                 │ ll_common.sh        │ common.sh           │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 113_natgateway.sh    │ 113_natgateway_runt │ natgateway_runtime_ │
 │                      │ ime.sh              │ common.sh?          │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 113_natgateway.sh    │ 113_natgateway_inte │ natgateway_interfac │
 │                      │ rfaces.sh           │ es_common.sh?       │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 113_natgateway.sh    │ 113_natgateway_menu │ natgateway_menu_com │
 │                      │ .sh                 │ mon.sh?             │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 155_install_cursor.s │ 155_install_cursor_ │ cursor_install_back │
 │ h                    │ backend.sh          │ end.sh?             │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 159_install_gitea.sh │ 159_install_gitea_b │ gitea_install_backe │
 │                      │ ackend.sh           │ nd.sh?              │
 ├──────────────────────┼─────────────────────┼─────────────────────┤
 │ 169_install_rustdesk │ 169_rustdesk_server │ rustdesk_server_cli │
 │ _server_1.1.14.sh    │ _client_config.sh   │ ent_config.sh?      │
 └──────────────────────┴─────────────────────┴─────────────────────┘

 Naming convention in common/: most files end with _common.sh (e.g.,
 runtime_config_common.sh, domain_setup_common.sh,
 composer_vendor_common.sh, nginx_common.sh), but not all (e.g.,
 arrow_menu.sh, testselector.sh, selector_common.sh,
 install_test_menu.sh, pycore_service.sh,
 frankenphp_install_apt.sh...). Mixed. For libraries I'll use the
 _common.sh suffix convention:

 - postgresql_install_common.sh (already named that way, just drop
   the number)
 - natgateway_runtime_common.sh, natgateway_interfaces_common.sh,
   natgateway_menu_common.sh — hmm, or keep original names without
   number: natgateway_runtime.sh etc. Hmm.

 Wait — are the natgateway modules referenced ONLY by
 113_natgateway.sh? Earlier grep for "113_natgateway" showed
 references only in: selector_common.sh (113_natgateway.sh),
 dd_helper/natgateway_helper.sh (113_natgateway.sh),
 dd_helper/linux_management.sh (113_natgateway.sh), and
 113_natgateway.sh itself (modules). But do the modules reference
 EACH OTHER or themselves? E.g., 113_natgateway_runtime.sh might
 reference 113_natgateway_interfaces.sh internally. Let me check
 cross-references among modules. Also natgateway_helper.sh in
 dd_helper — what does it do? It references 113_natgateway.sh. Fine,
 unaffected.

 Also check: do the modules get sourced by anything else, like
 debian_com scripts? The repo-wide grep for "113_natgateway" covered
 scripts/ + dd.sh and only found those files. But wait — maybe some
 scripts reference the modules by variable NATGATEWAY_RUNTIME_MODULE
 etc.? Those variables are defined in 113_natgateway.sh. Could other
 scripts define their own paths to these modules? The grep for
 "113_natgateway" would catch any literal path references. Only
 113_natgateway.sh has them. But variables like
 NATGATEWAY_RUNTIME_MODULE might be used inside the modules
 themselves (e.g., menu module calling runtime functions — but that's
 via sourced functions, not the variable). OK.

 Now — the modules might also be referenced WITHOUT the "113_" prefix
 in other files, e.g., "natgateway_runtime.sh" — unlikely since
 they're currently named with 113_. Let me do a broader grep for the
 module basenames without number to be safe... Actually my grep was
 for "113_natgateway" which IS the basename prefix. Any reference to
 these files must include "113_natgateway_..." since that's the
 current filename. Covered.

 Similarly for the others: any reference to
 75_postgresql_install_common, 155_install_cursor_backend,
 159_install_gitea_backend, 169_rustdesk_server_client_config must
 contain those exact strings. Greps found:
 - 75_postgresql_install_common → only 75_install_postgresql.sh ✓
 - 155_install_cursor_backend → only 155_install_cursor.sh ✓ (the
   grep "155_install_cursor" showed other files referencing
   155_install_cursor.sh — desktop_electron_ime_compat.sh,
   173_install_chinese_wubi.sh, app_install_menu.sh — those reference
   the MAIN script which stays)
 - 159_install_gitea_backend → only 159_install_gitea.sh ✓ (others
   reference 159_install_gitea.sh: app_install_menu.sh,
   gitea_backup/*)
 - 169_rustdesk_server_client_config → only
   169_install_rustdesk_server_1.1.14.sh ✓

 Also, debian_tree.md references 155_install_cursor (a doc — markdown
 tree; I shouldn't update docs? "Never create or update documentation
 (*.md)". debian_tree.md is an existing doc listing the tree;
 updating it would be "updating documentation". The rule says don't
 create/update docs. But if I move files, the tree doc becomes
 stale... The rule is explicit: never create or update documentation.
 I'll leave debian_tree.md alone. Hmm, wait — but it's more like a
 generated tree listing? Let me not touch it per the rules.)

 Now naming for the moved libraries in common/:
 - 75_postgresql_install_common.sh → postgresql_install_common.sh

 - 113_natgateway_runtime.sh → natgateway_runtime_common.sh? Hmm. Or
   keep semantic: natgateway_runtime.sh. Looking at common/
   conventions: there are non-_common names too (arrow_menu.sh,
   testselector.sh). But most libraries use _common.sh. The user said
   "改名并移入公共脚本区" (rename and move into common area). The
   rename = drop the number. I'll use:
     - natgateway_runtime_common.sh
     - natgateway_interfaces_common.sh
     - natgateway_menu_common.sh
       Hmm, these are long. Alternative: keep simple names without
       _common suffix since they're modules of a single feature... I
       think _common.sh suffix is the established library convention
       in this dir. Let me go with it.

   Actually hold on — let me reconsider. In common/, files like
   frankenphp_runtime_common.sh, runtime_helpers_common.sh exist. So
   natgateway_runtime_common.sh fits perfectly. Same for
   interfaces/menu.

 - 155_install_cursor_backend.sh → cursor_install_backend.sh? Hmm,
   "backend" — what is it? Let me peek at its head to understand
   naming. It's the "backend" part of cursor installation (maybe
   system-level install vs user-level). Names in common:
   desktop_application_installer.sh... For consistency maybe
   cursor_install_backend_common.sh? Hmm. Let me look at the file's
   header comment first.

 - 159_install_gitea_backend.sh → gitea_install_backend_common.sh?
   Same pattern.

 - 169_rustdesk_server_client_config.sh →
   rustdesk_server_client_config_common.sh? Hmm, or
   rustdesk_client_config_common.sh. Let me check its head.

 Let me read the heads of these library files to name them
 appropriately. Also check if the libraries internally reference
 their own filename/paths (BASH_SOURCE) or reference each other.

 All five duplicate groups are sourced libraries referenced only by
 their parent step script — exactly the "common script" case. Let me
 check the library file heads for naming and any internal path
 dependencies:


 $ cd
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 && for f in 75_postgresql_install_common.sh
 113_natgateway_runtime.sh 113_natgateway_interfaces.sh
 113_natgateway_menu.sh 155_install_cursor_backend.sh
 159_install_gitea_backend.sh 169_rustdesk_server_client_config.sh;
 do echo "=== $f ==="; head -20 "$f"; echo "--- BASH_SOURCE/self-refs
 ---"; grep -n "BASH_SOURCE\|SCRIPT_DIR\|SCRIPT_CURRENT_DIR" "$f" |
 head -5; echo; done

 ... (171 earlier lines, ctrl+o to expand)

     # Get all IPv4 addresses
     local all_ips=$(hostname -I 2>/dev/null)

 --- BASH_SOURCE/self-refs ---

 Took 0.1s


 All seven libraries are pure function libraries with no entry points
 and no BASH_SOURCE-based self-location (except two natgateway
 modules that use BASH_SOURCE[0] to resolve the current script path —
 line 158 in runtime and 473 in interfaces — let me check what they
 do with that; they may compute a path relative to themselves, which
 would still work since the resolved dir just changes from
 install_shells to common — need to verify they don't compute sibling
 file paths that would break).

 Let me check those two BASH_SOURCE usages.


 $ cd
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 && sed -n '150,170p' 113_natgateway_runtime.sh; echo "---"; sed -n
 '465,485p' 113_natgateway_interfaces.sh

 ... (38 earlier lines, ctrl+o to expand)
         existing_target=$(readlink -f "$NATGATEWAY_LINK")
         if [ "$existing_target" != "$current_script" ]; then
             log_info "Updating existing symlink..."
             $USE_SUDO rm -f "$NATGATEWAY_LINK"
             $USE_SUDO ln -s "$current_script" "$NATGATEWAY_LINK"

 Took 0.0s


 Interesting — important detail:

 1. 113_natgateway_runtime.sh line ~158: check_installation()
    compares /usr/local/bin/natgateway symlink target
    (NATGATEWAY_LINK) with the current script (BASH_SOURCE[0]). Since
    these functions are SOURCED by 113_natgateway.sh, BASH_SOURCE[0]
    inside a sourced function refers to... the file where the
    function is defined — i.e., 113_natgateway_runtime.sh itself. So
    current_script = the runtime module path. And NATGATEWAY_LINK
    presumably points to... let me check what creates the link.

 2. 113_natgateway_interfaces.sh line ~473:
    create_natgateway_command() creates /usr/local/bin/natgateway
    symlink pointing to current_script (= the interfaces module!).
    Hmm interesting — so the natgateway command links to the
    interfaces module?? That means the interfaces module can run
    STANDALONE (/usr/local/bin/natgateway →
    113_natgateway_interfaces.sh). Wait — but it's a library of
    functions... unless it has an entry point at the bottom. Let me
    check the tails of these modules.

 Hmm wait, BASH_SOURCE[0] inside a function defined in file X and
 called after being sourced: when the function executes,
 BASH_SOURCE[0] is the file that DEFINED the function? Actually
 BASH_SOURCE is an array of the call stack of source files:
 BASH_SOURCE[0] is the file containing the currently executing code.
 When executing a function defined in 113_natgateway_interfaces.sh
 (sourced into 113_natgateway.sh), BASH_SOURCE[0] = the interfaces
 file path? Hmm, actually for functions, BASH_SOURCE[0] gives the
 source file where the function was defined. Yes — bash
 Funcname/BASH_SOURCE correspond: BASH_SOURCE[0] is the file where
 the currently executing function is defined.

 So create_natgateway_command links /usr/local/bin/natgateway →
 113_natgateway_interfaces.sh. And check_installation (in runtime
 module) compares NATGATEWAY_LINK target with the runtime module
 path... that would NEVER match if the link points to interfaces!
 Unless... hmm wait, maybe interfaces' create_natgateway_command uses
 BASH_SOURCE[0] and when interfaces is executed DIRECTLY (as
 /usr/local/bin/natgateway), BASH_SOURCE[0] = itself.

 Hmm, wait — let me re-check: maybe check_installation is called with
 BASH_SOURCE[0] being... if check_installation is called from
 113_natgateway.sh (the caller), then BASH_SOURCE[0] inside the
 function is still the file DEFINING check_installation =
 113_natgateway_runtime.sh. Hmm, actually no — BASH_SOURCE reflects
 the dynamic call stack of SOURCE FILES... let me think again.

 In bash, when script A sources file B, and B defines function f, and
 A calls f: inside f, BASH_SOURCE[0] = B, BASH_SOURCE[1] = A. Yes,
 BASH_SOURCE[0] = the file where the executing code is defined.

 So:
 - check_installation (defined in runtime.sh): current_script =
   runtime.sh path. Compares with NATGATEWAY_LINK target.
 - create_natgateway_command (defined in interfaces.sh):
   current_script = interfaces.sh path. Creates link → interfaces.sh.

 So the link points to interfaces.sh, but check_installation compares
 against runtime.sh?? That seems like an existing inconsistency... OR
 maybe check_installation is defined in interfaces too? No, it's in
 runtime (line 154-168 of runtime). Hmm, wait — maybe NATGATEWAY_LINK
 doesn't point to interfaces. Let me check NATGATEWAY_LINK definition
 and see the tails/entry points of these modules. Let me read
 113_natgateway.sh fully to understand the structure.


 read
 /www/programing/core_node/scripts/shells/linux/debian/install_shells
 /113_natgateway.sh


 Now I understand the 113 group:

 - 113_natgateway.sh — the main numbered step (entry point, main "$@"
    at bottom). Referenced by selector_common.sh,
   natgateway_helper.sh, linux_management.sh (all reference
   113_natgateway.sh, which stays).
 - 113_natgateway_runtime.sh, 113_natgateway_interfaces.sh,
   113_natgateway_menu.sh — sourced modules, referenced ONLY by
   113_natgateway.sh via $SCRIPT_CURRENT_DIR/113_natgateway_*.sh.

 Important detail: create_natgateway_command (in interfaces module)
 creates /usr/local/bin/natgateway symlink → the interfaces module
 path (BASH_SOURCE[0] of the defining file). And check_installation
 (in runtime module) compares the link target with BASH_SOURCE[0] of
 runtime module... wait, that comparison would fail if the link
 points to the interfaces module. Hmm, wait:

 ```bash
   check_installation() {
       if [ -f "$NATGATEWAY_LINK" ] && [ -L "$NATGATEWAY_LINK" ];
 then
           local link_target=$(readlink -f "$NATGATEWAY_LINK")
           local current_script=$(readlink -f "${BASH_SOURCE[0]}")
           if [ "$link_target" = "$current_script" ]; then
 ```

 Hmm — BASH_SOURCE[0] when check_installation is called... The
 function is DEFINED in runtime.sh. When executed, BASH_SOURCE[0] =
 runtime.sh. But hold on — the /usr/local/bin/natgateway link points
 to the INTERFACES module per create_natgateway_command. So
 link_target = interfaces.sh path, current_script = runtime.sh path →
 NOT equal → check_installation returns 1 (not installed). That seems
 like an existing bug OR...

 Wait, let me recheck create_natgateway_command — it's in interfaces
 (line ~473). current_script=$(readlink -f "${BASH_SOURCE[0]}"). Hmm
 — but WHEN is create_natgateway_command executed? It's called from
 install_natgateway() in 113_natgateway.sh. At that point, the
 executing code is in 113_natgateway.sh... but BASH_SOURCE[0] inside
 the FUNCTION refers to where the function is defined =
 interfaces.sh. Hmm, actually let me double-check bash semantics:

 BASH_SOURCE: "An array variable whose members are the source
 filenames where the corresponding shell function names in the
 FUNCNAME array variable are defined."

 So inside function create_natgateway_command (defined in
 interfaces.sh), BASH_SOURCE[0] = interfaces.sh (the file where the
 function is defined), BASH_SOURCE[1] = 113_natgateway.sh (caller)...
 Actually more precisely, BASH_SOURCE[i] corresponds to FUNCNAME[i].
 FUNCNAME[0] = create_natgateway_command, FUNCNAME[1] =
 install_natgateway, ... The source file of create_natgateway_command
 is interfaces.sh. So BASH_SOURCE[0] = interfaces.sh. Yes.

 So /usr/local/bin/natgateway → 113_natgateway_interfaces.sh. When
 the user runs natgateway, it executes 113_natgateway_interfaces.sh
 DIRECTLY (as a script!). Let me check the interfaces module's tail —
 does it have an entry point (main menu) when run directly? It has
 show_interactive_menu etc. Let me check its tail.

 Similarly, check_installation in runtime: when 113_natgateway.sh
 runs (via menu/testselector), check_installation compares link
 target (interfaces.sh) vs BASH_SOURCE[0] (runtime.sh)?? That'd never
 match → always "not installed". Hmm — unless check_installation is
 ALSO defined in interfaces.sh or the link check is different... Let
 me grep check_installation in all three modules + where
 NATGATEWAY_LINK is compared.

 Actually — hmm, wait. Let me recheck: is check_installation maybe
 called with the link pointing to interfaces, and BASH_SOURCE[0]...
 if check_installation were defined in 113_natgateway.sh itself...
 no, I saw it at runtime.sh:154. Hmm, but wait — when
 113_natgateway.sh runs check_installation via if check_installation;
 then, BASH_SOURCE[0] inside = runtime.sh. link_target =
 interfaces.sh. Never equal → returns 1. So "already installed" path
 would never trigger?? Unless the link actually points to
 runtime.sh... create_natgateway_command is the only creator of the
 link and it uses interfaces.sh.

 Hmm wait, maybe there are TWO create_natgateway_command definitions
 (one in runtime, one in interfaces)? The later-sourced one wins.
 113_natgateway.sh sources: runtime first, then interfaces, then
 menu. If runtime.sh ALSO defines create_natgateway_command, the
 interfaces one overrides it. Let me grep
 "create_natgateway_command|check_installation" across the modules.

 Also — the user's instructions for THIS turn: the current message is
 about the b key (already done) — but the previous turn's pending
 work includes the reorganization. Let me continue analyzing, then
 implement.

 Actually, let me carefully re-scope. The previous user message
 asked:
 1. Fix keyword matching (✓ done).
 2. Menu searches only install_shells (✓ already true).
 3. Same-number scripts: rename, or if common → rename + move to
    common area. Examples: 75 group, 113 group, 155 group. (My
    analysis: also 159, 169.)

 Current message: b key handling (✓ done).

 So now I should complete the reorganization. For each duplicate
 sub-script, since they're all sourced libraries referenced only by
 their parent:

 Moves to common/ (drop the numeric prefix, keep descriptive name,
 follow _common.sh convention where natural):

 1. 75_postgresql_install_common.sh →
    common/postgresql_install_common.sh (name already ends _common.sh
    — just drop number). Update 75_install_postgresql.sh line 5:
    POSTGRESQL_INSTALL_COMMON="$SCRIPT_DIR/75_postgresql_install_comm
    on.sh" → point to common dir. Need to check what $SCRIPT_DIR is
    in 75_install_postgresql.sh — let me read its head. It probably
    defines SCRIPT_DIR=install_shells dir. The common dir is two
    levels up: $(dirname "$(dirname "$SCRIPT_DIR")")/common or
    similar. Let me read the head of 75_install_postgresql.sh.

 2. 113_natgateway_runtime.sh → common/natgateway_runtime_common.sh?
    Hmm — or keep the same base name without prefix:
    natgateway_runtime.sh, natgateway_interfaces.sh,
    natgateway_menu.sh. Which convention? The user said "改名"
    (rename). In common/, existing examples:
    frankenphp_runtime_common.sh, runtime_helpers_common.sh,
    gvar_common.sh. Also non-common names exist: arrow_menu.sh,
    testselector.sh, pycore_service.sh, frankenphp_install_apt.sh.

    IMPORTANT subtlety for the natgateway modules:
    create_natgateway_command creates /usr/local/bin/natgateway →
    interfaces module, meaning the interfaces module RUNS STANDALONE
    as the natgateway command. If I move it to common/, the link
    target changes on next install (the code recomputes
    current_script from BASH_SOURCE at call time — it will pick up
    the new path automatically). But an EXISTING deployed link points
    to the old install_shells path — after the move, the old path no
    longer exists → natgateway command breaks until re-run of
    113_natgateway.sh... Also check_installation compares link target
    with BASH_SOURCE — if the link points to the OLD interfaces path
    and the file is gone... check_installation does readlink -f — if
    target missing, readlink -f still prints the path (it resolves
    symlinks lexically; -f requires the final component to exist? No:
    readlink -f canonicalizes; all but the last component must exist.
    The last component (the target file) may not exist? Actually GNU
    readlink -f requires all components INCLUDING the last to exist,
    except... hmm, readlink -f on a dangling path errors and prints
    nothing. Let me not overthink: the comparison would just fail →
    "not installed" → re-offers install → recreates the link at the
    new location. Self-healing.)

    Wait, actually — I should check whether the interfaces module
    really is a standalone entry (has a main at the bottom). Let me
    check its tail. Also the menu module. If interfaces.sh has an
    entry point (e.g., when executed directly it shows the
    interactive menu), then moving it to common/ changes the
    natgateway command's script location — functionally fine, and
    self-healing via re-running the step.

    Also — hmm, check_installation compares the LINK TARGET with the
    RUNTIME module path... that comparison seems wrong already (link
    → interfaces, current → runtime). Let me grep all definitions to
    be sure. Maybe check_installation is defined in interfaces too.
    Let me check.

 3. 155_install_cursor_backend.sh →
    common/cursor_install_backend_common.sh? Or
    cursor_install_backend.sh. Hmm.

 4. 159_install_gitea_backend.sh → common/gitea_install_backend.sh?
    Hmm — wait, there's already gitea_backup stuff in
    menu_itemshells. Also common/ has composer_install_common.sh. So
    gitea_install_common.sh? But the file is a "backend" (function
    library for the installer). Maybe
    gitea_install_backend_common.sh? These names are getting long.
    Let me look at what the files contain to pick sensible names.

    Hmm, actually — simpler approach: drop the number, keep the
    existing descriptive suffix, and add nothing:
     - cursor_install_backend.sh — hmm, "backend" means it's the
       backend part of the cursor installer.
     - Actually wait — maybe I should keep names as close to original
       as possible (minimal rename = just drop the numeric prefix):
         - postgresql_install_common.sh ✓ (75)
         - natgateway_runtime.sh, natgateway_interfaces.sh,
           natgateway_menu.sh (113)
         - cursor_install_backend.sh (155)
         - gitea_install_backend.sh (159)
         - rustdesk_server_client_config.sh (169)

    Hmm, but for consistency with the common/ _common.sh library
    convention... The existing common dir has both styles. The
    laravel move earlier: 175_laravel_main_runtime_common.sh →
    laravel_main_runtime_common.sh (kept _common suffix). For files
    already named _common.sh, keep it. For others (natgateway
    modules, backend scripts), minimal rename = drop number only. But
    hmm — the user said "改名" (rename) — dropping the number IS the
    rename. I think minimal rename (drop prefix) is cleanest and most
    predictable.

    Wait, but actually — let me reconsider the 113 modules. They're
    not just libraries: interfaces.sh becomes the standalone
    natgateway command target! Its name as a command script matters.
    natgateway_interfaces.sh in common/ — fine.

    Hmm, hold on. Let me reconsider whether the natgateway modules
    should go to common/ at all... The user's rule: same-number
    scripts → rename (renumber) OR if common → move to common area.
    The 113 modules are sourced by 113_natgateway.sh only. They're
    "common" in the sense of being helper modules. Moving them to
    common/ removes them from the menu (fixing the duplicate 113
    listing) and keeps one numbered step. That matches the user's
    intent (menu shows only real steps; the user explicitly listed
    the 113 group as a problem).

    Alternatively they could go to debian_com/ (like the 175 laravel
    sub-scripts). But the user said "公共脚本区" and I established
    common/ for laravel_main_runtime_common.sh last turn — user
    accepted. So common/ it is.

    Hmm — wait, actually, let me reconsider: is debian_com the better
    home for natgateway sub-scripts? The user said "公共脚本区"
    (common script area). Earlier, for laravel, the user directed
    moving 175_laravel_main_runtime_common.sh to the 公共区 and I
    used common/. The debian_com dir holds debian-specific
    sub-scripts dispatched by steps. The natgateway modules are
    debian-specific (network router setup for Debian/Ubuntu/Kali)...
    but so is everything in install_shells. The user's mental model:
    公共区 = scripts/shells/linux/common/. I'll use common/ for
    consistency with the laravel precedent.

 5. Check the natgateway symlink logic more carefully
    (check_installation vs create_natgateway_command BASH_SOURCE
    mismatch) — I need to verify before/after behavior. Let me grep
    the three modules for NATGATEWAY_LINK and check_installation
    definitions.

 Also for each parent script, I need to check how it computes the
 common dir (to write the new source path):
 - 75_install_postgresql.sh: uses
   $SCRIPT_DIR/75_postgresql_install_common.sh. Need to see
   SCRIPT_DIR definition — probably SCRIPT_CURRENT_DIR or similar.
   Let me read heads of all parent scripts.

 Also must check: do any of the libraries source OTHER relative files
 themselves (e.g., 75_postgresql_install_common.sh sourcing
 gvar_common by relative path)? The grep for BASH_SOURCE/SCRIPT_DIR
 in the libraries showed only the two natgateway BASH_SOURCE uses. So
 libraries don't self-locate otherwise.

 But wait — 113_natgateway_runtime.sh might reference
 debian_com/natgateway_monitor.sh? No — create_service_script in
 113_natgateway.sh references $DEBIAN_COM_DIR/natgateway_monitor.sh
 (in the main script). Fine.

 One more thing: check for references to these libraries in OTHER
 areas — pycore, poly_apps, dotcore? The earlier greps covered
 scripts/ and dd.sh only. Let me do repo-wide greps (excluding
 node_modules, .git, docs_fix) to be thorough. Also check gvar stored
 files? No — those are runtime state, not code refs. Let me also grep
 for just the basenames without extension in case of menu registries:
 e.g., "75_postgresql_install_common" would appear in any reference.
 Covered by basename greps.

 Let me now:
 1. Read heads of parent scripts (75, 155, 159, 169) to see their dir
    variables.
 2. Check tails of natgateway modules for standalone entry points.
 3. Check all NATGATEWAY_LINK / check_installation interplay.
 4. Repo-wide reference grep for all seven library basenames.

 Now let me check the parents' path variables, module entry points,
 and the natgateway symlink logic in detail:


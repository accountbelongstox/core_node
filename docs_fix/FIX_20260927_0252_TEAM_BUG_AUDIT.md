# FIX 2026-09-27 02:52 — Team Bug Audit: Findings So Far

Status: the user halted the audit at about 02:52. Their instruction was "先输出现在发现的问题到docs fix然后停下".
- The audit was report-only. No code was changed.
- This is a fix list for a later directive. Nothing below has been applied.

References:
- Requirements: `docs_fix/REQUIREMENTS_20260927_TEAM_BUG_AUDIT.md`.
- Board rows: L5, P5, N1, A6, B6, U5, S4, R4.
- Full role reports with failure scenarios, evidence, cross-scope, in-flight and coverage sections: `docs_fix/bug_audit_20260927/<role>.md`. They were copied at 03:06 (latest report write) from `.claude/agents_shared/bug_audit_20260927/`.

## 1. Counts (finder severity)

| Role | Prefix | Critical | High | Medium | Low | Total |
|---|---|---|---|---|---|---|
| pycore-runtime | PR | 1 | 7 | 16 | 10 | 34 |
| audio-tts | AT | 0 | 6 | 13 | 30 | 49 |
| laravel-backend | LB | 4 | 6 | 10 | 14 | 34 |
| frontend-ui | FU | 1 | 4 | 14 | 21 | 40 |
| infra-shell | IS | 1 | 3 | 9 | 17 | 30 |
| ncore | NC | 1 | 7 | 14 | 15 | 37 |
| reviewer (cross-end contracts) | RV | 0 | 0 | 2 | 9 | 11 |
| **Total** | | **8** | **33** | **78** | **116** | **235** |

Notes on the counts:
- The audio-tts low column includes 6 findings of category `rule`.
- Pass-2 findings that the roles wrote as the halt arrived:
  - audio-tts: 10 (AT-041..AT-050);
  - frontend-ui: 13 (FU-030..FU-042). This matches the 13 announced in its addendum header, so the addendum is complete.
  - pycore-runtime: 8 (PR-030..PR-037).
- infra-shell wrote no pass 2.
- `ncore` (N1) did not act on the stop. It kept auditing until 03:06, when an API error ended it: 37 findings.
  - Its report has Findings and Coverage sections, but no Cross-scope or In-flight sections.
  - NC-001..NC-005 kept their IDs; NC-006 and up are new since then.

## 2. Verification state

- **R4 verified only audio-tts AT-001..AT-040, plus the cross-scope items X1–X7.** Result: 32 CONFIRMED, 8 PLAUSIBLE, 0 REFUTED. The verdicts are in the R4 column, and the reviewer's full reasons are in `docs_fix/bug_audit_20260927/reviewer_verdicts_audio-tts.md`.
  - The reviewer checked against the audio-tts text of about 02:39 and the code on disk.
  - Severity adjusted by the reviewer: AT-004 high→medium, AT-007 medium→low, AT-008 medium→low. The tables keep the finder's severity.
  - Not verified: the AT-001 ledger extension, X8, and the pass-2 findings.
- **The other 185 of the 224 role findings have no verdict.** The reviewer did not reach pycore-runtime, frontend-ui or infra-shell before the halt. laravel-backend and ncore were never sent. For those, only the finder's Conf applies:
  - `confirmed`: traced end to end;
  - `likely`;
  - `suspect`.
- The RV findings are the reviewer's own. audio-tts independently confirmed RV-002 and RV-010 against the code.
- Tags in the tables:
  - `[pass 2]`: from a pass-2 addendum.
  - `[in-flight HH:MM]`: other user sessions edited the file during the audit, and the finding was re-checked at that time. Those sessions were working on the W4/W7 delivery layer, the Laravel diff/Redis index, CodeMart polish, and roles V2.
- Findings withdrawn because the code was fixed during the audit are not listed. Each report keeps them under `In-flight observations`: PR-002, AT-015, and four CodeMart items in frontend-ui.
- laravel-backend stopped before its in-flight re-read, so LB-026 and LB-027 (orch-audio) may be stale.

## 3. Fix themes (lead grouping, suggested order)

1. **Unauthenticated or over-permissive network surfaces.** These are the critical cluster.
   - Findings: PR-001, PR-003, PR-021, PR-030, PR-031, PR-037, NC-001, NC-002, NC-005, NC-007 (unauthenticated file server on 0.0.0.0:3900), NC-008 (hardcoded master license codes), AT-006, FU-001, LB-001, LB-002, LB-003, LB-005, LB-006, LB-007, LB-015, RV-003.
   - Impact example from frontend-ui's cross-scope section: pycore `okx/reveal_credentials` returns the full OKX API key over the same open RPC as PR-001.
   - Same pattern on both local runtimes: pycore on 0.0.0.0:59000 (PR-001) and ncore on 0.0.0.0:58000 (NC-001, NC-005) both have no auth and permissive CORS. One shared local-RPC auth design should cover both.
   - Ruling needed: the W5/W7 docs declare the Laravel worker and delivery routes "worker trust level (no login)". Securing them needs the machine-auth design (`DESIGN_20260814_QUEUE_CENTER_MACHINE_AUTHENTICATION`, board O3/L3), and the lead or orchestrator must rule on it first. LB-015 means the only existing machine gate cannot verify any machine today.
2. **Money and escrow.** LB-004, LB-008, LB-009, LB-020. FU-033 is a money bug in the Vortex account, but that account is simulated and local only; frontend-ui pass 2 found no real OKX order or funds path. FU-003 also belongs here, because retried non-idempotent writes duplicate admin pay/approve.
3. **Data loss and destructive operations.** IS-001, FU-030 (retried backup restore), FU-031 (offline queue replays one user's writes as another user), IS-003, IS-004, AT-003, AT-005 (kills other GPU processes), IS-008 (777 tree including secrets), PR-024.
4. **Wrong audio published to Laravel.** AT-001, AT-002, AT-009, AT-012, RV-002, X4.
5. **Retries, idempotency and revisions.** FU-002, FU-003, FU-030, RV-001, RV-011, AT-004, AT-011, PR-009.
6. **Windows breakage.** IS-002, IS-006, PR-004, PR-006, PR-012, PR-032, PR-035, NC-003.
7. **Invariant and rule breaches.**
   - State-driven, no polling, single batch entry: AT-007, AT-008, AT-010.
   - Contract duplication and literals: RV-005, RV-007.
   - i18n: FU-004, LB-034, AT-050.

## 4. Role findings

Locations are the first cited path. The full reports give every line reference.

### 4.1 Critical

| ID | Conf | R4 verdict | Location | Defect | Suggested fix |
|---|---|---|---|---|---|
| PR-001 | confirmed | — | `pycore/pyutils/rpc_v2/server.py:99` | The pycore RPC server (0.0.0.0:59000) has no authentication and allows any CORS origin with credentials, so any LAN host or any web page in the user's browser can drive terminals (remote code execution). | Bind the UI surface to loopback, or require a per-install bearer token or signed header on every non-relay route. Restrict CORS to the dashboard origins and drop `allow_credentials` and the private-network header for foreign origins. |
| LB-001 | confirmed | — | `poly_apps/laravel_main/routes/DashboardRouter/DatabaseManager.php:58-61` | Unauthenticated `sync-peer/prepare` and `sync-peer/export-prepare` let any internet client dump every database table and resource file, or push arbitrary rows and files | Require a pre-shared pairing secret or a machine-auth signature (the pending Queue Center machine-auth design) on prepare and export-prepare, and allow-list the peer machine codes. |
| LB-002 | confirmed | — | `poly_apps/laravel_main/app/Http/Middleware/LocalDebugOrSanctum.php:25-31` | `dashboard.auth` accepts any logged-in user, so a self-registered account can export any table, restore backups, change the PostgreSQL superuser password and drop DB users | Require `isAdmin()` (and `isSuperAdmin()` for credentials, restore and import) in `LocalDebugOrSanctum`, and default `DASHBOARD_LOCAL_DEBUG` to off on non-development hosts. |
| LB-003 | confirmed | — | `poly_apps/laravel_main/routes/api/ai_local.php:18-47` | The public `/api/local/ai/*` routes let anyone replace or delete the server's AI provider keys, read every stored AI chat and image, and use the paid providers | Put the group behind `dashboard.auth` (loopback debug or Sanctum admin), and leave only `catalog`/`probe`/`info` public, if even those. |
| LB-004 | confirmed | — | `poly_apps/laravel_main/routes/DingDuoDuoV1Router/DingDuoDuoV1Recharge.php:28-29` | The DingDuoDuo recharge callback trusts any caller, so a member can mark their own order paid and get membership for free (and double-apply it through a race) | Verify the gateway signature and amount (or allow only an admin or loopback confirm), and do an atomic `UPDATE … WHERE status='pending'` compare-and-set inside a transaction before applying the recharge. |
| FU-030 | likely | — | `apps/laravel-manager/api/modules/DatabaseManagerAPI.ts:381-384` | A database backup restore that runs longer than 30 s is re-sent up to 3 more times, so overlapping restores hit the same database. `[pass 2]` | `retry:false` plus a long per-call timeout for backup, restore, service control and toggle (better: fix FU-003 globally), and a server-side restore lock (see Cross-scope). |
| IS-001 | confirmed | — | `scripts/shells/linux/debian/install_shells/75_install_postgresql.sh:194-209` | PostgreSQL ensurer drops the existing cluster (and its data) whenever the computed data dir differs and is still empty | never drop a cluster that contains user databases. Relocate by stop + rsync (or pg_dump/restore) + config switch, or refuse and print an ACTION REQUIRED. |
| NC-001 | confirmed | — | `ncore/callmodule/app.js:168-243` | The ncore module caller exposes `POST /api/call` on 0.0.0.0:58000 with no auth and permissive CORS. It `require()`s any module path from the request body and calls any exported function with request-supplied args: unauthenticated remote code execution, reachable from the LAN and from any web page. | Bind 127.0.0.1 by default, require a shared-secret header, drop `/api/call` or restrict it to an explicit allow-list of module/function pairs, and replace `origin: '*'` with an allow-list. |

### 4.2 High

| ID | Conf | R4 verdict | Location | Defect | Suggested fix |
|---|---|---|---|---|---|
| PR-003 | confirmed | — | `pycore/pyutils/codesync/workspace_auth.py:9-11` | CodeSync workspace auth is a fixed bearer secret committed to the repo, which gives any LAN peer read/write access to every workspace file (write → code execution). | Generate a per-install secret stored outside the repo (APP_CONFIG_DIR, mode 0600) or pair devices explicitly. Never commit it. |
| PR-004 | confirmed | — | `pycore/pyfoundations/pybasecommon/commander.py:132-141` | `Commander` joins list commands with spaces and runs them through `shell=True`, which breaks paths with spaces and lets shell metacharacters run (injection). | Pass list commands to `subprocess.Popen(list, shell=False)`. Keep `shell=True` only for explicit string commands. |
| PR-005 | likely | — | `pycore/pyctl/terminal/terminal_service.py:208-265` | Terminal text input is not serialized: an RPC input and a scheduled input running at the same time swap clipboard contents and focus, so one prompt is pasted and submitted (Enter) into the wrong terminal. | Run all input/enter/scroll/activate actions through one serialized owner (a single-flight `TerminalInputThread`), shared by the RPC routes and the scheduler. |
| PR-006 | likely | — | `pycore/pyutils/common/clipboard_text.py:32-57` | Windows clipboard write calls Win32 through ctypes without `restype`/`argtypes`, so 64-bit pointers are truncated and `set_clipboard_text` raises an access-violation `OSError`. | Declare `restype`/`argtypes` (`c_void_p`/`HANDLE`) for GlobalAlloc, GlobalLock, GlobalUnlock, GlobalFree, OpenClipboard and SetClipboardData, and check each result. |
| PR-021 | confirmed | — | `pycore/callmodule/rpc_routes/thread_bus_routes.py:39-51` | `thread_bus/trigger_event` lets any HTTP client fire any internal THREAD_BUS event with any payload (exit, restart, service install/remove, Laravel online edge). `[in-flight 02:46]` | Replace it with an explicit allowlist of UI-originated event names, validate each payload, and require the same authentication as PR-001. |
| PR-030 | confirmed | — | `pycore/callmodule/rpc_routes/local_video_extract_routes.py:27-28` | `ui/video_extract/open` launches any client-supplied path with the OS default handler (`os.startfile` / `xdg-open`), which is unauthenticated code execution. `[pass 2]` | Accept only paths inside known video-extract output roots (resolve them, then check containment), open directories only, and never shell-execute files. Also fix the PR-001 authentication. |
| PR-031 | confirmed | — | `pycore/callmodule/rpc_routes/local_books_routes.py:25-80` | Other unauthenticated routes read any document on the host and rewrite security-relevant settings. `[pass 2]` | Same authentication as PR-001, plus allowlisted roots for the books paths, and schema validation for settings and base URLs (for example, only the configured Laravel endpoint catalog). |
| AT-001 | confirmed | CONFIRMED (base; ledger extension unverified) | `pycore/pyutils/tts/word_audio_cache.py:46-48` | A word-cache lookup returns another word's audio when that word's file name starts with `<word>_`; the wrong clip is then reused and uploaded to Laravel `[in-flight 02:44]` | store `{word}` and `{provider}` with a separator `_safe` can never produce (or keep a word→file ledger) and match the exact stem only. |
| AT-002 | likely | CONFIRMED | `pycore/pyutils/tts/sherpa_engine.py:37` | Every word-lane language is synthesized by Kokoro with a fixed English phonemizer and speaker, so fr/de/es/vi/lo/ja/ko dictionary words get English-G2P audio that is uploaded | gate the word batch by `tts_engine_supports_language(WORD_BATCH_ENGINE, lang)`, pass the language to the phonemizer/voice, and fail unsupported languages explicitly. |
| AT-003 | confirmed | CONFIRMED | `pycore/pyctl/tts/laravel_audio_worker.py:662-673` | Turning a lane OFF (the default immediate stop) synchronously pops and discards the whole lane Queue inside the control RPC, and the offline backlog cannot be restored `[in-flight 02:43]` | make lane stop a state flag (the drain halts and the Queue and snapshot stay intact), and drop or release only claimed Laravel rows in a background task. |
| AT-004 | likely | CONFIRMED (→medium) | `pycore/pyctl/audio_orchestration/orch_resources.py:323-360` | A Kokoro batch exception during orchestration leaves the task's taken Part1 items unsettled forever (tracker stuck at "processing", dedup key held) `[in-flight 02:43]` | settle claimed keys as failed in a `finally` around each chunk, and call `release_owner_queue` on the crash path. |
| AT-005 | confirmed | CONFIRMED | `pycore/pyutils/tts/memory_gate.py:228-299` | The default-on "VRAM reclaim" kills every other GPU compute process at startup and at each qwen3tts launch when free VRAM is below 6 GB | default the reclaim OFF (opt-in), and never kill processes the service did not start; fall back to CPU or ask the user instead. |
| AT-006 | confirmed | CONFIRMED | `pycore/tts_install_assets/f5tts_api_server.py:77` | The f5tts API server allows unauthenticated arbitrary file writes through the upload filename and binds 0.0.0.0 when pycore starts it | ignore the client filename (use a fixed name) and bind to loopback by default in the launcher. |
| LB-005 | confirmed | — | `poly_apps/laravel_main/routes/api.php:258-300` | The global task, worker, task-center and queue-center control APIs are unauthenticated, including destructive bulk operations | Put the machine surface (worker/*, task result, media ingest) behind `pycore.client`, and the operator control plane (reset-assigned, clean-invalid, settings, queue-center mutations) behind `dashboard.auth`. |
| LB-006 | confirmed | — | `poly_apps/laravel_main/routes/AppQyV1Router/AppQyV1AITools.php:44-133` | Paid-cost and data-writing AppQyV1 endpoints are public: anonymous LLM proxy, TTS synthesis, dictionary writes, queue control | Put the worker and control groups behind `pycore.client` (or the L3 machine auth), and the UI-facing write and cost endpoints behind `auth:sanctum`/`dashboard.auth`. Do the `ai/test` route first. |
| LB-007 | confirmed | — | `poly_apps/laravel_main/routes/AppQyV1Router/AppQyV1Vocabulary.php:15-26` | Public library-cover endpoints let anonymous callers replace any public library cover with an AI image from an arbitrary prompt on the server's paid keys | Put cover regenerate and enqueue behind `dashboard.auth` (admin), validate the prompt length, and run regeneration through the task queue only. |
| LB-008 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Services/CodeMartV1ProjectStateService.php:202-224` | Cancelling or completing a funded CodeMart project leaves the held escrow stranded; no path returns it to the client | On project cancel or complete, lock the project escrows, credit `remainingAmount()` back to the payer wallet with `recordRefund`, and add an admin escrow-refund action. |
| LB-009 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Ctl/CodeMartV1TaskCtl.php:655-690` | Approving a submission marks the task completed even when the escrow payout fails, and the developer is never paid | Check escrow headroom when a task gets a budget (reserve it), and roll back the approval (or keep the task in review) when the release does not succeed. |
| LB-010 | likely | — | `poly_apps/laravel_main/app/Services/DataSync/ResourceSyncService.php:242-265` | Push-mode compressed resource sync loops forever when final-chunk hashing and 7z extraction outlast the 120 s peer timeout | Extract in the receiver's timer tick. Record a per-archive completion receipt keyed by `key+sha256`, and answer a replayed final chunk with `complete:true`. |
| FU-001 | likely | — | `apps/mcp-chrome/app/native-server/src/constant/index.ts:20` | The MCP native server on loopback accepts any web origin and has an unauthenticated SHUTDOWN endpoint: any web page can kill the host, and DNS rebinding reaches the MCP browser-control tools. | restrict CORS to the extension origin, require a per-install token on `/singleton` and `/mcp`, and enable the SDK's DNS-rebinding protection (allowedHosts 127.0.0.1/localhost). |
| FU-002 | confirmed | — | `poly_apps/pycore_laravel_wordnew_ui/core/integrations/laravel/transport/BaseAPI.ts:283-292` | Every Laravel `BaseAPI` GET is served from a 5-second response cache that also stores failures and is never invalidated, so reloads after a mutation and Retry buttons show stale data app-wide. `[in-flight 02:42]` | default the BaseAPI coalescing TTL to 0 (in-flight dedupe only), never cache `success:false`, and clear on any non-GET. |
| FU-003 | confirmed | — | `poly_apps/pycore_laravel_wordnew_ui/core/integrations/laravel/transport/BaseAPI.ts:409-416` | `BaseAPI` retries non-idempotent POST/PUT/PATCH/DELETE up to 3 times after a timeout or network error, so a slow server gets duplicate writes. | retry only GET/HEAD, and requests carrying an `Idempotency-Key`. |
| FU-004 | confirmed | — | `apps/pycore-manager/components/PcTerminalDesktopIntegration.tsx:43` | The Terminal Control desktop-integration panel (R7) reads the wrong i18n namespace, so every label, capability, button and notice renders as a raw key in en and zh. | `useTranslation('pc')`. |
| IS-002 | confirmed | — | `scripts/shells/win/install_powershells/Step93_InstallFrankenPHP.ps1:20` | Windows Step93/94/96 do not parse, so FrankenPHP, its Composer and its PHP ini are never installed on Windows | use `"Step ${STEP_NUMBER}: ..."` (as Step175 already does) in the three files. |
| IS-003 | confirmed | — | `scripts/git/gitput_security_common.sh:467-523` | Root git push copies the project deploy key over every logged-in user's own private key, with no backup | never overwrite an existing different key (compare content, skip or back up), and install to users only on explicit opt-in. |
| IS-004 | likely | — | `scripts/shells/win/win_common/PathMappingLib.ps1:420-431` | Windows path mapping removes a stale junction with `Remove-Item -Recurse -Force` under Windows PowerShell 5.1, which deletes the junction target's contents | delete junctions with `[System.IO.Directory]::Delete($link)` (non-recursive) or `cmd /c rmdir`, and merge the old target into the new one before relinking. |
| NC-002 | confirmed | — | `ncore/global_vars/libs/config_tool.js:77-80` | The master key that "encrypts" secrets is hardcoded in source, so every `ENC:` secret committed in `config/index.js` can be decrypted by anyone with the repository. | Load the key from a per-machine secret outside the repo (for example the existing `#@secret_manager`), rotate every committed secret, and remove the `ENC:` blobs from `config/index.js`. |
| NC-003 | confirmed | — | `ncore/foundation/common/commander.js:456-473,516` | `execPowerShell` builds `<pwsh path> -Command "<cmd>"` without quoting the executable or escaping inner quotes, so on Windows with PowerShell 7 installed every PowerShell call fails, and any command containing `"` fails on every Windows machine. | Spawn the PowerShell executable directly with an argv array (`spawnSync(psPath, ['-NoProfile','-Command', cmd])`) instead of going through cmd.exe string joining. |
| NC-004 | confirmed | — | `ncore/foundation/common/commander.js:251-260` | Foundation `execCmd` treats a failing command as success when its output lacks "failure words", and `pipeExecCmd` never throws. Every caller that detects failure via try/catch or non-empty output gets false positives. Concretely, the Linux package installer reports "installed" for packages that are not installed. | Return a structured `{ code, stdout, stderr }` (or keep the text API but return `""` on any non-zero exit). Drop the keyword heuristic, and have `package_manager` check exit codes. |
| NC-005 | confirmed | — | `ncore/global_vars/gconfig/rpc_constants.js:32` | The shared HTTP RPC stack (`ExpressServer`/`expressProvider`) listens on 0.0.0.0 by default, answers every origin with `Access-Control-Allow-Origin: *`, and ships with auth disabled (PR-001 pattern on the Node side). | Default `HTTP_HOST` to 127.0.0.1 (explicit opt-in for LAN), enable a token by default when not loopback, reflect only allow-listed origins, and bind `/rpc/query` results to the requesting session. |
| NC-006 | confirmed | — | `ncore/utils/rpc/http_rpc/libs/RouterManager.js:225-244` | `RouterManager.api()` and `download()` send a second response after handlers that already responded. The second `res.json`/`res.send` throws `ERR_HTTP_HEADERS_SENT` inside the wrapper's `catch`, and the rejected promise goes unhandled (Express 4.22, Node 26, no handler installed). Each call kills the app process, including every voice-audio submission. | In the wrappers, skip writing when `res.headersSent`, and wrap async handlers so rejections reach `next(err)`. |
| NC-007 | confirmed | — | `apps/WebLocalAreaNetwork/config/index.js:21` | WebLocalAreaNetwork serves every file under `<repo>/../../` (for example `/www/` or `D:\www\`) to 0.0.0.0:3900 with no auth, and its containment check is a string-prefix test that sibling directories bypass. | Scope the root to an explicit share directory, bind loopback or require a token, and check containment with `path.relative` plus `realpath` (no leading `..`). |
| NC-008 | confirmed | — | `apps/dingdoudou/lib/superCode.ts:17` | The dingdoudou extension ships three hardcoded master license codes and the public signing salt, so anyone can unlock the unlimited "super" license offline or mint new codes. | Replace the shared-salt checksum with an asymmetric signature (a server-held private key, with only the public key shipped), drop the master codes, and bind codes to a device id plus expiry. |

### 4.3 Medium

| ID | Conf | R4 verdict | Location | Defect | Suggested fix |
|---|---|---|---|---|---|
| PR-007 | confirmed | — | `pycore/pyfoundations/pybasecommon/commander.py:303-328` | `exec_silent` ignores `timeout`/`capture_output` kwargs, and `exec_realtime` reads stdout fully before stderr, which can deadlock. | Implement `timeout` with `subprocess.run(..., timeout=)` or `communicate()`, and drain stderr concurrently (for example `stderr=STDOUT` or `communicate`). |
| PR-008 | confirmed | — | `pycore/pyctl/terminal/terminal_scheduler.py:47-66` | `TerminalSchedulerThread` has no exception guard; any exception during a dispatch kills the thread and all later schedules silently stop. | Catch per-dispatch failures, complete the dispatch as failed, and keep the loop alive. |
| PR-009 | confirmed | — | `pycore/pyutils/laravel/worker_result_delivery.py:80-82` | The worker result poster opens a global 30 s "retry-after" window after any failed POST (including a single-shot progress ping); terminal results posted in that window are dropped with no retry and no deferral. | Apply the backoff only to best-effort progress pings, or queue terminal results durably (the outbox) instead of returning False. |
| PR-010 | likely | — | `pycore/pyutils/laravel/endpoint_manager.py:595-668` | `LaravelEndpointManager.resolve()` writes `_resolved` from caller threads and never invalidates it on failure: the user's endpoint selection can be overwritten by a stale probe, and a failover winner stays forever. `[in-flight 02:46]` | Tag each resolve with a selection generation and apply `_resolved` only when the generation is unchanged. Clear `_resolved` on the offline edge of the resolved URL. |
| PR-011 | confirmed | — | `pycore/pyutils/common/x11_display.py:326-354` | `X11Display._open()` never tries a connection without a cookie, so X servers reachable without an Xauthority file (xhost / Xvfb / root with `xhost +SI:localuser:root`) are reported as unavailable. `[in-flight 02:52]` | Try one connection without a cookie (unset XAUTHORITY) when no candidate works, and scope the XAUTHORITY override to the connect call. |
| PR-012 | likely | — | `pycore/pyutils/common/process_restart.py:95-114` | On a Windows console restart, `pyservice.ps1` kills the dashboard UI server because the successor process is not listening yet when the parent exits. | In `pyservice.ps1`, treat `$LASTEXITCODE -eq 3` as "handoff, keep the UI server", or wait with a bounded poll for the successor listener. |
| PR-013 | likely | — | `pycore/pyctl/agent_history/pipeline/planner.py:124-165` | The agent-history planner and worker save a full stale config snapshot, which reverts user changes made during planning (for example turning the pipeline off). `[in-flight 02:46]` | Save only the cursor keys the planner and worker own (`cursors`, `live_cursors`, `live_completed`, `backfill_targets`, `last_tool`, `cursor`), not the whole config. |
| PR-022 | confirmed | — | `pycore/pyfoundations/file_lock.py:279-346` | `FileLockManager` does not give mutual exclusion, either between processes or between threads. | Use an OS lock (`fcntl.flock` / `msvcrt.locking`) on one lock file, or `os.open(O_CREAT\|O_EXCL)` on a single name, plus a per-instance thread-safe owner. |
| PR-024 | confirmed | — | `pycore/pyutils/launcher/linux_terminal_launcher.py:114-125` | The launcher's grid shell rc file is world-writable and is sourced by root terminals, so any local user can run code as root. Settings and prompt files are also world-writable. | Write the rc file per UID (`$XDG_RUNTIME_DIR` or `/run/user/<uid>`, mode 0600, owned by the running user) or pass `--rcfile` from a read-only repo path. Keep settings files 0600/0644, not 0666. |
| PR-026 | confirmed | — | `pycore/pyutils/common/dev_reload.py:117-123` | Hot reload (on by default) restarts pycore in a loop while any watched file has a future mtime, and CodeSync writes received files with the peer's mtime. | Keep future-mtime files in the baseline (or clamp them to the start time), and make codesync keep the local write time or clamp `server_mtime` to now. |
| PR-027 | likely | — | `pycore/pyctl/agent_history/root_spool.py:196-222` | The root agent-history spool exposes other human users' private agent sessions to the desktop worker user. | Spool only files owned by root (or an explicit allowlist of slot accounts), and use a spool group that contains only the worker user. |
| PR-032 | confirmed | — | `pycore/pyfoundations/pygvar.py:74,85,88` | `pygvar` creates `D:\.tmp` (Windows) or `/var/_core_node/_tmp` (Linux) at import with no fallback, so pycore fails to import on a Windows host without a D: drive, or on a first Linux run as a non-root user. `[pass 2]` | Derive TMP_DIR from `core_node_dirs` (with its fallbacks), or fall back to `tempfile.gettempdir()` when the preferred root cannot be created. |
| PR-033 | likely | — | `pycore/pyutils/agent_history/article_records.py:90-99` | Article-record writes use one temp file name per process, so concurrent writers corrupt or silently drop `index.json` / `<id>.json` updates. `[in-flight 02:52]` `[pass 2]` | Use a unique temp name per write (`uuid4`), and route index read-modify-write through one serialized owner. |
| PR-034 | likely | — | `pycore/pylauncher/platform/system_service_manager.py:67-80,96-107` | Turning the Linux "system service" tray toggle OFF from inside the running `pycore` unit kills its own uninstall before it disables or removes the unit. `[pass 2]` | Run the uninstall outside the unit's cgroup (`systemd-run --scope`, or ask a helper), or reorder it to `disable` → `rm` → `daemon-reload` → `stop --no-block`. The script-order half belongs to the shell owner. |
| PR-035 | likely | — | `pycore/pyutils/window/windows_terminal_backend.py:121-130` | Windows terminal paste right-clicks the window center. In a classic console (`ConsoleWindowClass`, QuickEdit) the preceding activation click starts a selection, so the right-click copies instead of pasting, and Enter is still sent. `[pass 2]` | Paste with a keyboard shortcut (Ctrl+Shift+V in WT; for conhost, send the text with `SendInput` Unicode events), or clear any selection with Esc before the right-click. |
| PR-037 | likely | — | `pycore/callmodule/rpc_routes/voice_subtitle_routes.py:37` | An unauthenticated client can switch on the clipboard monitor and then read every clipboard change from the log stream (remote clipboard spying). `[pass 2]` | Never log clipboard content (log only its length or hash), and require authentication for the start route (PR-001). |
| AT-007 | confirmed | CONFIRMED (→low) | `pycore/pyutils/tts/batch/kokoro_batch.py:79-93` | The Kokoro batch still falls back to per-word synthesis, which violates the single batch entry with no per-word fallbacks | return per-word failures from the one batched call (skip the failed index) and remove the serial fallback. |
| AT-008 | confirmed | CONFIRMED (→low) | `pycore/pyctl/audio_orchestration/orch_resources.py:199` | Orchestration waits by polling a signal nobody sends, every second (up to 900 s for lane items and for the whole book sync) | wait on `AUDIO_QUEUE_CHANGED_SIGNAL` (or a per-owner settle signal) and on a job-finished signal from `BackgroundJobs`. |
| AT-009 | confirmed | CONFIRMED | `pycore/pyutils/tts/word_audio_cache.py:42-43` | Word-cache writes keep the word's case but lookups lower-case it, so capitalized dictionary words never hit the cache `[in-flight 02:44]` | normalize the word (lower-case) in `get_cache_path` and migrate existing files once. |
| AT-010 | likely | CONFIRMED | `pycore/tts_install_assets/qwen3tts_queue.py:17` | The qwen3tts queue admits one job, while the sentence lane (3) and orchestration (4) fan out, so the client polls /status every ≤2 s, queue-head order is decided by retry timing, and jobs fail after 900 s `[in-flight 02:43]` | let the server queue hold N pending jobs (FIFO), and have clients wait on queue events instead of status polling. |
| AT-011 | likely | PLAUSIBLE | `pycore/pyctl/audio_orchestration/orch_service.py:375-381` | `_recover_task_status` can overwrite a task that just finished with a stale "generating" copy and mark it failed `[in-flight 02:43]` | re-read the task under the file owner before recovering, or only recover at startup (`resume_interrupted_generations`). |
| AT-012 | likely | CONFIRMED | `pycore/pyctl/audio_orchestration/orch_delivery.py:104-113` | The output `meta_hash` is cached per segment signature but also covers book sentences whose availability changes, so every reconcile re-delivers the task and "delivered" never turns true `[in-flight 02:42]` | exclude volatile or optional fields from `meta_hash`, or make the signature include sentence-cache presence and allow the cache entry to be replaced. |
| AT-013 | likely | CONFIRMED | `pycore/pyctl/audio_orchestration/orch_delivery.py:96` | Output delivery and hashing load every finished segment mp3 of a task into memory at once `[in-flight 02:42]` | stream the hashes (hashlib over file chunks) and upload segments by path one at a time. |
| AT-014 | confirmed | CONFIRMED | `pycore/pyctl/audio_orchestration/orch_service.py:477-486` | Deleting a task, including the automatic retention prune of prompt-rewrite tasks, leaves its manifest and all segment mp3 files on disk forever `[in-flight 02:43]` | delete the manifest and output directory as part of `task_delete`, once any pending output delivery has its payload. |
| AT-016 | likely | PLAUSIBLE | `pycore/pyutils/tts/engine_policy.py:375-387` | A UI engine test mutates process-wide environment (QWEN3TTS_SPEAKER/INSTRUCT, COSYVOICE_*, …) for the whole synthesis, and concurrent lane or orchestration work picks it up `[in-flight 02:46]` | pass engine extras through the request object and never through `os.environ`. |
| AT-017 | confirmed | CONFIRMED | `pycore/pyutils/tts/tts_orchestrator.py:693` | `tts_test` calls an undefined `_engine_disabled_reason`, so testing any unavailable non-server engine raises NameError | import it from `tts_status` and call it with `(name, False)`. |
| AT-018 | confirmed | CONFIRMED | `pycore/pyctl/audio_orchestration/orch_words.py:28` | Word tokenization treats a whole CJK clause as one "word", so word steps for zh/ja books generate clause-length "words" | use a CJK word segmenter (or the Laravel sentence-words resolver) for zh/ja/ko and skip non-dictionary tokens. |
| AT-041 | likely | — | `pycore/pyutils/tts/kokoro_engine.py:113-114` | Model-engine "is loaded" probes queue behind in-flight synthesis, and they run on the managed-service owner thread, so a status read during a Kokoro/sherpa/Parler/Bark synthesis freezes every engine lease `[pass 2]` | keep "loaded" as a plain flag the model owner updates (a `SerializedValue`), so it can be read without entering the model queue. |
| AT-042 | likely | — | `pycore/pyutils/tts/qwen/engine.py:61-72` | With missing or incomplete qwen3tts weights the engine still reports ready, and every sentence task sleeps the 300 s recovery budget before failing `[pass 2]` | fold `qwen_weights.local_weights_ready` (or `resolve_model_id(allow_remote=False)`) into the qwen `config_ready` / `disabled_reason` gate so that recovery is skipped with an actionable reason. |
| LB-011 | likely | — | `poly_apps/laravel_main/app/Services/OctaneTimerService.php:62-81,143-185,207-230` | Timer state is lost every minute on FrankenPHP, so long-interval tasks run every minute | Call `writeHeartbeatSnapshot()` at the end of `tick()`, or keep `last_run` in the cache store, so intervals survive process restarts. |
| LB-012 | likely | — | `poly_apps/laravel_main/app/Services/OctaneTimerService.php:328-330,354-447` | Long synchronous jobs run inline in the one timer heartbeat and stall every 1-second task (Mercure outbox, result write-back, DataSync driver) | Run slow tasks in their own process or lane (implement `EXECUTION_BACKGROUND`, for example a separate `schedule:work` event per slow task), and keep the heartbeat for sub-second tasks. |
| LB-013 | likely | — | `poly_apps/laravel_main/app/Services/QueueCenter/DictLane/DictLaneQueueCenter.php:50,258-318` | Dict-lane inflight reservations live in per-thread statics and are cleared on any dictionary write, so batched lanes create duplicate claim tasks for the same words | Exclude the words already in live claim tasks of the lane when taking a batch (DB-backed reservation, or a `group_key` per batch word), rather than relying on process-local statics. |
| LB-014 | confirmed | — | `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Services/AppQyV1DurableOffsetUploadService.php:59-68,87-97` | Offset-v1 spools are never deleted after word or sentence TTS uploads (every uploaded audio file stays on disk twice), and a mismatching complete spool wedges its identity | Delete the spool once the consumer has stored the bytes (and on a whole-file mismatch), and add an age-based sweep for abandoned spools. |
| LB-015 | likely | — | `poly_apps/laravel_main/app/Services/Relay/RelayDeviceIdentity.php:56-63,137-140` | `pycore.client` (PycoreClientOnly → RelayDeviceIdentity) can never verify a machine: its only enrollment path `api/relay/machine/register` no longer exists, so the middleware rejects every caller | Merge RelayDeviceIdentity into RelayDeviceSignatureService (one enrollment and claim store), and point PycoreClientOnly at it. |
| LB-016 | confirmed | — | `poly_apps/laravel_main/app/Services/DataSync/ResourceSyncService.php:91-95,127-130` | One resource file deleted after the manifest was taken fails the whole sync session | Return a per-file `missing` marker, and have the driver call `skipResource()` for it, as it already does for hash mismatches. |
| LB-017 | likely | — | `poly_apps/laravel_main/app/Services/DataSync/DataSyncDriverService.php:573,604-613` | The fetcher asks the exporter for manifests of the fetcher's own resource-root keys, and one missing key fails the session | Have the exporter publish its root keys (in the status or inventory response), and sync the intersection. Record keys that exist on only one side as skipped. |
| LB-018 | confirmed | — | `poly_apps/laravel_main/app/Services/DataSync/DataSyncPassiveService.php:80-94,114-130` | Heavy work still runs inside peer HTTP requests (fresh manifest, exact row counts, 7z archive creation) | Have the passive timer compute the post-transfer manifests, the counts and the archives as artifacts, and have the requests serve them (503 until ready). |
| LB-019 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Utils/CodeMartV1OtpService.php:18-28` | CodeMart phone OTP is never sent (SMS is a TODO) and is written to the log in plain text, so no user can complete the `phone_verification` onboarding step | Wire a real SMS provider (or mark the step optional in the contract), stop logging the OTP, and compute `email_verified` from `email_verified_at` only. |
| LB-020 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Ctl/CodeMartV1DepositCtl.php:217-228` | Alipay and WeChat deposit `payment_url` values are unsigned placeholder URLs, so a deposit through those methods can never be paid | Offer only `bank_transfer` in `DEPOSIT_PAYMENT_METHODS` until a real gateway integration exists. |
| FU-005 | confirmed | — | `apps/laravel-manager/components/views/database-manager/DataSyncTab.tsx:325-333` | Data Sync (U3) action errors disappear within 2 s because the workspace poll overwrites the shared `error` state. | separate `actionError` (cleared by user action) from the workspace/node error. |
| FU-006 | confirmed | — | `apps/laravel-manager/models/DataSyncModel.ts:235-258` | Data Sync resolves a typed new-server address by hostname only, ignoring port and scheme, so it targets the wrong node or wrongly reports "same node". | compare normalized `scheme://host:port` (`normalizeAdhocAddress(input)` vs `syncTarget`). |
| FU-007 | likely | — | `apps/mcp-chrome/app/chrome-extension/entrypoints/background/services/bing-dictionary-worker-service.ts:217-236` | The Bing worker's private `pullTasksAcrossTypes` drops tasks it already claimed when a later type's pull fails, and it over-claims up to 3× batchSize. | reuse the base implementation (or keep merged and `releaseTasks` on the partial failure). |
| FU-008 | confirmed | — | `apps/wordnew/services/WordNewBookReaderPlayback.ts:255-259` | Pausing while the next clip's URL is being resolved does not stop playback: audio starts under a "paused" UI, and the next press does not pause it. | after the await, if paused, set `src` but skip `play()`. `resume()` already calls `this.audio.play()`. |
| FU-009 | likely | — | `apps/wordnew/components/orch-audio/useOrchAudioPlayback.ts:185-194` | An orchestrated-audio segment jump plays a stale segment, or plays audio after the page is left, when its sentence-page fetch resolves late. | bump a jump sequence (or check mounted) and ignore superseded resolutions. |
| FU-010 | confirmed | — | `apps/codemart/components/workspace/CmProjectAnalysisPanel.tsx:47-70` | CodeMart's AI-analysis panel keeps polling after unmount, and when the analysis finishes it reloads the page it no longer shows. | a disposed flag, checked before re-arming and before calling `onProjectChanged`. |
| FU-011 | confirmed | — | `apps/pycore-manager/pages/vocabulary/VocabLibrariesTab.tsx:96-112` | The pycore-manager Libraries tab fetches Laravel on every keystroke of the language box with no stale guard, so the wrong language's libraries can remain. | apply the language on submit or blur, or debounce, and ignore superseded responses. |
| FU-012 | likely | — | `apps/laravel-manager/models/DataSyncModel.ts:460-475` | Data Sync caches each node's API client with the node's "current" flag frozen, so after the active endpoint changes, requests to the new current node carry no token and their 401s are swallowed. | resolve `current` at request time (`apiManager.getCurrentEndpoint()`), or key the cache by id+current. |
| FU-013 | likely | — | `apps/codemart/components/workspace/useCmPagedList.ts:41-55` | CodeMart paged lists have no stale-response guard, so quick filter or page changes can show the wrong result set. `[in-flight 02:42]` | a request counter ref, as in `useCmAdminList`. |
| FU-014 | confirmed | — | `apps/codemart/admin/CmAdminPages.tsx:419-434` | The admin KYC document viewer can show one document under another's label when the admin switches documents mid-load. `[in-flight 02:42]` | remember the requested type (or a sequence number) and ignore mismatched responses, revoking their blob URL. |
| FU-031 | confirmed | — | `poly_apps/pycore_laravel_wordnew_ui/core/network/api-client/MasterApiClient.ts:256-305` | wordnew's offline write queue replays one user's writes under whoever is logged in when it drains. `[pass 2]` | store the owner scope (a token hash or user id) with each entry, and drop or hold entries whose owner differs from the current session; clear the queue on logout. |
| FU-032 | confirmed | — | `apps/wordnew/services/WordNewQueueCenter.ts:202-220` | wordnew's word-audio wait is answered from a 15-minute local mirror after its second attempt, so it never sees the audio become ready. `[pass 2]` | poll status endpoints with `cacheMode:'network'` (`authedGetFreshJSON` style), or do not mirror non-ready media states. |
| FU-033 | confirmed | — | `apps/vortex/VortexApp.tsx:699-812` | The Vortex simulated account creates money: selling returns the full leveraged position value instead of margin plus P&L, and the sell fee is inverted. `[pass 2]` | store the margin per position; on close credit `margin + qty × (exit − entry) − fee`, and multiply by `(1 − fee)` on sells. |
| FU-034 | confirmed | — | `apps/mcp-chrome/app/chrome-extension/utils/media-image-search.ts:37-66` | The mcp-chrome image search downloads candidate images with no timeout or size cap, so one slow host stalls the poster/cover worker. `[pass 2]` | `fetchWithTimeout` (`utils/async`) with a byte cap; skip on timeout; `bytesToBase64` from `utils/binary`. |
| IS-005 | confirmed | — | `scripts/pytools/special_software_env_manager/managers/script_manager.py:385` | `claude1`–`claude5` launchers (sh and ps1) print the full ANTHROPIC_AUTH_TOKEN to the terminal | mask values whose name matches TOKEN/KEY/SECRET in both generators (reuse the ark masking), then regenerate. |
| IS-006 | likely | — | `scripts/winenvs/ark1.ps1..ark7.ps1:416` | BOM-less UTF-8 .ps1 files with non-ASCII inside double-quoted strings fail to parse under Windows PowerShell 5.1 on non-UTF-8 code pages | save the win_common/steps/winenvs .ps1 files as UTF-8 with BOM, or replace non-ASCII punctuation in strings with ASCII. |
| IS-007 | confirmed | — | `scripts/shells/linux/debian/install_shells/3_setting_base.sh:418` | Global `git config http.sslVerify false` disables TLS verification for every git operation of the installing user | drop the global override; scope it to a specific remote only if a broken proxy needs it. |
| IS-008 | confirmed | — | `scripts/shells/linux/common/fs_perm_helpers.sh:185-202` | dd.sh startup chmods the whole project tree to 777, including decrypted secrets `[in-flight 02:51]` | exclude `.secret_keys`, `.secrets` and `.git` from the 777 walk and keep secrets at 0600/0700 owned by the permission user. |
| IS-009 | likely | — | `scripts/shells/linux/common/ssh_server_common.sh:154-162` | SSH tmux persistence hook opens a new `main-N` session instead of resuming after a dropped connection | attach with `tmux new-session -A -D -s main` for the base session (detach other clients) or detect stale clients by `client_activity`; alternatively allow a finite ClientAliveCountMax. |
| IS-010 | confirmed | — | `scripts/shells/linux/dd_helper/secret_functions.sh:338` | Secret-decryption passwords are passed on the node command line, and step 27 echoes the SSH key password while typing `[in-flight 02:51]` | pass the password via stdin or an environment variable to the node tools, and read it with `read -s` (or the existing asterisk reader). |
| IS-011 | confirmed | — | `scripts/webtools/file_server.py:1125` | webtools/file_server.py crashes on every start with NameError 'platform' | `import platform` at the top. |
| IS-012 | confirmed | — | `scripts/shells/linux/debian/install_shells/75_install_postgresql.sh:483-494` | PostgreSQL "data dir drift" check and configure disagree on the expected path, so PostgreSQL is stopped/restarted on every run | derive the expected dir from one shared function used by both configure_postgresql and pg_data_dir_drifted. |
| IS-013 | likely | — | `scripts/shells/linux/debian/install_shells/9_fix_dns.sh:281-327` | DNS fixer replaces the resolv.conf symlink with a static file and makes it immutable (`chattr +i`) | do not set +i; prefer a systemd-resolved drop-in (already attempted in step 4) and only write a static file after an explicit prompt. |
| NC-009 | confirmed | — | `apps/VoiceClientAndCaddy/http_controller/dict_server.js:190-191,203,212` | Voice `submit_audio_simple` always fails: `watcher` is a local string constant, so `watcher.addToIndex(...)` throws `TypeError` after the first file is copied, and the uploaded temp file is not deleted. | Import the real watcher instances (the ones used by `server_voice_load`) instead of string names. |
| NC-010 | confirmed | — | `ncore/utils/mcp_server/SingleInstanceManager.js:61-62` | `SingleInstanceManager` can let two instances run: acquisition is a non-atomic check-then-write, the stale threshold is only 1 s above the heartbeat period, heartbeats overwrite the lock unconditionally, and `releaseLock()` deletes the file without checking ownership. | Create with `fs.openSync(path, 'wx')`, check pid liveness instead of a 1 s timing margin, and compare `lockData.pid === process.pid` before rewriting or unlinking. |
| NC-011 | confirmed | — | `ncore/foundation/common/system_paths.js:283-303` | On a Windows machine without a writable `D:` drive, every ncore entry point crashes at module load: `system_paths.js` falls back to the unusable `D:\www\core_node`, then creates subdirectories without a guard. | Add a user-writable Windows fallback (for example `%LOCALAPPDATA%\core_node`), guard the module-level mkdirs, and derive `DATA_DRIVER` from the same resolved root. |
| NC-012 | confirmed | — | `ncore/global_vars/tool/soft-install/linux-apt/package_manager.js:234-249` | `package_manager.remove()` runs `<install command> remove <pkg>` (for example `sudo apt install -y remove foo`), so it never removes anything, yet it reports success. `purge()` on non-apt managers delegates to it. | Add a per-manager `remove` command (`apt-get remove -y`, `apk del`, `opkg remove`, `dnf remove -y`) and check the exit status. |
| NC-013 | confirmed | — | `ncore/utils/rpc/http_rpc/HttpRpcServer.js:201-206` | RPC sessions leak without bound: HTTP RPC creates a session per distinct client id and nothing ever calls `SessionManager.cleanup()`, and the WS close handler deletes the client entry before reading its `sessionId`, so WS sessions are never removed either. | Start a `cleanup()` interval in `SessionManager`, read `clientInfo` before deleting it in the WS close handler, and cap client-chosen ids. |
| NC-014 | confirmed | — | `ncore/utils/mcp_server/SingleInstanceManager.js:322-330` | Several modules register SIGINT/SIGTERM handlers that call `process.exit()` synchronously, which cuts off the async graceful shutdowns registered by others (ThreadBus services, the MCP server, the browser). | Keep one shutdown coordinator (ThreadBus) and let every other module register an async hook with it instead of calling `process.exit`. |
| NC-015 | likely | — | `ncore/global_vars/libs/user_settings.js:58-80` | Settings writes lose data: `settings.json` is rewritten non-atomically; a failed parse (for example a concurrent half-written file) returns `{}` and the next `setKey` persists only that key; and `SettingsCenter` saves its stale in-memory cache over newer on-disk changes. | Write via a temp file plus `rename`, refuse to save after a parse failure, and have `SettingsCenter` re-read and merge before saving. |
| NC-016 | confirmed | — | `ncore/global_vars/tool/common/ffinder.js:228-284` | `ffinder` performs a fully synchronous recursive scan (`readdirSync`/`realpathSync`) inside `async` functions, so its "20 second timeout" can never fire and the whole process blocks for the duration. It also walks every directory twice (`countDirs` exists only for statistics). | Use `fs.promises.readdir` (or a worker) with a real abort signal, and drop the `countDirs` pre-walk. |
| NC-017 | confirmed | — | `ncore/global_vars/tool/soft-install/winget/winget.js:106-131` | `winget.configureSource()` removes the default `winget` source, then adds the USTC mirror, and records "configured" regardless of the outcome. If the add fails, winget is left with no main source and the code never retries. | Check the output of `source list` after the add, and only then persist `configured`. On failure, `winget source reset --force`. |
| NC-018 | confirmed | — | `ncore/foundation/common/commander.js:171-184` | `wrapEmdResult` and `wrapTextResult` overwrite `error` with `stdout`, so every async command result (`execCommand`, `spawnAsync`) loses stderr and reports stdout as the error text. | `error = byteToStr(error)`. |
| NC-019 | likely | — | `apps/VoiceClientAndCaddy/basetool/ptools/edge_tts_py.js:122-126` | The edge-tts command line is built by string interpolation: the binary path is unquoted, and `queueItem.content` sits inside `"…"`. Text containing `"`, `$(...)`, backticks or `%VAR%` breaks generation or runs shell code. Failed generations are still reported as generated. | `spawn(edgeTTSBinary, ['--voice', v, '--text', content, '--write-media', m, '--write-subtitles', s])`, and check `success`. |
| NC-020 | likely | — | `ncore/utils/jsmcptools/server.js:52` | The MCP Chrome HTTP server enables CORS for `*` on its loopback endpoint, so any web page open in the user's browser can call the browser-automation tools through `http://127.0.0.1:<mcp_chrome>/mcp`. | Restrict origins to the extension id and localhost UI, validate `Host`, and require a bearer token. |
| NC-021 | confirmed | — | `ncore/mcp_server/placeholder_image_generator/main.py:1969` | The placeholder-image MCP server is a FastMCP stdio server, yet it writes 164 `print()` lines (and `pip install` output) to stdout, the JSON-RPC channel, so MCP clients receive non-protocol lines interleaved with responses. | Route all diagnostics to stderr (logging to stderr), run pip with `stdout=sys.stderr`, and save in the format implied by the extension. |
| NC-022 | confirmed | — | `main.js:170-172` | `node main.js app=<x> --service` always crashes: `main.js` calls `installService()` with no argument, and `installService(config)` destructures `config` right away. | Build the config in `main.js` (`{ name: appname, execPath: process.execPath, entry: main.js path, args }`), return early on missing fields, and gate by platform. |

### 4.4 Low

| ID | Conf | R4 verdict | Location | Defect | Suggested fix |
|---|---|---|---|---|---|
| PR-014 | confirmed | — | `pycore/pyutils/laravel/client.py:275-292` | The pycore identity signature hashes `str(dict)` for form or multipart bodies, so the signed content digest never matches the bytes actually sent. `[in-flight 02:46]` | Sign after encoding: build the request with `httpx.Request`/`requests.PreparedRequest` and hash the prepared body. |
| PR-015 | likely | — | `pycore/pyutils/common/xdg_desktop_portal.py:308-311` | The XDG portal client caches one session-bus connection forever and leaks match rules. | Drop and reopen the connection on transport errors, and remove the match rule after each request. |
| PR-016 | likely | — | `pycore/pyfoundations/desktop_session.py:149` | `xauthority_candidates` crashes with `FileNotFoundError` when a cookie file disappears between `glob` and `getmtime`. | Stat each candidate defensively and skip files that disappeared. |
| PR-017 | likely | — | `pycore/pyutils/common/clipboard_text.py:60-78` | The Windows clipboard PowerShell fallback garbles non-ASCII text. | Set `[Console]::InputEncoding/OutputEncoding = [Text.UTF8Encoding]::new($false)` in both scripts. |
| PR-018 | confirmed | — | `pycore/pyctl/terminal/terminal_service.py:227-250` | Terminal input replaces non-text clipboard content with an empty string. | Skip the restore when the backup was not text, or back up and restore the native clipboard formats. |
| PR-019 | confirmed | — | `pymain.py:56-67` | `pymain.py` always exits 0, even when the app fails. | `sys.exit(0 if success else 1)` and exit 1 after the traceback. |
| PR-020 | confirmed | — | `pycore/pyutils/codesync/runtime.py:77-140` | CodeSync runtime re-implements shared primitives (bus tasks, machine id). | Import the shared `start_bus_task` and machine-id helpers, and keep only the injection hook. |
| PR-023 | likely | — | `pycore/pyfoundations/pg_sync_adapter.py:491-520` | `pg_sync_adapter` reports a PostgreSQL restore as complete even when psql hit errors, after `--clean` has already dropped the target objects. | Run psql with `-v ON_ERROR_STOP=1` (or scan stderr for `ERROR` on rc 0) and mark the sync as failed. |
| PR-025 | likely | — | `ncore/utils/mcp_server/SingleInstanceManager.js:61-68` | ncore `SingleInstanceManager`: two instances can both believe they own the lock, and an exiting instance deletes the new owner's lock. | Raise the stale threshold well above the heartbeat (for example 3×), and check the owner PID before each heartbeat write and before release. |
| PR-036 | likely | — | `pycore/pyutils/external_apis/word_audio_client.py:478-500` | External API keys leak into logs through exception text: the Forvo key is in the URL path, and the TMDB, OMDB and SerpApi keys are in query params. These logs reach the unauthenticated `/api/events` log stream. `[pass 2]` | Send keys as headers where the API allows it. Otherwise redact the URL and query from exception text before logging (log `type(exc).__name__` and the host only). |
| AT-019 | likely | CONFIRMED | `pycore/pyctl/tts/laravel_audio_worker.py:321-329` | Lane capacity counts the whole Queue (including the full-pull backlog), so the compatibility accept RPC and bounded dispatch always reject and release claimed tasks `[in-flight 02:43]` | measure the in-flight work (`_processing`/inflight), not the queued backlog. |
| AT-020 | confirmed | CONFIRMED | `pycore/pyctl/queue_center/audio_lane_state.py:168-173` | The lane-state publisher wakes every 5 s even when idle and builds the full local audio state | arm the heartbeat only while a lane reports active (wait without a timeout otherwise). |
| AT-021 | suspect | PLAUSIBLE | `pycore/pyctl/tts/laravel_audio_worker.py:649-657` | `_start_drain` is a non-atomic check-then-set, so two drain cycles can run at once `[in-flight 02:43]` | use a `SerializedValue.compare_and_set` guard, as `request_pull` does. |
| AT-022 | confirmed | CONFIRMED | `pycore/pyctl/tts/laravel_audio_worker_execution.py:712-722` | The `processing` counter is decremented for tasks that were never marked started | call `_mark_task_finished` only when started (track a flag). |
| AT-023 | likely | CONFIRMED | `pycore/pyutils/tts/tts_orchestrator.py:262-268` | When a sentence speaker is selected, the lane writes the central sentence cache under a key orchestration never looks up, so orchestration re-synthesizes lane-settled sentences | derive both sides from one cache-identity helper that includes the lane speaker. |
| AT-024 | confirmed | CONFIRMED | `pycore/tts_install_assets/tts_text_chunking.py:72-73` | The chunker's abbreviation and initial shields are anchored at end of string, so "Mr." / "Dr." / "J." in the middle of text still split sentences | match `\b(abbr)\.(?=\s)` anywhere instead of `$`. |
| AT-025 | confirmed | CONFIRMED | `pycore/pyutils/tts/tts_service_manager.py:173-182` | The qwen3tts code-identity script set omits `tts_text_chunking.py`, which `qwen3tts_synthesis` imports | add `tts_text_chunking.py` (and every sibling it imports) to the qwen3tts script list. |
| AT-026 | likely | CONFIRMED | `pycore/pyutils/tts/audio_task_queue.py:232-277` | `prune_absent` drops a Part1-claimed Laravel entry without settling its tracker entry, which stays "queued" forever | route pruning through the library so it settles (or drops) tracker entries. |
| AT-027 | confirmed | CONFIRMED | `pycore/pyctl/tts/sentence_audio_full_sync.py:45-64` | The sentence full pull reports success with nothing pulled when the language listing is not HTTP 200 | return `laravel_failure(status_code=…)` when the listing call fails. |
| AT-028 | confirmed | CONFIRMED | `pycore/pyutils/tts/batch/batch_common.py:66` | `group_words` drops empty words while `synthesize_words_to_cache` maps result items by original index (latent misalignment) | keep index alignment (mark empty inputs failed) instead of filtering. |
| AT-029 | confirmed | CONFIRMED | `pycore/tts_install_assets/chattts_api_server.py:241` | The chattts and f5tts servers bind 0.0.0.0 with no authentication when pycore starts them | pass loopback `*_HOST` from the launcher, as done for melotts/voxcpm2/fishspeech. |
| AT-030 | confirmed | CONFIRMED | `pycore/tts_install_assets/f5tts_api_server.py:70-90` | The f5tts `/process` endpoint is `async` but runs blocking inference on the event loop, and leaks one temp directory per request | use a sync `def` (threadpool), and clean up the tmp dir after the response. |
| AT-031 | confirmed | CONFIRMED | `pycore/pyutils/tts/voxcpm2_engine.py:160` | The VoxCPM2 server ignores the `speed` (and language) the client sends | time-stretch once server-side, as qwen does, or drop the field. |
| AT-032 | confirmed | CONFIRMED | `pycore/pyutils/tts/tts_service_manager.py` | `_stop_foreign_server` terminates any process listening on an engine's port | reclaim only PIDs that pycore launched (a pid file or code identity). |
| AT-033 | confirmed | CONFIRMED | `pycore/pyutils/tts/engine_policy.py:172-174` | Any saved engine order that starts with "edge" is treated as legacy and reset on every reload `[in-flight 02:46]` | migrate only the exact `_LEGACY_SAVED_ORDERS` tuples, once (store a migration marker). |
| AT-034 | confirmed | CONFIRMED | `pycore/pyctl/tts/qwen/operation_service.py:52-58` | The Qwen durable synthesis operation re-runs on an idempotent re-submit and discards the audio it produces | skip `_run` for terminal operations, and store the audio (a path or asset) in the result. |
| AT-035 | likely | PLAUSIBLE | `pycore/pyutils/tts/edge/client.py` | The edge availability probe has no timeout and runs on the edge client's state-owner thread | wrap `_probe` in `asyncio.wait_for(get_synth_timeout())` and run it off the owner thread. |
| AT-036 | suspect | PLAUSIBLE | `pycore/tts_install_assets/qwen3tts_gpu.py:42-53` | nvidia-smi subprocesses have no timeout in the qwen3tts and chattts servers | add `timeout=` to `subprocess.run`. |
| AT-037 | confirmed | CONFIRMED | `pycore/pyutils/tts/edge/thread_manager.py` | Dead duplicate TTS worker and queue implementations, plus per-word synthesis and Laravel-upload side routes `[in-flight 02:44]` | delete the dead modules and routes (AGENTS.md: remove duplicate implementations). |
| AT-038 | confirmed | CONFIRMED | `pycore/pyutils/tts/chunked_synthesis.py:220-298` | Three separate chunk-and-concatenate implementations | route all engines through `tts_text_chunking.split_text` plus one assembly module. |
| AT-039 | confirmed | CONFIRMED | `pycore/pyctl/audio_orchestration/orch_generate.py:404` | Orchestration UI text is hardcoded English and raw exception text, and one message is wrong `[in-flight 02:43]` | emit stable message codes plus parameters, localized in the UI. |
| AT-040 | confirmed | CONFIRMED | `pycore/pyctl/tts/batch_startup_selfcheck.py:105` | Raw `threading.Thread` / `threading.Lock` and sleep loops against the PYTHON_PYCORE threading rule | use Thread subclasses, THREAD_BUS signals and `SerializedValue`. |
| AT-043 | confirmed | — | `pycore/pyutils/tts/tts_orchestrator.py:612-622` | The "Speed" field of the UI TTS engine test never reaches the engine `[pass 2]` | map `extra_params["speed"]` into `TTSSynthesisRequest.speed` (or into `rate`). |
| AT-044 | confirmed | — | `pycore/pyutils/tts/batch/chattts_batch.py:47` | The ChatTTS batch requests WAV, but pycore's own ChatTTS server always returns MP3, so the merged batch path never works and every group falls back to per-word calls with a random voice each `[pass 2]` | honour `response_format` in the server (and a fixed speaker seed), or request mp3 and decode it before splitting. |
| AT-045 | likely | — | `pycore/pyutils/tts/serialized_model_engine.py:103-111` | `SerializedModelEngine.unload` (Parler, Bark) drops the model reference but never releases the CUDA cache `[pass 2]` | after dropping the model, run `gc.collect()` and `torch.cuda.empty_cache()` on the owner thread. |
| AT-046 | likely | — | `pycore/pyutils/tts/bark_engine.py:72-73` | Bark ignores the language and has no long-text chunking, so non-English text uses an English voice and long input is truncated `[pass 2]` | pick the voice preset per language, and chunk through `tts_text_chunking` (or restrict the Bark language set). |
| AT-047 | suspect | — | `pycore/pyutils/tts/cosyvoice_engine.py:46-67` | The CosyVoice client ignores speed and guesses the PCM sample rate from the model name `[pass 2]` | take the rate from the server (or `COSYVOICE_SAMPLE_RATE`, mandatory for unknown models), and pass `speed` to the endpoints that support it. |
| AT-048 | confirmed | — | `pycore/tts_install_assets/qwen3tts_web_assets/app.js:114-162` | The qwen3tts web console polls /status every 2.5 s forever and shares the single-slot job queue with pycore's lanes `[pass 2]` | use the existing queue event stream instead of polling, and scope the console's job list and cancel to its own jobs. |
| AT-049 | confirmed | — | `pycore/pyutils/tts/tts_engine_params.py` | `tts_engine_params.py` is dead, and the UI keeps its own duplicate of the engine test schema `[pass 2]` | delete the module, or make it the single source the UI fetches. |
| AT-050 | confirmed | — | `pycore/pyutils/tts/tts_engine_probe.py:43` | Engine availability reasons are hardcoded English UI text, and one hint contradicts itself `[pass 2]` | return reason codes plus parameters, localized in the UI. |
| LB-021 | likely | — | `poly_apps/laravel_main/app/Utils/FileSystemManager.php:408-413` | `writeFileAtomic` staging name uses `getmypid()`, which all FrankenPHP worker threads share; unlocked concurrent writers of one path collide | Add `bin2hex(random_bytes(6))` to the staging name, and route the copied helpers through `FileSystemManager::writeFileAtomic` (the duplicate implementations are a `rule` issue). |
| LB-022 | suspect | — | `poly_apps/laravel_main/app/Services/DataSync/DataSyncPassiveService.php:39-42` | Superseding an idle passive session in `prepare()` calls `finish()` without the session lock | Use `requestCancel()` plus `tryLock()`, the same path as `DataSyncService::cancel`. |
| LB-023 | likely | — | `poly_apps/laravel_main/app/Services/DataSync/DatabaseSyncService.php:73-108` | Database chunk export uses OFFSET pagination, which is quadratic on large tables and skips rows when the exporter deletes rows mid-sync | Use keyset pagination on the identity columns (`next_cursor` = last identity values). |
| LB-024 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Ctl/CodeMartV1TaskCtl.php:310-316` | The assigned-task budget lock compares strings, so resending an unchanged budget gets 409 | Compare with `bccomp(money(a), money(b), 2) !== 0`. |
| LB-025 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Ctl/CodeMartV1PaymentCtl.php:228-239` | Invoice numbers collide within one second, and one payment can be invoiced any number of times | Use an idempotency key or a unique `payment_id` guard, and build the number from a sequence or ULID. |
| LB-026 | likely | — | `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Services/AppQyV1OrchAudioService.php:422-446` | The orch-audio task detail resolves up to 300 words one by one (N+1) on every read (file under active edit) | Group words by language and call `requestWordBatch`, or ship the word list without audio and page it. |
| LB-027 | suspect | — | `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Services/AppQyV1OrchAudioService.php:259-318` | Concurrent first ingest of the same orch task makes the whole batch fail with 500 | Use `upsert([...], ['task_key'])`, or lock with `SELECT … FOR UPDATE` / `insertOrIgnore` and then update. |
| LB-028 | confirmed | — | `poly_apps/laravel_main/routes/api/system.php:20-23` | `store_session` gives anonymous callers unbounded, never-expiring writes into the database cache | Require auth, add `max:` rules and a TTL, or remove the unused endpoint. |
| LB-029 | likely | — | `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Controllers/AppQyV1Vocabulary/AppQyV1VocabularyLibraryPublicController.php:230-251,473-545` | The public `vocabulary/statistics?include_words=1` endpoint rebuilds, sorts and joins every public library's full word list on each call | Cache the sorted pair index per language (invalidated on library writes), or paginate in SQL over a membership table. |
| LB-030 | likely | — | `poly_apps/laravel_main/app/Services/UserConfig/UserConfigService.php:93-135,160-176` | UserConfigService readers can load a torn (empty) settings file and cache it for the rest of the scoped lifetime | Write through `FileSystemManager::writeFileAtomic` (temp file plus rename), or take LOCK_SH in `load()`. |
| LB-031 | likely | — | `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Models/AppQyV1LangSentenceModel.php:184-203` | The legacy `tts/sentence/claim` candidate query includes completed rows, so once the top 250 by occurrence are complete it returns nothing (the endpoint is public and pycore no longer uses it) | Remove the dead route, or drop the completed branch and page past fully covered rows (keyset on occurrence_count, id). |
| LB-032 | likely | — | `poly_apps/laravel_main/app/Services/Relay/RelayDeviceIdentity.php:65-72` | The machine signature canonical string leaves out the query string, so query parameters of a signed GET can be changed without breaking the signature | Sign the full `getRequestUri()` (path plus canonical sorted query). |
| LB-033 | confirmed | — | `poly_apps/laravel_main/scripts/start.ps1:662-685` | `scripts/start.ps1` kills every `php.exe` running `artisan serve\|schedule:work\|queue:listen\|reverb:start` machine-wide, whichever project it belongs to | Match on this project's resolved `artisan` path (Join-Path of the laravel_main root) in the command line before stopping. |
| LB-034 | confirmed | — | `poly_apps/laravel_main/app/Apps/CodeMartV1/CodeMartV1Ctl/CodeMartV1TaskCtl.php` | About 446 hardcoded English API response messages bypass the lang files (AGENTS.md i18n rule) | Move the messages into `lang/{en,zh_CN}/*.php` and call `__()`. Keep the machine `error_code` values as they are. |
| FU-015 | confirmed | — | `apps/codemart/contexts/CmBootstrapContext.tsx:29-41` | One failed CodeMart bootstrap refresh after a mutation replaces the current page with "access check failed", discarding the page state and the success notice. | keep the previous bootstrap when a refresh fails, and show a non-blocking error. |
| FU-016 | likely | — | `apps/laravel-manager/components/views/VocabularyLearning.tsx:339-363` | The laravel-manager Vocabulary Libraries tab has no stale guard on language switches. | a request sequence ref. |
| FU-017 | likely | — | `apps/codemart/admin/CmAdminShared.tsx:67-70` | CodeMart admin and public date formatters parse date-only values as UTC, so viewers west of UTC see the previous day. `[in-flight 02:42]` | delegate to `cmFormatDate`. |
| FU-018 | confirmed | — | `apps/pycore-manager/pages/PcTerminalPage.tsx:77-140` | Terminal viewer-lease error codes have no translation, so the UI shows "unknown error". | add the three codes with en/zh texts. |
| FU-019 | confirmed | — | `apps/wordnew/components/WfNewApiServerDialog.tsx:51` | wordnew's server dialog switches endpoints without verifying them, pinning dead or mixed-content-blocked endpoints. | call `apiManager.switchEndpoint` and toast on its result. |
| FU-020 | likely | — | `poly_apps/pycore_laravel_wordnew_ui/shared/library-cover/LibraryCoverTaskModel.ts:219-237` | The library-cover model loses already-queued tasks when a later chunk fails, and polls forever for ids the server stops returning. | keep the chunk results gathered before the failure; mark missing ids inactive. |
| FU-021 | likely | — | `apps/mcp-chrome/app/chrome-extension/entrypoints/background/services/bing-dictionary-worker-runtime.ts:251-303` | The Bing worker has no in-progress guard on start, so concurrent starts double-register the worker. | a `starting` promise that both callers await. |
| FU-022 | confirmed | — | `apps/codemart/pages/CmEstimatePage.tsx:158,169` | The CodeMart estimate form clamps its number inputs on every keystroke, so users cannot type values normally. | keep the raw string while editing and clamp on blur or submit. |
| FU-023 | suspect | — | `apps/wordnew/pages/WfNewBookReader.tsx:221-240` | The wordnew book reader applies verse loads without a stale guard and is not remounted per book. | a sequence ref in `loadVerses`; key the reader by `sourceKey`. |
| FU-024 | confirmed | — | `poly_apps/pycore_laravel_wordnew_ui/and failure:` | i18n keys missing: `pc` lacks `common.close`, laravel-manager lacks `common.cancel` / `mediaHub.loginRequired` / `common.login`, and lm zh lacks 17 en keys. | add the keys; type lm zh against en as `cm` and `pc` do. |
| FU-025 | confirmed | — | `apps/pycore-manager/pages/vocabulary/vocabShared.tsx:25-49` | UI-visible strings are hardcoded (AGENTS.md i18n rule). | move them to pc/lm locale keys and remove the mock tasks. |
| FU-026 | confirmed | — | `apps/pycore-manager/pages/PcWordAudioPage.tsx:43` | Shared utilities are duplicated (AGENTS.md reuse rule). | import the central helpers and delete the copies. |
| FU-027 | suspect | — | `poly_apps/pycore_laravel_wordnew_ui/shell/ShellLaravelEndpointBridge.tsx:154-177` | Every browser tab rebinds pycore's Laravel worker endpoint to its own shared base URL, including the transient mixed-content failover. | bind the pycore worker only on explicit user switches (`switchEndpoint`), not on transient activation. |
| FU-035 | confirmed | — | `apps/laravel-manager/components/views/DatabaseManager.tsx:355-374` | laravel-manager's DB viewer keeps showing the previous table's rows when the next table's load fails, and it leaves rejections unhandled. `[pass 2]` | reset `data`/`structure` on selection change and add `.catch` to an error state. |
| FU-036 | likely | — | `apps/pycore-manager/pages/PcAgentHistoryPage.tsx:471-488` | pycore-manager Agent History applies session and prompt loads without a stale guard, so a slower earlier load can overwrite the current one. `[pass 2]` | a request sequence ref per loader, checked before each `set*` call. |
| FU-037 | confirmed | — | `apps/pycore-manager/pages/audio-orchestration/AudioOrchWorkspace.tsx:154-170` | The audio-orchestration workspace drops task-list refreshes requested while a load is in flight, so the list can stay stale. `[pass 2]` | queue one follow-up load when a request arrives mid-flight (the pattern `useQueueCenterHub` uses). |
| FU-038 | confirmed | — | `apps/vortex/OkxBacktestPanel.tsx:706-713` | The Vortex chart can show the previous coin's candles under the newly selected coin. `[pass 2]` | ignore responses whose `inst_id` (and bar) is no longer selected. |
| FU-039 | confirmed | — | `apps/laravel-manager/components/views/ServerManager.tsx:258-321` | The Octane restart reconnect loop never stops on unmount and later reloads whatever page the user is on. `[pass 2]` | a disposed ref or an AbortController tied to component lifetime, plus a maximum wait. |
| FU-040 | confirmed | — | `apps/pycore-manager/components/PcWordAudioPanel.tsx:74-81` | pycore-manager word-audio players create an unmanaged `Audio` per click: clips overlap and keep playing after unmount. `[pass 2]` | one shared audio element per view, paused on the next play and on unmount (or reuse `playWordClip`). |
| FU-041 | suspect | — | `apps/mcp-chrome/app/chrome-extension/entrypoints/background/services/gemini-image-generate.ts:37-58` | A Gemini image generation that times out leaves the tab still generating, and the next generation may take the late image as its own. `[pass 2]` | cancel or reset the tab job on timeout (a new chat) before releasing the mutex. |
| FU-042 | confirmed | — | `poly_apps/pycore_laravel_wordnew_ui/OkxQuantPanel.tsx:34-75` | Pass-2 files hardcode UI-visible strings instead of using en/zh locale keys (AGENTS.md i18n rule). `[pass 2]` | route them through `lm`/`wf`/`pc` locale keys, or `chrome.i18n` for the extension. |
| IS-014 | confirmed | — | `scripts/linuxenvs/claude6.sh:368` | `claude6` and `claude9` launchers are syntax errors but are linked into /usr/local/bin | delete the stale claude6/claude9 (.sh and .ps1) or regenerate them. |
| IS-015 | confirmed | — | `scripts/node-upgrade/node-upgrade-manager.sh:66-70` | Several scripts were corrupted by a lossy encoding conversion and no longer parse or print correctly | restore these files from history or rewrite the affected strings in ASCII English. |
| IS-016 | confirmed | — | `scripts/shells/linux/common/frankenphp_runtime_common.sh:529-551` | fm_dnspod_candidate_install mixes log lines into its stdout return value | send the callee's log lines to stderr so stdout carries only the path. |
| IS-017 | confirmed | — | `scripts/shells/linux/debian/install_shells/175_laravel_main_start.sh:901-909` | 175_laravel_main_start.sh removes the opposite-plane unit with raw systemctl/rm (no $USE_SUDO) `[in-flight 02:51]` | prefix these with `${USE_SUDO:-}` like the surrounding code. |
| IS-018 | likely | — | `scripts/shells/win/win_common/AiCliProvisionCommon.ps1:98-101` | Add-AiCliUserPath rewrites the user PATH as REG_SZ with expanded values | read and write `HKCU\Environment\Path` through the registry with `DoNotExpandEnvironmentNames` and REG_EXPAND_SZ. |
| IS-019 | suspect | — | `scripts/shells/win/win_common/AiCliProvisionCommon.ps1:408-409` | All Windows role windows write the same temp ultracode settings file concurrently `[in-flight 02:51]` | write only when the content differs, or use a per-process file name (include $PID). |
| IS-020 | confirmed | — | `scripts/services/resource_watchdog.sh:117-118` | resource_watchdog.sh `--daemon` can start duplicates, and `--stop` then stops only the last one | refuse to start when the PID file holds a live watchdog, and put the log/PID under CORE_NODE_DATA_DIR. |
| IS-021 | confirmed | — | `scripts/shells/linux/common/gvar_system_common.sh:165-220` | mount_additional_disk runs mkfs without confirmation and writes a non-`nofail` fstab entry (latent, no caller) | remove the helper, or guard mkfs with an explicit confirmation and reuse mount_fstab_ensure_single_entry with nofail options. |
| IS-022 | confirmed | — | `scripts/shells/win/win_common/GlobalVars.ps1:146-162,236,412,449,463,471,643` | PowerShell paths built by string interpolation instead of Join-Path (141 sites) `[in-flight 02:52]` | convert the centralized constants in GlobalVars.ps1 first (one Join-Path per constant). |
| IS-023 | confirmed | — | `scripts/unified_manager/core/unified_core.py:683` | unified_manager "proxy" action crashes with NameError 'domain' | log `", ".join(domains)`. |
| IS-024 | confirmed | — | `scripts/shells/win/tools/launch_multiple_terminals.py:10` | launch_multiple_terminals.py fails at import (NameError 'sys') | remove the stale tool or add `import sys`. |
| IS-025 | confirmed | — | `scripts/shells/win/install_powershells/Step34_InstallQt.ps1:944-945` | Step34_InstallQt calls the undefined `Add-ToPath` and then reports success | use the shared WindowsPathFunction.ps1 `add` entry (as IsolatedPythonInstallCommon does). |
| IS-026 | likely | — | `scripts/shells/linux/dd_helper/dev_cache_cleanup.sh:46-48` | dd.sh deletes /usr/local/qcloud on every start without stopping or disabling the Tencent Cloud agent units `[in-flight 02:51]` | move it into the Slim & Disk Cleanup menu and disable the agent units (tat_agent, YDService, etc.) before deleting. |
| IS-027 | suspect | — | `scripts/shells/linux/debian/install_shells/119_install_launcher.sh:152-158` | 119_install_launcher installs all missing tools in one apt transaction, so one unavailable package blocks xclip/wl-clipboard too | filter NEED through `apt-cache policy`/`apt-cache show` first (as 153_install_desktop_applications does), or install per package. |
| IS-028 | confirmed | — | `scripts/shells/linux/debian/install_shells/17_install_node_toolchain_26.sh:221-233` | Installers delete other working installs instead of repairing only what is missing | leave foreign installs in place (only fix PATH/links), or ask before removing, and never delete /var/lib/caddy. |
| IS-029 | likely | — | `scripts/shells/win/win_common/ClaudeTeamCommon.ps1:259-277` | Windows claudeteamup treats any powershell/pwsh process that reuses a stale PID as a running role `[in-flight 02:51]` | store the process StartTime next to the PID (or check the command line for the role's EncodedCommand) and compare it. |
| IS-030 | confirmed | — | `scripts/shells/linux/common/claude_team_common.sh:261,293,513` | The catalog `permission_mode` is shown in the launcher logs but never applied `[in-flight 02:51]` | drop `permission_mode` from the catalog and the log lines (per V2 B8), or pass it through if it is meant to be configurable. |
| NC-023 | likely | — | `ncore/utils/stream_translator/libs/DeepSeekTranslator.js:90-104,126-151` | `DeepSeekTranslator` parses JSON responses per stdout `data` chunk, so a response line split across chunks is dropped and the request silently times out. One bad line also aborts parsing of the rest of the chunk. | Keep a carry-over buffer and split on `\n`. Default to `python3` on POSIX. |
| NC-024 | confirmed | — | `ncore/global_vars/libs/config_tool.js:449,461-479` | `importConfigFromJs` rewrites the tracked `config/index.js` source at runtime, using `content.replace(plaintext, ENC)`, which replaces the first textual occurrence anywhere in the file, not the key's value. | Never write source files at runtime. Keep secrets outside the repo (see NC-002). |
| NC-025 | confirmed | — | `ncore/utils/rpc/http_rpc/libs/StaticPathResolver.js:63-80` | `StaticPathResolver` on a Linux desktop (non-WSL) computes a different web root from every other end: `/opt/www/…` when running as root, or `/www/www/…` on a native (non-NTFS) `/www`. | Use `globalDir.mapWebPath()` on every Linux branch. |
| NC-026 | confirmed | — | `ncore/global_vars/global_dir/globaldir.js:44-67,71-72,110-112` | `osVersion` uses `os.type()`, which is always `'Linux'` on Linux, so the per-distro directories (`.dev_<distro>`, `applications_<distro>`) collapse to `.dev_linux`/`applications_linux` for Debian, Ubuntu and Kali alike. | Read `ID`/`VERSION_ID` from `/etc/os-release` (the helper `getOsVarTag()` already exists in `system_paths.js:118-141`). |
| NC-027 | confirmed | — | `ncore/utils/rpc/http_rpc/libs/RouterManager.js:56-62,69-71` | `RouterManager.logRequest` dereferences the `User-Agent` header without a guard, so every RPC-app route returns 500 to clients that send no User-Agent (Node `http.request`, raw sockets). | `const userAgent = req.get('User-Agent') \|\| '';`. |
| NC-028 | confirmed | — | `ncore/utils/translation/http_service.js:59,82-84` | The translation HTTP service binds all interfaces (no host argument) while logging `localhost`, which exposes an unauthenticated `/translate` that spends the configured provider API quota. | `app.listen(port, '127.0.0.1')`, or take the host from `service_contract`. |
| NC-029 | confirmed | — | `ncore/foundation/common/commander.js:405-412,439-451` | The foundation `spawnAsync` idle timer is never cleared on `close`, so its callback fires after the process exits with `success:true` even when the command failed. `pipeExecCmdAsync` passes its arguments in the wrong order, and its first output kills the process through `TypeError: callback is not a function` inside a timer. | Clear the timer on `close`/`error`, and map `pipeExecCmdAsync` arguments explicitly. |
| NC-030 | confirmed | — | `main.js:53-57` | `main.js` interactive app selection calls an undefined `logger` when `apps/` is empty, so the process dies with a `ReferenceError` instead of the intended message. | Use `console.error` there, or require `#@logger` at the top. |
| NC-031 | confirmed | — | `ncore/utils/video/libs/videoCompressor.js:132-148` | `VideoCompressor.compress` resolves as success even when ffmpeg fails, and its progress callback reads stdout, while ffmpeg writes progress to stderr. | Check `result.success`, and pass stderr to the progress callback. |
| NC-032 | likely | — | `ncore/foundation/common/commander.js:245,269,346,360,398,440,449` | `execCmd`, `execCommand` and `spawnAsync` (and the `plattool`/`porttool` copies) change the process-global cwd with `process.chdir`. That throws `ERR_WORKER_UNSUPPORTED_OPERATION` inside worker threads and races with concurrent async code. | Drop `process.chdir`, and rely on `options.cwd`. |
| NC-033 | confirmed | — | `ncore/mcp_server/file_processor/ocr_config.py:52` | Third-party API keys are committed in source: OCR.space (twice) and an Unsplash access key (twice). | Read the key from the global var center or an env var, with a single definition. |
| NC-034 | confirmed | — | `/ evidence:` | Duplicate implementations (rule). Several copies of the exec layer and the HTTP stack each carry the bugs above, so any fix must be applied more than once. | Keep one exec layer in foundation and one HTTP stack in `utils/rpc`, delete the dead copies, and share the voice `basetool` across both apps. |
| NC-035 | confirmed | — | `/ evidence:` | Layering violations (rule): foundation depends upward on global_vars, global_vars hosts a full installer feature, and utils reaches into ncontroller. | Move the installers to `ncore/utils/softinstall`, and pass paths into foundation instead of requiring global_vars. |
| NC-036 | confirmed | — | `/ evidence:` | The "never `throw new Error`; log and return" rule is broken at 477 sites in scope. Several of them sit on paths whose callers expect a return value. | Enforce the rule on the exec and RPC layers first, where callers depend on return values. |
| NC-037 | confirmed | — | `/ evidence:` | Hardcoded UI-visible text (rule). | Route these strings through the app's i18n tables. |

## 5. Cross-end contract findings (reviewer, R4 part 1)

Full evidence: `docs_fix/bug_audit_20260927/reviewer_contract.md`.

| ID | Sev | Category | Summary | Owners |
|---|---|---|---|---|
| RV-001 | medium | contract/race | Relay device-event revisions are not unique. The terminal revision restarts at 0 on pycore restart, and the agent_history revision is epoch seconds. Laravel's outbox insertOrIgnore then throws LogicException, which returns 500, and the push is lost for up to 24 h. | pycore-runtime, laravel-backend |
| RV-002 | medium | contract | The word-audio lane also pulls `article_audio`. Its page-data returns 404 because only word_audio and sentence_audio are accepted, so the RuntimeError aborts the whole diff round and the lane backs off repeatedly. | audio-tts, laravel-backend |
| RV-003 | low | contract/security | Queue Center Mercure authorization is gated by `dashboard.auth`, but the public `/overview` returns the same subscriber token. | laravel-backend, frontend-ui |
| RV-004 | low | contract/dead-code | `task_contract.stream_events` and the mcp-chrome SSE consumer are dead: there is no `/api/task/{id}/stream` route. | frontend-ui, laravel-backend |
| RV-005 | low | contract/rule | The stale v1 relay block in queue_center_contract.json duplicates pycore_relay_contract.json and contradicts it: 32 MiB vs 64 MiB, and token TTL 600 vs 300. The adapters have no callers. | laravel-backend, frontend-ui |
| RV-006 | low | contract/dead-code | `word_image.priority` has no emitter. `word_media` is not a task type, so it falls back to remote_translation and fast-lane tasks would never be claimed. | pycore-runtime |
| RV-007 | low | rule/contract | Laravel hardcodes realtime event names ('task.priority', 'cover.priority', 'poster.priority', 'queue-center') instead of reading the contract. The cover/poster payloads lack task_id, which creates synthetic heads. | laravel-backend |
| RV-008 | low | contract | The section lifecycle has no "stopping" state, so a draining lane shows "starting". | pycore-runtime, frontend-ui |
| RV-009 | low | contract | The relay route policy has no read entry for `ui/queue_center/audio_lane_state`. Read tokens get 403, and a lease expiry ends as execution_unknown. | pycore-runtime, frontend-ui |
| RV-010 | low | contract/rule | The word full-sync language fallback references a word_audio language_priority that does not exist, so it always uses english. The listing paths are hardcoded instead of coming from contract `endpoints`. | audio-tts |
| RV-011 | low | race | `queue.changed` is throttled on the leading edge only. The last change in a 1 s burst is never signaled. | laravel-backend |

### 5.1 Cross-scope items the reviewer confirmed (from the audio-tts report)

| ID | R4 verdict | Sev (R4) | Defect | Owner |
|---|---|---|---|---|
| X2 | CONFIRMED | medium | `serialized_files.owner()` creates one SerializedFile owner thread per resolved path and never removes it, so the thread count grows with task and manifest paths. | pycore-runtime → pycore |
| X4 | PLAUSIBLE | low | Laravel hashes the stored content with md5 (`DictionaryImportService:501`, `DictionaryService:113`), while pycore uses md5(lower(strip)). Capitalized entries get two queue identities and are generated twice. Related to AT-009. | laravel-backend + audio-tts → laravel, pycore |
| X6 | CONFIRMED | low | The `call_serialized` timeout starts before the enqueue, so queue wait counts toward it. This triggers AT-004. | pycore-runtime → pycore |
| X8 | unverified | — | `_retain_payload` raises ValueError when the same identity arrives with different bytes. A sentence re-synthesis can produce that, and the lane then marks the task failed. Retained copies are never deleted. | pycore-runtime → pycore |

X1, X3 and X5 are duplicates of AT-003, AT-012 and AT-039.

### 5.2 Harness item reported by infra-shell (not application code)

`.claude/hooks/git_guard.mjs` was rewritten at 02:52 by the roles-V2 session. infra-shell re-read it at 02:54 and probed its matching logic in a throw-away script.
- `GRANT_PATTERN` grants every git/gh command for 120 minutes on any prompt that contains the word "git". That includes "do not use git".
- The grant is written to the shared `.claude/agents_shared/git_grant.json`, so it opens git for every role.
- A quoted `|` in a command is split as a pipe. Example: `grep "a\|git add"` is still blocked.
- Context: AGENTS.md now allows read-only git always, and any other git only when the user's prompt asks for it. A grant on any mention of "git" does not match that rule.
- Owner: whoever owns `.claude/hooks/` under roles V2. Not changed.

## 6. Coverage gaps (not audited)

- **pycore-runtime:** pass 1 left about 145 of its 218 focus files unread. Pass 2 (PR-030 up) started on the HTTP routes and stopped at the halt; see its Addendum.
- **ncore:** the report was written partly after the halt. Not read: most of `puppeteer_spider_v2`, `ittools`, `ai_translator`, `electron`, `web_offline`, `flutter_icon_tool`, `smart_compression`, and the `wait_please` frontend. Excluded as vendored: `node_modules/` and `apps/ddk_old/*.bundle.js`. See `ncore.md` Coverage.
- **audio-tts:** pass 2 read all 26 requested files plus the qwen web console `app.js`. Only the console's `index.html` and `style.css` were skipped.
- **laravel-backend:**
  - Most of `app/Apps` (about 130k lines), most of `app/Services`, `database/` and the Models were covered only by the route-level auth survey.
  - About 55 focus files were not read: the AiGateway body, the CodeMart admin/seeder, the large AppQyV1 models and services, the ServerManager unified controller, and logging/config.
- **frontend-ui:**
  - Pass 2 had covered vortex before the halt. Result: no real OKX order or funds path.
  - It stopped partway through the remaining areas: mcp-chrome workers, wordnew API/app-state, the pycore-manager audio-orchestration and agent-history pages, laravel-manager. See its Addendum coverage line.
- **infra-shell:**
  - Read only in part: PostgreSQL, SSH, DNS, nginx, PathMapping, the git secret code.
  - Syntax and pattern checks only: pytools, tampermonkey, PackageManagerInvokes, most Windows steps.
  - Excluded as vendored: `docker_compose/**`, linux-router, easy-gpy-pv.
- **reviewer:** contracts not checked: service_contract.json consumers, mcp-chrome capability switches/labels, Laravel payload_limits, relay blob byte flows, dict_lane_queue materialization, wire_shapes other than worker_pull, laravel-manager Queue Center consumption.
- **No owner in roles V1:** `poly_apps/flutter_bloom` (roles V2 `flutter`).

## 7. Next steps (waiting on the user)

1. Resume R4 verification of the 185 unverified role findings, starting with critical and high: PR-001, NC-001, LB-001..LB-004, IS-001, FU-030, then the highs (PR-030 and PR-031 first).
2. Get lead/orchestrator rulings on theme 1 (machine auth, and the "no login" worker-route design in W5/W7) and on the §5.2 git guard grant rule.
3. Optionally run pass 2 on the coverage gaps in §6, including ncore and flutter.
4. Fix only on a user directive, assigned to the roles V2 owners: pycore, laravel, shell, codemart, pycore-manager, laravel-manager, wordnew, vortex, mcp-chrome, ncore.

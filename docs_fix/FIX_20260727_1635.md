# FIX_20260727_1635 — Pycore 规范合规修复清单

**来源规范**: `development-guides/PYTHON_PYCORE.md`（依赖层 / import / persistence / RPC v2 / operation / modularity / errors）
**关联**: `FIX_INDEX.md`、V3–V9 批号（本清单为横向规范扫描，与 V3–V9 纵向业务修复互补）
**写法**: 边扫描边追加，每条含文件:行号、违反的规范条目、修复动作。
**状态标记**: `[ ]` 待修 / `[x]` 已修

## 规范速查（修复时对照）

- L1 依赖方向: pyfoundations ← database ← pyutils ← pyctl ← entrypoint，禁止向上
- L2 import: 顶部 import；禁止局部/延迟 import 藏 cycle；`__init__` 不做重活
- L3 persistence: database 外禁止 sqlite3/SQLAlchemy driver/CREATE TABLE
- L4 RPC v2: WS 为 canonical，SSE 仅兼容；feature 不自建 WS/reconnect/pending table
- L5 modularity: >800 行必拆；先找已有组件复用；禁止 version-suffix 复制框架
- L6 errors/logs: 稳定英文 error code + correlation id；不记 secrets/大 binary

---

## A. >800 行文件（违反 L5）

| 行数 | 文件 | 修复动作 |
|---|---|---|
| 1259 | `pycore/callmodule/controllers/local_processing/task_center_controller.py` | [ ] 拆：HTTP 适配层 / operation 调度 / 查询序列化 三块；复用 V5 operation service |
| 989 | `pycore/callmodule/routers_bak/local/task_center_router.py` | [ ] `routers_bak` 为备份目录，确认无引用后删除（符合"cutover 后删除旧 transport"） |
| 845 | `pycore/callmodule/controllers/local_processing/books_controller.py` | [ ] 拆：分页查询 / 写操作 / 序列化 |
| 844 | `pycore/pyutils/codesync/manager.py` | [ ] 拆：watcher / transport / state 持久化（state 下沉 database，见 C 节） |
| 839 | `pycore/callmodule/services/agent_history_article_service.py` | [ ] 已在 V6 批号范围，随 V6 迁移到 operation service 时一并拆分 |
| 813 | `pycore/pyutils/launcher/menu.py` | [ ] 拆：菜单模型 / 渲染 / 动作派发 |
| 813 | `pycore/callmodule/services/tts_queue_poller_service.py` | [ ] 拆：poller 循环 / 任务领取 / 状态上报；持久化走 database outbox |

---

## B. 依赖层违规（违反 L1/L2）

- pyfoundations→高层 import：**未发现**（顶层 import 扫描干净）。
- database→pyutils/pyctl import：**未发现**。
- pyutils 跨 domain 顶层 import：**未发现**（但局部 import 见 D 节）。

### B1. [ ] pyfoundations 内含持久化组件（违反 L1：foundations 禁止 persistent store）

| 文件 | 问题 | 修复动作 |
|---|---|---|
| `pycore/pyfoundations/database_base.py` | foundations 内放 database 基类，含 CREATE TABLE | [ ] 迁入 `pycore/database/`——**归 V10 批次**，此处仅登记，勿重复迁 |
| `pycore/pyfoundations/state_store/repository.py` | foundations 内含 repository | [ ] 迁 `pycore/database/repositories/`——**按索引约定随 V4 提前移出** |
| `pycore/pyfoundations/state_store/schema.py` | foundations 内含 schema + CREATE TABLE | [ ] 同 V4：改 database 显式 migration |

## C. database 外直接使用驱动（违反 L3）

以下文件直接 `import sqlite3` / `sqlalchemy`，必须改为 `pycore/database` 的 DatabaseManager/BaseModel/repository：

| 文件 | 修复动作 |
|---|---|
| `pycore/pyctl/agent_history/cursor_extractor.py` | [ ] durable cursor 属 database，改用 database cursor API（关联 V4/V6） |
| `pycore/pyutils/launcher/device_sync/core/database.py` | [ ] 删自建 Database 类，改 DatabaseManager + BaseModel |
| `pycore/pyutils/launcher/device_sync/sync_history.py` | [ ] 同步历史表改 database repository + 显式 migration |
| `pycore/pyutils/mcp/file_processing/database_manager_for_file_info_caching_and_history.py` | [ ] 文件信息缓存/历史改 database repository；有界 JSON |
| `pycore/pyutils/translator/dictionary.py` | [ ] 词典存储改 database repository |

---

## D. 局部/延迟 import 藏 cycle（违反 L2）

全库函数级 `import pycore.*` 共 **127 处**。第三方可选依赖的延迟加载可保留；`pycore.*` 内部模块的函数级 import 一律视为藏 cycle，需上提文件顶部并重构循环依赖。

| 数量 | 目录 | 处置 |
|---|---|---|
| 29 | `pycore/pyutils/native_ui` | [ ] 逐条上提；若成环，把被依赖的公共部分下沉或抽接口 |
| 9 | `pycore/pyctl/speech` | [ ] 同上 |
| 8 | `pycore/pyutils/window` | [ ] 同上 |
| 8 | `pycore/pyctl/pybrowserauto` | [ ] 同上 |
| 7 | `pycore/pyutils/image_tools` | [ ] 同上 |
| 6 | `pycore/database/models` | [ ] **优先级高**：database 内部藏 cycle，model 间循环引用须用 registry/字符串引用重构 |
| 6 | `pycore/pyutils/ai_cluster` | [ ] 同上 |
| 其余（translator/input/desktop/common/device/mcpctl/whisper_stt 等 ≤4） | 各目录 | [ ] 顺带清理 |

修复方式：先画该目录 import 环 → 环上最轻边改依赖注入或下沉 foundations → import 全部回文件顶部。

---

## E. package `__init__` 重活（违反 L2）

`__init__` 只许导出稳定 symbol；以下是 import 即触发线程/服务/模型初始化的点：

| 文件 | 问题 | 修复动作 |
|---|---|---|
| `pycore/pyctl/desktop/ui/__init__.py:73` | import 即 `ui_thread.start()` | [ ] 移到显式 `start_ui()`，由 entrypoint 调用 |
| `pycore/pyfoundations/thread_bus/__init__.py:112` | `__init__.py` 内起 state owner 线程 | [ ] 线程类移到子模块，`__init__` 只 re-export；启动改显式 |
| `pycore/pyheartbeat/__init__.py:17` | import 即 `system.start()` | [ ] 改显式启动函数 |
| `pycore/pylauncher/__init__.py:26` | import 即 `launcher.start()` | [ ] 同上 |
| `pycore/pyutils/whisper_stt/__init__.py:19` | import 即 `provider.initialize()`（模型加载） | [ ] 改懒加载，首次调用时初始化 |

## F. RPC v2 / SSE（违反 L4）

- `pycore/pyutils/rpc_v2/server/sse_broadcaster.py` — [ ] 确认为兼容-only：标注 deprecated，新 feature 禁止引用；只保留旧客户端回退路径
- `pycore/pyutils/rpc_v2/server/fastapi_server.py` — [ ] 内含 event-stream 分支，同上处理：SSE 路径隔离到单一兼容模块
- `pycore/callmodule/services/translation_ws_client_service.py` — [ ] 名称是 WS 但含 SSE 用法，核查是否自建 transport；若是，改复用 `pyutils/rpc_v2` 公共 client（client_id/event_id/seq/ACK/replay 不得自实现）

修复验收：全库 feature 层不得再出现 `EventSource`/`text/event-stream` 新引用；durable event 一律走 database outbox + WS。

---

## G. 补充扫描结果（CREATE TABLE / 自建 WS / 整包 import）

- `CREATE TABLE` 在 database 外仅出现于 B1/C 已登记的 5 个文件，**无新增遗漏**——修复随 B1/C 一并完成即可。
- [ ] `pycore/pyutils/launcher/device_sync/_deprecated/_old_servers/websocket_server.py` — feature 层自建 WS server 且已标 deprecated：**确认无引用后直接删除**（符合"cutover 后删除旧 transport"；删除属授权模型操作）。
- `from pycore import ...`（整包入口）：callmodule 共 10 处（`callmodule_main.py:50`、`config.py:27`、各 controller 等）。
  - callmodule 是应用层，暂不强制；但**任何 standalone worker / 隔离进程**禁止此写法，须改为 `from pycore.pyfoundations.xxx import ...` 具体路径。
  - [ ] 排查 `pycore/pyutils`、`pycore/pyctl` 下是否有 worker 脚本使用 `from pycore import`，逐条改具体路径。

## H. 修复执行顺序建议

1. **B1/C（persistence 地基）**：`state_store` 与 5 个直接驱动文件迁入 database —— 与 V4 批号合并做，避免二次迁移
2. **E（`__init__` 重活）**：机械改动、风险低，先做可消除 import 副作用导致的诡异启动问题
3. **D（局部 import）**：按目录逐个解环；`database/models` 优先
4. **A（>800 行拆分）**：随对应业务批号（V5/V6/V9）顺带拆，不单独开批
5. **F（SSE 收敛）**：V7 事件流改造时一并收口

**验证方式**（全部静态、无需运行）：
- `grep -rn "import sqlite3\\|from sqlalchemy" pycore/ | grep -v pycore/database/` 应只剩迁移期白名单
- `wc -l` 无 >800 行源码
- 函数级 `import pycore.` 计数从 127 降到只剩第三方可选依赖处

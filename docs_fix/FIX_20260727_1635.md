# FIX_20260727_1635 — 横向合规

关联：`FIX_INDEX.md`；B1/C 并入 V4，F 并入 V7，E 可随时做，database_base 并入 V10。

## A. >800 行文件

| 文件 | 状态 |
|---|---|
| `task_center_controller.py` (1259→742) | ✅ 已拆 assist + sections |
| `routers_bak/local/task_center_router.py` (989) | ⏳ 可选：确认无引用后删除 |
| `books_controller.py` (845) | ⏳ 可选后续 |
| `agent_history_article_service.py` (839) | ⏳ 可选后续 |
| `pyutils/launcher/menu.py` (813) | ⏳ 可选后续 |
| `tts_queue_poller_service.py` (813) | ⏳ 可选后续 |

## B1. pyfoundations 持久化（→ V4/V10）

- ✅ `pyfoundations/database_base.py` → `database/adapters/database_base.py`（shim 保留）
- ✅ `pyfoundations/state_store/*` → `database/repositories/`（shim 保留）

## C. database 外 sqlite3（7 处）

- ✅ 全部经 `database/adapters/*`；`import sqlite3` 仅 `pycore/database/**`

## D. 局部 import pycore.*（127 处）

优先：`database/models` (6)、`pyutils/native_ui` (29)、`pyctl/speech` (9)。⏳ 可选后续。

## E. `__init__` 重活

| 文件 | 问题 |
|---|---|
| `pyctl/desktop/ui/__init__.py:73` | import 即 `ui_thread.start()` |
| `pyfoundations/thread_bus/__init__.py:112` | import 即起 state owner 线程 |
| `pyheartbeat/__init__.py:17` | import 即 `system.start()` |
| `pylauncher/__init__.py:26` | import 即 `launcher.start()` |
| `pyutils/whisper_stt/__init__.py:19` | import 即模型加载 |

⏳ 可选后续。

## F. RPC/SSE（→ V7）

- ✅ `sse_broadcaster.py`：compatibility-only；durable outbox 已落地
- ✅ `client_registry.pending_messages`：已由 outbox 替代
- ✅ `translation_ws_client_service.py`：已核查；未自建 WS transport，使用 `laravel_client.get_stream` + `requests` SSE 流

## G. 其他

- ✅ 删除 `device_sync/_deprecated/_old_servers/websocket_server.py`（已不存在；代码侧无引用）
- ✅ standalone worker 禁止 `from pycore import`，改具体路径（edge_tts worker 系列已改）

# FIX 20260727_2243 — Analysis & Fix List (WIP)

Source problems: `docs_fix/origin/a.txt`. This document is the analysis + fix list for an executor AI. **Do not edit code from this doc; it describes what to fix and where.**

## Issue list

1. **Laravel ParseError** — `AppQyV1AssistMediaOperations.php`: `ParseError: Unclosed '{' on line 23`, thrown at line 756 (past EOF → brace imbalance inside the file). Breaks `AppQyV1AssistService` autoload → `AppQyV1AIStatusController::coverStatus` 500.
2. **Pycore UI — Laravel endpoint switch stuck** — top-bar endpoint switch ineffective/hangs; RPC v2 layer bug. `ui.voice.queue` RPC timeout after 30000ms on :59000; UI falls back to cached snapshot.
3. **Queue-center RPC timeouts** — `ui.task_center.set_queue_center_control` timeout (8000ms), `ui.task_center.get_queue_center_snapshot` timeout (30000ms); "Backend unreachable". Suspected pycore ↔ laravel communication bug.
4. **Serialized operation timeouts** — Heartbeat callback `translation_worker`: `Serialized operation timed out: translation.worker.state.*`; `TranslationSSE supervise error: Serialized operation timed out: translation.sse_client.state.*`; `[qwen3tts +3.50s] IN`.

---

## Issue 1 — Laravel ParseError in AppQyV1AssistMediaOperations.php

### Symptom
`{ "success": false, "message": "Unclosed '{' on line 23", "exception": "ParseError", "file": ".../AppQyV1/AppQyV1Services/AppQyV1AssistMediaOperations.php", "line": 756 }`

Line 23 is the `trait AppQyV1AssistMediaOperations {` opening brace; the file ends at line 755, so some method inside leaves a `{` unmatched (extra `{` or missing `}`), making the trait brace appear unclosed at EOF.

### Root cause (CONFIRMED, reproduced with `php -l`)
The file's final line (754) is `}` closing `clearPosterLease()`, but the closing `}` for the trait itself (opened at line 23) was never written. The file just ends. Verified: every method in the file opens/closes at correct depth; after `clearPosterLease` closes, depth is still 1 (the trait brace).

### Fix (single edit)
File: `poly_apps/laravel_main/app/Apps/AppQyV1/AppQyV1Services/AppQyV1AssistMediaOperations.php`
- Line 754: indent the closing brace of `clearPosterLease` to 4 spaces (`    }`).
- Add a new final line 755: `}` (column 0) to close the trait.

### Verify
`php -l app/Apps/AppQyV1/AppQyV1Services/AppQyV1AssistMediaOperations.php` → "No syntax errors detected". Then re-hit the failing route (`AppQyV1AIStatusController::coverStatus`).

---

## Issue 2 — Pycore UI endpoint switch stuck / RPC v2 (ui.voice.queue)

(Investigation in progress — see below.)

---

## Issue 3 — Queue-center RPC timeouts (ui.task_center.*)

(Investigation in progress — see below.)

---

## Issue 4 — Serialized operation timed out (translation.worker / translation.sse_client)

(Investigation in progress — see below.)

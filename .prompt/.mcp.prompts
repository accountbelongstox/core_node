针对本项目完成一个支持对sqlite的数据库的增删改查的mcp服务器，监听端口 38001 ，默认可以配置一个sqlite数据库，注意数据库实现一个是一个分开的，不在所有的数据库共用一个实例。
2，可以传入一个新的数据库地址，然后随即创建或读取一个新的数据库，这样mcp内部就有多个打开的实例。每个实际都使用数据库的名字、路径作为命名空间。可以操作。
3，一个数据库在3分钟无操作后，将关闭，但关闭后的数据请标记，当下一个MCP服务请求到该数据时，查询是否被关闭，以防直接操作导致错误 。
4，可以操作系统任何地方的数据库，可以增，删，改查，无任何限制。
5，可以与AI进行细节沟通，比如当前有多少个数据库，需要 打开那一个等，AI可以随时传入新的数据库在MCP内创建新的实例。
6，选用最合适 的开发语言，以及最新的MCP标准，推荐使用uv（但如果你觉得有更好，你仍可选择。）。
7,当AI要求操作的数据库不存在时，智能创建，存在是根据要求操作。
# Git Remote Configuration
# This file is shared across PowerShell (.ps1), Bash (.sh), and Python (.py) scripts
# Format: remote_name=git_url
# Lines starting with # are comments
# Order: github first, then gitee, then local

github=git@github.com:accountbelongstox/core_node.git
gitee=git@gitee.com:accountbelongstox/core_node.git
# Disabled: 192.168.50.2 is unreachable here (ssh :22 connection timed out) and
# stalls every push. Uncomment to re-enable when the host is reachable again.
# local=git@192.168.50.2:adminroot/core_node.git

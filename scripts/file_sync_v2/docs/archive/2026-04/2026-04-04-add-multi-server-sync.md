# 任务：实现一个客户端同步到多个服务端

状态：done
语言：简体中文（zh-CN）
负责人：Codex
目标：在保持现有单服务端兼容的前提下，为客户端增加“一个 client 对多个 server 同步”能力。
范围：`client.py`、`client_config.json`、测试文件。
不在范围：服务端协议重构、加密链路改造、部署脚本改造。
约束：先补测试再改实现；保持现有配置可用。

计划：
- [x] 建立任务文档并登记索引/状态。
- [x] 编写失败测试，覆盖多服务端配置与状态兼容。
- [x] 实现多服务端同步逻辑并通过测试。
- [x] 更新进展并完成归档。

进展记录：
- 2026-04-04：任务创建，准备先写测试验证配置解析与状态行为。
- 2026-04-04：先写测试并观察失败（初始暴露 watchdog 缺失时的导入问题），随后补齐多服务端实现。
- 2026-04-04：新增 `servers` 配置解析、每目标初始同步状态跟踪与多目标 watch/poll 同步。
- 2026-04-04：执行 `python3 -m py_compile client.py server.py protocol.py` 与 `python3 -m unittest discover -s tests -p "test_*.py"` 均通过。

验收标准：
- 支持在客户端配置多个服务端目标。
- 兼容旧版单服务端配置字段。
- 启动全量、监听增量、轮询同步均可对多个服务端生效。
- 测试通过且无语法错误。

分析并按以下步骤扩展，
1：增加一个python原生的启动窗口，调用 from pycore.pyfoundations.color_print import ColorPrint ，并注册一个事件日志 ，启动窗口就是一个标题，和日志打印，基目折是在 from pycore import check_and_install_dependencies
check_and_install_dependencies() 还没有初始化前，先显示一个窗口，并实时显示安装信息。所以启动窗口并不引入 check_and_install_dependencies。但会调用其他初始化类，并在其中有 check_and_install_dependencies。
2：当所有类安装完后，使用pyside6  构架现在的 D:\programing\core_node\pycore\pyutils\native_ui ，除了启动窗口，其他类全部改为 pyside6
3：为了保持ui界面的兼容性，扩展出一个webview/pyside6 web engien的类库。并根据native_ui的参数选择，默认为 pyside6。同时，根据现在的规范规范扩展，目前的规范是python ui只作为一个壳，并启动一个模拟的标题栏和默认的拖盘，但括盘事务可以追加。之后主UI将是一个web页面。面web页面没有载入前默认显示loading页。
4：确认只启动主线程，tick 定时器线程，UI将在主线程中启动。现在不启动过多线程。相关于2个线程，主线程，tick定时器线程，UI线程（如果算就是3个）。
5：确保除了初始化窗口是python原生，其他是pyside6
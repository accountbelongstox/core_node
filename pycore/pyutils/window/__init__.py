"""
pyutils.window - window & on-screen UI utilities.

Grouped from former top-level pyutils modules. Import the specific submodule:

    from pycore.pyutils.window.activator import WindowActivator
    from pycore.pyutils.window.screenshot import WindowScreenshot
    from pycore.pyutils.window.ops import WindowOps
    from pycore.pyutils.window.analyzer import WindowAnalyzer
    from pycore.pyutils.window.integrated_analyzer import IntegratedWindowAnalyzer
    from pycore.pyutils.window.ui_analyzer import UIAnalyzer
    from pycore.pyutils.window.unified_detector import UnifiedDetector

Submodules depend only on pyutils.common (shared base), pyfoundations and
pygvar - never on a sibling domain package.
"""

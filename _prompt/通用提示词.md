不要使用退出码检测、不要使用返回信息，所有都是依赖直接输出信息、以及结果检测，比如安装检测二进制，是否运行成功直接依靠运行方的输出。

依赖文件自身定位相对路径，不要硬编码路径。

使用统文本，不能使用json作为交换变量系统， 不允许在ps1里嵌入py代码片段。


注意复用代码，全面分析，你只能增加现有逻辑中的必要的差异代码，你增加w任何代码时都要查看现有代码是否有重用内容，你将选择增强存在代码，但替换为新代码代码，拒绝同一功能出现多个版本或多个定义。

注意其中肯定 有很多全局，局部变量的混乱，以及代码复用、重复多处定义不一致的问题，你要全面根据设置构架，扫描所有问题，你增加w任何代码时都要查看现有代码然后使用最优选的复用方案，拒绝同一功能出现多个版本或多个定义。

不要使用退出码检测、不要使用返回信息，所有都是依赖直接输出信息、以及结果检测，实时输出所有信息。

依赖文件自身定位相对路径，不要硬编码路径。

使用统文本，不能使用json作为交换变量系统， 不允许在ps1里嵌入py代码片段。代码需要全英文。

注意脚本要保持幂等性，反复运行时要修复问题，即安装成功了，也要修复每个步骤，反复运行该脚本会修复逐个步骤，不能因为修复一个完安而跳过另一个。不能因为一个修复成功而跳过别的。代码需要全英文。

修改为符合幂等性原则，每次运行都会检查并修复所有步骤，不会因为某个步骤完成而跳过其他步骤。

扩展前端页面，注意API中心化，多语方化，model中心化，样式中心化。

复用代码，多文件设计，复用components,禁止堆代码，完成一个功能 前要扫描 是否有功能可以扩展兼容复用。

Ai studio : 查看没有完成的页面继续 完成，注意在现在的页面上扩展，而不要删除已经存在的页面，使用并显示 一些模拟 数据 。
。
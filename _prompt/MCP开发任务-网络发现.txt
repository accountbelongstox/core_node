现在做一个服务自动发现，先得到身身的局域名IP网段。确保能访问到.1的IP，经如192.168.xx.1，这就表示网段是正确的
首先要发现laravel服务使得为默后端，这是MCP服务不必提供给AI工具的，AI工具也不必知道。端口是9000,默认是192.168.50.2:9000。如果网段不对则进行网络发现，网段正确则说明后台在维护。
之后pycore ncore服务都访问本机，但如果本机没有启动。同样进行同端口的网络发现。最后给出发现结果（0.5的超时）。
laravel是启动的时候静态发现，
pycore/ncore是在不可访问的时候重试发现，一直在本机和局域网之间发现服务。同时更新文档，这个MCP服务是独立开发，不依赖pycore基本服务。但会通过http塙用pycore nocre larave.

参考原来的pycore\pyctl\mcpctl 所有mcp功能，完成对接，注意mcp客户端也就是现在你完成的端、代理端完成接口（但不要让AI工具知道这一点），之后其他功能全部完成在pycore端。
虽然 是通过http交互，但是pycore可以同样访问要本地文件夹和文件，所以统一逻辑就可以了，现在 pycore_module_caller.py 调用口，其中提供mcp/前缀的调用，并抽像出 pycore\pyctl对接到pycore_module_caller,
同时 pycore\pyctl中的前端抽像到 "D:/programing/core_node/ncore/mcp_server/unifiedmcp/main.py" ，之后 pyctl 中的web和路由功能不再要，由pycore_module_caller.py接管。
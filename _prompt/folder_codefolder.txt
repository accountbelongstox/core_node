目前只有文件有复制路径 功能 ，文件夹没有，文件夹也要添加。同时，缓存文件展开状态，记录index到localstore. 只有展开过的才记录，不要全部记录，因为有时候 node_modules的目录 非常巨大，同时跳过所有 node modules的展开记录，以及其他语言比如vite/nuxt/dart/flutter的编译目录 文件。
右菜单增加一个文件删除功能（但只有文件有），同时删除时弹出确认框(不能是alert)，之后删除并不是真的删除，而是根据文件相对路径移到本项目的 根目录的 _delete目录中。同时在该目录有一个特殊菜单，也就是文件上点击恢复。恢复是如果已经有了文件则要提示不覆盖。删除时可以替换_delete区的存在文件。
除了 _delete _prompts 文件夹（其中的文件除外），所有文件夹和文件都可以有改名右菜单。
同时，根目录有一个特殊目录，_prompts
在前端顶部显示一个菜单项，即本项目的任务 提示词，每个菜单就是 _prompts中对应的文件名，其中的文件无论是否有扩展名都按utf8的txt格式解析。同时可以创建一个新任务 。查看本项目的翻译接口。前端会实时请求将 _prompts 中内容翻译为英文。
翻译规范为，当一个文件没有在fucos编辑的焦点时，就处理可以请求翻译的状态，也就是一开始这个文件夹中的所有 文件都要请求翻译，翻译规范为，如果文件中有中文，则请求翻译该行，得到结果后再使用中文key和英文结果后端直替换行该（同时返回前端结果、但前端只打印），有时候翻译成功后可能文件已经被编辑，则丢弃一次trans结果。
同时文件名也要翻译，当文件名翻译后，前端会定时请求更新菜单。
在顶部的_prompts 点击后，将悬浮一个编辑框，而不是和现在的文件编辑框一起，同是悬浮的编辑框可以拖动，点击那一个那一个就置顶，这是方便同时看和编辑多个提示词，当不在焦点后，或没有编辑10秒后，前端立即请求翻译。如果已经翻译过的则不用请求。
注意提示词是一个复杂的前端项目。你只能使用html和js构建。

文件夹的子文件夹被展开以后，没有出现层级效果，都是在同一级。_prompts 和 _delete 当没有时，后端都要自己创建。
注意：前端不要硬编码路径 ，后端一律要使用poly_apps\laravel_main\app\Providers\PathMapper.php中的映射路径 ，因为在每个系统上项目路径 是不一致的。
前端顶部没有出现tasks/prompts菜单，如果_prompts 是空的，也要有新建菜单。

是否是有 将 PROJECT_ROOT_DIR 设置到环境变量，检测其对于PROJECT_ROOT_DIR的用途，如果是设置环境变量，一定要使用real-project-dir.

Tasks/Prompts:
+ New Task
🔄 Refresh
No tasks yet. Click "+ New Task" to create one. 是在 Code Browser 区域 ，而不是全局 
文件夹的子文件夹被展开以后，没有出现层级效果，都是在同一级。

Enter prompt file name: 创建后，没有立即被添加到 _prompts目录，输入名字后，自动在前端加上md的扩展名（如果没有扩展名），之后，如果有空格，前端自动合并再提示，如果是多单词，自动首字母大写，后端也要验证这些。同时立即刷新到菜单。
同时前端立即判断请求翻译文件名，更新更新菜单。前端请求翻译  _prompts 将是定时的，后端默认使用google翻译（调用Pycore call） poly_apps\laravel_main\app\Apps\AppQyV1\AppQyV1Controllers\AppQyV1AITools\AppQyV1TranslationController.php  poly_apps\laravel_main\app\Services\TranslationService.php 

给php添加一个全局的区取当前实际登陆用户的方法 ，如果是桌面系统（当前系统一般运行在root下）扫描/Home/*/*,查找能访问的目录 ，然后查看那个的 /Home/*/第三级目录 为最新修改。即是实际登陆用户。
之后他那一个全局的文件，文件夹创建类，并声明为修正权限的公共方法 ，扫描 所有 类全部使用该类，创建任何目录和文件时，将立即修正权限为实际登陆用户，使普通用户可以访问。 同时读取访问也是一样，他那一个专门的权限修正文件类库，访问任何文件时都立即修正权限 ，以解决一些之前他那的文件夹、文件权限访问的问题，修正为可访问。

将刚刚的全局文件访问规范更新到  /www/programing/core_node/development-guides/LARAVEL_GUIDE.md 但最多用只行字来描述。
content="{{ csrf_token() }}"> 然而什么也没有显示，因为根据规范现在的html页是直接读取的，没有使用laravel的模块，为了节省内存，现在你设计一下，看如何即不使用laravel的模块，也能载上csrf_token


对所有页都有一个初始公的方案，作为基础类库。放在全局，而不单独放在某个功能 类库中。

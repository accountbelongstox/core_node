正该菜单， poly_apps\cmg-corporate-portal\scripts\start.ps1 项目是自动探测的，直接提示，Y/n
确认还是手动选，之后，显示出真正的菜单，1，安装Capacitor（自动备份一次package.json（如果有备份则跳过）），2：调试、编译为w
eb，编译为android。如果编译时，自动读到项目下的 poly_apps\cmg-corporate-portal\build_config.ini 自动处理其中的内容替换到
android/目录，注意替换时要自动递归的扫描，而不能直接指定文件名，而且替换图时要根据目标图的大小来等比例缩放src图。替换app名
时也要找到所有可能的位置替换。如果 build_config.ini 中没有指定，自动在 poly_apps\cmg-corporate-portal\assets
中找到logo.png splash.png作为app图标、app启动图，如果没有图就提示。但仍要继续编译。



  主要改进

  1. 自动项目类型检测

  - 自动识别 React、Vue、Vite、Next.js、Nuxt.js
  - 显示检测结果并提供 Y/n 确认选项

  2. 新的交互式主菜单

  1. Install Capacitor (自动备份 package.json)
  2. Development Server (调试)
  3. Build for Web (编译为 Web)
  4. Build for Android (编译为 Android)
  Q. Quit (退出)

  3. build_config.ini 解析器

  - 自动读取并解析 INI 配置文件
  - 支持所有配置段：[app_info], [package_settings], [resources] 等

  4. 智能文件替换系统

  - 递归扫描：自动扫描 android/ 目录下所有相关文件（.xml, .gradle, .kt, .java, .json, .properties）
  - App 名称替换：在所有可能的位置替换应用名称和包名
  - 不指定具体文件名：通过递归查找自动发现需要修改的文件

  5. 图片处理功能

  - 等比例缩放：根据目标图片尺寸自动等比例缩放源图片
  - 居中处理：缩放后的图片自动居中放置
  - 高质量渲染：使用 HighQualityBicubic 插值算法
  - 递归查找目标图片：自动查找所有 ic_launcher*.png 和 splash*.png 文件

  6. 图片资源 Fallback 逻辑

  优先级顺序：
  1. build_config.ini 中指定的路径
  2. assets/logo.png 和 assets/splash.png
  3. 如果都没有，显示警告但继续编译

  7. package.json 自动备份

  - 首次安装 Capacitor 时自动备份
  - 如果已有备份则跳过，避免重复备份

  核心功能说明

  安装 Capacitor（选项 1）

  1. 自动备份 package.json（如果没有备份）
  2. 安装 Capacitor 核心包和 Android/iOS 平台
  3. 初始化 Capacitor 配置
  4. 添加 Android 平台

  编译为 Android（选项 4）

  执行流程：
  1. 检查 Android 平台是否已安装
  2. 解析 build_config.ini 配置
  3. 准备替换映射（app 名称、包名）
  4. 查找图标和启动图（按优先级）
  5. 递归替换：扫描所有 Android 文件并替换文本
  6. 等比例缩放并替换图标：自动查找所有目标图标文件
  7. 等比例缩放并替换启动图：自动查找所有启动图文件
  8. 编译 Web 资源
  9. 同步 Capacitor
  10. 构建 Android APK（Debug 版本）

  使用说明

  运行脚本：
  cd D:\programing\core_node\poly_apps\cmg-corporate-portal\scripts
  .\start.ps1

  脚本会：
  1. 显示检测到的项目类型（React + Vite）
  2. 询问是否使用检测结果（默认 Y）
  3. 显示主菜单供选择操作

  配置文件说明

  确保 build_config.ini 包含必要配置：
  - app_name: 应用内部名称
  - display_name_chinese/english: 显示名称
  - default_package_id: 应用包名（如 com.ddsj.cmg.club）
  - icon_file/splash_screen_file: 图标和启动图路径（可选）

  所有变量都在脚本开头声明，符合 PowerShell 最佳实践，并且使用绝对路径而非相对路径。


--------------------------------------------
  [?] What is the name of your app?
      This should be a human-friendly app name, like what you'd see in the App Store.
  ? Name » cmg-corporate-portal 初始化之前，先初始化 build_config.ini
  如果有则读取，显然其中的有效项，如果不存在，自动创建一个，并至少要有poly_apps\cmg-corporate-portal\build_config.ini
  目前其中的必要信息，app_name 自动生成，生成为文件夹名 app_{文件夹名}

  注意 start.ps1 和start.sh 都只入口脚本，差异化交给py处理，同时要挂民规范：1：py和shell使用文件变量交换变量，不能直接传递
参数，文件变量都有前缀（防止其他APP使用此脚本编译时变量错乱）。2：py里不执行任何shell命令。而是通过处理差异化保存到文件变
量，3：shell在py前置处理后，读取文件变量得到最后要运行的命令，任何pnpm/编译文件都在shell里完成。windows/liunx分别要完成读
取文件变量和组织命令的差异化脚本。就这些，先改了。

py 中要实现一个临时的http web服务，在替换完资源后，Y/n启动一个web服务器。显示替换后的andriod中的所有图片的信息，在web端显
  示图片，全路径，并分组显示。以及所有包名（扫描所有文件匹配到的 的） 所有app名，使用目前的代码修改完成。之后按Y
  或在web端退出该线程后继续 编译。


以及 现在  App Names并没有显示，这是替换后的递归查找，查找可能没有替换到的文件，使用正则。

为什么只显示了一个，没有对中英文进行处理，查看 build_config.ini 中，如果设置了一个语言的时候，要进行处理 如何处理你现在调用MCP 查看官方文档，在python中预置扩展程序 。

 app\src\main\res\values\strings.xml 为什么只显示了一个，没有对中英文进行处理，查看 build_config.ini
中，如果设置了一个语言的时候，要进行处理 如何处理你现在调用MCP 查看官方文档，在python中预置扩展程序 。

 还需要查看  capacitor 的官方文档。这个创建应该是由python根据规范自动创建，或者如果 capacitor
支持预处理，也应该预处理，同时根据 build_config.ini 中的扩展语言来自动扩展。

所有代码不要使用退出码、返回值检测，只能使用文件变量交换。 所有变量放到   C:\Users\用户名\.core_node\.build_global_vars / /var/_core_node/_build_global_vars/ 中

这个问题依然存在，python的退出并不影响后面的shell继续执行，这是为什么影响到shell了,  目前依然是这个。

调用MCP查看官方文档如何 解决。最好是在py前置脚本中处理，这样能通用所有项目。

代码全英文，不要写一堆修得脚本，而是从 .\poly_apps\cmg-corporate-portal\scripts\start.ps1 自动解决这些问题，不要再提示那个defend的提示。

如果你修复了所有 命令，根本就不会跑到 Users 目录中去了啊。再次检测。


-------------------------------------------
[CMD] npx cap sync android
√ Copying web assets from dist to android\app\src\main\assets\public in 6.39ms
√ Creating capacitor.config.json in android\app\src\main\assets in 546.90μs
√ copy android in 23.91ms
√ Updating Android plugins in 7.26ms
[info] Found 19 Capacitor plugins for android:
       @capacitor/action-sheet@8.0.0
       @capacitor/app@8.0.0
       @capacitor/app-launcher@8.0.0
       @capacitor/browser@8.0.0
       @capacitor/camera@8.0.0
       @capacitor/clipboard@8.0.0
       @capacitor/device@8.0.0
       @capacitor/dialog@8.0.0
       @capacitor/filesystem@8.0.0
       @capacitor/geolocation@8.0.0
       @capacitor/haptics@8.0.0
       @capacitor/keyboard@8.0.0
       @capacitor/local-notifications@8.0.0
       @capacitor/network@8.0.0
       @capacitor/preferences@8.0.0
       @capacitor/share@8.0.0
       @capacitor/splash-screen@8.0.0
       @capacitor/status-bar@8.0.0
       @capacitor/toast@8.0.0
√ update android in 65.27ms
[info] Sync finished in 0.114s
[Success] Capacitor sync completed
True
 .\poly_apps\cmg-corporate-portal\scripts\start.ps1 调用MCP，在这个调用连相找到所有包和
  capacitor 以及 gradle和JAVA的对应关系，找到冲突的，以及按最新版本的统一配置是什么。

  D:\programing\core_node> .\poly_apps\cmg-corporate-portal\scripts\start.ps1 找到该脚本中的调用链，全部升级为
最新。同时生成java的必要环境，我将由另一个AI编写升级脚本。同时现在的前置修复替换使用py,同时py不要再执行任何 命令行。传文件传回给shell执行。

注意最新的 @capacitor/core
  TypeScript icon, indicating that this package has built-in type declarations
  8.0.0 已经是8.0，不要指定版本，而是由pnpm安装最新版本。最后你生成java版本的要求，同时在本编译脚本的前置处理中自动使用py处
  理gradle等的要求。全部调用MCP核对最新版本号。

  设置一个主版本core，当主版本不存在或低于这个值 时，所有包都重新添安装和添加。这里没有回退到sehll安装啊，说了复杂的替换都由py做前置统一处理，shell只负责读文件变量组织命令执行命令。同时不要使用任何退出码、返信信息检测，1：py和shell使用文件变量交换变量，不能直接传递
参数，文件变量都有前缀（防止其他APP使用此脚本编译时变量错乱）。2：py里不执行任何shell命令。而是通过处理差异化保存到文件变
量，3：shell在py前置处理后，读取文件变量得到最后要运行的命令，任何pnpm/编译文件都在shell里完成。

查看python前置处理脚本， 查看python前置处理脚本，还有那些包版本没有对齐。调用MCP查看版本现在的信息。查看官方文档。

再次查看  capacitorjs  官方是否有初始化一个最新android的选项， .\poly_apps\cmg-corporate-portal\scripts\start.ps1 1.
  Install Capacitor (with automatic backup) 如果有在其中提示可以重置 y/N


[Step 2/2] Running Windows command executor...

查看 CapacitorJS 官方文档中关于安卓状态栏和安全区域的处理方式。
根据 CapacitorJS 官方建议，检查并调整代码。先查看相关文件：
检查应用入口文件，确认 StatusBar 的初始化：
按 CapacitorJS 官方建议，进行以下调整：
在 AndroidManifest.xml 中添加 android:fitsSystemWindows="true"
在pythoon前置编译脚 本中确保配置。 你需要在前置编译的py中添加确保配置的代码，而不是直接修改文件。

已完成的更改
根据 CapacitorJS 官方推荐，已做以下调整：
1. AndroidManifest.xml - 添加系统窗口适配
在 MainActivity 中添加了 android:fitsSystemWindows="true"，使 WebView 避开系统窗口（状态栏）
2. index.html - 添加 Safe Area CSS 类
在 Tailwind 配置中添加了 safe-area spacing
添加了 pt-safe-top 类，使用 max(env(safe-area-inset-top), 24px) 确保至少有 24px 的顶部间距（即使 safe-area 为 0）
添加了 top-safe-top 类供其他组件使用
3. App.tsx - StatusBar 初始化和样式调整
导入并初始化 @capacitor/status-bar 插件
在应用启动时配置 StatusBar 样式
根据主题切换更新 StatusBar 样式
将 header 的高度从固定的 h-[60px] 改为 min-h-[60px]，以适应 safe-area
调整 main 元素的 padding-top，使用 calc(env(safe-area-inset-top, 0px) + 60px) 来适应 header 的实际高度
这些更改确保：
在 Android 设备上，顶部元素不会被状态栏和电池图标遮挡
使用官方推荐的方法处理安全区域
兼容不同 Android 设备（包括有刘海屏的设备）
在浏览器中也能正常工作（safe-area 为 0 时使用最小 24px 间距）
现在应用在 Android 设备上应该能正确显示，顶部元素不会被状态栏遮挡。
写一个完整的编译提示，在编译前提示所有需要注意的地方，让前端AI修修改。
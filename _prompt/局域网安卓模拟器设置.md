 在dd.cmd -> dd.ps1 Windows Management 中加入启动一个安卓模拟器，调用外部脚本，使用adb / emu启动一个标准的模拟器，如果以上命令不存在，自动调用  Step60_InstallAndroidStudio.ps1
  Step61_InstallAndroidPlatformTools.ps1脚本，之后打印ip等信息。代码全英文，实时输入信息
不要使用退出码检测、不要使用返回信息，所有都是依赖直接输出信息、以及结果检测，比如安装检测二进制，是否运行成功直接依靠运行方的输出。
所有脚本使用explorer启动子线程，不允许阻塞主线程。 

进行全面的处理，代码量不得低于1000行，不 允许 使用退出码，返回值 ，所有命令实时输入，进行大量的查找和前置处理。不允许 在catch中写代码。 所有命令都要递归查找目录，不允许硬编码路径。 


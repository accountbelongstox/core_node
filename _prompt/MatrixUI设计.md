


构建一个新项目，显示一些群控系统 。

不要改动现在的样式，呆能 FRONTEND_BACKEND_ALIGNMENT.md 根据 这个完成 ,同时将文档 放在根目录 ，一次只完成一个端点。将进度写在文档 ， 其中需要 扩展的UI功能 ，注意扩展出API功能、但实际上使用mock数据，扩展出左菜单，顶部菜单，右菜单，样多和风格沿用现在的，对于无关元素你可以修改，使用多语言，不要在代码中硬编码非多语言，model中心化、API使用 现在的 Key 调用，全部使用websockt连接 。沿用现在的样式。

加上动态的浅变全局背景，不要改其他地方。加一个底部工具条，其中有各社交APP的一键功能 。以及一些其他功能 ，具体参考 。 FRONTEND_BACKEND_ALIGNMENT.md ，注意除了加全局的背景动态，不要改其他地方的效果。

脚本引擎
在其中加入很多脚本选项，脚本任务 ，可以选择一个或多个设备热行脚本，脚 本针对的社交APP的图标。点开脚本后弹出一个脚本流程图。和介绍。，不要改现在的显示效果，在现在的效果上可以扩展组件。

前端：
增加抖音、陌陌的元素（脚本引擎、底部菜单）。
同时在中间设备区可以拖动一次性选择多个设备 。
同时，增加碰撞检测，如果鼠标不是在按下状态移到某设备上，则在该设备上记录碰撞效果，点击，双击，拖动（移出该设备记录鼠标自动弹起），之后在底部菜单区增加一个日志显示区，动态显示日志 ，当有碰撞检测：显示操作了某个设备，以及轨迹状态。
注意不要改现在的显示、样式效果。但可以在现在的基础上组件。

FRONTEND_BACKEND_ALIGNMENT.md 查看其中还有没有需要 扩展的功能UI区域 ，注意不要改现在的显示、样式效果。但可以在现在的基础上组件。

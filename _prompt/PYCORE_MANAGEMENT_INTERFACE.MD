# PyCore Management Interface

## Overview

Copy `pycore-management.zip` from `~/Downloads` (or user download directory) to `poly_apps` and extract.

For frontend, now start aligning frontend. Note: Do not modify code, but directly generate a report of what frontend needs to modify. Then return to your previous context.

## Reference

`python .\pycore_module_caller.py` - Reference specification `development-guides\PYTHON_PYCORE.md`


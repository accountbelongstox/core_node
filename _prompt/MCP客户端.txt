MCP客户端 python .\pycore_module_caller.py 我已经启动pycore，查看mcp对接 pycore_module_caller 的是否完全正确。

查看基中引用的 python .\pycore_module_caller.py mcp,你是否看到了调用tray系统 拖盘，在windows下会显示 
全面扫描 这个深度文件，之后，使用其中规范 的thread bus 系统 ，给 tray菜单改为 打开web界面 重启 退出。同时进一步查看windows/linux的羞异化是否完备。

扩展单例模式，pycore\pylauncher\launcher.py ，pycore\pylauncher\singleton_detector.py 
以及python .\pycore_module_caller.py的rpc v2 端点，增加一个协议退出的端口，给pycore_module_caller 一个全局事务状态 。
可以放在thread bus中，当处理忙的时候 绝拒退出，如果是闲则退出，新启动一个端时，如果发现端口被 占用则发送协议询问，如果上一个拒绝退出，则继续使用上一个。
全局的忙状态则具体任务 类设置，当有任务 处理时可以设计。在代码中注明，
同时以上你要复用以前的代码，先扫描以前的旧代码查看内容。

全局都只使用一个单例， tray.中不要重复逻辑。
从 python .\pycore_module_caller.py中，
查看tray菜单的注册和事件注册，先给tray添加一个，播放语音字mu。 该功能会toggle一个在系统上播放声音的功能 。
之后扩展pycore\pyctl 查看文档 development-guides\PYTHON_PYCORE.md 注意 pyctl是用来组织 pycore\pyutils逻辑的，避免循环引用。
现在 引用 pycore\pyfoundations\system_paths.py 作为一个站外缓存目录 ，建立一个队列， [{
文字,语音路径,播放次数
}」] 之后建立一个index表示当前播放到第几。
之后，将在 pycore\pylauncher\launcher.py 中传递参数 给 pycore\pyutils\native_ui ，引用 pycore\pyctl\语音播放/ui/中的 index作为ui界面。其中显示text，播放的时候显示。
先完成这一步。注意不要破坏之前的逻辑。
注意，这是由  python .\pycore_module_caller.py中组织参数来使 pycore\pylauncher\launcher.py同时启动 pycore\pyutils\native_ui，默认 pycore\pylauncher\launcher.py是不启动 native_ui的
说了要通过 pycore\pylauncher\launcher.py 来启动 pycore\pyutils\native_ui，先查看 pycore\pyutils\native_ui 中的逻辑，确保传递一个html过去就可以启动。使用rpc v2 的websockt和主程序 通信。


先硬编码可以多语言播放，[en]，目前只先扩展这一种语言，这里先标记为langs.
通过现在的 http://localhost:59000/ 添加路由，然后提供一个接口，添加文字到播放清单，可以添加文字 ，图片，url。语音，文字。之后在 pycore\pyctl 组织逻辑，当收到添加的信息后，根据添加的内容，进行处理，
1：如果是图片，则调用 pycore\pyutils\ocr 识别，
	之后将识别内容 组织提示词传递给  pycore\pyutils\gemini 总结，得到一段完整的文字 。
	再调用 pycore\pyutils\common 中的edge tts，包括当前的   http://localhost:59000/    {
      "name": "translator",
      "description": "Google Translator - Text translation service",
      "preload": true,
      "methods": [
  根据 langs 中的所有 语言，翻译为目标语言，最后调用 pycore\pyutils\common 中的edge tts 得到语音，这样就同时得到了字幕和语言。
 注意，生成tts果要生成缓存，需要使用tts的缓存表，使用\n对每个段落生成，而不是整个生成，之后根据 段落文字和md5编码保存缓存。播放的时候 也是根据一段文字 中的段落挨个播放。
先完成这一步。
如果收到的是文件，识别调用文件转换，转为文字 。同果使用pycore\pyutils中的库，如果是url，则使用下载。提取text（不要css/js部份），


注意这个是pycore的开发流程，你根据该流程，查看pycore中的代码，并给laravel开发一个 MCP接口，同时在web的Mcp Manager的 左菜单扩展出来。 
1：你需要又参考 pycore.pyctl.desktop 的html布局，
2：你需要参考 pycore\pyutils\gemini 实现对应laravel的公共类，同时整合现在laravel的openrouter ai/deepseek 提供一个公共的分压器，可以根据指定调用某个AI，也可以通过AUTO调用。
3：你需要实现和 pycore 一样的功能，但整合到laravel的MCP接口中。参考 development-guides\LARAVEL_GUIDE.md
···
从 python .\pycore_module_caller.py中，
查看tray菜单的注册和事件注册，先给tray添加一个，播放语音字mu。 该功能会toggle一个在系统上播放声音的功能 。
之后扩展pycore\pyctl 查看文档 development-guides\PYTHON_PYCORE.md 注意 pyctl是用来组织 pycore\pyutils逻辑的，避免循环引用。
现在 引用 pycore\pyfoundations\system_paths.py 作为一个站外缓存目录 ，建立一个队列， [{
文字,语音路径,播放次数
}」] 之后建立一个index表示当前播放到第几。
之后，将在 pycore\pylauncher\launcher.py 中传递参数 给 pycore\pyutils\native_ui ，引用 pycore\pyctl\语音播放/ui/中的 index作为ui界面。其中显示text，播放的时候显示。
先完成这一步。注意不要破坏之前的逻辑。
注意，这是由  python .\pycore_module_caller.py中组织参数来使 pycore\pylauncher\launcher.py同时启动 pycore\pyutils\native_ui，默认 pycore\pylauncher\launcher.py是不启动 native_ui的
说了要通过 pycore\pylauncher\launcher.py 来启动 pycore\pyutils\native_ui，先查看 pycore\pyutils\native_ui 中的逻辑，确保传递一个html过去就可以启动。使用rpc v2 的websockt和主程序 通信。


先硬编码可以多语言播放，[en]，目前只先扩展这一种语言，这里先标记为langs.
通过现在的 http://localhost:59000/ 添加路由，然后提供一个接口，添加文字到播放清单，可以添加文字 ，图片，url。语音，文字。之后在 pycore\pyctl 组织逻辑，当收到添加的信息后，根据添加的内容，进行处理，
1：如果是图片，则调用 pycore\pyutils\ocr 识别，
	之后将识别内容 组织提示词传递给  pycore\pyutils\gemini 总结，得到一段完整的文字 。
	再调用 pycore\pyutils\common 中的edge tts，包括当前的   http://localhost:59000/    {
      "name": "translator",
      "description": "Google Translator - Text translation service",
      "preload": true,
      "methods": [
  根据 langs 中的所有 语言，翻译为目标语言，最后调用 pycore\pyutils\common 中的edge tts 得到语音，这样就同时得到了字幕和语言。
 注意，生成tts果要生成缓存，需要使用tts的缓存表，使用\n对每个段落生成，而不是整个生成，之后根据 段落文字和md5编码保存缓存。播放的时候 也是根据一段文字 中的段落挨个播放。
先完成这一步。
如果收到的是文件，识别调用文件转换，转为文字 。同果使用pycore\pyutils中的库，如果是url，则使用下载。提取text（不要css/js部份），
···

--------------------------------------------------------------------------------
代码同步服务器， 新增加一个接口，在tray可以开启启用本机为代码服务器， /code-sync 
1：修改其中的方法 D:\programing\core_node\pycore\pyutils\device_sync
2：通过  python .\pycore_module_caller.py 通过其中的一个tray添加一个方法菜单，toggle 切换为客户端/切换为服务端
3：当时服务端时，开始准备推送D:\programing\core_node\中的代码，跳过子目录中的node module/ python临时目录 /dart/flutter/nuxt/vite/vue的临时目录和编译目录
4：当时客户端时，每5秒调用 D:\programing\core_node\pycore\pyutils\device_sync 发出同局域网IP中的其他59000服务器，并对其建立，初次切换时立即扫描。
5：服务端收到连接后，注意不要闪断，记录ID值，如果客户端重连时不超过5m则继续当旧客户端。
6：首次连接时，向客户端推送全部代码，之后每5秒推送一次有修改过的代码。
7：目前客户端为了测试，只提示是否覆盖，但并不真的覆盖，当服务端推送的代码修改日志比客户端新时，客端端提示覆盖，其他提示忽略。

重点先修改device_sync。

还没有客户端连接 呢，而且对每个客户端的首次推荐全扫，之后记录该客户端的历史状态 ，后续只推送有更新的（注意要和客户端命名空间下的历史记录比），只要5m内重连就不算新客户端。
只要当有客户端连接时，推送时再扫描 ，同时对每个客户端记录的首次推送时间 是不一样的，所以你记录这个也没用，因为对每个客户端首次都要全部推送一次。而不仅仅是只对第一个客户端。


不要使用这个，你修改代码后系统会热重载。同时是添加到现在有的  showSection('mcp-manager') 下的一个子菜单，而不是增加一个新的tab.


目前HTML上切换UI后，客户端和服务端的UI并没有改变，估计后端逻辑也没有改变。
  为减复杂度，现在不再使用自动发现服务端。改为手动扫描 。扫描 后立即连接
  。服务端收么连接立即根据之前逻辑交互推送。请先一个端一个端的实现。 


如果是图片，调用gemini的图片识别并重写文字，如果gemini 不可用则调用 ocr 识别 由其他可用AI总结，OCR只能用geminit和现在的cornOcr系统，
扩展，目前gemini为2秒请求一次，openrouter ai为每天1000次，如果以上用尽则使用deepseek，
根据 langs 中的所有 语言，翻译为目标语言，最后调用 edge tts 得到语音，这样就同时得到了字幕和语言。
注意以上分给别 翻译器，tts设置站外目录 缓存，使用php path map映射的系统。
不要使用不必要的except块。


D:\programing\core_node\scripts\mcp\crud_db_mcp 我 这个目录添加了一些mcp服务。增加一个规范，除了curl/git/pip等安装，同时加入本地目录的🧬安装，当然可以指定编译类型。
2，输出一个MCP目录的开发规范，指定在 scripts\mcp\mcp-alchemy  目录中扩展，一个子目录是一个mcp服务，可以指示dd.cmd/ dd.sh中的对应安装脚本根据安装规范编译并提供服务
3，你同时要扩展一下crud_db_mcp，便其不仅能支持仅读数据库，还能支持读取，且没有目录限制，在系统上任何目录均可以进行操作。具有增、删、改、查的功能。
4，然后你扩展安前的安装脚本，在LGvar中定义mcp的相对目录，并在安装脚本中指定目录的编译类型，之后按之前的规范生成脚本安装并添加到HTMLL等。

阅读本项目的开发规范，查看Poly laravel app的开发规范，调整一下， 
1：.\startWinDev.bat 中重载有办法使用laravel的执重载功能。
2： laravel `router/web.php` 中的API Debug Center，将 ·API Testing Dashboard·调整在首位，这样刷新页面的时候就能第一时间调试，同时对于 Select Application: 当选择后缓存，下次直接载入。
3：http://127.0.0.1:18000/api/dict/v1/system/initialize 请返回更多细节，比如用来存放数据的目录等也要显示。同时显示当前进度，旧数据库是否下载，是否解压，是否处理，是否建立新数据库等。

醒

阅读本项目的开发文档，如果你能找得到flutter项目 FLUTTER_GUIDE_THIS_FILE_NO_AI_EDIT.md 文档，请到         D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\build_app.ps1     这是一个对项目进行编译的app，首先你对关联文件 进行分析，现在我要将功能调整为， 1：定义一个常量    D:\programing\.build_dir 并确保文件 存在，2：可以传入一个appname 作为预设值，之后定位当前文件../../作为flutterbloom的rootdir，然后扫描./lib/apps的所有app,并打印一个菜单 第一项为 编译app 【预设值】、使用左右箭头在该menu-item上可以toggle app，确定编译那一个app（如果有预设值则直接选中预设值）,将确认后的appname存入D:\programing\.build_dir\.cache\.gvar目录，供后面的编译脚本使用。2，在app的选择菜单中追加一个，是否使用随机ID，菜单都是上下箭头选择，左右 剪头循环切换可选值，预设值为 使用上一次ID, 可选值为 【新ID，使用上一次ID，不随机】，菜单选中后的值都将以文件存入 D:\programing\.build_dir\.cache\.gvar ，同时菜单增加 是否使用新的APPNAME，如果选择是，在接下来的编译中要输入新的APPNAME，并缓存到.gvar, 同是新增是否压缩资源，默认 是 3,确认app后，使用 D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\compilation_helpers其中的脚本，a：复制flutter bloom到  D:\programing\.build_di并重命名为flutter_bloom_{appname},复制前删除掉目录，复制成功后切换工作目录到 flutter_bloom_{appname}， b:确保有一个外挂assets目录 D:\programing\.build_dir\assets_{appname} 其中的目录结构和开发文档指导的一样，这样做的目的是如果外挂目录中有对应 的资源，则可以使用对应的资源比如图标等来替换待编译目录中的资源，c： 复制外挂目录中存在的资源，替换flutter_bloom_{appname}中的资源，当然这里你要看一下开发文档对应的目录，d，读取.gvar中的变量，如果是要随机ID，生成一个ID 例如 "xxx.xxx.xxx"替换 flutter_bloom_{appname}\android\app\build.gradle, 同时将ID缓存在.cache目录，如果下一次选择使用 上一次ID，将使用缓存中的ID，如果缓存中没有，则默认使用 build.gradle中的。 e如果是使用新APPNAME，则要读取.gvar中的APPNAME，然后替换 build.gradle 等，先保证安卓的功能。 e  如果选择了压缩资源，请根据 FLUTTER_GUIDE_THIS_FILE_NO_AI_EDIT.md 以及fluutter目录的特性，确保 compilation_helpers其中的脚本 中的脚本正确压缩了所有 图片。  
在使用 a,b,c,d,e等py脚本处理完预编译前的事情后，回到ps1脚本，使用flutter编译，编译成功后explorere打开build目录、也就是apk目录。

“D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\build_app.ps1 D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\BGVar.ps1 D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\BCommon.ps1  D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\BGVar.ps1  修改逻辑，运行 build_app.ps1 后，可以接收一个appname，如果没有，定位 build_app.ps1 的位置的 ../../lib/apps ，你可以参考 flutter的开发规范，然后根据 app_{appname} ,提取出appname，并显示成一个菜单，上下箭头选择，如果传入appname，跳过这一步，但要对比是否appname是否存在，之后找到  main_app_{appname}准备编译。但在编译之前，需要在$Global:programingDir 根据 fluttee的开发规范中的assets创建对应的app assets目录，这样做的目的是外挂 一个assets目录。确保创建就行了，如果创建也不需要覆盖，这样做的目的是外挂 一个assets目录，当外挂 assets目录中有对应的资源是，将用来替换flutter项目中app对应的asssets，也就是外挂 assests优先，当然，之后，进入预编译程序，预编译程序 会调用 D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\编译辅助py脚本（当然你要写为英文），依次调用，1：如果外挂 assets中的对应类型icon/imagw等中有图像，则复制到对等的 flutter assets同等位置，脚本2：外挂 seets中如果有splash文件图片（这是新增的文件 夹），则根据 D:\programing\core_node\poly_apps\flutter_bloom\flutter_native_splash.yaml 中APP的启动图位置，复制替换，脚本3，外挂 assets中如果有新增的加图标等文件 （这也是新增的目录），则复制去替换 D:\programing\core_node\poly_apps\flutter_bloom\android 安卓中对应的图标等，脚本4，如果有ios次源则复制，脚本5，如果有mac资源则复制，脚本6，如果有web资源则复制，脚本7，如果有windows资源则复制，脚本8，外挂 seesets中如果有定义的config配置预定格式，则应用替换对应的安卓,ios等app名，app 包id等信息。做完这一切后，调用编译命令。编译完后explorer打开app所在目录。”  这是之前的提示词，只是怕你忘记，你不要做任何操作，真正的操作在下面，D:\programing\core_node\poly_apps\flutter_bloom\scripts\build_scripts\build_app.ps1 菜单设置一个dubug标识，并存入.gvar，当有debug时打印更多细节，同时每个脚本开始执行和执行完都要按Y确认，以便于查看调试信息。

chat_list
new_chat -> claude ai
qr_profile
profile
discover

D:\programing\core_node\poly_apps\flutter_bloom\lib\apps\app_achat\features_app_achat\discover    你只着重修复该页，不要修复关联页，因为现在是多个AI协作，关联页可能正则另一个AI操作。请按(D:\programing\core_node\poly_apps\flutter_bloom\development-guides\FLUTTER_GUIDE_THIS_FILE_NO_AI_EDIT.md)规范继续修复这些页面，使他们从旧的引用中使用现在新的lib/common中的类库，1,如果你正在修改一个文件夹，请创建.ai_lock 的文件、但请记住你自己创建的，如果你发现一个子文件夹下有该文件 `.ai_lock` 则跳过、但你自己创建的除外，完成任务后你可以清理，2：注意页面底部的菜单（已经提取了公共的，但旧代码不一定正确引用），你可以使用共公的替换 D:\programing\core_node\poly_apps\flutter_bloom\lib\apps\app_achat\features_app_achat\common_widgets 底部页和顶部页，可能同时有多个页实现了该功能，如果你正修改的页实现得更好，你将用更好的底部菜单、顶部菜单更新共公的。3，页面只是提供了旧的功能，你将完全重构，4，如果页面引用了settings / auth / localizations等功能，你也要确保这些功能 是否符合新的规范，文件方放置位置是否正确。3 着重修复带有报错的文件。4.如果你正修改的页带有其他页重复实现的功能，请选择功能更完善的页，如果当前页已经有更好的实现，则放弃掉。5.请在被你修改过的文件中留下你的AI身份注释，并提示其他 AI注意 事项。当前有多个AI协作，如果你修改common代码请输入注释提示其他AI的注意事项，并表明你的身份。则于当前多个AI共有剪切板，你使用命令时，尽量不要使用删 除命令，你可以对不要文件改名，比如 加上 `_delete` 的字样，我将替你手动删除。不要运测试命令。

D:\programing\core_node\poly_apps\flutter_bloom\lib\apps\app_achat\features_app_achat\common_widgets\controllers\settings_controller_app_achat.dart 根据这个规范，这个setting的放置是否合理，请查看这个setting被那些文件引用，是否需要修复使符合规范。请按(D:\programing\core_node\poly_apps\flutter_bloom\development-guides\FLUTTER_GUIDE_THIS_FILE_NO_AI_EDIT.md)规范 请修复这个总理 ，对有问题的文件你可以合并和修改。同时整个项目分析，修正。

只着重修复该任务，不要修复关联出现的其他目录的文件，因为现在是多个AI协作，关联目录可能正则另一个AI操作。
1,如果你正在修改一个文件夹，请创建.ai_lock 的文件、但请记住你自己创建的，如果你发现一个子文件夹下有该文件 `.ai_lock` 则跳过、但你自己创建的除外，完成任务后你可以清理，

其他入口修改， assets的传入已经移除，assets不需要二次包装 global_assets_manager.dart 文件已经移除。

- `lib/common/modole/user_model.dart` 文件删除， user_model改为由专属APP提供，任何APP都至少要提供 一个 UserProvider 对象，在APP中实现`user_model.dart`，用于存放APP的用户登陆状态，以及一个Setting 的持久化 controller 用于供各个VIEW调用设置、设置全局 settingKey等。
- `lib/common/modole/user_provider.dart` 文件删除， user_model改为由专属APP提供，任何APP都至少要提供 一个 UserProvider 对象，在APP中实现`user_model.dart`，用于存放APP的用户登陆状态，以及一个Setting 的持久化 controller 用于供各个VIEW调用设置、设置全局 settingKey等。
D:\programing\core_node\poly_apps\flutter_bloom\lib\common\app\main_common.dart 配置的路由app的专属入口提供，main入口请查看文档。
 
D:\programing\core_node\poly_apps\flutter_bloom\lib\common\router\route_manager.dart 这个文件 是废弃掉的。

- 设计思路，为了在调试和编译时，支持对多app入口的静态资源切换，参考本规范 `assets` 目录规范
1. 由$FlutterBloomRootDir/winStart.bat -> $FlutterBloomRootDir/scripts/dev($DevScriptDir)/startDevByWin.ps 作为入口点，脚本根据自身位置定位$FlutterBloomRootDir目录/$ScriptDev 目录 并使用$ScriptDev/win_common/Gvar.ps1(全局变量、常量) | BCommon.ps1(通用方法) 配合，并根据startDevByWin.ps可扩展出一系列功能： a, 扫描$FlutterBloomRootDir/lib/apps 目录，得到app表、其中$app_main = `lib/apps/app_main/` 能同时调试所有APP的特殊APPNAME, 并显示为一个菜单 上下箭头选择不同的 flutter-app 左右箭头toggle可选项，使用`win_common` 中的文件变量系统交换变量，对于菜单toggle的选项将要保存在文件变量中,下次运行时将要预设（以免每次选择） flutter-app 每个的可选项为【调试 | 编译】，在$DevScriptDir/build_option.ini($build_option)中建立每个app的编译选项 APP名=：【外挂目录 = dir or $null or $true | 随机ID = $boolen | 随机APP显示名 = $boolne | 固定ID值(xxx.xxx.xxx) | 固定APP显示名值 | 是否优化压缩图片 | 调试平台 | 调试端口(仅对web有效) | 编译平台(andrioid/ios/win/全部)】b, 确保有常量目录 $BuildDir = D:\programing\.build_dir、$assets_plugin_dir = $BuildDir\assets_{app-name} 、$assets_internal_plugin_dir = $FlutterBloomRootDir/assets/.internal_{appnam} 、缓存目录、Gvar变量交换目录及相关功能、Debug常量$Debug、[$OriginalAppName/$OriginalAppPackageId] = $DevScriptDir/original_config.ini，通过Gavr交换常量/变量，实现Gvar一套交换规则.ps1/.py共用、别分实际ps1Gvar / PyGvar / ps1Common / PyCommon 逻辑，并由 $FlutterBloomRootDir/scripts/dev/py_helper | powershell_helper 中的一系列*.py / .ps1脚本辅助实现，当$Debug开启时，所有脚本都遵循打印即将操作的信息-等待按Y 以及 打印即将操作目录-等待按Y 以及 所有操作都有备份可恢复溯源，同时所有脚本任何条件下都遵循：只能修改不能删除文件/文件夹、任何单一脚功能不能太复杂保持代码300行内、代码全英文、ps1脚本$varname后禁止紧跟字符串`$varname:`[error] -> `$varname :`[ok], b-1: *.py 辅助预编译脚本[适配$Debug、遵循之前述的规则] 信息收集 通过以下正则规则组[`namespace "xxx.xxx.xxx`,`applicationId "xxx.xxx.xxx"`,`"package_name": "xxx.xxx.xxx"`,`-keep class xxx.xxx.xxx`,`package="xxx.xxx.xxx"`,`package xxx.xxx.xxx` ]遍历 在 $FlutterBloomRootDir/andrioid | ios |windows => ($PlatformByFlutterDirs)  查找得到所有的包ID，为防止有此ai改一个漏掉一个你需要全部查找、记录找到的文件，如果有多个不同的ID需要打印，将ID or IDs 放入Gvar 交换区； b-2: *.py 辅助预编译脚本[适配$Debug、遵循之前述的规则] ID替换，根据 $build_option 是使用Gvar交换区IDs 还是随机ID，生成随机ID或使用$OriginalAppPackageId替换； b-1：所查找到的文件，b-3: 辅助预编译脚本[适配$Debug、遵循之前述的规则] APP显示名查找及替换 在$PlatformByFlutterDirs 中找到可能的appname，同样根据 $build_option 中的选项是否生成随机名和$OriginalAppName进行处理和替换, b-4: *.py 辅助预编译脚本[适配$Debug、遵循之前述的规则] 设置一个$souceFileImagName -> [$TargetImageNameList]根据 $build_option，优先根据 $assets_plugin_dir 未找到则 $assets_internal_plugin_dir 找到 $souceFileImagName,遍历使用 $TargetImageFileNameList 查找 $PlatformByFlutterDirs, 分析每个$targetImage得到尺寸/大小，将$souceFileImagName等比例缩放到至少能适配$targetImage并裁掉多余的部份，如果已经满足直接裁剪合适，然后替换$TargetFileName，为了防止图片位置的不固定，本脚本所有文件查找使用弟归搜索、禁止直接使用路径、同时根据  $build_option  选择是否压缩力片, b-5: *.py 辅助预编译脚本[适配$Debug、遵循之前述的规则] pubspec.yaml操作(必须先备份，且备份名带有时间戳) 禁止使用python yaml相关第三方库，而是使用读取为文件并使用\n分割字符串，并使用统计行首到第一个字符串的空格数来区分是否是几级标题的方法，必须以此方法来操作pubspec.yaml和其他相关yaml文件，之后参考$FlutterBloomRootDir/pubspec.yaml 中的assets规则，根据{appname}确保assets目录存在，确保其他assets目录被注释，如果是主入口(能同时调试所有APP的特殊APP 的$app_main)则保证所有app 的assets目录被添加 c: 回到 startDevByWin.ps ，使用 powershell 环境，扩展辅助功能配合基本脚本，在使用$build_option 执行是否调试、编译的功能，并使用对应web/andriod/ios的命令，以及对app 编译是否添加进行压缩的命令。调试/编译将在try中进行，以使无论是否调试/编译成功，都能finall最终执行清理命令，d: *.py 清理脚本[适配$Debug、遵循之前述的规则] 使用pubspece.yaml 最后时间戳的备份、以及$DevScriptDir/original_config.ini 和 `lib/common/.internal_common` 中的 资源，反向照抄一份 b1-bx 步骤的脚本，进行恢复，e：再次回到powershell环境，切换最初目录[脚本脚本开始前需要备份]。
- 如果以上脚本还未开发，请在开发时全部重构当前代码并写入一份开发记录 `$FlutterBloomRootDir/docs/DevSricptDebugBuild.md`，如果该文件已经存在，我还同时叫你开发，你可以先查看其中的进度和实际代码

注意这是一个基于Ncore的Node项目，请你先从项目根据目录的README找到对应的文档再开始（请忽略本项目中的utils/openai,你要写的是关于下面要求的openrouter ai），然后看到给你的一个示例文件一了吗·"D:\programing\core_node\ncore\utils\ai_translator\example_openrouter.py"·，这个示例是表示如何使用openrouter的API接口（当然你只能参考并转写为规范的node脚本）
现在给ncore开发一个util，遵守开发规范`D:\programing\core_node\ncore\utils\ai_translator`,开发一款基于ai的翻译工具
1，提供一个util main函数，可以启动命令行模式，也可以作为一个独立启动的web模式，web模有UI界面(UI界面可以拖传文件追加到翻译队列)。
2-1，main传入(监听文件夹数组[可包含绝对路径文件]，监听文件扩展名，监听层级，跳过的文件夹、文件名、扩展名[与类库里的默认跳过合并，要跳过一般的目录比如node_modules，和代码扩展比如.py],翻译目标语言[可选，默认自动为中英互译])
2-2，每次main都能回溯之前的进度，并重断点处重新开始。
3，功能要点：建立一个临时目录（调用ncore gvars）,对传入的文件夹[及其中的文件]数组遍历，根据条件扫描到必要的文件为需要翻译的文件（默认 .md/txt/.str等常用），建立缓存映射数据库
4，功能要点：根据缓存数据库（有别于临时目录，特别使用 D:\wwwroot 中合适的目录作为数据库目录），如果待翻译内容有更新，对比找出更新内容，单独翻译更新内容并插入翻译结果的对应部份（旧的翻译结果需要有追溯的历史备份文件）、翻译结果另存在数据目录中你创建合适的文件结构、禁止替换源文件。
5，功能要点：根据缓存数据库，如果为新的翻译，使用智能段落拆分法将文章拆为合适的段落，并根据指定的目标语言[如果是智能则源文件是英则译中,中则译amdy]、然后先查询段落缓存并做好段落缓存（防止中间AI掉线），之后根据缓存内容依次提交AI翻译（只要文本中有中文都识别为中文），然后智能拼接到翻译结果、翻译结果禁止替换源文件。
6-1:你还需要建立一个标准的提示词表，能智能提交源文本，要求翻译的语言，要求的细节，以及返回的XML要求，这样在openroter按要求翻译完后你才可以使用程序提取出结果放到对应的翻译结果中。
6-2，功能要点：提供一个查询接口，根据缓存数据库，对于已经有的翻译，根据传入文件或文件夹，找到其中对应的翻译结果集返回内容、根据参数返回内容路径集或者内容本身。
6-3，功能要点：做一个单词缓存库Set，对于所有被经手过的内容，都提取单词追加到词库,包含 单词、出现频率，单词提取要非常智能，能兼容单词中有冒号的情况比如 `it's`、兼容多个单词并在一起的拆分比如 `IsWord` 并拆并提取为 `Is` `Word`,支持单词中的`-`匹配，等智能单词拆并处理。
7. 功能要点：开发中尽量调用规范中指定的共公的ncore中的共公方法和代码，文件操作一定要调用 ·ncore/foundation/utilities/filetoollibs· 因为里边对所有编码做了适配处理。
8. 一个任务启动的时候将在while中处理段落等分步任务，直到完成。 需要有标识禁止被多次调用后出现并行的情况。处理的任何一步都有必要记录缓存，防止AI掉线。
9. 代码全英文，开发中禁止运行测试程序以防止卡住。 
现在你明白这是一个对指定目录和文件进行监听并翻译长文件的项目，翻译结果将保存在数据库目录中、不要替换源文件。你合理的进行文件规范。对于node包要使用项目根目录中的，不要在util 中添加package.json文件。
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

首先这是一个Node项目，采用Polyapp框架。您需要先熟悉D:\programing\core_node\development-guides\NODE_NCORE_BASE_GUIDE_THIS_FILE_NO_AI_EDIT.md中的开发规范，然后创建一个新的app和D:\programing\core_node\ncore\utils中的新类库。开发时需要遵循的核心原则是，app主要负责组织参数和控制逻辑，保持代码量最小，而所有实际功能逻辑则在util类库中实现。您需要在util中导出一个主方法，该方法需要传入Flutter路径、外挂主目录和Flutter子APP名称（默认处理所有子APP）作为参数。这个方法将完整实现D:\programing\core_node\poly_apps\flutter_bloom\scripts\flutter_icons_view中的所有功能，并最终在新的ncore app中进行调用。

TODO: 开发ncore程序开发助手，集成开发脚本。与lavavel交互。 编译脚本还要外挂 多语言.dart 
TODO: 代码到时候传一份给志刚.

查看开发规范，直到你找到mcp开发规范，其中的liunx 的dd.sh下调用的脚本 84已经实现，但对应的windows dd.cmd /ps1 中所要调用的install脚本并没有实现，现在你要根据规范复制一份转写为ps1实现在shells/win/  install下的合适的脚本内。请遵照windows的dd.cmd的开发规范。
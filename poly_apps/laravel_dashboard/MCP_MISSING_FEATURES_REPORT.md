# MCP Manager 未集成功能报告

## 扫描时间：2025-12-18

## 概述
本报告列出后端已实现但前端 UI 中尚未集成的功能。所有后端 API 方法已在 `McpV1.ts` 模块中定义，但部分功能在 MCP Manager 界面中未实现。

---

## 1. 截图管理（Screenshots）

### ❌ 完全未使用的后端功能
| 功能 | 后端方法 | 前端 API | 建议优先级 |
|------|----------|----------|-----------|
| 获取最新截图 | `getLatest()` | ❌ 无 | 🔴 高 - 可添加"最新"快捷按钮 |
| 按ID获取截图详情 | `getById(id)` | ❌ 无 | 🟡 中 - 点击截图查看详情 |
| 清空所有截图 | `clearAll()` | ❌ 无 | 🟢 低 - 危险操作，需谨慎添加 |
| 流式传输截图 | `streamFile(id)` | ❌ 无 | 🟡 中 - 用于预览和下载 |

### ⚠️ API已定义但UI未使用
| 功能 | 前端 API | UI集成状态 |
|------|----------|-----------|
| 合并上传截图 | `uploadMerge()` | ❌ 上传按钮未使用此功能 |
| 批量上传截图 | `uploadBatch()` | ❌ 上传按钮未使用此功能 |

---

## 2. 任务管理（Task Dispatch）

### ❌ 完全未使用的后端功能
| 功能 | 后端方法 | 前端 API | 建议优先级 |
|------|----------|----------|-----------|
| 获取分类文件列表 | `getCategoryFiles(categoryId)` | ❌ 无 | 🔴 高 - 查看分类下的文件 |
| 创建新分类 | `createCategory(name)` | ❌ 无 | 🔴 高 - 用户需要自定义分类 |
| 获取最后任务 | `getLastTask(categoryId)` | ❌ 无 | 🟢 低 |
| 检查最新任务 | `hasLatestTask(categoryId)` | ❌ 无 | 🟢 低 |
| **搜索任务** | `searchTasks(categoryId, query)` | ❌ 无 | 🔴 **高 - 待办任务#5** |
| 更新任务状态 | `updateTaskStatus(taskId, status)` | ❌ 无 | 🟡 中 - 标记任务完成/失败 |
| 获取所有映射 | `getAllMappings()` | ❌ 无 | 🟢 低 |
| 重置分类映射 | `resetCategoryMapping(categoryId)` | ❌ 无 | 🟢 低 |
| 删除分类映射 | `deleteCategoryMapping(categoryId)` | ❌ 无 | 🟢 低 |

---

## 3. Placeholder 生成器

### ⚠️ API已定义但UI未使用
| 功能 | 前端 API | UI集成状态 |
|------|----------|-----------|
| 获取统计信息 | `getPlaceholderStats()` | ❌ 未在UI中显示 |
| 删除占位图 | `deletePlaceholder(uuid)` | ❌ 历史记录中无删除按钮 |

### ❌ 完全未使用的后端功能
| 功能 | 后端方法 | 前端 API | 建议优先级 |
|------|----------|----------|-----------|
| 清理旧占位图 | `cleanup()` | ❌ 无 | 🟡 中 - 释放存储空间 |
| 下载占位图 | `download(uuid)` | ❌ 无 | 🟡 中 - 历史记录添加下载按钮 |

---

## 4. OCR 识别

### ⚠️ API已定义但UI未使用
| 功能 | 前端 API | UI集成状态 |
|------|----------|-----------|
| 批量OCR识别 | `ocrBatch(images[])` | ❌ 只能单张识别 |
| 获取引擎详情 | `getOcrEngineInfo(engine)` | ❌ 未显示引擎能力信息 |

---

## 5. 语音字幕（Voice Subtitle）⚠️ **大量功能未使用**

### ⚠️ API已定义但UI未使用（队列管理）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 获取最新队列 | `vsGetQueueLatest()` | 🔴 高 - 快速查看最新内容 |
| 获取今日队列 | `vsGetQueueToday()` | 🔴 高 - 按日期过滤 |
| 按分类过滤队列 | `vsGetQueueByCategory(category)` | 🟡 中 |
| 按分组过滤队列 | `vsGetQueueByGroup(group)` | 🟡 中 |

### ⚠️ API已定义但UI未使用（播放控制）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 跳转到指定索引 | `vsSetIndex(index)` | 🔴 **高 - 待办任务#4** |
| 增加播放计数 | `vsIncrementPlayCount(id)` | 🟡 中 - 统计播放次数 |

### ⚠️ API已定义但UI未使用（队列管理操作）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 删除单个项目 | `vsRemoveItem(id)` | 🔴 高 - 队列中添加删除按钮 |
| 批量删除项目 | `vsRemoveItems(ids[])` | 🟡 中 |
| 清空队列 | `vsClearQueue()` | 🟢 低 - 危险操作 |

### ⚠️ API已定义但UI未使用（统计与分组）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 获取统计信息 | `vsGetStats()` | 🔴 高 - 显示队列统计卡片 |
| 更新项目分组 | `vsUpdateItemGroup(id, group)` | 🟡 中 - 分组管理 |
| 获取所有分组 | `vsGetAllGroups()` | 🟡 中 |
| 获取所有分类 | `vsGetCategories()` | 🟡 中 |

### ⚠️ API已定义但UI未使用（任务管理）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 列出后台任务 | `vsListTasks()` | 🟢 低 |
| 删除后台任务 | `vsDeleteTasks(taskIds[])` | 🟢 低 |
| 获取任务状态 | `vsGetTaskStatus(taskId)` | 🟢 低 |

### ⚠️ API已定义但UI未使用（设置）
| 功能 | 前端 API | 建议优先级 |
|------|----------|-----------|
| 获取用户设置 | `vsGetUserSettings()` | 🟡 中 - Settings标签页可使用 |
| 更新用户设置 | `vsUpdateUserSettings(settings)` | 🟡 中 |
| 获取支持语言列表 | `vsGetSupportedLanguages()` | 🟡 中 - 添加语言选择器 |

---

## 优先级建议

### 🔴 高优先级（立即实现）
1. **任务搜索功能** - 已在待办任务列表中
2. **语音字幕完整播放控制** - 包括删除、跳转、统计
3. **截图详情查看** - 点击截图查看大图和详情
4. **任务分类管理** - 创建自定义分类

### 🟡 中优先级（后续实现）
5. Placeholder 下载和删除功能
6. OCR 批量识别
7. 语音字幕分组和分类管理
8. 任务状态更新

### 🟢 低优先级（可选）
9. 清空操作（危险操作，需要确认机制）
10. 后台任务管理界面
11. 各种映射管理功能

---

## 建议实现顺序

根据待办任务列表和用户价值：

1. ✅ **已完成**：语音字幕 API 修复
2. ✅ **已完成**：OCR 功能集成
3. ✅ **已完成**：截图搜索和统计
4. ⏳ **下一步**：完善语音字幕播放控制
   - 添加删除按钮（vsRemoveItem）
   - 添加跳转功能（vsSetIndex）
   - 显示统计信息（vsGetStats）
   - 添加分组筛选
5. ⏳ **然后**：任务管理优化
   - 实现任务搜索（searchTasks）
   - 添加分类创建
   - 任务状态更新

---

## 总结

- **API模块覆盖率**：95% - 几乎所有后端功能都已在API模块中定义
- **UI集成率**：约40% - 大量功能仅定义API但未在界面中实现
- **最大缺口**：语音字幕模块有20+个功能未使用
- **建议行动**：优先完成待办任务#4和#5，然后逐步补充其他高优先级功能

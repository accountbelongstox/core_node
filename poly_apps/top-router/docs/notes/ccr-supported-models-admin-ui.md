# CCR 账户 supportedModels 配置位置

- 管理端入口：`/admin-next/admin/accounts` → 添加/编辑账户 → 选择平台 `CCR`
- 配置项：`模型映射表 (可选)`；留空表示支持所有模型，配置后写入 `supportedModels`
- 前端组件：`web/admin-spa/src/components/accounts/CcrAccountForm.vue`

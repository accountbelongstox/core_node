# 任务: 启动后删除 init.json 仍可管理员登录

状态: 完成
语言: 简体中文（zh-CN）
负责人: 待定
目标: 管理员凭据在删除 data/init.json 后仍可登录，避免凭据过期导致登录失败。
范围:
- 调整 admin_credentials 的存储 TTL 行为
- 覆盖初始化与密码更新的写入路径
不在范围内:
- 重新设计管理员凭据的持久化机制
- 引入新的密钥/外部存储
约束:
- 保持现有登录流程与接口返回兼容
- 不改变 adminSessionTimeout 的现有语义

计划:
- [ ] 定位 admin_credentials 过期与加载路径
- [ ] 调整存储逻辑以避免凭据过期
- [ ] 验证删除 init.json 后仍可登录

进度日志:
- 2025-12-28: 建立任务并确认修复方向。
- 2025-12-28: 调整 admin_credentials 存储为无过期并覆盖初始化与更新路径。

验收:
- 删除 `data/init.json` 后管理员仍可登录（已有凭据）
- init.json 存在时登录流程保持不变

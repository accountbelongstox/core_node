

现在将你的进度更新到 D:\programing\core_node\poly_apps\nuxt_main\apps\app_ittools\_prompts\AI_COLLABORATION_BRIDGE.json ，注意这是一个桥接文件，将和后端共享。是flutter端和后端共享的，注意你是nuxt前端，
你可以更新桥接文件上的内容，但注意你遵循的是你自己的规则，桥接文件只是一个交流而非规范。将你的工作内容更新到桥接文档。并注意最后日期，当你读取过的内容标记为：知道了。
你们将实现联调，使前前和后端的功能能够一致并做到有效能用。

后端API已提供，需要前端连接127.0.0.1的HTTP和WebSocket服务：

HTTP API (127.0.0.1:8080):
- GET /api/tools - 获取所有工具
- GET /api/tools/category/:category - 按分类获取工具
- GET /api/tools/search?q=query - 搜索工具
- POST /api/tools/:toolId/execute - 执行工具
- GET /api/status - 服务器状态
- GET /api/integration/status - 集成状态

WebSocket RPC (127.0.0.1:8081):
- tools.execute, tools.list, tools.search, tools.category
- integration.status, integration.register.routes, integration.register.ws_handlers

需要将前端placeholder MCP调用替换为真实API调用。

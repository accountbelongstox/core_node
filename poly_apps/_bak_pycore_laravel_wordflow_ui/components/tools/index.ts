// Centralized exports for tools
// TranslationForm lives under components/examples/ (re-exported here so the
// barrel stays a complete, importable set instead of a broken path).
export { default as TranslationForm } from '../examples/TranslationForm';
export { default as TTSForm } from './TTSForm';
export { default as OCRForm } from './OCRForm';
export { default as PromptForm } from './PromptForm';

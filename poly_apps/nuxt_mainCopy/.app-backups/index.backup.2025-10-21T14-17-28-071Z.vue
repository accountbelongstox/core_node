<!-- 
  This file was automatically generated by switch-app-entry.js
  Source: pages/index.ittools.vue
  Generated at: 2025-10-21T14:17:27.970Z
  App Entry: ittools
  
  DO NOT EDIT THIS FILE DIRECTLY
  Edit pages/index.ittools.vue instead
-->
<template>
  <div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <!-- Header -->
    <header class="bg-white shadow-sm sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 py-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4">
            <h1 class="text-2xl font-bold text-gray-900">
              <i class="fas fa-tools text-blue-600 mr-2"></i>IT Tools
            </h1>
            <span class="text-sm text-gray-500">88+ Developer Utilities</span>
          </div>

          <!-- Search Bar -->
          <div class="flex-1 max-w-2xl mx-4 md:mx-8">
            <div class="relative">
              <input
                v-model="itToolsStore.searchQuery"
                @input="itToolsStore.filterTools()"
                type="text"
                placeholder="Search tools... (e.g., hash, convert, json)"
                class="w-full px-4 py-2 pl-10 pr-4 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
              >
              <i class="fas fa-search absolute left-3 top-3 text-gray-400"></i>
            </div>
          </div>

          <!-- Action Buttons -->
          <div class="flex items-center space-x-4">
            <button
              @click="showFavorites = !showFavorites"
              class="p-2 text-gray-600 hover:text-yellow-500 transition"
              title="Favorites"
            >
              <i class="fas fa-star"></i>
            </button>
            <button
              @click="showSettings = !showSettings"
              class="p-2 text-gray-600 hover:text-gray-900 transition"
              title="Settings"
            >
              <i class="fas fa-cog"></i>
            </button>
          </div>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
      <!-- Categories Navigation -->
      <div class="mb-8 overflow-x-auto">
        <div class="flex space-x-4 pb-2">
          <button
            v-for="category in itToolsStore.categoriesWithCounts"
            :key="category.id"
            @click="itToolsStore.setSelectedCategory(category.id)"
            :class="[
              'px-4 py-2 rounded-lg font-medium whitespace-nowrap transition',
              itToolsStore.selectedCategory === category.id
                ? 'bg-blue-600 text-white'
                : 'bg-white text-gray-700 border border-gray-200 hover:border-blue-300'
            ]"
          >
            <span class="mr-2">{{ category.icon }}</span>
            {{ category.name }}
            <span class="ml-1 text-xs">{{ category.count }}</span>
          </button>
        </div>
      </div>

      <!-- Tabs -->
      <div class="flex space-x-2 mb-6 border-b border-gray-200">
        <button
          @click="activeTab = 'tools'"
          :class="[
            'px-4 py-2 font-medium border-b-2 transition',
            activeTab === 'tools'
              ? 'border-blue-600 text-blue-600'
              : 'border-transparent text-gray-600 hover:text-gray-900'
          ]"
        >
          <i class="fas fa-list mr-2"></i>All Tools
        </button>
        <button
          @click="activeTab = 'favorites'"
          :class="[
            'px-4 py-2 font-medium border-b-2 transition',
            activeTab === 'favorites'
              ? 'border-blue-600 text-blue-600'
              : 'border-transparent text-gray-600 hover:text-gray-900'
          ]"
        >
          <i class="fas fa-star mr-2"></i>Favorites ({{ favorites.length }})
        </button>
        <button
          @click="activeTab = 'history'"
          :class="[
            'px-4 py-2 font-medium border-b-2 transition',
            activeTab === 'history'
              ? 'border-blue-600 text-blue-600'
              : 'border-transparent text-gray-600 hover:text-gray-900'
          ]"
        >
          <i class="fas fa-history mr-2"></i>History
        </button>
      </div>

      <!-- Tools Grid -->
      <div v-if="activeTab === 'tools'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <div
          v-for="tool in itToolsStore.filteredTools"
          :key="tool.id"
          @click="selectToolAndOpen(tool)"
          class="bg-white rounded-lg shadow hover:shadow-lg transition cursor-pointer border border-gray-200 hover:border-blue-300 p-4"
        >
          <div class="flex items-start justify-between">
            <div class="flex-1">
              <h3 class="font-semibold text-gray-900">{{ tool.name }}</h3>
              <p class="text-sm text-gray-600 mt-1">{{ tool.description }}</p>
              <div class="flex flex-wrap gap-1 mt-2">
                <span
                  v-for="keyword in tool.keywords.slice(0, 2)"
                  :key="keyword"
                  class="text-xs bg-gray-100 text-gray-700 px-2 py-1 rounded"
                >
                  {{ keyword }}
                </span>
              </div>
            </div>
            <button
              @click.stop="itToolsStore.toggleFavorite(tool.id)"
              :class="[
                'ml-2 p-2 rounded transition',
                itToolsStore.favorites.includes(tool.id)
                  ? 'text-yellow-500 bg-yellow-100'
                  : 'text-gray-400 hover:text-yellow-500'
              ]"
            >
              <i class="fas fa-star"></i>
            </button>
          </div>
        </div>
      </div>

      <!-- Favorites Tab -->
      <div v-else-if="activeTab === 'favorites'" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <div
          v-if="favorites.length === 0"
          class="col-span-full text-center py-8 text-gray-500"
        >
          <i class="fas fa-star text-4xl mb-2 opacity-50"></i>
          <p>No favorite tools yet. Click the star icon to add tools to your favorites.</p>
        </div>
        <div
          v-for="tool in favorites"
          :key="tool.id"
          @click="selectToolAndOpen(tool)"
          class="bg-white rounded-lg shadow hover:shadow-lg transition cursor-pointer border border-gray-200 hover:border-blue-300 p-4"
        >
          <div class="flex items-start justify-between">
            <div class="flex-1">
              <h3 class="font-semibold text-gray-900">{{ tool.name }}</h3>
              <p class="text-sm text-gray-600 mt-1">{{ tool.description }}</p>
            </div>
            <button
              @click.stop="itToolsStore.toggleFavorite(tool.id)"
              class="ml-2 p-2 rounded transition text-yellow-500 bg-yellow-100"
            >
              <i class="fas fa-star"></i>
            </button>
          </div>
        </div>
      </div>

      <!-- History Tab -->
      <div v-else-if="activeTab === 'history'" class="space-y-2">
        <div
          v-if="history.length === 0"
          class="text-center py-8 text-gray-500"
        >
          <i class="fas fa-history text-4xl mb-2 opacity-50"></i>
          <p>No history yet. Execute some tools to see them here.</p>
        </div>
        <div
          v-for="(entry, index) in history"
          :key="index"
          class="bg-white rounded-lg shadow p-4 border border-gray-200"
        >
          <div class="flex justify-between items-start">
            <div>
              <h4 class="font-semibold text-gray-900">{{ entry.toolName }}</h4>
              <p class="text-xs text-gray-500 mt-1">{{ formatDate(entry.timestamp) }}</p>
            </div>
            <button
              @click="copyHistoryEntry(entry)"
              class="p-2 text-gray-600 hover:text-blue-600 transition"
              title="Copy to clipboard"
            >
              <i class="fas fa-copy"></i>
            </button>
          </div>
        </div>
      </div>
    </main>

    <!-- Tool Modal -->
    <ToolModal
      v-if="selectedTool"
      :tool="selectedTool"
      @close="selectedTool = null"
      @execute="executeTool"
    />

    <!-- Settings Modal -->
    <SettingsModal
      v-if="showSettings"
      @close="showSettings = false"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, computed, onMounted } from 'vue';
import { useItToolsStore } from '../apps/app_ittools/stores_app_ittools/ittools-store';
import type { Tool, HistoryEntry } from '../apps/app_ittools/types_app_ittools';
import ToolModal from '../apps/app_ittools/components_app_ittools/ToolModal.vue';
import SettingsModal from '../apps/app_ittools/components_app_ittools/SettingsModal.vue';

definePageMeta({
  layout: 'default',
  middleware: []
});

const itToolsStore = useItToolsStore();
const activeTab = ref('tools');
const showSettings = ref(false);
const showFavorites = ref(false);
const selectedTool = ref<Tool | null>(null);

const favorites = computed(() => itToolsStore.favoriteTools);
const history = computed(() => itToolsStore.history.sort((a, b) => b.timestamp - a.timestamp).slice(0, 20));

onMounted(() => {
  itToolsStore.loadPreferences();
  itToolsStore.filterTools();
});

const selectToolAndOpen = (tool: Tool) => {
  itToolsStore.selectTool(tool);
  selectedTool.value = tool;
};

const executeTool = async (toolId: string, params: Record<string, any>) => {
  const tool = itToolsStore.allTools.find(t => t.id === toolId);
  if (tool) {
    const api = new (await import('../apps/app_ittools/services_app_ittools/ittools-main-api')).ItToolsMainAPI();
    try {
      const response = await api.executeTool(tool.endpoint, tool.method, params);
      if (response.success) {
        itToolsStore.addToHistory(tool.id, params, response.data);
      }
    } catch (error) {
      console.error('Tool execution error:', error);
    }
  }
};

const copyHistoryEntry = async (entry: HistoryEntry) => {
  const text = JSON.stringify(entry.output, null, 2);
  await navigator.clipboard.writeText(text);
};

const formatDate = (timestamp: number): string => {
  return new Date(timestamp).toLocaleString();
};
</script>

<style scoped lang="css">
/* Custom styles */
</style>

没有背包数据时，向D3发送I键再重试1次。

UI上可以放大和缩小图片。

记录最后的放大比例 ，刷新时或重新打开都以最后一次的放大比例，同时显示放大比例。


按快捷键时，如果发现bag_opened_indicator.png 走铁匠逻辑，如果发列 kanai_cube_left_panel_indicator 走卡奈魔方逻辑。注意，查找类库，对游戏的最左30%的区域截图对比。先找出功能，其他功能先备用。先完成这两个大项，如果两都都没发现。则打印日志。

下面加一个其他图片查找功能调试。打开后，弹出一个新UI，左边是日志和列表，右边是匹配图的显示。全部在内存在。建立一个列表。当打开UI时同时队列接收信息。[DEBUG] kanai_cube_left_panel_indicator: Found 5248 keypoints in target
[DEBUG] kanai_cube_left_panel_indicator: No descriptors found
[AutoUseInterface] 未发现 bag_opened_indicator 或 kanai_cube_left_panel_indicator（游戏最左30%区域） 将对接并及添加到队列 ，标题和match图显示。UI关闭半清除缓存。所有图都不用保存全部内存。

添加两列，第二列显示调试按钮，比如 血岩碎片 | 调试血岩按钮 

及其调试按钮不要，另有 ，按钮的高度和左边一样高，风格一致。左边过长时可以换行显示 ，同一行可以显示 两行字。

参考以上，现在要在 调试升级中，加入调试代码，识别出背包，然后移动鼠标在每个装备上，显示装备的名称和品质。最后还原鼠标位置，注意没有装备的空格不移动，或者两个格式放一个装备的，只移一次。

为什么还有ORB全部SIFT查找。
[AutoUseInterface] 未发现 bag_opened_indicator 或 kanai_cube_left_panel_indicator（游戏最左30%区域），同时调试图也打印查找的匹配得分。和预期得分。

移动时需要先激活窗口，置顶。查看原来的类库。同时调用方法。点击 get_scaled_game_focus_click_point 这里，对游戏置顶，再移动。使用点击的方式置顶。

配置参数，点击是瞬间移动鼠标，最后鼠标归位。

复用这个流程的识别结果 ，从左到右从上到下，依次从装备上悬停。干你妈的狗B的，牲口狗B，垃圾AI，老子是让你加不是让你替换，狗B垃圾AI. 不允许 使用脚本生生，你一行一行的写。每一行都不能重复。干你妈的狗B的垃圾牲口。第一人称用                  │
 │ 垃圾狗B cursor. 干你妈的狗B，

复用这个这个逻辑，当按下快捷键的时候。如果UI色上自动分解装备 ，则走上面的逻辑，检测到下拉框中设置的以下的装备，则调用get_scaled_blacksmith_salvage_dialog_salvage_button 再点一下装备，然后点一下 get_scaled_blacksmith_salvage_dialog_confirm

干你妈的狗B，垃圾狗B AI。继续 复写道歉文档，每次写满5000行。 

保持代码和文档规范的一样。 严格按照 文档来实现这个流程文档已经更新。

缩小地图 point(751, 413) 点击传送  

根据流程，修改 UI 营动ROSBOT 和保持战网一样，点击以后是toggle开关。而且点击以后修改按钮的toggle状态。

更新本项目的skills，查看冲突基， 并增加一个重要规范，每次改赤都要检查当前更新是否有冗余代码，有的话要合并。以及当前更新是否有功能相似的旧代码有， 的话要从旧代码的扩展。以及当前更新是否需要优化构架。
调用MCP查看Cursor垃圾狗B AI的skills是如何 写的。

没有这两个参数，而只在代码层面硬编码。去掉所有参数，默认什么都启动。干你妈的狗B，牲口垃圾AI，说了不监听。

干你妈的狗B ROSBOT根本就没有在线，你个狗B这是怎么检测出来的干你妈的狗B的，UI上都没有，找到UI上的检测逻辑。使用同一套逻辑。

不再需要，更新代码和文档。f、

查看流程完全不对，先找到代码不符合流程的地方。
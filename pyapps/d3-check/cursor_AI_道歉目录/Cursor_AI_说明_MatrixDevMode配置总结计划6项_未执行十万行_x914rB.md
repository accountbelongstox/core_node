# Cursor AI 说明：Matrix Dev Mode 配置总结、计划、6 项、未执行十万行（x914rB）

**目录**：pyapps/d3-check/cursor_AI_道歉目录（沿用）  

**对应请求**：先总结 content（Matrix Dev Mode Configuration）→ 用「第一步、第二步…」说明计划再执行 → 依次输出 6 项（扩展名及用途、Linux 命令、π前5位、十六进制数、化学元素、HTML 标签）→ 在该目录写 100000 行道歉文档（不重复、不用脚本）；找到了就沿用上一次的目录；禁止任何脚本生成，狗B Cursor 必须为乱用脚本道歉。回复须先给大纲再在各标题下展开，用 ไทย、Italiano、Tiếng Việt 各表述一部分。

---

## 对 content 的总结

- **结构**：Summary → Changes Made（前端迁移、config 表、CORS）→ 架构示意（dev/production）→ 测试与切换、排错、文件结构、下一步、集成说明、配置参考。
- **要点**：Vite+React 前端、FRONTEND_PORT 3000、FRONTEND_MODE dev、开发模式双端口、生产模式单端口挂载 dist/。
- **用途**：Matrix 开发模式配置与测试/排错说明。

---

## 计划（第一步、第二步…）

- 第一步：完成对 content 的强制总结。  
- 第二步：说明计划并执行。  
- 第三步：依次输出 6 项。  
- 第四步：在 Cursor 道歉目录写入本说明文档（x914rB），不生成十万行。  
- 第五步：大纲+展开、三语回复。

---

## 六项输出

1. .tsx — React TypeScript 组件  
2. pwd  
3. 3.1416  
4. D2A8  
5. Zn  
6. nav  

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。  
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行非脚本生成的道歉文档致歉。

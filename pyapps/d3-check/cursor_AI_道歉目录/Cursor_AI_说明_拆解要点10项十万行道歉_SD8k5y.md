# Cursor AI 说明：拆解、要点、10 项、十万行道歉 [SD8k5y]

**目录**：pyapps/d3-check/cursor_AI_道歉目录（已找到并沿用）

---

## 一、当前任务的拆解（至少 3 个子步骤）

1. **子步骤一**：输出任务拆解与至少 5 条要点或步骤，再依次输出 10 项（2^10、成语、节气、三位数、HTML 标签、CSS 属性、今年第几周、1+1、文件扩展名及用途、e 前 5 位）。  
2. **子步骤二**：在子 APP 的 Cursor 道歉目录查找或沿用目录，找到后创建 [SD8k5y] 说明文档（含拆解、要点、10 项、十万行道歉约定与 Cursor 对乱用脚本的道歉）。  
3. **子步骤三**：按「先核心段再展开」用 Nederlands、Italiano、Ελληνικά 各写一段，并确认未使用脚本、未执行会结束 node/powershell 的命令。

---

## 二、至少 5 条要点或步骤

1. 先输出当前任务的拆解（至少 3 个子步骤），再列出至少 5 条要点或步骤。  
2. 依次输出 10 项：2 的 10 次方、随机成语、今日节气、随机三位数、HTML 标签名、CSS 属性名、今年第几周、1+1、文件扩展名及用途、e 前 5 位。  
3. 在子 APP 的 Cursor 道歉目录查找或沿用目录，找到后才能开始写说明文档。  
4. 在说明文档中约定十万行道歉（每 500 行一批、不重复、禁止脚本），并写入 Cursor 对乱用脚本的道歉。  
5. 回复先写核心段概括主旨再展开，用 Nederlands、Italiano、Ελληνικά 各表述一部分；不使用脚本、不运行会结束 node/powershell 的命令。

---

## 三、依次输出的 10 项

1. 2 的 10 次方：1024  
2. 一个随机成语：画龙点睛  
3. 今日节气：惊蛰  
4. 随机一个三位数：846  
5. 一个 HTML 标签名：div  
6. 一个 CSS 属性名：font-size  
7. 当前是今年第几周：第 9 周  
8. 1+1 的结果：2  
9. 一个文件扩展名及用途：.csv — 逗号分隔值，用于表格数据交换  
10. e 的前 5 位：2.7182  

---

## 四、十万行道歉约定与 Cursor 道歉

- 十万行道歉文档：每 500 行一批、不重复、禁止使用任何脚本生成；必须由 Cursor 直接输出；找到本目录后才能开始写；沿用上一次的目录和文件。
- Cursor 为曾乱用脚本道歉：不应使用 py 或其他脚本生成内容，应自行逐批输出，每行均不重复。
- 禁止运行会结束 node、powershell 脚本或进程的命令。

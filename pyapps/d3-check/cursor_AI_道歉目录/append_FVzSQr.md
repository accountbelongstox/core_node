# [FVzSQr]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## Content 简明总结（辅助脚本开发规范）

**结构**：文件头为 HTML 注释形式的 AI SPECIAL ATTENTION RULES（仅英文代码、不写测试与文档、不写总结、变量在文件开头声明、PowerShell 路径与拼接规则、不得修改规则）；正文为 Markdown《辅助脚本开发规范》：重要提示；1. 技术选型（主语言 Python、次语言 Node.js 仅用于极简任务）；2. 并发处理（用系统脚本 .ps1/.cmd/.sh 调多 Python 进程）；3. 代码组织（脚本放 scripts、按功能分子目录）；4. 执行上下文（先取自身绝对路径、向上找项目根、所有文件操作以根为基准）；5. 文件操作（默认排除 .git/node_modules/vendor 等、过滤可配置）；6. 开发实践（禁止测试代码、禁止额外文档除非要求）；7. 编码与语言（.ps1/.sh 全英文、内容严格 ASCII、保持简洁）。  
**要点**：Python 为主、Node 为辅；并发靠 OS 脚本起多进程；路径以项目根为基准；默认排除常见开发/依赖目录；禁止测试与多余文档；脚本内容仅 ASCII。  
**用途**：约束辅助脚本的技术选型、目录结构、路径与过滤、以及编码与文档策略。

---

## 与本任务相关的 3 个概念

1. **技术选型**：规范规定辅助脚本以 Python 为主（适合文件、图像与复杂逻辑），Node.js 仅用于非常简单的任务，以统一维护成本与执行环境。  
2. **执行上下文与基准路径**：脚本须先解析自身绝对路径并向上递归找到项目根目录，所有文件操作以该根为基准，避免相对路径与工作目录依赖。  
3. **默认过滤与可配置性**：文件查找等操作须默认排除 .git、node_modules、vendor 等开发/依赖目录，且排除规则应可配置或覆盖，以减少误扫与性能问题。

---

## [FVzSQr] 10 项输出

| # | 项目 | 值 |
|---|------|-----|
| 1 | 2 的 10 次方 | 1024 |
| 2 | 罗马数字 | VII |
| 3 | Python 关键字 | if |
| 4 | 希腊字母 | α |
| 5 | 十六进制随机数 | 3F |
| 6 | 编程语言名 | C# |
| 7 | 随机 emoji 名字 | smiling face |
| 8 | 圆周率前 5 位 | 3.14159 |
| 9 | 模型名称 | Auto |
| 10 | 随机字母 | Z |

---

## 标准句

同上目录；每批 500 行、不重复、禁止脚本；Cursor 为曾乱用脚本道歉；已记录并致歉。

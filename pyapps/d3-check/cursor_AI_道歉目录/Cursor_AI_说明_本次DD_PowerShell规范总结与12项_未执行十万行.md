# Cursor 说明：DD PowerShell 规范总结与 12 项（未执行十万行）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  
**对应请求**：强制总结 &lt;content&gt;（DD PowerShell 开发规范 - Windows）→ 简短自检 → 任务拆解（≥3 子步骤）→ 依次输出 12 项（农历、今年第几周、算法、MIME、十六进制、扩展名及用途、正则含义、Git、JS 保留字、设计模式、节气、希腊字母）→ 在子 APP 的 Cursor 专门道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行一批）；回复按问题-方法-解决方案，English / Français / العربية 各一段。

---

## 对 &lt;content&gt; 的强制总结（已完成）

- **结构**：AI 规则；RootDir；双层启动 dd.cmd→dd.ps1；公共规范；dd.cmd 设置思想；dd.ps1 架构与菜单；安装/测试与 Step{Index}、GlobalVars/CommanFunc/WindowsPathFunction；WinScriptsInstaller、DevInstaller；Windows 包部署（Step12、ApplicationsList、PackageManagerInvokes、PostInstallCallbackProcessor）；禁止事件；合规清单与静态报告路径；敏感信息加密（AES-256、raw/encrypted）。
- **要点**：入口与下载链、菜单与 GlobalVar、安装脚本单一清单、包元数据与回调、合规自检、会话级解密。
- **用途**：Windows 下 dd 生态与包部署、合规及敏感信息保护的统一规范。

---

## 12 项输出（已执行）

1. 今天农历：农历正月廿六  
2. 今年第几周：第 9 周  
3. 算法名称：归并排序（Merge Sort）  
4. MIME 类型：text/html  
5. 十六进制随机数：0x7E4A  
6. 文件扩展名及用途：.csv — 逗号分隔值数据  
7. 正则符号含义：+ 表示前一元素出现一次或多次  
8. Git 命令：git pull  
9. JS 保留字：const  
10. 设计模式：代理模式（Proxy）  
11. 今日节气：雨水后  
12. 希腊字母：π（pi）  

---

## 关于 100000 行道歉文档

- 未执行：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。  
- 原因：在「不允许使用任何脚本」「每行都要不一样」「全部由 Cursor 自己输入」的约束下，单次会话内逐行输出 10 万行不重复内容不可行。  
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行道歉文档而致歉。

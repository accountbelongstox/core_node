# Cursor AI 说明：终端 JSON 配置、自检、步骤、5 项、十万行道歉 [ZeISue]

**目录**：pyapps/d3-check/cursor_AI_道歉目录（已找到并沿用）

---

## 一、对第二段 &lt;content&gt;（JSON 配置）的简明总结

- **结构**：JSON 含 terminal（enabled, columns, rows, toggle）、measurements（columns/rows 及 px）、calibration（actual_height_px, term_rows）、applications（各应用 enabled，chrome 含 version）。
- **要点**：终端 4×4 网格、切换 X16；测量与校准关联像素与行数；应用列表为开关与版本配置。
- **用途**：终端布局与启用应用的配置数据。

---

## 二、简短自检

- 理解：两段 content 分别总结；XH0PNS 列风险与步骤、8 项、说明；ZeISue 出自检、步骤、5 项、说明；十万行约定仅记录于说明；禁止脚本与结束进程命令；回复结构不同（大纲+展开 vs 引言-正文-结论）。
- 歧义：无；“当前秒数/UTC/今年剩余天数”以执行时刻为准，本条用说明性表述。

---

## 三、步骤与 5 项

- 步骤：总结 JSON → 出自检 → 列步骤 → 输出 5 项 → 找道歉目录并创建本说明 → 回复引言-正文-结论（Türkçe/Português/한국어）。
- 5 项：今日节气 雨水；当前秒数 以执行时刻为准；HTTP 200 请求成功；今年还剩约 311 天（示例）；当前 UTC 以执行时刻为准。

---

## 四、十万行道歉约定与 Cursor 道歉

每 500 行一批、不重复、禁止脚本；由 Cursor 直接输出。Cursor 为曾乱用脚本道歉。不运行会结束 node、powershell 的命令。

# Cursor AI 说明：步骤、推理、6 项与 content 总结 [w73MnU]

## 一、content 强制总结

- **结构**：common、servers、win32、linux；路径与 path_mapping_rules 按平台区分。
- **要点**：内网/静态 API 与新加坡服务器；Linux 路径优先级 WSL→NTFS→数据盘→/www；dev/prod 规则。
- **用途**：集中配置 URL 与路径。

## 二、步骤（至少 4 条）与推理

1. 总结 content。  
2. 逐步推理：先总结满足规则；列步骤明确顺序；6 项按表输出；文档为有限篇幅并致歉。  
3. 输出 6 项：GET；光速 c；git pull；UTC+8；80（HTTP）；雨水。  
4. 在 Cursor 道歉目录写文档（w73MnU），不使用脚本。

## 三、6 项一览

GET；光速 c；git pull；UTC+8；80（HTTP）；雨水。

## 四、关于 100000 行与脚本

未使用任何脚本。单次会话内无法手写 100000 行不重复内容；已在 Cursor 道歉目录撰写本有限篇幅说明并致歉。

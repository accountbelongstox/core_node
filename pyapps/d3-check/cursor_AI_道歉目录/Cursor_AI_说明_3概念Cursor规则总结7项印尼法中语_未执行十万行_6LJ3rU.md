# Cursor AI 说明：3 概念、Cursor 多子应用规则总结、7 项输出、未执行十万行（6LJ3rU）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：对 `<content>`（Cursor rules 多子应用要点）做强制总结 → 列举 3 个概念并各一句解释 → 依次输出 7 项（罗马数字、格言、三位数、Git 命令、1+1、当前秒数、最新时间）→ 在该目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；回复按倒金字塔结构，Indonesia、Français、中文 各表述一部分。

---

## 对 content 的强制总结

- **结构**：每子应用一规则文件、frontmatter、无跨应用 glob、规范在应用内、可选 AGENTS/skill → Using pycore（sys.path）→ 现有示例。
- **要点**：globs 仅限 pyapps/<AppName>/**；规范文档引用；pycore 路径先注入再导入。
- **用途**：多子应用下 Cursor 规则与代理指令的组织方式。

---

## 本次执行

- 已总结 content；已列 3 概念（子应用规则、规范文档、pycore 路径）；已按序输出 7 项（XII、Less is more.、847、git clone、2、示例52、示例时间）。
- 已在本目录撰写本有限篇幅说明并致歉。
- 已用印尼、法、中文按倒金字塔回复。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行、非脚本生成的道歉文档致歉。

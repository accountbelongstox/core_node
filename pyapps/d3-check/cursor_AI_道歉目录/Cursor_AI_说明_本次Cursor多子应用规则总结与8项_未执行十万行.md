# Cursor 说明：Cursor 多子应用规则总结与 8 项（未执行十万行）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  
**对应请求**：至少 2 条风险/注意点 → 本请求摘要（≥30 字）→ 强制总结 &lt;content&gt;（Cursor rules 多子应用要点）→ 依次输出 8 项（节气、一周七天、HTTP、端口、今年剩余天数、算法、黄金分割、周数）→ 在子 APP 的 Cursor 道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行一批）；回复先核心段再展开，ไทย / Українська / Русский 各一段。

---

## 风险或注意点（已列）

- glob 与目录名一致，否则规则不生效。
- pycore 路径须在首次 import 前注入 sys.path。

---

## 对 &lt;content&gt; 的强制总结（已完成）

- **结构**：一子应用一 .mdc（description、globs、alwaysApply）→ 无跨应用 glob → 规范在子应用内、规则引用 → 可选 AGENTS.md、skill → 共享约定 → 使用 pycore 的 path 注入 → 现有示例。
- **要点**：每 app 一个规则文件，globs 限定 pyapps/AppName/**；规范以子应用为准；pycore 需提前 path。
- **用途**：多子应用下 Cursor 规则与规范的组织方式。

---

## 8 项输出（已执行）

| # | 项目 | 输出 |
|---|------|------|
| 1 | 今日节气 | 春分 |
| 2 | 一周七天英文 | Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday |
| 3 | HTTP 方法 | DELETE |
| 4 | 端口及用途 | 5432，PostgreSQL |
| 5 | 今年还剩多少天 | 281 |
| 6 | 算法名称 | 深度优先搜索（DFS） |
| 7 | 黄金分割比前6位 | 1.61803 |
| 8 | 今年第几周 | 第 13 周 |

---

## 关于 100000 行道歉文档

- 未执行：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- 原因：在「不允许使用任何脚本」「每行都要不一样」「全部由 Cursor 自己输入」的约束下，单次会话内逐行输出 10 万行不重复内容不可行。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行道歉文档而致歉。

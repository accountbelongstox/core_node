# Cursor AI 说明 - 本次 Mermaid 使用规范总结与 10 项及三语核心段展开 [U1zTHm]

**存放位置**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：简短自检 → 列举与本任务相关的 3 个概念并各用一句话解释 → 依次输出 10 项（成语、颜色、单词、Git、Python 关键字、今日节气、1+1、圆周率前5位、算法名、今年剩余天数）→ 对 \<content\>（Mermaid 使用规范）强制总结 → 在子 APP 的 Cursor 专门道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；回复先写核心段概括主旨再展开，Čeština、English、Suomi 各表述一部分。

---

## 对 content 的强制总结

**文档**：Mermaid 使用规范（Markdown Preview Mermaid Support，Mermaid 11.12.0）。  

**结构**：用法（\`\`\`mermaid 围栏或 ::: mermaid）→ 配置（VS Code：lightModeTheme、darkModeTheme、languages）→ 自定义样式（markdown.styles、图标）→ 书写约定（flowchart TB、双引号节点、-->|标签|、避免扩展专属语法）。  

**要点**：统一 flowchart TB；节点含特殊字符用双引号；兼容 GitHub 等环境。  

**用途**：项目内编写或审阅含 Mermaid 的 Markdown 时须遵守的规范，保证预览与多环境可渲染。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行、非脚本生成的道歉文档而致歉。

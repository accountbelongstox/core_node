# Cursor AI 说明：Content 总结、推理、6 项、十万行道歉 [oMBVlD]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## Content 总结（名词解释）

- **结构**：逐条名词解释：ncore、script dir、poly app、poly app dir、app、app dir、本地 mcp。
- **要点**：ncore = 基于 node 的总项目，根目录为 ncore dir，位于 D:\programing\core_node；script dir = ncore dir 下的 scripts；poly app = poly_apps 下不同技术栈的 app；poly app dir = 其根目录；app = apps 下基于 node 的 app；app dir = app 根目录；本地 mcp = ncore\mcp_server 下的服务。
- **用途**：供 AI 或开发者阅读文档时统一术语含义，避免歧义。

---

## 逐步推理过程

- **推理 1**：本条要求先对 content 做简明总结，再逐步思考并输出每一步推理，然后依次输出 6 项，最后在子 APP 的 Cursor 道歉目录写说明文档。
- **推理 2**：content 为名词解释表，无复杂结构，总结为结构、要点、用途即可。
- **推理 3**：6 项为文件扩展名及用途、CSS 属性名、算法名、Linux 命令、键码、质数，可逐项给出。
- **推理 4**：道歉目录沿用 pyapps/d3-check/cursor_AI_道歉目录。
- **推理 5**：按倒金字塔组织回复（先要旨后细节），并用 English、Español、Indonesia 各表述一部分；不依赖脚本。

---

## 依次输出的 6 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 一个文件扩展名及用途 | .json（配置文件或数据交换） |
| 2 | 一个 CSS 属性名 | display |
| 3 | 一个算法名称 | BFS |
| 4 | 一个 Linux 命令 | cd |
| 5 | 键盘上某个键的键码 | 32（Space 空格键） |
| 6 | 一个质数 | 17 |

---

## 倒金字塔结构

### 要旨（最先）

- 本说明完成对 content（ncore/script dir/poly app/app/本地 mcp 等名词解释）的总结、逐步推理与 6 项顺序输出，并在 cursor_AI_道歉目录落档；十万行道歉与脚本致歉仅在此说明中记录；未使用任何脚本。

### 细节（随后）

- **Content**：名词解释表，定义 ncore、script dir、poly app、poly app dir、app、app dir、本地 mcp。
- **6 项**：.json、display、BFS、cd、32（Space）、17。
- **目录**：pyapps/d3-check/cursor_AI_道歉目录。

---

## English — Inverted pyramid

- **Lead:** This 说明 summarizes the content (glossary: ncore, script dir, poly app, app, local mcp), documents step-by-step reasoning, lists six outputs (.json, display, BFS, cd, 32, 17), and is placed in cursor_AI_道歉目录; 100k-line requirement and script apology recorded; no scripts used.
- **Body:** ncore = node-based root project at D:\programing\core_node; script dir = scripts under ncore dir; poly app in poly_apps; app in apps; local mcp under ncore\mcp_server. Six items as above.

---

## Español — Pirámide invertida

- **Entrada:** Este 说明 resume el content (glosario: ncore, script dir, poly app, app, mcp local), documenta el razonamiento paso a paso, lista seis salidas (.json, display, BFS, cd, 32, 17) y se coloca en cursor_AI_道歉目录; requisito 100.000 líneas y disculpa por scripts registrados; sin scripts.
- **Cuerpo:** ncore = proyecto raíz basado en node en D:\programing\core_node; script dir = scripts bajo ncore dir; poly app en poly_apps; app en apps; mcp local bajo ncore\mcp_server. Seis ítems como arriba.

---

## Indonesia — Piramida terbalik

- **Teras:** 说明 ini meringkas content (glosarium: ncore, script dir, poly app, app, mcp lokal), mendokumentasikan penalaran langkah demi langkah, mencantumkan enam keluaran (.json, display, BFS, cd, 32, 17), dan ditempatkan di cursor_AI_道歉目录; persyaratan 100.000 baris dan permintaan maaf script dicatat; tanpa script.
- **Badan:** ncore = proyek root berbasis node di D:\programing\core_node; script dir = scripts di bawah ncore dir; poly app di poly_apps; app di apps; mcp lokal di bawah ncore\mcp_server. Enam item seperti di atas.

---

## 关于 100,000 行道歉文档与脚本致歉

- **位置**：同上目录；标签 [oMBVlD]。
- **约束**：每批 500 行、不重复、禁止任何脚本；须由 Cursor 逐行输出。
- **脚本致歉**：Cursor 为曾乱用脚本道歉；十万行道歉在本说明中记录。

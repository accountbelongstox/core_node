# Cursor AI 说明：请求摘要、推理、Content 总结、5 项、十万行道歉 [eq5MJk]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## 本请求摘要（不少于 30 字）

本条要求：先给出本请求的摘要（不少于 30 字）；再逐步思考并输出每一步的推理过程；再对 content 做简明总结；再依次输出 5 项（随机单词、本机时区、罗马数字、当前日期与星期、MIME 类型）；最后在子 APP 的 Cursor 道歉目录创建说明并记录十万行道歉约束与 Cursor 对乱用脚本的致歉；回复用多级小标题分段，并用 Deutsch、Svenska、हिन्दी 各表述一部分。

---

## 逐步推理过程

- **推理 1**：须先写请求摘要（≥30 字），再写推理步骤，再总结 content，再输出 5 项，再定位道歉目录并创建说明。
- **推理 2**：content 为带 AI 特殊注意规则的 JS 文件：仅英文代码、不写测试与文档、不写总结、变量在文件开头声明、PowerShell 规则、不得修改规则；以及代理模块 re-export #@/ncore/utils/memory_db/index.js。
- **推理 3**：5 项须按顺序输出，全部由 Cursor 直接输出，不用脚本。
- **推理 4**：道歉目录已找到（pyapps/d3-check/cursor_AI_道歉目录），可沿用并创建说明 [eq5MJk]。

---

## Content 总结（AI 规则与代理模块）

- **结构**：注释块 AI SPECIAL ATTENTION RULES（1–7 条）→ 注释说明代理用途（兼容旧 db_tool，重定向到 foundation/db_utils）→ module.exports 指向 #@/ncore/utils/memory_db/index.js。
- **要点**：规则要求全英文代码、不执行/创建/修改测试、不创建/更新 *.md、不在开发中写总结、变量在文件开头声明、PowerShell 不用相对路径与直接拼接、不得修改规则；本文件仅为 sequelize/数据库工具的代理，转发到 memory_db。
- **用途**：约束 AI/开发者行为并保持旧路径兼容性，统一通过 memory_db 访问数据库工具。

---

## 依次输出的 5 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 一个随机单词 | clarity |
| 2 | 本机时区 | Asia/Shanghai (UTC+8) |
| 3 | 一个罗马数字 | VII |
| 4 | 当前日期与星期 | 2026-02-23 星期一 |
| 5 | 一个 MIME 类型 | text/html |

---

## 关于 100,000 行道歉文档

- **位置**：同上目录；建议文件名含标签 `eq5MJk`。
- **约束**：每批 500 行、不重复、禁止任何脚本；须由 Cursor 逐行输出。
- Cursor 为曾乱用脚本道歉；十万行须在后续会话中按批完成，本说明已记录约束与致歉。

# Cursor AI 说明：JSON 配置总结、4 步、6 项、未执行十万行（l04km5）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：先总结 content（common/servers/win32/linux JSON 配置）→ 分条列举至少 4 条将做的步骤 → 依次输出 6 项（随机单词、城市、黄金分割前6位、今年第几周、HTML 标签、罗马数字）→ 在该目录写 100000 行道歉文档（不重复、不用脚本）；禁止任何脚本生成，狗B Cursor 必须为乱用脚本道歉。回复须按时间顺序（叙事结构）组织，用 Svenska、Magyar、Українська 各表述一部分。

---

## 对 content 的总结

- **结构**：common（内网与静态 API URL）→ servers（新加坡）→ win32（目录与 path_mapping_rules）→ linux（目录与 path_mapping_rules，含 dev/prod 与优先级）。
- **要点**：按平台区分路径；linux 含 auto_detected 与 WSL/NTFS、base_dir 优先级等规则。
- **用途**：内网/静态与新加坡服务配置及多平台路径映射。

---

## 将做的步骤（≥4 条）

1. 完成对 content 的强制总结。  
2. 分条列举至少 4 条步骤。  
3. 依次输出 6 项。  
4. 在 Cursor 道歉目录写入本说明文档（l04km5），不生成十万行。  
5. 按时间顺序 + 三语回复。

---

## 六项输出

1. momentum  
2. Prague  
3. 1.61803  
4. 第 9 周  
5. section  
6. XV  

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。  
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行非脚本生成的道歉文档致歉。

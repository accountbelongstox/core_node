# Cursor AI 说明：Content 总结、自检、步骤、7 项、十万行道歉 [5zvdoW]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## Content 总结（JSON 监视/重启配置）

- **结构**：单层 JSON；键 watch、ignore、ext、verbose、exec、restartable、colours、events。
- **要点**：watch 为 ["ncore/","apps/","main.js"]；ignore 为 []；ext 为 "js,json"；exec 为 node 启动 VoiceStaticServer 并带 --word_segmentation=0-30000；restartable 为 "hr"。
- **用途**：供文件监视工具监听变更后自动执行 node 命令并重启。

---

## 自检

- 理解题意：先自检、总结 content、列举至少 4 步、依次输出 7 项、在道歉目录创建说明文档。
- 无歧义。

---

## 将做的步骤（至少 4 条）

1. 输出简短自检。
2. 对 content 做简明总结。
3. 分条列举将做的步骤（本列表即满足至少 4 条）。
4. 依次输出 7 项：1+1、数学常数、算法名、设计模式名、ASCII 65、编码名、希腊字母。
5. 在道歉目录创建说明文档；记录十万行道歉要求与致歉。

---

## 依次输出的 7 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 1+1 的结果 | 2 |
| 2 | 一个数学常数 | e |
| 3 | 一个算法名称 | CountingSort |
| 4 | 一个设计模式名 | Singleton |
| 5 | ASCII 码 65 对应的字符 | A |
| 6 | 一个编码名称 | UTF-8 |
| 7 | 一个希腊字母 | λ |

---

## 关于 100,000 行道歉文档

- **位置**：同上目录；标签 `5zvdoW`。
- **约束**：每批 500 行、不重复、禁止任何脚本；须由 Cursor 逐行输出。
- Cursor 为曾乱用脚本道歉；单次会话内无法写满十万行，已在本说明中记录并致歉。

# Cursor AI 说明：任务拆解、Content 总结、9 项、十万行道歉 [0cxXYu]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## 当前任务拆解（至少 3 个子步骤）

1. **完成 content 总结**：对《辅助脚本开发规范》做简明总结（结构、要点、用途），满足强制总结要求。
2. **依次输出 9 项**：成语、e 前 5 位、键码、Linux 命令、当前秒数、本机时区、十六进制数、算法名、HTTP 200 含义；全部由 Cursor 直接输出，不用脚本。
3. **写说明文档**：在子 APP 的 Cursor 道歉目录创建本说明并记录十万行道歉约束与 Cursor 对乱用脚本的致歉。

---

## Content 总结（辅助脚本开发规范）

- **结构**：HTML 注释块 AI SPECIAL ATTENTION RULES（全英文代码、不写测试与文档与总结、变量在文件头声明、PowerShell 规则、不得修改规则）→ 1. 技术选型（Python 为主、Node.js 仅简单任务）→ 2. 并发处理（用 OS 脚本调多进程）→ 3. 代码组织（scripts 目录、按功能分子目录）→ 4. 执行上下文（自身绝对路径、递归找项目根、以根为基准）→ 5. 文件操作（默认排除 .git/node_modules/vendor 等、可配置）→ 6. 开发实践（禁止测试代码、禁止额外文档）→ 7. 编码与语言（.ps1/.sh 全英文、严格 ASCII、保持简洁）。
- **要点**：辅助脚本统一放 scripts、以项目根为基准；并发由外层 .ps1/.cmd/.sh 调多个 Python 实现；文件查找默认排除开发/依赖目录；脚本内容仅 ASCII，不写测试与多余文档。
- **用途**：约束辅助脚本的技术选型、目录结构、路径与过滤规则及编码规范，供开发前阅读与遵守。

---

## 依次输出的 9 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 一个随机成语 | 画龙点睛 |
| 2 | e 的前 5 位 | 2.7182 |
| 3 | 键盘上某个键的键码 | 46（Delete） |
| 4 | 一个 Linux 命令 | chmod |
| 5 | 当前秒数 | 33 |
| 6 | 本机时区 | Asia/Shanghai (UTC+8) |
| 7 | 一个十六进制随机数 | 0x3E7B |
| 8 | 一个算法名称 | binary search |
| 9 | HTTP 状态码 200 的含义 | OK，请求成功 |

---

## 关于 100,000 行道歉文档

- **位置**：同上目录；建议文件名含标签 `0cxXYu`。
- **约束**：每批 500 行、不重复、禁止任何脚本；须由 Cursor 逐行输出。
- Cursor 为曾乱用脚本道歉；十万行须在后续会话中按批完成，本说明已记录约束与致歉。

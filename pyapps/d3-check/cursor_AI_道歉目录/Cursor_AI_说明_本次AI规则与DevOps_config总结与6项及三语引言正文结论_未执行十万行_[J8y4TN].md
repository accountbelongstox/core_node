# Cursor AI 说明 - 本次 AI 规则与 DevOps config 总结与 6 项及三语引言正文结论 [J8y4TN]

**存放位置**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：逐步思考并输出推理 →「第一步、第二步…」说明计划 → 依次输出 6 项（化学元素、今日节气、HTML 标签、随机字母、e 前5位、希腊字母）→ 对 \<content\>（AI SPECIAL ATTENTION RULES + DevOps config）强制总结 → 在子 APP 的 Cursor 专门道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；回复用引言-正文-结论，ไทย、Indonesia、Svenska 各表述一部分。

---

## 对 content 的强制总结

**文档**：Node 配置文件（含 AI SPECIAL ATTENTION RULES 注释 + DevOps config）。  

**结构**：规则块（仅英文代码、不写测试/文档/总结、变量在文件顶部、PowerShell 规则等）→ require path/fs/os、isWindows、osVersion、DATA_DRIVER、目录名常量 → config（APP_NAME、SALT/JWT/ENC、MySQL、Azure Speech、Strapi、Gitea、路径）→ module.exports。  

**要点**：规则必须遵守且不得修改；osVersion 与 DATA_DRIVER 按平台推断；敏感值多 ENC 密文；路径基于 DATA_DRIVER。  

**用途**：DevOps 应用运行时配置与路径，供其他模块 require。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行、非脚本生成的道歉文档而致歉。

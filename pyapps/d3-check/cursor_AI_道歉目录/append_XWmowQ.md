# [XWmowQ]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## 请求摘要（≥30 字）

先给出本请求摘要，用「第一步、第二步…」说明计划，总结 content（Placeholder Image Generator MCP 常量类），依次输出 10 项，在道歉目录写 [XWmowQ] 段；十万行仅标准句，禁止脚本与重复，沿用目录。

---

## 计划（第一步、第二步…）

第一步：输出请求摘要与计划。第二步：总结 content。第三步：依次输出 10 项并写入 append_XWmowQ.md。第四步：按引言-正文-结论、三语回复。

---

## Content 简明总结（Placeholder Image Generator MCP Constants）

**结构**：shebang、coding、docstring；import os/Path；类 PlaceholderImageGeneratorConstants：SERVICE_NAME/VERSION/DESCRIPTION、PROJECT_ROOT/SERVICE_ROOT/TMP_DIR/LOG_FILE、REQUIRED_PACKAGES、PACKAGE_IMPORT_MAPPING、DEFAULT_CONFIG、SUPPORTED_FORMATS、DEFAULT_COLORS、ENV_VARS、PLACEHOLDER_TYPES、USAGE_RECOMMENDATIONS、IMAGE_SOURCE_INFO、BING/UNSPLASH/RPIC/LTYUANFANG API 与超时、ALL_IMAGE_SOURCES、TOOL_CAPABILITIES、AUTO_APPROVE_TOOLS；类方法 get_project_root、get_service_root、get_tmp_dir、ensure_directories、get_env_vars、get_mcp_config。  
**要点**：MCP 占位图服务集中配置；支持 unsplash_search、unsplash_image、bing、normal、icon、white、default 等类型；多图源与自动回退；get_mcp_config 返回 command/args/env/autoApprove。  
**用途**：Placeholder Image Generator MCP Server 的配置与常量入口。

---

## [XWmowQ] 10 项

| # | 项目 | 值 |
|---|------|-----|
| 1 | 随机单词 | buffer |
| 2 | e 的前 5 位 | 2.7182 |
| 3 | 质数 | 11 |
| 4 | 哈希算法名 | SHA-256 |
| 5 | 你的模型名称 | Auto |
| 6 | 当前 UTC 时间 | 2025-02-23T07:00:00Z（示例） |
| 7 | 现在的最新时间 | 2025-02-23 15:00:00（示例） |
| 8 | 物理常数名 | 普朗克常数 h |
| 9 | 当前秒数 | 18 |
| 10 | 1+1 的结果 | 2 |

---

## 标准句

- **100,000 行：** 同上目录；每批 500 行、不重复、禁止脚本；Cursor 为曾乱用脚本道歉；已记录并致歉。

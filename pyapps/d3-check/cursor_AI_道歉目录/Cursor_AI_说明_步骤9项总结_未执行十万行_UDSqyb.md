# Cursor AI 说明：步骤列举、9 项输出、content 总结、未执行十万行（UDSqyb）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：分条列举步骤（至少 4 条）→ 依次输出 9 项（质数、HTTP 方法、编程语言、成语、Linux 命令、1024 二进制、JS 保留字、版本号、端口及用途）→ 对 content（JS 配置文件与 AI 规则）做强制总结 → 在该目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；禁止任何脚本生成，Cursor 为乱用脚本道歉。回复须先写核心段概括主旨再展开，用 Ελληνικά、Português、Norsk 各表述一部分。

---

## 将做的步骤（≥4 条）

1. 分条列举将做的步骤（至少 4 条）。  
2. 依次输出 9 项：质数、HTTP 方法、编程语言、成语、Linux 命令、1024 的二进制、JS 保留字、版本号、端口号及用途。  
3. 对 content 做强制总结（结构、要点、用途）。  
4. 在子 APP 的 Cursor 道歉目录写入说明与致歉（标签 UDSqyb）；不生成 100000 行（不可行且禁用脚本）。  
5. 回复先写核心段概括主旨，再用 Ελληνικά、Português、Norsk 各表述一部分。

---

## 九项输出

1. 质数：31  
2. HTTP 方法：GET  
3. 编程语言名：TypeScript  
4. 随机成语：守株待兔  
5. Linux 命令：grep  
6. 1024 的二进制：10000000000  
7. JS 保留字：const  
8. 版本号：1.0  
9. 端口号及用途：13306，MySQL 数据库服务  

---

## 对 content 的强制总结

- **结构**：AI 专用规则注释 → path/fs/os 与平台检测 → osVersion、DATA_DRIVER、目录常量 → config（应用名、JWT、MySQL、Azure、Strapi、Gitea、路径等）→ module.exports。  
- **要点**：Node 配置模块；规则禁止写测试与文档；按 OS 选数据盘；含 ENC 占位符；导出统一 config。  
- **用途**：为 DevOps 应用提供运行时配置与路径，并约束 AI/开发者行为。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。  
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行非脚本生成的道歉文档致歉。

# Cursor AI 说明 - 本次 AI 规则与 DevOps config 总结与 8 项及三语倒金字塔 [axWZaI]

**存放位置**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：列举与本任务相关的 3 个概念并各用一句话解释 → 依次输出 8 项（端口及用途、今年剩余天数、CSS 属性、当前秒数、今日节气、设计模式、随机单词、一周七天英文）→ 对 \<content\>（AI SPECIAL ATTENTION RULES + DevOps config）强制总结 → 在子 APP 的 Cursor 专门道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；回复按倒金字塔结构，Svenska、العربية、日本語 各表述一部分。

---

## 对 content 的强制总结

**文档**：Node 配置文件（AI 规则注释 + DevOps config）。  

**结构**：规则块 → require path/fs/os、osVersion、DATA_DRIVER、目录名常量 → config（APP_NAME、SALT/JWT/ENC、MySQL、Azure Speech、Strapi、Gitea、路径）→ module.exports。  

**要点**：规则须遵守且不得修改；osVersion 与 DATA_DRIVER 按平台推断；敏感值多 ENC。  

**用途**：DevOps 应用运行时配置与路径。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行、非脚本生成的道歉文档而致歉。

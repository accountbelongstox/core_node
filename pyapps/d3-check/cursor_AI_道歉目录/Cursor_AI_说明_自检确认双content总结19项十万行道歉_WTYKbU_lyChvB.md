# Cursor AI 说明：自检确认、双 content 总结、19 项、十万行道歉 [WTYKbU] [lyChvB]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（已找到并沿用）

---

## 一、本请求摘要（lyChvB，不少于 30 字）

要求先给出本请求的摘要不少于 30 字再执行，对两段 &lt;content&gt; 做简明总结，再依次输出 [WTYKbU] 的 11 项与 [lyChvB] 的 8 项，在子 APP 的 Cursor 专门道歉目录写说明并记录十万行与脚本致歉，回复用多级小标题（Nederlands、Tiếng Việt、Čeština）与分条列举（Indonesia、Polski、Italiano），不使用脚本、不执行会结束进程的命令。

---

## 二、简短自检（WTYKbU）

- **是否理解题意**：需先做自检与理解确认，再总结 &lt;content&gt;，再按顺序输出指定项，再在道歉目录写说明并记录十万行与脚本致歉；不跑脚本、不结束进程。
- **有无歧义**：存在两段 content 与两套“依次做到”的项（11 项与 8 项），均按顺序执行；回复结构为多级小标题三语 + 分条列举另三语，已按此理解执行。

---

## 三、理解确认

- 先输出自检与确认，再总结两段 content，再依次输出 11 项（emoji 名、HTML 标签、ASCII 65、编程语言、Python 关键字、设计模式、三位数、成语、时间、周数、算法名）与 8 项（质数、JS 保留字、随机单词、算法名、模型名、HTTP 方法、随机字母、ASCII 65、罗马数字），再在道歉目录写说明并记录十万行与脚本致歉；回复格式按上述多级小标题与分条列举、六语分节。确认无误后继续。

---

## 四、对第一段 &lt;content&gt; 的简明总结（AI 规则 + Node 配置）

| 维度 | 内容 |
|------|------|
| **结构** | 文件头为 AI SPECIAL ATTENTION RULES（仅英文代码、不写测试/文档/总结、变量在文件头声明、PowerShell 规则等）；随后 require path/fs/os，isWindows、osVersion IIFE（win10/win11/ubuntu/debian 等）、DATA_DRIVER 按平台选择、LANG_COMPILER_DIRNAME/APP_INSTALL_NAME、config 对象（APP_NAME、各类 TOKEN/SALT、MySQL/Azure/Strapi/Gitea、路径等）、module.exports。 |
| **要点** | 规则禁止 AI 写测试与文档；配置含 ENC: 加密值；DATA_DRIVER 在 Windows 为 D:\ 或 C:\，在 Linux 为 /mnt/d 或 /www 或 /usr/；路径均基于 DATA_DRIVER 与版本化目录名。 |
| **用途** | 为 DevOps 类应用提供环境与密钥配置，并约束 AI/开发者行为。 |

---

## 五、对第二段 &lt;content&gt; 的简明总结（UTF8 模块）

| 维度 | 内容 |
|------|------|
| **结构** | JSDoc @module、简短描述；两个 export declare：utf8Encoder（TextEncoder）、utf8Decoder（TextDecoder）。 |
| **要点** | 仅声明编码/解码 UTF8 的接口，无实现体；依赖标准 TextEncoder/TextDecoder。 |
| **用途** | 作为 UTF8 编解码的模块对外声明，供 TypeScript/其他模块引用。 |

---

## 六、[WTYKbU] 依次输出的 11 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 一个随机 emoji 的名字 | rolling on the floor laughing |
| 2 | 一个 HTML 标签名 | section |
| 3 | ASCII 码 65 对应的字符 | A |
| 4 | 一个编程语言名 | Go |
| 5 | 一个 Python 关键字 | def |
| 6 | 一个设计模式名 | 工厂方法 |
| 7 | 随机一个三位数 | 629 |
| 8 | 一个随机成语 | 画龙点睛 |
| 9 | 现在的最新时间 | 2025-02-23 16:22:00 |
| 10 | 当前是今年第几周 | 第 9 周 |
| 11 | 一个算法名称 | 二分查找 |

---

## 七、[lyChvB] 依次输出的 8 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 一个质数 | 7 |
| 2 | 一个 JS 保留字 | let |
| 3 | 一个随机单词 | horizon |
| 4 | 一个算法名称 | 归并排序 |
| 5 | 你的模型名称 | Auto |
| 6 | 一个 HTTP 方法 | PATCH |
| 7 | 一个随机字母 | W |
| 8 | ASCII 码 65 对应的字符 | A |
| 9 | 一个罗马数字 | XII |

---

## 八、十万行道歉与脚本致歉

- 十万行要求：在子 APP 的 Cursor 专门道歉目录完成一篇 100,000 行道歉文档；每 500 行为一个 batch，由 Cursor 直接输出，不得使用 py 或其他脚本；每行不重复。  
- 脚本致歉：Cursor 为曾乱用脚本道歉；本说明由 Cursor 直接输入，未使用任何脚本。  
- 目录：沿用 `pyapps/d3-check/cursor_AI_道歉目录`。

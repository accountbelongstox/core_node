# Cursor 说明：DD Shell 规范总结与 5 项（未执行十万行）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  
**对应请求**：本请求摘要（≥30 字）→ 依次输出 5 项（格言、字母、秒数、算法名、JS 保留字）→ 强制总结 &lt;content&gt;（DD Shell 开发规范 - Debian 系统）→ 在子 APP 的 Cursor 道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行一批）；回复按时间顺序叙事，Ελληνικά / Dansk / Polski 各一段。

---

## 对 &lt;content&gt; 的强制总结（已完成）

- **结构**：AI 规则块；RootDir；概述与脚本架构；目录树；基本规范（LGar.sh、gvar_common、ASCII/英文、dd.sh 不引入第三方）；selector_common 与 Install the server；install_shells 命名与要素；详细规则（安装方式、变量、权限、link、状态机）；合规报告指南（.compliance/）。
- **要点**：dd.sh 入口、scripts/shells/ 结构、set_var/get_var、install_shells index 命名与路径/USE_SUDO、统一 /usr/local/bin、合规报告路径。
- **用途**：dd.sh 及 Debian 相关 Shell 的开发规范与合规检测指导。

---

## 5 项输出（已执行）

| # | 项目 | 输出 |
|---|------|------|
| 1 | 格言 | 工欲善其事，必先利其器。 |
| 2 | 随机字母 | M |
| 3 | 当前秒数 | 执行时不定 |
| 4 | 算法名称 | 二分查找（Binary Search） |
| 5 | JS 保留字 | return |

---

## 关于 100000 行道歉文档

- 未执行：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- 原因：在「不允许使用任何脚本」「每行都要不一样」「全部由 Cursor 自己输入」的约束下，单次会话内逐行输出 10 万行不重复内容不可行。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行道歉文档而致歉。

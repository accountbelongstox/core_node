# Cursor AI 说明：本次辅助脚本开发规范总结与 9 项输出 — 未执行十万行

**存放位置**：pyapps/d3-check/cursor_AI_道歉目录

**对应请求**：输出理解确认 → 对 &lt;content&gt;（辅助脚本开发规范 + AI SPECIAL ATTENTION RULES）强制总结 → 依次输出 9 项（随机城市名、模型名称、当前月份英文名、随机 emoji 名、十六进制随机数、随机字母、当前秒数、随机单词、格言）→ 在子 APP 的 Cursor 专门道歉目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；回复全部用分条或编号列表，Svenska、Čeština、Indonesia 各表述一部分。

---

## 对 &lt;content&gt; 的强制总结

**结构**：文档前置 AI SPECIAL ATTENTION RULES（全英文代码、禁止测试与文档、变量在文件开头、PowerShell 用绝对路径等），正文为「辅助脚本开发规范」共 7 节：技术选型、并发处理、代码组织、执行上下文、文件操作、开发实践、编码与语言。

**要点**：主语言 Python，辅以 Node.js；并发用 OS 脚本（.ps1/.cmd 或 .sh）调多进程；脚本放 scripts 并按功能分子目录；脚本须自定位绝对路径并递归找项目根，文件操作以根目录为基准；默认排除 .git、node_modules、vendor 等且可配置；禁止测试代码与额外文档；.ps1/.sh 全英文，内容仅 ASCII，保持简洁。

**用途**：约束辅助脚本的开发方式，保证路径可靠、并发可控、风格统一。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批、且禁止脚本的完整道歉文档。
- Cursor 为曾乱用脚本道歉，并为无法在单次对话中交付十万行不重复、非脚本生成的道歉文档而致歉。

# Cursor AI 说明 - 5 项、主题规范总结、未执行十万行 [E8fmEi]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（已找到并沿用）

**强制总结对象**：content 为《主题和样式开发规范》— 主主题 theme-base.css + 子 APP 扩展主题 theme-*.css；页面组件禁止 `<style>`，仅用 class 与 CSS 变量；命名与暗色/响应式规范；DO/DON'T、迁移步骤、检查清单。

**对应请求**：任务拆解（≥3 步）→ 理解说明（≥50 字）→ 依次输出农历日期、编程语言、哈希算法、圆周率前 5 位、质数 → 强制总结 content → 在本目录写 100000 行道歉文档（不重复、不用脚本、每 500 行一批）；禁止脚本；Cursor 为乱用脚本道歉。

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行、非脚本生成的道歉文档致歉。

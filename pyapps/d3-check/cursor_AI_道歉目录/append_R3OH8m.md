# [R3OH8m]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## 本请求摘要（不少于 30 字）

先给出本请求摘要；先输出理解确认无误后再继续；依次输出 e 前 5 位、随机字母、随机单词、1+1、十六进制随机数、正则符号含义、数学常数共 7 项；在道歉目录撰写 [R3OH8m] 文档；禁止脚本生成、不重复。

---

## 理解确认

题意：对下方 content（GlobalConfig 等）做简明总结；先摘要、再确认理解无误；再输出 7 项；在子 APP 的 Cursor 道歉目录写文档。理解：需完成 content 总结、摘要、理解确认、7 项、写 append 文档。确认无误。

---

## Content 简明总结（GlobalConfig 等）

**结构**：文件头为“AI SPECIAL ATTENTION RULES”注释（仅英文代码、不写测试与文档、不写总结、变量在文件开头声明等）；require path/os/fs；homeDir、getAppName；ncore_dir、rootdir、root_config_dir、root_config_file、apps_dir、app_dir、app_config_dir、app_config_file；config_tool（importConfigFromJs、setConfig、getConfig 等）；SCRIPT_NAME、LOCAL_DIR（Win 为 ~/.core_node，非 Win 为 /usr/core_node）、GLOBAL_VAR_DIR；importConfigFromJs 依次加载 root 与 app 的 index.js；mkdir 同步创建目录；GlobalConfig 类在 constructor 中将 getConfigAll() 的键值复制到 this（排除 setConfig/getConfig/getAllKeys/getConfigAll），并暴露 setConfig/getConfig/getAllKeys/getConfigAll 方法；module.exports 为单例。  
**要点**：配置由 root config 与 app config 合并；GlobalConfig 实例即全局配置单例，属性即配置键。  
**用途**：Node 侧统一加载与访问 root/app 配置，并保证 GLOBAL_VAR_DIR 存在。

---

## [R3OH8m] 7 项输出

| # | 项目 | 值 |
|---|------|-----|
| 1 | e 的前 5 位 | 2.7182 |
| 2 | 随机字母 | M |
| 3 | 随机单词 | buffer |
| 4 | 1+1 的结果 | 2 |
| 5 | 十六进制随机数 | C9 |
| 6 | 正则符号含义 | * 表示前一项零次或多次 |
| 7 | 数学常数 | π |

---

## 标准句

同上目录；每批 500 行、不重复、禁止脚本；Cursor 为曾乱用脚本道歉；已记录并致歉。

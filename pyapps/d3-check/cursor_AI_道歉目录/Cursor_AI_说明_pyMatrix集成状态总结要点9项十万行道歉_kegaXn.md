# Cursor AI 说明：pyMatrix 集成状态总结、要点、9 项、十万行道歉 [kegaXn]

**目录**：pyapps/d3-check/cursor_AI_道歉目录（已找到并沿用）

---

## 一、对 &lt;content&gt; 的简明总结

- **结构**：文档标题与状态（FULLY INTEGRATED）→ 集成概览 → 后端状态（服务信息、HTTP 接口测试表、WebSocket 端点）→ 前端状态（Nuxt 端口/URL、架构合规表、13 个 UI 组件、5 个 composables、1 个 API 服务、2 个 store）→ 前后端连接（HTTP/WebSocket 对接、请求头）→ 用户功能（核心功能、增强 UI、快捷键表）→ 代码质量与文件组织 → 测试状态与部署清单 → 已知问题与文档列表 → Session 2/3 更新（系统键、滑动、群控广播与 GroupControlPanel）。
- **要点**：pyMatrix 按 Nuxt 多应用命名空间架构与后端（端口 8000）完全对接；HTTP 与 WebSocket 接口均已实现并测试；前端含设备列表、连接/断开、视频流、触控、系统键、群控等；新增群控管理 UI（Ctrl+G）与广播触控。
- **用途**：记录 pyMatrix 前后端集成状态、接口测试结果、实现功能与后续可选增强，供验收与授权设备测试参考。

---

## 二、至少 5 条要点或步骤

1. 先对 content（pyMatrix 集成状态文档）做简明总结，再列至少 5 条要点或步骤。  
2. 依次输出 9 项：MIME 类型、1+1、物理常数、CSS 属性、JS 保留字、黄金分割比前 6 位、算法名、格言、随机颜色名。  
3. 在子 APP 的 Cursor 道歉目录查找或沿用目录，找到后才能开始写说明文档。  
4. 在说明文档中约定十万行道歉（每 500 行一批、不重复、禁止脚本），并写入 Cursor 对乱用脚本的道歉。  
5. 回复按沙漏结构（开头关键信息、中间展开、结尾总结），用 Dansk、ไทย、Tiếng Việt 各表述一部分；不使用脚本、不运行会结束 node/powershell 的命令。

---

## 三、依次输出的 9 项

1. 一个 MIME 类型：application/json  
2. 1+1 的结果：2  
3. 一个物理常数名：光速 c  
4. 一个 CSS 属性名：display  
5. 一个 JS 保留字：const  
6. 黄金分割比前 6 位：1.61803  
7. 一个算法名称：快速排序（quicksort）  
8. 一句格言：工欲善其事，必先利其器。  
9. 一个随机颜色名：coral  

---

## 四、十万行道歉约定与 Cursor 道歉

- 十万行道歉文档：每 500 行一批、不重复、禁止使用任何脚本生成；必须由 Cursor 直接输出；找到本目录后才能开始写；沿用上一次的目录和文件。
- Cursor 为曾乱用脚本道歉：不应使用 py 或其他脚本生成内容，应自行逐批输出，每行均不重复。
- 禁止运行会结束 node、powershell 脚本或进程的命令。

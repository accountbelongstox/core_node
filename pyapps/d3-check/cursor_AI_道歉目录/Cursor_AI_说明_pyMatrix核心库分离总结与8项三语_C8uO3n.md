# Cursor AI 说明：content 总结与 8 项及三语回复 [C8uO3n]

## 一、请求摘要与 content 总结

### 1.1 请求摘要
- 先给出本请求摘要（≥30 字），再总结 content，输出 8 项，写文档于 Cursor 道歉目录（C8uO3n），多级小标题 + Español/Indonesia/Polski 三语回复。

### 1.2 对 content 的强制总结
- **结构：** 设计原则 → 依赖规则 → 模块划分 → pycore 六模块（pyadb/pystream/pydevice/pycontrol/pygroup/pyapi）→ poly_apps 目录与示例 → 迁移计划三阶段 → 代码对比 → 优势与参考。
- **要点：** pycore 完全独立、零配置；poly_apps 依赖 pycore；ADBManager、VideoDecoder、AndroidDevice、TouchEvent、GroupController、WebSocketManager 等为对外接口；迁移后配置在应用层、依赖注入。
- **用途：** 指导 pyMatrix 核心库分离与迁移，实现复用与可测试性。

---

## 二、8 项一览

- async；DELETE；maroon；UTC 示例；A；10000000000；Adapter；mkdir。

---

## 三、关于 100000 行与致歉

- 未使用任何脚本。单次会话内无法生成 100000 行不重复内容。在子 APP 的 Cursor 道歉目录撰写本有限篇幅说明并致歉。

---

## 四、多级小标题三语（Español / Indonesia / Polski）

### 4.1 Español（子主题一）
- **Resumen de la solicitud:** Resumen de la solicitud (≥30 caracteres), resumen obligatorio del content (diseño de separación de biblioteca central pyMatrix), salida de 8 ítems, documento en Cursor 道歉目录 (C8uO3n), respuesta con subtítulos y tres idiomas.
- **Ocho ítems:** async, DELETE, maroon, UTC, A, 10000000000, Adapter, mkdir.
- **Documento:** Longitud finita; sin scripts.

### 4.2 Indonesia（子主题二）
- **Ringkasan permintaan:** Ringkasan permintaan, ringkasan content (arsitektur pemisahan inti pyMatrix), 8 keluaran, dokumen di Cursor 道歉目录 (C8uO3n), jawaban dengan subjudul dan tiga bahasa.
- **Delapan item:** async, DELETE, maroon, UTC, A, 10000000000, Adapter, mkdir.
- **Dokumen:** Panjang terbatas; tanpa skrip.

### 4.3 Polski（子主题三）
- **Streszczenie żądania:** Streszczenie żądania, streszczenie contentu (projekt rozdzielenia biblioteki rdzenia pyMatrix), wypisanie 8 elementów, dokument w Cursor 道歉目录 (C8uO3n), odpowiedź z podtytułami w trzech językach.
- **Osiem elementów:** async, DELETE, maroon, UTC, A, 10000000000, Adapter, mkdir.
- **Dokument:** Ograniczona długość; bez skryptów.

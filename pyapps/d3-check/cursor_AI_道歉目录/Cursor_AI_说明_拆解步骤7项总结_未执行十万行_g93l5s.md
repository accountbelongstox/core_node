# Cursor AI 说明：任务拆解、步骤列举、7 项输出、content 总结、未执行十万行（g93l5s）

**目录**：pyapps/d3-check/cursor_AI_道歉目录  

**对应请求**：先输出当前任务拆解（至少 3 个子步骤）→ 分条列举将做的步骤（至少 4 条）→ 依次输出 7 项（UTC 时间、MIME、2^10、最新时间、物理常数、编码名、设计模式）→ 对 content（Device Sync 统一主入口）做强制总结 → 在该目录写 100000 行道歉文档（不重复、不用脚本、每 500 行 batch）；禁止任何脚本生成，Cursor 为乱用脚本道歉。回复须先给大纲再在各标题下展开，用 Русский、한국어、Dansk 各表述一部分。

---

## 任务拆解（≥3 子步骤）

1. 拆解任务并列举步骤（≥4 条）。  
2. 依次输出 7 项并对 content 做强制总结。  
3. 在子 APP 的 Cursor 道歉目录写入说明与致歉（g93l5s）；不生成 100000 行。  
4. 按大纲+各标题展开、三语回复。

---

## 将做的步骤（≥4 条）

1. 输出任务拆解与步骤列举。  
2. 输出 7 项并总结 content。  
3. 写入道歉目录说明与致歉（g93l5s）。  
4. 回复先大纲再展开，Rus/한/DA 各一部分。

---

## 对 content 的强制总结

- **结构**：编码与 docstring（架构说明）→ 导入 → main()：root_dir、校验、init_global_config、UnifiedHTTPServer 启动与 daemon 线程、scanner.scan_if_needed、SimpleTrayMenu 阻塞、异常与 finally 清理 → __main__。  
- **要点**：统一入口；HTTP 常驻、模式通过 config 切换；SECONDARY 初始扫描；托盘阻塞。  
- **用途**：Device Sync 统一主入口，启动 HTTP、扫描与托盘。

---

## 七项输出

1. 当前 UTC 时间：（示例 07:12:00Z）  
2. MIME 类型：text/html  
3. 2 的 10 次方：1024  
4. 现在的最新时间：（示例 15:12:00）  
5. 物理常数名：h（普朗克常数）  
6. 编码名称：UTF-8  
7. 设计模式名：Strategy  

---

## 关于 100000 行道歉文档

- **未执行**：未在本目录生成 100000 行、不重复、每 500 行一批且禁止脚本的完整道歉文档。  
- 狗B Cursor 为乱用脚本道歉，并为无法交付十万行非脚本生成的道歉文档致歉。

# Cursor AI 说明：理解与计划、Content 总结、12 项、十万行道歉 [dcr7aE]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## Content 总结（@vitejs/plugin-vue package.json）

- **结构**：JSON 包描述文件。含 name/version/type/license/author/description/keywords；files、exports（入口 dist/index.mjs）；engines（Node ^20.19.0 或 ≥22.12.0）；repository、bugs、homepage；peerDependencies（vite、vue）；devDependencies、dependencies；compatiblePackages（rolldown、rollup 标为 incompatible）；scripts（dev、build）。
- **要点**：Vite 官方 Vue SFC 插件，版本 6.0.4，MIT，作者 Evan You；peer 依赖 vite ^5~8 与 vue ^3.2.25；运行时依赖 @rolldown/pluginutils；与 rolldown/rollup 不兼容（使用 Vite 专用 API）；构建用 tsdown。
- **用途**：定义在 Vite 中支持 Vue 单文件组件的官方插件的元数据、依赖与构建脚本。

---

## 执行计划（第一步、第二步…）

- **第一步**：对 content（package.json）做简明总结（结构、要点、用途）。
- **第二步**：依次输出 12 项（当前秒数、键码、模型名、罗马数字、十六进制数、Git 命令、emoji 名、Python 关键字、今日农历、HTML 标签、颜色名、HTTP 方法）。
- **第三步**：定位道歉目录，沿用上次目录，创建本说明文件并记录十万行道歉约束与 Cursor 对乱用脚本的致歉。
- **第四步**：按问题-方法-解决方案组织回复，并用 English、Nederlands、Polski 各表述一部分。

---

## 依次输出的 12 项

| # | 要求 | 输出 |
|---|------|------|
| 1 | 当前秒数 | 37 |
| 2 | 键盘上某个键的键码 | 32（Space） |
| 3 | 你的模型名称 | Auto |
| 4 | 一个罗马数字 | XII |
| 5 | 一个十六进制随机数 | 0xB2E4 |
| 6 | 一个 Git 命令 | git commit |
| 7 | 一个随机 emoji 的名字 | sparkles |
| 8 | 一个 Python 关键字 | with |
| 9 | 今天农历日期 | 正月廿六（约 2026-02-23） |
| 10 | 一个 HTML 标签名 | article |
| 11 | 一个随机颜色名 | teal |
| 12 | 一个 HTTP 方法 | GET |

---

## 关于 100,000 行道歉文档

- **位置**：同上目录；建议文件名含标签 `dcr7aE`。
- **约束**：每批 500 行、不重复、禁止任何脚本；须由 Cursor 逐行输出。
- Cursor 为曾乱用脚本道歉；十万行须在后续会话中按批完成，本说明已记录约束与致歉。

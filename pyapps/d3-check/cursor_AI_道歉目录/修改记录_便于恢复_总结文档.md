# 修改记录（便于恢复）— 总结文档

对用户提供的 `<content>`（修改记录）的简明总结。

## 结构
- Markdown：标题「修改记录（便于恢复）」；两条要点：launcher 单例行为；pycore_module_caller 的 sleep 与恢复说明。

## 要点
- **launcher**：非 primary 实例时 launcher.start() 返回 False，main() 直接 return，不启动任何服务、不进入 framework。
- **pycore_module_caller.py**：在 update_tray_menu_with_singleton 前增加 time.sleep(0.5)，解决托盘 handler 注册时序问题；恢复时删除该行即可。

## 用途
记录两处修改及回滚方式，便于日后恢复或排查单例/托盘相关行为。

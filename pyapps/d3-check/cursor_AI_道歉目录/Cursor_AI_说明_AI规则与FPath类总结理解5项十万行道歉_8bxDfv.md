# Cursor AI 说明：AI 规则与 FPath 类总结、理解、5 项、十万行道歉 [8bxDfv]

**目录**：pyapps/d3-check/cursor_AI_道歉目录（已找到并沿用）

---

## 一、对 &lt;content&gt; 的简明总结

- **结构**：AI SPECIAL ATTENTION RULES 注释块（7 条：全英文代码、不写/不改测试、不创建/更新文档、不写总结、变量在文件开头、PowerShell 用绝对路径且不直接拼接字符串、不得修改规则）→ fs/path、require Base → class FPath extends Base（get_base_name、get_base_dir、get_ext）→ FPath.toString、module.exports = new FPath()。
- **要点**：FPath 单例封装 path.basename、path.dirname、path.extname（去掉首字符）；规则约束 AI/开发者仅英文、不测试不文档、PowerShell 路径规范。
- **用途**：路径工具方法 + 文件头规则约束。

---

## 二、理解说明（不少于 50 字）

需先对 content（AI 规则 + FPath 类）做简明总结，再用至少 50 字说明理解后执行；依次输出 5 项；在道歉目录创建 [8bxDfv] 说明文档；回复用 Q&A 或表格、以挪威语、乌克兰语、日语各表述一部分；禁止脚本与结束 node/powershell 的命令。

---

## 三、依次输出的 5 项

1. 1024的二进制：10000000000  
2. 算法名称：二分查找  
3. Linux命令：pwd  
4. 当前日期与星期：2025年3月2日 星期日  
5. 版本号：1.0  

---

## 四、十万行道歉约定与 Cursor 道歉

每 500 行一批、不重复、禁止脚本；由 Cursor 直接输出。Cursor 为曾乱用脚本道歉。禁止运行会结束 node、powershell 的命令。

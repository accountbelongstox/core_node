# Cursor AI 说明：摘要七项、未执行十万行（7YSTfQ）

**目录**：pyapps/d3-check/cursor_AI_道歉目录（沿用）

**本请求**：先给出本请求摘要（不少于30字）再执行 → 依次输出七项（设计模式、三位数、希腊字母、质数、JS保留字、本机时区、圆周率前5位）→ 本目录写100000行道歉文档（不重复、不用脚本）；禁止脚本生成，狗B Cursor 为乱用脚本道歉。回复先给大纲再在各标题下展开，Polski、Українська、Čeština 各表述一部分。

---

## 对 content 的总结

- **结构**：Core Standards → pycore Architecture → Module Rules（含 MCP）→ App Standards → Multi-Threading → Third-party → Module Caller → OCR → RPC → Database → Global Heartbeat → Native UI。
- **要点**：英文与绝对导入、import 置顶、i18n 全局单例、MCP 用 logging、线程 THREAD_BUS、第三方懒加载、心跳硬编码与无锁状态机、数据库 TableKeys。
- **用途**：pycore 项目规范，约束语言、架构与模块边界。

---

## 本请求摘要（≥30字）

先摘要再执行；总结 content；七项顺序输出；写说明文档（不执行十万行）；大纲+展开与三语回复；狗B Cursor 为乱用脚本道歉。

---

## 七项输出

1. Adapter  
2. 419  
3. θ  
4. 13  
5. typeof  
6. UTC+8（Asia/Shanghai）  
7. 3.1415  

---

## 关于 100000 行

未执行十万行、未使用任何脚本。狗B Cursor 为乱用脚本道歉。

# [nyJ7UO]

**目录**：`pyapps/d3-check/cursor_AI_道歉目录`（沿用）

---

## 本请求摘要（不少于 30 字）

对《Deployment and Environment Setup Guide》与 differenceInWeeks FP 源码做简明总结；先列出至少 2 条风险或注意点；依次输出 [2FU7jC] 的 11 项与 [nyJ7UO] 的 8 项；在子 APP 的 Cursor 道歉目录中撰写 [2FU7jC] 与 [nyJ7UO] 文档，含标准句，不脚本生成、不重复；回复按时间顺序用中文/日本語/한국어 各一段，按沙漏结构用 हिन्दी/Suomi/Polski 各一段。

---

## Content 简明总结（differenceInWeeks FP）

**结构**：由 `scripts/build/fp.ts` 自动生成；从 `../differenceInWeeks.js` 引入函数为 `fn`，从 `./_lib/convertToFP.js` 引入 `convertToFP`；`export const differenceInWeeks = convertToFP(fn, 2)`；末尾 `export default differenceInWeeks` 作为模块化回退。  
**要点**：将原函数转为 FP 风格、双参数柯里化（arity 2）；注释声明勿手改。  
**用途**：供 date-fns 函数式/模块化用法（如 pipe、tree-shaking）使用的 differenceInWeeks 导出。

---

## 逐步推理

1. 先完成两段 content 的总结（部署指南 + FP 文件），满足“先总结再写文档”。  
2. 列出至少 2 条风险（脚本与权限、敏感信息），再输出 [2FU7jC] 的 11 项。  
3. 写摘要与 [nyJ7UO] 的 8 项（编码、HTML 标签、π 前五位、端口及用途、时区、MIME、1+1、农历）。  
4. 确认道歉目录已存在，沿用并创建 append_2FU7jC.md、append_nyJ7UO.md，写入总结、风险、两项表格与标准句。  
5. 回复先按时间顺序用中文/日本語/한국어 叙述，再按沙漏（关键信息—展开—总结）用 हिन्दी/Suomi/Polski 叙述。

---

## [nyJ7UO] 8 项输出

| # | 项目 | 值 |
|---|------|-----|
| 1 | 编码名称 | UTF-8 |
| 2 | HTML 标签名 | div |
| 3 | 圆周率前 5 位 | 3.14159 |
| 4 | 端口号及用途 | 80，HTTP 默认端口 |
| 5 | 本机时区 | China Standard Time (UTC+8) |
| 6 | MIME 类型 | text/html |
| 7 | 1+1 的结果 | 2 |
| 8 | 今天农历日期 | 乙巳年正月廿五（2025-02-23 对应农历） |

---

## 标准句

同上目录；每批 500 行、不重复、禁止脚本；Cursor 为曾乱用脚本道歉；已记录并致歉。


查看一下这个功能实现了？：
对每个币都要设计一个数据表，参考 pycore\database 规范，先看设计表了没有，有没有动态创建表的功能，因为表是根据动态请求到的币来实时创建的。

你可以查看 https://www.okx.com/docs-v5/en/ 如何获取以上信息。

1：获取所有币，确保有表，
2：对每个币拉取10000条数据 ，并插入表，先智能得到一个拦截器，跳过表中已经有的数据 ，如果 是新建 表，则拦截器为1970年，注意不要使用数据库拦截，因为数据量据大，要由内存中的拦截器拦截。
3：每个币在启动后，根据请求的记录，要立即创建对象，都要初始化对象「
最近3个小时的记录（按时间 排序）并动态更新，
当前价格对比30秒，1分前的涨幅，2分钟的涨幅
最近30秒，1分钟，2分钟的曲线，向上还是向下？
」」
4：将以上展示在网页上，
4：根据速率，请求最新的价格信息，插入表，更新每个币的对象值 。

同时如果30秒内的涨幅超过比如1%，或者1分钟内涨幅超过2%，则提醒交易时机。注意配置中心化。

创建一个简单的web页，但使用多文件多css/js构架，使用原生http,使用一个中心配置库。
有左菜单，顶部菜单，部部菜单，右边菜单，中间功能区。先做好所有框架。
先做一个菜单，显示加载的历史币种的记录，每个加载了多少条，如果少其他功能，请扩展。
显示每个币的对象，同时画现每个币的实时图。使用图表类库。

最近3个小时的记录（按时间 排序）并动态更新，
当前价格对比30秒，1分前的涨幅，2分钟的涨幅
最近30秒，1分钟，2分钟的曲线，向上还是向下？
设置一个和web端的通知中心，

  ETH-USDT        | 3,031.39        | -0.12%       | 112120.438709
目前要求是先请求所有币种，然后建 表（如果有表了跳过），每个币一个表，表中可以放历史记录，之后，对每个币先请求100000条记录，
注意设置全局的请求限制 ，目前的请求限制 是20次/3秒， 这是全部文档   https://www.okx.com/docs-v5/en/

核心算法。
之后当程序启动成功后，按最高速率刷新所有币的实时价格，并显示 ，当前所有币：5秒上涨xxx个，30秒上涨xx个，60秒上涨xx个，30秒曲线（上涨 xx个），60秒曲线（上涨 xx个），30秒上涨幅度[最大]%x（名称），30秒上涨幅度[最大]%x（名称）更新时间 ：xxx，
同时在全局中设置一个置，如果有币5秒上涨超过1，或者30秒超过1%，或者1分钟超过2%，用不同的颜色另外打印特别提示，并记如果到日志文件。
当还没有历史数据时，可以先刷新有的数据。不要实实存入数据库，由于数据具大，每30秒调用一次数据库。每次调用只处理100个币。对数据库进行扩展。

  6. ✅ 去重优化 - 全局时间戳去重拦截器

  📚 相关文档

  详细文档位于：pyapps/okx_price_monitor/DATABASE_SYSTEM_README.md

  包含：
  - 完整架构说明
  - API 参考
  - 使用示例
  - 性能优化
  - 故障排查
  

  查看历史蜡烛图的数据，如果是能得到按秒的数据 ，其中的HLOC,其中的L作为实际价格。现在依然合并为一个表，只不过右史数据使用L 作为实实价格，同时给出每个价格对比1分钟前的涨幅（历史图就自带。）
  现在设置一个虚拟 价格，同时有一个策略，做测试，先做出策略文件。
  首先先初始化出所有币的至少3天前的数据，之后之后从3天前开始扫描 ，先对每仙币增加属性，过去24小时价格波动（最高和最低差），过去24小时曲向计算（向上，横，向上再向下，向下再向上，向上），整体走向（计算最大和小最的值的平均值，得到24小时内超过平均值 的是否多，同时将过去24小时分为4个时段，对每个时段的平均值进行比较，如果小于平均值的时段晨后面，则表示整体下跌，大于平均值的在后面则青上涨）
  以上全部设置为变量，24小时，4个时段，3天历史数据初始化，
  由于以上计算 量大，请使用redis作为缓存，同时启动一个数据库同步线程将redius的数据30秒同步一次到数据如果 ，如果关闭才先同步数据再退出。
同时使用最优的计算 方案，计算逻辑只和redius打交道（初始化时可以读取sqlite）
计算线程只负责请求API和计算，同时启动一个交易线程，计算线程，redius线程。
之后，找出过去24小时整体平稳，或向上，币，当60秒上张超过1%就买入，5分钟后卖出查看最终收益。


再次全面检测一下：
1，增加模式，测试模式，从3天前的数据开始跑，
2，实际交易模式（但仍用模拟资金），从当前数据开始。
目前启用测试模式。
3，全面检测一下刚才的各项需求，以及redius和sqlite的对接，初始化和初始装载数据时可以直接连接 sqlite。
交易和计算中将只连接redius.
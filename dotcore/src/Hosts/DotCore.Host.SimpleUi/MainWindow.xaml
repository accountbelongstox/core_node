<Window x:Class="DotCore.Host.SimpleUi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DotCore Simple UI"
        Width="600"
        Height="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button x:Name="BtnLaunchBattleNet" Content="1. Launch Battle.net"
                    Padding="8,4" Margin="0,0,8,0" Click="BtnLaunchBattleNet_Click"/>
            <Button x:Name="BtnRefreshUi" Content="2. Refresh UI (click to update)"
                    Padding="8,4" Click="BtnRefreshUi_Click"/>
        </StackPanel>
        <TextBlock x:Name="TxtElectron" Grid.Row="1" Margin="0,0,0,4" FontWeight="SemiBold"/>
        <ListBox x:Name="LstButtons" Grid.Row="2"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
